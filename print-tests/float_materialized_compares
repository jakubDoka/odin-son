=========== OPT LEVEL: none ===========
f32c:
    xor ecx, ecx
    ucomiss xmm0, xmm1
    setz al
    movzx rax, al
    movzx eax, al
    add rcx, rax
    ucomiss xmm0, xmm1
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    ucomiss xmm1, xmm0
    setnbe al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    ucomiss xmm1, xmm0
    setnc al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    ucomiss xmm0, xmm1
    setnbe cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    ucomiss xmm0, xmm1
    setnc cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
3:
    xor ecx, ecx
    ucomisd xmm0, xmm1
    setz al
    movzx rax, al
    movzx eax, al
    add rcx, rax
    ucomisd xmm0, xmm1
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    ucomisd xmm1, xmm0
    setnbe al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    ucomisd xmm1, xmm0
    setnc al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    ucomisd xmm0, xmm1
    setnbe cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    ucomisd xmm0, xmm1
    setnc cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
4:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    movss.d xmm0, [rip + 0xbf]
    movss.d xmm1, [rip + 0xbc]
    call f32c
    mov r14, rax
    movss.d xmm1, [rip + 0xab]
    movss.d xmm0, [rip + 0xa2]
    call f32c
    mov r13, rax
    movss.d xmm0, [rip + 0x97]
    movss.d xmm1, [rip + 0x88]
    call f32c
    mov r12, rax
    movsd.q xmm0, [rip + 0x83]
    movsd.q xmm1, [rip + 0x84]
    call 3
    mov rbp, rax
    movsd.q xmm1, [rip + 0x73]
    movsd.q xmm0, [rip + 0x6a]
    call 3
    mov rbx, rax
    movsd.q xmm0, [rip + 0x63]
    movsd.q xmm1, [rip + 0x50]
    call 3
    mov rsi, rax
    mov rcx, r14
    add rcx, r13
    add rcx, r12
    xor eax, eax
    add rax, rcx
    mov rcx, rbp
    add rcx, rbx
    add rcx, rsi
    add rax, rcx
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
f32c:
    xor ecx, ecx
    ucomiss xmm0, xmm1
    setz al
    movzx rax, al
    movzx eax, al
    add rcx, rax
    ucomiss xmm0, xmm1
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    ucomiss xmm1, xmm0
    setnbe al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    ucomiss xmm1, xmm0
    setnc al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    ucomiss xmm0, xmm1
    setnbe cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    ucomiss xmm0, xmm1
    setnc cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
3:
    xor ecx, ecx
    ucomisd xmm0, xmm1
    setz al
    movzx rax, al
    movzx eax, al
    add rcx, rax
    ucomisd xmm0, xmm1
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    ucomisd xmm1, xmm0
    setnbe al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    ucomisd xmm1, xmm0
    setnc al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    ucomisd xmm0, xmm1
    setnbe cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    ucomisd xmm0, xmm1
    setnc cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
4:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    movss.d xmm0, [rip + 0xbf]
    movss.d xmm1, [rip + 0xbc]
    call f32c
    mov r14, rax
    movss.d xmm1, [rip + 0xab]
    movss.d xmm0, [rip + 0xa2]
    call f32c
    mov r13, rax
    movss.d xmm0, [rip + 0x97]
    movss.d xmm1, [rip + 0x88]
    call f32c
    mov r12, rax
    movsd.q xmm0, [rip + 0x83]
    movsd.q xmm1, [rip + 0x84]
    call 3
    mov rbp, rax
    movsd.q xmm1, [rip + 0x73]
    movsd.q xmm0, [rip + 0x6a]
    call 3
    mov rbx, rax
    movsd.q xmm0, [rip + 0x63]
    movsd.q xmm1, [rip + 0x50]
    call 3
    mov rsi, rax
    mov rcx, r14
    add rcx, r13
    add rcx, r12
    xor eax, eax
    add rax, rcx
    mov rcx, rbp
    add rcx, rbx
    add rcx, rsi
    add rax, rcx
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
f32c:
    ucomiss xmm0, xmm1
    setz al
    movzx rax, al
    movzx ecx, al
    ucomiss xmm0, xmm1
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    ucomiss xmm1, xmm0
    setnbe al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    ucomiss xmm1, xmm0
    setnc al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    ucomiss xmm0, xmm1
    setnbe cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    ucomiss xmm0, xmm1
    setnc cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
3:
    ucomisd xmm0, xmm1
    setz al
    movzx rax, al
    movzx ecx, al
    ucomisd xmm0, xmm1
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    ucomisd xmm1, xmm0
    setnbe al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    ucomisd xmm1, xmm0
    setnc al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    ucomisd xmm0, xmm1
    setnbe cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    ucomisd xmm0, xmm1
    setnc cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
4:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    movss.d xmm0, [rip + 0xbf]
    movss.d xmm1, [rip + 0xbc]
    call f32c
    mov r14, rax
    movss.d xmm1, [rip + 0xab]
    movss.d xmm0, [rip + 0xa2]
    call f32c
    mov r13, rax
    movss.d xmm0, [rip + 0x97]
    movss.d xmm1, [rip + 0x88]
    call f32c
    mov r12, rax
    movsd.q xmm0, [rip + 0x83]
    movsd.q xmm1, [rip + 0x84]
    call 3
    mov rbp, rax
    movsd.q xmm1, [rip + 0x73]
    movsd.q xmm0, [rip + 0x6a]
    call 3
    mov rbx, rax
    movsd.q xmm0, [rip + 0x63]
    movsd.q xmm1, [rip + 0x50]
    call 3
    mov rsi, rax
    mov rcx, r14
    add rcx, r13
    add rcx, r12
    mov rax, rbp
    add rax, rbx
    add rax, rsi
    add rcx, rax
    mov rax, rcx
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: all ===========
f32c:
    ucomiss xmm0, xmm1
    setz al
    movzx rax, al
    movzx ecx, al
    ucomiss xmm0, xmm1
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    ucomiss xmm1, xmm0
    setnbe al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    ucomiss xmm1, xmm0
    setnc al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    ucomiss xmm0, xmm1
    setnbe cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    ucomiss xmm0, xmm1
    setnc cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
3:
    ucomisd xmm0, xmm1
    setz al
    movzx rax, al
    movzx ecx, al
    ucomisd xmm0, xmm1
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    ucomisd xmm1, xmm0
    setnbe al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    ucomisd xmm1, xmm0
    setnc al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    ucomisd xmm0, xmm1
    setnbe cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    ucomisd xmm0, xmm1
    setnc cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
4:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    movss.d xmm0, [rip + 0xbf]
    movss.d xmm1, [rip + 0xbc]
    call f32c
    mov r14, rax
    movss.d xmm1, [rip + 0xab]
    movss.d xmm0, [rip + 0xa2]
    call f32c
    mov r13, rax
    movss.d xmm0, [rip + 0x97]
    movss.d xmm1, [rip + 0x88]
    call f32c
    mov r12, rax
    movsd.q xmm0, [rip + 0x83]
    movsd.q xmm1, [rip + 0x84]
    call 3
    mov rbp, rax
    movsd.q xmm1, [rip + 0x73]
    movsd.q xmm0, [rip + 0x6a]
    call 3
    mov rbx, rax
    movsd.q xmm0, [rip + 0x63]
    movsd.q xmm1, [rip + 0x50]
    call 3
    mov rsi, rax
    mov rcx, r14
    add rcx, r13
    add rcx, r12
    mov rax, rbp
    add rax, rbx
    add rax, rsi
    add rcx, rax
    mov rax, rcx
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
