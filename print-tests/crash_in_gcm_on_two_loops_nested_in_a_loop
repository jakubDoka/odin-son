=========== OPT LEVEL: none ===========
main:
    xor eax, eax
    xor r8d, r8d
1:
    cmp r8, 4
    jnl 6
    xor edx, edx
    xor esi, esi
2:
    cmp rdx, 4
    jnl 3
    mov rcx, rdx
    add rcx, 1
    add rsi, rdx
    mov rdx, rcx
    jmp 2
3:
    xor edx, edx
    xor edi, edi
4:
    cmp rdx, 4
    jnl 5
    mov rcx, rdx
    add rcx, 1
    lea rdi, [rdi + rdx*2]
    mov rdx, rcx
    jmp 4
5:
    add rsi, rdi
    add r8, 1
    add rax, rsi
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: mininal ===========
main:
    xor eax, eax
    xor r8d, r8d
1:
    cmp r8, 4
    jnl 6
    xor edx, edx
    xor esi, esi
2:
    cmp rdx, 4
    jnl 3
    mov rcx, rdx
    add rcx, 1
    add rsi, rdx
    mov rdx, rcx
    jmp 2
3:
    xor edx, edx
    xor edi, edi
4:
    cmp rdx, 4
    jnl 5
    mov rcx, rdx
    add rcx, 1
    lea rdi, [rdi + rdx*2]
    mov rdx, rcx
    jmp 4
5:
    add rsi, rdi
    add r8, 1
    add rax, rsi
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: moderate ===========
main:
    xor eax, eax
    xor r8d, r8d
1:
    cmp r8, 4
    jnl 6
    xor edx, edx
    xor esi, esi
2:
    cmp rdx, 4
    jnl 3
    mov rcx, rdx
    add rcx, 1
    add rsi, rdx
    mov rdx, rcx
    jmp 2
3:
    xor edx, edx
    xor edi, edi
4:
    cmp rdx, 4
    jnl 5
    mov rcx, rdx
    add rcx, 1
    lea rdi, [rdi + rdx*2]
    mov rdx, rcx
    jmp 4
5:
    add rsi, rdi
    add r8, 1
    add rax, rsi
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: all ===========
main:
    xor eax, eax
    xor r8d, r8d
1:
    cmp r8, 4
    jnl 6
    xor edx, edx
    xor esi, esi
2:
    cmp rdx, 4
    jnl 3
    mov rcx, rdx
    add rcx, 1
    add rsi, rdx
    mov rdx, rcx
    jmp 2
3:
    xor edx, edx
    xor edi, edi
4:
    cmp rdx, 4
    jnl 5
    mov rcx, rdx
    add rcx, 1
    lea rdi, [rdi + rdx*2]
    mov rdx, rcx
    jmp 4
5:
    add rsi, rdi
    add r8, 1
    add rax, rsi
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: aggresive ===========
