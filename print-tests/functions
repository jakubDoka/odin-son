=========== OPT LEVEL: none ===========
main:
     sub rsp, 0x08
     mov rdi, 0x0A
     call :fib
     add rsp, 0x08
     ret
fib:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp rbx, 0x02
     jle :0
     mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib
     add rbp, rax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: mininal ===========
main:
     sub rsp, 0x08
     mov rdi, 0x0A
     call :fib
     add rsp, 0x08
     ret
fib:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp rbx, 0x02
     jle :0
     mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib
     add rbp, rax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: moderate ===========
main:
     sub rsp, 0x08
     mov rdi, 0x0A
     call :fib
     add rsp, 0x08
     ret
fib:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp rbx, 0x02
     jle :0
     mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib
     add rbp, rax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: all ===========
main:
     sub rsp, 0x08
     mov rdi, 0x0A
     call :fib
     add rsp, 0x08
     ret
fib:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp rbx, 0x02
     jle :0
     mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib
     add rbp, rax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: aggresive ===========
main:
     sub rsp, 0x08
     mov rdi, 0x0A
     call :fib
     add rsp, 0x08
     ret
fib:
     push rbx
     push rbp
     push r12
     mov rbx, rdi
     cmp rbx, 0x02
     jle :0
     mov rax, rbx
     sub rax, 0x01
     mov rbp, rax
     cmp rbp, 0x02
     jle :1
     mov rax, rbp
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov r12, rax
     mov rax, rbp
     sub rax, 0x02
     mov rdi, rax
     call :fib
     add r12, rax
     jmp :2

001: mov r12, 0x01
002: mov rbp, r12
     mov rax, rbx
     sub rax, 0x02
     mov rbx, rax
     cmp rbx, 0x02
     jle :3
     mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov r12, rax
     mov rax, rbx
     sub rax, 0x02
     mov rdi, rax
     call :fib
     add r12, rax
     jmp :4

003: mov r12, 0x01
004: mov rax, r12
     mov rcx, rbp
     add rcx, rax
     jmp :5

000: mov rcx, 0x01
005: mov rax, rcx
     pop r12
     pop rbp
     pop rbx
     ret
