=========== OPT LEVEL: none ===========
f32c:
    xor eax, eax
    ucomiss xmm0, xmm1
    setz cl
    movzx rcx, cl
    movzx ecx, cl
    add rax, rcx
    ucomiss xmm0, xmm1
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    ucomiss xmm1, xmm0
    setnbe cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    ucomiss xmm0, xmm1
    setnbe cl
    movzx rcx, cl
    ucomiss xmm1, xmm0
    setnc dl
    movzx rdx, dl
    movzx edx, dl
    lea rax, [rax + rdx*8]
    ucomiss xmm0, xmm1
    setnc dl
    movzx rdx, dl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    movzx ecx, dl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
3:
    xor eax, eax
    ucomisd xmm0, xmm1
    setz cl
    movzx rcx, cl
    movzx ecx, cl
    add rax, rcx
    ucomisd xmm0, xmm1
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    ucomisd xmm1, xmm0
    setnbe cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    ucomisd xmm0, xmm1
    setnbe cl
    movzx rcx, cl
    ucomisd xmm1, xmm0
    setnc dl
    movzx rdx, dl
    movzx edx, dl
    lea rax, [rax + rdx*8]
    ucomisd xmm0, xmm1
    setnc dl
    movzx rdx, dl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    movzx ecx, dl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
4:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    movss.d xmm0, [rip + 0xbf]
    movss.d xmm1, [rip + 0xbc]
    call f32c
    mov rbx, rax
    movss.d xmm1, [rip + 0xab]
    movss.d xmm0, [rip + 0xa2]
    call f32c
    mov rbp, rax
    movss.d xmm0, [rip + 0x97]
    movss.d xmm1, [rip + 0x88]
    call f32c
    mov r12, rax
    movsd.q xmm0, [rip + 0x83]
    movsd.q xmm1, [rip + 0x84]
    call 3
    mov r13, rax
    movsd.q xmm1, [rip + 0x73]
    movsd.q xmm0, [rip + 0x6a]
    call 3
    mov r14, rax
    movsd.q xmm0, [rip + 0x63]
    movsd.q xmm1, [rip + 0x50]
    call 3
    mov rdx, rbx
    add rdx, rbp
    mov rsi, r13
    add rsi, r14
    add rdx, r12
    xor ecx, ecx
    add rcx, rdx
    add rsi, rax
    add rcx, rsi
    mov rax, rcx
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
f32c:
    xor eax, eax
    ucomiss xmm0, xmm1
    setz cl
    movzx rcx, cl
    movzx ecx, cl
    add rax, rcx
    ucomiss xmm0, xmm1
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    ucomiss xmm1, xmm0
    setnbe cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    ucomiss xmm0, xmm1
    setnbe cl
    movzx rcx, cl
    ucomiss xmm1, xmm0
    setnc dl
    movzx rdx, dl
    movzx edx, dl
    lea rax, [rax + rdx*8]
    ucomiss xmm0, xmm1
    setnc dl
    movzx rdx, dl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    movzx ecx, dl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
3:
    xor eax, eax
    ucomisd xmm0, xmm1
    setz cl
    movzx rcx, cl
    movzx ecx, cl
    add rax, rcx
    ucomisd xmm0, xmm1
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    ucomisd xmm1, xmm0
    setnbe cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    ucomisd xmm0, xmm1
    setnbe cl
    movzx rcx, cl
    ucomisd xmm1, xmm0
    setnc dl
    movzx rdx, dl
    movzx edx, dl
    lea rax, [rax + rdx*8]
    ucomisd xmm0, xmm1
    setnc dl
    movzx rdx, dl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    movzx ecx, dl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
4:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    movss.d xmm0, [rip + 0xbf]
    movss.d xmm1, [rip + 0xbc]
    call f32c
    mov rbx, rax
    movss.d xmm1, [rip + 0xab]
    movss.d xmm0, [rip + 0xa2]
    call f32c
    mov rbp, rax
    movss.d xmm0, [rip + 0x97]
    movss.d xmm1, [rip + 0x88]
    call f32c
    mov r12, rax
    movsd.q xmm0, [rip + 0x83]
    movsd.q xmm1, [rip + 0x84]
    call 3
    mov r13, rax
    movsd.q xmm1, [rip + 0x73]
    movsd.q xmm0, [rip + 0x6a]
    call 3
    mov r14, rax
    movsd.q xmm0, [rip + 0x63]
    movsd.q xmm1, [rip + 0x50]
    call 3
    mov rdx, rbx
    add rdx, rbp
    mov rsi, r13
    add rsi, r14
    add rdx, r12
    xor ecx, ecx
    add rcx, rdx
    add rsi, rax
    add rcx, rsi
    mov rax, rcx
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
f32c:
    ucomiss xmm0, xmm1
    setz al
    movzx rax, al
    movzx eax, al
    ucomiss xmm0, xmm1
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    ucomiss xmm1, xmm0
    setnbe cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    ucomiss xmm0, xmm1
    setnbe cl
    movzx rcx, cl
    ucomiss xmm1, xmm0
    setnc dl
    movzx rdx, dl
    movzx edx, dl
    lea rax, [rax + rdx*8]
    ucomiss xmm0, xmm1
    setnc dl
    movzx rdx, dl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    movzx ecx, dl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
3:
    ucomisd xmm0, xmm1
    setz al
    movzx rax, al
    movzx eax, al
    ucomisd xmm0, xmm1
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    ucomisd xmm1, xmm0
    setnbe cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    ucomisd xmm0, xmm1
    setnbe cl
    movzx rcx, cl
    ucomisd xmm1, xmm0
    setnc dl
    movzx rdx, dl
    movzx edx, dl
    lea rax, [rax + rdx*8]
    ucomisd xmm0, xmm1
    setnc dl
    movzx rdx, dl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    movzx ecx, dl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
4:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    movss.d xmm0, [rip + 0xb7]
    movss.d xmm1, [rip + 0xb4]
    call f32c
    mov rbx, rax
    movss.d xmm1, [rip + 0xa3]
    movss.d xmm0, [rip + 0x9a]
    call f32c
    mov rbp, rax
    movss.d xmm0, [rip + 0x8f]
    movss.d xmm1, [rip + 0x80]
    call f32c
    mov r12, rax
    movsd.q xmm0, [rip + 0x7b]
    movsd.q xmm1, [rip + 0x7c]
    call 3
    mov r13, rax
    movsd.q xmm1, [rip + 0x6b]
    movsd.q xmm0, [rip + 0x62]
    call 3
    mov r14, rax
    movsd.q xmm0, [rip + 0x5b]
    movsd.q xmm1, [rip + 0x48]
    call 3
    mov rsi, r13
    add rsi, r14
    mov rdx, rbx
    add rdx, rbp
    add rdx, r12
    add rsi, rax
    add rdx, rsi
    mov rax, rdx
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: all ===========
f32c:
    ucomiss xmm0, xmm1
    setz al
    movzx rax, al
    movzx eax, al
    ucomiss xmm0, xmm1
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    ucomiss xmm1, xmm0
    setnbe cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    ucomiss xmm0, xmm1
    setnbe cl
    movzx rcx, cl
    ucomiss xmm1, xmm0
    setnc dl
    movzx rdx, dl
    movzx edx, dl
    lea rax, [rax + rdx*8]
    ucomiss xmm0, xmm1
    setnc dl
    movzx rdx, dl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    movzx ecx, dl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
3:
    ucomisd xmm0, xmm1
    setz al
    movzx rax, al
    movzx eax, al
    ucomisd xmm0, xmm1
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    ucomisd xmm1, xmm0
    setnbe cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    ucomisd xmm0, xmm1
    setnbe cl
    movzx rcx, cl
    ucomisd xmm1, xmm0
    setnc dl
    movzx rdx, dl
    movzx edx, dl
    lea rax, [rax + rdx*8]
    ucomisd xmm0, xmm1
    setnc dl
    movzx rdx, dl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    movzx ecx, dl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
4:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    movss.d xmm0, [rip + 0xb7]
    movss.d xmm1, [rip + 0xb4]
    call f32c
    mov rbx, rax
    movss.d xmm1, [rip + 0xa3]
    movss.d xmm0, [rip + 0x9a]
    call f32c
    mov rbp, rax
    movss.d xmm0, [rip + 0x8f]
    movss.d xmm1, [rip + 0x80]
    call f32c
    mov r12, rax
    movsd.q xmm0, [rip + 0x7b]
    movsd.q xmm1, [rip + 0x7c]
    call 3
    mov r13, rax
    movsd.q xmm1, [rip + 0x6b]
    movsd.q xmm0, [rip + 0x62]
    call 3
    mov r14, rax
    movsd.q xmm0, [rip + 0x5b]
    movsd.q xmm1, [rip + 0x48]
    call 3
    mov rsi, r13
    add rsi, r14
    mov rdx, rbx
    add rdx, rbp
    add rdx, r12
    add rsi, rax
    add rdx, rsi
    mov rax, rdx
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
