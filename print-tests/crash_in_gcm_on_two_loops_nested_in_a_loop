=========== OPT LEVEL: none ===========
main:
     xor eax, eax
     xor edi, edi
005: cmp rdi, 0x04
     jnl :0
     xor ecx, ecx
     xor esi, esi
002: cmp rcx, 0x04
     jnl :1
     add rsi, rcx
     add rcx, 0x01
     jmp :2

001: xor ecx, ecx
     xor edx, edx
004: cmp rdx, 0x04
     jnl :3
     lea rcx, [rcx+rdx*2]
     add rdx, 0x01
     jmp :4

003: add rsi, rcx
     add rsi, rax
     add rdi, 0x01
     mov rax, rsi
     jmp :5

000: mov rcx, 0xFB
     cqo
     idiv rcx
     mov rax, rdx
     ret
=========== OPT LEVEL: mininal ===========
main:
     xor eax, eax
     xor edi, edi
005: cmp rdi, 0x04
     jnl :0
     xor ecx, ecx
     xor esi, esi
002: cmp rcx, 0x04
     jnl :1
     add rsi, rcx
     add rcx, 0x01
     jmp :2

001: xor ecx, ecx
     xor edx, edx
004: cmp rdx, 0x04
     jnl :3
     lea rcx, [rcx+rdx*2]
     add rdx, 0x01
     jmp :4

003: add rsi, rcx
     add rsi, rax
     add rdi, 0x01
     mov rax, rsi
     jmp :5

000: mov rcx, 0xFB
     cqo
     idiv rcx
     mov rax, rdx
     ret
=========== OPT LEVEL: moderate ===========
main:
     xor eax, eax
     xor edi, edi
005: cmp rdi, 0x04
     jnl :0
     xor ecx, ecx
     xor esi, esi
002: cmp rcx, 0x04
     jnl :1
     add rsi, rcx
     add rcx, 0x01
     jmp :2

001: xor ecx, ecx
     xor edx, edx
004: cmp rdx, 0x04
     jnl :3
     lea rcx, [rcx+rdx*2]
     add rdx, 0x01
     jmp :4

003: add rsi, rcx
     add rsi, rax
     add rdi, 0x01
     mov rax, rsi
     jmp :5

000: mov rcx, 0xFB
     cqo
     idiv rcx
     mov rax, rdx
     ret
=========== OPT LEVEL: all ===========
main:
     xor eax, eax
     xor edi, edi
005: cmp rdi, 0x04
     jnl :0
     xor ecx, ecx
     xor esi, esi
002: cmp rcx, 0x04
     jnl :1
     add rsi, rcx
     add rcx, 0x01
     jmp :2

001: xor ecx, ecx
     xor edx, edx
004: cmp rdx, 0x04
     jnl :3
     lea rcx, [rcx+rdx*2]
     add rdx, 0x01
     jmp :4

003: add rsi, rcx
     add rsi, rax
     add rdi, 0x01
     mov rax, rsi
     jmp :5

000: mov rcx, 0xFB
     cqo
     idiv rcx
     mov rax, rdx
     ret
=========== OPT LEVEL: aggresive ===========
