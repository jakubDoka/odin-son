=========== OPT LEVEL: all ===========
main:
    sub sp, sp, #32
    stp x29, x30, [xzr, #7]
    movz x1, #10, #0
    add x0, sp, #24
    str x1, [x0]
    movz x1, #6, #0
    add x0, sp, #16
    str x1, [x0]
    movz x1, #5, #0
    add x0, sp, #24
    bl or_into
    movz x1, #3, #0
    add x0, sp, #16
    bl xor_into
    add x0, sp, #24
    ldr x1, [x0]
    movz x0, #8, #0
    add x1, x1, x0
    add x0, sp, #16
    ldr x0, [x0]
    add x0, x1, x0
    ldp x29, x30, [xzr, #7]
    add sp, sp, #32
    ret
or_into:
    mov x2, x0
    ldr x0, [x2]
    orr x0, x0, x1
    str x0, [x2]
    ret
xor_into:
    mov x2, x0
    ldr x0, [x2]
    eor x0, x0, x1
    str x0, [x2]
    ret
