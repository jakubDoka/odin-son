=========== OPT LEVEL: none ===========
opaque:
    mov rax, rdi
    ret
2:
    sub rsp, 0x18
    xor esi, esi
    lea rdi, [rsp]
    mov rdx, 0x10
    call.q [rip - 0x28]
    mov rdi, 3
    call opaque
    mov rdi, 4
    mov.q [rsp], rax
    call opaque
    mov rdi, 1
    mov.q [rsp + 0x8], rax
    call opaque
    cmp rax, 1
    jz 3
    jmp 4
3:
    mov rdi, 0xa
    call opaque
    mov.q [rsp], rax
4:
    mov.q rdi, [rsp]
    call opaque
    mov rcx, rax
    mov.q rax, [rsp]
    add.q rax, [rsp + 0x8]
    add rax, rcx
    add rsp, 0x18
    ret
=========== OPT LEVEL: mininal ===========
opaque:
    mov rax, rdi
    ret
2:
    sub rsp, 0x18
    xor esi, esi
    lea rdi, [rsp]
    mov rdx, 0x10
    call.q [rip - 0x28]
    mov rdi, 3
    call opaque
    mov rdi, 4
    mov.q [rsp], rax
    call opaque
    mov rdi, 1
    mov.q [rsp + 0x8], rax
    call opaque
    cmp rax, 1
    jz 3
    jmp 4
3:
    mov rdi, 0xa
    call opaque
    mov.q [rsp], rax
4:
    mov.q rdi, [rsp]
    call opaque
    mov rcx, rax
    mov.q rax, [rsp]
    add.q rax, [rsp + 0x8]
    add rax, rcx
    add rsp, 0x18
    ret
=========== OPT LEVEL: moderate ===========
opaque:
    mov rax, rdi
    ret
2:
    sub rsp, 0x18
    mov rdi, 3
    pxor xmm0, xmm0
    movups.x [rsp], xmm0
    call opaque
    mov rdi, 4
    mov.q [rsp], rax
    call opaque
    mov rdi, 1
    mov.q [rsp + 0x8], rax
    call opaque
    cmp rax, 1
    jz 3
    jmp 4
3:
    mov rdi, 0xa
    call opaque
    mov.q [rsp], rax
4:
    mov.q rdi, [rsp]
    call opaque
    mov rcx, rax
    mov.q rax, [rsp]
    add.q rax, [rsp + 0x8]
    add rax, rcx
    add rsp, 0x18
    ret
=========== OPT LEVEL: all ===========
opaque:
    mov rax, rdi
    ret
2:
    sub rsp, 0x18
    mov rdi, 3
    pxor xmm0, xmm0
    movups.x [rsp], xmm0
    call opaque
    mov rdi, 4
    mov.q [rsp], rax
    call opaque
    mov rdi, 1
    mov.q [rsp + 0x8], rax
    call opaque
    cmp rax, 1
    jz 3
    jmp 4
3:
    mov rdi, 0xa
    call opaque
    mov.q [rsp], rax
4:
    mov.q rdi, [rsp]
    call opaque
    mov rcx, rax
    mov.q rax, [rsp]
    add.q rax, [rsp + 0x8]
    add rax, rcx
    add rsp, 0x18
    ret
=========== OPT LEVEL: aggresive ===========
opaque:
    mov rax, 0x18
    ret
