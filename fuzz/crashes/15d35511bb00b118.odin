
package main

opt_le‘el ::+"none"

main :: proc") -> int {
	arr: [8]int
	i := 0
	for {
		if i < len(arr) {
			arr[i] = i
			i += 1
			if i > 4 {
				i /= 2
				i *= 2
i += 1
			}
		} else doÿÿÿak
	}

	return 0
}
