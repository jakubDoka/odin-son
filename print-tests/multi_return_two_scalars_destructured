=========== OPT LEVEL: none ===========
divmod:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    cqo
    idiv rsi
    mov.q [rdi], rax
    mov rax, rcx
    cqo
    idiv rsi
    mov rax, rdx
    ret
main:
    sub rsp, 8
    mov rdi, 0x2f
    mov rsi, 5
    lea rdx, [rsp]
    call divmod
    mov rcx, rax
    mov.q rax, [rsp]
    imul rax, rax, 0x64
    add rax, rcx
    add rsp, 8
    ret
=========== OPT LEVEL: mininal ===========
divmod:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    cqo
    idiv rsi
    mov.q [rdi], rax
    mov rax, rcx
    cqo
    idiv rsi
    mov rax, rdx
    ret
main:
    sub rsp, 8
    mov rdi, 0x2f
    mov rsi, 5
    lea rdx, [rsp]
    call divmod
    mov rcx, rax
    mov.q rax, [rsp]
    imul rax, rax, 0x64
    add rax, rcx
    add rsp, 8
    ret
=========== OPT LEVEL: moderate ===========
divmod:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    cqo
    idiv rsi
    mov.q [rdi], rax
    mov rax, rcx
    cqo
    idiv rsi
    mov rax, rdx
    ret
main:
    sub rsp, 8
    mov rdi, 0x2f
    mov rsi, 5
    lea rdx, [rsp]
    call divmod
    mov rcx, rax
    mov.q rax, [rsp]
    imul rax, rax, 0x64
    add rax, rcx
    add rsp, 8
    ret
=========== OPT LEVEL: all ===========
divmod:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    cqo
    idiv rsi
    mov.q [rdi], rax
    mov rax, rcx
    cqo
    idiv rsi
    mov rax, rdx
    ret
main:
    sub rsp, 8
    mov rdi, 0x2f
    mov rsi, 5
    lea rdx, [rsp]
    call divmod
    mov rcx, rax
    mov.q rax, [rsp]
    imul rax, rax, 0x64
    add rax, rcx
    add rsp, 8
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 0x386
    ret
