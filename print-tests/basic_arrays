=========== OPT LEVEL: none ===========
main:
    push rbx
    sub rsp, 0x90
    lea rdi, [rsp + 0x48]
    mov rdx, 0x20
    xor esi, esi
    call.q [rip - 0x26]
    xor edx, edx
1:
    cmp rdx, 0x20
    jnl 2
    mov rax, rdx
    mov rcx, rdx
    add rcx, 1
    mov.b [rsp + rdx + 0x48], al
    mov rdx, rcx
    jmp 1
2:
    xor ecx, ecx
    xor edx, edx
3:
    mov rbx, rcx
    cmp rdx, 0x20
    jnl 4
    movzx.b eax, [rsp + rdx + 0x48]
    add rdx, 1
    mov rcx, rbx
    add rcx, rax
    jmp 3
4:
    lea rdi, [rsp + 0x68]
    mov rdx, 0x20
    xor esi, esi
    call.q [rip - 0x93]
    mov.q [rsp + 0x68], 0x10
    mov.q [rsp + 0x70], 0x19
    mov.q [rsp + 0x78], 0x1f
    mov.q [rsp + 0x80], 0x40
    xor edx, edx
5:
    cmp rdx, 4
    jnl 6
    mov rax, rdx
    add rax, 1
    add.q [rsp + rdx*8 + 0x68], rdx
    mov rdx, rax
    jmp 5
6:
    xor ecx, ecx
7:
    cmp rcx, 4
    jnl 8
    mov rax, rcx
    add rax, 1
    add.q [rsp + rcx*8 + 0x68], 1
    mov rcx, rax
    jmp 7
8:
    mov rax, 1
    xor esi, esi
9:
    cmp rax, 4
    jnl 10
    mov.q rcx, [rsp + rax*8 + 0x68]
    add rcx, 1
    mov rdx, rsi
    add rdx, 1
    add rax, 1
    mov.q [rsp + rsi*8 + 0x68], rcx
    mov rsi, rdx
    jmp 9
10:
    xor edx, edx
11:
    cmp rdx, 4
    jnl 12
    mov rax, rdx
    add rax, 1
    add.q rbx, [rsp + rdx*8 + 0x68]
    mov rdx, rax
    jmp 11
12:
    mov rdx, 0x48
    xor esi, esi
    lea rdi, [rsp]
    call.q [rip - 0x188]
    xor edi, edi
13:
    cmp rdi, 3
    jnl 16
    xor esi, esi
14:
    cmp rsi, 3
    jnl 15
    imul rax, rdi, 0x18
    mov rdx, rdi
    imul rdx, rsi
    lea rcx, [rsp]
    add rax, rcx
    mov rcx, rsi
    add rcx, 1
    mov.q [rax + rsi*8], rdx
    mov rsi, rcx
    jmp 14
15:
    add rdi, 1
    jmp 13
16:
    xor edi, edi
17:
    cmp rdi, 3
    jnl 20
    xor esi, esi
18:
    cmp rsi, 3
    jnl 19
    imul rax, rdi, 0x18
    lea rcx, [rsp]
    add rax, rcx
    mov rcx, rsi
    add rcx, 1
    add.q rbx, [rax + rsi*8]
    mov rsi, rcx
    jmp 18
19:
    add rdi, 1
    jmp 17
20:
    mov rax, rbx
    add rsp, 0x90
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
main:
    push rbx
    sub rsp, 0x90
    lea rdi, [rsp + 0x48]
    mov rdx, 0x20
    xor esi, esi
    call.q [rip - 0x26]
    xor edx, edx
1:
    cmp rdx, 0x20
    jnl 2
    mov rax, rdx
    mov rcx, rdx
    add rcx, 1
    mov.b [rsp + rdx + 0x48], al
    mov rdx, rcx
    jmp 1
2:
    xor ecx, ecx
    xor edx, edx
3:
    mov rbx, rcx
    cmp rdx, 0x20
    jnl 4
    movzx.b eax, [rsp + rdx + 0x48]
    add rdx, 1
    mov rcx, rbx
    add rcx, rax
    jmp 3
4:
    lea rdi, [rsp + 0x68]
    mov rdx, 0x20
    xor esi, esi
    call.q [rip - 0x93]
    mov.q [rsp + 0x68], 0x10
    mov.q [rsp + 0x70], 0x19
    mov.q [rsp + 0x78], 0x1f
    mov.q [rsp + 0x80], 0x40
    xor edx, edx
5:
    cmp rdx, 4
    jnl 6
    mov rax, rdx
    add rax, 1
    add.q [rsp + rdx*8 + 0x68], rdx
    mov rdx, rax
    jmp 5
6:
    xor ecx, ecx
7:
    cmp rcx, 4
    jnl 8
    mov rax, rcx
    add rax, 1
    add.q [rsp + rcx*8 + 0x68], 1
    mov rcx, rax
    jmp 7
8:
    mov rax, 1
    xor esi, esi
9:
    cmp rax, 4
    jnl 10
    mov.q rcx, [rsp + rax*8 + 0x68]
    add rcx, 1
    mov rdx, rsi
    add rdx, 1
    add rax, 1
    mov.q [rsp + rsi*8 + 0x68], rcx
    mov rsi, rdx
    jmp 9
10:
    xor edx, edx
11:
    cmp rdx, 4
    jnl 12
    mov rax, rdx
    add rax, 1
    add.q rbx, [rsp + rdx*8 + 0x68]
    mov rdx, rax
    jmp 11
12:
    mov rdx, 0x48
    xor esi, esi
    lea rdi, [rsp]
    call.q [rip - 0x188]
    xor edi, edi
13:
    cmp rdi, 3
    jnl 16
    xor esi, esi
14:
    cmp rsi, 3
    jnl 15
    imul rax, rdi, 0x18
    mov rdx, rdi
    imul rdx, rsi
    lea rcx, [rsp]
    add rax, rcx
    mov rcx, rsi
    add rcx, 1
    mov.q [rax + rsi*8], rdx
    mov rsi, rcx
    jmp 14
15:
    add rdi, 1
    jmp 13
16:
    xor edi, edi
17:
    cmp rdi, 3
    jnl 20
    xor esi, esi
18:
    cmp rsi, 3
    jnl 19
    imul rax, rdi, 0x18
    lea rcx, [rsp]
    add rax, rcx
    mov rcx, rsi
    add rcx, 1
    add.q rbx, [rax + rsi*8]
    mov rsi, rcx
    jmp 18
19:
    add rdi, 1
    jmp 17
20:
    mov rax, rbx
    add rsp, 0x90
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
main:
    push rbx
    sub rsp, 0x90
    pxor xmm0, xmm0
    movups.x [rsp + 0x58], xmm0
    movups.x [rsp + 0x48], xmm0
    xor edx, edx
1:
    cmp rdx, 0x20
    jnl 2
    mov rax, rdx
    mov rcx, rdx
    add rcx, 1
    mov.b [rsp + rdx + 0x48], al
    mov rdx, rcx
    jmp 1
2:
    xor ecx, ecx
    xor edx, edx
3:
    mov rbx, rcx
    cmp rdx, 0x20
    jnl 4
    movzx.b eax, [rsp + rdx + 0x48]
    add rdx, 1
    mov rcx, rbx
    add rcx, rax
    jmp 3
4:
    mov.q [rsp + 0x68], 0x10
    mov.q [rsp + 0x70], 0x19
    mov.q [rsp + 0x78], 0x1f
    mov.q [rsp + 0x80], 0x40
    xor edx, edx
5:
    cmp rdx, 4
    jnl 6
    mov rax, rdx
    add rax, 1
    add.q [rsp + rdx*8 + 0x68], rdx
    mov rdx, rax
    jmp 5
6:
    xor ecx, ecx
7:
    cmp rcx, 4
    jnl 8
    mov rax, rcx
    add rax, 1
    add.q [rsp + rcx*8 + 0x68], 1
    mov rcx, rax
    jmp 7
8:
    mov rax, 1
    xor esi, esi
9:
    cmp rax, 4
    jnl 10
    mov.q rcx, [rsp + rax*8 + 0x68]
    add rcx, 1
    add rax, 1
    mov rdx, rsi
    add rdx, 1
    mov.q [rsp + rsi*8 + 0x68], rcx
    mov rsi, rdx
    jmp 9
10:
    xor edx, edx
11:
    cmp rdx, 4
    jnl 12
    mov rax, rdx
    add rax, 1
    add.q rbx, [rsp + rdx*8 + 0x68]
    mov rdx, rax
    jmp 11
12:
    xor esi, esi
    mov rdx, 0x48
    lea rdi, [rsp]
    call.q [rip - 0x16f]
    xor edi, edi
13:
    cmp rdi, 3
    jnl 16
    xor esi, esi
14:
    cmp rsi, 3
    jnl 15
    imul rax, rdi, 0x18
    mov rdx, rdi
    imul rdx, rsi
    lea rcx, [rsp]
    add rax, rcx
    mov rcx, rsi
    add rcx, 1
    mov.q [rax + rsi*8], rdx
    mov rsi, rcx
    jmp 14
15:
    add rdi, 1
    jmp 13
16:
    xor edi, edi
17:
    cmp rdi, 3
    jnl 20
    xor esi, esi
18:
    cmp rsi, 3
    jnl 19
    imul rax, rdi, 0x18
    lea rcx, [rsp]
    add rax, rcx
    mov rcx, rsi
    add rcx, 1
    add.q rbx, [rax + rsi*8]
    mov rsi, rcx
    jmp 18
19:
    add rdi, 1
    jmp 17
20:
    mov rax, rbx
    add rsp, 0x90
    pop rbx
    ret
=========== OPT LEVEL: all ===========
main:
    push rbx
    sub rsp, 0x90
    pxor xmm0, xmm0
    movups.x [rsp + 0x58], xmm0
    movups.x [rsp + 0x48], xmm0
    xor edx, edx
1:
    mov rcx, rdx
    mov rdx, rcx
    add rdx, 1
    mov rax, rcx
    mov.b [rsp + rcx + 0x48], al
    cmp rdx, 0x20
    jnge 1
    xor ecx, ecx
    xor edx, edx
2:
    mov rax, rdx
    mov rdx, rax
    add rdx, 1
    movzx.b eax, [rsp + rax + 0x48]
    add rcx, rax
    mov rdi, rcx
    cmp rdx, 0x20
    mov rcx, rdi
    jnge 2
    mov.q [rsp + 0x68], 0x10
    mov.q [rsp + 0x70], 0x19
    mov.q [rsp + 0x78], 0x1f
    mov.q [rsp + 0x80], 0x40
    xor edx, edx
3:
    mov rcx, rdx
    mov rdx, rcx
    add rdx, 1
    add.q [rsp + rcx*8 + 0x68], rcx
    cmp rdx, 4
    jnge 3
    xor ecx, ecx
4:
    mov rax, rcx
    mov rcx, rax
    add rcx, 1
    add.q [rsp + rax*8 + 0x68], 1
    cmp rcx, 4
    jnge 4
    xor esi, esi
    mov rcx, 1
5:
    mov rax, rcx
    add rax, 1
    mov.q rcx, [rsp + rcx*8 + 0x68]
    add rcx, 1
    mov.q [rsp + rsi*8 + 0x68], rcx
    add rsi, 1
    cmp rax, 4
    mov rcx, rax
    jnge 5
    xor edx, edx
6:
    mov rcx, rdi
    mov rax, rdx
    mov rdx, rax
    add rdx, 1
    add.q rcx, [rsp + rax*8 + 0x68]
    mov rbx, rcx
    cmp rdx, 4
    mov rdi, rbx
    jnge 6
    xor esi, esi
    mov rdx, 0x48
    lea rdi, [rsp]
    call.q [rip - 0x160]
    xor r8d, r8d
7:
    xor edi, edi
8:
    mov rdx, rdi
    mov rdi, rdx
    add rdi, 1
    imul rax, r8, 0x18
    lea rcx, [rsp]
    add rax, rcx
    mov rsi, r8
    imul rsi, rdx
    mov.q [rax + rdx*8], rsi
    cmp rdi, 3
    jnge 8
    add r8, 1
    cmp r8, 3
    jnge 7
    xor r8d, r8d
9:
    xor edi, edi
10:
    mov rsi, rbx
    mov rdx, rdi
    mov rdi, rdx
    add rdi, 1
    imul rax, r8, 0x18
    lea rcx, [rsp]
    add rax, rcx
    add.q rsi, [rax + rdx*8]
    cmp rdi, 3
    mov rbx, rsi
    jnge 10
    add r8, 1
    cmp r8, 3
    mov rbx, rsi
    jnge 9
    mov rax, rsi
    add rsp, 0x90
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
