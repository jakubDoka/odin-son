=========== OPT LEVEL: none ===========
opaque:
    mov rax, rdi
    ret
main:
    sub rsp, 0x18
    lea rdi, [rsp]
    xor esi, esi
    mov rdx, 0x10
    call.q [rip - 0x28]
    mov rdi, 3
    call opaque
    mov.q [rsp], rax
    mov rdi, 4
    call opaque
    mov.q [rsp + 0x8], rax
    mov rdi, 1
    call opaque
    cmp rax, 1
    jz 2
    jmp 3
2:
    mov rdi, 0xa
    call opaque
    mov.q [rsp], rax
3:
    mov.q rdi, [rsp]
    call opaque
    mov rcx, rax
    mov.q rax, [rsp]
    add.q rax, [rsp + 0x8]
    add rax, rcx
    add rsp, 0x18
    ret
=========== OPT LEVEL: mininal ===========
opaque:
    mov rax, rdi
    ret
main:
    sub rsp, 0x18
    lea rdi, [rsp]
    xor esi, esi
    mov rdx, 0x10
    call.q [rip - 0x28]
    mov rdi, 3
    call opaque
    mov.q [rsp], rax
    mov rdi, 4
    call opaque
    mov.q [rsp + 0x8], rax
    mov rdi, 1
    call opaque
    cmp rax, 1
    jz 2
    jmp 3
2:
    mov rdi, 0xa
    call opaque
    mov.q [rsp], rax
3:
    mov.q rdi, [rsp]
    call opaque
    mov rcx, rax
    mov.q rax, [rsp]
    add.q rax, [rsp + 0x8]
    add rax, rcx
    add rsp, 0x18
    ret
=========== OPT LEVEL: moderate ===========
opaque:
    mov rax, rdi
    ret
main:
    sub rsp, 0x18
    pxor xmm0, xmm0
    movups.x [rsp], xmm0
    mov rdi, 3
    call opaque
    mov.q [rsp], rax
    mov rdi, 4
    call opaque
    mov.q [rsp + 0x8], rax
    mov rdi, 1
    call opaque
    cmp rax, 1
    jz 2
    jmp 3
2:
    mov rdi, 0xa
    call opaque
    mov.q [rsp], rax
3:
    mov.q rdi, [rsp]
    call opaque
    mov rcx, rax
    mov.q rax, [rsp]
    add.q rax, [rsp + 0x8]
    add rax, rcx
    add rsp, 0x18
    ret
=========== OPT LEVEL: all ===========
opaque:
    mov rax, rdi
    ret
main:
    sub rsp, 0x18
    pxor xmm0, xmm0
    movups.x [rsp], xmm0
    mov rdi, 3
    call opaque
    mov.q [rsp], rax
    mov rdi, 4
    call opaque
    mov.q [rsp + 0x8], rax
    mov rdi, 1
    call opaque
    cmp rax, 1
    jz 2
    jmp 3
2:
    mov rdi, 0xa
    call opaque
    mov.q [rsp], rax
3:
    mov.q rdi, [rsp]
    call opaque
    mov rcx, rax
    mov.q rax, [rsp]
    add.q rax, [rsp + 0x8]
    add rax, rcx
    add rsp, 0x18
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 0x18
    ret
