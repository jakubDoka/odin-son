=========== OPT LEVEL: none-quick ===========
main:
    sub rsp, 8
    mov rdi, 2
    mov rsi, 4
    mov rdx, 6
    call funnel
    add rsp, 8
    ret
funnel:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rax, rsi
    mov rax, rax
    add rax, rdx
    mov rax, rax
    add rsp, 8
    ret
=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    mov rdi, 2
    mov rsi, 4
    mov rdx, 6
    call funnel
    add rsp, 8
    ret
funnel:
    mov rax, rdi
    add rax, rsi
    add rax, rdx
    ret
=========== OPT LEVEL: mininal-quick ===========
main:
    sub rsp, 8
    mov rdi, 2
    mov rsi, 4
    mov rdx, 6
    call funnel
    add rsp, 8
    ret
funnel:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rax, rsi
    mov rax, rax
    add rax, rdx
    mov rax, rax
    add rsp, 8
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, 2
    mov rsi, 4
    mov rdx, 6
    call funnel
    add rsp, 8
    ret
funnel:
    mov rax, rdi
    add rax, rsi
    add rax, rdx
    ret
=========== OPT LEVEL: moderate-quick ===========
main:
    sub rsp, 8
    mov rdi, 2
    mov rsi, 4
    mov rdx, 6
    call funnel
    add rsp, 8
    ret
funnel:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rax, rsi
    mov rax, rax
    add rax, rdx
    mov rax, rax
    add rsp, 8
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, 2
    mov rsi, 4
    mov rdx, 6
    call funnel
    add rsp, 8
    ret
funnel:
    mov rax, rdi
    add rax, rsi
    add rax, rdx
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, 2
    mov rsi, 4
    mov rdx, 6
    call funnel
    add rsp, 8
    ret
funnel:
    mov rax, rdi
    add rax, rsi
    add rax, rdx
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 0xc
    ret
