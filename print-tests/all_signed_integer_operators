=========== OPT LEVEL: none ===========
main:
    mov rsi, -6
    mov rax, 0x14
    cqo
    idiv rsi
    xor ecx, ecx
    add rcx, rax
    mov rax, 0x14
    cqo
    idiv rsi
    add rcx, rdx
    mov rax, -7
    cqo
    idiv rsi
    add rcx, rax
    mov rax, -7
    cqo
    idiv rsi
    add rcx, rdx
    mov rax, -7
    sar rax, 1
    add rcx, rax
    mov rax, -6
    sar rax, 1
    add rcx, rax
    cmp rsi, 0x14
    jnge 1
    jmp 2
1:
    add rcx, 1
2:
    mov rax, -7
    cmp rax, -6
    jnge 3
    jmp 4
3:
    add rcx, 2
4:
    mov rax, 0x14
    cmp rax, -6
    jnle 5
    jmp 6
5:
    add rcx, 4
6:
    cmp rsi, -7
    jng 7
    jmp 8
7:
    add rcx, 8
8:
    mov rax, 0x14
    cmp rax, -6
    jnl 9
    jmp 10
9:
    add rcx, 0x10
10:
    mov rax, rcx
    ret
=========== OPT LEVEL: mininal ===========
main:
    mov dl, 1
    xor eax, eax
    add rax, -3
    add rax, 2
    add rax, 1
    add rax, -1
    add rax, -4
    add rax, -3
    test dl, dl
    jnz 1
    jmp 2
1:
    add rax, 1
2:
    test dl, dl
    jnz 3
    jmp 4
3:
    add rax, 2
4:
    test dl, dl
    jnz 5
    jmp 6
5:
    add rax, 4
6:
    xor ecx, ecx
    test cl, cl
    jnz 7
    jmp 8
7:
    add rax, 8
8:
    test dl, dl
    jnz 9
    jmp 10
9:
    add rax, 0x10
10:
    ret
=========== OPT LEVEL: moderate ===========
main:
    mov rax, 0xf
    ret
=========== OPT LEVEL: all ===========
main:
    mov rax, 0xf
    ret
=========== OPT LEVEL: aggresive ===========
