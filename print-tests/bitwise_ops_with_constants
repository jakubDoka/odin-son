=========== OPT LEVEL: None ===========
main:
     mov rcx, 0x0C
     mov rax, 0x00
     mov rdx, 0x0A
     and rdx, rcx
     add rax, rdx
     mov rdx, 0x03
     or rdx, rcx
     add rax, rdx
     mov rdx, 0x06
     xor rdx, rcx
     add rax, rdx
     ret
=========== OPT LEVEL: Full ===========
main:
     mov rax, 0x21
     ret
