=========== OPT LEVEL: none ===========
main:
     sub rsp, 0x08
     xor edi, edi
     sub rdi, 0x64
     call :opaque
     mov cl, 0x03
     cbw
     idiv cl
     movsx rax, al
     add rsp, 0x08
     ret
opaque:
     mov rax, rdi
     ret
=========== OPT LEVEL: mininal ===========
main:
     sub rsp, 0x08
     mov rdi, 0xFFFFFFFFFFFFFF9C
     call :opaque
     mov cl, 0x03
     cbw
     idiv cl
     movsx rax, al
     add rsp, 0x08
     ret
opaque:
     mov rax, rdi
     ret
=========== OPT LEVEL: moderate ===========
main:
     sub rsp, 0x08
     mov rdi, 0xFFFFFFFFFFFFFF9C
     call :opaque
     mov cl, 0x03
     cbw
     idiv cl
     movsx rax, al
     add rsp, 0x08
     ret
opaque:
     mov rax, rdi
     ret
=========== OPT LEVEL: all ===========
main:
     sub rsp, 0x08
     mov rdi, 0xFFFFFFFFFFFFFF9C
     call :opaque
     mov cl, 0x03
     cbw
     idiv cl
     movsx rax, al
     add rsp, 0x08
     ret
opaque:
     mov rax, rdi
     ret
=========== OPT LEVEL: aggresive ===========
main:
     mov rax, 0xFFFFFFFFFFFFFFDF
     ret
opaque:
