=========== OPT LEVEL: none ===========
s8:
    xor eax, eax
s64:
    cmp dil, sil
    setz cl
    movzx rcx, cl
    movzx ecx, cl
    add rax, rcx
    cmp dil, sil
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    cmp dil, sil
    setnge cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    cmp dil, sil
    setng cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*8]
    cmp dil, sil
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp dil, sil
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
6:
    xor eax, eax
    cmp di, si
    setz cl
    movzx rcx, cl
    movzx ecx, cl
    add rax, rcx
    cmp di, si
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    cmp di, si
    setnge cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    cmp di, si
    setng cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*8]
    cmp di, si
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp di, si
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
7:
    xor eax, eax
    cmp edi, esi
    setz cl
    movzx rcx, cl
    movzx ecx, cl
    add rax, rcx
    cmp edi, esi
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    cmp edi, esi
    setnge cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    cmp edi, esi
    setng cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*8]
    cmp edi, esi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp edi, esi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
8:
    xor eax, eax
    cmp rdi, rsi
    setz cl
    movzx rcx, cl
    movzx ecx, cl
    add rax, rcx
    cmp rdi, rsi
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    cmp rdi, rsi
    setnge cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    cmp rdi, rsi
    setng cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*8]
    cmp rdi, rsi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp rdi, rsi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
9:
    xor eax, eax
    cmp rdi, rsi
    setz cl
    movzx rcx, cl
    movzx ecx, cl
    add rax, rcx
    cmp rdi, rsi
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    cmp rdi, rsi
    setnge cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    cmp rdi, rsi
    setng cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*8]
    cmp rdi, rsi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp rdi, rsi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
10:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 0x48
    mov dil, -1
    mov sil, 1
    call s8
    mov rbx, rax
    mov sil, 5
    mov dil, 5
    call s8
    mov rbp, rax
    mov dil, 7
    mov sil, -3
    call s8
    mov r12, rax
    mov rdi, -1
    mov rsi, 1
    call 6
    mov r13, rax
    mov rsi, 5
    mov rdi, 5
    call 6
    mov r14, rax
    mov rdi, 7
    mov rsi, -3
    call 6
    mov r15, rax
    mov rdi, -1
    mov rsi, 1
    call 7
    mov.q [rsp], rax
    mov rsi, 5
    mov rdi, 5
    call 7
    mov.q [rsp + 0x8], rax
    mov rdi, 7
    mov rsi, -3
    call 7
    mov.q [rsp + 0x10], rax
    mov rdi, -1
    mov rsi, 1
    call 8
    mov.q [rsp + 0x18], rax
    mov rsi, 5
    mov rdi, 5
    call 8
    mov.q [rsp + 0x20], rax
    mov rdi, 7
    mov rsi, -3
    call 8
    mov.q [rsp + 0x28], rax
    mov rdi, -1
    mov rsi, 1
    call 9
    mov.q [rsp + 0x30], rax
    mov rsi, 5
    mov rdi, 5
    call 9
    mov.q [rsp + 0x38], rax
    mov rdi, 7
    mov rsi, -3
    call 9
    mov rcx, rax
    mov rax, rbx
    add rax, rbp
    add rax, r12
    xor edx, edx
    add rdx, rax
    mov rax, rdx
    mov rdx, r13
    add rdx, r14
    mov rsi, rdx
    add rsi, r15
    add rax, rsi
    mov.q rsi, [rsp]
    mov.q rdx, [rsp + 0x8]
    add rsi, rdx
    mov.q rdx, [rsp + 0x10]
    add rsi, rdx
    add rax, rsi
    mov.q rsi, [rsp + 0x18]
    mov.q rdx, [rsp + 0x20]
    add rsi, rdx
    mov.q rdx, [rsp + 0x28]
    add rsi, rdx
    mov rdi, rax
    add rdi, rsi
    mov.q rdx, [rsp + 0x30]
    mov.q rax, [rsp + 0x38]
    add rdx, rax
    add rdx, rcx
    add rdi, rdx
    mov rax, rdi
    add rsp, 0x48
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
s8:
    xor eax, eax
s64:
    cmp dil, sil
    setz cl
    movzx rcx, cl
    movzx ecx, cl
    add rax, rcx
    cmp dil, sil
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    cmp dil, sil
    setnge cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    cmp dil, sil
    setng cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*8]
    cmp dil, sil
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp dil, sil
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
6:
    xor eax, eax
    cmp di, si
    setz cl
    movzx rcx, cl
    movzx ecx, cl
    add rax, rcx
    cmp di, si
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    cmp di, si
    setnge cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    cmp di, si
    setng cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*8]
    cmp di, si
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp di, si
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
7:
    xor eax, eax
    cmp edi, esi
    setz cl
    movzx rcx, cl
    movzx ecx, cl
    add rax, rcx
    cmp edi, esi
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    cmp edi, esi
    setnge cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    cmp edi, esi
    setng cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*8]
    cmp edi, esi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp edi, esi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
8:
    xor eax, eax
    cmp rdi, rsi
    setz cl
    movzx rcx, cl
    movzx ecx, cl
    add rax, rcx
    cmp rdi, rsi
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    cmp rdi, rsi
    setnge cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    cmp rdi, rsi
    setng cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*8]
    cmp rdi, rsi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp rdi, rsi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
9:
    xor eax, eax
    cmp rdi, rsi
    setz cl
    movzx rcx, cl
    movzx ecx, cl
    add rax, rcx
    cmp rdi, rsi
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    cmp rdi, rsi
    setnge cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    cmp rdi, rsi
    setng cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*8]
    cmp rdi, rsi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp rdi, rsi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
10:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 0x48
    mov dil, -1
    mov sil, 1
    call s8
    mov rbx, rax
    mov sil, 5
    mov dil, 5
    call s8
    mov rbp, rax
    mov dil, 7
    mov sil, -3
    call s8
    mov r12, rax
    mov rdi, -1
    mov rsi, 1
    call 6
    mov r13, rax
    mov rsi, 5
    mov rdi, 5
    call 6
    mov r14, rax
    mov rdi, 7
    mov rsi, -3
    call 6
    mov r15, rax
    mov rdi, -1
    mov rsi, 1
    call 7
    mov.q [rsp], rax
    mov rsi, 5
    mov rdi, 5
    call 7
    mov.q [rsp + 0x8], rax
    mov rdi, 7
    mov rsi, -3
    call 7
    mov.q [rsp + 0x10], rax
    mov rdi, -1
    mov rsi, 1
    call 8
    mov.q [rsp + 0x18], rax
    mov rsi, 5
    mov rdi, 5
    call 8
    mov.q [rsp + 0x20], rax
    mov rdi, 7
    mov rsi, -3
    call 8
    mov.q [rsp + 0x28], rax
    mov rdi, -1
    mov rsi, 1
    call 9
    mov.q [rsp + 0x30], rax
    mov rsi, 5
    mov rdi, 5
    call 9
    mov.q [rsp + 0x38], rax
    mov rdi, 7
    mov rsi, -3
    call 9
    mov rcx, rax
    mov rax, rbx
    add rax, rbp
    add rax, r12
    xor edx, edx
    add rdx, rax
    mov rax, rdx
    mov rdx, r13
    add rdx, r14
    mov rsi, rdx
    add rsi, r15
    add rax, rsi
    mov.q rsi, [rsp]
    mov.q rdx, [rsp + 0x8]
    add rsi, rdx
    mov.q rdx, [rsp + 0x10]
    add rsi, rdx
    add rax, rsi
    mov.q rsi, [rsp + 0x18]
    mov.q rdx, [rsp + 0x20]
    add rsi, rdx
    mov.q rdx, [rsp + 0x28]
    add rsi, rdx
    mov rdi, rax
    add rdi, rsi
    mov.q rdx, [rsp + 0x30]
    mov.q rax, [rsp + 0x38]
    add rdx, rax
    add rdx, rcx
    add rdi, rdx
    mov rax, rdi
    add rsp, 0x48
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
s8:
    cmp dil, sil
s64:
    setz al
    movzx rax, al
    movzx eax, al
    cmp dil, sil
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    cmp dil, sil
    setnge cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    cmp dil, sil
    setng cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*8]
    cmp dil, sil
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp dil, sil
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
6:
    cmp di, si
    setz al
    movzx rax, al
    movzx eax, al
    cmp di, si
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    cmp di, si
    setnge cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    cmp di, si
    setng cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*8]
    cmp di, si
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp di, si
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
7:
    cmp edi, esi
    setz al
    movzx rax, al
    movzx eax, al
    cmp edi, esi
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    cmp edi, esi
    setnge cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    cmp edi, esi
    setng cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*8]
    cmp edi, esi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp edi, esi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
8:
    cmp rdi, rsi
    setz al
    movzx rax, al
    movzx eax, al
    cmp rdi, rsi
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    cmp rdi, rsi
    setnge cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    cmp rdi, rsi
    setng cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*8]
    cmp rdi, rsi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp rdi, rsi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
9:
    cmp rdi, rsi
    setz al
    movzx rax, al
    movzx eax, al
    cmp rdi, rsi
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    cmp rdi, rsi
    setnge cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    cmp rdi, rsi
    setng cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*8]
    cmp rdi, rsi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp rdi, rsi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
10:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 0x48
    mov dil, -1
    mov sil, 1
    call s8
    mov rbx, rax
    mov sil, 5
    mov dil, 5
    call s8
    mov rbp, rax
    mov dil, 7
    mov sil, -3
    call s8
    mov r12, rax
    mov rdi, -1
    mov rsi, 1
    call 6
    mov r13, rax
    mov rsi, 5
    mov rdi, 5
    call 6
    mov r14, rax
    mov rdi, 7
    mov rsi, -3
    call 6
    mov r15, rax
    mov rdi, -1
    mov rsi, 1
    call 7
    mov.q [rsp], rax
    mov rsi, 5
    mov rdi, 5
    call 7
    mov.q [rsp + 0x8], rax
    mov rdi, 7
    mov rsi, -3
    call 7
    mov.q [rsp + 0x10], rax
    mov rdi, -1
    mov rsi, 1
    call 8
    mov.q [rsp + 0x18], rax
    mov rsi, 5
    mov rdi, 5
    call 8
    mov.q [rsp + 0x20], rax
    mov rdi, 7
    mov rsi, -3
    call 8
    mov.q [rsp + 0x28], rax
    mov rdi, -1
    mov rsi, 1
    call 9
    mov.q [rsp + 0x30], rax
    mov rsi, 5
    mov rdi, 5
    call 9
    mov.q [rsp + 0x38], rax
    mov rdi, 7
    mov rsi, -3
    call 9
    mov rcx, rax
    mov rax, rbx
    add rax, rbp
    add rax, r12
    mov rdx, r13
    add rdx, r14
    mov rsi, rdx
    add rsi, r15
    add rax, rsi
    mov.q rsi, [rsp]
    mov.q rdx, [rsp + 0x8]
    add rsi, rdx
    mov.q rdx, [rsp + 0x10]
    add rsi, rdx
    add rax, rsi
    mov.q rsi, [rsp + 0x18]
    mov.q rdx, [rsp + 0x20]
    add rsi, rdx
    mov.q rdx, [rsp + 0x28]
    add rsi, rdx
    mov rdx, rax
    add rdx, rsi
    mov.q rsi, [rsp + 0x30]
    mov.q rax, [rsp + 0x38]
    add rsi, rax
    add rsi, rcx
    add rdx, rsi
    mov rax, rdx
    add rsp, 0x48
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: all ===========
s8:
    cmp dil, sil
s64:
    setz al
    movzx rax, al
    movzx eax, al
    cmp dil, sil
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    cmp dil, sil
    setnge cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    cmp dil, sil
    setng cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*8]
    cmp dil, sil
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp dil, sil
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
6:
    cmp di, si
    setz al
    movzx rax, al
    movzx eax, al
    cmp di, si
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    cmp di, si
    setnge cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    cmp di, si
    setng cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*8]
    cmp di, si
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp di, si
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
7:
    cmp edi, esi
    setz al
    movzx rax, al
    movzx eax, al
    cmp edi, esi
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    cmp edi, esi
    setnge cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    cmp edi, esi
    setng cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*8]
    cmp edi, esi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp edi, esi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
8:
    cmp rdi, rsi
    setz al
    movzx rax, al
    movzx eax, al
    cmp rdi, rsi
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    cmp rdi, rsi
    setnge cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    cmp rdi, rsi
    setng cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*8]
    cmp rdi, rsi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp rdi, rsi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
9:
    cmp rdi, rsi
    setz al
    movzx rax, al
    movzx eax, al
    cmp rdi, rsi
    setnz cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*2]
    cmp rdi, rsi
    setnge cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*4]
    cmp rdi, rsi
    setng cl
    movzx rcx, cl
    movzx ecx, cl
    lea rax, [rax + rcx*8]
    cmp rdi, rsi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp rdi, rsi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
10:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 0x48
    mov dil, -1
    mov sil, 1
    call s8
    mov rbx, rax
    mov sil, 5
    mov dil, 5
    call s8
    mov rbp, rax
    mov dil, 7
    mov sil, -3
    call s8
    mov r12, rax
    mov rdi, -1
    mov rsi, 1
    call 6
    mov r13, rax
    mov rsi, 5
    mov rdi, 5
    call 6
    mov r14, rax
    mov rdi, 7
    mov rsi, -3
    call 6
    mov r15, rax
    mov rdi, -1
    mov rsi, 1
    call 7
    mov.q [rsp], rax
    mov rsi, 5
    mov rdi, 5
    call 7
    mov.q [rsp + 0x8], rax
    mov rdi, 7
    mov rsi, -3
    call 7
    mov.q [rsp + 0x10], rax
    mov rdi, -1
    mov rsi, 1
    call 8
    mov.q [rsp + 0x18], rax
    mov rsi, 5
    mov rdi, 5
    call 8
    mov.q [rsp + 0x20], rax
    mov rdi, 7
    mov rsi, -3
    call 8
    mov.q [rsp + 0x28], rax
    mov rdi, -1
    mov rsi, 1
    call 9
    mov.q [rsp + 0x30], rax
    mov rsi, 5
    mov rdi, 5
    call 9
    mov.q [rsp + 0x38], rax
    mov rdi, 7
    mov rsi, -3
    call 9
    mov rcx, rax
    mov rax, rbx
    add rax, rbp
    add rax, r12
    mov rdx, r13
    add rdx, r14
    mov rsi, rdx
    add rsi, r15
    add rax, rsi
    mov.q rsi, [rsp]
    mov.q rdx, [rsp + 0x8]
    add rsi, rdx
    mov.q rdx, [rsp + 0x10]
    add rsi, rdx
    add rax, rsi
    mov.q rsi, [rsp + 0x18]
    mov.q rdx, [rsp + 0x20]
    add rsi, rdx
    mov.q rdx, [rsp + 0x28]
    add rsi, rdx
    mov rdx, rax
    add rdx, rsi
    mov.q rsi, [rsp + 0x30]
    mov.q rax, [rsp + 0x38]
    add rsi, rax
    add rsi, rcx
    add rdx, rsi
    mov rax, rdx
    add rsp, 0x48
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
