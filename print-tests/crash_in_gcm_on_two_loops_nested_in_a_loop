=========== OPT LEVEL: none ===========
main:
    xor eax, eax
    xor edi, edi
5:
    cmp rdi, 4
    jnl 0
    xor ecx, ecx
    xor edx, edx
2:
    cmp rcx, 4
    jnl 1
    add rdx, rcx
    add rcx, 1
    jmp 2
1:
    xor esi, esi
    xor ecx, ecx
4:
    cmp rcx, 4
    jnl 3
    lea rsi, [rsi + rcx*2]
    add rcx, 1
    jmp 4
3:
    add rdx, rsi
    add rax, rdx
    add rdi, 1
    jmp 5
0:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: mininal ===========
main:
    xor eax, eax
    xor edi, edi
5:
    cmp rdi, 4
    jnl 0
    xor ecx, ecx
    xor edx, edx
2:
    cmp rcx, 4
    jnl 1
    add rdx, rcx
    add rcx, 1
    jmp 2
1:
    xor esi, esi
    xor ecx, ecx
4:
    cmp rcx, 4
    jnl 3
    lea rsi, [rsi + rcx*2]
    add rcx, 1
    jmp 4
3:
    add rdx, rsi
    add rax, rdx
    add rdi, 1
    jmp 5
0:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: moderate ===========
main:
    xor eax, eax
    xor edi, edi
5:
    cmp rdi, 4
    jnl 0
    xor ecx, ecx
    xor edx, edx
2:
    cmp rcx, 4
    jnl 1
    add rdx, rcx
    add rcx, 1
    jmp 2
1:
    xor esi, esi
    xor ecx, ecx
4:
    cmp rcx, 4
    jnl 3
    lea rsi, [rsi + rcx*2]
    add rcx, 1
    jmp 4
3:
    add rdx, rsi
    add rax, rdx
    add rdi, 1
    jmp 5
0:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: all ===========
main:
    xor eax, eax
    xor edi, edi
5:
    cmp rdi, 4
    jnl 0
    xor ecx, ecx
    xor edx, edx
2:
    cmp rcx, 4
    jnl 1
    add rdx, rcx
    add rcx, 1
    jmp 2
1:
    xor esi, esi
    xor ecx, ecx
4:
    cmp rcx, 4
    jnl 3
    lea rsi, [rsi + rcx*2]
    add rcx, 1
    jmp 4
3:
    add rdx, rsi
    add rax, rdx
    add rdi, 1
    jmp 5
0:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: aggresive ===========
