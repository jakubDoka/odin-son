=========== OPT LEVEL: None ===========
main:
     push rbx
     push rbp
     mov rbx, 0x06
     mov rax, 0x07
     mov rcx, 0x00
     sub rcx, rax
     mov rbp, rcx
     mov rax, 0x14
     cqo
     idiv rbx
     mov rcx, rax
     mov rax, 0x00
     add rcx, rax
     mov rax, 0x14
     cqo
     idiv rbx
     add rcx, rdx
     mov rax, rbp
     cqo
     idiv rbx
     add rcx, rax
     mov rax, rbp
     cqo
     idiv rbx
     add rcx, rdx
     mov rax, 0x14
     and rax, rbx
     add rcx, rax
     mov rax, 0x14
     or rax, rbx
     mov rdx, rcx
     add rdx, rax
     mov rax, 0x14
     xor rax, rbx
     add rdx, rax
     mov rax, 0x14
     mov rcx, 0x06
     not rcx
     and rcx, rax
     add rdx, rcx
     mov rcx, 0x02
     mov rax, 0x14
     shl rax, cl
     add rdx, rax
     mov rax, 0x14
     sar rax, cl
     add rdx, rax
     mov rax, rbp
     sar rax, cl
     add rdx, rax
     mov rax, 0x14
     cmp rax, rbx
     setnle al
     movzx rax, al
     test rax, rax
     jz :0
     jmp :1

000: jmp :2

001: mov rax, 0x01
     add rax, rdx
     mov rdx, rax
     jmp :2

002: mov rsi, rdx
     mov rax, 0x14
     mov rdx, 0x06
     cmp rdx, rax
     setl dl
     movzx rdx, dl
     test rdx, rdx
     jz :3
     jmp :4

003: jmp :5

004: add rsi, rcx
     jmp :5

005: mov rax, 0x14
     cmp rax, rbx
     setnl al
     movzx rax, al
     test rax, rax
     jz :6
     jmp :7

006: jmp :8

007: mov rax, rsi
     mov rsi, 0x04
     add rsi, rax
     jmp :8

008: mov rax, 0x14
     cmp rax, rbx
     setle al
     movzx rax, al
     test rax, rax
     jz :9
     jmp :10

009: jmp :11

010: mov rax, rsi
     mov rsi, 0x08
     add rsi, rax
     jmp :11

011: mov rcx, 0x14
     mov rax, 0x14
     cmp rcx, rax
     setz cl
     movzx rcx, cl
     test rcx, rcx
     jz :12
     jmp :13

012: jmp :14

013: mov rax, rsi
     mov rsi, 0x10
     add rsi, rax
     jmp :14

014: mov rax, 0x14
     cmp rax, rbx
     setnz al
     movzx rax, al
     test rax, rax
     jz :15
     jmp :16

015: jmp :17

016: mov rax, rsi
     mov rcx, 0x20
     add rcx, rax
     mov rsi, rcx
     jmp :17

017: mov rcx, rsi
     mov rax, rbp
     cmp rax, rbx
     setl al
     movzx rax, al
     test rax, rax
     jz :18
     jmp :19

018: jmp :20

019: mov rax, rcx
     mov rcx, 0x40
     add rcx, rax
     jmp :20

020: mov rax, rcx
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: Full ===========
main:
     mov rcx, 0x01
     test rcx, rcx
     jz :0
     jmp :1

000: mov rax, 0x92
     jmp :2

001: mov rax, 0x93
     jmp :2

002: test rcx, rcx
     jz :3
     jmp :4

003: jmp :5

004: add rax, 0x02
     jmp :5

005: test rcx, rcx
     jz :6
     jmp :7

006: jmp :8

007: add rax, 0x04
     jmp :8

008: mov rdx, 0x00
     test rdx, rdx
     jz :9
     jmp :10

009: jmp :11

010: add rax, 0x08
     jmp :11

011: test rcx, rcx
     jz :12
     jmp :13

012: jmp :14

013: add rax, 0x10
     jmp :14

014: test rcx, rcx
     jz :15
     jmp :16

015: jmp :17

016: add rax, 0x20
     jmp :17

017: test rcx, rcx
     jz :18
     jmp :19

018: jmp :20

019: add rax, 0x40
     jmp :20

020: ret
