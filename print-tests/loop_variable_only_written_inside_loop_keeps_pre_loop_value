=========== OPT LEVEL: none ===========
main:
    xor ecx, ecx
    mov rax, 0x2a
1:
    cmp rcx, 3
    jz 2
    add rcx, 1
    mov rax, rcx
    add rax, 7
    jmp 1
2:
    ret
=========== OPT LEVEL: mininal ===========
main:
    xor ecx, ecx
    mov rax, 0x2a
1:
    cmp rcx, 3
    jz 2
    mov rax, rcx
    add rax, 8
    add rcx, 1
    jmp 1
2:
    ret
=========== OPT LEVEL: moderate ===========
main:
    xor ecx, ecx
    mov rax, 0x2a
1:
    cmp rcx, 3
    jz 2
    mov rax, rcx
    add rax, 8
    add rcx, 1
    jmp 1
2:
    ret
=========== OPT LEVEL: all ===========
main:
    xor ecx, ecx
    mov rax, 0x2a
1:
    cmp rcx, 3
    jz 2
    mov rax, rcx
    add rax, 8
    add rcx, 1
    jmp 1
2:
    ret
=========== OPT LEVEL: aggresive ===========
