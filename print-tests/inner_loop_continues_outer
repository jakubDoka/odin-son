=========== OPT LEVEL: None ===========
main:
     push rbx
     push rbp
     mov rbp, 0x00
     mov rax, 0x00
     jmp :0

000: mov rcx, 0x01
     add rbp, rcx
     mov rdx, 0x05
     cmp rbp, rdx
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :1
     jmp :2

001: mov rbx, 0x00
     add rbx, rcx
     cmp rbx, rdx
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :3
     jmp :4

003: mov rbx, rax
     mov rdx, 0x00
     jmp :5

005: add rdx, rcx
     add rbx, rcx
     mov rax, 0x02
     cmp rax, rdx
     setz al
     movzx rax, al
     test rax, rax
     jz :6
     jmp :7

006: jmp :5

007: jmp :8

004: mov rbx, rax
     jmp :8

008: mov rax, rbx
     jmp :0

002: pop rbp
     pop rbx
     ret
=========== OPT LEVEL: Full ===========
main:
     push rbx
     mov rbx, 0x00
     mov rax, 0x00
     jmp :0

000: add rbx, 0x01
     mov rcx, 0x05
     cmp rbx, rcx
     jnz :1
     jmp :2

001: mov rdx, rax
     mov rcx, 0x00
     jmp :3

003: add rcx, 0x01
     add rdx, 0x01
     mov rax, 0x02
     cmp rcx, rax
     jnz :4
     jmp :5

004: jmp :3

005: mov rax, rdx
     jmp :0

002: pop rbx
     ret
