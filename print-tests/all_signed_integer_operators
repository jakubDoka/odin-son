=========== OPT LEVEL: none ===========
main:
     push rbx
     push rbp
     mov rcx, 0x00
     mov rax, 0x06
     mov rdx, 0x00
     sub rdx, rax
     mov rbx, rdx
     mov rax, 0x07
     mov rdx, 0x00
     sub rdx, rax
     mov rbp, rdx
     mov rax, 0x14
     cqo
     idiv rbx
     add rax, rcx
     mov rsi, rax
     mov rax, 0x14
     cqo
     idiv rbx
     add rsi, rdx
     mov rax, rbp
     cqo
     idiv rbx
     add rsi, rax
     mov rax, rbp
     cqo
     idiv rbx
     add rsi, rdx
     mov rcx, 0x01
     mov rax, rbp
     sar rax, cl
     add rsi, rax
     mov rax, rbx
     sar rax, cl
     add rsi, rax
     mov rax, 0x14
     cmp rbx, rax
     setl al
     movzx rax, al
     test rax, rax
     jz :0
     jmp :1

000: jmp :2

001: add rsi, rcx
     jmp :2

002: mov rcx, rsi
     mov rax, rbp
     cmp rax, rbx
     setl al
     movzx rax, al
     test rax, rax
     jz :3
     jmp :4

003: jmp :5

004: mov rax, 0x02
     add rax, rcx
     mov rcx, rax
     jmp :5

005: mov rax, 0x14
     cmp rax, rbx
     setnle al
     movzx rax, al
     test rax, rax
     jz :6
     jmp :7

006: jmp :8

007: mov rax, rcx
     mov rcx, 0x04
     add rcx, rax
     jmp :8

008: mov rax, rbp
     cmp rbx, rax
     setle al
     movzx rax, al
     test rax, rax
     jz :9
     jmp :10

009: jmp :11

010: mov rax, rcx
     mov rcx, 0x08
     add rcx, rax
     jmp :11

011: mov rax, 0x14
     cmp rax, rbx
     setnl al
     movzx rax, al
     test rax, rax
     jz :12
     jmp :13

012: jmp :14

013: mov rax, rcx
     mov rcx, 0x10
     add rcx, rax
     jmp :14

014: mov rax, rcx
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: mininal ===========
main:
     push rbx
     push rbp
     mov rcx, 0x00
     mov rax, 0x06
     mov rdx, 0x00
     sub rdx, rax
     mov rbx, rdx
     mov rax, 0x07
     mov rdx, 0x00
     sub rdx, rax
     mov rbp, rdx
     mov rax, 0x14
     cqo
     idiv rbx
     add rax, rcx
     mov rsi, rax
     mov rax, 0x14
     cqo
     idiv rbx
     add rsi, rdx
     mov rax, rbp
     cqo
     idiv rbx
     add rsi, rax
     mov rax, rbp
     cqo
     idiv rbx
     add rsi, rdx
     mov rcx, 0x01
     mov rax, rbp
     sar rax, cl
     add rsi, rax
     mov rax, rbx
     sar rax, cl
     add rsi, rax
     mov rax, 0x14
     cmp rbx, rax
     setl al
     movzx rax, al
     test rax, rax
     jz :0
     jmp :1

000: jmp :2

001: add rsi, rcx
     jmp :2

002: mov rcx, rsi
     mov rax, rbp
     cmp rax, rbx
     setl al
     movzx rax, al
     test rax, rax
     jz :3
     jmp :4

003: jmp :5

004: mov rax, 0x02
     add rax, rcx
     mov rcx, rax
     jmp :5

005: mov rax, 0x14
     cmp rax, rbx
     setnle al
     movzx rax, al
     test rax, rax
     jz :6
     jmp :7

006: jmp :8

007: mov rax, rcx
     mov rcx, 0x04
     add rcx, rax
     jmp :8

008: mov rax, rbp
     cmp rbx, rax
     setle al
     movzx rax, al
     test rax, rax
     jz :9
     jmp :10

009: jmp :11

010: mov rax, rcx
     mov rcx, 0x08
     add rcx, rax
     jmp :11

011: mov rax, 0x14
     cmp rax, rbx
     setnl al
     movzx rax, al
     test rax, rax
     jz :12
     jmp :13

012: jmp :14

013: mov rax, rcx
     mov rcx, 0x10
     add rcx, rax
     jmp :14

014: mov rax, rcx
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: all ===========
main:
     mov rax, 0x0F
     ret
