=========== OPT LEVEL: none ===========
main:
    push rbx
    sub rsp, 0x90
    lea rdi, [rsp + 0x48]
    xor esi, esi
    mov rdx, 0x20
    call.q [rip - 0x26]
    xor eax, eax
1:
    cmp rax, 0x20
    jnl 2
    mov rdx, rax
    add rdx, 1
    mov rcx, rax
    mov.b [rsp + rax + 0x48], cl
    mov rax, rdx
    jmp 1
2:
    xor ecx, ecx
    xor eax, eax
3:
    mov rbx, rcx
    cmp rax, 0x20
    jnl 4
    movzx.b edx, [rsp + rax + 0x48]
    mov rcx, rbx
    add rcx, rdx
    add rax, 1
    jmp 3
4:
    lea rdi, [rsp + 0x68]
    xor esi, esi
    mov rdx, 0x20
    call.q [rip - 0x93]
    mov.q [rsp + 0x68], 0x10
    mov.q [rsp + 0x70], 0x19
    mov.q [rsp + 0x78], 0x1f
    mov.q [rsp + 0x80], 0x40
    xor eax, eax
5:
    cmp rax, 4
    jnl 6
    add.q [rsp + rax*8 + 0x68], rax
    add rax, 1
    jmp 5
6:
    xor eax, eax
7:
    cmp rax, 4
    jnl 8
    add.q [rsp + rax*8 + 0x68], 1
    add rax, 1
    jmp 7
8:
    mov rsi, 1
    xor eax, eax
9:
    cmp rsi, 4
    jnl 10
    mov.q rdx, [rsp + rsi*8 + 0x68]
    add rdx, 1
    mov.q [rsp + rax*8 + 0x68], rdx
    add rax, 1
    add rsi, 1
    jmp 9
10:
    xor eax, eax
11:
    cmp rax, 4
    jnl 12
    add.q rbx, [rsp + rax*8 + 0x68]
    add rax, 1
    jmp 11
12:
    mov rdx, 0x48
    xor esi, esi
    lea rdi, [rsp]
    call.q [rip - 0x170]
    xor eax, eax
13:
    cmp rax, 3
    jnl 16
    xor ecx, ecx
14:
    cmp rcx, 3
    jnl 15
    imul rdi, rax, 0x18
    lea rdx, [rsp]
    add rdi, rdx
    mov rdx, rax
    imul rdx, rcx
    mov.q [rdi + rcx*8], rdx
    add rcx, 1
    jmp 14
15:
    add rax, 1
    jmp 13
16:
    xor eax, eax
17:
    cmp rax, 3
    jnl 20
    xor ecx, ecx
18:
    cmp rcx, 3
    jnl 19
    imul rdi, rax, 0x18
    lea rdx, [rsp]
    add rdi, rdx
    add.q rbx, [rdi + rcx*8]
    add rcx, 1
    jmp 18
19:
    add rax, 1
    jmp 17
20:
    mov rax, rbx
    add rsp, 0x90
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
main:
    push rbx
    sub rsp, 0x90
    lea rdi, [rsp + 0x48]
    xor esi, esi
    mov rdx, 0x20
    call.q [rip - 0x26]
    xor eax, eax
1:
    cmp rax, 0x20
    jnl 2
    mov rdx, rax
    add rdx, 1
    mov rcx, rax
    mov.b [rsp + rax + 0x48], cl
    mov rax, rdx
    jmp 1
2:
    xor ecx, ecx
    xor eax, eax
3:
    mov rbx, rcx
    cmp rax, 0x20
    jnl 4
    movzx.b edx, [rsp + rax + 0x48]
    mov rcx, rbx
    add rcx, rdx
    add rax, 1
    jmp 3
4:
    lea rdi, [rsp + 0x68]
    xor esi, esi
    mov rdx, 0x20
    call.q [rip - 0x93]
    mov.q [rsp + 0x68], 0x10
    mov.q [rsp + 0x70], 0x19
    mov.q [rsp + 0x78], 0x1f
    mov.q [rsp + 0x80], 0x40
    xor eax, eax
5:
    cmp rax, 4
    jnl 6
    add.q [rsp + rax*8 + 0x68], rax
    add rax, 1
    jmp 5
6:
    xor eax, eax
7:
    cmp rax, 4
    jnl 8
    add.q [rsp + rax*8 + 0x68], 1
    add rax, 1
    jmp 7
8:
    mov rsi, 1
    xor eax, eax
9:
    cmp rsi, 4
    jnl 10
    mov.q rdx, [rsp + rsi*8 + 0x68]
    add rdx, 1
    mov.q [rsp + rax*8 + 0x68], rdx
    add rax, 1
    add rsi, 1
    jmp 9
10:
    xor eax, eax
11:
    cmp rax, 4
    jnl 12
    add.q rbx, [rsp + rax*8 + 0x68]
    add rax, 1
    jmp 11
12:
    mov rdx, 0x48
    xor esi, esi
    lea rdi, [rsp]
    call.q [rip - 0x170]
    xor eax, eax
13:
    cmp rax, 3
    jnl 16
    xor ecx, ecx
14:
    cmp rcx, 3
    jnl 15
    imul rdi, rax, 0x18
    lea rdx, [rsp]
    add rdi, rdx
    mov rdx, rax
    imul rdx, rcx
    mov.q [rdi + rcx*8], rdx
    add rcx, 1
    jmp 14
15:
    add rax, 1
    jmp 13
16:
    xor eax, eax
17:
    cmp rax, 3
    jnl 20
    xor ecx, ecx
18:
    cmp rcx, 3
    jnl 19
    imul rdi, rax, 0x18
    lea rdx, [rsp]
    add rdi, rdx
    add.q rbx, [rdi + rcx*8]
    add rcx, 1
    jmp 18
19:
    add rax, 1
    jmp 17
20:
    mov rax, rbx
    add rsp, 0x90
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
main:
    push rbx
    sub rsp, 0x90
    pxor xmm0, xmm0
    movups.x [rsp + 0x58], xmm0
    movups.x [rsp + 0x48], xmm0
    xor eax, eax
1:
    cmp rax, 0x20
    jnl 2
    mov rdx, rax
    add rdx, 1
    mov rcx, rax
    mov.b [rsp + rax + 0x48], cl
    mov rax, rdx
    jmp 1
2:
    xor ecx, ecx
    xor eax, eax
3:
    mov rbx, rcx
    cmp rax, 0x20
    jnl 4
    movzx.b edx, [rsp + rax + 0x48]
    mov rcx, rbx
    add rcx, rdx
    add rax, 1
    jmp 3
4:
    mov.q [rsp + 0x68], 0x10
    mov.q [rsp + 0x70], 0x19
    mov.q [rsp + 0x78], 0x1f
    mov.q [rsp + 0x80], 0x40
    xor eax, eax
5:
    cmp rax, 4
    jnl 6
    add.q [rsp + rax*8 + 0x68], rax
    add rax, 1
    jmp 5
6:
    xor eax, eax
7:
    cmp rax, 4
    jnl 8
    add.q [rsp + rax*8 + 0x68], 1
    add rax, 1
    jmp 7
8:
    mov rsi, 1
    xor eax, eax
9:
    cmp rsi, 4
    jnl 10
    mov.q rdx, [rsp + rsi*8 + 0x68]
    add rdx, 1
    mov.q [rsp + rax*8 + 0x68], rdx
    add rsi, 1
    add rax, 1
    jmp 9
10:
    xor eax, eax
11:
    cmp rax, 4
    jnl 12
    add.q rbx, [rsp + rax*8 + 0x68]
    add rax, 1
    jmp 11
12:
    xor esi, esi
    mov rdx, 0x48
    lea rdi, [rsp]
    call.q [rip - 0x157]
    xor eax, eax
13:
    cmp rax, 3
    jnl 16
    xor ecx, ecx
14:
    cmp rcx, 3
    jnl 15
    imul rdi, rax, 0x18
    lea rdx, [rsp]
    add rdi, rdx
    mov rdx, rax
    imul rdx, rcx
    mov.q [rdi + rcx*8], rdx
    add rcx, 1
    jmp 14
15:
    add rax, 1
    jmp 13
16:
    xor eax, eax
17:
    cmp rax, 3
    jnl 20
    xor ecx, ecx
18:
    cmp rcx, 3
    jnl 19
    imul rdi, rax, 0x18
    lea rdx, [rsp]
    add rdi, rdx
    add.q rbx, [rdi + rcx*8]
    add rcx, 1
    jmp 18
19:
    add rax, 1
    jmp 17
20:
    mov rax, rbx
    add rsp, 0x90
    pop rbx
    ret
=========== OPT LEVEL: all ===========
main:
    push rbx
    sub rsp, 0x90
    pxor xmm0, xmm0
    movups.x [rsp + 0x58], xmm0
    movups.x [rsp + 0x48], xmm0
    xor eax, eax
1:
    mov rcx, rax
    mov rax, rcx
    add rax, 1
    mov rdx, rcx
    mov.b [rsp + rcx + 0x48], dl
    cmp rax, 0x20
    jnge 1
    xor edx, edx
    xor eax, eax
2:
    movzx.b ecx, [rsp + rax + 0x48]
    add rdx, rcx
    mov rcx, rdx
    add rax, 1
    cmp rax, 0x20
    mov rdx, rcx
    jnge 2
    mov.q [rsp + 0x68], 0x10
    mov.q [rsp + 0x70], 0x19
    mov.q [rsp + 0x78], 0x1f
    mov.q [rsp + 0x80], 0x40
    xor eax, eax
3:
    add.q [rsp + rax*8 + 0x68], rax
    add rax, 1
    cmp rax, 4
    jnge 3
    xor eax, eax
4:
    add.q [rsp + rax*8 + 0x68], 1
    add rax, 1
    cmp rax, 4
    jnge 4
    mov rdi, 1
    xor eax, eax
5:
    mov.q rsi, [rsp + rdi*8 + 0x68]
    add rsi, 1
    mov.q [rsp + rax*8 + 0x68], rsi
    add rdi, 1
    add rax, 1
    cmp rdi, 4
    jnge 5
    xor eax, eax
6:
    add.q rcx, [rsp + rax*8 + 0x68]
    mov rbx, rcx
    add rax, 1
    cmp rax, 4
    mov rcx, rbx
    jnge 6
    xor esi, esi
    mov rdx, 0x48
    lea rdi, [rsp]
    call.q [rip - 0x13f]
    xor eax, eax
7:
    xor ecx, ecx
8:
    imul rdi, rax, 0x18
    lea rdx, [rsp]
    add rdi, rdx
    mov rdx, rax
    imul rdx, rcx
    mov.q [rdi + rcx*8], rdx
    add rcx, 1
    cmp rcx, 3
    jnge 8
    add rax, 1
    cmp rax, 3
    jnge 7
    xor eax, eax
9:
    xor ecx, ecx
10:
    mov rsi, rbx
    imul rdi, rax, 0x18
    lea rdx, [rsp]
    add rdi, rdx
    add.q rsi, [rdi + rcx*8]
    add rcx, 1
    cmp rcx, 3
    mov rbx, rsi
    jnge 10
    add rax, 1
    cmp rax, 3
    mov rbx, rsi
    jnge 9
    mov rax, rsi
    add rsp, 0x90
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
