=========== OPT LEVEL: None ===========
main:
     push rbx
     push rbp
     mov rcx, 0x0A
     mov rdx, 0x00
     mov rbx, 0x01
     mov rsi, 0x00
     mov rax, 0x01
     jmp :0

000: cmp rdx, rcx
     setz bpl
     movzx rbp, bpl
     test rbp, rbp
     jz :1
     jmp :2

001: mov rbp, rsi
     add rbp, rax
     sub rcx, rbx
     mov rsi, rax
     mov rax, rbp
     jmp :0

002: pop rbp
     pop rbx
     ret
=========== OPT LEVEL: Full ===========
main:
     push rbx
     push rbp
     mov rcx, 0x0A
     mov rdx, 0x00
     mov rbp, 0x00
     mov rax, 0x01
     jmp :0

000: cmp rdx, rcx
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :1
     jmp :2

001: mov rbx, rbp
     add rbx, rax
     sub rcx, 0x01
     mov rbp, rax
     mov rax, rbx
     jmp :0

002: pop rbp
     pop rbx
     ret
