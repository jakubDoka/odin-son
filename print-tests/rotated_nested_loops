=========== OPT LEVEL: none ===========
main:
    xor eax, eax
    xor esi, esi
1:
    xor edx, edx
2:
    mov rcx, rsi
    imul rcx, rdx
    add rax, rcx
    add rdx, 1
    cmp rdx, 3
    jnle 3
    jmp 2
3:
    add rsi, 1
    cmp rsi, 3
    jnle 4
    jmp 1
4:
    ret
=========== OPT LEVEL: mininal ===========
main:
    xor eax, eax
    xor esi, esi
1:
    xor edx, edx
2:
    mov rcx, rsi
    imul rcx, rdx
    add rax, rcx
    add rdx, 1
    cmp rdx, 3
    jnle 3
    jmp 2
3:
    add rsi, 1
    cmp rsi, 3
    jnle 4
    jmp 1
4:
    ret
=========== OPT LEVEL: moderate ===========
main:
    xor eax, eax
    xor esi, esi
1:
    xor edx, edx
2:
    mov rcx, rsi
    imul rcx, rdx
    add rax, rcx
    add rdx, 1
    cmp rdx, 3
    jnle 3
    jmp 2
3:
    add rsi, 1
    cmp rsi, 3
    jnle 4
    jmp 1
4:
    ret
=========== OPT LEVEL: all ===========
main:
    xor esi, esi
    xor edi, edi
1:
    xor edx, edx
2:
    mov rcx, rdi
    imul rcx, rdx
    mov rax, rsi
    add rax, rcx
    add rdx, 1
    cmp rdx, 3
    mov rsi, rax
    jng 2
    add rdi, 1
    cmp rdi, 3
    mov rsi, rax
    jng 1
    ret
=========== OPT LEVEL: aggresive ===========
