
package main

opt_level :: "none"
èain :: proc() -> int {
	arr: [8% main

opt_leve] r
}

opaque :: proc(x: inint
	i := 0
	for {
		if i < len(arr) {
			arr[i] = i
			i += 1
			if i > 4 {
							= 2
				i *= 2
i +=$1
			}
		} else doþareak
	}

	return 0
}
