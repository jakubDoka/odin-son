=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    mov rdi, -0x2a
    call 2
    mov rcx, rax
    movsd.q xmm1, [rip + 0x86]
    cvtsi2ss xmm0, rcx
    cvttss2si rsi, xmm0
    xor eax, eax
    movss.d xmm0, [rip + 0x7a]
    cvtss2sd xmm0, xmm0
    cvtsd2ss xmm0, xmm0
    cvttss2si rdx, xmm0
    add rax, rdx
    movsd.q xmm0, [rip + 0x65]
    cvtsd2ss xmm0, xmm0
    cvtss2sd xmm0, xmm0
    cvttsd2si rdx, xmm0
    add rax, rdx
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rsi, rcx
    add rax, rsi
    cvttsd2si ecx, xmm1
    movsx rcx, cl
    add rax, rcx
    cvttsd2si ecx, xmm1
    movsx rcx, cx
    add rax, rcx
    cvttsd2si ecx, xmm1
    movsxd rcx, ecx
    add rax, rcx
    cvttsd2si rcx, xmm1
    add rax, rcx
    add rsp, 8
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, -0x2a
    call 2
    cvtsi2ss xmm0, rax
    cvttss2si rdx, xmm0
    xor ecx, ecx
    cvtsi2sd xmm0, rax
    cvttsd2si rax, xmm0
    add rdx, rax
    lea rax, [rcx + rdx - 0x96]
    add rax, 0x7b
    add rax, 0x7b
    add rax, 0x7b
    add rax, 0x7b
    add rsp, 8
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, -0x2a
    call 2
    mov rcx, rax
    cvtsi2ss xmm0, rcx
    cvttss2si rax, xmm0
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rax, 0x156
    add rsp, 8
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, -0x2a
    call 2
    mov rcx, rax
    cvtsi2ss xmm0, rcx
    cvttss2si rax, xmm0
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rax, 0x156
    add rsp, 8
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 0x102
    ret
