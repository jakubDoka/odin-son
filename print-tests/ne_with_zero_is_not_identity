=========== OPT LEVEL: none-quick ===========
opaque:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rsp, 8
    ret
2:
    sub rsp, 8
    mov rdi, 5
    call opaque
    cmp rax, 0
    setnz al
    movzx rax, al
    movzx eax, al
    add rsp, 8
    ret
=========== OPT LEVEL: none ===========
opaque:
    mov rax, rdi
    ret
2:
    sub rsp, 8
    mov rdi, 5
    call opaque
    cmp rax, 0
    setnz al
    movzx rax, al
    movzx eax, al
    add rsp, 8
    ret
=========== OPT LEVEL: mininal-quick ===========
opaque:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rsp, 8
    ret
2:
    sub rsp, 8
    mov rdi, 5
    call opaque
    cmp rax, 0
    setnz al
    movzx rax, al
    movzx eax, al
    add rsp, 8
    ret
=========== OPT LEVEL: mininal ===========
opaque:
    mov rax, rdi
    ret
2:
    sub rsp, 8
    mov rdi, 5
    call opaque
    cmp rax, 0
    setnz al
    movzx rax, al
    movzx eax, al
    add rsp, 8
    ret
=========== OPT LEVEL: moderate-quick ===========
opaque:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rsp, 8
    ret
2:
    sub rsp, 8
    mov rdi, 5
    call opaque
    cmp rax, 0
    setnz al
    movzx rax, al
    movzx eax, al
    add rsp, 8
    ret
=========== OPT LEVEL: moderate ===========
opaque:
    mov rax, rdi
    ret
2:
    sub rsp, 8
    mov rdi, 5
    call opaque
    cmp rax, 0
    setnz al
    movzx rax, al
    movzx eax, al
    add rsp, 8
    ret
=========== OPT LEVEL: all ===========
opaque:
    mov rax, rdi
    ret
2:
    sub rsp, 8
    mov rdi, 5
    call opaque
    cmp rax, 0
    setnz al
    movzx rax, al
    movzx eax, al
    add rsp, 8
    ret
=========== OPT LEVEL: aggresive ===========
opaque:
    mov rax, 1
    ret
