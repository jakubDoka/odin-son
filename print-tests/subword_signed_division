=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    xor edi, edi
    sub rdi, 0x64
    call +0x15
    mov cl, 3
    cbw
    idiv cl
    movsx rax, al
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, -0x64
    call +0x18
    mov cl, 3
    cbw
    idiv cl
    movsx rax, al
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, -0x64
    call +0x18
    mov cl, 3
    cbw
    idiv cl
    movsx rax, al
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, -0x64
    call +0x18
    mov cl, 3
    cbw
    idiv cl
    movsx rax, al
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, -0x21
    ret
opaque:
