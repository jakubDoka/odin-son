=========== OPT LEVEL: none ===========
main:
     mov rcx, 0x00
     mov rax, 0x00
002: add rcx, 0x01
     cmp rcx, 0x02
     jz :0
     cmp rcx, 0x01
     jz :1
     add rcx, 0x01
     mov rax, 0x01
     jmp :1

001: jmp :2

000: cmp rax, 0x00
     jz :3
     jmp :4

003: mov rcx, 0x00
007: add rcx, 0x01
     cmp rcx, 0x03
     jz :4
     cmp rcx, 0x02
     jz :5
     cmp rcx, 0x01
     jz :6
     add rcx, 0x01
     mov rax, 0x02
     jmp :5

006: jmp :5

005: jmp :7

004: cmp rax, 0x00
     jnz :8
     jmp :9

008: mov rax, 0x00
012: add rax, 0x01
     cmp rax, 0x03
     jz :10
     add rax, 0x01
     cmp rax, 0x04
     jz :11
     jmp :12

011: mov rax, 0x03
     jmp :9

010: mov rax, 0x01
009: ret
=========== OPT LEVEL: mininal ===========
main:
     mov rcx, 0x00
     mov rax, 0x00
002: add rcx, 0x01
     cmp rcx, 0x02
     jz :0
     cmp rcx, 0x01
     jz :1
     add rcx, 0x01
     mov rax, 0x01
     jmp :1

001: jmp :2

000: cmp rax, 0x00
     jz :3
     jmp :4

003: mov rcx, 0x00
007: add rcx, 0x01
     cmp rcx, 0x03
     jz :4
     cmp rcx, 0x02
     jz :5
     cmp rcx, 0x01
     jz :6
     add rcx, 0x01
     mov rax, 0x02
     jmp :5

006: jmp :5

005: jmp :7

004: cmp rax, 0x00
     jnz :8
     jmp :9

008: mov rax, 0x00
012: add rax, 0x01
     cmp rax, 0x03
     jz :10
     add rax, 0x01
     cmp rax, 0x04
     jz :11
     jmp :12

011: mov rax, 0x03
     jmp :9

010: mov rax, 0x01
009: ret
=========== OPT LEVEL: all ===========
main:
     mov rdx, 0x00
     mov rcx, 0x00
003: mov rax, rdx
     add rax, 0x01
     cmp rax, 0x02
     jz :0
     cmp rax, 0x01
     jz :1
     add rdx, 0x02
     mov rcx, 0x01
     jmp :2

001: mov rdx, rax
002: jmp :3

000: cmp rcx, 0x00
     jz :4
     jmp :5

004: mov rdx, 0x00
009: mov rax, rdx
     add rax, 0x01
     cmp rax, 0x03
     jz :5
     cmp rax, 0x02
     jz :6
     cmp rax, 0x01
     jz :7
     add rdx, 0x02
     mov rcx, 0x02
     jmp :8

007: jmp :6

006: mov rdx, rax
008: jmp :9

005: cmp rcx, 0x00
     jnz :10
     mov rax, rcx
     jmp :11

010: mov rcx, 0x00
014: mov rax, rcx
     add rax, 0x01
     cmp rax, 0x03
     jz :12
     add rcx, 0x02
     cmp rcx, 0x04
     jz :13
     jmp :14

013: mov rax, 0x03
     jmp :11

012: mov rax, 0x01
011: ret
