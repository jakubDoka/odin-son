=========== OPT LEVEL: none ===========
opaque:
     mov rax, rdi
     ret
main:
     sub rsp, 0x18
     mov rsi, 0x00
     lea rdi, [rsp]
     mov rdx, 0x10
     call :$set
     mov rdi, 0x03
     call :opaque
     mov [rsp], rax
     mov rdi, 0x04
     call :opaque
     mov [rsp+0x08], rax
     mov rdi, 0x01
     call :opaque
     cmp rax, 0x01
     jz :0
     jmp :1

000: mov rdi, 0x0A
     call :opaque
     mov [rsp], rax
001: mov rax, [rsp]
     mov rcx, [rsp+0x08]
     add rax, rcx
     mov [rsp+0x08], rax
     mov rax, [rsp]
     mov rcx, [rsp+0x08]
     add rax, rcx
     add rsp, 0x18
     ret
=========== OPT LEVEL: mininal ===========
opaque:
     mov rax, rdi
     ret
main:
     sub rsp, 0x18
     mov rsi, 0x00
     lea rdi, [rsp]
     mov rdx, 0x10
     call :$set
     mov rdi, 0x03
     call :opaque
     mov [rsp], rax
     mov rdi, 0x04
     call :opaque
     mov [rsp+0x08], rax
     mov rdi, 0x01
     call :opaque
     cmp rax, 0x01
     jz :0
     jmp :1

000: mov rdi, 0x0A
     call :opaque
     mov [rsp], rax
001: mov rax, [rsp]
     mov rcx, [rsp+0x08]
     add rax, rcx
     mov [rsp+0x08], rax
     mov rax, [rsp]
     mov rcx, [rsp+0x08]
     add rax, rcx
     add rsp, 0x18
     ret
=========== OPT LEVEL: moderate ===========
opaque:
     mov rax, rdi
     ret
main:
     sub rsp, 0x18
     mov qword ptr [rsp], 0x00
     mov qword ptr [rsp+0x08], 0x00
     mov rdi, 0x03
     call :opaque
     mov [rsp], rax
     mov rdi, 0x04
     call :opaque
     mov [rsp+0x08], rax
     mov rdi, 0x01
     call :opaque
     cmp rax, 0x01
     jz :0
     jmp :1

000: mov rdi, 0x0A
     call :opaque
     mov [rsp], rax
001: mov rcx, [rsp]
     add rcx, [rsp+0x08]
     mov [rsp+0x08], rcx
     mov rax, [rsp]
     add rax, rcx
     add rsp, 0x18
     ret
=========== OPT LEVEL: all ===========
opaque:
     mov rax, rdi
     ret
main:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rdi, 0x03
     call :opaque
     mov rbp, rax
     mov rdi, 0x04
     call :opaque
     mov rbx, rax
     mov rdi, 0x01
     call :opaque
     cmp rax, 0x01
     jz :0
     jmp :1

000: mov rdi, 0x0A
     call :opaque
     mov rbp, rax
001: mov rax, rbp
     add rbx, rax
     add rbx, rbp
     mov rax, rbx
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
