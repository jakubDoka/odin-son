=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    mov rdi, -0x2a
    call 2
    mov rdx, rax
    movsd.q xmm0, [rip + 0x86]
    movss.d xmm1, [rip + 0x87]
    cvtss2sd xmm1, xmm1
    cvtsd2ss xmm1, xmm1
    movsd.q xmm2, [rip + 0x7a]
    xor eax, eax
    cvtsd2ss xmm2, xmm2
    cvtsi2ss xmm3, rdx
    cvtss2sd xmm2, xmm2
    cvttss2si rcx, xmm1
    add rax, rcx
    cvtsi2sd xmm1, rdx
    cvttsd2si rcx, xmm1
    cvttss2si rdx, xmm3
    cvttsd2si rsi, xmm2
    add rax, rsi
    cvttsd2si esi, xmm0
    add rdx, rcx
    add rax, rdx
    cvttsd2si ecx, xmm0
    movsx rdx, sil
    add rax, rdx
    cvttsd2si edx, xmm0
    movsx rcx, cx
    add rax, rcx
    movsxd rcx, edx
    add rax, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rsp, 8
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, -0x2a
    call 2
    cvtsi2sd xmm0, rax
    cvtsi2ss xmm1, rax
    cvttss2si rax, xmm1
    cvttsd2si rcx, xmm0
    xor edx, edx
    add rax, rcx
    lea rax, [rdx + rax - 0x96]
    add rax, 0x7b
    add rax, 0x7b
    add rax, 0x7b
    add rax, 0x7b
    add rsp, 8
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, -0x2a
    call 2
    cvtsi2sd xmm0, rax
    cvtsi2ss xmm1, rax
    cvttss2si rax, xmm1
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rax, 0x156
    add rsp, 8
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, -0x2a
    call 2
    cvtsi2sd xmm0, rax
    cvtsi2ss xmm1, rax
    cvttss2si rax, xmm1
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rax, 0x156
    add rsp, 8
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 0x102
    ret
