=========== OPT LEVEL: none ===========
main:
    xor eax, eax
    xor edi, edi
1:
    cmp rdi, 4
    jnl 6
    xor ecx, ecx
    xor edx, edx
2:
    cmp rcx, 4
    jnl 3
    add rdx, rcx
    add rcx, 1
    jmp 2
3:
    xor ecx, ecx
    xor esi, esi
4:
    cmp rcx, 4
    jnl 5
    lea rsi, [rsi + rcx*2]
    add rcx, 1
    jmp 4
5:
    add rdx, rsi
    add rax, rdx
    add rdi, 1
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: mininal ===========
main:
    xor eax, eax
    xor edi, edi
1:
    cmp rdi, 4
    jnl 6
    xor ecx, ecx
    xor edx, edx
2:
    cmp rcx, 4
    jnl 3
    add rdx, rcx
    add rcx, 1
    jmp 2
3:
    xor ecx, ecx
    xor esi, esi
4:
    cmp rcx, 4
    jnl 5
    lea rsi, [rsi + rcx*2]
    add rcx, 1
    jmp 4
5:
    add rdx, rsi
    add rax, rdx
    add rdi, 1
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: moderate ===========
main:
    xor eax, eax
    xor edi, edi
1:
    cmp rdi, 4
    jnl 6
    xor ecx, ecx
    xor edx, edx
2:
    cmp rcx, 4
    jnl 3
    add rdx, rcx
    add rcx, 1
    jmp 2
3:
    xor ecx, ecx
    xor esi, esi
4:
    cmp rcx, 4
    jnl 5
    lea rsi, [rsi + rcx*2]
    add rcx, 1
    jmp 4
5:
    add rdx, rsi
    add rax, rdx
    add rdi, 1
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: all ===========
main:
    xor eax, eax
    xor edi, edi
1:
    cmp rdi, 4
    jnl 6
    xor ecx, ecx
    xor edx, edx
2:
    cmp rcx, 4
    jnl 3
    add rdx, rcx
    add rcx, 1
    jmp 2
3:
    xor ecx, ecx
    xor esi, esi
4:
    cmp rcx, 4
    jnl 5
    lea rsi, [rsi + rcx*2]
    add rcx, 1
    jmp 4
5:
    add rdx, rsi
    add rax, rdx
    add rdi, 1
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: aggresive ===========
