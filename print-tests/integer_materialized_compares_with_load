=========== OPT LEVEL: none ===========
sld:
    mov.d eax, [rsi]
    cmp edi, eax
    setz cl
    movzx rcx, cl
    xor edx, edx
    cmp edi, eax
    setnz sil
    movzx rsi, sil
    movzx ecx, cl
    add rdx, rcx
    cmp edi, eax
    setnge cl
    movzx rcx, cl
    movzx esi, sil
    lea rdx, [rdx + rsi*2]
    cmp edi, eax
    setnle sil
    movzx rsi, sil
    cmp edi, eax
    setng r8b
    movzx r8, r8b
    movzx ecx, cl
    lea rcx, [rdx + rcx*4]
    cmp edi, eax
    setnl dl
    movzx rdx, dl
    movzx esi, sil
    movzx eax, r8b
    lea rax, [rcx + rax*8]
    movzx ecx, dl
    imul rdx, rsi, 0x10
    add rax, rdx
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
3:
    mov.d eax, [rsi]
    cmp edi, eax
    setz cl
    movzx rcx, cl
    xor edx, edx
    cmp edi, eax
    setnz sil
    movzx rsi, sil
    movzx ecx, cl
    add rdx, rcx
    cmp edi, eax
    setnae cl
    movzx rcx, cl
    movzx esi, sil
    lea rdx, [rdx + rsi*2]
    cmp edi, eax
    setnbe sil
    movzx rsi, sil
    cmp edi, eax
    setna r8b
    movzx r8, r8b
    movzx ecx, cl
    lea rcx, [rdx + rcx*4]
    cmp edi, eax
    setnc dl
    movzx rdx, dl
    movzx esi, sil
    movzx eax, r8b
    lea rax, [rcx + rax*8]
    movzx ecx, dl
    imul rdx, rsi, 0x10
    add rax, rdx
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
4:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    sub rsp, 0x10
    mov rdi, 3
    mov.d [rsp], 5
    lea rsi, [rsp]
    call sld
    mov rbx, rax
    lea rsi, [rsp]
    mov rdi, 5
    call sld
    mov rbp, rax
    mov rdi, 9
    lea rsi, [rsp]
    call sld
    mov r12, rax
    movabs rax, 0xee6b2800
    mov.d [rsp + 0x4], eax
    mov rdi, 5
    lea rsi, [rsp + 0x4]
    call 3
    mov r13, rax
    lea rsi, [rsp + 0x4]
    movabs rdi, 0xee6b2800
    call 3
    mov r14, rax
    movabs rdi, 0xee6b2801
    lea rsi, [rsp + 0x4]
    call 3
    mov rdx, rbx
    add rdx, rbp
    mov rsi, r13
    add rsi, r14
    add rdx, r12
    xor ecx, ecx
    add rcx, rdx
    add rsi, rax
    add rcx, rsi
    mov rax, rcx
    add rsp, 0x10
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
sld:
    mov.d eax, [rsi]
    cmp edi, eax
    setz cl
    movzx rcx, cl
    xor edx, edx
    cmp edi, eax
    setnz sil
    movzx rsi, sil
    movzx ecx, cl
    add rdx, rcx
    cmp edi, eax
    setnge cl
    movzx rcx, cl
    movzx esi, sil
    lea rdx, [rdx + rsi*2]
    cmp edi, eax
    setnle sil
    movzx rsi, sil
    cmp edi, eax
    setng r8b
    movzx r8, r8b
    movzx ecx, cl
    lea rcx, [rdx + rcx*4]
    cmp edi, eax
    setnl dl
    movzx rdx, dl
    movzx esi, sil
    movzx eax, r8b
    lea rax, [rcx + rax*8]
    movzx ecx, dl
    imul rdx, rsi, 0x10
    add rax, rdx
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
3:
    mov.d eax, [rsi]
    cmp edi, eax
    setz cl
    movzx rcx, cl
    xor edx, edx
    cmp edi, eax
    setnz sil
    movzx rsi, sil
    movzx ecx, cl
    add rdx, rcx
    cmp edi, eax
    setnae cl
    movzx rcx, cl
    movzx esi, sil
    lea rdx, [rdx + rsi*2]
    cmp edi, eax
    setnbe sil
    movzx rsi, sil
    cmp edi, eax
    setna r8b
    movzx r8, r8b
    movzx ecx, cl
    lea rcx, [rdx + rcx*4]
    cmp edi, eax
    setnc dl
    movzx rdx, dl
    movzx esi, sil
    movzx eax, r8b
    lea rax, [rcx + rax*8]
    movzx ecx, dl
    imul rdx, rsi, 0x10
    add rax, rdx
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
4:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    sub rsp, 0x10
    mov rdi, 3
    mov.d [rsp], 5
    lea rsi, [rsp]
    call sld
    mov rbx, rax
    lea rsi, [rsp]
    mov rdi, 5
    call sld
    mov rbp, rax
    mov rdi, 9
    lea rsi, [rsp]
    call sld
    mov r12, rax
    movabs rax, 0xee6b2800
    mov.d [rsp + 0x4], eax
    mov rdi, 5
    lea rsi, [rsp + 0x4]
    call 3
    mov r13, rax
    lea rsi, [rsp + 0x4]
    movabs rdi, 0xee6b2800
    call 3
    mov r14, rax
    movabs rdi, 0xee6b2801
    lea rsi, [rsp + 0x4]
    call 3
    mov rdx, rbx
    add rdx, rbp
    mov rsi, r13
    add rsi, r14
    add rdx, r12
    xor ecx, ecx
    add rcx, rdx
    add rsi, rax
    add rcx, rsi
    mov rax, rcx
    add rsp, 0x10
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
sld:
    mov.d eax, [rsi]
    cmp edi, eax
    setnz cl
    movzx rcx, cl
    cmp edi, eax
    setz dl
    movzx rdx, dl
    movzx edx, dl
    cmp edi, eax
    setnge sil
    movzx rsi, sil
    movzx ecx, cl
    lea rcx, [rdx + rcx*2]
    cmp edi, eax
    setnle dl
    movzx rdx, dl
    cmp edi, eax
    setng r8b
    movzx r8, r8b
    movzx esi, sil
    lea rcx, [rcx + rsi*4]
    cmp edi, eax
    setnl sil
    movzx rsi, sil
    movzx edx, dl
    movzx eax, r8b
    lea rax, [rcx + rax*8]
    movzx ecx, sil
    imul rdx, rdx, 0x10
    add rax, rdx
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
3:
    mov.d eax, [rsi]
    cmp edi, eax
    setnz cl
    movzx rcx, cl
    cmp edi, eax
    setz dl
    movzx rdx, dl
    movzx edx, dl
    cmp edi, eax
    setnae sil
    movzx rsi, sil
    movzx ecx, cl
    lea rcx, [rdx + rcx*2]
    cmp edi, eax
    setnbe dl
    movzx rdx, dl
    cmp edi, eax
    setna r8b
    movzx r8, r8b
    movzx esi, sil
    lea rcx, [rcx + rsi*4]
    cmp edi, eax
    setnc sil
    movzx rsi, sil
    movzx edx, dl
    movzx eax, r8b
    lea rax, [rcx + rax*8]
    movzx ecx, sil
    imul rdx, rdx, 0x10
    add rax, rdx
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
4:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    sub rsp, 0x10
    mov rdi, 3
    mov.d [rsp], 5
    lea rsi, [rsp]
    call sld
    mov rbx, rax
    lea rsi, [rsp]
    mov rdi, 5
    call sld
    mov rbp, rax
    mov rdi, 9
    lea rsi, [rsp]
    call sld
    mov r12, rax
    movabs rax, 0xee6b2800
    mov.d [rsp + 0x4], eax
    mov rdi, 5
    lea rsi, [rsp + 0x4]
    call 3
    mov r13, rax
    lea rsi, [rsp + 0x4]
    movabs rdi, 0xee6b2800
    call 3
    mov r14, rax
    movabs rdi, 0xee6b2801
    lea rsi, [rsp + 0x4]
    call 3
    mov rsi, r13
    add rsi, r14
    mov rdx, rbx
    add rdx, rbp
    add rdx, r12
    add rsi, rax
    add rdx, rsi
    mov rax, rdx
    add rsp, 0x10
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: all ===========
sld:
    mov.d eax, [rsi]
    cmp edi, eax
    setnz cl
    movzx rcx, cl
    cmp edi, eax
    setz dl
    movzx rdx, dl
    movzx edx, dl
    cmp edi, eax
    setnge sil
    movzx rsi, sil
    movzx ecx, cl
    lea rcx, [rdx + rcx*2]
    cmp edi, eax
    setnle dl
    movzx rdx, dl
    cmp edi, eax
    setng r8b
    movzx r8, r8b
    movzx esi, sil
    lea rcx, [rcx + rsi*4]
    cmp edi, eax
    setnl sil
    movzx rsi, sil
    movzx edx, dl
    movzx eax, r8b
    lea rax, [rcx + rax*8]
    movzx ecx, sil
    imul rdx, rdx, 0x10
    add rax, rdx
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
3:
    mov.d eax, [rsi]
    cmp edi, eax
    setnz cl
    movzx rcx, cl
    cmp edi, eax
    setz dl
    movzx rdx, dl
    movzx edx, dl
    cmp edi, eax
    setnae sil
    movzx rsi, sil
    movzx ecx, cl
    lea rcx, [rdx + rcx*2]
    cmp edi, eax
    setnbe dl
    movzx rdx, dl
    cmp edi, eax
    setna r8b
    movzx r8, r8b
    movzx esi, sil
    lea rcx, [rcx + rsi*4]
    cmp edi, eax
    setnc sil
    movzx rsi, sil
    movzx edx, dl
    movzx eax, r8b
    lea rax, [rcx + rax*8]
    movzx ecx, sil
    imul rdx, rdx, 0x10
    add rax, rdx
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
4:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    sub rsp, 0x10
    mov rdi, 3
    mov.d [rsp], 5
    lea rsi, [rsp]
    call sld
    mov rbx, rax
    lea rsi, [rsp]
    mov rdi, 5
    call sld
    mov rbp, rax
    mov rdi, 9
    lea rsi, [rsp]
    call sld
    mov r12, rax
    movabs rax, 0xee6b2800
    mov.d [rsp + 0x4], eax
    mov rdi, 5
    lea rsi, [rsp + 0x4]
    call 3
    mov r13, rax
    lea rsi, [rsp + 0x4]
    movabs rdi, 0xee6b2800
    call 3
    mov r14, rax
    movabs rdi, 0xee6b2801
    lea rsi, [rsp + 0x4]
    call 3
    mov rsi, r13
    add rsi, r14
    mov rdx, rbx
    add rdx, rbp
    add rdx, r12
    add rsi, rax
    add rdx, rsi
    mov rax, rdx
    add rsp, 0x10
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
