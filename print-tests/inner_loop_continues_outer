main:
     push rbx
     push rbp
     mov rcx, 0x00
     mov rsi, rcx
     mov rax, rcx
     jmp :0

000: mov rdx, 0x01
     add rsi, rdx
     mov rbx, 0x05
     mov rbp, rsi
     cmp rbp, rbx
     setz bpl
     movzx rbp, bpl
     test rbp, rbp
     jz :1
     jmp :2

001: mov rbp, rcx
     add rbp, rdx
     cmp rbp, rbx
     setz bpl
     movzx rbp, bpl
     test rbp, rbp
     jz :3
     jmp :4

003: mov rbp, rax
     mov rbx, rcx
     jmp :5

005: add rbx, rdx
     add rbp, rdx
     mov rax, 0x02
     cmp rax, rbx
     setz al
     movzx rax, al
     test rax, rax
     jz :6
     jmp :7

006: jmp :5

007: jmp :8

004: mov rbp, rax
     jmp :8

008: mov rax, rbp
     jmp :0

002: pop rbp
     pop rbx
     ret
