=========== OPT LEVEL: all ===========
main:
    movz x3, #0, #0
    movz x1, #0, #0
1:
    movz x2, #0, #0
2:
    movz x5, #1, #0
    movz x4, #2, #0
    add x0, x3, x5
    add x2, x2, x5
    cmp w2, w4
    mov x3, x0
    b.ne 2
    add x3, x1, x4
    add x1, x1, x5
    movz x2, #5, #0
    cmp w3, w2
    mov x3, x0
    b.ne 1
    ret
