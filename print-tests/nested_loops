=========== OPT LEVEL: none ===========
main:
    xor edx, edx
    xor esi, esi
1:
    mov rax, rsi
    cmp rdx, 3
    jz 4
    xor ecx, ecx
    mov rsi, rax
2:
    cmp rcx, 3
    jz 3
    mov rax, rdx
    imul rax, rcx
    add rsi, rax
    add rcx, 1
    jmp 2
3:
    add rdx, 1
    jmp 1
4:
    ret
=========== OPT LEVEL: mininal ===========
main:
    xor edx, edx
    xor esi, esi
1:
    mov rax, rsi
    cmp rdx, 3
    jz 4
    xor ecx, ecx
    mov rsi, rax
2:
    cmp rcx, 3
    jz 3
    mov rax, rdx
    imul rax, rcx
    add rsi, rax
    add rcx, 1
    jmp 2
3:
    add rdx, 1
    jmp 1
4:
    ret
=========== OPT LEVEL: moderate ===========
main:
    xor edx, edx
    xor esi, esi
1:
    mov rax, rsi
    cmp rdx, 3
    jz 4
    xor ecx, ecx
    mov rsi, rax
2:
    cmp rcx, 3
    jz 3
    mov rax, rdx
    imul rax, rcx
    add rsi, rax
    add rcx, 1
    jmp 2
3:
    add rdx, 1
    jmp 1
4:
    ret
=========== OPT LEVEL: all ===========
main:
    xor edx, edx
    xor edi, edi
1:
    xor esi, esi
2:
    mov rax, rdx
    mov rcx, rdi
    imul rcx, rsi
    mov rdx, rax
    add rdx, rcx
    mov rcx, rsi
    add rcx, 1
    cmp rsi, 3
    mov rsi, rcx
    jnz 2
    add rdi, 1
    cmp rdi, 3
    mov rdx, rax
    jnz 1
    ret
=========== OPT LEVEL: aggresive ===========
