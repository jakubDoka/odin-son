=========== OPT LEVEL: none ===========
main:
    push rbx
    mov rdi, 0xa
    call fib i32
    mov rbx, rax
    mov rdi, 0xa
    call fib int
    mov rcx, rax
    movsxd rax, ebx
    add rax, rcx
    pop rbx
    ret
fib i32:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp ebx, 2
    jle 3
    mov rax, rbx
    sub eax, 1
    mov rdi, rax
    call fib i32
    mov rbp, rax
    sub ebx, 2
    mov rdi, rbx
    call fib i32
    add ebp, eax
    jmp 4
3:
    mov rbp, 1
4:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
fib int:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jle 6
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call fib int
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call fib int
    add rbp, rax
    jmp 7
6:
    mov rbp, 1
7:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
main:
    push rbx
    mov rdi, 0xa
    call fib i32
    mov rbx, rax
    mov rdi, 0xa
    call fib int
    mov rcx, rax
    movsxd rax, ebx
    add rax, rcx
    pop rbx
    ret
fib i32:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp ebx, 2
    jle 3
    mov rax, rbx
    sub eax, 1
    mov rdi, rax
    call fib i32
    mov rbp, rax
    sub ebx, 2
    mov rdi, rbx
    call fib i32
    add ebp, eax
    jmp 4
3:
    mov rbp, 1
4:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
fib int:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jle 6
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call fib int
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call fib int
    add rbp, rax
    jmp 7
6:
    mov rbp, 1
7:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
main:
    push rbx
    mov rdi, 0xa
    call fib i32
    mov rbx, rax
    mov rdi, 0xa
    call fib int
    mov rcx, rax
    movsxd rax, ebx
    add rax, rcx
    pop rbx
    ret
fib i32:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp ebx, 2
    jle 3
    mov rax, rbx
    sub eax, 1
    mov rdi, rax
    call fib i32
    mov rbp, rax
    sub ebx, 2
    mov rdi, rbx
    call fib i32
    add ebp, eax
    jmp 4
3:
    mov rbp, 1
4:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
fib int:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jle 6
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call fib int
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call fib int
    add rbp, rax
    jmp 7
6:
    mov rbp, 1
7:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: all ===========
main:
    push rbx
    mov rdi, 0xa
    call fib i32
    mov rbx, rax
    mov rdi, 0xa
    call fib int
    mov rcx, rax
    movsxd rax, ebx
    add rax, rcx
    pop rbx
    ret
fib i32:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp ebx, 2
    jle 3
    mov rax, rbx
    sub eax, 1
    mov rdi, rax
    call fib i32
    mov rbp, rax
    sub ebx, 2
    mov rdi, rbx
    call fib i32
    add ebp, eax
    jmp 4
3:
    mov rbp, 1
4:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
fib int:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jle 6
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call fib int
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call fib int
    add rbp, rax
    jmp 7
6:
    mov rbp, 1
7:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
