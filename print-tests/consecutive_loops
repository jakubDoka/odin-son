main:
     push rcx
     push rbp
     mov rax, 0x00
     mov rcx, 0x01
     test rax, rax
     jz :0
     jmp :1

000: jmp :2

001: mov rbx, rax
     jmp :3

003: add rbx, rcx
     mov rdx, rbx
     cmp rdx, rcx
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :4
     jmp :5

004: jmp :3

005: jmp :2

002: mov rsi, rax
     jmp :6

006: add rsi, rcx
     mov rdx, 0x03
     mov rbx, rsi
     cmp rbx, rdx
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :7
     jmp :8

007: mov rbp, rax
     jmp :9

009: add rbp, rcx
     mov rbx, rbp
     cmp rbx, rdx
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :10
     jmp :11

010: jmp :9

011: mov rbp, rax
     jmp :12

012: add rbp, rcx
     mov rbx, 0x02
     cmp rbx, rbp
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :13
     jmp :14

013: mov rbx, rbp
     cmp rbx, rdx
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :15
     jmp :16

015: jmp :12

016: jmp :6

014: jmp :17

008: jmp :17

017: pop rbp
     pop rcx
     ret
