=========== OPT LEVEL: none ===========
s8:
     xor eax, eax
     cmp dil, sil
     setz cl
     movzx rcx, cl
     movzx ecx, cl
     add rax, rcx
     cmp dil, sil
     setnz cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*2]
     cmp dil, sil
     setl cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*4]
     cmp dil, sil
     setle cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*8]
     cmp dil, sil
     setnle cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x10
     add rax, rcx
     cmp dil, sil
     setnl cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x20
     add rax, rcx
     ret
s16:
     xor eax, eax
     cmp di, si
     setz cl
     movzx rcx, cl
     movzx ecx, cl
     add rax, rcx
     cmp di, si
     setnz cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*2]
     cmp di, si
     setl cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*4]
     cmp di, si
     setle cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*8]
     cmp di, si
     setnle cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x10
     add rax, rcx
     cmp di, si
     setnl cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x20
     add rax, rcx
     ret
s32:
     xor eax, eax
     cmp edi, esi
     setz cl
     movzx rcx, cl
     movzx ecx, cl
     add rax, rcx
     cmp edi, esi
     setnz cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*2]
     cmp edi, esi
     setl cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*4]
     cmp edi, esi
     setle cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*8]
     cmp edi, esi
     setnle cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x10
     add rax, rcx
     cmp edi, esi
     setnl cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x20
     add rax, rcx
     ret
s64:
     xor eax, eax
     cmp rdi, rsi
     setz cl
     movzx rcx, cl
     movzx ecx, cl
     add rax, rcx
     cmp rdi, rsi
     setnz cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*2]
     cmp rdi, rsi
     setl cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*4]
     cmp rdi, rsi
     setle cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*8]
     cmp rdi, rsi
     setnle cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x10
     add rax, rcx
     cmp rdi, rsi
     setnl cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x20
     add rax, rcx
     ret
sint:
     xor eax, eax
     cmp rdi, rsi
     setz cl
     movzx rcx, cl
     movzx ecx, cl
     add rax, rcx
     cmp rdi, rsi
     setnz cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*2]
     cmp rdi, rsi
     setl cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*4]
     cmp rdi, rsi
     setle cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*8]
     cmp rdi, rsi
     setnle cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x10
     add rax, rcx
     cmp rdi, rsi
     setnl cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x20
     add rax, rcx
     ret
main:
     push rbx
     push rbp
     push r12
     push r13
     push r14
     push r15
     sub rsp, 0x48
     xor eax, eax
     sub al, 0x01
     mov sil, 0x01
     mov rdi, rax
     call :s8
     mov rbx, rax
     mov sil, 0x05
     mov dil, 0x05
     call :s8
     mov rbp, rax
     mov dil, 0x07
     xor eax, eax
     sub al, 0x03
     mov rsi, rax
     call :s8
     mov r12, rax
     xor eax, eax
     sub ax, 0x01
     mov rsi, 0x01
     mov rdi, rax
     call :s16
     mov r13, rax
     mov rsi, 0x05
     mov rdi, 0x05
     call :s16
     mov r14, rax
     mov rdi, 0x07
     xor eax, eax
     sub ax, 0x03
     mov rsi, rax
     call :s16
     mov r15, rax
     xor eax, eax
     sub eax, 0x01
     mov rsi, 0x01
     mov rdi, rax
     call :s32
     mov [rsp], rax
     mov rsi, 0x05
     mov rdi, 0x05
     call :s32
     mov [rsp+0x08], rax
     mov rdi, 0x07
     xor eax, eax
     sub eax, 0x03
     mov rsi, rax
     call :s32
     mov [rsp+0x10], rax
     xor eax, eax
     sub rax, 0x01
     mov [rsp+0x18], rax
     mov rdi, [rsp+0x18]
     mov rsi, 0x01
     call :s64
     mov [rsp+0x20], rax
     mov rsi, 0x05
     mov rdi, 0x05
     call :s64
     mov [rsp+0x28], rax
     xor eax, eax
     sub rax, 0x03
     mov [rsp+0x30], rax
     mov rsi, [rsp+0x30]
     mov rdi, 0x07
     call :s64
     mov [rsp+0x38], rax
     mov rdi, [rsp+0x18]
     mov rsi, 0x01
     call :sint
     mov [rsp+0x18], rax
     mov rsi, 0x05
     mov rdi, 0x05
     call :sint
     mov [rsp+0x40], rax
     mov rsi, [rsp+0x30]
     mov rdi, 0x07
     call :sint
     mov rcx, rax
     mov rax, rbx
     add rax, rbp
     add rax, r12
     mov rdx, rax
     xor eax, eax
     add rdx, rax
     mov rax, rdx
     mov rdx, r13
     add rdx, r14
     mov rsi, rdx
     add rsi, r15
     add rax, rsi
     mov rsi, [rsp]
     mov rdx, [rsp+0x08]
     add rsi, rdx
     mov rdx, [rsp+0x10]
     add rsi, rdx
     add rax, rsi
     mov rsi, [rsp+0x20]
     mov rdx, [rsp+0x28]
     add rsi, rdx
     mov rdx, [rsp+0x38]
     add rsi, rdx
     mov rdx, rax
     add rdx, rsi
     mov rsi, [rsp+0x18]
     mov rax, [rsp+0x40]
     add rsi, rax
     add rsi, rcx
     add rdx, rsi
     mov rax, rdx
     add rsp, 0x48
     pop r15
     pop r14
     pop r13
     pop r12
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: mininal ===========
s8:
     xor eax, eax
     cmp dil, sil
     setz cl
     movzx rcx, cl
     movzx ecx, cl
     add rax, rcx
     cmp dil, sil
     setnz cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*2]
     cmp dil, sil
     setl cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*4]
     cmp dil, sil
     setle cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*8]
     cmp dil, sil
     setnle cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x10
     add rax, rcx
     cmp dil, sil
     setnl cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x20
     add rax, rcx
     ret
s16:
     xor eax, eax
     cmp di, si
     setz cl
     movzx rcx, cl
     movzx ecx, cl
     add rax, rcx
     cmp di, si
     setnz cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*2]
     cmp di, si
     setl cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*4]
     cmp di, si
     setle cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*8]
     cmp di, si
     setnle cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x10
     add rax, rcx
     cmp di, si
     setnl cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x20
     add rax, rcx
     ret
s32:
     xor eax, eax
     cmp edi, esi
     setz cl
     movzx rcx, cl
     movzx ecx, cl
     add rax, rcx
     cmp edi, esi
     setnz cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*2]
     cmp edi, esi
     setl cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*4]
     cmp edi, esi
     setle cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*8]
     cmp edi, esi
     setnle cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x10
     add rax, rcx
     cmp edi, esi
     setnl cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x20
     add rax, rcx
     ret
s64:
     xor eax, eax
     cmp rdi, rsi
     setz cl
     movzx rcx, cl
     movzx ecx, cl
     add rax, rcx
     cmp rdi, rsi
     setnz cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*2]
     cmp rdi, rsi
     setl cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*4]
     cmp rdi, rsi
     setle cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*8]
     cmp rdi, rsi
     setnle cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x10
     add rax, rcx
     cmp rdi, rsi
     setnl cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x20
     add rax, rcx
     ret
sint:
     xor eax, eax
     cmp rdi, rsi
     setz cl
     movzx rcx, cl
     movzx ecx, cl
     add rax, rcx
     cmp rdi, rsi
     setnz cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*2]
     cmp rdi, rsi
     setl cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*4]
     cmp rdi, rsi
     setle cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*8]
     cmp rdi, rsi
     setnle cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x10
     add rax, rcx
     cmp rdi, rsi
     setnl cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x20
     add rax, rcx
     ret
main:
     push rbx
     push rbp
     push r12
     push r13
     push r14
     push r15
     sub rsp, 0x48
     mov dil, 0xFF
     mov sil, 0x01
     call :s8
     mov rbx, rax
     mov sil, 0x05
     mov dil, 0x05
     call :s8
     mov rbp, rax
     mov dil, 0x07
     mov sil, 0xFD
     call :s8
     mov r12, rax
     mov rdi, 0xFFFFFFFFFFFFFFFF
     mov rsi, 0x01
     call :s16
     mov r13, rax
     mov rsi, 0x05
     mov rdi, 0x05
     call :s16
     mov r14, rax
     mov rdi, 0x07
     mov rsi, 0xFFFFFFFFFFFFFFFD
     call :s16
     mov r15, rax
     mov rdi, 0xFFFFFFFFFFFFFFFF
     mov rsi, 0x01
     call :s32
     mov [rsp], rax
     mov rsi, 0x05
     mov rdi, 0x05
     call :s32
     mov [rsp+0x08], rax
     mov rdi, 0x07
     mov rsi, 0xFFFFFFFFFFFFFFFD
     call :s32
     mov [rsp+0x10], rax
     mov rdi, 0xFFFFFFFFFFFFFFFF
     mov rsi, 0x01
     call :s64
     mov [rsp+0x18], rax
     mov rsi, 0x05
     mov rdi, 0x05
     call :s64
     mov [rsp+0x20], rax
     mov rdi, 0x07
     mov rsi, 0xFFFFFFFFFFFFFFFD
     call :s64
     mov [rsp+0x28], rax
     mov rdi, 0xFFFFFFFFFFFFFFFF
     mov rsi, 0x01
     call :sint
     mov [rsp+0x30], rax
     mov rsi, 0x05
     mov rdi, 0x05
     call :sint
     mov [rsp+0x38], rax
     mov rdi, 0x07
     mov rsi, 0xFFFFFFFFFFFFFFFD
     call :sint
     mov rcx, rax
     mov rax, rbx
     add rax, rbp
     add rax, r12
     xor edx, edx
     add rdx, rax
     mov rax, rdx
     mov rdx, r13
     add rdx, r14
     mov rsi, rdx
     add rsi, r15
     add rax, rsi
     mov rsi, [rsp]
     mov rdx, [rsp+0x08]
     add rsi, rdx
     mov rdx, [rsp+0x10]
     add rsi, rdx
     add rax, rsi
     mov rsi, [rsp+0x18]
     mov rdx, [rsp+0x20]
     add rsi, rdx
     mov rdx, [rsp+0x28]
     add rsi, rdx
     mov rdi, rax
     add rdi, rsi
     mov rdx, [rsp+0x30]
     mov rax, [rsp+0x38]
     add rdx, rax
     add rdx, rcx
     add rdi, rdx
     mov rax, rdi
     add rsp, 0x48
     pop r15
     pop r14
     pop r13
     pop r12
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: moderate ===========
s8:
     cmp dil, sil
     setz al
     movzx rax, al
     movzx eax, al
     cmp dil, sil
     setnz cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*2]
     cmp dil, sil
     setl cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*4]
     cmp dil, sil
     setle cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*8]
     cmp dil, sil
     setnle cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x10
     add rax, rcx
     cmp dil, sil
     setnl cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x20
     add rax, rcx
     ret
s16:
     cmp di, si
     setz al
     movzx rax, al
     movzx eax, al
     cmp di, si
     setnz cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*2]
     cmp di, si
     setl cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*4]
     cmp di, si
     setle cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*8]
     cmp di, si
     setnle cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x10
     add rax, rcx
     cmp di, si
     setnl cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x20
     add rax, rcx
     ret
s32:
     cmp edi, esi
     setz al
     movzx rax, al
     movzx eax, al
     cmp edi, esi
     setnz cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*2]
     cmp edi, esi
     setl cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*4]
     cmp edi, esi
     setle cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*8]
     cmp edi, esi
     setnle cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x10
     add rax, rcx
     cmp edi, esi
     setnl cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x20
     add rax, rcx
     ret
s64:
     cmp rdi, rsi
     setz al
     movzx rax, al
     movzx eax, al
     cmp rdi, rsi
     setnz cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*2]
     cmp rdi, rsi
     setl cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*4]
     cmp rdi, rsi
     setle cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*8]
     cmp rdi, rsi
     setnle cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x10
     add rax, rcx
     cmp rdi, rsi
     setnl cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x20
     add rax, rcx
     ret
sint:
     cmp rdi, rsi
     setz al
     movzx rax, al
     movzx eax, al
     cmp rdi, rsi
     setnz cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*2]
     cmp rdi, rsi
     setl cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*4]
     cmp rdi, rsi
     setle cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*8]
     cmp rdi, rsi
     setnle cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x10
     add rax, rcx
     cmp rdi, rsi
     setnl cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x20
     add rax, rcx
     ret
main:
     push rbx
     push rbp
     push r12
     push r13
     push r14
     push r15
     sub rsp, 0x48
     mov dil, 0xFF
     mov sil, 0x01
     call :s8
     mov rbx, rax
     mov sil, 0x05
     mov dil, 0x05
     call :s8
     mov rbp, rax
     mov dil, 0x07
     mov sil, 0xFD
     call :s8
     mov r12, rax
     mov rdi, 0xFFFFFFFFFFFFFFFF
     mov rsi, 0x01
     call :s16
     mov r13, rax
     mov rsi, 0x05
     mov rdi, 0x05
     call :s16
     mov r14, rax
     mov rdi, 0x07
     mov rsi, 0xFFFFFFFFFFFFFFFD
     call :s16
     mov r15, rax
     mov rdi, 0xFFFFFFFFFFFFFFFF
     mov rsi, 0x01
     call :s32
     mov [rsp], rax
     mov rsi, 0x05
     mov rdi, 0x05
     call :s32
     mov [rsp+0x08], rax
     mov rdi, 0x07
     mov rsi, 0xFFFFFFFFFFFFFFFD
     call :s32
     mov [rsp+0x10], rax
     mov rdi, 0xFFFFFFFFFFFFFFFF
     mov rsi, 0x01
     call :s64
     mov [rsp+0x18], rax
     mov rsi, 0x05
     mov rdi, 0x05
     call :s64
     mov [rsp+0x20], rax
     mov rdi, 0x07
     mov rsi, 0xFFFFFFFFFFFFFFFD
     call :s64
     mov [rsp+0x28], rax
     mov rdi, 0xFFFFFFFFFFFFFFFF
     mov rsi, 0x01
     call :sint
     mov [rsp+0x30], rax
     mov rsi, 0x05
     mov rdi, 0x05
     call :sint
     mov [rsp+0x38], rax
     mov rdi, 0x07
     mov rsi, 0xFFFFFFFFFFFFFFFD
     call :sint
     mov rcx, rax
     mov rax, rbx
     add rax, rbp
     add rax, r12
     mov rdx, r13
     add rdx, r14
     mov rsi, rdx
     add rsi, r15
     add rax, rsi
     mov rsi, [rsp]
     mov rdx, [rsp+0x08]
     add rsi, rdx
     mov rdx, [rsp+0x10]
     add rsi, rdx
     add rax, rsi
     mov rsi, [rsp+0x18]
     mov rdx, [rsp+0x20]
     add rsi, rdx
     mov rdx, [rsp+0x28]
     add rsi, rdx
     mov rdx, rax
     add rdx, rsi
     mov rsi, [rsp+0x30]
     mov rax, [rsp+0x38]
     add rsi, rax
     add rsi, rcx
     add rdx, rsi
     mov rax, rdx
     add rsp, 0x48
     pop r15
     pop r14
     pop r13
     pop r12
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: all ===========
s8:
     cmp dil, sil
     setz al
     movzx rax, al
     movzx eax, al
     cmp dil, sil
     setnz cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*2]
     cmp dil, sil
     setl cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*4]
     cmp dil, sil
     setle cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*8]
     cmp dil, sil
     setnle cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x10
     add rax, rcx
     cmp dil, sil
     setnl cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x20
     add rax, rcx
     ret
s16:
     cmp di, si
     setz al
     movzx rax, al
     movzx eax, al
     cmp di, si
     setnz cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*2]
     cmp di, si
     setl cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*4]
     cmp di, si
     setle cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*8]
     cmp di, si
     setnle cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x10
     add rax, rcx
     cmp di, si
     setnl cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x20
     add rax, rcx
     ret
s32:
     cmp edi, esi
     setz al
     movzx rax, al
     movzx eax, al
     cmp edi, esi
     setnz cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*2]
     cmp edi, esi
     setl cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*4]
     cmp edi, esi
     setle cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*8]
     cmp edi, esi
     setnle cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x10
     add rax, rcx
     cmp edi, esi
     setnl cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x20
     add rax, rcx
     ret
s64:
     cmp rdi, rsi
     setz al
     movzx rax, al
     movzx eax, al
     cmp rdi, rsi
     setnz cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*2]
     cmp rdi, rsi
     setl cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*4]
     cmp rdi, rsi
     setle cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*8]
     cmp rdi, rsi
     setnle cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x10
     add rax, rcx
     cmp rdi, rsi
     setnl cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x20
     add rax, rcx
     ret
sint:
     cmp rdi, rsi
     setz al
     movzx rax, al
     movzx eax, al
     cmp rdi, rsi
     setnz cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*2]
     cmp rdi, rsi
     setl cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*4]
     cmp rdi, rsi
     setle cl
     movzx rcx, cl
     movzx ecx, cl
     lea rax, [rax+rcx*8]
     cmp rdi, rsi
     setnle cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x10
     add rax, rcx
     cmp rdi, rsi
     setnl cl
     movzx rcx, cl
     movzx ecx, cl
     imul rcx, rcx, 0x20
     add rax, rcx
     ret
main:
     push rbx
     push rbp
     push r12
     push r13
     push r14
     push r15
     sub rsp, 0x48
     mov dil, 0xFF
     mov sil, 0x01
     call :s8
     mov rbx, rax
     mov sil, 0x05
     mov dil, 0x05
     call :s8
     mov rbp, rax
     mov dil, 0x07
     mov sil, 0xFD
     call :s8
     mov r12, rax
     mov rdi, 0xFFFFFFFFFFFFFFFF
     mov rsi, 0x01
     call :s16
     mov r13, rax
     mov rsi, 0x05
     mov rdi, 0x05
     call :s16
     mov r14, rax
     mov rdi, 0x07
     mov rsi, 0xFFFFFFFFFFFFFFFD
     call :s16
     mov r15, rax
     mov rdi, 0xFFFFFFFFFFFFFFFF
     mov rsi, 0x01
     call :s32
     mov [rsp], rax
     mov rsi, 0x05
     mov rdi, 0x05
     call :s32
     mov [rsp+0x08], rax
     mov rdi, 0x07
     mov rsi, 0xFFFFFFFFFFFFFFFD
     call :s32
     mov [rsp+0x10], rax
     mov rdi, 0xFFFFFFFFFFFFFFFF
     mov rsi, 0x01
     call :s64
     mov [rsp+0x18], rax
     mov rsi, 0x05
     mov rdi, 0x05
     call :s64
     mov [rsp+0x20], rax
     mov rdi, 0x07
     mov rsi, 0xFFFFFFFFFFFFFFFD
     call :s64
     mov [rsp+0x28], rax
     mov rdi, 0xFFFFFFFFFFFFFFFF
     mov rsi, 0x01
     call :sint
     mov [rsp+0x30], rax
     mov rsi, 0x05
     mov rdi, 0x05
     call :sint
     mov [rsp+0x38], rax
     mov rdi, 0x07
     mov rsi, 0xFFFFFFFFFFFFFFFD
     call :sint
     mov rcx, rax
     mov rax, rbx
     add rax, rbp
     add rax, r12
     mov rdx, r13
     add rdx, r14
     mov rsi, rdx
     add rsi, r15
     add rax, rsi
     mov rsi, [rsp]
     mov rdx, [rsp+0x08]
     add rsi, rdx
     mov rdx, [rsp+0x10]
     add rsi, rdx
     add rax, rsi
     mov rsi, [rsp+0x18]
     mov rdx, [rsp+0x20]
     add rsi, rdx
     mov rdx, [rsp+0x28]
     add rsi, rdx
     mov rdx, rax
     add rdx, rsi
     mov rsi, [rsp+0x30]
     mov rax, [rsp+0x38]
     add rsi, rax
     add rsi, rcx
     add rdx, rsi
     mov rax, rdx
     add rsp, 0x48
     pop r15
     pop r14
     pop r13
     pop r12
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: aggresive ===========
