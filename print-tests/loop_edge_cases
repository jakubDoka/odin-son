=========== OPT LEVEL: None ===========
main:
     push rbx
     push rbp
     mov rcx, 0x00
     mov rsi, 0x00
     mov rbp, 0x00
     jmp :0

000: mov rax, 0x01
     add rsi, rax
     mov rdx, 0x02
     cmp rsi, rdx
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :1
     jmp :2

001: cmp rsi, rax
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :3
     jmp :4

003: add rsi, rax
     mov rbp, 0x01
     jmp :5

004: jmp :5

005: jmp :0

002: cmp rbp, rcx
     setz bl
     movzx rbx, bl
     mov rsi, 0x03
     test rbx, rbx
     jz :6
     jmp :7

006: jmp :8

007: mov rdi, 0x00
     jmp :9

009: add rdi, rax
     cmp rdi, rsi
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :10
     jmp :11

010: cmp rdi, rdx
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :12
     jmp :13

012: cmp rdi, rax
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :14
     jmp :15

014: add rdi, rax
     mov rbp, 0x02
     jmp :16

015: jmp :17

013: jmp :17

017: jmp :16

016: jmp :9

011: jmp :8

008: cmp rbp, rcx
     setnz cl
     movzx rcx, cl
     test rcx, rcx
     jz :18
     jmp :19

018: mov rax, rbp
     jmp :20

019: mov rdx, 0x00
     jmp :21

021: add rdx, rax
     cmp rdx, rsi
     setz cl
     movzx rcx, cl
     test rcx, rcx
     jz :22
     jmp :23

022: add rdx, rax
     mov rcx, 0x04
     cmp rcx, rdx
     setz cl
     movzx rcx, cl
     test rcx, rcx
     jz :24
     jmp :25

024: jmp :21

025: mov rax, 0x03
     jmp :26

023: mov rax, 0x01
     jmp :26

026: jmp :20

020: pop rbp
     pop rbx
     ret
=========== OPT LEVEL: Full ===========
main:
     push rbx
     push rbp
     mov rax, 0x00
     mov rdi, 0x00
     mov rsi, 0x00
     jmp :0

000: mov rdx, 0x01
     mov rbx, 0x02
     mov rcx, rdi
     add rcx, 0x01
     cmp rcx, rbx
     setz bpl
     movzx rbp, bpl
     test rbp, rbp
     jz :1
     jmp :2

001: cmp rcx, rdx
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :3
     jmp :4

003: add rdi, 0x02
     mov rsi, 0x01
     jmp :5

004: mov rdi, rcx
     jmp :5

005: jmp :0

002: cmp rsi, rax
     setz bpl
     movzx rbp, bpl
     mov rcx, 0x03
     test rbp, rbp
     jz :6
     jmp :7

006: jmp :8

007: mov rdi, 0x00
     mov r8, rsi
     jmp :9

009: mov rbp, rdi
     add rbp, 0x01
     cmp rbp, rcx
     setz sil
     movzx rsi, sil
     test rsi, rsi
     jz :10
     jmp :11

010: cmp rbp, rbx
     setz sil
     movzx rsi, sil
     test rsi, rsi
     jz :12
     jmp :13

012: cmp rbp, rdx
     setz sil
     movzx rsi, sil
     test rsi, rsi
     jz :14
     jmp :15

014: add rdi, 0x02
     mov r8, 0x02
     jmp :16

015: jmp :17

013: jmp :17

017: mov rdi, rbp
     jmp :16

016: jmp :9

011: mov rsi, r8
     jmp :8

008: cmp rsi, rax
     setnz al
     movzx rax, al
     test rax, rax
     jz :18
     jmp :19

018: mov rax, rsi
     jmp :20

019: mov rdx, 0x00
     jmp :21

021: mov rax, rdx
     add rax, 0x01
     cmp rax, rcx
     setz al
     movzx rax, al
     test rax, rax
     jz :22
     jmp :23

022: add rdx, 0x02
     mov rax, 0x04
     cmp rax, rdx
     setz al
     movzx rax, al
     test rax, rax
     jz :24
     jmp :25

024: jmp :21

025: mov rax, 0x03
     jmp :26

023: mov rax, 0x01
     jmp :26

026: jmp :20

020: pop rbp
     pop rbx
     ret
