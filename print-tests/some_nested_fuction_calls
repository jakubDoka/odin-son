main:
     mov rdi, 0x01
     mov rsi, 0x02
     call :a
     ret
a:
     push rbx
     push rbp
     mov rbx, rsi
     call :b
     mov rbp, rax
     mov rdi, rbx
     call :c
     mov rcx, rbp
     add rcx, rax
     mov rax, rcx
     pop rbp
     pop rbx
     ret
b:
     mov rax, 0x02
     imul rdi, rax
     mov rax, rdi
     ret
c:
     mov rax, 0x03
     imul rdi, rax
     mov rax, rdi
     ret
