=========== OPT LEVEL: none ===========
main:
     xor eax, eax
     xor edx, edx
004: add rdx, 0x01
     cmp rdx, 0x05
     jz :0
     xor ecx, ecx
     add rcx, 0x01
     cmp rcx, 0x05
     jz :1
     mov rcx, rax
     xor eax, eax
003: add rcx, 0x01
     add rax, 0x01
     cmp rax, 0x02
     jz :2
     jmp :3

001: mov rcx, rax
002: mov rax, rcx
     jmp :4

000: ret
=========== OPT LEVEL: mininal ===========
main:
     xor eax, eax
     xor edx, edx
004: add rdx, 0x01
     cmp rdx, 0x05
     jz :0
     xor ecx, ecx
     add rcx, 0x01
     cmp rcx, 0x05
     jz :1
     mov rcx, rax
     xor eax, eax
003: add rcx, 0x01
     add rax, 0x01
     cmp rax, 0x02
     jz :2
     jmp :3

001: mov rcx, rax
002: mov rax, rcx
     jmp :4

000: ret
=========== OPT LEVEL: moderate ===========
main:
     xor edx, edx
     xor eax, eax
001: add rdx, 0x01
     cmp rdx, 0x05
     jz :0
     xor ecx, ecx
002: add rax, 0x01
     add rcx, 0x01
     cmp rcx, 0x02
     jz :1
     jmp :2

000: ret
=========== OPT LEVEL: all ===========
main:
     xor edx, edx
     xor eax, eax
001: add rdx, 0x01
     cmp rdx, 0x05
     jz :0
     xor ecx, ecx
002: add rax, 0x01
     add rcx, 0x01
     cmp rcx, 0x02
     jz :1
     jmp :2

000: ret
=========== OPT LEVEL: aggresive ===========
