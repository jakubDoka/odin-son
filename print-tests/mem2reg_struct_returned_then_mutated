=========== OPT LEVEL: none ===========
opaque:
    mov rax, rdi
    ret
mk:
    push rbx
    push rbp
    sub rsp, 0x18
    mov rbx, rdi
    mov rbp, rsi
    xor esi, esi
    lea rdi, [rsp]
    mov rdx, 0x10
    call.q [rip - 0x32]
    mov.q [rsp], rbx
    mov.q [rsp + 0x8], rbp
    mov.q rax, [rsp]
    mov.q rdx, [rsp + 0x8]
    add rsp, 0x18
    pop rbp
    pop rbx
    ret
main:
    push rbx
    sub rsp, 0x10
    xor esi, esi
    lea rdi, [rsp]
    mov rdx, 0x10
    call.q [rip - 0x6a]
    mov rdi, 6
    call -0x6e
    mov rbx, rax
    mov rdi, 9
    call -0x7d
    mov rdi, rbx
    mov rsi, rax
    call -0x80
    shr rax, 0
    mov.q [rsp], rax
    shr rdx, 0
    mov.q [rsp + 0x8], rdx
    mov.q rax, [rsp + 0x8]
    add.q [rsp], rax
    mov.q rax, [rsp]
    imul rax, rax, 0x64
    add.q rax, [rsp + 0x8]
    add rsp, 0x10
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
opaque:
    mov rax, rdi
    ret
mk:
    push rbx
    push rbp
    sub rsp, 0x18
    mov rbx, rdi
    mov rbp, rsi
    xor esi, esi
    lea rdi, [rsp]
    mov rdx, 0x10
    call.q [rip - 0x32]
    mov.q [rsp], rbx
    mov.q [rsp + 0x8], rbp
    mov.q rax, [rsp]
    mov.q rdx, [rsp + 0x8]
    add rsp, 0x18
    pop rbp
    pop rbx
    ret
main:
    push rbx
    sub rsp, 0x10
    xor esi, esi
    lea rdi, [rsp]
    mov rdx, 0x10
    call.q [rip - 0x6a]
    mov rdi, 6
    call -0x6e
    mov rbx, rax
    mov rdi, 9
    call -0x7d
    mov rdi, rbx
    mov rsi, rax
    call -0x80
    shr rax, 0
    mov.q [rsp], rax
    shr rdx, 0
    mov.q [rsp + 0x8], rdx
    mov.q rax, [rsp + 0x8]
    add.q [rsp], rax
    mov.q rax, [rsp]
    imul rax, rax, 0x64
    add.q rax, [rsp + 0x8]
    add rsp, 0x10
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
opaque:
    mov rax, rdi
    ret
mk:
    mov rax, rdi
    mov rcx, rsi
    mov rdx, rcx
    ret
main:
    push rbx
    mov rdi, 6
    call -0x26
    mov rbx, rax
    mov rdi, 9
    call -0x35
    mov rdi, rbx
    mov rsi, rax
    call -0x38
    add rax, rdx
    imul rax, rax, 0x64
    add rax, rdx
    pop rbx
    ret
=========== OPT LEVEL: all ===========
opaque:
    mov rax, rdi
    ret
mk:
    mov rax, rdi
    mov rcx, rsi
    mov rdx, rcx
    ret
main:
    push rbx
    mov rdi, 6
    call -0x26
    mov rbx, rax
    mov rdi, 9
    call -0x35
    mov rdi, rbx
    mov rsi, rax
    call -0x38
    add rax, rdx
    imul rax, rax, 0x64
    add rax, rdx
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
opaque:
mk:
main:
    mov rax, 0x5e5
    ret
