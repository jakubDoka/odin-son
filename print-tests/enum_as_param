=========== OPT LEVEL: none ===========
apply:
     mov rcx, rsi
     cmp rdi, 0x01
     jz :0
     jmp :1

000: mov rax, 0x64
     sub rax, rcx
     mov rcx, rax
001: mov rax, rcx
     ret
main:
     sub rsp, 0x08
     mov rdi, 0x01
     mov rsi, 0x09
     call :apply
     add rsp, 0x08
     ret
=========== OPT LEVEL: mininal ===========
apply:
     mov rcx, rsi
     cmp rdi, 0x01
     jz :0
     jmp :1

000: mov rax, 0x64
     sub rax, rcx
     mov rcx, rax
001: mov rax, rcx
     ret
main:
     sub rsp, 0x08
     mov rdi, 0x01
     mov rsi, 0x09
     call :apply
     add rsp, 0x08
     ret
=========== OPT LEVEL: moderate ===========
apply:
     mov rcx, rsi
     cmp rdi, 0x01
     jz :0
     jmp :1

000: mov rax, 0x64
     sub rax, rcx
     mov rcx, rax
001: mov rax, rcx
     ret
main:
     sub rsp, 0x08
     mov rdi, 0x01
     mov rsi, 0x09
     call :apply
     add rsp, 0x08
     ret
=========== OPT LEVEL: all ===========
apply:
     mov rcx, rsi
     cmp rdi, 0x01
     jz :0
     jmp :1

000: mov rax, 0x64
     sub rax, rcx
     mov rcx, rax
001: mov rax, rcx
     ret
main:
     sub rsp, 0x08
     mov rdi, 0x01
     mov rsi, 0x09
     call :apply
     add rsp, 0x08
     ret
