=========== OPT LEVEL: none ===========
opaque:
     mov rax, rdi
     ret
main:
     push rbx
     push rbp
     push r12
     push r13
     push r14
     push r15
     sub rsp, 0x08
     mov rdi, 0x00
     sub rdi, 0x64
     call :opaque
     mov rbx, rax
     mov rax, 0x00
     sub rax, 0xBB8
     mov rdi, rax
     call :opaque
     mov rbp, rax
     mov rax, 0x00
     sub rax, 0x11170
     mov rdi, rax
     call :opaque
     mov r12, rax
     mov rax, 0x00
     sub rax, 0x4C4B40
     mov rdi, rax
     call :opaque
     mov r13, rax
     mov rdi, 0xC8
     call :opaque
     mov r14, rax
     mov rdi, 0xEA60
     call :opaque
     mov r15, rax
     mov rdi, 0x3D0900
     call :opaque
     mov [rsp], rax
     mov rdi, 0x6ACFC0
     call :opaque
     mov rcx, rax
     movsd xmm1, qword ptr [0x000000000040028A]
     mov rdx, r13
     mov rax, rbx
     movsx rsi, al
     mov rax, rbp
     movsx rdi, ax
     mov rax, r12
     movsxd r8, eax
     mov rax, r14
     movzx r9d, al
     mov rax, r15
     movzx r10d, ax
     mov rax, [rsp]
     mov r11d, eax
     movss xmm0, dword ptr [0x0000000000400294]
     cvtss2sd xmm3, xmm0
     movss xmm2, dword ptr [0x000000000040029A]
     movsd xmm0, qword ptr [0x00000000004002A0]
     subsd xmm0, [0x00000000004002AA]
     addsd xmm3, [0x00000000004002B4]
     cvtsd2ss xmm3, xmm3
     cvttss2si rax, xmm3
     mov rbx, 0x00
     add rax, rbx
     cvtsi2ss xmm3, rsi
     cvttss2si rbx, xmm3
     add rax, rbx
     cvtsi2sd xmm3, rsi
     cvttsd2si rsi, xmm3
     add rax, rsi
     cvtsi2ss xmm3, rdi
     cvttss2si rsi, xmm3
     add rax, rsi
     cvtsi2sd xmm3, rdi
     cvttsd2si rsi, xmm3
     add rax, rsi
     cvtsi2ss xmm3, r8
     cvttss2si rsi, xmm3
     add rax, rsi
     cvtsi2sd xmm3, r8
     cvttsd2si rsi, xmm3
     add rax, rsi
     cvtsi2ss xmm3, rdx
     cvttss2si rsi, xmm3
     add rax, rsi
     cvtsi2sd xmm3, rdx
     cvttsd2si rdx, xmm3
     add rax, rdx
     cvtsi2ss xmm3, r9
     cvttss2si rdx, xmm3
     add rax, rdx
     cvtsi2sd xmm3, r9
     cvttsd2si rdx, xmm3
     add rax, rdx
     cvtsi2ss xmm3, r10
     cvttss2si rdx, xmm3
     add rax, rdx
     cvtsi2sd xmm3, r10
     cvttsd2si rdx, xmm3
     add rax, rdx
     cvtsi2ss xmm3, r11
     cvttss2si rdx, xmm3
     add rax, rdx
     cvtsi2sd xmm3, r11
     cvttsd2si rdx, xmm3
     add rax, rdx
     cvtsi2ss xmm3, rcx
     cvttss2si rdx, xmm3
     add rax, rdx
     cvtsi2sd xmm3, rcx
     cvttsd2si rcx, xmm3
     add rax, rcx
     cvttss2si ecx, xmm2
     movsxd rcx, ecx
     add rax, rcx
     cvttss2si rcx, xmm2
     mov rcx, rcx
     add rax, rcx
     cvttsd2si ecx, xmm0
     movsxd rcx, ecx
     add rax, rcx
     cvttsd2si rcx, xmm0
     mov rcx, rcx
     add rax, rcx
     cvttsd2si ecx, xmm1
     movzx ecx, cl
     add rax, rcx
     movss xmm0, dword ptr [0x00000000004002BE]
     cvttss2si ecx, xmm0
     movzx ecx, cx
     add rax, rcx
     cvttsd2si ecx, xmm1
     mov ecx, ecx
     add rax, rcx
     cvttsd2si rcx, xmm1
     mov rcx, rcx
     add rax, rcx
     add rsp, 0x08
     pop r15
     pop r14
     pop r13
     pop r12
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: mininal ===========
opaque:
     mov rax, rdi
     ret
main:
     push rbx
     push rbp
     push r12
     push r13
     push r14
     push r15
     sub rsp, 0x08
     mov rdi, 0xFFFFFFFFFFFFFF9C
     call :opaque
     mov rbx, rax
     mov rdi, 0xFFFFFFFFFFFFF448
     call :opaque
     mov rbp, rax
     mov rdi, 0xFFFFFFFFFFFEEE90
     call :opaque
     mov r12, rax
     mov rdi, 0xFFFFFFFFFFB3B4C0
     call :opaque
     mov r13, rax
     mov rdi, 0xC8
     call :opaque
     mov r14, rax
     mov rdi, 0xEA60
     call :opaque
     mov r15, rax
     mov rdi, 0x3D0900
     call :opaque
     mov [rsp], rax
     mov rdi, 0x6ACFC0
     call :opaque
     mov r10, rax
     mov rax, rbx
     movsx rcx, al
     mov rax, rbp
     movsx rdx, ax
     mov rax, r12
     movsxd rsi, eax
     mov rax, [rsp]
     mov edi, eax
     mov rax, r14
     movzx r8d, al
     mov rax, r15
     movzx r9d, ax
     mov rax, 0x00
     cvtsi2ss xmm0, rcx
     cvttss2si r11, xmm0
     lea rax, [rax+r11*1+0x06]
     cvtsi2sd xmm0, rcx
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, rdx
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, rdx
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, rsi
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, rsi
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, r13
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, r13
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, r8
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, r8
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, r9
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, r9
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, rdi
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, rdi
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, r10
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, r10
     cvttsd2si rcx, xmm0
     add rax, rcx
     add rax, 0x07
     add rax, 0x07
     add rax, 0xFFFFFFFFFFFFFFF4
     add rax, 0xFFFFFFFFFFFFFFF4
     add rax, 0xFA
     add rax, 0xFDE8
     add rax, 0xFA
     add rax, 0xFA
     add rsp, 0x08
     pop r15
     pop r14
     pop r13
     pop r12
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: moderate ===========
opaque:
     mov rax, rdi
     ret
main:
     push rbx
     push rbp
     push r12
     push r13
     push r14
     push r15
     sub rsp, 0x08
     mov rdi, 0xFFFFFFFFFFFFFF9C
     call :opaque
     mov rbx, rax
     mov rdi, 0xFFFFFFFFFFFFF448
     call :opaque
     mov rbp, rax
     mov rdi, 0xFFFFFFFFFFFEEE90
     call :opaque
     mov r12, rax
     mov rdi, 0xFFFFFFFFFFB3B4C0
     call :opaque
     mov r13, rax
     mov rdi, 0xC8
     call :opaque
     mov r14, rax
     mov rdi, 0xEA60
     call :opaque
     mov r15, rax
     mov rdi, 0x3D0900
     call :opaque
     mov [rsp], rax
     mov rdi, 0x6ACFC0
     call :opaque
     mov r9, rax
     mov rax, rbp
     movsx rcx, ax
     mov rax, r12
     movsxd rdx, eax
     mov rax, rbx
     movsx rax, al
     mov rsi, [rsp]
     mov esi, esi
     mov rdi, r14
     movzx edi, dil
     mov r8, r15
     movzx r8d, r8w
     cvtsi2ss xmm0, rax
     cvttss2si r10, xmm0
     cvtsi2sd xmm0, rax
     cvttsd2si rax, xmm0
     lea rax, [r10+rax*1+0x06]
     cvtsi2ss xmm0, rcx
     cvttss2si r10, xmm0
     add rax, r10
     cvtsi2sd xmm0, rcx
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, rdx
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, rdx
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, r13
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, r13
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, rdi
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, rdi
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, r8
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, r8
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, rsi
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, rsi
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, r9
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, r9
     cvttsd2si rcx, xmm0
     add rax, rcx
     add rax, 0x100CC
     add rsp, 0x08
     pop r15
     pop r14
     pop r13
     pop r12
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: all ===========
opaque:
     mov rax, rdi
     ret
main:
     push rbx
     push rbp
     push r12
     push r13
     push r14
     push r15
     sub rsp, 0x08
     mov rdi, 0xFFFFFFFFFFFFFF9C
     call :opaque
     mov rbx, rax
     mov rdi, 0xFFFFFFFFFFFFF448
     call :opaque
     mov rbp, rax
     mov rdi, 0xFFFFFFFFFFFEEE90
     call :opaque
     mov r12, rax
     mov rdi, 0xFFFFFFFFFFB3B4C0
     call :opaque
     mov r13, rax
     mov rdi, 0xC8
     call :opaque
     mov r14, rax
     mov rdi, 0xEA60
     call :opaque
     mov r15, rax
     mov rdi, 0x3D0900
     call :opaque
     mov [rsp], rax
     mov rdi, 0x6ACFC0
     call :opaque
     mov r9, rax
     mov rax, rbp
     movsx rcx, ax
     mov rax, r12
     movsxd rdx, eax
     mov rax, rbx
     movsx rax, al
     mov rsi, [rsp]
     mov esi, esi
     mov rdi, r14
     movzx edi, dil
     mov r8, r15
     movzx r8d, r8w
     cvtsi2ss xmm0, rax
     cvttss2si r10, xmm0
     cvtsi2sd xmm0, rax
     cvttsd2si rax, xmm0
     lea rax, [r10+rax*1+0x06]
     cvtsi2ss xmm0, rcx
     cvttss2si r10, xmm0
     add rax, r10
     cvtsi2sd xmm0, rcx
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, rdx
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, rdx
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, r13
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, r13
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, rdi
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, rdi
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, r8
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, r8
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, rsi
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, rsi
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, r9
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, r9
     cvttsd2si rcx, xmm0
     add rax, rcx
     add rax, 0x100CC
     add rsp, 0x08
     pop r15
     pop r14
     pop r13
     pop r12
     pop rbp
     pop rbx
     ret
