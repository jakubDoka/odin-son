=========== OPT LEVEL: none ===========
main:
    mov rcx, 0xa
    mov rdx, 1
    xor esi, esi
1:
    mov rax, rdx
    cmp rcx, 0
    jz 2
    mov rdx, rsi
    add rdx, rax
    sub rcx, 1
    mov rsi, rax
    jmp 1
2:
    ret
=========== OPT LEVEL: mininal ===========
main:
    mov rcx, 0xa
    mov rdx, 1
    xor esi, esi
1:
    mov rax, rdx
    cmp rcx, 0
    jz 2
    mov rdx, rsi
    add rdx, rax
    sub rcx, 1
    mov rsi, rax
    jmp 1
2:
    ret
=========== OPT LEVEL: moderate ===========
main:
    mov rcx, 0xa
    mov rdx, 1
    xor esi, esi
1:
    mov rax, rdx
    cmp rcx, 0
    jz 2
    mov rdx, rsi
    add rdx, rax
    sub rcx, 1
    mov rsi, rax
    jmp 1
2:
    ret
=========== OPT LEVEL: all ===========
main:
    mov rdx, 1
    mov rsi, 0xa
    xor edi, edi
1:
    mov rax, rdx
    mov rcx, rsi
    mov rsi, rcx
    sub rsi, 1
    mov rdx, rdi
    add rdx, rax
    cmp rcx, 0
    mov rdi, rax
    jnz 1
    ret
=========== OPT LEVEL: aggresive ===========
