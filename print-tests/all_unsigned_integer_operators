=========== OPT LEVEL: none ===========
main:
    mov rdi, 6
    mov rax, 0x14
    xor rdx, rdx
    div rdi
    cmp rax, 3
    jz 1
    xor ecx, ecx
    jmp 2
1:
    xor ecx, ecx
    add rcx, 1
2:
    mov rax, 0x14
    xor rdx, rdx
    div rdi
    cmp rdx, 2
    jz 3
    jmp 4
3:
    add rcx, 2
4:
    xor eax, eax
    sub rax, 7
    mov rsi, rax
    mov rax, rsi
    xor rdx, rdx
    div rdi
    cmp rax, 0x14
    jnbe 5
    jmp 6
5:
    add rcx, 4
6:
    mov rax, rsi
    xor rdx, rdx
    div rdi
    cmp rdx, 3
    jz 7
    jmp 8
7:
    add rcx, 8
8:
    mov rax, rsi
    shr rax, 0x3c
    cmp rax, 0xf
    jz 9
    jmp 10
9:
    add rcx, 0x10
10:
    mov rax, 0x14
    shr rax, 2
    cmp rax, 5
    jz 11
    jmp 12
11:
    add rcx, 0x20
12:
    cmp rsi, 6
    jnbe 13
    jmp 14
13:
    add rcx, 0x40
14:
    cmp rsi, 0x14
    jnc 15
    jmp 16
15:
    add rcx, 0x80
16:
    cmp rdi, rsi
    jc 17
    jmp 18
17:
    add rcx, 0x100
18:
    mov rax, rcx
    cmp rdi, rsi
    jbe 19
    jmp 20
19:
    add rax, 0x200
20:
    mov rdx, rsi
    mov rcx, 0x14
    cmp rcx, rdx
    jc 21
    jmp 22
21:
    add rax, 0x400
22:
    ret
=========== OPT LEVEL: mininal ===========
main:
    mov r8b, 1
    test r8b, r8b
    jnz 1
    xor edi, edi
    jmp 2
1:
    xor edi, edi
    add rdi, 1
2:
    test r8b, r8b
    jnz 3
    jmp 4
3:
    add rdi, 2
4:
    xor eax, eax
    sub rax, 7
    mov rcx, rax
    mov rsi, 6
    mov rax, rcx
    xor rdx, rdx
    div rsi
    cmp rax, 0x14
    jnbe 5
    jmp 6
5:
    add rdi, 4
6:
    mov rax, rcx
    xor rdx, rdx
    div rsi
    cmp rdx, 3
    jz 7
    jmp 8
7:
    add rdi, 8
8:
    mov rax, rcx
    shr rax, 0x3c
    cmp rax, 0xf
    jz 9
    jmp 10
9:
    add rdi, 0x10
10:
    test r8b, r8b
    jnz 11
    jmp 12
11:
    add rdi, 0x20
12:
    cmp rcx, 6
    setnbe dl
    movzx rdx, dl
    test dl, dl
    jnz 13
    jmp 14
13:
    add rdi, 0x40
14:
    cmp rcx, 0x14
    jnc 15
    jmp 16
15:
    add rdi, 0x80
16:
    test dl, dl
    jnz 17
    jmp 18
17:
    add rdi, 0x100
18:
    cmp rcx, 6
    jnc 19
    jmp 20
19:
    add rdi, 0x200
20:
    cmp rcx, 0x14
    jnbe 21
    jmp 22
21:
    add rdi, 0x400
22:
    mov rax, rdi
    ret
=========== OPT LEVEL: moderate ===========
main:
    mov rax, 0x7ff
    ret
=========== OPT LEVEL: all ===========
main:
    mov rax, 0x7ff
    ret
=========== OPT LEVEL: aggresive ===========
