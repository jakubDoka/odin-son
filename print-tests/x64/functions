=========== OPT LEVEL: none-quick ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call fib
    add rsp, 8
    ret
fib:
    sub rsp, 0x18
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    cmp rax, 2
    jng 2
    mov.q rax, [rsp]
    sub rax, 1
    mov rdi, rax
    call fib
    mov.q [rsp + 0x8], rax
    mov.q rax, [rsp]
    sub rax, 2
    mov rdi, rax
    call fib
    mov.q rcx, [rsp + 0x8]
    add rcx, rax
    mov rax, rcx
    jmp 3
2:
    mov rax, 1
3:
    mov rax, rax
    add rsp, 0x18
    ret
=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call fib
    add rsp, 8
    ret
fib:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    cmp rbp, 2
    jng 2
    mov rax, rbp
    sub rax, 1
    mov rdi, rax
    call fib
    mov rbx, rax
    sub rbp, 2
    mov rdi, rbp
    call fib
    add rbx, rax
    jmp 3
2:
    mov rbx, 1
3:
    mov rax, rbx
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal-quick ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call fib
    add rsp, 8
    ret
fib:
    sub rsp, 0x18
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    cmp rax, 2
    jng 2
    mov.q rax, [rsp]
    sub rax, 1
    mov rdi, rax
    call fib
    mov.q [rsp + 0x8], rax
    mov.q rax, [rsp]
    sub rax, 2
    mov rdi, rax
    call fib
    mov.q rcx, [rsp + 0x8]
    add rcx, rax
    mov rax, rcx
    jmp 3
2:
    mov rax, 1
3:
    mov rax, rax
    add rsp, 0x18
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call fib
    add rsp, 8
    ret
fib:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    cmp rbp, 2
    jng 2
    mov rax, rbp
    sub rax, 1
    mov rdi, rax
    call fib
    mov rbx, rax
    sub rbp, 2
    mov rdi, rbp
    call fib
    add rbx, rax
    jmp 3
2:
    mov rbx, 1
3:
    mov rax, rbx
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate-quick ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call fib
    add rsp, 8
    ret
fib:
    sub rsp, 0x18
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    cmp rax, 2
    jng 2
    mov.q rax, [rsp]
    sub rax, 1
    mov rdi, rax
    call fib
    mov.q [rsp + 0x8], rax
    mov.q rax, [rsp]
    sub rax, 2
    mov rdi, rax
    call fib
    mov.q rcx, [rsp + 0x8]
    add rcx, rax
    mov rax, rcx
    jmp 3
2:
    mov rax, 1
3:
    mov rax, rax
    add rsp, 0x18
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call fib
    add rsp, 8
    ret
fib:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    cmp rbp, 2
    jng 2
    mov rax, rbp
    sub rax, 1
    mov rdi, rax
    call fib
    mov rbx, rax
    sub rbp, 2
    mov rdi, rbp
    call fib
    add rbx, rax
    jmp 3
2:
    mov rbx, 1
3:
    mov rax, rbx
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call fib
    add rsp, 8
    ret
fib:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    cmp rbp, 2
    jng 2
    mov rax, rbp
    sub rax, 1
    mov rdi, rax
    call fib
    mov rbx, rax
    sub rbp, 2
    mov rdi, rbp
    call fib
    add rbx, rax
    jmp 3
2:
    mov rbx, 1
3:
    mov rax, rbx
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
