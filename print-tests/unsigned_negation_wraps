=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    mov rdi, 1
    call +0x10
    neg ax
    movzx eax, ax
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, 1
    call +0x10
    neg ax
    movzx eax, ax
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, 1
    call +0x10
    neg ax
    movzx eax, ax
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, 1
    call +0x10
    neg ax
    movzx eax, ax
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 0xffff
    ret
opaque:
