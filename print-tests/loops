=========== OPT LEVEL: none ===========
main:
     mov rcx, 0x0A
     mov rsi, 0x00
     mov rax, 0x01
001: cmp rcx, 0x00
     jz :0
     mov rdx, rsi
     add rdx, rax
     sub rcx, 0x01
     mov rsi, rax
     mov rax, rdx
     jmp :1

000: ret
=========== OPT LEVEL: mininal ===========
main:
     mov rcx, 0x0A
     mov rsi, 0x00
     mov rax, 0x01
001: cmp rcx, 0x00
     jz :0
     mov rdx, rsi
     add rdx, rax
     sub rcx, 0x01
     mov rsi, rax
     mov rax, rdx
     jmp :1

000: ret
=========== OPT LEVEL: moderate ===========
main:
     mov rcx, 0x0A
     mov rsi, 0x00
     mov rax, 0x01
001: cmp rcx, 0x00
     jz :0
     mov rdx, rsi
     add rdx, rax
     sub rcx, 0x01
     mov rsi, rax
     mov rax, rdx
     jmp :1

000: ret
=========== OPT LEVEL: all ===========
main:
     mov rcx, 0x0A
     mov rsi, 0x00
     mov rax, 0x01
001: cmp rcx, 0x00
     jz :0
     mov rdx, rsi
     add rdx, rax
     sub rcx, 0x01
     mov rsi, rax
     mov rax, rdx
     jmp :1

000: ret
=========== OPT LEVEL: aggresive ===========
