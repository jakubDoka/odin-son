=========== OPT LEVEL: none ===========
opaque:
    mov rax, rdi
    ret
minmax:
    cmp rdi, rsi
    jnge 0
    mov.q [rdx], rsi
    jmp 1
0:
    mov.q [rdx], rdi
    mov rdi, rsi
1:
    mov rax, rdi
    ret
main:
    push rbx
    sub rsp, 0x10
    mov rdi, 9
    call -0x3a
    mov rbx, rax
    mov rdi, 4
    call -0x49
    lea rdx, [rsp]
    mov rdi, rbx
    mov rsi, rax
    call -0x50
    mov.q rcx, [rsp]
    sub rax, rcx
    imul rax, rax, 2
    add rax, rcx
    add rsp, 0x10
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
opaque:
    mov rax, rdi
    ret
minmax:
    cmp rdi, rsi
    jnge 0
    mov.q [rdx], rsi
    jmp 1
0:
    mov.q [rdx], rdi
    mov rdi, rsi
1:
    mov rax, rdi
    ret
main:
    push rbx
    sub rsp, 0x10
    mov rdi, 9
    call -0x3a
    mov rbx, rax
    mov rdi, 4
    call -0x49
    lea rdx, [rsp]
    mov rdi, rbx
    mov rsi, rax
    call -0x50
    mov.q rcx, [rsp]
    sub rax, rcx
    imul rax, rax, 2
    add rax, rcx
    add rsp, 0x10
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
opaque:
    mov rax, rdi
    ret
minmax:
    cmp rdi, rsi
    jnge 0
    mov.q [rdx], rsi
    jmp 1
0:
    mov.q [rdx], rdi
    mov rdi, rsi
1:
    mov rax, rdi
    ret
main:
    push rbx
    sub rsp, 0x10
    mov rdi, 9
    call -0x3a
    mov rbx, rax
    mov rdi, 4
    call -0x49
    lea rdx, [rsp]
    mov rdi, rbx
    mov rsi, rax
    call -0x50
    mov.q rcx, [rsp]
    sub rax, rcx
    imul rax, rax, 2
    add rax, rcx
    add rsp, 0x10
    pop rbx
    ret
=========== OPT LEVEL: all ===========
opaque:
    mov rax, rdi
    ret
minmax:
    cmp rdi, rsi
    jnge 0
    mov.q [rdx], rsi
    jmp 1
0:
    mov.q [rdx], rdi
    mov rdi, rsi
1:
    mov rax, rdi
    ret
main:
    push rbx
    sub rsp, 0x10
    mov rdi, 9
    call -0x3a
    mov rbx, rax
    mov rdi, 4
    call -0x49
    lea rdx, [rsp]
    mov rdi, rbx
    mov rsi, rax
    call -0x50
    mov.q rcx, [rsp]
    sub rax, rcx
    imul rax, rax, 2
    add rax, rcx
    add rsp, 0x10
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
opaque:
minmax:
main:
    mov rax, 0xe
    ret
