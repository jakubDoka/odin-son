=========== OPT LEVEL: none ===========
main:
     sub rsp, 0x08
     mov rdi, 0x01
     mov rsi, 0x02
     call :a
     add rsp, 0x08
     ret
a:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rsi
     cmp rdi, 0x00
     jz :0
     cmp rbx, 0x02
     jz :1
     cmp rdi, 0x01
     jz :2
     call :b
     mov rbp, rax
     mov rdi, rbx
     call :c
     add rbp, rax
     jmp :3

002: call :b
     mov rbp, rax
     imul rdi, rbx, 0x02
     call :c
     add rbp, rax
     jmp :3

001: imul rbp, rbx, 0x06
     jmp :3

000: mov rbp, 0x00
003: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
b:
     imul rax, rdi, 0x02
     ret
c:
     imul rax, rdi, 0x03
     ret
=========== OPT LEVEL: mininal ===========
main:
     sub rsp, 0x08
     mov rdi, 0x01
     mov rsi, 0x02
     call :a
     add rsp, 0x08
     ret
a:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rsi
     cmp rdi, 0x00
     jz :0
     cmp rbx, 0x02
     jz :1
     cmp rdi, 0x01
     jz :2
     call :b
     mov rbp, rax
     mov rdi, rbx
     call :c
     add rbp, rax
     jmp :3

002: call :b
     mov rbp, rax
     imul rdi, rbx, 0x02
     call :c
     add rbp, rax
     jmp :3

001: imul rbp, rbx, 0x06
     jmp :3

000: mov rbp, 0x00
003: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
b:
     imul rax, rdi, 0x02
     ret
c:
     imul rax, rdi, 0x03
     ret
=========== OPT LEVEL: moderate ===========
main:
     sub rsp, 0x08
     mov rdi, 0x01
     mov rsi, 0x02
     call :a
     add rsp, 0x08
     ret
a:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rsi
     cmp rdi, 0x00
     jz :0
     cmp rbx, 0x02
     jz :1
     cmp rdi, 0x01
     jz :2
     call :b
     mov rbp, rax
     mov rdi, rbx
     call :c
     add rbp, rax
     jmp :3

002: call :b
     mov rbp, rax
     imul rdi, rbx, 0x02
     call :c
     add rbp, rax
     jmp :3

001: imul rbp, rbx, 0x06
     jmp :3

000: mov rbp, 0x00
003: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
b:
     imul rax, rdi, 0x02
     ret
c:
     imul rax, rdi, 0x03
     ret
=========== OPT LEVEL: all ===========
main:
     sub rsp, 0x08
     mov rdi, 0x01
     mov rsi, 0x02
     call :a
     add rsp, 0x08
     ret
a:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rsi
     cmp rdi, 0x00
     jz :0
     cmp rbx, 0x02
     jz :1
     cmp rdi, 0x01
     jz :2
     call :b
     mov rbp, rax
     mov rdi, rbx
     call :c
     add rbp, rax
     jmp :3

002: call :b
     mov rbp, rax
     imul rdi, rbx, 0x02
     call :c
     add rbp, rax
     jmp :3

001: imul rbp, rbx, 0x06
     jmp :3

000: mov rbp, 0x00
003: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
b:
     imul rax, rdi, 0x02
     ret
c:
     imul rax, rdi, 0x03
     ret
