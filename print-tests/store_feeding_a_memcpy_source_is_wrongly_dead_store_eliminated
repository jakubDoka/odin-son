=========== OPT LEVEL: none ===========
ident:
    mov rax, rdi
    ret
2:
    push rbx
    sub rsp, 0x20
    xor esi, esi
    mov rdx, 0x20
    lea rdi, [rsp]
    call.q [rip - 0x2a]
    lea rdi, [rsp]
    call ident
    mov rbx, rax
    mov.q [rbx], 5
    mov rdx, 0x10
    mov rax, rbx
    add rax, 0x10
    mov.q [rbx + 0x8], 6
    mov rsi, rbx
    mov rdi, rax
    call.q [rip - 0x6a]
    mov.q [rbx], 7
    mov.q rax, [rbx + 0x18]
    mov.q rcx, [rbx]
    imul rdx, rax, 0xa
    mov.q rax, [rbx + 0x10]
    imul rcx, rcx, 0x64
    add rax, rdx
    add rax, rcx
    add rsp, 0x20
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
ident:
    mov rax, rdi
    ret
2:
    push rbx
    sub rsp, 0x20
    xor esi, esi
    mov rdx, 0x20
    lea rdi, [rsp]
    call.q [rip - 0x2a]
    lea rdi, [rsp]
    call ident
    mov rbx, rax
    mov.q [rbx], 5
    mov rdx, 0x10
    mov rax, rbx
    add rax, 0x10
    mov.q [rbx + 0x8], 6
    mov rsi, rbx
    mov rdi, rax
    call.q [rip - 0x6a]
    mov.q [rbx], 7
    mov.q rax, [rbx + 0x18]
    imul rcx, rax, 0xa
    mov.q rax, [rbx + 0x10]
    add rax, rcx
    add rax, 0x2bc
    add rsp, 0x20
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
ident:
    mov rax, rdi
    ret
2:
    push rbx
    sub rsp, 0x20
    pxor xmm0, xmm0
    lea rdi, [rsp]
    movups.x [rsp + 0x10], xmm0
    movups.x [rsp], xmm0
    call ident
    mov rbx, rax
    mov.q [rbx], 5
    mov rdx, 0x10
    mov rax, rbx
    add rax, 0x10
    mov.q [rbx + 0x8], 6
    mov rsi, rbx
    mov rdi, rax
    call.q [rip - 0x66]
    mov.q [rbx], 7
    mov.q rax, [rbx + 0x18]
    imul rcx, rax, 0xa
    mov.q rax, [rbx + 0x10]
    add rax, rcx
    add rax, 0x2bc
    add rsp, 0x20
    pop rbx
    ret
=========== OPT LEVEL: all ===========
ident:
    mov rax, rdi
    ret
2:
    push rbx
    sub rsp, 0x20
    pxor xmm0, xmm0
    lea rdi, [rsp]
    movups.x [rsp + 0x10], xmm0
    movups.x [rsp], xmm0
    call ident
    mov rbx, rax
    mov.q [rbx], 5
    mov rdx, 0x10
    mov rax, rbx
    add rax, 0x10
    mov.q [rbx + 0x8], 6
    mov rsi, rbx
    mov rdi, rax
    call.q [rip - 0x66]
    mov.q [rbx], 7
    mov.q rax, [rbx + 0x18]
    imul rcx, rax, 0xa
    mov.q rax, [rbx + 0x10]
    add rax, rcx
    add rax, 0x2bc
    add rsp, 0x20
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
ident:
    sub rsp, 0x28
    pxor xmm0, xmm0
    movups.x [rsp], xmm0
    mov.q [rsp], 5
    lea rax, [rsp]
    add rax, 0x10
    mov.q [rsp + 0x8], 6
    mov rdx, 0x10
    lea rsi, [rsp]
    mov rdi, rax
    call.q [rip - 0x4e]
    mov.q [rsp], 7
    mov.q rcx, [rsp + 0x18]
    mov.q rax, [rsp + 0x10]
    imul rcx, rcx, 0xa
    add rax, rcx
    add rax, 0x2bc
    add rsp, 0x28
    ret
