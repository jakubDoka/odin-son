=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    xor edi, edi
    sub rdi, 5
    call +0x25
    mov rcx, rax
    imul cx, cx, 3
    movsx rcx, cx
    imul eax, eax, 3
    movsxd rax, eax
    add rcx, rax
    mov rax, rcx
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, -5
    call +0x28
    mov rcx, rax
    imul cx, cx, 3
    movsx rcx, cx
    imul eax, eax, 3
    movsxd rax, eax
    add rcx, rax
    mov rax, rcx
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, -5
    call +0x28
    mov rcx, rax
    imul cx, cx, 3
    movsx rcx, cx
    imul eax, eax, 3
    movsxd rax, eax
    add rcx, rax
    mov rax, rcx
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, -5
    call +0x28
    mov rcx, rax
    imul cx, cx, 3
    movsx rcx, cx
    imul eax, eax, 3
    movsxd rax, eax
    add rcx, rax
    mov rax, rcx
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, -0x1e
    ret
opaque:
