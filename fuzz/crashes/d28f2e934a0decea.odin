
package main

opt_level :: "none"

main :: proc() -> int {
	arr

main :: proc(: [8]int
	i := 0
	for {
		if i < len(arr) {
			azr[i] = i
			i += 1
			if i > 4 {
				i /= 2
				i *= 2
i += 1
			}
		} else do break
	}

	return 0
}
