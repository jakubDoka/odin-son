=========== OPT LEVEL: none ===========
main:
     sub rsp, 0x08
     mov rdi, 0x0A
     call :fib
     add rsp, 0x08
     ret
fib:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp rbx, 0x02
     jle :0
     mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib
     add rbp, rax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: mininal ===========
main:
     sub rsp, 0x08
     mov rdi, 0x0A
     call :fib
     add rsp, 0x08
     ret
fib:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp rbx, 0x02
     jle :0
     mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib
     add rbp, rax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: moderate ===========
main:
     sub rsp, 0x08
     mov rdi, 0x0A
     call :fib
     add rsp, 0x08
     ret
fib:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp rbx, 0x02
     jle :0
     mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib
     add rbp, rax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: all ===========
main:
     sub rsp, 0x08
     mov rdi, 0x0A
     call :fib
     add rsp, 0x08
     ret
fib:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp rbx, 0x02
     jle :0
     mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib
     add rbp, rax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: aggresive ===========
main:
     push rbx
     mov rdi, 0x09
     call :fib
     mov rbx, rax
     mov rdi, 0x08
     call :fib
     add rbx, rax
     mov rax, rbx
     pop rbx
     ret
fib:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp rbx, 0x02
     jle :0
     mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib
     add rbp, rax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
