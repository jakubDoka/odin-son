=========== OPT LEVEL: none ===========
main:
     mov rcx, 0x07
     mov rsi, 0x04
     mov rax, 0x64
     cqo
     idiv rcx
     cqo
     idiv rsi
     mov rdi, rdx
     mov rax, 0x64
     xor rdx, rdx
     div rcx
     xor rdx, rdx
     div rsi
     mov rax, rdx
     add rdi, rax
     mov rax, rdi
     ret
=========== OPT LEVEL: mininal ===========
main:
     mov rcx, 0x07
     mov rsi, 0x04
     mov rax, 0x64
     cqo
     idiv rcx
     cqo
     idiv rsi
     mov rdi, rdx
     mov rax, 0x64
     xor rdx, rdx
     div rcx
     xor rdx, rdx
     div rsi
     add rdi, rdx
     mov rax, rdi
     ret
=========== OPT LEVEL: all ===========
main:
     mov rax, 0x04
     ret
