=========== OPT LEVEL: None ===========
main:
     push rbx
     push rbp
     mov rcx, 0x00
     mov rsi, 0x00
     mov rbp, 0x00
     jmp :0

000: mov rax, 0x01
     add rsi, rax
     mov rdx, 0x02
     mov rbx, rsi
     cmp rbx, rdx
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :1
     jmp :2

001: mov rdx, rsi
     cmp rdx, rax
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :3
     jmp :4

003: add rsi, rax
     mov rbp, 0x01
     jmp :5

004: jmp :5

005: jmp :0

002: mov rbx, rbp
     cmp rbx, rcx
     setz bl
     movzx rbx, bl
     mov rsi, 0x03
     test rbx, rbx
     jz :6
     jmp :7

006: jmp :8

007: mov rdi, 0x00
     jmp :9

009: add rdi, rax
     mov rbx, rdi
     cmp rbx, rsi
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :10
     jmp :11

010: mov rbx, rdi
     cmp rbx, rdx
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :12
     jmp :13

012: mov rbx, rdi
     cmp rbx, rax
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :14
     jmp :15

014: add rdi, rax
     mov rbp, 0x02
     jmp :16

015: jmp :17

013: jmp :17

017: jmp :16

016: jmp :9

011: jmp :8

008: mov rdx, rbp
     cmp rdx, rcx
     setnz dl
     movzx rdx, dl
     test rdx, rdx
     jz :18
     jmp :19

018: mov rax, rbp
     jmp :20

019: mov rdx, 0x00
     jmp :21

021: add rdx, rax
     mov rcx, rdx
     cmp rcx, rsi
     setz cl
     movzx rcx, cl
     test rcx, rcx
     jz :22
     jmp :23

022: add rdx, rax
     mov rcx, 0x04
     cmp rcx, rdx
     setz cl
     movzx rcx, cl
     test rcx, rcx
     jz :24
     jmp :25

024: jmp :21

025: mov rax, 0x03
     jmp :26

023: mov rax, 0x01
     jmp :26

026: jmp :20

020: pop rbp
     pop rbx
     ret
=========== OPT LEVEL: Full ===========
main:
     push rbx
     push rbp
     mov rax, 0x00
     mov rdi, 0x00
     mov rsi, 0x00
     jmp :0

000: mov rcx, 0x01
     mov rbx, 0x02
     mov rdx, rdi
     add rdx, 0x01
     mov rbp, rdx
     cmp rbp, rbx
     setz bpl
     movzx rbp, bpl
     test rbp, rbp
     jz :1
     jmp :2

001: mov rbx, rdx
     cmp rbx, rcx
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :3
     jmp :4

003: add rdi, 0x02
     mov rsi, 0x01
     jmp :5

004: mov rdi, rdx
     jmp :5

005: jmp :0

002: mov rbp, rsi
     cmp rbp, rax
     setz bpl
     movzx rbp, bpl
     mov rdx, 0x03
     test rbp, rbp
     jz :6
     jmp :7

006: jmp :8

007: mov rdi, 0x00
     mov r8, rsi
     jmp :9

009: mov rbp, rdi
     add rbp, 0x01
     mov rsi, rbp
     cmp rsi, rdx
     setz sil
     movzx rsi, sil
     test rsi, rsi
     jz :10
     jmp :11

010: mov rsi, rbp
     cmp rsi, rbx
     setz sil
     movzx rsi, sil
     test rsi, rsi
     jz :12
     jmp :13

012: mov rsi, rbp
     cmp rsi, rcx
     setz sil
     movzx rsi, sil
     test rsi, rsi
     jz :14
     jmp :15

014: add rdi, 0x02
     mov r8, 0x02
     jmp :16

015: jmp :17

013: jmp :17

017: mov rdi, rbp
     jmp :16

016: jmp :9

011: mov rsi, r8
     jmp :8

008: mov rcx, rsi
     cmp rcx, rax
     setnz cl
     movzx rcx, cl
     test rcx, rcx
     jz :18
     jmp :19

018: mov rax, rsi
     jmp :20

019: mov rcx, 0x00
     jmp :21

021: mov rax, rcx
     add rax, 0x01
     cmp rax, rdx
     setz al
     movzx rax, al
     test rax, rax
     jz :22
     jmp :23

022: mov rax, 0x04
     add rcx, 0x02
     cmp rax, rcx
     setz al
     movzx rax, al
     test rax, rax
     jz :24
     jmp :25

024: jmp :21

025: mov rax, 0x03
     jmp :26

023: mov rax, 0x01
     jmp :26

026: jmp :20

020: pop rbp
     pop rbx
     ret
