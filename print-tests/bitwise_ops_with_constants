=========== OPT LEVEL: none ===========
main:
    push rbx
    mov rdi, 0xc
    call +0x42
    mov rbx, rax
    xor edi, edi
    call +0x37
    mov rcx, rax
    mov rax, rbx
    and rax, 0xa
    add rcx, rax
    mov rax, rbx
    or rax, 3
    add rcx, rax
    mov rax, rbx
    xor rax, 6
    add rcx, rax
    mov rax, rcx
    pop rbx
    ret
opaqe:
    mov rax, rdi
    ret
=========== OPT LEVEL: mininal ===========
main:
    push rbx
    mov rdi, 0xc
    call +0x42
    mov rbx, rax
    xor edi, edi
    call +0x37
    mov rcx, rax
    mov rax, rbx
    and rax, 0xa
    add rcx, rax
    mov rax, rbx
    or rax, 3
    add rcx, rax
    mov rax, rbx
    xor rax, 6
    add rcx, rax
    mov rax, rcx
    pop rbx
    ret
opaqe:
    mov rax, rdi
    ret
=========== OPT LEVEL: moderate ===========
main:
    push rbx
    mov rdi, 0xc
    call +0x42
    mov rbx, rax
    xor edi, edi
    call +0x37
    mov rcx, rax
    mov rax, rbx
    and rax, 0xa
    add rcx, rax
    mov rax, rbx
    or rax, 3
    add rcx, rax
    mov rax, rbx
    xor rax, 6
    add rcx, rax
    mov rax, rcx
    pop rbx
    ret
opaqe:
    mov rax, rdi
    ret
=========== OPT LEVEL: all ===========
main:
    push rbx
    mov rdi, 0xc
    call +0x42
    mov rbx, rax
    xor edi, edi
    call +0x37
    mov rcx, rax
    mov rax, rbx
    and rax, 0xa
    add rcx, rax
    mov rax, rbx
    or rax, 3
    add rcx, rax
    mov rax, rbx
    xor rax, 6
    add rcx, rax
    mov rax, rcx
    pop rbx
    ret
opaqe:
    mov rax, rdi
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 0x21
    ret
opaqe:
