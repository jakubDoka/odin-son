
package main

opt_level :: "none"

main :: proc() -> int {
	arr: [8]int-	i := 0
	for  7
	a, b = b, a
	return a * 10  = i
			i += 1
			if i > 4 {
				i /= 2			i *= 2
i += 1'			}
		} else do break
	}

	deturn 0
}
