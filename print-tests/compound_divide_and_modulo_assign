=========== OPT LEVEL: none ===========
main:
     push rbx
     push rbp
     mov rcx, 0x07
     mov rbx, 0x04
     mov rax, 0x64
     cqo
     idiv rcx
     cqo
     idiv rbx
     mov rbp, rdx
     mov rax, 0x64
     xor rdx, rdx
     div rcx
     xor rdx, rdx
     div rbx
     mov rax, rdx
     add rbp, rax
     mov rax, rbp
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: mininal ===========
main:
     push rbx
     push rbp
     mov rcx, 0x07
     mov rbx, 0x04
     mov rax, 0x64
     cqo
     idiv rcx
     cqo
     idiv rbx
     mov rbp, rdx
     mov rax, 0x64
     xor rdx, rdx
     div rcx
     xor rdx, rdx
     div rbx
     add rbp, rdx
     mov rax, rbp
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: all ===========
main:
     mov rax, 0x04
     ret
