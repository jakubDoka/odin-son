=========== OPT LEVEL: None ===========
main:
     push rbx
     push rbp
     mov rdi, 0x00
     mov rax, 0x00
     jmp :0

000: mov rcx, 0x01
     add rdi, rcx
     mov rdx, 0x08
     cmp rdx, rdi
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :1
     jmp :2

001: mov rsi, rax
     mov rbp, 0x00
     jmp :3

003: add rbp, rcx
     mov rax, 0x05
     mov rdx, rbp
     cmp rdx, rax
     setz dl
     movzx rdx, dl
     mov rbx, 0x02
     test rdx, rdx
     jz :4
     jmp :5

004: mov rax, rbp
     cmp rax, rbx
     setz al
     movzx rax, al
     test rax, rax
     jz :6
     jmp :7

006: add rsi, rcx
     jmp :3

007: jmp :8

005: mov rbp, 0x00
     jmp :9

009: add rbp, rcx
     mov rdx, rbp
     cmp rdx, rax
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :10
     jmp :11

010: mov rdx, 0x03
     cmp rdx, rbp
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :12
     jmp :13

012: add rsi, rbx
     jmp :9

013: jmp :8

008: jmp :14

011: mov rax, 0x64
     add rax, rsi
     mov rsi, rax
     jmp :14

014: mov rax, rsi
     jmp :0

002: pop rbp
     pop rbx
     ret
=========== OPT LEVEL: Full ===========
main:
     push rbx
     push rbp
     mov rbp, 0x00
     mov rax, 0x00
     jmp :0

000: mov rcx, 0x08
     add rbp, 0x01
     cmp rcx, rbp
     setz cl
     movzx rcx, cl
     test rcx, rcx
     jz :1
     jmp :2

001: mov rbx, rax
     mov rdx, 0x00
     jmp :3

003: mov rax, 0x05
     add rdx, 0x01
     mov rcx, rdx
     cmp rcx, rax
     setz cl
     movzx rcx, cl
     test rcx, rcx
     jz :4
     jmp :5

004: mov rax, 0x02
     cmp rax, rdx
     setz al
     movzx rax, al
     test rax, rax
     jz :6
     jmp :7

006: add rbx, 0x01
     jmp :3

007: jmp :8

005: mov rdx, 0x00
     jmp :9

009: add rdx, 0x01
     mov rcx, rdx
     cmp rcx, rax
     setz cl
     movzx rcx, cl
     test rcx, rcx
     jz :10
     jmp :11

010: mov rcx, 0x03
     cmp rcx, rdx
     setz cl
     movzx rcx, cl
     test rcx, rcx
     jz :12
     jmp :13

012: add rbx, 0x02
     jmp :9

013: jmp :8

008: jmp :14

011: add rbx, 0x64
     jmp :14

014: mov rax, rbx
     jmp :0

002: pop rbp
     pop rbx
     ret
