=========== OPT LEVEL: none ===========
main:
     mov rdi, 0x03
     call :opaque
     mov rcx, 0x05
     cmp rcx, rax
     setnl al
     movzx rax, al
     movzx eax, al
     ret
opaque:
     mov rax, rdi
     ret
=========== OPT LEVEL: mininal ===========
main:
     mov rdi, 0x03
     call :opaque
     cmp rax, 0x05
     setl al
     movzx rax, al
     movzx eax, al
     ret
opaque:
     mov rax, rdi
     ret
=========== OPT LEVEL: all ===========
main:
     mov rdi, 0x03
     call :opaque
     cmp rax, 0x05
     setl al
     movzx rax, al
     movzx eax, al
     ret
opaque:
     mov rax, rdi
     ret
