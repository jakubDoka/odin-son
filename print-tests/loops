main:
     push rbx
     push rbp
     mov rsi, 0x0A
     mov rcx, 0x00
     mov rdx, 0x01
     mov rax, rdx
     mov rbp, rcx
     jmp :0

000: mov rbx, rsi
     cmp rbx, rcx
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :1
     jmp :2

001: mov rbx, rbp
     add rbx, rax
     sub rsi, rdx
     mov rbp, rax
     mov rax, rbx
     jmp :0

002: pop rbp
     pop rbx
     ret
