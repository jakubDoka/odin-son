=========== OPT LEVEL: none-quick ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call 2
    add rsp, 8
    ret
2:
    sub rsp, 0x18
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    cmp rax, 2
    jng 3
    mov.q rax, [rsp]
    sub rax, 1
    mov rdi, rax
    call 2
    mov.q [rsp + 0x8], rax
    mov.q rax, [rsp]
    sub rax, 2
    mov rdi, rax
    call 2
    mov.q rcx, [rsp + 0x8]
    add rcx, rax
    mov rax, rcx
    jmp 4
3:
    mov rax, 1
4:
    mov rax, rax
    add rsp, 0x18
    ret
=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call 2
    add rsp, 8
    ret
2:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    cmp rbp, 2
    jng 3
    mov rax, rbp
    sub rax, 1
    mov rdi, rax
    call 2
    mov rbx, rax
    sub rbp, 2
    mov rdi, rbp
    call 2
    add rbx, rax
    jmp 4
3:
    mov rbx, 1
4:
    mov rax, rbx
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal-quick ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call 2
    add rsp, 8
    ret
2:
    sub rsp, 0x18
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    cmp rax, 2
    jng 3
    mov.q rax, [rsp]
    sub rax, 1
    mov rdi, rax
    call 2
    mov.q [rsp + 0x8], rax
    mov.q rax, [rsp]
    sub rax, 2
    mov rdi, rax
    call 2
    mov.q rcx, [rsp + 0x8]
    add rcx, rax
    mov rax, rcx
    jmp 4
3:
    mov rax, 1
4:
    mov rax, rax
    add rsp, 0x18
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call 2
    add rsp, 8
    ret
2:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    cmp rbp, 2
    jng 3
    mov rax, rbp
    sub rax, 1
    mov rdi, rax
    call 2
    mov rbx, rax
    sub rbp, 2
    mov rdi, rbp
    call 2
    add rbx, rax
    jmp 4
3:
    mov rbx, 1
4:
    mov rax, rbx
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate-quick ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call 2
    add rsp, 8
    ret
2:
    sub rsp, 0x18
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    cmp rax, 2
    jng 3
    mov.q rax, [rsp]
    sub rax, 1
    mov rdi, rax
    call 2
    mov.q [rsp + 0x8], rax
    mov.q rax, [rsp]
    sub rax, 2
    mov rdi, rax
    call 2
    mov.q rcx, [rsp + 0x8]
    add rcx, rax
    mov rax, rcx
    jmp 4
3:
    mov rax, 1
4:
    mov rax, rax
    add rsp, 0x18
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call 2
    add rsp, 8
    ret
2:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    cmp rbp, 2
    jng 3
    mov rax, rbp
    sub rax, 1
    mov rdi, rax
    call 2
    mov rbx, rax
    sub rbp, 2
    mov rdi, rbp
    call 2
    add rbx, rax
    jmp 4
3:
    mov rbx, 1
4:
    mov rax, rbx
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call 2
    add rsp, 8
    ret
2:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    cmp rbp, 2
    jng 3
    mov rax, rbp
    sub rax, 1
    mov rdi, rax
    call 2
    mov rbx, rax
    sub rbp, 2
    mov rdi, rbp
    call 2
    add rbx, rax
    jmp 4
3:
    mov rbx, 1
4:
    mov rax, rbx
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
