=========== OPT LEVEL: none-quick ===========
main:
    push rbx
    push rbp
    sub rsp, 8
    xor eax, eax
    xor ecx, ecx
1:
    cmp rcx, 4
    jnl 6
    xor edx, edx
    xor ebx, ebx
2:
    mov rbp, rbx
    cmp rbp, 4
    jnl 3
    mov rsi, rbp
    add rsi, 1
    add rdx, rbp
    mov rbx, rsi
    jmp 2
3:
    xor ebx, ebx
    xor ebp, ebp
4:
    cmp rbx, 4
    jnl 5
    mov rsi, rbx
    add rsi, 1
    lea rdi, [rbp + rbx*2]
    mov rbx, rsi
    mov rbp, rdi
    jmp 4
5:
    add rdx, rbp
    mov rbx, rcx
    add rbx, 1
    mov rbp, rax
    add rbp, rdx
    mov rcx, rbx
    mov rax, rbp
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: none ===========
main:
    xor eax, eax
    xor ecx, ecx
1:
    cmp rcx, 4
    jnl 6
    xor r8d, r8d
    xor edx, edx
2:
    cmp rdx, 4
    jnl 3
    mov rsi, rdx
    add rsi, 1
    add r8, rdx
    mov rdx, rsi
    jmp 2
3:
    xor esi, esi
    xor edx, edx
4:
    cmp rsi, 4
    jnl 5
    mov rdi, rsi
    add rdi, 1
    lea rdx, [rdx + rsi*2]
    mov rsi, rdi
    jmp 4
5:
    add r8, rdx
    add rcx, 1
    add rax, r8
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: mininal-quick ===========
main:
    push rbx
    push rbp
    sub rsp, 8
    xor eax, eax
    xor ecx, ecx
1:
    cmp rcx, 4
    jnl 6
    xor edx, edx
    xor ebx, ebx
2:
    mov rbp, rbx
    cmp rbp, 4
    jnl 3
    mov rsi, rbp
    add rsi, 1
    add rdx, rbp
    mov rbx, rsi
    jmp 2
3:
    xor ebx, ebx
    xor ebp, ebp
4:
    cmp rbx, 4
    jnl 5
    mov rsi, rbx
    add rsi, 1
    lea rdi, [rbp + rbx*2]
    mov rbx, rsi
    mov rbp, rdi
    jmp 4
5:
    add rdx, rbp
    mov rbx, rcx
    add rbx, 1
    mov rbp, rax
    add rbp, rdx
    mov rcx, rbx
    mov rax, rbp
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
main:
    xor eax, eax
    xor ecx, ecx
1:
    cmp rcx, 4
    jnl 6
    xor r8d, r8d
    xor edx, edx
2:
    cmp rdx, 4
    jnl 3
    mov rsi, rdx
    add rsi, 1
    add r8, rdx
    mov rdx, rsi
    jmp 2
3:
    xor esi, esi
    xor edx, edx
4:
    cmp rsi, 4
    jnl 5
    mov rdi, rsi
    add rdi, 1
    lea rdx, [rdx + rsi*2]
    mov rsi, rdi
    jmp 4
5:
    add r8, rdx
    add rcx, 1
    add rax, r8
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: moderate-quick ===========
main:
    push rbx
    push rbp
    sub rsp, 8
    xor eax, eax
    xor ecx, ecx
1:
    cmp rcx, 4
    jnl 6
    xor edx, edx
    xor ebx, ebx
2:
    mov rbp, rbx
    cmp rbp, 4
    jnl 3
    mov rsi, rbp
    add rsi, 1
    add rdx, rbp
    mov rbx, rsi
    jmp 2
3:
    xor ebx, ebx
    xor ebp, ebp
4:
    cmp rbx, 4
    jnl 5
    mov rsi, rbx
    add rsi, 1
    lea rdi, [rbp + rbx*2]
    mov rbx, rsi
    mov rbp, rdi
    jmp 4
5:
    add rdx, rbp
    mov rbx, rcx
    add rbx, 1
    mov rbp, rax
    add rbp, rdx
    mov rcx, rbx
    mov rax, rbp
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
main:
    xor eax, eax
    xor ecx, ecx
1:
    cmp rcx, 4
    jnl 6
    xor r8d, r8d
    xor edx, edx
2:
    cmp rdx, 4
    jnl 3
    mov rsi, rdx
    add rsi, 1
    add r8, rdx
    mov rdx, rsi
    jmp 2
3:
    xor esi, esi
    xor edx, edx
4:
    cmp rsi, 4
    jnl 5
    mov rdi, rsi
    add rdi, 1
    lea rdx, [rdx + rsi*2]
    mov rsi, rdi
    jmp 4
5:
    add r8, rdx
    add rcx, 1
    add rax, r8
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: all ===========
main:
    xor ecx, ecx
    xor edx, edx
1:
    xor eax, eax
    xor edi, edi
2:
    mov rsi, rdi
    mov rdi, rsi
    add rdi, rax
    mov r8, rax
    add r8, 1
    cmp rax, 4
    mov rax, r8
    jnge 2
    xor eax, eax
    xor edi, edi
3:
    mov r9, rdi
    mov r8, rax
    add r8, 1
    lea rdi, [r9 + rax*2]
    cmp rax, 4
    mov rax, r8
    jnge 3
    add rdx, 1
    add rsi, r9
    mov rax, rcx
    add rax, rsi
    cmp rdx, 4
    mov rcx, rax
    jnge 1
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: aggresive ===========
