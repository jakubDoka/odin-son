=========== OPT LEVEL: none ===========
main:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 0x48
    xor eax, eax
    add rax, 0xf
    mov.q [rsp + 0x10], rax
    xor eax, eax
    add rax, 0x10
    mov.q [rsp + 0x18], rax
    xor eax, eax
    add rax, 0xd
    mov.q [rsp], rax
    xor eax, eax
    add rax, 0xe
    mov r15, rax
    xor eax, eax
    add rax, 0xc
    mov r14, rax
    xor eax, eax
    add rax, 1
    mov r13, rax
    xor eax, eax
    add rax, 0xb
    mov r12, rax
    xor eax, eax
    add rax, 0xa
    mov rbp, rax
    xor esi, esi
    add rsi, 2
    xor eax, eax
    add rax, 3
    mov rbx, rax
    xor ecx, ecx
    add rcx, 4
    xor eax, eax
    add rax, 5
    xor edx, edx
    add rdx, 6
    mov.q [rsp + 0x20], rdx
    xor edx, edx
    add rdx, 7
    mov r11, rdx
    xor edx, edx
    add rdx, 8
    mov.q [rsp + 0x8], rdx
    xor edx, edx
    add rdx, 9
    mov r8, r13
    mov.q rdi, [rsp + 0x18]
    imul r8, rdi
    mov rdi, r8
    add rdi, rsi
    mov.q [rsp + 0x40], rdi
    mov r8, rcx
    mov.q rdi, [rsp]
    imul r8, rdi
    add r8, rax
    mov r9, rbx
    imul r9, r15
    mov rdi, r9
    add rdi, rcx
    mov.q [rsp + 0x38], rdi
    mov r9, rsi
    mov.q rdi, [rsp + 0x10]
    imul r9, rdi
    mov rdi, r9
    add rdi, rbx
    mov.q r9, [rsp + 0x8]
    imul r9, rdx
    mov r10, r9
    add r10, r13
    imul r10, r8
    mov.q r9, [rsp + 0x40]
    add r10, r9
    mov.q [rsp + 0x30], r10
    mov.q r9, [rsp + 0x20]
    imul r9, r12
    mov r10, r9
    add r10, r11
    imul r10, rdi
    mov.q r9, [rsp + 0x38]
    add r10, r9
    mov.q [rsp + 0x28], r10
    mov r10, r11
    imul r10, rbp
    mov r9, r10
    mov.q r10, [rsp + 0x8]
    add r9, r10
    mov.q r10, [rsp + 0x38]
    imul r9, r10
    add r9, r8
    mov r10, rax
    imul r10, r14
    mov r8, r10
    mov.q r10, [rsp + 0x20]
    add r8, r10
    mov.q r10, [rsp + 0x40]
    imul r8, r10
    add r8, rdi
    mov rdi, r8
    imul rdi, r9
    mov.q r8, [rsp + 0x28]
    add rdi, r8
    mov.q r8, [rsp + 0x28]
    mov.q r10, [rsp + 0x30]
    imul r8, r10
    add r8, r9
    imul rdi, r8
    mov r8, rdi
    mov.q rdi, [rsp + 0x30]
    add r8, rdi
    mov rdi, r8
    add rdi, r13
    add rdi, rsi
    mov rsi, rdi
    add rsi, rbx
    add rsi, rcx
    add rsi, rax
    mov.q rax, [rsp + 0x20]
    add rsi, rax
    add rsi, r11
    mov.q rax, [rsp + 0x8]
    add rsi, rax
    add rsi, rdx
    add rsi, rbp
    add rsi, r12
    add rsi, r14
    mov.q rax, [rsp]
    add rsi, rax
    add rsi, r15
    mov.q rax, [rsp + 0x10]
    add rsi, rax
    mov rax, rsi
    mov.q rcx, [rsp + 0x18]
    add rax, rcx
    add rsp, 0x48
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
main:
    movabs rax, 0x2990b80b3f6b
    ret
=========== OPT LEVEL: moderate ===========
main:
    movabs rax, 0x2990b80b3f6b
    ret
=========== OPT LEVEL: all ===========
main:
    movabs rax, 0x2990b80b3f6b
    ret
=========== OPT LEVEL: aggresive ===========
