=========== OPT LEVEL: none ===========
opaque:
     mov rax, rdi
     ret
mk:
     push rbx
     push rbp
     sub rsp, 0x18
     mov rbx, rdi
     mov rbp, rsi
     xor esi, esi
     lea rdi, [rsp]
     mov rdx, 0x10
     call :$set
     mov [rsp], rbx
     mov [rsp+0x08], rbp
     mov rax, [rsp]
     mov rdx, [rsp+0x08]
     add rsp, 0x18
     pop rbp
     pop rbx
     ret
main:
     push rbx
     sub rsp, 0x10
     xor esi, esi
     lea rdi, [rsp]
     mov rdx, 0x10
     call :$set
     mov rdi, 0x06
     call :opaque
     mov rbx, rax
     mov rdi, 0x09
     call :opaque
     mov rdi, rbx
     mov rsi, rax
     call :mk
     shr rax, 0x00
     mov [rsp], rax
     shr rdx, 0x00
     mov [rsp+0x08], rdx
     mov rax, [rsp+0x08]
     add [rsp], rax
     mov rax, [rsp]
     imul rax, rax, 0x64
     add rax, [rsp+0x08]
     add rsp, 0x10
     pop rbx
     ret
=========== OPT LEVEL: mininal ===========
opaque:
     mov rax, rdi
     ret
mk:
     push rbx
     push rbp
     sub rsp, 0x18
     mov rbx, rdi
     mov rbp, rsi
     xor esi, esi
     lea rdi, [rsp]
     mov rdx, 0x10
     call :$set
     mov [rsp], rbx
     mov [rsp+0x08], rbp
     mov rax, [rsp]
     mov rdx, [rsp+0x08]
     add rsp, 0x18
     pop rbp
     pop rbx
     ret
main:
     push rbx
     sub rsp, 0x10
     xor esi, esi
     lea rdi, [rsp]
     mov rdx, 0x10
     call :$set
     mov rdi, 0x06
     call :opaque
     mov rbx, rax
     mov rdi, 0x09
     call :opaque
     mov rdi, rbx
     mov rsi, rax
     call :mk
     shr rax, 0x00
     mov [rsp], rax
     shr rdx, 0x00
     mov [rsp+0x08], rdx
     mov rax, [rsp+0x08]
     add [rsp], rax
     mov rax, [rsp]
     imul rax, rax, 0x64
     add rax, [rsp+0x08]
     add rsp, 0x10
     pop rbx
     ret
=========== OPT LEVEL: moderate ===========
opaque:
     mov rax, rdi
     ret
mk:
     mov rax, rdi
     mov rcx, rsi
     mov rdx, rcx
     ret
main:
     push rbx
     mov rdi, 0x06
     call :opaque
     mov rbx, rax
     mov rdi, 0x09
     call :opaque
     mov rdi, rbx
     mov rsi, rax
     call :mk
     add rax, rdx
     imul rax, rax, 0x64
     add rax, rdx
     pop rbx
     ret
=========== OPT LEVEL: all ===========
opaque:
     mov rax, rdi
     ret
mk:
     mov rax, rdi
     mov rcx, rsi
     mov rdx, rcx
     ret
main:
     push rbx
     mov rdi, 0x06
     call :opaque
     mov rbx, rax
     mov rdi, 0x09
     call :opaque
     mov rdi, rbx
     mov rsi, rax
     call :mk
     add rax, rdx
     imul rax, rax, 0x64
     add rax, rdx
     pop rbx
     ret
=========== OPT LEVEL: aggresive ===========
opaque:
mk:
main:
     mov rax, 0x5E5
     ret
