=========== OPT LEVEL: none ===========
main:
     push rbx
     push rbp
     mov rcx, 0x06
     mov rax, 0x14
     xor rdx, rdx
     div rcx
     cmp rax, 0x03
     jnz :0
     jmp :1

000: mov rbx, 0x00
     jmp :2

001: mov rbx, 0x00
     add rbx, 0x01
     jmp :2

002: mov rax, 0x14
     xor rdx, rdx
     div rcx
     cmp rdx, 0x02
     jnz :3
     jmp :4

003: jmp :5

004: add rbx, 0x02
     jmp :5

005: mov rax, 0x00
     sub rax, 0x07
     mov rbp, rax
     mov rax, rbp
     xor rdx, rdx
     div rcx
     cmp rax, 0x14
     jbe :6
     jmp :7

006: jmp :8

007: add rbx, 0x04
     jmp :8

008: mov rax, rbp
     xor rdx, rdx
     div rcx
     cmp rdx, 0x03
     jnz :9
     jmp :10

009: jmp :11

010: add rbx, 0x08
     jmp :11

011: mov rax, rbp
     shr rax, 0x3C
     cmp rax, 0x0F
     jnz :12
     jmp :13

012: jmp :14

013: add rbx, 0x10
     jmp :14

014: mov rax, 0x14
     shr rax, 0x02
     cmp rax, 0x05
     jnz :15
     jmp :16

015: jmp :17

016: add rbx, 0x20
     jmp :17

017: mov rax, rbp
     cmp rax, 0x06
     jbe :18
     jmp :19

018: jmp :20

019: add rbx, 0x40
     jmp :20

020: mov rax, rbp
     cmp rax, 0x14
     jb :21
     jmp :22

021: jmp :23

022: add rbx, 0x80
     jmp :23

023: mov rax, rbp
     cmp rcx, rax
     jnb :24
     jmp :25

024: jmp :26

025: add rbx, 0x100
     jmp :26

026: mov rdx, rbx
     mov rax, rbp
     cmp rcx, rax
     jnbe :27
     jmp :28

027: jmp :29

028: add rdx, 0x200
     jmp :29

029: mov rax, 0x14
     mov rcx, rbp
     cmp rax, rcx
     jnb :30
     jmp :31

030: jmp :32

031: add rdx, 0x400
     jmp :32

032: mov rax, rdx
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: mininal ===========
main:
     push rbx
     push rbp
     mov rcx, 0x01
     test rcx, rcx
     jz :0
     jmp :1

000: mov rbx, 0x00
     jmp :2

001: mov rbx, 0x00
     add rbx, 0x01
     jmp :2

002: test rcx, rcx
     jz :3
     jmp :4

003: jmp :5

004: add rbx, 0x02
     jmp :5

005: mov rbp, 0x06
     mov rax, 0x00
     sub rax, 0x07
     mov rsi, rax
     mov rax, rsi
     xor rdx, rdx
     div rbp
     cmp rax, 0x14
     jbe :6
     jmp :7

006: jmp :8

007: add rbx, 0x04
     jmp :8

008: mov rax, rsi
     xor rdx, rdx
     div rbp
     cmp rdx, 0x03
     jnz :9
     jmp :10

009: jmp :11

010: add rbx, 0x08
     jmp :11

011: mov rax, rsi
     shr rax, 0x3C
     cmp rax, 0x0F
     jnz :12
     jmp :13

012: jmp :14

013: add rbx, 0x10
     jmp :14

014: test rcx, rcx
     jz :15
     jmp :16

015: jmp :17

016: add rbx, 0x20
     jmp :17

017: mov rax, rsi
     cmp rax, 0x06
     jbe :18
     jmp :19

018: jmp :20

019: add rbx, 0x40
     jmp :20

020: mov rax, rsi
     cmp rax, 0x14
     jb :21
     jmp :22

021: jmp :23

022: add rbx, 0x80
     jmp :23

023: mov rax, rsi
     cmp rbp, rax
     jnb :24
     jmp :25

024: jmp :26

025: add rbx, 0x100
     jmp :26

026: mov rax, rsi
     cmp rbp, rax
     jnbe :27
     jmp :28

027: jmp :29

028: add rbx, 0x200
     jmp :29

029: mov rcx, rbx
     mov rax, 0x14
     cmp rax, rsi
     jnb :30
     jmp :31

030: jmp :32

031: add rcx, 0x400
     jmp :32

032: mov rax, rcx
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: all ===========
main:
     mov rax, 0x7FF
     ret
