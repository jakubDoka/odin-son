=========== OPT LEVEL: None ===========
main:
     push rbx
     push rbp
     mov rsi, 0x00
     mov rax, 0x00
     jmp :0

000: mov rcx, 0x03
     mov rdx, rsi
     cmp rdx, rcx
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :1
     jmp :2

001: mov rbx, 0x00
     mov rbp, rax
     jmp :3

003: mov rax, rbx
     cmp rax, rcx
     setz al
     movzx rax, al
     mov rdx, 0x01
     test rax, rax
     jz :4
     jmp :5

004: mov rax, rsi
     imul rax, rbx
     add rax, rbp
     add rbx, rdx
     mov rbp, rax
     jmp :3

005: add rsi, rdx
     mov rax, rbp
     jmp :0

002: pop rbp
     pop rbx
     ret
=========== OPT LEVEL: Full ===========
main:
     push rbx
     push rbp
     mov rbp, 0x00
     mov rax, 0x00
     jmp :0

000: mov rcx, 0x03
     mov rdx, rbp
     cmp rdx, rcx
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :1
     jmp :2

001: mov rdx, 0x00
     mov rbx, rax
     jmp :3

003: mov rax, rdx
     cmp rax, rcx
     setz al
     movzx rax, al
     test rax, rax
     jz :4
     jmp :5

004: mov rax, rbp
     imul rax, rdx
     add rax, rbx
     add rdx, 0x01
     mov rbx, rax
     jmp :3

005: add rbp, 0x01
     mov rax, rbx
     jmp :0

002: pop rbp
     pop rbx
     ret
