=========== OPT LEVEL: none-quick ===========
main:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    adr x0, 
    bl d
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
d:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    blr x0
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
:
    movz x0, #10, #0
    ret
=========== OPT LEVEL: none ===========
main:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    adr x0, 
    bl d
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
d:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    blr x0
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
:
    movz x0, #10, #0
    ret
=========== OPT LEVEL: mininal-quick ===========
main:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    adr x0, 
    bl d
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
d:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    blr x0
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
:
    movz x0, #10, #0
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    adr x0, 
    bl d
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
d:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    blr x0
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
:
    movz x0, #10, #0
    ret
=========== OPT LEVEL: moderate-quick ===========
main:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    adr x0, 
    bl d
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
d:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    blr x0
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
:
    movz x0, #10, #0
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    adr x0, 
    bl d
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
d:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    blr x0
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
:
    movz x0, #10, #0
    ret
=========== OPT LEVEL: all ===========
main:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    adr x0, 
    bl d
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
d:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    blr x0
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
:
    movz x0, #10, #0
    ret
=========== OPT LEVEL: aggresive ===========
main:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    adr x0, 
    blr x0
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
:
    movz x0, #10, #0
    ret
