=========== OPT LEVEL: none ===========
main:
    xor eax, eax
    sub rax, 6
    mov rcx, rax
    xor eax, eax
    sub rax, 7
    mov rsi, rax
    mov rdi, rcx
    mov rax, 0x14
    cqo
    idiv rdi
    mov rdx, rax
    xor eax, eax
    add rdx, rax
    mov r8, rdx
    mov rdi, rcx
    mov rax, 0x14
    cqo
    idiv rdi
    add r8, rdx
    mov rdi, rcx
    mov rax, rsi
    cqo
    idiv rdi
    add r8, rax
    mov rdi, rcx
    mov rax, rsi
    cqo
    idiv rdi
    add r8, rdx
    mov rax, rsi
    sar rax, 1
    add r8, rax
    mov rax, rcx
    sar rax, 1
    add r8, rax
    cmp rcx, 0x14
    jnge 1
    jmp 2
1:
    add r8, 1
2:
    mov rdi, r8
    cmp rsi, rcx
    jnge 3
    jmp 4
3:
    add rdi, 2
4:
    mov rax, rcx
    mov rdx, 0x14
    cmp rdx, rax
    jnle 5
    jmp 6
5:
    add rdi, 4
6:
    cmp rcx, rsi
    jle 7
    jmp 8
7:
    add rdi, 8
8:
    mov rax, rcx
    mov rcx, 0x14
    cmp rcx, rax
    jnl 9
    jmp 10
9:
    add rdi, 0x10
10:
    mov rax, rdi
    ret
=========== OPT LEVEL: mininal ===========
main:
    mov cl, 1
    xor eax, eax
    add rax, -3
    add rax, 2
    add rax, 1
    add rax, -1
    add rax, -4
    add rax, -3
    test cl, cl
    jne 1
    jmp 2
1:
    add rax, 1
2:
    test cl, cl
    jne 3
    jmp 4
3:
    add rax, 2
4:
    test cl, cl
    jne 5
    jmp 6
5:
    add rax, 4
6:
    xor edx, edx
    test dl, dl
    jne 7
    jmp 8
7:
    add rax, 8
8:
    test cl, cl
    jne 9
    jmp 10
9:
    add rax, 0x10
10:
    ret
=========== OPT LEVEL: moderate ===========
main:
    mov rax, 0xf
    ret
=========== OPT LEVEL: all ===========
main:
    mov rax, 0xf
    ret
=========== OPT LEVEL: aggresive ===========
