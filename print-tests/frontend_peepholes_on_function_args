=========== OPT LEVEL: none ===========
main:
     sub rsp, 0x08
     mov rax, 0x02
     add rax, 0x02
     mov rcx, rax
     add rcx, 0x02
     mov rdi, 0x02
     mov rsi, rax
     mov rdx, rcx
     call :funnel
     add rsp, 0x08
     ret
funnel:
     mov rax, rdi
     add rax, rsi
     add rax, rdx
     ret
=========== OPT LEVEL: mininal ===========
main:
     sub rsp, 0x08
     mov rdi, 0x02
     mov rsi, 0x04
     mov rdx, 0x06
     call :funnel
     add rsp, 0x08
     ret
funnel:
     mov rax, rdi
     add rax, rsi
     add rax, rdx
     ret
=========== OPT LEVEL: moderate ===========
main:
     sub rsp, 0x08
     mov rdi, 0x02
     mov rsi, 0x04
     mov rdx, 0x06
     call :funnel
     add rsp, 0x08
     ret
funnel:
     mov rax, rdi
     add rax, rsi
     add rax, rdx
     ret
=========== OPT LEVEL: all ===========
main:
     sub rsp, 0x08
     mov rdi, 0x02
     mov rsi, 0x04
     mov rdx, 0x06
     call :funnel
     add rsp, 0x08
     ret
funnel:
     mov rax, rdi
     add rax, rsi
     add rax, rdx
     ret
