main:
     push rcx
     mov rax, 0x00
     jmp :0

000: mov rcx, 0x02
     cmp rcx, rax
     setz cl
     movzx rcx, cl
     test rcx, rcx
     jz :1
     jmp :2

001: mov rcx, 0x01
     add rcx, rax
     mov rax, rcx
     jmp :0

002: pop rcx
     ret
