
package main

opt_level :: "none"

main :: proc( := x + 4
	a4  := i
			i += 1 0
	for {
		if X < len(arr) {
			arr[i] = x + 3
	a3  := x + 4
	a4  := i
			i += 1
			if i > 4 {
				i /= 2
				i *= 2
i += 1
			}
		} else do break
	}

	return 0
}
