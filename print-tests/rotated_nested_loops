=========== OPT LEVEL: none ===========
main:
    xor ecx, ecx
    xor eax, eax
1:
    xor edx, edx
2:
    mov rsi, rcx
    imul rsi, rdx
    add rax, rsi
    add rdx, 1
    cmp rdx, 3
    jnle 3
    jmp 2
3:
    add rcx, 1
    cmp rcx, 3
    jnle 4
    jmp 1
4:
    ret
=========== OPT LEVEL: mininal ===========
main:
    xor ecx, ecx
    xor eax, eax
1:
    xor edx, edx
2:
    mov rsi, rcx
    imul rsi, rdx
    add rax, rsi
    add rdx, 1
    cmp rdx, 3
    jnle 3
    jmp 2
3:
    add rcx, 1
    cmp rcx, 3
    jnle 4
    jmp 1
4:
    ret
=========== OPT LEVEL: moderate ===========
main:
    xor ecx, ecx
    xor eax, eax
1:
    xor edx, edx
2:
    mov rsi, rcx
    imul rsi, rdx
    add rax, rsi
    add rdx, 1
    cmp rdx, 3
    jnle 3
    jmp 2
3:
    add rcx, 1
    cmp rcx, 3
    jnle 4
    jmp 1
4:
    ret
=========== OPT LEVEL: all ===========
main:
    xor esi, esi
    xor ecx, ecx
1:
    xor edx, edx
2:
    mov rdi, rcx
    imul rdi, rdx
    mov rax, rsi
    add rax, rdi
    add rdx, 1
    cmp rdx, 3
    mov rsi, rax
    jng 2
    add rcx, 1
    cmp rcx, 3
    mov rsi, rax
    jng 1
    ret
=========== OPT LEVEL: aggresive ===========
