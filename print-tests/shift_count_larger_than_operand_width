=========== OPT LEVEL: none ===========
sh:
    mov rdx, rdi
    mov rax, rsi
    mov rcx, rax
    shl rdx, cl
    mov rax, rdx
    ret
main:
    push rbx
    mov rdi, 1
    mov rsi, 0x40
    call sh
    mov rbx, rax
    mov rdi, 3
    mov rsi, 0x41
    call sh
    add rbx, rax
    mov rax, rbx
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
sh:
    mov rdx, rdi
    mov rax, rsi
    mov rcx, rax
    shl rdx, cl
    mov rax, rdx
    ret
main:
    push rbx
    mov rdi, 1
    mov rsi, 0x40
    call sh
    mov rbx, rax
    mov rdi, 3
    mov rsi, 0x41
    call sh
    add rbx, rax
    mov rax, rbx
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
sh:
    mov rdx, rdi
    mov rax, rsi
    mov rcx, rax
    shl rdx, cl
    mov rax, rdx
    ret
main:
    push rbx
    mov rdi, 1
    mov rsi, 0x40
    call sh
    mov rbx, rax
    mov rdi, 3
    mov rsi, 0x41
    call sh
    add rbx, rax
    mov rax, rbx
    pop rbx
    ret
=========== OPT LEVEL: all ===========
sh:
    mov rdx, rdi
    mov rax, rsi
    mov rcx, rax
    shl rdx, cl
    mov rax, rdx
    ret
main:
    push rbx
    mov rdi, 1
    mov rsi, 0x40
    call sh
    mov rbx, rax
    mov rdi, 3
    mov rsi, 0x41
    call sh
    add rbx, rax
    mov rax, rbx
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
main:
    xor eax, eax
    ret
