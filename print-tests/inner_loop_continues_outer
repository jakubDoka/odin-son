=========== OPT LEVEL: None ===========
main:
     push rbx
     push rbp
     mov rbp, 0x00
     mov rax, 0x00
     jmp :0

000: mov rcx, 0x01
     add rbp, rcx
     mov rdx, 0x05
     mov rbx, rbp
     cmp rbx, rdx
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :1
     jmp :2

001: mov rbx, 0x00
     add rbx, rcx
     cmp rbx, rdx
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :3
     jmp :4

003: mov rbx, rax
     mov rdx, 0x00
     jmp :5

005: add rdx, rcx
     add rbx, rcx
     mov rax, 0x02
     cmp rax, rdx
     setz al
     movzx rax, al
     test rax, rax
     jz :6
     jmp :7

006: jmp :5

007: jmp :8

004: mov rbx, rax
     jmp :8

008: mov rax, rbx
     jmp :0

002: pop rbp
     pop rbx
     ret
=========== OPT LEVEL: Full ===========
main:
     push rbx
     push rbp
     mov rcx, 0x00
     mov rbp, 0x00
     mov rax, 0x00
     jmp :0

000: mov rdx, 0x05
     add rbp, 0x01
     cmp rdx, rbp
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :1
     jmp :2

001: test rcx, rcx
     jz :3
     jmp :4

003: mov rbx, rax
     mov rdx, 0x00
     jmp :5

005: mov rax, 0x02
     add rdx, 0x01
     add rbx, 0x01
     cmp rax, rdx
     setz al
     movzx rax, al
     test rax, rax
     jz :6
     jmp :7

006: jmp :5

007: jmp :8

004: mov rbx, rax
     jmp :8

008: mov rax, rbx
     jmp :0

002: pop rbp
     pop rbx
     ret
