=========== OPT LEVEL: none ===========
main:
     sub rsp, 0x08
     mov rdi, 0x03
     call :opaque
     mov rcx, 0x05
     cmp rcx, rax
     setnl al
     movzx rax, al
     movzx eax, al
     add rsp, 0x08
     ret
opaque:
     mov rax, rdi
     ret
=========== OPT LEVEL: mininal ===========
main:
     sub rsp, 0x08
     mov rdi, 0x03
     call :opaque
     cmp rax, 0x05
     setle al
     movzx rax, al
     movzx eax, al
     add rsp, 0x08
     ret
opaque:
     mov rax, rdi
     ret
=========== OPT LEVEL: moderate ===========
main:
     sub rsp, 0x08
     mov rdi, 0x03
     call :opaque
     cmp rax, 0x05
     setle al
     movzx rax, al
     movzx eax, al
     add rsp, 0x08
     ret
opaque:
     mov rax, rdi
     ret
=========== OPT LEVEL: all ===========
main:
     sub rsp, 0x08
     mov rdi, 0x03
     call :opaque
     cmp rax, 0x05
     setle al
     movzx rax, al
     movzx eax, al
     add rsp, 0x08
     ret
opaque:
     mov rax, rdi
     ret
=========== OPT LEVEL: aggresive ===========
main:
     mov rax, 0x01
     ret
opaque:
