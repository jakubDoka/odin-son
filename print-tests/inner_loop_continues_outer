=========== OPT LEVEL: none ===========
main:
    xor eax, eax
    xor edx, edx
1:
    add rdx, 1
    cmp rdx, 5
    jz 5
    xor ecx, ecx
    add rcx, 1
    cmp rcx, 5
    jz 3
    mov rcx, rax
    xor eax, eax
2:
    add rcx, 1
    add rax, 1
    cmp rax, 2
    jz 4
    jmp 2
3:
    mov rcx, rax
4:
    mov rax, rcx
    jmp 1
5:
    ret
=========== OPT LEVEL: mininal ===========
main:
    xor eax, eax
    xor edx, edx
1:
    add rdx, 1
    cmp rdx, 5
    jz 5
    xor ecx, ecx
    add rcx, 1
    cmp rcx, 5
    jz 3
    mov rcx, rax
    xor eax, eax
2:
    add rcx, 1
    add rax, 1
    cmp rax, 2
    jz 4
    jmp 2
3:
    mov rcx, rax
4:
    mov rax, rcx
    jmp 1
5:
    ret
=========== OPT LEVEL: moderate ===========
main:
    xor edx, edx
    xor eax, eax
1:
    add rdx, 1
    cmp rdx, 5
    jz 4
    mov rcx, rax
    xor eax, eax
2:
    add rcx, 1
    add rax, 1
    cmp rax, 2
    jz 3
    jmp 2
3:
    mov rax, rcx
    jmp 1
4:
    ret
=========== OPT LEVEL: all ===========
main:
    xor edx, edx
    xor eax, eax
1:
    add rdx, 1
    cmp rdx, 5
    jz 4
    mov rcx, rax
    xor eax, eax
2:
    add rcx, 1
    add rax, 1
    cmp rax, 2
    jz 3
    jmp 2
3:
    mov rax, rcx
    jmp 1
4:
    ret
=========== OPT LEVEL: aggresive ===========
