     push rcx
     mov rcx, 0x00
     mov rax, rcx
     mov rdx, rcx
     jmp :0

000: mov rcx, 0x01
     add rdx, rcx
     mov rbx, 0x02
     cmp rbx, rdx
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :1
     jmp :2

001: mov rbx, 0x03
     cmp rbx, rdx
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :3
     jmp :4

003: mov rcx, 0x0A
     add rcx, rax
     mov rax, rcx
     jmp :0

004: jmp :5

005: add rax, rcx
     jmp :5

002: pop rcx
     ret
