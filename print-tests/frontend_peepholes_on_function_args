=========== OPT LEVEL: none ===========
main:
     mov rax, 0x02
     add rax, 0x02
     mov rcx, rax
     add rcx, 0x02
     mov rdi, 0x02
     mov rsi, rax
     mov rdx, rcx
     call :funnel
     ret
funnel:
     mov rax, rdi
     add rax, rsi
     add rax, rdx
     ret
=========== OPT LEVEL: mininal ===========
main:
     mov rdi, 0x02
     mov rsi, 0x04
     mov rdx, 0x06
     call :funnel
     ret
funnel:
     mov rax, rdi
     add rax, rsi
     add rax, rdx
     ret
=========== OPT LEVEL: all ===========
main:
     mov rdi, 0x02
     mov rsi, 0x04
     mov rdx, 0x06
     call :funnel
     ret
funnel:
     mov rax, rdi
     add rax, rsi
     add rax, rdx
     ret
