=========== OPT LEVEL: none-quick ===========
main:
    sub rsp, 8
    mov rdi, -0x64
    call opaque
    mov cl, 3
    cbw
    idiv cl
    movsx rax, al
    add rsp, 8
    ret
opaque:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rsp, 8
    ret
=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    mov rdi, -0x64
    call opaque
    mov cl, 3
    cbw
    idiv cl
    movsx rax, al
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: mininal-quick ===========
main:
    sub rsp, 8
    mov rdi, -0x64
    call opaque
    mov cl, 3
    cbw
    idiv cl
    movsx rax, al
    add rsp, 8
    ret
opaque:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rsp, 8
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, -0x64
    call opaque
    mov cl, 3
    cbw
    idiv cl
    movsx rax, al
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: moderate-quick ===========
main:
    sub rsp, 8
    mov rdi, -0x64
    call opaque
    mov cl, 3
    cbw
    idiv cl
    movsx rax, al
    add rsp, 8
    ret
opaque:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rsp, 8
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, -0x64
    call opaque
    mov cl, 3
    cbw
    idiv cl
    movsx rax, al
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, -0x64
    call opaque
    mov cl, 3
    cbw
    idiv cl
    movsx rax, al
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, -0x21
    ret
