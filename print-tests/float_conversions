=========== OPT LEVEL: none-quick ===========
opaque:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rsp, 8
    ret
2:
    push rbx
    push rbp
    sub rsp, 0x48
    mov rdi, -0x64
    call opaque
    mov.q [rsp], rax
    mov rdi, -0xbb8
    call opaque
    mov.q [rsp + 0x8], rax
    mov rdi, -0x11170
    call opaque
    mov.q [rsp + 0x10], rax
    mov rdi, -0x4c4b40
    call opaque
    mov.q [rsp + 0x18], rax
    mov rdi, 0xc8
    call opaque
    mov.q [rsp + 0x20], rax
    mov rdi, 0xea60
    call opaque
    mov.q [rsp + 0x28], rax
    mov rdi, 0x3d0900
    call opaque
    mov.q [rsp + 0x30], rax
    mov rdi, 0x6acfc0
    call opaque
    mov.q [rsp + 0x38], rax
    movsd.q xmm0, [rip + 0x1ba]
    mov.q rax, [rsp]
    movsx rcx, al
    mov.q rax, [rsp + 0x30]
    mov edx, eax
    mov.q rax, [rsp + 0x8]
    movsx rbx, ax
    mov.q rax, [rsp + 0x10]
    movsxd rbp, eax
    mov.q rax, [rsp + 0x20]
    movzx esi, al
    mov.q rax, [rsp + 0x28]
    movzx edi, ax
    movss.d xmm1, [rip + 0x188]
    movsd.q xmm2, [rip + 0x185]
    xor eax, eax
    movss.d xmm3, [rip + 0x183]
    cvtss2sd xmm3, xmm3
    addsd.q xmm3, [rip + 0x17b]
    cvtsd2ss xmm3, xmm3
    cvttss2si r8, xmm3
    add rax, r8
    cvtsi2ss xmm3, rcx
    cvttss2si r8, xmm3
    add rax, r8
    cvtsi2sd xmm3, rcx
    cvttsd2si rcx, xmm3
    add rax, rcx
    cvtsi2ss xmm3, rbx
    cvttss2si rcx, xmm3
    add rax, rcx
    cvtsi2sd xmm3, rbx
    cvttsd2si rcx, xmm3
    add rax, rcx
    cvtsi2ss xmm3, rbp
    cvttss2si rcx, xmm3
    add rax, rcx
    cvtsi2sd xmm3, rbp
    cvttsd2si rcx, xmm3
    add rax, rcx
    mov.q rcx, [rsp + 0x18]
    cvtsi2ss xmm3, rcx
    cvttss2si rcx, xmm3
    add rax, rcx
    mov.q rcx, [rsp + 0x18]
    cvtsi2sd xmm3, rcx
    cvttsd2si rcx, xmm3
    add rax, rcx
    cvtsi2ss xmm3, rsi
    cvttss2si rcx, xmm3
    add rax, rcx
    cvtsi2sd xmm3, rsi
    cvttsd2si rcx, xmm3
    add rax, rcx
    cvtsi2ss xmm3, rdi
    cvttss2si rcx, xmm3
    add rax, rcx
    cvtsi2sd xmm3, rdi
    cvttsd2si rcx, xmm3
    add rax, rcx
    cvtsi2ss xmm3, rdx
    cvttss2si rcx, xmm3
    add rax, rcx
    cvtsi2sd xmm3, rdx
    cvttsd2si rcx, xmm3
    add rax, rcx
    mov.q rcx, [rsp + 0x38]
    cvtsi2ss xmm3, rcx
    cvttss2si rcx, xmm3
    add rax, rcx
    mov.q rcx, [rsp + 0x38]
    cvtsi2sd xmm3, rcx
    cvttsd2si rcx, xmm3
    add rax, rcx
    cvttss2si ecx, xmm1
    movsxd rcx, ecx
    add rax, rcx
    cvttss2si rcx, xmm1
    add rax, rcx
    cvttsd2si ecx, xmm2
    movsxd rcx, ecx
    add rax, rcx
    cvttsd2si rcx, xmm2
    add rax, rcx
    cvttsd2si ecx, xmm0
    movzx ecx, cl
    add rax, rcx
    movss.d xmm1, [rip + 0x59]
    cvttss2si ecx, xmm1
    movzx ecx, cx
    add rax, rcx
    cvttsd2si ecx, xmm0
    mov ecx, ecx
    add rax, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rsp, 0x48
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: none ===========
opaque:
    mov rax, rdi
    ret
2:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, -0x64
    call opaque
    mov.q [rsp], rax
    mov rdi, -0xbb8
    call opaque
    mov r15, rax
    mov rdi, -0x11170
    call opaque
    mov r14, rax
    mov rdi, -0x4c4b40
    call opaque
    mov r13, rax
    mov rdi, 0xc8
    call opaque
    mov r12, rax
    mov rdi, 0xea60
    call opaque
    mov rbp, rax
    mov rdi, 0x3d0900
    call opaque
    mov rbx, rax
    mov rdi, 0x6acfc0
    call opaque
    mov r11, rax
    movsd.q xmm0, [rip + 0x1a0]
    mov.q rax, [rsp]
    movsx r9, al
    mov rax, rbx
    mov ecx, eax
    mov rax, r15
    movsx r8, ax
    mov rax, r14
    movsxd rdi, eax
    mov rax, r12
    movzx esi, al
    mov rax, rbp
    movzx edx, ax
    movss.d xmm2, [rip + 0x178]
    movsd.q xmm1, [rip + 0x175]
    xor eax, eax
    movss.d xmm3, [rip + 0x173]
    cvtss2sd xmm3, xmm3
    addsd.q xmm3, [rip + 0x16b]
    cvtsd2ss xmm3, xmm3
    cvttss2si r10, xmm3
    add rax, r10
    cvtsi2ss xmm3, r9
    cvttss2si r10, xmm3
    add rax, r10
    cvtsi2sd xmm3, r9
    cvttsd2si r9, xmm3
    add rax, r9
    cvtsi2ss xmm3, r8
    cvttss2si r9, xmm3
    add rax, r9
    cvtsi2sd xmm3, r8
    cvttsd2si r8, xmm3
    add rax, r8
    cvtsi2ss xmm3, rdi
    cvttss2si r8, xmm3
    add rax, r8
    cvtsi2sd xmm3, rdi
    cvttsd2si rdi, xmm3
    add rax, rdi
    cvtsi2ss xmm3, r13
    cvttss2si rdi, xmm3
    add rax, rdi
    cvtsi2sd xmm3, r13
    cvttsd2si rdi, xmm3
    add rax, rdi
    cvtsi2ss xmm3, rsi
    cvttss2si rdi, xmm3
    add rax, rdi
    cvtsi2sd xmm3, rsi
    cvttsd2si rsi, xmm3
    add rax, rsi
    cvtsi2ss xmm3, rdx
    cvttss2si rsi, xmm3
    add rax, rsi
    cvtsi2sd xmm3, rdx
    cvttsd2si rdx, xmm3
    add rax, rdx
    cvtsi2ss xmm3, rcx
    cvttss2si rdx, xmm3
    add rax, rdx
    cvtsi2sd xmm3, rcx
    cvttsd2si rcx, xmm3
    add rax, rcx
    cvtsi2ss xmm3, r11
    cvttss2si rcx, xmm3
    add rax, rcx
    cvtsi2sd xmm3, r11
    cvttsd2si rcx, xmm3
    add rax, rcx
    cvttss2si ecx, xmm2
    movsxd rcx, ecx
    add rax, rcx
    cvttss2si rcx, xmm2
    add rax, rcx
    cvttsd2si ecx, xmm1
    movsxd rcx, ecx
    add rax, rcx
    cvttsd2si rcx, xmm1
    add rax, rcx
    cvttsd2si ecx, xmm0
    movzx ecx, cl
    add rax, rcx
    movss.d xmm1, [rip + 0x5d]
    cvttss2si ecx, xmm1
    movzx ecx, cx
    add rax, rcx
    cvttsd2si ecx, xmm0
    mov ecx, ecx
    add rax, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal-quick ===========
opaque:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rsp, 8
    ret
2:
    push rbx
    push rbp
    sub rsp, 0x48
    mov rdi, -0x64
    call opaque
    mov.q [rsp], rax
    mov rdi, -0xbb8
    call opaque
    mov.q [rsp + 0x8], rax
    mov rdi, -0x11170
    call opaque
    mov.q [rsp + 0x10], rax
    mov rdi, -0x4c4b40
    call opaque
    mov.q [rsp + 0x18], rax
    mov rdi, 0xc8
    call opaque
    mov.q [rsp + 0x20], rax
    mov rdi, 0xea60
    call opaque
    mov.q [rsp + 0x28], rax
    mov rdi, 0x3d0900
    call opaque
    mov.q [rsp + 0x30], rax
    mov rdi, 0x6acfc0
    call opaque
    mov.q [rsp + 0x38], rax
    mov.q rax, [rsp + 0x30]
    mov ecx, eax
    mov.q rax, [rsp]
    movsx rdx, al
    mov.q rax, [rsp + 0x28]
    movzx ebx, ax
    mov.q rax, [rsp + 0x8]
    movsx rbp, ax
    mov.q rax, [rsp + 0x10]
    movsxd rsi, eax
    mov.q rax, [rsp + 0x20]
    movzx edi, al
    xor eax, eax
    cvtsi2ss xmm0, rdx
    cvttss2si r8, xmm0
    lea rax, [rax + r8 + 0x6]
    cvtsi2sd xmm0, rdx
    cvttsd2si rdx, xmm0
    add rax, rdx
    cvtsi2ss xmm0, rbp
    cvttss2si rdx, xmm0
    add rax, rdx
    cvtsi2sd xmm0, rbp
    cvttsd2si rdx, xmm0
    add rax, rdx
    cvtsi2ss xmm0, rsi
    cvttss2si rdx, xmm0
    add rax, rdx
    cvtsi2sd xmm0, rsi
    cvttsd2si rdx, xmm0
    add rax, rdx
    mov.q rdx, [rsp + 0x18]
    cvtsi2ss xmm0, rdx
    cvttss2si rdx, xmm0
    add rax, rdx
    mov.q rdx, [rsp + 0x18]
    cvtsi2sd xmm0, rdx
    cvttsd2si rdx, xmm0
    add rax, rdx
    cvtsi2ss xmm0, rdi
    cvttss2si rdx, xmm0
    add rax, rdx
    cvtsi2sd xmm0, rdi
    cvttsd2si rdx, xmm0
    add rax, rdx
    cvtsi2ss xmm0, rbx
    cvttss2si rdx, xmm0
    add rax, rdx
    cvtsi2sd xmm0, rbx
    cvttsd2si rdx, xmm0
    add rax, rdx
    cvtsi2ss xmm0, rcx
    cvttss2si rdx, xmm0
    add rax, rdx
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    mov.q rcx, [rsp + 0x38]
    cvtsi2ss xmm0, rcx
    cvttss2si rcx, xmm0
    add rax, rcx
    mov.q rcx, [rsp + 0x38]
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rax, 7
    add rax, 7
    add rax, -0xc
    add rax, -0xc
    add rax, 0xfa
    add rax, 0xfde8
    add rax, 0xfa
    add rax, 0xfa
    add rsp, 0x48
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
opaque:
    mov rax, rdi
    ret
2:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, -0x64
    call opaque
    mov.q [rsp], rax
    mov rdi, -0xbb8
    call opaque
    mov r15, rax
    mov rdi, -0x11170
    call opaque
    mov r14, rax
    mov rdi, -0x4c4b40
    call opaque
    mov r13, rax
    mov rdi, 0xc8
    call opaque
    mov r12, rax
    mov rdi, 0xea60
    call opaque
    mov rbp, rax
    mov rdi, 0x3d0900
    call opaque
    mov rbx, rax
    mov rdi, 0x6acfc0
    call opaque
    mov r11, rax
    mov rax, rbx
    mov ecx, eax
    mov.q rax, [rsp]
    movsx r9, al
    mov rax, rbp
    movzx edx, ax
    mov rax, r15
    movsx r8, ax
    mov rax, r14
    movsxd rdi, eax
    mov rax, r12
    movzx esi, al
    xor r10d, r10d
    cvtsi2ss xmm0, r9
    cvttss2si rax, xmm0
    lea rax, [r10 + rax + 0x6]
    cvtsi2sd xmm0, r9
    cvttsd2si r9, xmm0
    add rax, r9
    cvtsi2ss xmm0, r8
    cvttss2si r9, xmm0
    add rax, r9
    cvtsi2sd xmm0, r8
    cvttsd2si r8, xmm0
    add rax, r8
    cvtsi2ss xmm0, rdi
    cvttss2si r8, xmm0
    add rax, r8
    cvtsi2sd xmm0, rdi
    cvttsd2si rdi, xmm0
    add rax, rdi
    cvtsi2ss xmm0, r13
    cvttss2si rdi, xmm0
    add rax, rdi
    cvtsi2sd xmm0, r13
    cvttsd2si rdi, xmm0
    add rax, rdi
    cvtsi2ss xmm0, rsi
    cvttss2si rdi, xmm0
    add rax, rdi
    cvtsi2sd xmm0, rsi
    cvttsd2si rsi, xmm0
    add rax, rsi
    cvtsi2ss xmm0, rdx
    cvttss2si rsi, xmm0
    add rax, rsi
    cvtsi2sd xmm0, rdx
    cvttsd2si rdx, xmm0
    add rax, rdx
    cvtsi2ss xmm0, rcx
    cvttss2si rdx, xmm0
    add rax, rdx
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, r11
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, r11
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rax, 7
    add rax, 7
    add rax, -0xc
    add rax, -0xc
    add rax, 0xfa
    add rax, 0xfde8
    add rax, 0xfa
    add rax, 0xfa
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate-quick ===========
opaque:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rsp, 8
    ret
2:
    push rbx
    push rbp
    sub rsp, 0x48
    mov rdi, -0x64
    call opaque
    mov.q [rsp], rax
    mov rdi, -0xbb8
    call opaque
    mov.q [rsp + 0x8], rax
    mov rdi, -0x11170
    call opaque
    mov.q [rsp + 0x10], rax
    mov rdi, -0x4c4b40
    call opaque
    mov.q [rsp + 0x18], rax
    mov rdi, 0xc8
    call opaque
    mov.q [rsp + 0x20], rax
    mov rdi, 0xea60
    call opaque
    mov.q [rsp + 0x28], rax
    mov rdi, 0x3d0900
    call opaque
    mov.q [rsp + 0x30], rax
    mov rdi, 0x6acfc0
    call opaque
    mov.q [rsp + 0x38], rax
    mov.q rax, [rsp + 0x30]
    mov ecx, eax
    mov.q rax, [rsp]
    movsx rax, al
    mov.q rdx, [rsp + 0x8]
    movsx rdx, dx
    mov.q rbx, [rsp + 0x10]
    movsxd rbx, ebx
    mov.q rbp, [rsp + 0x20]
    movzx ebp, bpl
    mov.q rsi, [rsp + 0x28]
    movzx esi, si
    cvtsi2ss xmm0, rax
    cvttss2si rdi, xmm0
    cvtsi2sd xmm0, rax
    cvttsd2si rax, xmm0
    lea rax, [rdi + rax + 0x6]
    cvtsi2ss xmm0, rdx
    cvttss2si rdi, xmm0
    add rax, rdi
    cvtsi2sd xmm0, rdx
    cvttsd2si rdx, xmm0
    add rax, rdx
    cvtsi2ss xmm0, rbx
    cvttss2si rdx, xmm0
    add rax, rdx
    cvtsi2sd xmm0, rbx
    cvttsd2si rdx, xmm0
    add rax, rdx
    mov.q rdx, [rsp + 0x18]
    cvtsi2ss xmm0, rdx
    cvttss2si rdx, xmm0
    add rax, rdx
    mov.q rdx, [rsp + 0x18]
    cvtsi2sd xmm0, rdx
    cvttsd2si rdx, xmm0
    add rax, rdx
    cvtsi2ss xmm0, rbp
    cvttss2si rdx, xmm0
    add rax, rdx
    cvtsi2sd xmm0, rbp
    cvttsd2si rdx, xmm0
    add rax, rdx
    cvtsi2ss xmm0, rsi
    cvttss2si rdx, xmm0
    add rax, rdx
    cvtsi2sd xmm0, rsi
    cvttsd2si rdx, xmm0
    add rax, rdx
    cvtsi2ss xmm0, rcx
    cvttss2si rdx, xmm0
    add rax, rdx
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    mov.q rcx, [rsp + 0x38]
    cvtsi2ss xmm0, rcx
    cvttss2si rcx, xmm0
    add rax, rcx
    mov.q rcx, [rsp + 0x38]
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rax, 0x100cc
    add rsp, 0x48
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
opaque:
    mov rax, rdi
    ret
2:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, -0x64
    call opaque
    mov.q [rsp], rax
    mov rdi, -0xbb8
    call opaque
    mov r15, rax
    mov rdi, -0x11170
    call opaque
    mov r14, rax
    mov rdi, -0x4c4b40
    call opaque
    mov r13, rax
    mov rdi, 0xc8
    call opaque
    mov r12, rax
    mov rdi, 0xea60
    call opaque
    mov rbp, rax
    mov rdi, 0x3d0900
    call opaque
    mov rbx, rax
    mov rdi, 0x6acfc0
    call opaque
    mov r10, rax
    mov rax, rbx
    mov ecx, eax
    mov.q rax, [rsp]
    movsx rax, al
    mov rdx, r15
    movsx r8, dx
    mov rdx, r14
    movsxd rdi, edx
    mov rdx, r12
    movzx esi, dl
    mov rdx, rbp
    movzx edx, dx
    cvtsi2ss xmm0, rax
    cvttss2si r9, xmm0
    cvtsi2sd xmm0, rax
    cvttsd2si rax, xmm0
    lea rax, [r9 + rax + 0x6]
    cvtsi2ss xmm0, r8
    cvttss2si r9, xmm0
    add rax, r9
    cvtsi2sd xmm0, r8
    cvttsd2si r8, xmm0
    add rax, r8
    cvtsi2ss xmm0, rdi
    cvttss2si r8, xmm0
    add rax, r8
    cvtsi2sd xmm0, rdi
    cvttsd2si rdi, xmm0
    add rax, rdi
    cvtsi2ss xmm0, r13
    cvttss2si rdi, xmm0
    add rax, rdi
    cvtsi2sd xmm0, r13
    cvttsd2si rdi, xmm0
    add rax, rdi
    cvtsi2ss xmm0, rsi
    cvttss2si rdi, xmm0
    add rax, rdi
    cvtsi2sd xmm0, rsi
    cvttsd2si rsi, xmm0
    add rax, rsi
    cvtsi2ss xmm0, rdx
    cvttss2si rsi, xmm0
    add rax, rsi
    cvtsi2sd xmm0, rdx
    cvttsd2si rdx, xmm0
    add rax, rdx
    cvtsi2ss xmm0, rcx
    cvttss2si rdx, xmm0
    add rax, rdx
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, r10
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, r10
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rax, 0x100cc
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: all ===========
opaque:
    mov rax, rdi
    ret
2:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, -0x64
    call opaque
    mov.q [rsp], rax
    mov rdi, -0xbb8
    call opaque
    mov r15, rax
    mov rdi, -0x11170
    call opaque
    mov r14, rax
    mov rdi, -0x4c4b40
    call opaque
    mov r13, rax
    mov rdi, 0xc8
    call opaque
    mov r12, rax
    mov rdi, 0xea60
    call opaque
    mov rbp, rax
    mov rdi, 0x3d0900
    call opaque
    mov rbx, rax
    mov rdi, 0x6acfc0
    call opaque
    mov r10, rax
    mov rax, rbx
    mov ecx, eax
    mov.q rax, [rsp]
    movsx rax, al
    mov rdx, r15
    movsx r8, dx
    mov rdx, r14
    movsxd rdi, edx
    mov rdx, r12
    movzx esi, dl
    mov rdx, rbp
    movzx edx, dx
    cvtsi2ss xmm0, rax
    cvttss2si r9, xmm0
    cvtsi2sd xmm0, rax
    cvttsd2si rax, xmm0
    lea rax, [r9 + rax + 0x6]
    cvtsi2ss xmm0, r8
    cvttss2si r9, xmm0
    add rax, r9
    cvtsi2sd xmm0, r8
    cvttsd2si r8, xmm0
    add rax, r8
    cvtsi2ss xmm0, rdi
    cvttss2si r8, xmm0
    add rax, r8
    cvtsi2sd xmm0, rdi
    cvttsd2si rdi, xmm0
    add rax, rdi
    cvtsi2ss xmm0, r13
    cvttss2si rdi, xmm0
    add rax, rdi
    cvtsi2sd xmm0, r13
    cvttsd2si rdi, xmm0
    add rax, rdi
    cvtsi2ss xmm0, rsi
    cvttss2si rdi, xmm0
    add rax, rdi
    cvtsi2sd xmm0, rsi
    cvttsd2si rsi, xmm0
    add rax, rsi
    cvtsi2ss xmm0, rdx
    cvttss2si rsi, xmm0
    add rax, rsi
    cvtsi2sd xmm0, rdx
    cvttsd2si rdx, xmm0
    add rax, rdx
    cvtsi2ss xmm0, rcx
    cvttss2si rdx, xmm0
    add rax, rdx
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, r10
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, r10
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rax, 0x100cc
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
opaque:
    mov rax, 0xb7b70a
    ret
