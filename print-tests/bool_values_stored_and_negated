=========== OPT LEVEL: none ===========
main:
    mov rcx, 5
    xor eax, eax
    cmp rcx, 0xa
    setnge dl
    movzx rdx, dl
    test dl, dl
    jne 0
    jmp 1
0:
    add rax, 1
1:
    cmp rcx, 0xa
    setz cl
    movzx rcx, cl
    cmp cl, 0
    jz 2
    jmp 3
2:
    add rax, 2
3:
    cmp dl, 0
    jz 4
    jmp 5
4:
    add rax, 4
5:
    ret
=========== OPT LEVEL: mininal ===========
main:
    mov cl, 1
    xor eax, eax
    test cl, cl
    jne 0
    jmp 1
0:
    add rax, 1
1:
    test cl, cl
    jne 2
    jmp 3
2:
    add rax, 2
3:
    xor ecx, ecx
    test cl, cl
    jne 4
    jmp 5
4:
    add rax, 4
5:
    ret
=========== OPT LEVEL: moderate ===========
main:
    mov rax, 3
    ret
=========== OPT LEVEL: all ===========
main:
    mov rax, 3
    ret
=========== OPT LEVEL: aggresive ===========
