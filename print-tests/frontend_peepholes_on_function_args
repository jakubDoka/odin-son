=========== OPT LEVEL: none-quick ===========
main:
    sub rsp, 8
    mov rdi, 2
    mov rsi, 4
    mov rdx, 6
    call funnel
    add rsp, 8
    ret
funnel:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rax, rsi
    mov rax, rax
    add rax, rdx
    mov rax, rax
    add rsp, 8
    ret
=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    mov rdi, 2
    mov rsi, 4
    mov rdx, 6
    call funnel
    add rsp, 8
    ret
funnel:
    mov rax, rdi
    add rax, rsi
    add rax, rdx
    ret
=========== OPT LEVEL: mininal-quick ===========
main:
    sub rsp, 8
    mov rdi, 2
    mov rsi, 4
    mov rdx, 6
    call funnel
    add rsp, 8
    ret
funnel:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rax, rsi
    mov rax, rax
    add rax, rdx
    mov rax, rax
    add rsp, 8
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, 2
    mov rsi, 4
    mov rdx, 6
    call funnel
    add rsp, 8
    ret
funnel:
    mov rax, rdi
    add rax, rsi
    add rax, rdx
    ret
=========== OPT LEVEL: moderate-quick ===========
main:
    sub rsp, 8
    mov rdi, 2
    mov rsi, 4
    mov rdx, 6
    call funnel
    add rsp, 8
    ret
funnel:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rax, rsi
    mov rax, rax
    add rax, rdx
    mov rax, rax
    add rsp, 8
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, 2
    mov rsi, 4
    mov rdx, 6
    call funnel
    add rsp, 8
    ret
funnel:
    mov rax, rdi
    add rax, rsi
    add rax, rdx
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, 2
    mov rsi, 4
    mov rdx, 6
    call funnel
    add rsp, 8
    ret
funnel:
    mov rax, rdi
    add rax, rsi
    add rax, rdx
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 0xc
    ret
=========== OPT LEVEL: none-quick ===========
(module
  (type (;0;) (func (result i64)))
  (type (;1;) (func (param i64 i64 i64) (result i64)))
  (func $main (type 0) (result i64)
    (local i64)
    i64.const 2
    i64.const 4
    i64.const 6
    call $funnel
    local.set 0
    local.get 0
    return
    end
  )
  (func $funnel (type 1) (param i64 i64 i64) (result i64)
    local.get 0
    local.get 1
    i64.add
    local.get 2
    i64.add
    return
    end
  )
  (memory (;0;) 16)
  (global (;0;) (mut i64) i64.const 1048576)
  (export "main" (func 0))
  (export "__stack_pointer" (global 0))
  (export "memory" (memory 0))
)
