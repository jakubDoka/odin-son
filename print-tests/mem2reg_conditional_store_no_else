=========== OPT LEVEL: none ===========
opaque:
    mov rax, rdi
    ret
2:
    sub rsp, 0x18
    xor esi, esi
    lea rdi, [rsp]
    mov rdx, 0x10
    call.q [rip - 0x28]
    mov rdi, 3
    call opaque
    mov rdi, 5
    mov.q [rsp], rax
    call opaque
    mov rdi, 1
    mov.q [rsp + 0x8], rax
    call opaque
    cmp rax, 0
    jnle 3
    mov.q [rsp], 0x64
    jmp 3
3:
    mov.q rax, [rsp]
    add rsp, 0x18
    ret
=========== OPT LEVEL: mininal ===========
opaque:
    mov rax, rdi
    ret
2:
    sub rsp, 0x18
    xor esi, esi
    lea rdi, [rsp]
    mov rdx, 0x10
    call.q [rip - 0x28]
    mov rdi, 3
    call opaque
    mov rdi, 5
    mov.q [rsp], rax
    call opaque
    mov rdi, 1
    mov.q [rsp + 0x8], rax
    call opaque
    cmp rax, 0
    jnle 3
    mov.q [rsp], 0x64
    jmp 3
3:
    mov.q rax, [rsp]
    add rsp, 0x18
    ret
=========== OPT LEVEL: moderate ===========
opaque:
    mov rax, rdi
    ret
2:
    sub rsp, 0x18
    mov rdi, 3
    pxor xmm0, xmm0
    movups.x [rsp], xmm0
    call opaque
    mov rdi, 5
    mov.q [rsp], rax
    call opaque
    mov rdi, 1
    mov.q [rsp + 0x8], rax
    call opaque
    cmp rax, 0
    jnle 3
    mov.q [rsp], 0x64
    jmp 3
3:
    mov.q rax, [rsp]
    add rsp, 0x18
    ret
=========== OPT LEVEL: all ===========
opaque:
    mov rax, rdi
    ret
2:
    sub rsp, 0x18
    mov rdi, 3
    pxor xmm0, xmm0
    movups.x [rsp], xmm0
    call opaque
    mov rdi, 5
    mov.q [rsp], rax
    call opaque
    mov rdi, 1
    mov.q [rsp + 0x8], rax
    call opaque
    cmp rax, 0
    jnle 3
    mov.q [rsp], 0x64
    jmp 3
3:
    mov.q rax, [rsp]
    add rsp, 0x18
    ret
=========== OPT LEVEL: aggresive ===========
opaque:
    mov rax, 3
    ret
