=========== OPT LEVEL: none ===========
main:
    mov rcx, 0xa
    xor esi, esi
    mov rax, 1
1:
    cmp rcx, 0
    jz 2
    sub rcx, 1
    mov rdx, rsi
    add rdx, rax
    mov rsi, rax
    mov rax, rdx
    jmp 1
2:
    ret
=========== OPT LEVEL: mininal ===========
main:
    mov rcx, 0xa
    xor esi, esi
    mov rax, 1
1:
    cmp rcx, 0
    jz 2
    sub rcx, 1
    mov rdx, rsi
    add rdx, rax
    mov rsi, rax
    mov rax, rdx
    jmp 1
2:
    ret
=========== OPT LEVEL: moderate ===========
main:
    mov rcx, 0xa
    xor esi, esi
    mov rax, 1
1:
    cmp rcx, 0
    jz 2
    sub rcx, 1
    mov rdx, rsi
    add rdx, rax
    mov rsi, rax
    mov rax, rdx
    jmp 1
2:
    ret
=========== OPT LEVEL: all ===========
main:
    xor esi, esi
    mov rdi, 0xa
    mov rdx, 1
1:
    mov rax, rdx
    mov rcx, rdi
    mov rdi, rcx
    sub rdi, 1
    mov rdx, rsi
    add rdx, rax
    cmp rcx, 0
    mov rsi, rax
    jnz 1
    ret
=========== OPT LEVEL: aggresive ===========
