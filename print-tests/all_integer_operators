=========== OPT LEVEL: none ===========
batch:
    push rbx
batch int:
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0x14
    mov rsi, 6
    mov rdx, -7
    call 9
    mov rbx, rax
batch u32:
    mov dl, 7
    mov dil, 0x14
    mov sil, 6
    call 24
    mov rbp, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 38
    mov r12, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 52
    mov r13, rax
    mov dl, -7
    mov dil, 0x14
    mov sil, 6
    call 67
    mov r14, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 82
    mov r15, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 97
    mov rdx, rax
    xor eax, eax
    add rax, rbx
    add rax, rbp
    add rax, r12
    add rax, r13
    add rax, r14
    add rax, r15
    add rax, rdx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
9:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul rax, rsi
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rax
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rax
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rcx
    and rax, rsi
    add r8, rax
    mov rax, rcx
    or rax, rsi
    add r8, rax
    mov rax, rcx
    xor rax, rsi
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not rdx
    and rdx, rax
    add r8, rdx
    mov rax, rcx
    sal rax, 2
    add r8, rax
    mov rax, rcx
    sar rax, 2
    add r8, rax
    mov rax, rdi
    sar rax, 2
    add r8, rax
    cmp rcx, rsi
    jnle 10
    jmp 11
10:
    add r8, 1
11:
    cmp rsi, rcx
    jnge 12
    jmp 13
12:
    add r8, 2
13:
    mov rdx, r8
    cmp rcx, rsi
    jnl 14
    jmp 15
14:
    add rdx, 4
15:
    cmp rcx, rsi
    jng 16
    jmp 17
16:
    add rdx, 8
17:
    cmp rcx, rcx
    jz 18
    jmp 19
18:
    add rdx, 0x10
19:
    cmp rcx, rsi
    jnz 20
    jmp 21
20:
    add rdx, 0x20
21:
    cmp rdi, rsi
    jnge 22
    jmp 23
22:
    add rdx, 0x40
23:
    mov rax, rdx
    ret
24:
    mov rcx, rsi
    mov rsi, rdi
    mov rdi, rdx
    mov rax, rcx
    imul sil
    movzx eax, al
    xor r8d, r8d
    add r8, rax
    mov r9, rcx
    mov rax, rsi
    movzx eax, al
    div r9b
    movzx eax, al
    mov r9, r8
    add r9, rax
    mov r8, rcx
    mov rax, rsi
    movzx eax, al
    div r8b
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov r8, rcx
    mov rax, rdi
    movzx eax, al
    div r8b
    movzx eax, al
    add r9, rax
    mov r8, rcx
    mov rax, rdi
    movzx eax, al
    div r8b
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov rax, rcx
    mov rdx, rsi
    and dl, al
    movzx eax, dl
    mov r8, r9
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    or dl, al
    movzx eax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    xor dl, al
    movzx eax, dl
    add r8, rax
    mov rdx, rcx
    not dl
    and dl, sil
    movzx eax, dl
    add r8, rax
    mov rax, rsi
    shl al, 2
    movzx eax, al
    add r8, rax
    mov rax, rsi
    shr al, 2
    movzx eax, al
    add r8, rax
    mov rax, rdi
    shr al, 2
    movzx eax, al
    add r8, rax
    cmp sil, cl
    jnbe 25
    jmp +0x7
25:
    add r8, 1
    cmp cl, sil
    jc 26
    jmp 27
26:
    add r8, 2
27:
    cmp sil, cl
    jnc 28
    jmp 29
28:
    add r8, 4
29:
    cmp sil, cl
    jbe 30
    jmp 31
30:
    add r8, 8
31:
    cmp sil, sil
    jz 32
    jmp 33
32:
    add r8, 0x10
33:
    cmp sil, cl
    jnz 34
    jmp 35
34:
    add r8, 0x20
35:
    cmp dil, cl
    jc 36
    jmp 37
36:
    add r8, 0x40
37:
    mov rax, r8
    ret
38:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movzx eax, ax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    sal ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    shr ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    shr ax, 2
    movzx eax, ax
    add r8, rax
    cmp cx, si
    jnbe 39
    jmp 40
39:
    add r8, 1
40:
    mov rdx, r8
    cmp si, cx
    jc +0x5
    jmp 41
    add rdx, 2
41:
    cmp cx, si
    jnc 42
    jmp 43
42:
    add rdx, 4
43:
    cmp cx, si
    jbe 44
    jmp 45
44:
    add rdx, 8
45:
    cmp cx, cx
    jz 46
    jmp 47
46:
    add rdx, 0x10
47:
    cmp cx, si
    jnz 48
    jmp 49
48:
    add rdx, 0x20
49:
    cmp di, si
    jc 50
    jmp 51
50:
    add rdx, 0x40
51:
    mov rax, rdx
    ret
52:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    mov eax, eax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    mov eax, edx
    add r8, rax
    mov rax, rcx
    sal eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rcx
    shr eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rdi
    shr eax, 2
    mov eax, eax
    add r8, rax
    cmp ecx, esi
    jnbe 53
    jmp 54
53:
    add r8, 1
54:
    mov rdx, r8
    cmp esi, ecx
    jc 55
    jmp 56
55:
    add rdx, 2
56:
    cmp ecx, esi
    jnc 57
    jmp 58
57:
    add rdx, 4
58:
    cmp ecx, esi
    jbe 59
    jmp 60
59:
    add rdx, 8
60:
    cmp ecx, ecx
    jz 61
    jmp 62
61:
    add rdx, 0x10
62:
    cmp ecx, esi
    jnz 63
    jmp 64
63:
    add rdx, 0x20
64:
    cmp edi, esi
    jc 65
    jmp 66
65:
    add rdx, 0x40
66:
    mov rax, rdx
    ret
67:
    mov rcx, rsi
    mov rsi, rdi
    mov rdi, rdx
    mov rax, rcx
    imul sil
    movsx rax, al
    xor r8d, r8d
    add r8, rax
    mov r9, rcx
    mov rax, rsi
    cbw
    idiv r9b
    movsx rax, al
    mov r9, r8
    add r9, rax
    mov r8, rcx
    mov rax, rsi
    cbw
    idiv r8b
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov r8, rcx
    mov rax, rdi
    cbw
    idiv r8b
    movsx rax, al
    add r9, rax
    mov r8, rcx
    mov rax, rdi
    cbw
    idiv r8b
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov rax, rcx
    mov rdx, rsi
    and dl, al
    movsx rax, dl
    mov r8, r9
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    or dl, al
    movsx rax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    xor dl, al
    movsx rax, dl
    add r8, rax
    mov rdx, rcx
    not dl
    and dl, sil
    movsx rax, dl
    add r8, rax
    mov rax, rsi
    shl al, 2
    movsx rax, al
    add r8, rax
    mov rax, rsi
    sar al, 2
    movsx rax, al
    add r8, rax
    mov rax, rdi
    sar al, 2
    movsx rax, al
    add r8, rax
    cmp sil, cl
    jnle 68
    jmp 69
68:
    add r8, 1
69:
    cmp cl, sil
    jnge 70
    jmp 71
70:
    add r8, 2
71:
    cmp sil, cl
    jnl 72
    jmp 73
72:
    add r8, 4
73:
    cmp sil, cl
    jng 74
    jmp 75
74:
    add r8, 8
75:
    cmp sil, sil
    jz 76
    jmp 77
76:
    add r8, 0x10
77:
    cmp sil, cl
    jnz 78
    jmp 79
78:
    add r8, 0x20
79:
    cmp dil, cl
    jnge 80
    jmp 81
80:
    add r8, 0x40
81:
    mov rax, r8
    ret
82:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movsx rax, ax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    sal ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    sar ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    sar ax, 2
    movsx rax, ax
    add r8, rax
    cmp cx, si
    jnle 83
    jmp 84
83:
    add r8, 1
84:
    mov rdx, r8
    cmp si, cx
    jnge 85
    jmp 86
85:
    add rdx, 2
86:
    cmp cx, si
    jnl 87
    jmp 88
87:
    add rdx, 4
88:
    cmp cx, si
    jng 89
    jmp 90
89:
    add rdx, 8
90:
    cmp cx, cx
    jz 91
    jmp 92
91:
    add rdx, 0x10
92:
    cmp cx, si
    jnz 93
    jmp 94
93:
    add rdx, 0x20
94:
    cmp di, si
    jnge 95
    jmp 96
95:
    add rdx, 0x40
96:
    mov rax, rdx
    ret
97:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    movsxd rax, eax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    sal eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    cmp ecx, esi
    jnle 98
    jmp 99
98:
    add r8, 1
99:
    mov rdx, r8
    cmp esi, ecx
    jnge 100
    jmp 101
100:
    add rdx, 2
101:
    cmp ecx, esi
    jnl 102
    jmp 103
102:
    add rdx, 4
103:
    cmp ecx, esi
    jng 104
    jmp 105
104:
    add rdx, 8
105:
    cmp ecx, ecx
    jz 106
    jmp 107
106:
    add rdx, 0x10
107:
    cmp ecx, esi
    jnz 108
    jmp 109
108:
    add rdx, 0x20
109:
    cmp edi, esi
    jnge 110
    jmp 111
110:
    add rdx, 0x40
111:
    mov rax, rdx
    ret
=========== OPT LEVEL: mininal ===========
batch:
    push rbx
batch int:
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0x14
    mov rsi, 6
    mov rdx, -7
    call 9
    mov rbx, rax
batch u32:
    mov dl, 7
    mov dil, 0x14
    mov sil, 6
    call 24
    mov rbp, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 38
    mov r12, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 52
    mov r13, rax
    mov dl, -7
    mov dil, 0x14
    mov sil, 6
    call 67
    mov r14, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 82
    mov r15, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 97
    mov rdx, rax
    xor eax, eax
    add rax, rbx
    add rax, rbp
    add rax, r12
    add rax, r13
    add rax, r14
    add rax, r15
    add rax, rdx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
9:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul rax, rsi
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rax
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rax
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rcx
    and rax, rsi
    add r8, rax
    mov rax, rcx
    or rax, rsi
    add r8, rax
    mov rax, rcx
    xor rax, rsi
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not rdx
    and rdx, rax
    add r8, rdx
    mov rax, rcx
    sal rax, 2
    add r8, rax
    mov rax, rcx
    sar rax, 2
    add r8, rax
    mov rax, rdi
    sar rax, 2
    add r8, rax
    cmp rcx, rsi
    jnle 10
    jmp 11
10:
    add r8, 1
11:
    cmp rsi, rcx
    jnge 12
    jmp 13
12:
    add r8, 2
13:
    mov rdx, r8
    cmp rcx, rsi
    jnl 14
    jmp 15
14:
    add rdx, 4
15:
    cmp rcx, rsi
    jng 16
    jmp 17
16:
    add rdx, 8
17:
    mov rax, 1
    test rax, rax
    jnz 18
    jmp 19
18:
    add rdx, 0x10
19:
    cmp rcx, rsi
    jnz 20
    jmp 21
20:
    add rdx, 0x20
21:
    cmp rdi, rsi
    jnge 22
    jmp 23
22:
    add rdx, 0x40
23:
    mov rax, rdx
    ret
24:
    mov rcx, rsi
    mov rsi, rdi
    mov rdi, rdx
    mov rax, rcx
    imul sil
    movzx eax, al
    xor r8d, r8d
    add r8, rax
    mov r9, rcx
    mov rax, rsi
    movzx eax, al
    div r9b
    movzx eax, al
    mov r9, r8
    add r9, rax
    mov r8, rcx
    mov rax, rsi
    movzx eax, al
    div r8b
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov r8, rcx
    mov rax, rdi
    movzx eax, al
    div r8b
    movzx eax, al
    add r9, rax
    mov r8, rcx
    mov rax, rdi
    movzx eax, al
    div r8b
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov rax, rcx
    mov rdx, rsi
    and dl, al
    movzx eax, dl
    mov r8, r9
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    or dl, al
    movzx eax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    xor dl, al
    movzx eax, dl
    add r8, rax
    mov rdx, rcx
    not dl
    and dl, sil
    movzx eax, dl
    add r8, rax
    mov rax, rsi
    shl al, 2
    movzx eax, al
    add r8, rax
    mov rax, rsi
    shr al, 2
    movzx eax, al
    add r8, rax
    mov rax, rdi
    shr al, 2
    movzx eax, al
    add r8, rax
    cmp sil, cl
    jnbe 25
    jmp +0x7
25:
    add r8, 1
    cmp cl, sil
    jc 26
    jmp 27
26:
    add r8, 2
27:
    cmp sil, cl
    jnc 28
    jmp 29
28:
    add r8, 4
29:
    cmp sil, cl
    jbe 30
    jmp 31
30:
    add r8, 8
31:
    mov rax, 1
    test rax, rax
    jnz 32
    jmp 33
32:
    add r8, 0x10
33:
    cmp sil, cl
    jnz 34
    jmp 35
34:
    add r8, 0x20
35:
    cmp dil, cl
    jc 36
    jmp 37
36:
    add r8, 0x40
37:
    mov rax, r8
    ret
38:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movzx eax, ax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    sal ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    shr ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    shr ax, 2
    movzx eax, ax
    add r8, rax
    cmp cx, si
    jnbe 39
    jmp 40
39:
    add r8, 1
40:
    mov rdx, r8
    cmp si, cx
    jc +0x5
    jmp 41
    add rdx, 2
41:
    cmp cx, si
    jnc 42
    jmp 43
42:
    add rdx, 4
43:
    cmp cx, si
    jbe 44
    jmp 45
44:
    add rdx, 8
45:
    mov rax, 1
    test rax, rax
    jnz 46
    jmp 47
46:
    add rdx, 0x10
47:
    cmp cx, si
    jnz 48
    jmp 49
48:
    add rdx, 0x20
49:
    cmp di, si
    jc 50
    jmp 51
50:
    add rdx, 0x40
51:
    mov rax, rdx
    ret
52:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    mov eax, eax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    mov eax, edx
    add r8, rax
    mov rax, rcx
    sal eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rcx
    shr eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rdi
    shr eax, 2
    mov eax, eax
    add r8, rax
    cmp ecx, esi
    jnbe 53
    jmp 54
53:
    add r8, 1
54:
    mov rdx, r8
    cmp esi, ecx
    jc 55
    jmp 56
55:
    add rdx, 2
56:
    cmp ecx, esi
    jnc 57
    jmp 58
57:
    add rdx, 4
58:
    cmp ecx, esi
    jbe 59
    jmp 60
59:
    add rdx, 8
60:
    mov rax, 1
    test rax, rax
    jnz 61
    jmp 62
61:
    add rdx, 0x10
62:
    cmp ecx, esi
    jnz 63
    jmp 64
63:
    add rdx, 0x20
64:
    cmp edi, esi
    jc 65
    jmp 66
65:
    add rdx, 0x40
66:
    mov rax, rdx
    ret
67:
    mov rcx, rsi
    mov rsi, rdi
    mov rdi, rdx
    mov rax, rcx
    imul sil
    movsx rax, al
    xor r8d, r8d
    add r8, rax
    mov r9, rcx
    mov rax, rsi
    cbw
    idiv r9b
    movsx rax, al
    mov r9, r8
    add r9, rax
    mov r8, rcx
    mov rax, rsi
    cbw
    idiv r8b
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov r8, rcx
    mov rax, rdi
    cbw
    idiv r8b
    movsx rax, al
    add r9, rax
    mov r8, rcx
    mov rax, rdi
    cbw
    idiv r8b
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov rax, rcx
    mov rdx, rsi
    and dl, al
    movsx rax, dl
    mov r8, r9
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    or dl, al
    movsx rax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    xor dl, al
    movsx rax, dl
    add r8, rax
    mov rdx, rcx
    not dl
    and dl, sil
    movsx rax, dl
    add r8, rax
    mov rax, rsi
    shl al, 2
    movsx rax, al
    add r8, rax
    mov rax, rsi
    sar al, 2
    movsx rax, al
    add r8, rax
    mov rax, rdi
    sar al, 2
    movsx rax, al
    add r8, rax
    cmp sil, cl
    jnle 68
    jmp 69
68:
    add r8, 1
69:
    cmp cl, sil
    jnge 70
    jmp 71
70:
    add r8, 2
71:
    cmp sil, cl
    jnl 72
    jmp 73
72:
    add r8, 4
73:
    cmp sil, cl
    jng 74
    jmp 75
74:
    add r8, 8
75:
    mov rax, 1
    test rax, rax
    jnz 76
    jmp 77
76:
    add r8, 0x10
77:
    cmp sil, cl
    jnz 78
    jmp 79
78:
    add r8, 0x20
79:
    cmp dil, cl
    jnge 80
    jmp 81
80:
    add r8, 0x40
81:
    mov rax, r8
    ret
82:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movsx rax, ax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    sal ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    sar ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    sar ax, 2
    movsx rax, ax
    add r8, rax
    cmp cx, si
    jnle 83
    jmp 84
83:
    add r8, 1
84:
    mov rdx, r8
    cmp si, cx
    jnge 85
    jmp 86
85:
    add rdx, 2
86:
    cmp cx, si
    jnl 87
    jmp 88
87:
    add rdx, 4
88:
    cmp cx, si
    jng 89
    jmp 90
89:
    add rdx, 8
90:
    mov rax, 1
    test rax, rax
    jnz 91
    jmp 92
91:
    add rdx, 0x10
92:
    cmp cx, si
    jnz 93
    jmp 94
93:
    add rdx, 0x20
94:
    cmp di, si
    jnge 95
    jmp 96
95:
    add rdx, 0x40
96:
    mov rax, rdx
    ret
97:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    movsxd rax, eax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    sal eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    cmp ecx, esi
    jnle 98
    jmp 99
98:
    add r8, 1
99:
    mov rdx, r8
    cmp esi, ecx
    jnge 100
    jmp 101
100:
    add rdx, 2
101:
    cmp ecx, esi
    jnl 102
    jmp 103
102:
    add rdx, 4
103:
    cmp ecx, esi
    jng 104
    jmp 105
104:
    add rdx, 8
105:
    mov rax, 1
    test rax, rax
    jnz 106
    jmp 107
106:
    add rdx, 0x10
107:
    cmp ecx, esi
    jnz 108
    jmp 109
108:
    add rdx, 0x20
109:
    cmp edi, esi
    jnge 110
    jmp 111
110:
    add rdx, 0x40
111:
    mov rax, rdx
    ret
=========== OPT LEVEL: moderate ===========
batch:
    push rbx
batch int:
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0x14
    mov rsi, 6
    mov rdx, -7
    call 9
    mov rbx, rax
    mov dl, 7
    mov dil, 0x14
    mov sil, 6
batch i8:
    call 22
    mov rbp, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 35
    mov r12, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 48
    mov r13, rax
    mov dl, -7
    mov dil, 0x14
    mov sil, 6
    call 61
    mov r14, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 74
    mov r15, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 87
    mov rdx, rbx
    add rdx, rbp
    add rdx, r12
    add rdx, r13
    add rdx, r14
    add rdx, r15
    add rdx, rax
    mov rax, rdx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
9:
    mov rcx, rdi
    mov rdi, rdx
    mov r8, rcx
    imul r8, rsi
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rax
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rax
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rcx
    and rax, rsi
    add r8, rax
    mov rax, rcx
    or rax, rsi
    add r8, rax
    mov rax, rcx
    xor rax, rsi
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not rdx
    and rdx, rax
    add r8, rdx
    mov rax, rcx
    sal rax, 2
    add r8, rax
    mov rax, rcx
    sar rax, 2
    add r8, rax
    mov rax, rdi
    sar rax, 2
    add r8, rax
    cmp rcx, rsi
    jnle 10
    jmp 11
10:
    add r8, 1
11:
    cmp rsi, rcx
    jnge 12
    jmp 13
12:
    add r8, 2
13:
    mov rdx, r8
    cmp rcx, rsi
    jnl 14
    jmp 15
14:
    add rdx, 4
15:
    cmp rcx, rsi
    jng 16
    jmp 17
16:
    add rdx, 8
17:
    cmp rcx, rsi
    jnz 18
    add rdx, 0x10
    jmp 19
18:
    add rdx, 0x30
19:
    cmp rdi, rsi
    jnge 20
    jmp 21
20:
    add rdx, 0x40
21:
    mov rax, rdx
    ret
22:
    mov rcx, rsi
    mov rsi, rdi
    mov rdi, rdx
    mov rax, rcx
    imul sil
    movzx r8d, al
    mov r9, rcx
    mov rax, rsi
    movzx eax, al
    div r9b
    movzx eax, al
    mov r9, r8
    add r9, rax
    mov r8, rcx
    mov rax, rsi
    movzx eax, al
    div r8b
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov r8, rcx
    mov rax, rdi
    movzx eax, al
    div r8b
    movzx eax, al
    add r9, rax
    mov r8, rcx
    mov rax, rdi
    movzx eax, al
    div r8b
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov rax, rcx
    mov rdx, rsi
    and dl, al
    movzx eax, dl
    mov r8, r9
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    or dl, al
    movzx eax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    xor dl, al
    movzx eax, dl
    add r8, rax
    mov rdx, rcx
    not dl
    and dl, sil
    movzx eax, dl
    add r8, rax
    mov rax, rsi
    shl al, 2
    movzx eax, al
    add r8, rax
    mov rax, rsi
    shr al, 2
    movzx eax, al
    add r8, rax
    mov rax, rdi
    shr al, 2
    movzx eax, al
    add r8, rax
    cmp sil, cl
    jnbe 23
    jmp 24
23:
    add r8, 1
24:
    cmp cl, sil
    jc 25
    jmp 26
25:
    add r8, 2
26:
    cmp sil, cl
    jnc 27
    jmp 28
27:
    add r8, 4
28:
    cmp sil, cl
    jbe 29
    jmp 30
29:
    add r8, 8
30:
    cmp sil, cl
    jnz 31
    add r8, 0x10
    jmp 32
31:
    add r8, 0x30
32:
    cmp dil, cl
    jc 33
    jmp 34
33:
    add r8, 0x40
34:
    mov rax, r8
    ret
35:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movzx r8d, ax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    sal ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    shr ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    shr ax, 2
    movzx eax, ax
    add r8, rax
    cmp cx, si
    jnbe 36
    jmp 37
36:
    add r8, 1
37:
    mov rdx, r8
    cmp si, cx
    jc 38
    jmp 39
38:
    add rdx, 2
39:
    cmp cx, si
    jnc 40
    jmp 41
40:
    add rdx, 4
41:
    cmp cx, si
    jbe 42
    jmp 43
42:
    add rdx, 8
43:
    cmp cx, si
    jnz 44
    add rdx, 0x10
    jmp 45
44:
    add rdx, 0x30
45:
    cmp di, si
    jc 46
    jmp 47
46:
    add rdx, 0x40
47:
    mov rax, rdx
    ret
48:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    mov r8d, eax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    mov eax, edx
    add r8, rax
    mov rax, rcx
    sal eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rcx
    shr eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rdi
    shr eax, 2
    mov eax, eax
    add r8, rax
    cmp ecx, esi
    jnbe 49
    jmp 50
49:
    add r8, 1
50:
    mov rdx, r8
    cmp esi, ecx
    jc 51
    jmp 52
51:
    add rdx, 2
52:
    cmp ecx, esi
    jnc 53
    jmp 54
53:
    add rdx, 4
54:
    cmp ecx, esi
    jbe 55
    jmp 56
55:
    add rdx, 8
56:
    cmp ecx, esi
    jnz 57
    add rdx, 0x10
    jmp 58
57:
    add rdx, 0x30
58:
    cmp edi, esi
    jc 59
    jmp 60
59:
    add rdx, 0x40
60:
    mov rax, rdx
    ret
61:
    mov rcx, rsi
    mov rsi, rdi
    mov rdi, rdx
    mov rax, rcx
    imul sil
    movsx r8, al
    mov r9, rcx
    mov rax, rsi
    cbw
    idiv r9b
    movsx rax, al
    mov r9, r8
    add r9, rax
    mov r8, rcx
    mov rax, rsi
    cbw
    idiv r8b
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov r8, rcx
    mov rax, rdi
    cbw
    idiv r8b
    movsx rax, al
    add r9, rax
    mov r8, rcx
    mov rax, rdi
    cbw
    idiv r8b
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov rax, rcx
    mov rdx, rsi
    and dl, al
    movsx rax, dl
    mov r8, r9
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    or dl, al
    movsx rax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    xor dl, al
    movsx rax, dl
    add r8, rax
    mov rdx, rcx
    not dl
    and dl, sil
    movsx rax, dl
    add r8, rax
    mov rax, rsi
    shl al, 2
    movsx rax, al
    add r8, rax
    mov rax, rsi
    sar al, 2
    movsx rax, al
    add r8, rax
    mov rax, rdi
    sar al, 2
    movsx rax, al
    add r8, rax
    cmp sil, cl
    jnle 62
    jmp 63
62:
    add r8, 1
63:
    cmp cl, sil
    jnge 64
    jmp 65
64:
    add r8, 2
65:
    cmp sil, cl
    jnl 66
    jmp 67
66:
    add r8, 4
67:
    cmp sil, cl
    jng 68
    jmp 69
68:
    add r8, 8
69:
    cmp sil, cl
    jnz 70
    add r8, 0x10
    jmp 71
70:
    add r8, 0x30
71:
    cmp dil, cl
    jnge 72
    jmp 73
72:
    add r8, 0x40
73:
    mov rax, r8
    ret
74:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movsx r8, ax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    sal ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    sar ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    sar ax, 2
    movsx rax, ax
    add r8, rax
    cmp cx, si
    jnle 75
    jmp 76
75:
    add r8, 1
76:
    mov rdx, r8
    cmp si, cx
    jnge 77
    jmp 78
77:
    add rdx, 2
78:
    cmp cx, si
    jnl 79
    jmp 80
79:
    add rdx, 4
80:
    cmp cx, si
    jng 81
    jmp 82
81:
    add rdx, 8
82:
    cmp cx, si
    jnz 83
    add rdx, 0x10
    jmp 84
83:
    add rdx, 0x30
84:
    cmp di, si
    jnge 85
    jmp 86
85:
    add rdx, 0x40
86:
    mov rax, rdx
    ret
87:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    movsxd r8, eax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    sal eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    cmp ecx, esi
    jnle 88
    jmp 89
88:
    add r8, 1
89:
    mov rdx, r8
    cmp esi, ecx
    jnge 90
    jmp 91
90:
    add rdx, 2
91:
    cmp ecx, esi
    jnl 92
    jmp 93
92:
    add rdx, 4
93:
    cmp ecx, esi
    jng 94
    jmp 95
94:
    add rdx, 8
95:
    cmp ecx, esi
    jnz 96
    add rdx, 0x10
    jmp 97
96:
    add rdx, 0x30
97:
    cmp edi, esi
    jnge 98
    jmp 99
98:
    add rdx, 0x40
99:
    mov rax, rdx
    ret
=========== OPT LEVEL: all ===========
batch:
    push rbx
batch int:
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0x14
    mov rsi, 6
    mov rdx, -7
    call 9
    mov rbx, rax
    mov dl, 7
    mov dil, 0x14
    mov sil, 6
batch i8:
    call 22
    mov rbp, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 35
    mov r12, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 48
    mov r13, rax
    mov dl, -7
    mov dil, 0x14
    mov sil, 6
    call 61
    mov r14, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 74
    mov r15, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 87
    mov rdx, rbx
    add rdx, rbp
    add rdx, r12
    add rdx, r13
    add rdx, r14
    add rdx, r15
    add rdx, rax
    mov rax, rdx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
9:
    mov rcx, rdi
    mov rdi, rdx
    mov r8, rcx
    imul r8, rsi
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rax
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rax
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rcx
    and rax, rsi
    add r8, rax
    mov rax, rcx
    or rax, rsi
    add r8, rax
    mov rax, rcx
    xor rax, rsi
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not rdx
    and rdx, rax
    add r8, rdx
    mov rax, rcx
    sal rax, 2
    add r8, rax
    mov rax, rcx
    sar rax, 2
    add r8, rax
    mov rax, rdi
    sar rax, 2
    add r8, rax
    cmp rcx, rsi
    jnle 10
    jmp 11
10:
    add r8, 1
11:
    cmp rsi, rcx
    jnge 12
    jmp 13
12:
    add r8, 2
13:
    mov rdx, r8
    cmp rcx, rsi
    jnl 14
    jmp 15
14:
    add rdx, 4
15:
    cmp rcx, rsi
    jng 16
    jmp 17
16:
    add rdx, 8
17:
    cmp rcx, rsi
    jnz 18
    add rdx, 0x10
    jmp 19
18:
    add rdx, 0x30
19:
    cmp rdi, rsi
    jnge 20
    jmp 21
20:
    add rdx, 0x40
21:
    mov rax, rdx
    ret
22:
    mov rcx, rsi
    mov rsi, rdi
    mov rdi, rdx
    mov rax, rcx
    imul sil
    movzx r8d, al
    mov r9, rcx
    mov rax, rsi
    movzx eax, al
    div r9b
    movzx eax, al
    mov r9, r8
    add r9, rax
    mov r8, rcx
    mov rax, rsi
    movzx eax, al
    div r8b
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov r8, rcx
    mov rax, rdi
    movzx eax, al
    div r8b
    movzx eax, al
    add r9, rax
    mov r8, rcx
    mov rax, rdi
    movzx eax, al
    div r8b
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov rax, rcx
    mov rdx, rsi
    and dl, al
    movzx eax, dl
    mov r8, r9
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    or dl, al
    movzx eax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    xor dl, al
    movzx eax, dl
    add r8, rax
    mov rdx, rcx
    not dl
    and dl, sil
    movzx eax, dl
    add r8, rax
    mov rax, rsi
    shl al, 2
    movzx eax, al
    add r8, rax
    mov rax, rsi
    shr al, 2
    movzx eax, al
    add r8, rax
    mov rax, rdi
    shr al, 2
    movzx eax, al
    add r8, rax
    cmp sil, cl
    jnbe 23
    jmp 24
23:
    add r8, 1
24:
    cmp cl, sil
    jc 25
    jmp 26
25:
    add r8, 2
26:
    cmp sil, cl
    jnc 27
    jmp 28
27:
    add r8, 4
28:
    cmp sil, cl
    jbe 29
    jmp 30
29:
    add r8, 8
30:
    cmp sil, cl
    jnz 31
    add r8, 0x10
    jmp 32
31:
    add r8, 0x30
32:
    cmp dil, cl
    jc 33
    jmp 34
33:
    add r8, 0x40
34:
    mov rax, r8
    ret
35:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movzx r8d, ax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    sal ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    shr ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    shr ax, 2
    movzx eax, ax
    add r8, rax
    cmp cx, si
    jnbe 36
    jmp 37
36:
    add r8, 1
37:
    mov rdx, r8
    cmp si, cx
    jc 38
    jmp 39
38:
    add rdx, 2
39:
    cmp cx, si
    jnc 40
    jmp 41
40:
    add rdx, 4
41:
    cmp cx, si
    jbe 42
    jmp 43
42:
    add rdx, 8
43:
    cmp cx, si
    jnz 44
    add rdx, 0x10
    jmp 45
44:
    add rdx, 0x30
45:
    cmp di, si
    jc 46
    jmp 47
46:
    add rdx, 0x40
47:
    mov rax, rdx
    ret
48:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    mov r8d, eax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    mov eax, edx
    add r8, rax
    mov rax, rcx
    sal eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rcx
    shr eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rdi
    shr eax, 2
    mov eax, eax
    add r8, rax
    cmp ecx, esi
    jnbe 49
    jmp 50
49:
    add r8, 1
50:
    mov rdx, r8
    cmp esi, ecx
    jc 51
    jmp 52
51:
    add rdx, 2
52:
    cmp ecx, esi
    jnc 53
    jmp 54
53:
    add rdx, 4
54:
    cmp ecx, esi
    jbe 55
    jmp 56
55:
    add rdx, 8
56:
    cmp ecx, esi
    jnz 57
    add rdx, 0x10
    jmp 58
57:
    add rdx, 0x30
58:
    cmp edi, esi
    jc 59
    jmp 60
59:
    add rdx, 0x40
60:
    mov rax, rdx
    ret
61:
    mov rcx, rsi
    mov rsi, rdi
    mov rdi, rdx
    mov rax, rcx
    imul sil
    movsx r8, al
    mov r9, rcx
    mov rax, rsi
    cbw
    idiv r9b
    movsx rax, al
    mov r9, r8
    add r9, rax
    mov r8, rcx
    mov rax, rsi
    cbw
    idiv r8b
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov r8, rcx
    mov rax, rdi
    cbw
    idiv r8b
    movsx rax, al
    add r9, rax
    mov r8, rcx
    mov rax, rdi
    cbw
    idiv r8b
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov rax, rcx
    mov rdx, rsi
    and dl, al
    movsx rax, dl
    mov r8, r9
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    or dl, al
    movsx rax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    xor dl, al
    movsx rax, dl
    add r8, rax
    mov rdx, rcx
    not dl
    and dl, sil
    movsx rax, dl
    add r8, rax
    mov rax, rsi
    shl al, 2
    movsx rax, al
    add r8, rax
    mov rax, rsi
    sar al, 2
    movsx rax, al
    add r8, rax
    mov rax, rdi
    sar al, 2
    movsx rax, al
    add r8, rax
    cmp sil, cl
    jnle 62
    jmp 63
62:
    add r8, 1
63:
    cmp cl, sil
    jnge 64
    jmp 65
64:
    add r8, 2
65:
    cmp sil, cl
    jnl 66
    jmp 67
66:
    add r8, 4
67:
    cmp sil, cl
    jng 68
    jmp 69
68:
    add r8, 8
69:
    cmp sil, cl
    jnz 70
    add r8, 0x10
    jmp 71
70:
    add r8, 0x30
71:
    cmp dil, cl
    jnge 72
    jmp 73
72:
    add r8, 0x40
73:
    mov rax, r8
    ret
74:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movsx r8, ax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    sal ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    sar ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    sar ax, 2
    movsx rax, ax
    add r8, rax
    cmp cx, si
    jnle 75
    jmp 76
75:
    add r8, 1
76:
    mov rdx, r8
    cmp si, cx
    jnge 77
    jmp 78
77:
    add rdx, 2
78:
    cmp cx, si
    jnl 79
    jmp 80
79:
    add rdx, 4
80:
    cmp cx, si
    jng 81
    jmp 82
81:
    add rdx, 8
82:
    cmp cx, si
    jnz 83
    add rdx, 0x10
    jmp 84
83:
    add rdx, 0x30
84:
    cmp di, si
    jnge 85
    jmp 86
85:
    add rdx, 0x40
86:
    mov rax, rdx
    ret
87:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    movsxd r8, eax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    sal eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    cmp ecx, esi
    jnle 88
    jmp 89
88:
    add r8, 1
89:
    mov rdx, r8
    cmp esi, ecx
    jnge 90
    jmp 91
90:
    add rdx, 2
91:
    cmp ecx, esi
    jnl 92
    jmp 93
92:
    add rdx, 4
93:
    cmp ecx, esi
    jng 94
    jmp 95
94:
    add rdx, 8
95:
    cmp ecx, esi
    jnz 96
    add rdx, 0x10
    jmp 97
96:
    add rdx, 0x30
97:
    cmp edi, esi
    jnge 98
    jmp 99
98:
    add rdx, 0x40
99:
    mov rax, rdx
    ret
=========== OPT LEVEL: aggresive ===========
batch:
    mov rax, 0x9dc
batch i16:
    ret
