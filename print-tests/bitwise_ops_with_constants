=========== OPT LEVEL: none ===========
main:
    push rbx
    mov rdi, 0xc
    call 2
    mov rbx, rax
    xor edi, edi
    call 2
    mov rcx, rbx
    and rcx, 0xa
    add rax, rcx
    mov rcx, rbx
    or rcx, 3
    add rax, rcx
    mov rcx, rbx
    xor rcx, 6
    add rax, rcx
    pop rbx
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: mininal ===========
main:
    push rbx
    mov rdi, 0xc
    call 2
    mov rbx, rax
    xor edi, edi
    call 2
    mov rcx, rbx
    and rcx, 0xa
    add rax, rcx
    mov rcx, rbx
    or rcx, 3
    add rax, rcx
    mov rcx, rbx
    xor rcx, 6
    add rax, rcx
    pop rbx
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: moderate ===========
main:
    push rbx
    mov rdi, 0xc
    call 2
    mov rbx, rax
    xor edi, edi
    call 2
    mov rcx, rbx
    and rcx, 0xa
    add rax, rcx
    mov rcx, rbx
    or rcx, 3
    add rax, rcx
    mov rcx, rbx
    xor rcx, 6
    add rax, rcx
    pop rbx
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: all ===========
main:
    push rbx
    mov rdi, 0xc
    call 2
    mov rbx, rax
    xor edi, edi
    call 2
    mov rcx, rbx
    and rcx, 0xa
    add rax, rcx
    mov rcx, rbx
    or rcx, 3
    add rax, rcx
    mov rcx, rbx
    xor rcx, 6
    add rax, rcx
    pop rbx
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 0x21
    ret
