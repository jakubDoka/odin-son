=========== OPT LEVEL: None ===========
main:
     push rbx
     sub rsp, 0x08
     lea rax, [rsp]
     mov rbx, rax
     mov rcx, 0x00
     mov rdx, rax
     mov [rdx], rcx
     mov rdi, rax
     call :set
     mov rax, [rbx]
     sub rsp, 0xFFFFFFFFFFFFFFF8
     pop rbx
     ret
set:
     mov rax, 0x01
     mov [rdi], rax
     mov rax, [rdi]
     ret
=========== OPT LEVEL: Full ===========
main:
     push rbx
     sub rsp, 0x08
     lea rax, [rsp]
     mov rbx, rax
     mov rcx, 0x00
     mov rdx, rax
     mov [rdx], rcx
     mov rdi, rax
     call :set
     mov rax, [rbx]
     sub rsp, 0xFFFFFFFFFFFFFFF8
     pop rbx
     ret
set:
     mov rax, 0x01
     mov [rdi], rax
     mov rax, [rdi]
     ret
