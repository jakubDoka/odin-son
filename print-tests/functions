=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call fib
    add rsp, 8
    ret
fib:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jle 2
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call fib
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call fib
    add rbp, rax
    jmp 3
2:
    mov rbp, 1
3:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call fib
    add rsp, 8
    ret
fib:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jle 2
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call fib
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call fib
    add rbp, rax
    jmp 3
2:
    mov rbp, 1
3:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call fib
    add rsp, 8
    ret
fib:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jle 2
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call fib
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call fib
    add rbp, rax
    jmp 3
2:
    mov rbp, 1
3:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call fib
    add rsp, 8
    ret
fib:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jle 2
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call fib
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call fib
    add rbp, rax
    jmp 3
2:
    mov rbp, 1
3:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
