=========== OPT LEVEL: none ===========
with_args:
    add rsi, rdi
    mov.q [r8], rsi
    add rdx, rcx
    mov.q [r9], rdx
    add rdi, rcx
    mov rax, rdi
    ret
main:
    sub rsp, 0x18
    mov rdi, 1
    mov rsi, 2
    mov rdx, 3
    mov rcx, 4
    lea r8, [rsp]
    lea r9, [rsp + 0x8]
    call with_args
    mov rdx, rax
    mov.q rax, [rsp]
    imul rax, rax, 0x64
    mov.q rcx, [rsp + 0x8]
    imul rcx, rcx, 0xa
    add rax, rcx
    add rax, rdx
    add rsp, 0x18
    ret
=========== OPT LEVEL: mininal ===========
with_args:
    add rsi, rdi
    mov.q [r8], rsi
    add rdx, rcx
    mov.q [r9], rdx
    add rdi, rcx
    mov rax, rdi
    ret
main:
    sub rsp, 0x18
    mov rdi, 1
    mov rsi, 2
    mov rdx, 3
    mov rcx, 4
    lea r8, [rsp]
    lea r9, [rsp + 0x8]
    call with_args
    mov rdx, rax
    mov.q rax, [rsp]
    imul rax, rax, 0x64
    mov.q rcx, [rsp + 0x8]
    imul rcx, rcx, 0xa
    add rax, rcx
    add rax, rdx
    add rsp, 0x18
    ret
=========== OPT LEVEL: moderate ===========
with_args:
    add rsi, rdi
    mov.q [r8], rsi
    add rdx, rcx
    mov.q [r9], rdx
    add rdi, rcx
    mov rax, rdi
    ret
main:
    sub rsp, 0x18
    mov rdi, 1
    mov rsi, 2
    mov rdx, 3
    mov rcx, 4
    lea r8, [rsp]
    lea r9, [rsp + 0x8]
    call with_args
    mov rdx, rax
    mov.q rax, [rsp]
    imul rax, rax, 0x64
    mov.q rcx, [rsp + 0x8]
    imul rcx, rcx, 0xa
    add rax, rcx
    add rax, rdx
    add rsp, 0x18
    ret
=========== OPT LEVEL: all ===========
with_args:
    add rsi, rdi
    mov.q [r8], rsi
    add rdx, rcx
    mov.q [r9], rdx
    add rdi, rcx
    mov rax, rdi
    ret
main:
    sub rsp, 0x18
    mov rdi, 1
    mov rsi, 2
    mov rdx, 3
    mov rcx, 4
    lea r8, [rsp]
    lea r9, [rsp + 0x8]
    call with_args
    mov rdx, rax
    mov.q rax, [rsp]
    imul rax, rax, 0x64
    mov.q rcx, [rsp + 0x8]
    imul rcx, rcx, 0xa
    add rax, rcx
    add rax, rdx
    add rsp, 0x18
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 0x177
    ret
