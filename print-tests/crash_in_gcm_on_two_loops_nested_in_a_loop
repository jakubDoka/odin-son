=========== OPT LEVEL: none ===========
main:
    xor eax, eax
    xor edi, edi
6:
    cmp rdi, 4
    jnl 1
    xor ecx, ecx
    xor edx, edx
3:
    cmp rcx, 4
    jnl 2
    add rdx, rcx
    add rcx, 1
    jmp 3
2:
    xor ecx, ecx
    xor esi, esi
5:
    cmp rcx, 4
    jnl 4
    lea rsi, [rsi + rcx*2]
    add rcx, 1
    jmp 5
4:
    add rdx, rsi
    add rax, rdx
    add rdi, 1
    jmp 6
1:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: mininal ===========
main:
    xor eax, eax
    xor edi, edi
6:
    cmp rdi, 4
    jnl 1
    xor ecx, ecx
    xor edx, edx
3:
    cmp rcx, 4
    jnl 2
    add rdx, rcx
    add rcx, 1
    jmp 3
2:
    xor ecx, ecx
    xor esi, esi
5:
    cmp rcx, 4
    jnl 4
    lea rsi, [rsi + rcx*2]
    add rcx, 1
    jmp 5
4:
    add rdx, rsi
    add rax, rdx
    add rdi, 1
    jmp 6
1:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: moderate ===========
main:
    xor eax, eax
    xor edi, edi
6:
    cmp rdi, 4
    jnl 1
    xor ecx, ecx
    xor edx, edx
3:
    cmp rcx, 4
    jnl 2
    add rdx, rcx
    add rcx, 1
    jmp 3
2:
    xor ecx, ecx
    xor esi, esi
5:
    cmp rcx, 4
    jnl 4
    lea rsi, [rsi + rcx*2]
    add rcx, 1
    jmp 5
4:
    add rdx, rsi
    add rax, rdx
    add rdi, 1
    jmp 6
1:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: all ===========
main:
    xor eax, eax
    xor edi, edi
6:
    cmp rdi, 4
    jnl 1
    xor ecx, ecx
    xor edx, edx
3:
    cmp rcx, 4
    jnl 2
    add rdx, rcx
    add rcx, 1
    jmp 3
2:
    xor ecx, ecx
    xor esi, esi
5:
    cmp rcx, 4
    jnl 4
    lea rsi, [rsi + rcx*2]
    add rcx, 1
    jmp 5
4:
    add rdx, rsi
    add rax, rdx
    add rdi, 1
    jmp 6
1:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: aggresive ===========
