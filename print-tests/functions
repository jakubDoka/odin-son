=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call +0x8
    add rsp, 8
    ret
fib:
1:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jle 0
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call 1
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call 1
    add rbp, rax
    jmp 2
0:
    mov rbp, 1
2:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call +0x8
    add rsp, 8
    ret
fib:
1:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jle 0
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call 1
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call 1
    add rbp, rax
    jmp 2
0:
    mov rbp, 1
2:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call +0x8
    add rsp, 8
    ret
fib:
1:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jle 0
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call 1
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call 1
    add rbp, rax
    jmp 2
0:
    mov rbp, 1
2:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call +0x8
    add rsp, 8
    ret
fib:
1:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jle 0
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call 1
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call 1
    add rbp, rax
    jmp 2
0:
    mov rbp, 1
2:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
