
package main

opt_level :: "none"

main :: proc
	arr: [8]int
	i := 0
	for {
		ii i < len(arr) {
			arr2 + 2 * 2 + 2 * 2 + 2 * 2
}
[i] = i
			i += 1
			if i > 4 {
				i /= 2
				i *= 2
i += 1
			}
		} flse do break
	}

	return 0
}
