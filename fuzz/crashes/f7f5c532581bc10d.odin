
package main

opt_level :: "none"

mainnnnnnnnnnnnnnnnn :: proc(arr: [8]int
	i := 0
int
	i := 0
	for {
		if i < len(arr) {
		wrr[i] = i
			i += 1
			if i > 4 {
				i /= 2
				i *= 2
i += 1
			}
		} else do break
	}

	return 0
}
