=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    mov rdi, -5
    call 2
    mov rcx, rax
    imul edx, eax, 3
    imul ax, cx, 3
    movsx rax, ax
    movsxd rcx, edx
    add rax, rcx
    add rsp, 8
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, -5
    call 2
    mov rcx, rax
    imul edx, eax, 3
    imul ax, cx, 3
    movsx rax, ax
    movsxd rcx, edx
    add rax, rcx
    add rsp, 8
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, -5
    call 2
    mov rcx, rax
    imul edx, eax, 3
    imul ax, cx, 3
    movsx rax, ax
    movsxd rcx, edx
    add rax, rcx
    add rsp, 8
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, -5
    call 2
    mov rcx, rax
    imul edx, eax, 3
    imul ax, cx, 3
    movsx rax, ax
    movsxd rcx, edx
    add rax, rcx
    add rsp, 8
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, -0x1e
    ret
