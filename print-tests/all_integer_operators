main:
     push rbx
     push rbp
     mov rax, 0x14
     mov rbp, rax
     mov rbx, 0x06
     mov rdi, 0x00
     mov rax, 0x07
     mov rcx, rdi
     sub rcx, rax
     mov rsi, rcx
     mov rax, rbp
     cqo
     idiv rbx
     mov rcx, rax
     add rcx, rdi
     mov rax, rbp
     cqo
     idiv rbx
     add rcx, rdx
     mov rax, rsi
     cqo
     idiv rbx
     add rcx, rax
     mov rax, rsi
     cqo
     idiv rbx
     add rcx, rdx
     mov rax, rbp
     and rax, rbx
     add rcx, rax
     mov rax, rbp
     or rax, rbx
     add rcx, rax
     mov rax, rbp
     xor rax, rbx
     mov rdx, rcx
     add rdx, rax
     mov rax, rbp
     mov rcx, rbx
     not rcx
     and rcx, rax
     add rdx, rcx
     mov rcx, 0x02
     mov rax, rbp
     shl rax, cl
     add rdx, rax
     mov rax, rbp
     sar rax, cl
     add rdx, rax
     mov rax, rsi
     sar rax, cl
     add rdx, rax
     mov rax, rbp
     cmp rax, rbx
     setnle al
     movzx rax, al
     test rax, rax
     jz :0
     jmp :1

000: jmp :2

001: mov rax, 0x01
     add rax, rdx
     mov rdx, rax
     jmp :2

002: mov rdi, rdx
     mov rax, rbp
     mov rdx, rbx
     cmp rdx, rax
     setl dl
     movzx rdx, dl
     test rdx, rdx
     jz :3
     jmp :4

003: jmp :5

004: add rdi, rcx
     jmp :5

005: mov rax, rbp
     cmp rax, rbx
     setnl al
     movzx rax, al
     test rax, rax
     jz :6
     jmp :7

006: jmp :8

007: mov rax, 0x04
     mov rcx, rdi
     mov rdi, rax
     add rdi, rcx
     jmp :8

008: mov rax, rbp
     cmp rax, rbx
     setle al
     movzx rax, al
     test rax, rax
     jz :9
     jmp :10

009: jmp :11

010: mov rax, 0x08
     mov rcx, rdi
     mov rdi, rax
     add rdi, rcx
     jmp :11

011: mov rdx, rdi
     mov rcx, rbp
     mov rax, rbp
     cmp rcx, rax
     setz cl
     movzx rcx, cl
     test rcx, rcx
     jz :12
     jmp :13

012: jmp :14

013: mov rax, 0x10
     mov rcx, rdx
     mov rdx, rax
     add rdx, rcx
     jmp :14

014: mov rax, rbp
     cmp rax, rbx
     setnz al
     movzx rax, al
     test rax, rax
     jz :15
     jmp :16

015: jmp :17

016: mov rax, 0x20
     mov rcx, rdx
     mov rdx, rax
     add rdx, rcx
     jmp :17

017: mov rax, rsi
     cmp rax, rbx
     setl al
     movzx rax, al
     test rax, rax
     jz :18
     jmp :19

018: jmp :20

019: mov rax, 0x40
     mov rcx, rdx
     mov rdx, rax
     add rdx, rcx
     jmp :20

020: mov rax, rdx
     pop rbp
     pop rbx
     ret
