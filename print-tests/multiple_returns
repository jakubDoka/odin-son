=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    mov rdi, 1
    mov rsi, 2
    call a
    add rsp, 8
    ret
a:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rsi
    cmp rdi, 0
    jz 2
    cmp rbx, 2
    jz 3
    cmp rdi, 1
    jz 4
    call b
    mov rbp, rax
    mov rdi, rbx
    call c
    add rbp, rax
    jmp 5
4:
    call b
    mov rbp, rax
    imul rdi, rbx, 2
    call c
    add rbp, rax
    jmp 5
3:
    imul rbp, rbx, 6
    jmp 5
2:
    xor ebp, ebp
5:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
b:
    imul rax, rdi, 2
    ret
c:
    imul rax, rdi, 3
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, 1
    mov rsi, 2
    call a
    add rsp, 8
    ret
a:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rsi
    cmp rdi, 0
    jz 2
    cmp rbx, 2
    jz 3
    cmp rdi, 1
    jz 4
    call b
    mov rbp, rax
    mov rdi, rbx
    call c
    add rbp, rax
    jmp 5
4:
    call b
    mov rbp, rax
    imul rdi, rbx, 2
    call c
    add rbp, rax
    jmp 5
3:
    imul rbp, rbx, 6
    jmp 5
2:
    xor ebp, ebp
5:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
b:
    imul rax, rdi, 2
    ret
c:
    imul rax, rdi, 3
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, 1
    mov rsi, 2
    call a
    add rsp, 8
    ret
a:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rsi
    cmp rdi, 0
    jz 2
    cmp rbx, 2
    jz 3
    cmp rdi, 1
    jz 4
    call b
    mov rbp, rax
    mov rdi, rbx
    call c
    add rbp, rax
    jmp 5
4:
    call b
    mov rbp, rax
    imul rdi, rbx, 2
    call c
    add rbp, rax
    jmp 5
3:
    imul rbp, rbx, 6
    jmp 5
2:
    xor ebp, ebp
5:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
b:
    imul rax, rdi, 2
    ret
c:
    imul rax, rdi, 3
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, 1
    mov rsi, 2
    call a
    add rsp, 8
    ret
a:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rsi
    cmp rdi, 0
    jz 2
    cmp rbx, 2
    jz 3
    cmp rdi, 1
    jz 4
    call b
    mov rbp, rax
    mov rdi, rbx
    call c
    add rbp, rax
    jmp 5
4:
    call b
    mov rbp, rax
    imul rdi, rbx, 2
    call c
    add rbp, rax
    jmp 5
3:
    imul rbp, rbx, 6
    jmp 5
2:
    xor ebp, ebp
5:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
b:
    imul rax, rdi, 2
    ret
c:
    imul rax, rdi, 3
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 0xc
    ret
