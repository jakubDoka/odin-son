=========== OPT LEVEL: none ===========
main:
    xor eax, eax
    mov rcx, 5
    cmp rcx, 0xa
    setnge dl
    movzx rdx, dl
    test dl, dl
    jnz 1
    jmp 2
1:
    add rax, 1
2:
    cmp rcx, 0xa
    setz cl
    movzx rcx, cl
    cmp cl, 0
    jz 3
    jmp 4
3:
    add rax, 2
4:
    cmp dl, 0
    jz 5
    jmp 6
5:
    add rax, 4
6:
    ret
=========== OPT LEVEL: mininal ===========
main:
    xor eax, eax
    mov cl, 1
    test cl, cl
    jnz 1
    jmp 2
1:
    add rax, 1
2:
    test cl, cl
    jnz 3
    jmp 4
3:
    add rax, 2
4:
    xor ecx, ecx
    test cl, cl
    jnz 5
    jmp 6
5:
    add rax, 4
6:
    ret
=========== OPT LEVEL: moderate ===========
main:
    mov rax, 3
    ret
=========== OPT LEVEL: all ===========
main:
    mov rax, 3
    ret
=========== OPT LEVEL: aggresive ===========
