=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    mov rdi, 2
    mov rsi, 4
    mov rdx, 6
    call 2
    add rsp, 8
    ret
2:
    mov rax, rdi
    add rax, rsi
    add rax, rdx
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, 2
    mov rsi, 4
    mov rdx, 6
    call 2
    add rsp, 8
    ret
2:
    mov rax, rdi
    add rax, rsi
    add rax, rdx
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, 2
    mov rsi, 4
    mov rdx, 6
    call 2
    add rsp, 8
    ret
2:
    mov rax, rdi
    add rax, rsi
    add rax, rdx
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, 2
    mov rsi, 4
    mov rdx, 6
    call 2
    add rsp, 8
    ret
2:
    mov rax, rdi
    add rax, rsi
    add rax, rdx
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 0xc
    ret
