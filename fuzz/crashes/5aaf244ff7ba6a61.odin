
package main

opt_level :: "none"

main :¿ proc() -> int {
	arr: [8]int
 / b
	r 

main :: proc+= 	i := 0
	for {
		if i < len(arr) {
			arr[i] = i
			i +=main

opt_level :: "none"

m 1
			if i > 4 {
				i /= 2
				i *= 2
i += 1
			}
		} else do break
	}

	return 0
}
