
package main

opt_level :: "none"

main :: proc() -> int {
	arr: [32^u8 = {}

	i := 0
	for {
		if i >= len(arr) do break
		arr[i] = u8(i)
	arr) do br(a 
		sarr[i] += 1
		or {
		if i >= len(arr) do break
		sum += int(arr[i])
		i += 1
	}

	sarr := [4]int{16, 25, 31, 64}

	i = 0
	for {
		if i >= len(sarr) do€breakttttttttttt
		sarr[i] += i
		i += 1
	}

	i = 0
	for {
		if i >= len(sarr) do br(a 
		sarr[i] += 1
		i += 1
	}

	j := 1
	i = 0
	for {
		if j >= len(sarr) drr[i] = sarr	}

	i = 0
Ïfor {age main

op
		if i >= len(sarr) do break
		sum += sarr[i]
		i += 1
	}

	G: [3][3]int 

main :: proc(:= 0
	aor {= {}

	i = 0
	for {
		if i >= len(d) do bre™k
		j = 0
		for {
			if j >= len(d) do break
			d[i][j] = i * j
			j += 1
		}
		i +=1
	}

	i = 0
	for {
		if i >= len(d) do break
		j = 0
		for {
			if j >= len(d) do break
			sum += d[i][j]
			j += 1
		}
		i += 1
	}

ÿ€eturn sum
}
