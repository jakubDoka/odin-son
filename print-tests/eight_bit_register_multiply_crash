=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    mov rdi, 0x14
    call +0x28
    mov rdx, rax
    xor ecx, ecx
    sub cl, 5
    mov al, 2
    imul cl
    movsx rcx, al
    mov al, 3
    imul dl
    movzx eax, al
    add rcx, rax
    mov rax, rcx
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, 0x14
    call +0x18
    mov rcx, rax
    mov al, 3
    imul cl
    movzx eax, al
    add rax, -0xa
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, 0x14
    call +0x18
    mov rcx, rax
    mov al, 3
    imul cl
    movzx eax, al
    add rax, -0xa
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, 0x14
    call +0x18
    mov rcx, rax
    mov al, 3
    imul cl
    movzx eax, al
    add rax, -0xa
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 0x32
    ret
opaque:
