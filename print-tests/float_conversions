=========== OPT LEVEL: none-quick ===========
opaque:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rsp, 8
    ret
2:
    push rbx
    push rbp
    sub rsp, 0x48
    mov rdi, -0x64
    call opaque
    mov.q [rsp], rax
    mov rdi, -0xbb8
    call opaque
    mov.q [rsp + 0x8], rax
    mov rdi, -0x11170
    call opaque
    mov.q [rsp + 0x10], rax
    mov rdi, -0x4c4b40
    call opaque
    mov.q [rsp + 0x18], rax
    mov rdi, 0xc8
    call opaque
    mov.q [rsp + 0x20], rax
    mov rdi, 0xea60
    call opaque
    mov.q [rsp + 0x28], rax
    mov rdi, 0x3d0900
    call opaque
    mov.q [rsp + 0x30], rax
    mov rdi, 0x6acfc0
    call opaque
    mov.q [rsp + 0x38], rax
    xor eax, eax
    mov.q rcx, [rsp]
    movsx rcx, cl
    movsd.q xmm0, [rip + 0x1af]
    movss.d xmm1, [rip + 0x1b0]
    cvtss2sd xmm1, xmm1
    mov.q rdx, [rsp + 0x8]
    movsx rdx, dx
    mov.q rbx, [rsp + 0x10]
    movsxd rbx, ebx
    mov.q rbp, [rsp + 0x20]
    movzx ebp, bpl
    mov.q rsi, [rsp + 0x28]
    movzx esi, si
    mov.q rdi, [rsp + 0x30]
    mov edi, edi
    addsd.q xmm1, [rip + 0x17d]
    cvtsd2ss xmm1, xmm1
    cvttss2si r8, xmm1
    add rax, r8
    movss.d xmm1, [rip + 0x171]
    cvtsi2ss xmm2, rcx
    cvttss2si r8, xmm2
    add rax, r8
    cvtsi2sd xmm2, rcx
    cvttsd2si rcx, xmm2
    add rax, rcx
    cvtsi2ss xmm2, rdx
    cvttss2si rcx, xmm2
    add rax, rcx
    cvtsi2sd xmm2, rdx
    cvttsd2si rcx, xmm2
    add rax, rcx
    cvtsi2ss xmm2, rbx
    cvttss2si rcx, xmm2
    add rax, rcx
    cvtsi2sd xmm2, rbx
    cvttsd2si rcx, xmm2
    add rax, rcx
    movsd.q xmm2, [rip + 0x120]
    mov.q rcx, [rsp + 0x18]
    cvtsi2ss xmm3, rcx
    cvttss2si rcx, xmm3
    add rax, rcx
    mov.q rcx, [rsp + 0x18]
    cvtsi2sd xmm3, rcx
    cvttsd2si rcx, xmm3
    add rax, rcx
    cvtsi2ss xmm3, rbp
    cvttss2si rcx, xmm3
    add rax, rcx
    cvtsi2sd xmm3, rbp
    cvttsd2si rcx, xmm3
    add rax, rcx
    cvtsi2ss xmm3, rsi
    cvttss2si rcx, xmm3
    add rax, rcx
    cvtsi2sd xmm3, rsi
    cvttsd2si rcx, xmm3
    add rax, rcx
    cvtsi2ss xmm3, rdi
    cvttss2si rcx, xmm3
    add rax, rcx
    cvtsi2sd xmm3, rdi
    cvttsd2si rcx, xmm3
    add rax, rcx
    mov.q rcx, [rsp + 0x38]
    cvtsi2ss xmm3, rcx
    cvttss2si rcx, xmm3
    add rax, rcx
    mov.q rcx, [rsp + 0x38]
    cvtsi2sd xmm3, rcx
    cvttsd2si rcx, xmm3
    add rax, rcx
    cvttss2si ecx, xmm1
    movsxd rcx, ecx
    add rax, rcx
    cvttss2si rcx, xmm1
    add rax, rcx
    cvttsd2si ecx, xmm2
    movsxd rcx, ecx
    add rax, rcx
    cvttsd2si rcx, xmm2
    add rax, rcx
    cvttsd2si ecx, xmm0
    movzx ecx, cl
    add rax, rcx
    movss.d xmm1, [rip + 0x59]
    cvttss2si ecx, xmm1
    movzx ecx, cx
    add rax, rcx
    cvttsd2si ecx, xmm0
    mov ecx, ecx
    add rax, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rsp, 0x48
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: none ===========
opaque:
    mov rax, rdi
    ret
2:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, -0x64
    call opaque
    mov.q [rsp], rax
    mov rdi, -0xbb8
    call opaque
    mov r15, rax
    mov rdi, -0x11170
    call opaque
    mov r14, rax
    mov rdi, -0x4c4b40
    call opaque
    mov r13, rax
    mov rdi, 0xc8
    call opaque
    mov r12, rax
    mov rdi, 0xea60
    call opaque
    mov rbp, rax
    mov rdi, 0x3d0900
    call opaque
    mov rbx, rax
    mov rdi, 0x6acfc0
    call opaque
    mov r11, rax
    xor eax, eax
    mov.q rcx, [rsp]
    movsx r9, cl
    movsd.q xmm0, [rip + 0x195]
    movss.d xmm1, [rip + 0x196]
    cvtss2sd xmm1, xmm1
    mov rcx, r15
    movsx r8, cx
    mov rcx, r14
    movsxd rdi, ecx
    mov rcx, r12
    movzx esi, cl
    mov rcx, rbp
    movzx edx, cx
    mov rcx, rbx
    mov ecx, ecx
    addsd.q xmm1, [rip + 0x16d]
    cvtsd2ss xmm1, xmm1
    cvttss2si r10, xmm1
    add rax, r10
    movss.d xmm1, [rip + 0x161]
    cvtsi2ss xmm2, r9
    cvttss2si r10, xmm2
    add rax, r10
    cvtsi2sd xmm2, r9
    cvttsd2si r9, xmm2
    add rax, r9
    cvtsi2ss xmm2, r8
    cvttss2si r9, xmm2
    add rax, r9
    cvtsi2sd xmm2, r8
    cvttsd2si r8, xmm2
    add rax, r8
    cvtsi2ss xmm2, rdi
    cvttss2si r8, xmm2
    add rax, r8
    cvtsi2sd xmm2, rdi
    cvttsd2si rdi, xmm2
    add rax, rdi
    movsd.q xmm3, [rip + 0x110]
    cvtsi2ss xmm2, r13
    cvttss2si rdi, xmm2
    add rax, rdi
    cvtsi2sd xmm2, r13
    cvttsd2si rdi, xmm2
    add rax, rdi
    cvtsi2ss xmm2, rsi
    cvttss2si rdi, xmm2
    add rax, rdi
    cvtsi2sd xmm2, rsi
    cvttsd2si rsi, xmm2
    add rax, rsi
    cvtsi2ss xmm2, rdx
    cvttss2si rsi, xmm2
    add rax, rsi
    cvtsi2sd xmm2, rdx
    cvttsd2si rdx, xmm2
    add rax, rdx
    cvtsi2ss xmm2, rcx
    cvttss2si rdx, xmm2
    add rax, rdx
    cvtsi2sd xmm2, rcx
    cvttsd2si rcx, xmm2
    add rax, rcx
    cvtsi2ss xmm2, r11
    cvttss2si rcx, xmm2
    add rax, rcx
    cvtsi2sd xmm2, r11
    cvttsd2si rcx, xmm2
    add rax, rcx
    cvttss2si ecx, xmm1
    movsxd rcx, ecx
    add rax, rcx
    cvttss2si rcx, xmm1
    add rax, rcx
    cvttsd2si ecx, xmm3
    movsxd rcx, ecx
    add rax, rcx
    cvttsd2si rcx, xmm3
    add rax, rcx
    cvttsd2si ecx, xmm0
    movzx ecx, cl
    add rax, rcx
    movss.d xmm1, [rip + 0x5d]
    cvttss2si ecx, xmm1
    movzx ecx, cx
    add rax, rcx
    cvttsd2si ecx, xmm0
    mov ecx, ecx
    add rax, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal-quick ===========
opaque:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rsp, 8
    ret
2:
    push rbx
    push rbp
    sub rsp, 0x48
    mov rdi, -0x64
    call opaque
    mov.q [rsp], rax
    mov rdi, -0xbb8
    call opaque
    mov.q [rsp + 0x8], rax
    mov rdi, -0x11170
    call opaque
    mov.q [rsp + 0x10], rax
    mov rdi, -0x4c4b40
    call opaque
    mov.q [rsp + 0x18], rax
    mov rdi, 0xc8
    call opaque
    mov.q [rsp + 0x20], rax
    mov rdi, 0xea60
    call opaque
    mov.q [rsp + 0x28], rax
    mov rdi, 0x3d0900
    call opaque
    mov.q [rsp + 0x30], rax
    mov rdi, 0x6acfc0
    call opaque
    mov.q [rsp + 0x38], rax
    xor eax, eax
    mov.q rcx, [rsp]
    movsx rcx, cl
    cvtsi2ss xmm0, rcx
    cvttss2si rdx, xmm0
    lea rax, [rax + rdx + 0x6]
    mov.q rdx, [rsp + 0x8]
    movsx rdx, dx
    mov.q rbx, [rsp + 0x10]
    movsxd rbx, ebx
    mov.q rbp, [rsp + 0x20]
    movzx ebp, bpl
    mov.q rsi, [rsp + 0x28]
    movzx esi, si
    mov.q rdi, [rsp + 0x30]
    mov edi, edi
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, rdx
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, rdx
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, rbx
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, rbx
    cvttsd2si rcx, xmm0
    add rax, rcx
    mov.q rcx, [rsp + 0x18]
    cvtsi2ss xmm0, rcx
    cvttss2si rcx, xmm0
    add rax, rcx
    mov.q rcx, [rsp + 0x18]
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, rbp
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, rbp
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, rsi
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, rsi
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, rdi
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, rdi
    cvttsd2si rcx, xmm0
    add rax, rcx
    mov.q rcx, [rsp + 0x38]
    cvtsi2ss xmm0, rcx
    cvttss2si rcx, xmm0
    add rax, rcx
    mov.q rcx, [rsp + 0x38]
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rax, 7
    add rax, 7
    add rax, -0xc
    add rax, -0xc
    add rax, 0xfa
    add rax, 0xfde8
    add rax, 0xfa
    add rax, 0xfa
    add rsp, 0x48
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
opaque:
    mov rax, rdi
    ret
2:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, -0x64
    call opaque
    mov.q [rsp], rax
    mov rdi, -0xbb8
    call opaque
    mov r15, rax
    mov rdi, -0x11170
    call opaque
    mov r14, rax
    mov rdi, -0x4c4b40
    call opaque
    mov r13, rax
    mov rdi, 0xc8
    call opaque
    mov r12, rax
    mov rdi, 0xea60
    call opaque
    mov rbp, rax
    mov rdi, 0x3d0900
    call opaque
    mov rbx, rax
    mov rdi, 0x6acfc0
    call opaque
    mov r10, rax
    xor ecx, ecx
    mov.q rax, [rsp]
    movsx r9, al
    cvtsi2ss xmm0, r9
    cvttss2si rax, xmm0
    lea rax, [rcx + rax + 0x6]
    mov rcx, r15
    movsx r8, cx
    mov rcx, r14
    movsxd rdi, ecx
    mov rcx, r12
    movzx esi, cl
    mov rcx, rbp
    movzx edx, cx
    mov rcx, rbx
    mov ecx, ecx
    cvtsi2sd xmm0, r9
    cvttsd2si r9, xmm0
    add rax, r9
    cvtsi2ss xmm0, r8
    cvttss2si r9, xmm0
    add rax, r9
    cvtsi2sd xmm0, r8
    cvttsd2si r8, xmm0
    add rax, r8
    cvtsi2ss xmm0, rdi
    cvttss2si r8, xmm0
    add rax, r8
    cvtsi2sd xmm0, rdi
    cvttsd2si rdi, xmm0
    add rax, rdi
    cvtsi2ss xmm0, r13
    cvttss2si rdi, xmm0
    add rax, rdi
    cvtsi2sd xmm0, r13
    cvttsd2si rdi, xmm0
    add rax, rdi
    cvtsi2ss xmm0, rsi
    cvttss2si rdi, xmm0
    add rax, rdi
    cvtsi2sd xmm0, rsi
    cvttsd2si rsi, xmm0
    add rax, rsi
    cvtsi2ss xmm0, rdx
    cvttss2si rsi, xmm0
    add rax, rsi
    cvtsi2sd xmm0, rdx
    cvttsd2si rdx, xmm0
    add rax, rdx
    cvtsi2ss xmm0, rcx
    cvttss2si rdx, xmm0
    add rax, rdx
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, r10
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, r10
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rax, 7
    add rax, 7
    add rax, -0xc
    add rax, -0xc
    add rax, 0xfa
    add rax, 0xfde8
    add rax, 0xfa
    add rax, 0xfa
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate-quick ===========
opaque:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rsp, 8
    ret
2:
    push rbx
    push rbp
    sub rsp, 0x48
    mov rdi, -0x64
    call opaque
    mov.q [rsp], rax
    mov rdi, -0xbb8
    call opaque
    mov.q [rsp + 0x8], rax
    mov rdi, -0x11170
    call opaque
    mov.q [rsp + 0x10], rax
    mov rdi, -0x4c4b40
    call opaque
    mov.q [rsp + 0x18], rax
    mov rdi, 0xc8
    call opaque
    mov.q [rsp + 0x20], rax
    mov rdi, 0xea60
    call opaque
    mov.q [rsp + 0x28], rax
    mov rdi, 0x3d0900
    call opaque
    mov.q [rsp + 0x30], rax
    mov rdi, 0x6acfc0
    call opaque
    mov.q [rsp + 0x38], rax
    mov.q rax, [rsp]
    movsx rax, al
    cvtsi2ss xmm0, rax
    cvttss2si rcx, xmm0
    mov.q rdx, [rsp + 0x8]
    movsx rdx, dx
    mov.q rbx, [rsp + 0x10]
    movsxd rbx, ebx
    mov.q rbp, [rsp + 0x20]
    movzx ebp, bpl
    mov.q rsi, [rsp + 0x28]
    movzx esi, si
    mov.q rdi, [rsp + 0x30]
    mov edi, edi
    cvtsi2sd xmm0, rax
    cvttsd2si rax, xmm0
    lea rax, [rcx + rax + 0x6]
    cvtsi2ss xmm0, rdx
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, rdx
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, rbx
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, rbx
    cvttsd2si rcx, xmm0
    add rax, rcx
    mov.q rcx, [rsp + 0x18]
    cvtsi2ss xmm0, rcx
    cvttss2si rcx, xmm0
    add rax, rcx
    mov.q rcx, [rsp + 0x18]
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, rbp
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, rbp
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, rsi
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, rsi
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, rdi
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, rdi
    cvttsd2si rcx, xmm0
    add rax, rcx
    mov.q rcx, [rsp + 0x38]
    cvtsi2ss xmm0, rcx
    cvttss2si rcx, xmm0
    add rax, rcx
    mov.q rcx, [rsp + 0x38]
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rax, 0x100cc
    add rsp, 0x48
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
opaque:
    mov rax, rdi
    ret
2:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, -0x64
    call opaque
    mov.q [rsp], rax
    mov rdi, -0xbb8
    call opaque
    mov r15, rax
    mov rdi, -0x11170
    call opaque
    mov r14, rax
    mov rdi, -0x4c4b40
    call opaque
    mov r13, rax
    mov rdi, 0xc8
    call opaque
    mov r12, rax
    mov rdi, 0xea60
    call opaque
    mov rbp, rax
    mov rdi, 0x3d0900
    call opaque
    mov rbx, rax
    mov rdi, 0x6acfc0
    call opaque
    mov r10, rax
    mov.q rax, [rsp]
    movsx r9, al
    cvtsi2ss xmm0, r9
    cvttss2si rax, xmm0
    mov rcx, r15
    movsx r8, cx
    mov rcx, r14
    movsxd rdi, ecx
    mov rcx, r12
    movzx esi, cl
    mov rcx, rbp
    movzx edx, cx
    mov rcx, rbx
    mov ecx, ecx
    cvtsi2sd xmm0, r9
    cvttsd2si r9, xmm0
    lea rax, [rax + r9 + 0x6]
    cvtsi2ss xmm0, r8
    cvttss2si r9, xmm0
    add rax, r9
    cvtsi2sd xmm0, r8
    cvttsd2si r8, xmm0
    add rax, r8
    cvtsi2ss xmm0, rdi
    cvttss2si r8, xmm0
    add rax, r8
    cvtsi2sd xmm0, rdi
    cvttsd2si rdi, xmm0
    add rax, rdi
    cvtsi2ss xmm0, r13
    cvttss2si rdi, xmm0
    add rax, rdi
    cvtsi2sd xmm0, r13
    cvttsd2si rdi, xmm0
    add rax, rdi
    cvtsi2ss xmm0, rsi
    cvttss2si rdi, xmm0
    add rax, rdi
    cvtsi2sd xmm0, rsi
    cvttsd2si rsi, xmm0
    add rax, rsi
    cvtsi2ss xmm0, rdx
    cvttss2si rsi, xmm0
    add rax, rsi
    cvtsi2sd xmm0, rdx
    cvttsd2si rdx, xmm0
    add rax, rdx
    cvtsi2ss xmm0, rcx
    cvttss2si rdx, xmm0
    add rax, rdx
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, r10
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, r10
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rax, 0x100cc
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: all ===========
opaque:
    mov rax, rdi
    ret
2:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, -0x64
    call opaque
    mov.q [rsp], rax
    mov rdi, -0xbb8
    call opaque
    mov r15, rax
    mov rdi, -0x11170
    call opaque
    mov r14, rax
    mov rdi, -0x4c4b40
    call opaque
    mov r13, rax
    mov rdi, 0xc8
    call opaque
    mov r12, rax
    mov rdi, 0xea60
    call opaque
    mov rbp, rax
    mov rdi, 0x3d0900
    call opaque
    mov rbx, rax
    mov rdi, 0x6acfc0
    call opaque
    mov r10, rax
    mov.q rax, [rsp]
    movsx r9, al
    cvtsi2ss xmm0, r9
    cvttss2si rax, xmm0
    mov rcx, r15
    movsx r8, cx
    mov rcx, r14
    movsxd rdi, ecx
    mov rcx, r12
    movzx esi, cl
    mov rcx, rbp
    movzx edx, cx
    mov rcx, rbx
    mov ecx, ecx
    cvtsi2sd xmm0, r9
    cvttsd2si r9, xmm0
    lea rax, [rax + r9 + 0x6]
    cvtsi2ss xmm0, r8
    cvttss2si r9, xmm0
    add rax, r9
    cvtsi2sd xmm0, r8
    cvttsd2si r8, xmm0
    add rax, r8
    cvtsi2ss xmm0, rdi
    cvttss2si r8, xmm0
    add rax, r8
    cvtsi2sd xmm0, rdi
    cvttsd2si rdi, xmm0
    add rax, rdi
    cvtsi2ss xmm0, r13
    cvttss2si rdi, xmm0
    add rax, rdi
    cvtsi2sd xmm0, r13
    cvttsd2si rdi, xmm0
    add rax, rdi
    cvtsi2ss xmm0, rsi
    cvttss2si rdi, xmm0
    add rax, rdi
    cvtsi2sd xmm0, rsi
    cvttsd2si rsi, xmm0
    add rax, rsi
    cvtsi2ss xmm0, rdx
    cvttss2si rsi, xmm0
    add rax, rsi
    cvtsi2sd xmm0, rdx
    cvttsd2si rdx, xmm0
    add rax, rdx
    cvtsi2ss xmm0, rcx
    cvttss2si rdx, xmm0
    add rax, rdx
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, r10
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, r10
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rax, 0x100cc
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
opaque:
    mov rax, 0xb7b70a
    ret
