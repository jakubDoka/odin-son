=========== OPT LEVEL: none-quick ===========
main:
    push rbx
    sub rsp, 0x10
    mov rcx, 0xa
    mov rdx, 1
    xor ebx, ebx
1:
    mov.q [rsp], rbx
    mov rax, rdx
    mov.q rdx, [rsp]
    cmp rcx, 0
    jz 2
    sub rcx, 1
    add rdx, rax
    mov rbx, rax
    jmp 1
2:
    add rsp, 0x10
    pop rbx
    ret
=========== OPT LEVEL: none ===========
main:
    mov rsi, 0xa
    mov rdx, 1
    xor ecx, ecx
1:
    mov rax, rdx
    mov rdx, rcx
    cmp rsi, 0
    jz 2
    sub rsi, 1
    add rdx, rax
    mov rcx, rax
    jmp 1
2:
    ret
=========== OPT LEVEL: mininal-quick ===========
main:
    push rbx
    sub rsp, 0x10
    mov rcx, 0xa
    mov rdx, 1
    xor ebx, ebx
1:
    mov.q [rsp], rbx
    mov rax, rdx
    mov.q rdx, [rsp]
    cmp rcx, 0
    jz 2
    sub rcx, 1
    add rdx, rax
    mov rbx, rax
    jmp 1
2:
    add rsp, 0x10
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
main:
    mov rsi, 0xa
    mov rdx, 1
    xor ecx, ecx
1:
    mov rax, rdx
    mov rdx, rcx
    cmp rsi, 0
    jz 2
    sub rsi, 1
    add rdx, rax
    mov rcx, rax
    jmp 1
2:
    ret
=========== OPT LEVEL: moderate-quick ===========
main:
    push rbx
    sub rsp, 0x10
    mov rcx, 0xa
    mov rdx, 1
    xor ebx, ebx
1:
    mov.q [rsp], rbx
    mov rax, rdx
    mov.q rdx, [rsp]
    cmp rcx, 0
    jz 2
    sub rcx, 1
    add rdx, rax
    mov rbx, rax
    jmp 1
2:
    add rsp, 0x10
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
main:
    mov rsi, 0xa
    mov rdx, 1
    xor ecx, ecx
1:
    mov rax, rdx
    mov rdx, rcx
    cmp rsi, 0
    jz 2
    sub rsi, 1
    add rdx, rax
    mov rcx, rax
    jmp 1
2:
    ret
=========== OPT LEVEL: all ===========
main:
    xor esi, esi
    mov rdx, 0xa
    mov rcx, 1
1:
    mov rax, rcx
    mov rdi, rdx
    sub rdi, 1
    mov rcx, rsi
    add rcx, rax
    cmp rdx, 0
    mov rsi, rax
    mov rdx, rdi
    jnz 1
    ret
=========== OPT LEVEL: aggresive ===========
