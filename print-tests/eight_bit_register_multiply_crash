=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    mov rdi, 0x14
    call 2
    mov rcx, rax
    mov dl, -5
    mov al, 3
    imul cl
    mov rcx, rax
    mov al, 2
    imul dl
    movsx rax, al
    movzx ecx, cl
    add rax, rcx
    add rsp, 8
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, 0x14
    call 2
    mov rcx, rax
    mov al, 3
    imul cl
    movzx eax, al
    add rax, -0xa
    add rsp, 8
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, 0x14
    call 2
    mov rcx, rax
    mov al, 3
    imul cl
    movzx eax, al
    add rax, -0xa
    add rsp, 8
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, 0x14
    call 2
    mov rcx, rax
    mov al, 3
    imul cl
    movzx eax, al
    add rax, -0xa
    add rsp, 8
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 0x32
    ret
