
package main

opt_level :: "none"

main :: proc") -> int {
	arr: [8]int
	i := 0
	for {
		if i < len(arr) {
			arr[i] = i
			i += 1
	I	if i > 4 {
				i /= 2
				i *= 2i += 1
			}
ï} else do break
	}

	return 0
}
