=========== OPT LEVEL: none-quick ===========
main:
    push rbx
    push rbp
    sub rsp, 0x18
    xor ecx, ecx
    xor edx, edx
1:
    mov.q [rsp], rcx
    mov.q [rsp + 0x8], rdx
    xor ebx, ebx
2:
    mov.q rbp, [rsp]
    imul rbp, rbx
    mov rsi, rbx
    add rsi, 1
    mov.q rax, [rsp + 0x8]
    add rax, rbp
    cmp rsi, 3
    jnle 3
    mov.q [rsp + 0x8], rax
    mov rbx, rsi
    jmp 2
3:
    mov.q rbx, [rsp]
    add rbx, 1
    cmp rbx, 3
    jnle 4
    mov rcx, rbx
    mov rdx, rax
    jmp 1
4:
    add rsp, 0x18
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: none ===========
main:
    xor ecx, ecx
    xor eax, eax
1:
    xor edx, edx
2:
    mov rsi, rcx
    imul rsi, rdx
    add rdx, 1
    add rax, rsi
    cmp rdx, 3
    jnle 3
    jmp 2
3:
    add rcx, 1
    cmp rcx, 3
    jnle 4
    jmp 1
4:
    ret
=========== OPT LEVEL: mininal-quick ===========
main:
    push rbx
    push rbp
    sub rsp, 0x18
    xor ecx, ecx
    xor edx, edx
1:
    mov.q [rsp], rcx
    mov.q [rsp + 0x8], rdx
    xor ebx, ebx
2:
    mov.q rbp, [rsp]
    imul rbp, rbx
    mov rsi, rbx
    add rsi, 1
    mov.q rax, [rsp + 0x8]
    add rax, rbp
    cmp rsi, 3
    jnle 3
    mov.q [rsp + 0x8], rax
    mov rbx, rsi
    jmp 2
3:
    mov.q rbx, [rsp]
    add rbx, 1
    cmp rbx, 3
    jnle 4
    mov rcx, rbx
    mov rdx, rax
    jmp 1
4:
    add rsp, 0x18
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
main:
    xor ecx, ecx
    xor eax, eax
1:
    xor edx, edx
2:
    mov rsi, rcx
    imul rsi, rdx
    add rdx, 1
    add rax, rsi
    cmp rdx, 3
    jnle 3
    jmp 2
3:
    add rcx, 1
    cmp rcx, 3
    jnle 4
    jmp 1
4:
    ret
=========== OPT LEVEL: moderate-quick ===========
main:
    push rbx
    push rbp
    sub rsp, 0x18
    xor ecx, ecx
    xor edx, edx
1:
    mov.q [rsp], rcx
    mov.q [rsp + 0x8], rdx
    xor ebx, ebx
2:
    mov.q rbp, [rsp]
    imul rbp, rbx
    mov rsi, rbx
    add rsi, 1
    mov.q rax, [rsp + 0x8]
    add rax, rbp
    cmp rsi, 3
    jnle 3
    mov.q [rsp + 0x8], rax
    mov rbx, rsi
    jmp 2
3:
    mov.q rbx, [rsp]
    add rbx, 1
    cmp rbx, 3
    jnle 4
    mov rcx, rbx
    mov rdx, rax
    jmp 1
4:
    add rsp, 0x18
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
main:
    xor ecx, ecx
    xor eax, eax
1:
    xor edx, edx
2:
    mov rsi, rcx
    imul rsi, rdx
    add rdx, 1
    add rax, rsi
    cmp rdx, 3
    jnle 3
    jmp 2
3:
    add rcx, 1
    cmp rcx, 3
    jnle 4
    jmp 1
4:
    ret
=========== OPT LEVEL: all ===========
main:
    xor esi, esi
    xor ecx, ecx
1:
    xor edx, edx
2:
    mov rdi, rcx
    imul rdi, rdx
    add rdx, 1
    mov rax, rsi
    add rax, rdi
    cmp rdx, 3
    mov rsi, rax
    jng 2
    add rcx, 1
    cmp rcx, 3
    mov rsi, rax
    jng 1
    ret
=========== OPT LEVEL: aggresive ===========
