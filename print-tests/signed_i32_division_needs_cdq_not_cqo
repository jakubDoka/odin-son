=========== OPT LEVEL: none ===========
main:
    push rbx
    push rbp
    sub rsp, 8
    mov rdi, 0xa
    call 2
    mov rbx, rax
    mov rdi, 0x1e
    call 2
    mov rbp, rax
    mov rdi, 3
    call 2
    mov rdx, rax
    mov rax, rbx
    mov rcx, rbp
    sub eax, ecx
    mov rcx, rdx
    cdq
    idiv ecx
    movsxd rax, eax
    add rsp, 8
    pop rbp
    pop rbx
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: mininal ===========
main:
    push rbx
    push rbp
    sub rsp, 8
    mov rdi, 0xa
    call 2
    mov rbx, rax
    mov rdi, 0x1e
    call 2
    mov rbp, rax
    mov rdi, 3
    call 2
    mov rdx, rax
    mov rax, rbx
    mov rcx, rbp
    sub eax, ecx
    mov rcx, rdx
    cdq
    idiv ecx
    movsxd rax, eax
    add rsp, 8
    pop rbp
    pop rbx
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: moderate ===========
main:
    push rbx
    push rbp
    sub rsp, 8
    mov rdi, 0xa
    call 2
    mov rbx, rax
    mov rdi, 0x1e
    call 2
    mov rbp, rax
    mov rdi, 3
    call 2
    mov rdx, rax
    mov rax, rbx
    mov rcx, rbp
    sub eax, ecx
    mov rcx, rdx
    cdq
    idiv ecx
    movsxd rax, eax
    add rsp, 8
    pop rbp
    pop rbx
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: all ===========
main:
    push rbx
    push rbp
    sub rsp, 8
    mov rdi, 0xa
    call 2
    mov rbx, rax
    mov rdi, 0x1e
    call 2
    mov rbp, rax
    mov rdi, 3
    call 2
    mov rdx, rax
    mov rax, rbx
    mov rcx, rbp
    sub eax, ecx
    mov rcx, rdx
    cdq
    idiv ecx
    movsxd rax, eax
    add rsp, 8
    pop rbp
    pop rbx
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, -6
    ret
