=========== OPT LEVEL: none ===========
main:
    xor eax, eax
    xor ecx, ecx
1:
    cmp rcx, 4
    jnl 6
    xor r8d, r8d
    xor edx, edx
2:
    cmp rdx, 4
    jnl 3
    mov rsi, rdx
    add rsi, 1
    add r8, rdx
    mov rdx, rsi
    jmp 2
3:
    xor esi, esi
    xor edx, edx
4:
    cmp rsi, 4
    jnl 5
    mov rdi, rsi
    add rdi, 1
    lea rdx, [rdx + rsi*2]
    mov rsi, rdi
    jmp 4
5:
    add r8, rdx
    add rcx, 1
    add rax, r8
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: mininal ===========
main:
    xor eax, eax
    xor ecx, ecx
1:
    cmp rcx, 4
    jnl 6
    xor r8d, r8d
    xor edx, edx
2:
    cmp rdx, 4
    jnl 3
    mov rsi, rdx
    add rsi, 1
    add r8, rdx
    mov rdx, rsi
    jmp 2
3:
    xor esi, esi
    xor edx, edx
4:
    cmp rsi, 4
    jnl 5
    mov rdi, rsi
    add rdi, 1
    lea rdx, [rdx + rsi*2]
    mov rsi, rdi
    jmp 4
5:
    add r8, rdx
    add rcx, 1
    add rax, r8
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: moderate ===========
main:
    xor eax, eax
    xor ecx, ecx
1:
    cmp rcx, 4
    jnl 6
    xor r8d, r8d
    xor edx, edx
2:
    cmp rdx, 4
    jnl 3
    mov rsi, rdx
    add rsi, 1
    add r8, rdx
    mov rdx, rsi
    jmp 2
3:
    xor esi, esi
    xor edx, edx
4:
    cmp rsi, 4
    jnl 5
    mov rdi, rsi
    add rdi, 1
    lea rdx, [rdx + rsi*2]
    mov rsi, rdi
    jmp 4
5:
    add r8, rdx
    add rcx, 1
    add rax, r8
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: all ===========
main:
    xor ecx, ecx
    xor edx, edx
1:
    xor eax, eax
    xor edi, edi
2:
    mov rsi, rdi
    mov rdi, rsi
    add rdi, rax
    mov r8, rax
    add r8, 1
    cmp rax, 4
    mov rax, r8
    jnge 2
    xor eax, eax
    xor edi, edi
3:
    mov r9, rdi
    mov r8, rax
    add r8, 1
    lea rdi, [r9 + rax*2]
    cmp rax, 4
    mov rax, r8
    jnge 3
    add rdx, 1
    add rsi, r9
    mov rax, rcx
    add rax, rsi
    cmp rdx, 4
    mov rcx, rax
    jnge 1
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: aggresive ===========
