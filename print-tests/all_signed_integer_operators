=========== OPT LEVEL: None ===========
main:
     push rbx
     push rbp
     mov rax, 0x14
     mov rbp, rax
     mov rcx, 0x00
     mov rax, 0x06
     mov rdx, rcx
     sub rdx, rax
     mov rbx, rdx
     mov rax, 0x07
     mov rdx, rcx
     sub rdx, rax
     mov rsi, rdx
     mov rax, rbp
     cqo
     idiv rbx
     mov rdi, rax
     add rdi, rcx
     mov rax, rbp
     cqo
     idiv rbx
     add rdi, rdx
     mov rax, rsi
     cqo
     idiv rbx
     add rdi, rax
     mov rax, rsi
     cqo
     idiv rbx
     add rdi, rdx
     mov rcx, 0x01
     mov rax, rsi
     sar rax, cl
     add rdi, rax
     mov rax, rbx
     sar rax, cl
     add rdi, rax
     mov rax, rbp
     mov rdx, rbx
     cmp rdx, rax
     setl dl
     movzx rdx, dl
     test rdx, rdx
     jz :0
     jmp :1

000: jmp :2

001: add rdi, rcx
     jmp :2

002: mov rdx, rdi
     mov rax, rsi
     cmp rax, rbx
     setl al
     movzx rax, al
     test rax, rax
     jz :3
     jmp :4

003: jmp :5

004: mov rax, 0x02
     add rax, rdx
     mov rdx, rax
     jmp :5

005: mov rax, rbp
     cmp rax, rbx
     setnle al
     movzx rax, al
     test rax, rax
     jz :6
     jmp :7

006: jmp :8

007: mov rax, 0x04
     mov rcx, rdx
     mov rdx, rax
     add rdx, rcx
     jmp :8

008: mov rax, rsi
     mov rcx, rbx
     cmp rcx, rax
     setle cl
     movzx rcx, cl
     test rcx, rcx
     jz :9
     jmp :10

009: jmp :11

010: mov rax, 0x08
     mov rcx, rdx
     mov rdx, rax
     add rdx, rcx
     jmp :11

011: mov rax, rbp
     cmp rax, rbx
     setnl al
     movzx rax, al
     test rax, rax
     jz :12
     jmp :13

012: jmp :14

013: mov rax, 0x10
     add rax, rdx
     mov rdx, rax
     jmp :14

014: mov rax, rdx
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: Full ===========
main:
     mov rcx, 0x01
     test rcx, rcx
     jz :0
     jmp :1

000: mov rax, 0xFFFFFFFFFFFFFFF8
     jmp :2

001: mov rax, 0xFFFFFFFFFFFFFFF9
     jmp :2

002: test rcx, rcx
     jz :3
     jmp :4

003: jmp :5

004: add rax, 0x02
     jmp :5

005: test rcx, rcx
     jz :6
     jmp :7

006: jmp :8

007: add rax, 0x04
     jmp :8

008: mov rdx, 0x00
     test rdx, rdx
     jz :9
     jmp :10

009: jmp :11

010: add rax, 0x08
     jmp :11

011: test rcx, rcx
     jz :12
     jmp :13

012: jmp :14

013: add rax, 0x10
     jmp :14

014: ret
