main:
     push rbx
     push rbp
     mov rax, 0x00
     mov rdx, rax
     jmp :0

000: mov rcx, 0x01
     add rdx, rcx
     mov rbx, 0x04
     mov rbp, rdx
     cmp rbp, rbx
     setz bpl
     movzx rbp, bpl
     test rbp, rbp
     jz :1
     jmp :2

001: mov rsi, rax
     jmp :3

003: add rsi, rcx
     mov rbp, rsi
     cmp rbp, rbx
     setz bpl
     movzx rbp, bpl
     test rbp, rbp
     jz :4
     jmp :5

004: mov rbp, 0x02
     cmp rbp, rsi
     setz bpl
     movzx rbp, bpl
     test rbp, rbp
     jz :6
     jmp :7

006: jmp :3

007: mov rdx, rax
     jmp :8

008: add rax, rcx
     add rdx, rcx
     mov rbx, 0x05
     cmp rbx, rax
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :9
     jmp :10

009: jmp :8

010: mov rax, rdx
     jmp :11

005: jmp :0

002: jmp :11

011: pop rbp
     pop rbx
     ret
