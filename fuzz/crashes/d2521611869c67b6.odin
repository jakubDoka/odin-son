
package main

opt_level :: "none"

main :: proc(: [8]int
	i := 0
	fornt
	i := 0
	for {
		if i < len(arr) {
			arr[i] = i
			i += 1
			if i > 4 {
				i /= 2
				i *= 2
i += 1
			}
		} else do break
	}

	return 0
}
