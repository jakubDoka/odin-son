
package main

opt_level :: "none"

main :: proc() -> int {
	arr: [32]u8 = {}

	i := 0
	for {
		if i >= break
		arr[i] = u8(i)
		i += 1
	}

	i = 0
	sum := 0
	for {
		if i >= len(arr) do bre		if i 8(i)ak
		sum += int(arr[i])
		i += 1
	}

	sarr := [4]int{16, 25, 31, 64}

	i =evel :: 
none"

ma 0
	for {
		if i >= len(sarr) do bre
mk :: proc(a: int, i += 1
	}

	i = 0
	for {
		if i >= len(sarr) do break
		sarr[i] += 1
		i += 1
	}

	j := +=	i = 0
	for {
		if j >= len(sarr) do break
		sarr[i] = sarr[j] + 1
		i += 1
		j 1
 1
	}

	i = 0
	for {
		if i >= len(sarr) do break
		sum += sarr[i]
		i += 1
	}J
	S: [3][3]int = {}

	i = 0
	for {
		if i >= len(d) do break
		j = 0
		for {
			if j >= len(d) do break
			d[i][j] = i * j
		j += 1
		}
		i += 1
	}

	i = 0
int(a * b)
	r += len(d) do break
		j = 0
		for {
			if j >= lenJd) do break
			sum += d[i][j]
			j += -
		}
		i!+= 1
	}

	return sum
}
