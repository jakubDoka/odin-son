=========== OPT LEVEL: none ===========
main:
    push rbx
fib i32:
    mov rdi, 0xa
    call 4
    mov rbx, rax
    mov rdi, 0xa
    call 7
    mov rcx, rax
    movsxd rax, ebx
    add rax, rcx
    pop rbx
    ret
4:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    cmp ebp, 2
    jng 5
    mov rax, rbp
    sub eax, 1
    mov rdi, rax
    call 4
    mov rbx, rax
    sub ebp, 2
    mov rdi, rbp
    call 4
    add ebx, eax
    jmp 6
5:
    mov rbx, 1
6:
    mov rax, rbx
    add rsp, 8
    pop rbp
    pop rbx
    ret
7:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    cmp rbp, 2
    jng 8
    mov rax, rbp
    sub rax, 1
    mov rdi, rax
    call 7
    mov rbx, rax
    sub rbp, 2
    mov rdi, rbp
    call 7
    add rbx, rax
    jmp 9
8:
    mov rbx, 1
9:
    mov rax, rbx
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
main:
    push rbx
fib i32:
    mov rdi, 0xa
    call 4
    mov rbx, rax
    mov rdi, 0xa
    call 7
    mov rcx, rax
    movsxd rax, ebx
    add rax, rcx
    pop rbx
    ret
4:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    cmp ebp, 2
    jng 5
    mov rax, rbp
    sub eax, 1
    mov rdi, rax
    call 4
    mov rbx, rax
    sub ebp, 2
    mov rdi, rbp
    call 4
    add ebx, eax
    jmp 6
5:
    mov rbx, 1
6:
    mov rax, rbx
    add rsp, 8
    pop rbp
    pop rbx
    ret
7:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    cmp rbp, 2
    jng 8
    mov rax, rbp
    sub rax, 1
    mov rdi, rax
    call 7
    mov rbx, rax
    sub rbp, 2
    mov rdi, rbp
    call 7
    add rbx, rax
    jmp 9
8:
    mov rbx, 1
9:
    mov rax, rbx
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
main:
    push rbx
fib i32:
    mov rdi, 0xa
    call 4
    mov rbx, rax
    mov rdi, 0xa
    call 7
    mov rcx, rax
    movsxd rax, ebx
    add rax, rcx
    pop rbx
    ret
4:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    cmp ebp, 2
    jng 5
    mov rax, rbp
    sub eax, 1
    mov rdi, rax
    call 4
    mov rbx, rax
    sub ebp, 2
    mov rdi, rbp
    call 4
    add ebx, eax
    jmp 6
5:
    mov rbx, 1
6:
    mov rax, rbx
    add rsp, 8
    pop rbp
    pop rbx
    ret
7:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    cmp rbp, 2
    jng 8
    mov rax, rbp
    sub rax, 1
    mov rdi, rax
    call 7
    mov rbx, rax
    sub rbp, 2
    mov rdi, rbp
    call 7
    add rbx, rax
    jmp 9
8:
    mov rbx, 1
9:
    mov rax, rbx
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: all ===========
main:
    push rbx
fib i32:
    mov rdi, 0xa
    call 4
    mov rbx, rax
    mov rdi, 0xa
    call 7
    mov rcx, rax
    movsxd rax, ebx
    add rax, rcx
    pop rbx
    ret
4:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    cmp ebp, 2
    jng 5
    mov rax, rbp
    sub eax, 1
    mov rdi, rax
    call 4
    mov rbx, rax
    sub ebp, 2
    mov rdi, rbp
    call 4
    add ebx, eax
    jmp 6
5:
    mov rbx, 1
6:
    mov rax, rbx
    add rsp, 8
    pop rbp
    pop rbx
    ret
7:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    cmp rbp, 2
    jng 8
    mov rax, rbp
    sub rax, 1
    mov rdi, rax
    call 7
    mov rbx, rax
    sub rbp, 2
    mov rdi, rbp
    call 7
    add rbx, rax
    jmp 9
8:
    mov rbx, 1
9:
    mov rax, rbx
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
