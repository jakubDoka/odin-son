=========== OPT LEVEL: none ===========
sld:
    mov.d ecx, [rsi]
    xor edx, edx
    cmp edi, ecx
    setz al
    movzx rax, al
    movzx eax, al
    add rdx, rax
    cmp edi, ecx
    setnz al
    movzx rax, al
    movzx eax, al
    lea rdx, [rdx + rax*2]
    cmp edi, ecx
    setnge al
    movzx rax, al
    movzx eax, al
    lea rdx, [rdx + rax*4]
    cmp edi, ecx
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rdx + rax*8]
    cmp edi, ecx
    setnle dl
    movzx rdx, dl
    movzx edx, dl
    imul rdx, rdx, 0x10
    add rax, rdx
    cmp edi, ecx
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
3:
    mov.d ecx, [rsi]
    xor edx, edx
    cmp edi, ecx
    setz al
    movzx rax, al
    movzx eax, al
    add rdx, rax
    cmp edi, ecx
    setnz al
    movzx rax, al
    movzx eax, al
    lea rdx, [rdx + rax*2]
    cmp edi, ecx
    setnae al
    movzx rax, al
    movzx eax, al
    lea rdx, [rdx + rax*4]
    cmp edi, ecx
    setna al
    movzx rax, al
    movzx eax, al
    lea rax, [rdx + rax*8]
    cmp edi, ecx
    setnbe dl
    movzx rdx, dl
    movzx edx, dl
    imul rdx, rdx, 0x10
    add rax, rdx
    cmp edi, ecx
    setnc cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
4:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    sub rsp, 0x10
    mov.d [rsp], 5
    mov rdi, 3
    lea rsi, [rsp]
    call sld
    mov r14, rax
    lea rsi, [rsp]
    mov rdi, 5
    call sld
    mov r13, rax
    mov rdi, 9
    lea rsi, [rsp]
    call sld
    mov r12, rax
    movabs rax, 0xee6b2800
    mov.d [rsp + 0x4], eax
    mov rdi, 5
    lea rsi, [rsp + 0x4]
    call 3
    mov rbp, rax
    lea rsi, [rsp + 0x4]
    movabs rdi, 0xee6b2800
    call 3
    mov rbx, rax
    movabs rdi, 0xee6b2801
    lea rsi, [rsp + 0x4]
    call 3
    mov rsi, rax
    mov rcx, r14
    add rcx, r13
    add rcx, r12
    xor eax, eax
    add rax, rcx
    mov rcx, rbp
    add rcx, rbx
    add rcx, rsi
    add rax, rcx
    add rsp, 0x10
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
sld:
    mov.d ecx, [rsi]
    xor edx, edx
    cmp edi, ecx
    setz al
    movzx rax, al
    movzx eax, al
    add rdx, rax
    cmp edi, ecx
    setnz al
    movzx rax, al
    movzx eax, al
    lea rdx, [rdx + rax*2]
    cmp edi, ecx
    setnge al
    movzx rax, al
    movzx eax, al
    lea rdx, [rdx + rax*4]
    cmp edi, ecx
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rdx + rax*8]
    cmp edi, ecx
    setnle dl
    movzx rdx, dl
    movzx edx, dl
    imul rdx, rdx, 0x10
    add rax, rdx
    cmp edi, ecx
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
3:
    mov.d ecx, [rsi]
    xor edx, edx
    cmp edi, ecx
    setz al
    movzx rax, al
    movzx eax, al
    add rdx, rax
    cmp edi, ecx
    setnz al
    movzx rax, al
    movzx eax, al
    lea rdx, [rdx + rax*2]
    cmp edi, ecx
    setnae al
    movzx rax, al
    movzx eax, al
    lea rdx, [rdx + rax*4]
    cmp edi, ecx
    setna al
    movzx rax, al
    movzx eax, al
    lea rax, [rdx + rax*8]
    cmp edi, ecx
    setnbe dl
    movzx rdx, dl
    movzx edx, dl
    imul rdx, rdx, 0x10
    add rax, rdx
    cmp edi, ecx
    setnc cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
4:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    sub rsp, 0x10
    mov.d [rsp], 5
    mov rdi, 3
    lea rsi, [rsp]
    call sld
    mov r14, rax
    lea rsi, [rsp]
    mov rdi, 5
    call sld
    mov r13, rax
    mov rdi, 9
    lea rsi, [rsp]
    call sld
    mov r12, rax
    movabs rax, 0xee6b2800
    mov.d [rsp + 0x4], eax
    mov rdi, 5
    lea rsi, [rsp + 0x4]
    call 3
    mov rbp, rax
    lea rsi, [rsp + 0x4]
    movabs rdi, 0xee6b2800
    call 3
    mov rbx, rax
    movabs rdi, 0xee6b2801
    lea rsi, [rsp + 0x4]
    call 3
    mov rsi, rax
    mov rcx, r14
    add rcx, r13
    add rcx, r12
    xor eax, eax
    add rax, rcx
    mov rcx, rbp
    add rcx, rbx
    add rcx, rsi
    add rax, rcx
    add rsp, 0x10
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
sld:
    mov.d ecx, [rsi]
    cmp edi, ecx
    setz al
    movzx rax, al
    movzx edx, al
    cmp edi, ecx
    setnz al
    movzx rax, al
    movzx eax, al
    lea rdx, [rdx + rax*2]
    cmp edi, ecx
    setnge al
    movzx rax, al
    movzx eax, al
    lea rdx, [rdx + rax*4]
    cmp edi, ecx
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rdx + rax*8]
    cmp edi, ecx
    setnle dl
    movzx rdx, dl
    movzx edx, dl
    imul rdx, rdx, 0x10
    add rax, rdx
    cmp edi, ecx
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
3:
    mov.d ecx, [rsi]
    cmp edi, ecx
    setz al
    movzx rax, al
    movzx edx, al
    cmp edi, ecx
    setnz al
    movzx rax, al
    movzx eax, al
    lea rdx, [rdx + rax*2]
    cmp edi, ecx
    setnae al
    movzx rax, al
    movzx eax, al
    lea rdx, [rdx + rax*4]
    cmp edi, ecx
    setna al
    movzx rax, al
    movzx eax, al
    lea rax, [rdx + rax*8]
    cmp edi, ecx
    setnbe dl
    movzx rdx, dl
    movzx edx, dl
    imul rdx, rdx, 0x10
    add rax, rdx
    cmp edi, ecx
    setnc cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
4:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    sub rsp, 0x10
    mov.d [rsp], 5
    mov rdi, 3
    lea rsi, [rsp]
    call sld
    mov r14, rax
    lea rsi, [rsp]
    mov rdi, 5
    call sld
    mov r13, rax
    mov rdi, 9
    lea rsi, [rsp]
    call sld
    mov r12, rax
    movabs rax, 0xee6b2800
    mov.d [rsp + 0x4], eax
    mov rdi, 5
    lea rsi, [rsp + 0x4]
    call 3
    mov rbp, rax
    lea rsi, [rsp + 0x4]
    movabs rdi, 0xee6b2800
    call 3
    mov rbx, rax
    movabs rdi, 0xee6b2801
    lea rsi, [rsp + 0x4]
    call 3
    mov rsi, rax
    mov rcx, r14
    add rcx, r13
    add rcx, r12
    mov rax, rbp
    add rax, rbx
    add rax, rsi
    add rcx, rax
    mov rax, rcx
    add rsp, 0x10
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: all ===========
sld:
    mov.d ecx, [rsi]
    cmp edi, ecx
    setz al
    movzx rax, al
    movzx edx, al
    cmp edi, ecx
    setnz al
    movzx rax, al
    movzx eax, al
    lea rdx, [rdx + rax*2]
    cmp edi, ecx
    setnge al
    movzx rax, al
    movzx eax, al
    lea rdx, [rdx + rax*4]
    cmp edi, ecx
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rdx + rax*8]
    cmp edi, ecx
    setnle dl
    movzx rdx, dl
    movzx edx, dl
    imul rdx, rdx, 0x10
    add rax, rdx
    cmp edi, ecx
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
3:
    mov.d ecx, [rsi]
    cmp edi, ecx
    setz al
    movzx rax, al
    movzx edx, al
    cmp edi, ecx
    setnz al
    movzx rax, al
    movzx eax, al
    lea rdx, [rdx + rax*2]
    cmp edi, ecx
    setnae al
    movzx rax, al
    movzx eax, al
    lea rdx, [rdx + rax*4]
    cmp edi, ecx
    setna al
    movzx rax, al
    movzx eax, al
    lea rax, [rdx + rax*8]
    cmp edi, ecx
    setnbe dl
    movzx rdx, dl
    movzx edx, dl
    imul rdx, rdx, 0x10
    add rax, rdx
    cmp edi, ecx
    setnc cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
4:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    sub rsp, 0x10
    mov.d [rsp], 5
    mov rdi, 3
    lea rsi, [rsp]
    call sld
    mov r14, rax
    lea rsi, [rsp]
    mov rdi, 5
    call sld
    mov r13, rax
    mov rdi, 9
    lea rsi, [rsp]
    call sld
    mov r12, rax
    movabs rax, 0xee6b2800
    mov.d [rsp + 0x4], eax
    mov rdi, 5
    lea rsi, [rsp + 0x4]
    call 3
    mov rbp, rax
    lea rsi, [rsp + 0x4]
    movabs rdi, 0xee6b2800
    call 3
    mov rbx, rax
    movabs rdi, 0xee6b2801
    lea rsi, [rsp + 0x4]
    call 3
    mov rsi, rax
    mov rcx, r14
    add rcx, r13
    add rcx, r12
    mov rax, rbp
    add rax, rbx
    add rax, rsi
    add rcx, rax
    mov rax, rcx
    add rsp, 0x10
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
