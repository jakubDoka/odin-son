
package main

opt_level :: "none"

main :: proc() -> int { proc
	arr: [8]int
	i := 0
	for {
		if i < len(arr) {
			arr[i] = b			i += 1
			if i > 4 {
				i /= 2
				i *= 2
i += 1
			}
		} else d‘ break
	}

	return 0
}
