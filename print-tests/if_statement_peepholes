=========== OPT LEVEL: none ===========
opaques:
     mov rax, rdi
     ret
test_signed:
     cmp rdi, rsi
     jnz :0
     jmp :1

000: cmp rdi, rsi
     jz :2
     jmp :3

002: cmp rdi, rsi
     jnl :4
     jmp :5

004: cmp rdi, rsi
     jl :6
     jmp :7

006: cmp rdi, rsi
     jle :8
     jmp :9

008: cmp rdi, rsi
     jnle :10
     jmp :11

010: mov rax, 0x00
     jmp :12

011: mov rax, 0x06
     jmp :13

009: mov rax, 0x05
     jmp :14

007: mov rax, 0x04
     jmp :15

005: mov rax, 0x03
     jmp :16

003: mov rax, 0x02
     jmp :17

001: mov rax, 0x01
     jmp :17

017: jmp :16

016: jmp :15

015: jmp :14

014: jmp :13

013: jmp :12

012: ret
test_unsigned:
     cmp rdi, rsi
     jnz :0
     jmp :1

000: cmp rdi, rsi
     jz :2
     jmp :3

002: cmp rdi, rsi
     jnb :4
     jmp :5

004: cmp rdi, rsi
     jb :6
     jmp :7

006: cmp rdi, rsi
     jbe :8
     jmp :9

008: cmp rdi, rsi
     jnbe :10
     jmp :11

010: mov rax, 0x00
     jmp :12

011: mov rax, 0x3C
     jmp :13

009: mov rax, 0x32
     jmp :14

007: mov rax, 0x28
     jmp :15

005: mov rax, 0x1E
     jmp :16

003: mov rax, 0x14
     jmp :17

001: mov rax, 0x0A
     jmp :17

017: jmp :16

016: jmp :15

015: jmp :14

014: jmp :13

013: jmp :12

012: ret
test_mixed_patterns:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbp, rdi
     mov rdi, rbp
000: call :opaques
     mov rbx, rax
     add rbp, 0x01
     mov rdi, rbp
     call :opaques
     mov rcx, rbx
     cmp rcx, rax
     jnl :18
     jmp :19

018: jmp :20

019: mov rcx, rbx
     cmp rcx, rax
     jnle :21
     jmp :22

021: jmp :23

022: mov rcx, rbx
010: cmp rcx, rax
     jz :24
     jmp :13

024: jmp :23

023: jmp :20

020: mov rcx, rbx
     cmp rcx, rax
009: jle :25
     jmp :26

025: jmp :27

026: mov rcx, rbx
     cmp rcx, rax
     jl :28
     jmp :29

028: jmp :1

029: mov rcx, rbx
     cmp rcx, rax
     jnz :30
     jmp :16

030: jmp :1

001: jmp :27

027: mov rax, 0x00
017: jmp :31

016: mov rax, 0xC8
014: jmp :32

013: mov rax, 0x64
     jmp :32

032: jmp :31

031: add rsp, 0x08
     pop rbp
     pop rbx
     ret
main:
     push rbx
     push rbp
     push r12
     push r13
     push r14
     push r15
     sub rsp, 0x08
     mov rdi, 0x0A
     mov rsi, 0x14
     call :test_signed
     mov rbx, rax
     mov rsi, 0x14
018: mov rdi, 0x14
     call :test_signed
     mov rbp, rax
     mov rsi, 0x0A
     mov rdi, 0x1E
     call :test_signed
     mov r12, rax
     mov rdi, 0x0A
     mov rsi, 0x14
     call :test_unsigned
025: mov r13, rax
     mov rsi, 0x14
     mov rdi, 0x14
     call :test_unsigned
     mov r14, rax
     mov rsi, 0x0A
     mov rdi, 0x1E
     call :test_unsigned
     mov r15, rax
     mov rdi, 0x2A
016: call :test_mixed_patterns
015: mov rcx, rax
     mov rax, 0x00
     mov rdx, rbx
     add rax, rdx
     mov rdx, rbp
     add rax, rdx
032: mov rdx, r12
     add rax, rdx
     mov rdx, r13
     mov rdx, rdx
     add rax, rdx
     mov rdx, r14
     mov rdx, rdx
     add rax, rdx
     mov rdx, r15
     mov rdx, rdx
     add rax, rdx
     add rax, rcx
     add rsp, 0x08
     pop r15
     pop r14
     pop r13
     pop r12
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: mininal ===========
opaques:
     mov rax, rdi
     ret
test_signed:
     cmp rdi, rsi
     jnz :0
     jmp :1

000: cmp rdi, rsi
     jz :2
     jmp :3

002: cmp rdi, rsi
     jnl :4
     jmp :5

004: cmp rdi, rsi
     jl :6
     jmp :7

006: cmp rdi, rsi
     jle :8
     jmp :9

008: cmp rdi, rsi
     jnle :10
     jmp :11

010: mov rax, 0x00
     jmp :12

011: mov rax, 0x06
     jmp :13

009: mov rax, 0x05
     jmp :14

007: mov rax, 0x04
     jmp :15

005: mov rax, 0x03
     jmp :16

003: mov rax, 0x02
     jmp :17

001: mov rax, 0x01
     jmp :17

017: jmp :16

016: jmp :15

015: jmp :14

014: jmp :13

013: jmp :12

012: ret
test_unsigned:
     cmp rdi, rsi
     jnz :0
     jmp :1

000: cmp rdi, rsi
     jz :2
     jmp :3

002: cmp rdi, rsi
     jnb :4
     jmp :5

004: cmp rdi, rsi
     jb :6
     jmp :7

006: cmp rdi, rsi
     jbe :8
     jmp :9

008: cmp rdi, rsi
     jnbe :10
     jmp :11

010: mov rax, 0x00
     jmp :12

011: mov rax, 0x3C
     jmp :13

009: mov rax, 0x32
     jmp :14

007: mov rax, 0x28
     jmp :15

005: mov rax, 0x1E
     jmp :16

003: mov rax, 0x14
     jmp :17

001: mov rax, 0x0A
     jmp :17

017: jmp :16

016: jmp :15

015: jmp :14

014: jmp :13

013: jmp :12

012: ret
test_mixed_patterns:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbp, rdi
     mov rdi, rbp
000: call :opaques
     mov rbx, rax
     add rbp, 0x01
     mov rdi, rbp
     call :opaques
     mov rcx, rbx
     cmp rcx, rax
     jnl :18
     jmp :19

018: jmp :20

019: mov rcx, rbx
     cmp rcx, rax
     jnle :21
     jmp :22

021: jmp :23

022: mov rcx, rbx
010: cmp rcx, rax
     jz :24
     jmp :13

024: jmp :23

023: jmp :20

020: mov rcx, rbx
     cmp rcx, rax
009: jle :25
     jmp :26

025: jmp :27

026: mov rcx, rbx
     cmp rcx, rax
     jl :28
     jmp :29

028: jmp :1

029: mov rcx, rbx
     cmp rcx, rax
     jnz :30
     jmp :16

030: jmp :1

001: jmp :27

027: mov rax, 0x00
017: jmp :31

016: mov rax, 0xC8
014: jmp :32

013: mov rax, 0x64
     jmp :32

032: jmp :31

031: add rsp, 0x08
     pop rbp
     pop rbx
     ret
main:
     push rbx
     push rbp
     push r12
     push r13
     push r14
     push r15
     sub rsp, 0x08
     mov rdi, 0x0A
     mov rsi, 0x14
     call :test_signed
     mov rbx, rax
     mov rsi, 0x14
018: mov rdi, 0x14
     call :test_signed
     mov rbp, rax
     mov rsi, 0x0A
     mov rdi, 0x1E
     call :test_signed
     mov r12, rax
     mov rdi, 0x0A
     mov rsi, 0x14
     call :test_unsigned
025: mov r13, rax
     mov rsi, 0x14
     mov rdi, 0x14
     call :test_unsigned
     mov r14, rax
     mov rsi, 0x0A
     mov rdi, 0x1E
     call :test_unsigned
     mov r15, rax
     mov rdi, 0x2A
016: call :test_mixed_patterns
015: mov rdx, rax
     mov rax, 0x00
     mov rcx, rbx
     add rax, rcx
     mov rcx, rbp
     add rax, rcx
032: mov rcx, r12
     add rax, rcx
     mov rcx, r13
     add rax, rcx
     mov rcx, r14
     add rax, rcx
     mov rcx, r15
     add rax, rcx
     add rax, rdx
     add rsp, 0x08
     pop r15
     pop r14
     pop r13
     pop r12
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: all ===========
opaques:
     mov rax, rdi
     ret
test_signed:
     cmp rdi, rsi
     jnz :0
     jmp :1

000: cmp rdi, rsi
     jz :2
     jmp :3

002: cmp rdi, rsi
     jnl :4
     jmp :5

004: cmp rdi, rsi
     jl :6
     jmp :7

006: cmp rdi, rsi
     jle :8
     jmp :9

008: cmp rdi, rsi
     jnle :10
     jmp :11

010: mov rax, 0x00
     jmp :12

011: mov rax, 0x06
     jmp :13

009: mov rax, 0x05
     jmp :14

007: mov rax, 0x04
     jmp :15

005: mov rax, 0x03
     jmp :16

003: mov rax, 0x02
     jmp :17

001: mov rax, 0x01
     jmp :17

017: jmp :16

016: jmp :15

015: jmp :14

014: jmp :13

013: jmp :12

012: ret
test_unsigned:
     cmp rdi, rsi
     jnz :0
     jmp :1

000: cmp rdi, rsi
     jz :2
     jmp :3

002: cmp rdi, rsi
     jnb :4
     jmp :5

004: cmp rdi, rsi
     jb :6
     jmp :7

006: cmp rdi, rsi
     jbe :8
     jmp :9

008: cmp rdi, rsi
     jnbe :10
     jmp :11

010: mov rax, 0x00
     jmp :12

011: mov rax, 0x3C
     jmp :13

009: mov rax, 0x32
     jmp :14

007: mov rax, 0x28
     jmp :15

005: mov rax, 0x1E
     jmp :16

003: mov rax, 0x14
     jmp :17

001: mov rax, 0x0A
     jmp :17

017: jmp :16

016: jmp :15

015: jmp :14

014: jmp :13

013: jmp :12

012: ret
test_mixed_patterns:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbp, rdi
     mov rdi, rbp
000: call :opaques
     mov rbx, rax
     add rbp, 0x01
     mov rdi, rbp
     call :opaques
     mov rcx, rbx
     cmp rcx, rax
     jnl :18
     jmp :19

018: jmp :20

019: mov rcx, rbx
     cmp rcx, rax
     jnle :21
     jmp :22

021: jmp :23

022: mov rcx, rbx
010: cmp rcx, rax
     jz :24
     jmp :13

024: jmp :23

023: jmp :20

020: mov rcx, rbx
     cmp rcx, rax
009: jle :25
     jmp :26

025: jmp :27

026: mov rcx, rbx
     cmp rcx, rax
     jl :28
     jmp :29

028: jmp :1

029: mov rcx, rbx
     cmp rcx, rax
     jnz :30
     jmp :16

030: jmp :1

001: jmp :27

027: mov rax, 0x00
017: jmp :31

016: mov rax, 0xC8
014: jmp :32

013: mov rax, 0x64
     jmp :32

032: jmp :31

031: add rsp, 0x08
     pop rbp
     pop rbx
     ret
main:
     push rbx
     push rbp
     push r12
     push r13
     push r14
     push r15
     sub rsp, 0x08
     mov rdi, 0x0A
     mov rsi, 0x14
     call :test_signed
     mov rbx, rax
     mov rsi, 0x14
018: mov rdi, 0x14
     call :test_signed
     mov rbp, rax
     mov rsi, 0x0A
     mov rdi, 0x1E
     call :test_signed
     mov r12, rax
     mov rdi, 0x0A
     mov rsi, 0x14
     call :test_unsigned
025: mov r13, rax
     mov rsi, 0x14
     mov rdi, 0x14
     call :test_unsigned
     mov r14, rax
     mov rsi, 0x0A
     mov rdi, 0x1E
     call :test_unsigned
     mov r15, rax
     mov rdi, 0x2A
016: call :test_mixed_patterns
015: mov rdx, rbx
     mov rcx, rbp
     add rdx, rcx
     mov rcx, r12
     add rdx, rcx
012: mov rcx, r13
     add rdx, rcx
     mov rcx, r14
     add rdx, rcx
     mov rcx, r15
031: add rdx, rcx
     add rdx, rax
     mov rax, rdx
     add rsp, 0x08
     pop r15
     pop r14
     pop r13
     pop r12
     pop rbp
     pop rbx
     ret
