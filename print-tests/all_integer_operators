=========== OPT LEVEL: none ===========
batch:
main:
     push rbx
     push rbp
     push r12
     push r13
     push r14
     push r15
     sub rsp, 0x08
     mov rsi, 0x06
     mov rdx, 0x07
     neg rdx
     mov rdi, 0x14
     call :batch int
     mov rbx, rax
     mov rax, 0x14
     mov rbp, rax
     mov rdi, rbp
     mov rsi, 0x06
     mov rdx, 0x07
     call :batch u8
     mov r12, rax
     mov rax, 0x14
     mov r13, rax
     mov rdi, r13
     mov rsi, 0x06
     mov rdx, 0x07
     call :batch u16
     mov r14, rax
     mov rax, 0x14
     mov r15, rax
     mov rdi, r15
     mov rsi, 0x06
     mov rdx, 0x07
     call :batch u32
     mov [rsp], rax
     mov rax, 0x07
     neg al
     mov rdi, rbp
     mov rsi, 0x06
     mov rdx, rax
     call :batch i8
     mov rbp, rax
     mov rax, 0x07
     neg ax
     mov rdi, r13
     mov rsi, 0x06
     mov rdx, rax
     call :batch i16
     mov r13, rax
     mov rax, 0x07
     neg eax
     mov rdi, r15
     mov rsi, 0x06
     mov rdx, rax
     call :batch i32
     mov rdx, rax
     mov rax, 0x00
     add rax, rbx
     add rax, r12
     add rax, r14
     mov rcx, [rsp]
     add rax, rcx
     add rax, rbp
     add rax, r13
     add rax, rdx
     add rsp, 0x08
     pop r15
     pop r14
     pop r13
     pop r12
     pop rbp
     pop rbx
     ret
batch int:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     imul rax, rsi
     mov rax, rax
     mov r8, 0x00
     add r8, rax
     mov rax, rcx
     cqo
     idiv rsi
     mov rax, rax
     add r8, rax
     mov rax, rcx
     cqo
     idiv rsi
     mov rax, rdx
     add r8, rax
     mov rax, rdi
     cqo
     idiv rsi
     mov rax, rax
     add r8, rax
     mov rax, rdi
     cqo
     idiv rsi
     mov rax, rdx
     add r8, rax
     mov rax, rcx
     and rax, rsi
     mov rax, rax
     add r8, rax
     mov rax, rcx
     or rax, rsi
     mov rax, rax
     add r8, rax
     mov rax, rcx
     xor rax, rsi
     mov rax, rax
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not rdx
     and rdx, rax
     mov rax, rdx
     add r8, rax
     mov rax, rcx
     shl rax, 0x02
     mov rax, rax
     add r8, rax
     mov rax, rcx
     sar rax, 0x02
     mov rax, rax
     add r8, rax
     mov rax, rdi
     sar rax, 0x02
     mov rax, rax
     add r8, rax
     cmp rcx, rsi
     jnle :0
     jmp :1

000: add r8, 0x01
001: mov rdx, r8
     cmp rsi, rcx
     jl :2
     jmp :3

002: add rdx, 0x02
003: cmp rcx, rsi
     jnl :4
     jmp :5

004: add rdx, 0x04
005: cmp rcx, rsi
     jle :6
     jmp :7

006: add rdx, 0x08
007: cmp rcx, rcx
     jz :8
     jmp :9

008: add rdx, 0x10
009: cmp rcx, rsi
     jnz :10
     jmp :11

010: add rdx, 0x20
011: cmp rdi, rsi
     jl :12
     jmp :13

012: add rdx, 0x40
013: mov rax, rdx
     ret
batch u8:
     mov rcx, rdi
     mov rdi, rdx
     mov rdx, rcx
     mov rax, rsi
     imul dl
     movzx eax, al
     mov r8, 0x00
     add r8, rax
     mov rax, rcx
     movzx eax, al
     div sil
     movzx eax, al
     mov r9, r8
     add r9, rax
     mov rax, rcx
     movzx eax, al
     div sil
     movsx edx, ah
     movzx eax, dl
     add r9, rax
     mov r8, rsi
     mov rax, rdi
     movzx eax, al
     div r8b
     movzx eax, al
     add r9, rax
     mov r8, rsi
     mov rax, rdi
     movzx eax, al
     div r8b
     movsx edx, ah
     movzx eax, dl
     add r9, rax
     mov rdx, rcx
     and dl, sil
     movzx eax, dl
     mov r8, r9
     add r8, rax
     mov rdx, rcx
     or dl, sil
     movzx eax, dl
     add r8, rax
     mov rdx, rcx
     xor dl, sil
     movzx eax, dl
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not dl
     and dl, al
     movzx eax, dl
     add r8, rax
     mov rax, rcx
     shl al, 0x02
     movzx eax, al
     add r8, rax
     mov rax, rcx
     shr al, 0x02
     movzx eax, al
     add r8, rax
     mov rax, rdi
     shr al, 0x02
     movzx eax, al
     add r8, rax
     cmp cl, sil
     jnbe :0
     jmp :1

000: add r8, 0x01
001: cmp sil, cl
     jb :2
     jmp :3

002: add r8, 0x02
003: cmp cl, sil
     jnb :4
     jmp :5

004: add r8, 0x04
005: cmp cl, sil
     jbe :6
     jmp :7

006: add r8, 0x08
007: cmp cl, cl
     jz :8
     jmp :9

008: add r8, 0x10
009: cmp cl, sil
     jnz :10
     jmp :11

010: add r8, 0x20
011: cmp dil, sil
     jb :12
     jmp :13

012: add r8, 0x40
013: mov rax, r8
     ret
batch u16:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     imul ax, si
     movzx eax, ax
     mov r8, 0x00
     add r8, rax
     mov rax, rcx
     xor rdx, rdx
     div si
     movzx eax, ax
     add r8, rax
     mov rax, rcx
     xor rdx, rdx
     div si
     movzx eax, dx
     add r8, rax
     mov rax, rdi
     xor rdx, rdx
     div si
     movzx eax, ax
     add r8, rax
     mov rax, rdi
     xor rdx, rdx
     div si
     movzx eax, dx
     add r8, rax
     mov rax, rcx
     and ax, si
     movzx eax, ax
     add r8, rax
     mov rax, rcx
     or ax, si
     movzx eax, ax
     add r8, rax
     mov rax, rcx
     xor ax, si
     movzx eax, ax
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not dx
     and dx, ax
     movzx eax, dx
     add r8, rax
     mov rax, rcx
     shl ax, 0x02
     movzx eax, ax
     add r8, rax
     mov rax, rcx
     shr ax, 0x02
     movzx eax, ax
     add r8, rax
     mov rax, rdi
     shr ax, 0x02
     movzx eax, ax
     add r8, rax
     cmp cx, si
     jnbe :0
     jmp :1

000: add r8, 0x01
001: mov rdx, r8
     cmp si, cx
     jb :2
     jmp :3

002: add rdx, 0x02
003: cmp cx, si
     jnb :4
     jmp :5

004: add rdx, 0x04
005: cmp cx, si
     jbe :6
     jmp :7

006: add rdx, 0x08
007: cmp cx, cx
     jz :8
     jmp :9

008: add rdx, 0x10
009: cmp cx, si
     jnz :10
     jmp :11

010: add rdx, 0x20
011: cmp di, si
     jb :12
     jmp :13

012: add rdx, 0x40
013: mov rax, rdx
     ret
batch u32:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     imul eax, esi
     mov eax, eax
     mov r8, 0x00
     add r8, rax
     mov rax, rcx
     xor rdx, rdx
     div esi
     mov eax, eax
     add r8, rax
     mov rax, rcx
     xor rdx, rdx
     div esi
     mov eax, edx
     add r8, rax
     mov rax, rdi
     xor rdx, rdx
     div esi
     mov eax, eax
     add r8, rax
     mov rax, rdi
     xor rdx, rdx
     div esi
     mov eax, edx
     add r8, rax
     mov rax, rcx
     and eax, esi
     mov eax, eax
     add r8, rax
     mov rax, rcx
     or eax, esi
     mov eax, eax
     add r8, rax
     mov rax, rcx
     xor eax, esi
     mov eax, eax
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not edx
     and edx, eax
     mov eax, edx
     add r8, rax
     mov rax, rcx
     shl eax, 0x02
     mov eax, eax
     add r8, rax
     mov rax, rcx
     shr eax, 0x02
     mov eax, eax
     add r8, rax
     mov rax, rdi
     shr eax, 0x02
     mov eax, eax
     add r8, rax
     cmp ecx, esi
     jnbe :0
     jmp :1

000: add r8, 0x01
001: mov rdx, r8
     cmp esi, ecx
     jb :2
     jmp :3

002: add rdx, 0x02
003: cmp ecx, esi
     jnb :4
     jmp :5

004: add rdx, 0x04
005: cmp ecx, esi
     jbe :6
     jmp :7

006: add rdx, 0x08
007: cmp ecx, ecx
     jz :8
     jmp :9

008: add rdx, 0x10
009: cmp ecx, esi
     jnz :10
     jmp :11

010: add rdx, 0x20
011: cmp edi, esi
     jb :12
     jmp :13

012: add rdx, 0x40
013: mov rax, rdx
     ret
batch i8:
     mov rcx, rdi
     mov rdi, rdx
     mov rdx, rcx
     mov rax, rsi
     imul dl
     movsx rax, al
     mov r8, 0x00
     add r8, rax
     mov rax, rcx
     cbw
     idiv sil
     movsx rax, al
     mov r9, r8
     add r9, rax
     mov rax, rcx
     cbw
     idiv sil
     movzx edx, ah
     movsx rax, dl
     add r9, rax
     mov r8, rsi
     mov rax, rdi
     cbw
     idiv r8b
     movsx rax, al
     add r9, rax
     mov r8, rsi
     mov rax, rdi
     cbw
     idiv r8b
     movzx edx, ah
     movsx rax, dl
     add r9, rax
     mov rdx, rcx
     and dl, sil
     movsx rax, dl
     mov r8, r9
     add r8, rax
     mov rdx, rcx
     or dl, sil
     movsx rax, dl
     add r8, rax
     mov rdx, rcx
     xor dl, sil
     movsx rax, dl
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not dl
     and dl, al
     movsx rax, dl
     add r8, rax
     mov rax, rcx
     shl al, 0x02
     movsx rax, al
     add r8, rax
     mov rax, rcx
     sar al, 0x02
     movsx rax, al
     add r8, rax
     mov rax, rdi
     sar al, 0x02
     movsx rax, al
     add r8, rax
     cmp cl, sil
     jnle :0
     jmp :1

000: add r8, 0x01
001: cmp sil, cl
     jl :2
     jmp :3

002: add r8, 0x02
003: cmp cl, sil
     jnl :4
     jmp :5

004: add r8, 0x04
005: cmp cl, sil
     jle :6
     jmp :7

006: add r8, 0x08
007: cmp cl, cl
     jz :8
     jmp :9

008: add r8, 0x10
009: cmp cl, sil
     jnz :10
     jmp :11

010: add r8, 0x20
011: cmp dil, sil
     jl :12
     jmp :13

012: add r8, 0x40
013: mov rax, r8
     ret
batch i16:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     imul ax, si
     movsx rax, ax
     mov r8, 0x00
     add r8, rax
     mov rax, rcx
     cwd
     idiv si
     movsx rax, ax
     add r8, rax
     mov rax, rcx
     cwd
     idiv si
     movsx rax, dx
     add r8, rax
     mov rax, rdi
     cwd
     idiv si
     movsx rax, ax
     add r8, rax
     mov rax, rdi
     cwd
     idiv si
     movsx rax, dx
     add r8, rax
     mov rax, rcx
     and ax, si
     movsx rax, ax
     add r8, rax
     mov rax, rcx
     or ax, si
     movsx rax, ax
     add r8, rax
     mov rax, rcx
     xor ax, si
     movsx rax, ax
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not dx
     and dx, ax
     movsx rax, dx
     add r8, rax
     mov rax, rcx
     shl ax, 0x02
     movsx rax, ax
     add r8, rax
     mov rax, rcx
     sar ax, 0x02
     movsx rax, ax
     add r8, rax
     mov rax, rdi
     sar ax, 0x02
     movsx rax, ax
     add r8, rax
     cmp cx, si
     jnle :0
     jmp :1

000: add r8, 0x01
001: mov rdx, r8
     cmp si, cx
     jl :2
     jmp :3

002: add rdx, 0x02
003: cmp cx, si
     jnl :4
     jmp :5

004: add rdx, 0x04
005: cmp cx, si
     jle :6
     jmp :7

006: add rdx, 0x08
007: cmp cx, cx
     jz :8
     jmp :9

008: add rdx, 0x10
009: cmp cx, si
     jnz :10
     jmp :11

010: add rdx, 0x20
011: cmp di, si
     jl :12
     jmp :13

012: add rdx, 0x40
013: mov rax, rdx
     ret
batch i32:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     imul eax, esi
     movsxd rax, eax
     mov r8, 0x00
     add r8, rax
     mov rax, rcx
     cdq
     idiv esi
     movsxd rax, eax
     add r8, rax
     mov rax, rcx
     cdq
     idiv esi
     movsxd rax, edx
     add r8, rax
     mov rax, rdi
     cdq
     idiv esi
     movsxd rax, eax
     add r8, rax
     mov rax, rdi
     cdq
     idiv esi
     movsxd rax, edx
     add r8, rax
     mov rax, rcx
     and eax, esi
     movsxd rax, eax
     add r8, rax
     mov rax, rcx
     or eax, esi
     movsxd rax, eax
     add r8, rax
     mov rax, rcx
     xor eax, esi
     movsxd rax, eax
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not edx
     and edx, eax
     movsxd rax, edx
     add r8, rax
     mov rax, rcx
     shl eax, 0x02
     movsxd rax, eax
     add r8, rax
     mov rax, rcx
     sar eax, 0x02
     movsxd rax, eax
     add r8, rax
     mov rax, rdi
     sar eax, 0x02
     movsxd rax, eax
     add r8, rax
     cmp ecx, esi
     jnle :0
     jmp :1

000: add r8, 0x01
001: mov rdx, r8
     cmp esi, ecx
     jl :2
     jmp :3

002: add rdx, 0x02
003: cmp ecx, esi
     jnl :4
     jmp :5

004: add rdx, 0x04
005: cmp ecx, esi
     jle :6
     jmp :7

006: add rdx, 0x08
007: cmp ecx, ecx
     jz :8
     jmp :9

008: add rdx, 0x10
009: cmp ecx, esi
     jnz :10
     jmp :11

010: add rdx, 0x20
011: cmp edi, esi
     jl :12
     jmp :13

012: add rdx, 0x40
013: mov rax, rdx
     ret
=========== OPT LEVEL: mininal ===========
batch:
main:
     push rbx
     push rbp
     push r12
     push r13
     push r14
     push r15
     sub rsp, 0x08
     mov rdi, 0x14
     mov rsi, 0x06
     mov rdx, 0xFFFFFFFFFFFFFFF9
     call :batch int
     mov rbx, rax
     mov rdx, 0x07
     mov rdi, 0x14
     mov rsi, 0x06
     call :batch u8
     mov rbp, rax
     mov rdx, 0x07
     mov rdi, 0x14
     mov rsi, 0x06
     call :batch u16
     mov r12, rax
     mov rdx, 0x07
     mov rdi, 0x14
     mov rsi, 0x06
     call :batch u32
     mov r13, rax
     mov rdx, 0xFFFFFFFFFFFFFFF9
     mov rdi, 0x14
     mov rsi, 0x06
     call :batch i8
     mov r14, rax
     mov rdx, 0xFFFFFFFFFFFFFFF9
     mov rdi, 0x14
     mov rsi, 0x06
     call :batch i16
     mov r15, rax
     mov rdx, 0xFFFFFFFFFFFFFFF9
     mov rdi, 0x14
     mov rsi, 0x06
     call :batch i32
     mov rdx, rax
     mov rax, 0x00
     add rax, rbx
     add rax, rbp
     add rax, r12
     add rax, r13
     add rax, r14
     add rax, r15
     add rax, rdx
     add rsp, 0x08
     pop r15
     pop r14
     pop r13
     pop r12
     pop rbp
     pop rbx
     ret
batch int:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     imul rax, rsi
     mov r8, 0x00
     add r8, rax
     mov rax, rcx
     cqo
     idiv rsi
     add r8, rax
     mov rax, rcx
     cqo
     idiv rsi
     add r8, rdx
     mov rax, rdi
     cqo
     idiv rsi
     add r8, rax
     mov rax, rdi
     cqo
     idiv rsi
     add r8, rdx
     mov rax, rcx
     and rax, rsi
     add r8, rax
     mov rax, rcx
     or rax, rsi
     add r8, rax
     mov rax, rcx
     xor rax, rsi
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not rdx
     and rdx, rax
     add r8, rdx
     mov rax, rcx
     shl rax, 0x02
     add r8, rax
     mov rax, rcx
     sar rax, 0x02
     add r8, rax
     mov rax, rdi
     sar rax, 0x02
     add r8, rax
     cmp rcx, rsi
     jnle :0
     jmp :1

000: add r8, 0x01
001: cmp rsi, rcx
     jl :2
     jmp :3

002: add r8, 0x02
003: mov rdx, r8
     cmp rcx, rsi
     jnl :4
     jmp :5

004: add rdx, 0x04
005: cmp rcx, rsi
     jle :6
     jmp :7

006: add rdx, 0x08
007: mov rax, 0x01
     test rax, rax
     jnz :8
     jmp :9

008: add rdx, 0x10
009: cmp rcx, rsi
     jnz :10
     jmp :11

010: add rdx, 0x20
011: cmp rdi, rsi
     jl :12
     jmp :13

012: add rdx, 0x40
013: mov rax, rdx
     ret
batch u8:
     mov rcx, rdi
     mov rdi, rdx
     mov rdx, rcx
     mov rax, rsi
     imul dl
     movzx eax, al
     mov r8, 0x00
     add r8, rax
     mov rax, rcx
     movzx eax, al
     div sil
     movzx eax, al
     mov r9, r8
     add r9, rax
     mov rax, rcx
     movzx eax, al
     div sil
     movsx edx, ah
     movzx eax, dl
     add r9, rax
     mov r8, rsi
     mov rax, rdi
     movzx eax, al
     div r8b
     movzx eax, al
     add r9, rax
     mov r8, rsi
     mov rax, rdi
     movzx eax, al
     div r8b
     movsx edx, ah
     movzx eax, dl
     add r9, rax
     mov rdx, rcx
     and dl, sil
     movzx eax, dl
     mov r8, r9
     add r8, rax
     mov rdx, rcx
     or dl, sil
     movzx eax, dl
     add r8, rax
     mov rdx, rcx
     xor dl, sil
     movzx eax, dl
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not dl
     and dl, al
     movzx eax, dl
     add r8, rax
     mov rax, rcx
     shl al, 0x02
     movzx eax, al
     add r8, rax
     mov rax, rcx
     shr al, 0x02
     movzx eax, al
     add r8, rax
     mov rax, rdi
     shr al, 0x02
     movzx eax, al
     add r8, rax
     cmp cl, sil
     jnbe :0
     jmp :1

000: add r8, 0x01
001: cmp sil, cl
     jb :2
     jmp :3

002: add r8, 0x02
003: cmp cl, sil
     jnb :4
     jmp :5

004: add r8, 0x04
005: cmp cl, sil
     jbe :6
     jmp :7

006: add r8, 0x08
007: mov rax, 0x01
     test rax, rax
     jnz :8
     jmp :9

008: add r8, 0x10
009: cmp cl, sil
     jnz :10
     jmp :11

010: add r8, 0x20
011: cmp dil, sil
     jb :12
     jmp :13

012: add r8, 0x40
013: mov rax, r8
     ret
batch u16:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     imul ax, si
     movzx eax, ax
     mov r8, 0x00
     add r8, rax
     mov rax, rcx
     xor rdx, rdx
     div si
     movzx eax, ax
     add r8, rax
     mov rax, rcx
     xor rdx, rdx
     div si
     movzx eax, dx
     add r8, rax
     mov rax, rdi
     xor rdx, rdx
     div si
     movzx eax, ax
     add r8, rax
     mov rax, rdi
     xor rdx, rdx
     div si
     movzx eax, dx
     add r8, rax
     mov rax, rcx
     and ax, si
     movzx eax, ax
     add r8, rax
     mov rax, rcx
     or ax, si
     movzx eax, ax
     add r8, rax
     mov rax, rcx
     xor ax, si
     movzx eax, ax
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not dx
     and dx, ax
     movzx eax, dx
     add r8, rax
     mov rax, rcx
     shl ax, 0x02
     movzx eax, ax
     add r8, rax
     mov rax, rcx
     shr ax, 0x02
     movzx eax, ax
     add r8, rax
     mov rax, rdi
     shr ax, 0x02
     movzx eax, ax
     add r8, rax
     cmp cx, si
     jnbe :0
     jmp :1

000: add r8, 0x01
001: mov rdx, r8
     cmp si, cx
     jb :2
     jmp :3

002: add rdx, 0x02
003: cmp cx, si
     jnb :4
     jmp :5

004: add rdx, 0x04
005: cmp cx, si
     jbe :6
     jmp :7

006: add rdx, 0x08
007: mov rax, 0x01
     test rax, rax
     jnz :8
     jmp :9

008: add rdx, 0x10
009: cmp cx, si
     jnz :10
     jmp :11

010: add rdx, 0x20
011: cmp di, si
     jb :12
     jmp :13

012: add rdx, 0x40
013: mov rax, rdx
     ret
batch u32:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     imul eax, esi
     mov eax, eax
     mov r8, 0x00
     add r8, rax
     mov rax, rcx
     xor rdx, rdx
     div esi
     mov eax, eax
     add r8, rax
     mov rax, rcx
     xor rdx, rdx
     div esi
     mov eax, edx
     add r8, rax
     mov rax, rdi
     xor rdx, rdx
     div esi
     mov eax, eax
     add r8, rax
     mov rax, rdi
     xor rdx, rdx
     div esi
     mov eax, edx
     add r8, rax
     mov rax, rcx
     and eax, esi
     mov eax, eax
     add r8, rax
     mov rax, rcx
     or eax, esi
     mov eax, eax
     add r8, rax
     mov rax, rcx
     xor eax, esi
     mov eax, eax
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not edx
     and edx, eax
     mov eax, edx
     add r8, rax
     mov rax, rcx
     shl eax, 0x02
     mov eax, eax
     add r8, rax
     mov rax, rcx
     shr eax, 0x02
     mov eax, eax
     add r8, rax
     mov rax, rdi
     shr eax, 0x02
     mov eax, eax
     add r8, rax
     cmp ecx, esi
     jnbe :0
     jmp :1

000: add r8, 0x01
001: mov rdx, r8
     cmp esi, ecx
     jb :2
     jmp :3

002: add rdx, 0x02
003: cmp ecx, esi
     jnb :4
     jmp :5

004: add rdx, 0x04
005: cmp ecx, esi
     jbe :6
     jmp :7

006: add rdx, 0x08
007: mov rax, 0x01
     test rax, rax
     jnz :8
     jmp :9

008: add rdx, 0x10
009: cmp ecx, esi
     jnz :10
     jmp :11

010: add rdx, 0x20
011: cmp edi, esi
     jb :12
     jmp :13

012: add rdx, 0x40
013: mov rax, rdx
     ret
batch i8:
     mov rcx, rdi
     mov rdi, rdx
     mov rdx, rcx
     mov rax, rsi
     imul dl
     movsx rax, al
     mov r8, 0x00
     add r8, rax
     mov rax, rcx
     cbw
     idiv sil
     movsx rax, al
     mov r9, r8
     add r9, rax
     mov rax, rcx
     cbw
     idiv sil
     movzx edx, ah
     movsx rax, dl
     add r9, rax
     mov r8, rsi
     mov rax, rdi
     cbw
     idiv r8b
     movsx rax, al
     add r9, rax
     mov r8, rsi
     mov rax, rdi
     cbw
     idiv r8b
     movzx edx, ah
     movsx rax, dl
     add r9, rax
     mov rdx, rcx
     and dl, sil
     movsx rax, dl
     mov r8, r9
     add r8, rax
     mov rdx, rcx
     or dl, sil
     movsx rax, dl
     add r8, rax
     mov rdx, rcx
     xor dl, sil
     movsx rax, dl
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not dl
     and dl, al
     movsx rax, dl
     add r8, rax
     mov rax, rcx
     shl al, 0x02
     movsx rax, al
     add r8, rax
     mov rax, rcx
     sar al, 0x02
     movsx rax, al
     add r8, rax
     mov rax, rdi
     sar al, 0x02
     movsx rax, al
     add r8, rax
     cmp cl, sil
     jnle :0
     jmp :1

000: add r8, 0x01
001: cmp sil, cl
     jl :2
     jmp :3

002: add r8, 0x02
003: cmp cl, sil
     jnl :4
     jmp :5

004: add r8, 0x04
005: cmp cl, sil
     jle :6
     jmp :7

006: add r8, 0x08
007: mov rax, 0x01
     test rax, rax
     jnz :8
     jmp :9

008: add r8, 0x10
009: cmp cl, sil
     jnz :10
     jmp :11

010: add r8, 0x20
011: cmp dil, sil
     jl :12
     jmp :13

012: add r8, 0x40
013: mov rax, r8
     ret
batch i16:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     imul ax, si
     movsx rax, ax
     mov r8, 0x00
     add r8, rax
     mov rax, rcx
     cwd
     idiv si
     movsx rax, ax
     add r8, rax
     mov rax, rcx
     cwd
     idiv si
     movsx rax, dx
     add r8, rax
     mov rax, rdi
     cwd
     idiv si
     movsx rax, ax
     add r8, rax
     mov rax, rdi
     cwd
     idiv si
     movsx rax, dx
     add r8, rax
     mov rax, rcx
     and ax, si
     movsx rax, ax
     add r8, rax
     mov rax, rcx
     or ax, si
     movsx rax, ax
     add r8, rax
     mov rax, rcx
     xor ax, si
     movsx rax, ax
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not dx
     and dx, ax
     movsx rax, dx
     add r8, rax
     mov rax, rcx
     shl ax, 0x02
     movsx rax, ax
     add r8, rax
     mov rax, rcx
     sar ax, 0x02
     movsx rax, ax
     add r8, rax
     mov rax, rdi
     sar ax, 0x02
     movsx rax, ax
     add r8, rax
     cmp cx, si
     jnle :0
     jmp :1

000: add r8, 0x01
001: mov rdx, r8
     cmp si, cx
     jl :2
     jmp :3

002: add rdx, 0x02
003: cmp cx, si
     jnl :4
     jmp :5

004: add rdx, 0x04
005: cmp cx, si
     jle :6
     jmp :7

006: add rdx, 0x08
007: mov rax, 0x01
     test rax, rax
     jnz :8
     jmp :9

008: add rdx, 0x10
009: cmp cx, si
     jnz :10
     jmp :11

010: add rdx, 0x20
011: cmp di, si
     jl :12
     jmp :13

012: add rdx, 0x40
013: mov rax, rdx
     ret
batch i32:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     imul eax, esi
     movsxd rax, eax
     mov r8, 0x00
     add r8, rax
     mov rax, rcx
     cdq
     idiv esi
     movsxd rax, eax
     add r8, rax
     mov rax, rcx
     cdq
     idiv esi
     movsxd rax, edx
     add r8, rax
     mov rax, rdi
     cdq
     idiv esi
     movsxd rax, eax
     add r8, rax
     mov rax, rdi
     cdq
     idiv esi
     movsxd rax, edx
     add r8, rax
     mov rax, rcx
     and eax, esi
     movsxd rax, eax
     add r8, rax
     mov rax, rcx
     or eax, esi
     movsxd rax, eax
     add r8, rax
     mov rax, rcx
     xor eax, esi
     movsxd rax, eax
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not edx
     and edx, eax
     movsxd rax, edx
     add r8, rax
     mov rax, rcx
     shl eax, 0x02
     movsxd rax, eax
     add r8, rax
     mov rax, rcx
     sar eax, 0x02
     movsxd rax, eax
     add r8, rax
     mov rax, rdi
     sar eax, 0x02
     movsxd rax, eax
     add r8, rax
     cmp ecx, esi
     jnle :0
     jmp :1

000: add r8, 0x01
001: mov rdx, r8
     cmp esi, ecx
     jl :2
     jmp :3

002: add rdx, 0x02
003: cmp ecx, esi
     jnl :4
     jmp :5

004: add rdx, 0x04
005: cmp ecx, esi
     jle :6
     jmp :7

006: add rdx, 0x08
007: mov rax, 0x01
     test rax, rax
     jnz :8
     jmp :9

008: add rdx, 0x10
009: cmp ecx, esi
     jnz :10
     jmp :11

010: add rdx, 0x20
011: cmp edi, esi
     jl :12
     jmp :13

012: add rdx, 0x40
013: mov rax, rdx
     ret
=========== OPT LEVEL: moderate ===========
batch:
main:
     push rbx
     push rbp
     push r12
     push r13
     push r14
     push r15
     sub rsp, 0x08
     mov rdi, 0x14
     mov rsi, 0x06
     mov rdx, 0xFFFFFFFFFFFFFFF9
     call :batch int
     mov rbx, rax
     mov rdx, 0x07
     mov rdi, 0x14
     mov rsi, 0x06
     call :batch u8
     mov rbp, rax
     mov rdx, 0x07
     mov rdi, 0x14
     mov rsi, 0x06
     call :batch u16
     mov r12, rax
     mov rdx, 0x07
     mov rdi, 0x14
     mov rsi, 0x06
     call :batch u32
     mov r13, rax
     mov rdx, 0xFFFFFFFFFFFFFFF9
     mov rdi, 0x14
     mov rsi, 0x06
     call :batch i8
     mov r14, rax
     mov rdx, 0xFFFFFFFFFFFFFFF9
     mov rdi, 0x14
     mov rsi, 0x06
     call :batch i16
     mov r15, rax
     mov rdx, 0xFFFFFFFFFFFFFFF9
     mov rdi, 0x14
     mov rsi, 0x06
     call :batch i32
     mov rdx, rbx
     add rdx, rbp
     add rdx, r12
     add rdx, r13
     add rdx, r14
     add rdx, r15
     add rdx, rax
     mov rax, rdx
     add rsp, 0x08
     pop r15
     pop r14
     pop r13
     pop r12
     pop rbp
     pop rbx
     ret
batch int:
     mov rcx, rdi
     mov rdi, rdx
     mov r8, rcx
     imul r8, rsi
     mov rax, rcx
     cqo
     idiv rsi
     add r8, rax
     mov rax, rcx
     cqo
     idiv rsi
     add r8, rdx
     mov rax, rdi
     cqo
     idiv rsi
     add r8, rax
     mov rax, rdi
     cqo
     idiv rsi
     add r8, rdx
     mov rax, rcx
     and rax, rsi
     add r8, rax
     mov rax, rcx
     or rax, rsi
     add r8, rax
     mov rax, rcx
     xor rax, rsi
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not rdx
     and rdx, rax
     add r8, rdx
     mov rax, rcx
     shl rax, 0x02
     add r8, rax
     mov rax, rcx
     sar rax, 0x02
     add r8, rax
     mov rax, rdi
     sar rax, 0x02
     add r8, rax
     cmp rcx, rsi
     jnle :0
     jmp :1

000: add r8, 0x01
001: cmp rsi, rcx
     jl :2
     jmp :3

002: add r8, 0x02
003: mov rdx, r8
     cmp rcx, rsi
     jnl :4
     jmp :5

004: add rdx, 0x04
005: cmp rcx, rsi
     jle :6
     jmp :7

006: add rdx, 0x08
007: cmp rcx, rsi
     jnz :8
     add rdx, 0x10
     jmp :9

008: add rdx, 0x30
009: cmp rdi, rsi
     jl :10
     jmp :11

010: add rdx, 0x40
011: mov rax, rdx
     ret
batch u8:
     mov rcx, rdi
     mov rdi, rdx
     mov rdx, rcx
     mov rax, rsi
     imul dl
     movzx r8d, al
     mov rax, rcx
     movzx eax, al
     div sil
     movzx eax, al
     mov r9, r8
     add r9, rax
     mov rax, rcx
     movzx eax, al
     div sil
     movsx edx, ah
     movzx eax, dl
     add r9, rax
     mov r8, rsi
     mov rax, rdi
     movzx eax, al
     div r8b
     movzx eax, al
     add r9, rax
     mov r8, rsi
     mov rax, rdi
     movzx eax, al
     div r8b
     movsx edx, ah
     movzx eax, dl
     add r9, rax
     mov rdx, rcx
     and dl, sil
     movzx eax, dl
     mov r8, r9
     add r8, rax
     mov rdx, rcx
     or dl, sil
     movzx eax, dl
     add r8, rax
     mov rdx, rcx
     xor dl, sil
     movzx eax, dl
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not dl
     and dl, al
     movzx eax, dl
     add r8, rax
     mov rax, rcx
     shl al, 0x02
     movzx eax, al
     add r8, rax
     mov rax, rcx
     shr al, 0x02
     movzx eax, al
     add r8, rax
     mov rax, rdi
     shr al, 0x02
     movzx eax, al
     add r8, rax
     cmp cl, sil
     jnbe :0
     jmp :1

000: add r8, 0x01
001: cmp sil, cl
     jb :2
     jmp :3

002: add r8, 0x02
003: cmp cl, sil
     jnb :4
     jmp :5

004: add r8, 0x04
005: cmp cl, sil
     jbe :6
     jmp :7

006: add r8, 0x08
007: cmp cl, sil
     jnz :8
     add r8, 0x10
     jmp :9

008: add r8, 0x30
009: cmp dil, sil
     jb :10
     jmp :11

010: add r8, 0x40
011: mov rax, r8
     ret
batch u16:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     imul ax, si
     movzx r8d, ax
     mov rax, rcx
     xor rdx, rdx
     div si
     movzx eax, ax
     add r8, rax
     mov rax, rcx
     xor rdx, rdx
     div si
     movzx eax, dx
     add r8, rax
     mov rax, rdi
     xor rdx, rdx
     div si
     movzx eax, ax
     add r8, rax
     mov rax, rdi
     xor rdx, rdx
     div si
     movzx eax, dx
     add r8, rax
     mov rax, rcx
     and ax, si
     movzx eax, ax
     add r8, rax
     mov rax, rcx
     or ax, si
     movzx eax, ax
     add r8, rax
     mov rax, rcx
     xor ax, si
     movzx eax, ax
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not dx
     and dx, ax
     movzx eax, dx
     add r8, rax
     mov rax, rcx
     shl ax, 0x02
     movzx eax, ax
     add r8, rax
     mov rax, rcx
     shr ax, 0x02
     movzx eax, ax
     add r8, rax
     mov rax, rdi
     shr ax, 0x02
     movzx eax, ax
     add r8, rax
     cmp cx, si
     jnbe :0
     jmp :1

000: add r8, 0x01
001: mov rdx, r8
     cmp si, cx
     jb :2
     jmp :3

002: add rdx, 0x02
003: cmp cx, si
     jnb :4
     jmp :5

004: add rdx, 0x04
005: cmp cx, si
     jbe :6
     jmp :7

006: add rdx, 0x08
007: cmp cx, si
     jnz :8
     add rdx, 0x10
     jmp :9

008: add rdx, 0x30
009: cmp di, si
     jb :10
     jmp :11

010: add rdx, 0x40
011: mov rax, rdx
     ret
batch u32:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     imul eax, esi
     mov r8d, eax
     mov rax, rcx
     xor rdx, rdx
     div esi
     mov eax, eax
     add r8, rax
     mov rax, rcx
     xor rdx, rdx
     div esi
     mov eax, edx
     add r8, rax
     mov rax, rdi
     xor rdx, rdx
     div esi
     mov eax, eax
     add r8, rax
     mov rax, rdi
     xor rdx, rdx
     div esi
     mov eax, edx
     add r8, rax
     mov rax, rcx
     and eax, esi
     mov eax, eax
     add r8, rax
     mov rax, rcx
     or eax, esi
     mov eax, eax
     add r8, rax
     mov rax, rcx
     xor eax, esi
     mov eax, eax
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not edx
     and edx, eax
     mov eax, edx
     add r8, rax
     mov rax, rcx
     shl eax, 0x02
     mov eax, eax
     add r8, rax
     mov rax, rcx
     shr eax, 0x02
     mov eax, eax
     add r8, rax
     mov rax, rdi
     shr eax, 0x02
     mov eax, eax
     add r8, rax
     cmp ecx, esi
     jnbe :0
     jmp :1

000: add r8, 0x01
001: mov rdx, r8
     cmp esi, ecx
     jb :2
     jmp :3

002: add rdx, 0x02
003: cmp ecx, esi
     jnb :4
     jmp :5

004: add rdx, 0x04
005: cmp ecx, esi
     jbe :6
     jmp :7

006: add rdx, 0x08
007: cmp ecx, esi
     jnz :8
     add rdx, 0x10
     jmp :9

008: add rdx, 0x30
009: cmp edi, esi
     jb :10
     jmp :11

010: add rdx, 0x40
011: mov rax, rdx
     ret
batch i8:
     mov rcx, rdi
     mov rdi, rdx
     mov rdx, rcx
     mov rax, rsi
     imul dl
     movsx r8, al
     mov rax, rcx
     cbw
     idiv sil
     movsx rax, al
     mov r9, r8
     add r9, rax
     mov rax, rcx
     cbw
     idiv sil
     movzx edx, ah
     movsx rax, dl
     add r9, rax
     mov r8, rsi
     mov rax, rdi
     cbw
     idiv r8b
     movsx rax, al
     add r9, rax
     mov r8, rsi
     mov rax, rdi
     cbw
     idiv r8b
     movzx edx, ah
     movsx rax, dl
     add r9, rax
     mov rdx, rcx
     and dl, sil
     movsx rax, dl
     mov r8, r9
     add r8, rax
     mov rdx, rcx
     or dl, sil
     movsx rax, dl
     add r8, rax
     mov rdx, rcx
     xor dl, sil
     movsx rax, dl
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not dl
     and dl, al
     movsx rax, dl
     add r8, rax
     mov rax, rcx
     shl al, 0x02
     movsx rax, al
     add r8, rax
     mov rax, rcx
     sar al, 0x02
     movsx rax, al
     add r8, rax
     mov rax, rdi
     sar al, 0x02
     movsx rax, al
     add r8, rax
     cmp cl, sil
     jnle :0
     jmp :1

000: add r8, 0x01
001: cmp sil, cl
     jl :2
     jmp :3

002: add r8, 0x02
003: cmp cl, sil
     jnl :4
     jmp :5

004: add r8, 0x04
005: cmp cl, sil
     jle :6
     jmp :7

006: add r8, 0x08
007: cmp cl, sil
     jnz :8
     add r8, 0x10
     jmp :9

008: add r8, 0x30
009: cmp dil, sil
     jl :10
     jmp :11

010: add r8, 0x40
011: mov rax, r8
     ret
batch i16:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     imul ax, si
     movsx r8, ax
     mov rax, rcx
     cwd
     idiv si
     movsx rax, ax
     add r8, rax
     mov rax, rcx
     cwd
     idiv si
     movsx rax, dx
     add r8, rax
     mov rax, rdi
     cwd
     idiv si
     movsx rax, ax
     add r8, rax
     mov rax, rdi
     cwd
     idiv si
     movsx rax, dx
     add r8, rax
     mov rax, rcx
     and ax, si
     movsx rax, ax
     add r8, rax
     mov rax, rcx
     or ax, si
     movsx rax, ax
     add r8, rax
     mov rax, rcx
     xor ax, si
     movsx rax, ax
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not dx
     and dx, ax
     movsx rax, dx
     add r8, rax
     mov rax, rcx
     shl ax, 0x02
     movsx rax, ax
     add r8, rax
     mov rax, rcx
     sar ax, 0x02
     movsx rax, ax
     add r8, rax
     mov rax, rdi
     sar ax, 0x02
     movsx rax, ax
     add r8, rax
     cmp cx, si
     jnle :0
     jmp :1

000: add r8, 0x01
001: mov rdx, r8
     cmp si, cx
     jl :2
     jmp :3

002: add rdx, 0x02
003: cmp cx, si
     jnl :4
     jmp :5

004: add rdx, 0x04
005: cmp cx, si
     jle :6
     jmp :7

006: add rdx, 0x08
007: cmp cx, si
     jnz :8
     add rdx, 0x10
     jmp :9

008: add rdx, 0x30
009: cmp di, si
     jl :10
     jmp :11

010: add rdx, 0x40
011: mov rax, rdx
     ret
batch i32:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     imul eax, esi
     movsxd r8, eax
     mov rax, rcx
     cdq
     idiv esi
     movsxd rax, eax
     add r8, rax
     mov rax, rcx
     cdq
     idiv esi
     movsxd rax, edx
     add r8, rax
     mov rax, rdi
     cdq
     idiv esi
     movsxd rax, eax
     add r8, rax
     mov rax, rdi
     cdq
     idiv esi
     movsxd rax, edx
     add r8, rax
     mov rax, rcx
     and eax, esi
     movsxd rax, eax
     add r8, rax
     mov rax, rcx
     or eax, esi
     movsxd rax, eax
     add r8, rax
     mov rax, rcx
     xor eax, esi
     movsxd rax, eax
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not edx
     and edx, eax
     movsxd rax, edx
     add r8, rax
     mov rax, rcx
     shl eax, 0x02
     movsxd rax, eax
     add r8, rax
     mov rax, rcx
     sar eax, 0x02
     movsxd rax, eax
     add r8, rax
     mov rax, rdi
     sar eax, 0x02
     movsxd rax, eax
     add r8, rax
     cmp ecx, esi
     jnle :0
     jmp :1

000: add r8, 0x01
001: mov rdx, r8
     cmp esi, ecx
     jl :2
     jmp :3

002: add rdx, 0x02
003: cmp ecx, esi
     jnl :4
     jmp :5

004: add rdx, 0x04
005: cmp ecx, esi
     jle :6
     jmp :7

006: add rdx, 0x08
007: cmp ecx, esi
     jnz :8
     add rdx, 0x10
     jmp :9

008: add rdx, 0x30
009: cmp edi, esi
     jl :10
     jmp :11

010: add rdx, 0x40
011: mov rax, rdx
     ret
=========== OPT LEVEL: all ===========
batch:
main:
     push rbx
     push rbp
     push r12
     push r13
     push r14
     push r15
     sub rsp, 0x08
     mov rdi, 0x14
     mov rsi, 0x06
     mov rdx, 0xFFFFFFFFFFFFFFF9
     call :batch int
     mov rbx, rax
     mov rdx, 0x07
     mov rdi, 0x14
     mov rsi, 0x06
     call :batch u8
     mov rbp, rax
     mov rdx, 0x07
     mov rdi, 0x14
     mov rsi, 0x06
     call :batch u16
     mov r12, rax
     mov rdx, 0x07
     mov rdi, 0x14
     mov rsi, 0x06
     call :batch u32
     mov r13, rax
     mov rdx, 0xFFFFFFFFFFFFFFF9
     mov rdi, 0x14
     mov rsi, 0x06
     call :batch i8
     mov r14, rax
     mov rdx, 0xFFFFFFFFFFFFFFF9
     mov rdi, 0x14
     mov rsi, 0x06
     call :batch i16
     mov r15, rax
     mov rdx, 0xFFFFFFFFFFFFFFF9
     mov rdi, 0x14
     mov rsi, 0x06
     call :batch i32
     mov rdx, rbx
     add rdx, rbp
     add rdx, r12
     add rdx, r13
     add rdx, r14
     add rdx, r15
     add rdx, rax
     mov rax, rdx
     add rsp, 0x08
     pop r15
     pop r14
     pop r13
     pop r12
     pop rbp
     pop rbx
     ret
batch int:
     mov rcx, rdi
     mov rdi, rdx
     mov r8, rcx
     imul r8, rsi
     mov rax, rcx
     cqo
     idiv rsi
     add r8, rax
     mov rax, rcx
     cqo
     idiv rsi
     add r8, rdx
     mov rax, rdi
     cqo
     idiv rsi
     add r8, rax
     mov rax, rdi
     cqo
     idiv rsi
     add r8, rdx
     mov rax, rcx
     and rax, rsi
     add r8, rax
     mov rax, rcx
     or rax, rsi
     add r8, rax
     mov rax, rcx
     xor rax, rsi
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not rdx
     and rdx, rax
     add r8, rdx
     mov rax, rcx
     shl rax, 0x02
     add r8, rax
     mov rax, rcx
     sar rax, 0x02
     add r8, rax
     mov rax, rdi
     sar rax, 0x02
     add r8, rax
     cmp rcx, rsi
     jnle :0
     jmp :1

000: add r8, 0x01
001: cmp rsi, rcx
     jl :2
     jmp :3

002: add r8, 0x02
003: mov rdx, r8
     cmp rcx, rsi
     jnl :4
     jmp :5

004: add rdx, 0x04
005: cmp rcx, rsi
     jle :6
     jmp :7

006: add rdx, 0x08
007: cmp rcx, rsi
     jnz :8
     add rdx, 0x10
     jmp :9

008: add rdx, 0x30
009: cmp rdi, rsi
     jl :10
     jmp :11

010: add rdx, 0x40
011: mov rax, rdx
     ret
batch u8:
     mov rcx, rdi
     mov rdi, rdx
     mov rdx, rcx
     mov rax, rsi
     imul dl
     movzx r8d, al
     mov rax, rcx
     movzx eax, al
     div sil
     movzx eax, al
     mov r9, r8
     add r9, rax
     mov rax, rcx
     movzx eax, al
     div sil
     movsx edx, ah
     movzx eax, dl
     add r9, rax
     mov r8, rsi
     mov rax, rdi
     movzx eax, al
     div r8b
     movzx eax, al
     add r9, rax
     mov r8, rsi
     mov rax, rdi
     movzx eax, al
     div r8b
     movsx edx, ah
     movzx eax, dl
     add r9, rax
     mov rdx, rcx
     and dl, sil
     movzx eax, dl
     mov r8, r9
     add r8, rax
     mov rdx, rcx
     or dl, sil
     movzx eax, dl
     add r8, rax
     mov rdx, rcx
     xor dl, sil
     movzx eax, dl
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not dl
     and dl, al
     movzx eax, dl
     add r8, rax
     mov rax, rcx
     shl al, 0x02
     movzx eax, al
     add r8, rax
     mov rax, rcx
     shr al, 0x02
     movzx eax, al
     add r8, rax
     mov rax, rdi
     shr al, 0x02
     movzx eax, al
     add r8, rax
     cmp cl, sil
     jnbe :0
     jmp :1

000: add r8, 0x01
001: cmp sil, cl
     jb :2
     jmp :3

002: add r8, 0x02
003: cmp cl, sil
     jnb :4
     jmp :5

004: add r8, 0x04
005: cmp cl, sil
     jbe :6
     jmp :7

006: add r8, 0x08
007: cmp cl, sil
     jnz :8
     add r8, 0x10
     jmp :9

008: add r8, 0x30
009: cmp dil, sil
     jb :10
     jmp :11

010: add r8, 0x40
011: mov rax, r8
     ret
batch u16:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     imul ax, si
     movzx r8d, ax
     mov rax, rcx
     xor rdx, rdx
     div si
     movzx eax, ax
     add r8, rax
     mov rax, rcx
     xor rdx, rdx
     div si
     movzx eax, dx
     add r8, rax
     mov rax, rdi
     xor rdx, rdx
     div si
     movzx eax, ax
     add r8, rax
     mov rax, rdi
     xor rdx, rdx
     div si
     movzx eax, dx
     add r8, rax
     mov rax, rcx
     and ax, si
     movzx eax, ax
     add r8, rax
     mov rax, rcx
     or ax, si
     movzx eax, ax
     add r8, rax
     mov rax, rcx
     xor ax, si
     movzx eax, ax
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not dx
     and dx, ax
     movzx eax, dx
     add r8, rax
     mov rax, rcx
     shl ax, 0x02
     movzx eax, ax
     add r8, rax
     mov rax, rcx
     shr ax, 0x02
     movzx eax, ax
     add r8, rax
     mov rax, rdi
     shr ax, 0x02
     movzx eax, ax
     add r8, rax
     cmp cx, si
     jnbe :0
     jmp :1

000: add r8, 0x01
001: mov rdx, r8
     cmp si, cx
     jb :2
     jmp :3

002: add rdx, 0x02
003: cmp cx, si
     jnb :4
     jmp :5

004: add rdx, 0x04
005: cmp cx, si
     jbe :6
     jmp :7

006: add rdx, 0x08
007: cmp cx, si
     jnz :8
     add rdx, 0x10
     jmp :9

008: add rdx, 0x30
009: cmp di, si
     jb :10
     jmp :11

010: add rdx, 0x40
011: mov rax, rdx
     ret
batch u32:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     imul eax, esi
     mov r8d, eax
     mov rax, rcx
     xor rdx, rdx
     div esi
     mov eax, eax
     add r8, rax
     mov rax, rcx
     xor rdx, rdx
     div esi
     mov eax, edx
     add r8, rax
     mov rax, rdi
     xor rdx, rdx
     div esi
     mov eax, eax
     add r8, rax
     mov rax, rdi
     xor rdx, rdx
     div esi
     mov eax, edx
     add r8, rax
     mov rax, rcx
     and eax, esi
     mov eax, eax
     add r8, rax
     mov rax, rcx
     or eax, esi
     mov eax, eax
     add r8, rax
     mov rax, rcx
     xor eax, esi
     mov eax, eax
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not edx
     and edx, eax
     mov eax, edx
     add r8, rax
     mov rax, rcx
     shl eax, 0x02
     mov eax, eax
     add r8, rax
     mov rax, rcx
     shr eax, 0x02
     mov eax, eax
     add r8, rax
     mov rax, rdi
     shr eax, 0x02
     mov eax, eax
     add r8, rax
     cmp ecx, esi
     jnbe :0
     jmp :1

000: add r8, 0x01
001: mov rdx, r8
     cmp esi, ecx
     jb :2
     jmp :3

002: add rdx, 0x02
003: cmp ecx, esi
     jnb :4
     jmp :5

004: add rdx, 0x04
005: cmp ecx, esi
     jbe :6
     jmp :7

006: add rdx, 0x08
007: cmp ecx, esi
     jnz :8
     add rdx, 0x10
     jmp :9

008: add rdx, 0x30
009: cmp edi, esi
     jb :10
     jmp :11

010: add rdx, 0x40
011: mov rax, rdx
     ret
batch i8:
     mov rcx, rdi
     mov rdi, rdx
     mov rdx, rcx
     mov rax, rsi
     imul dl
     movsx r8, al
     mov rax, rcx
     cbw
     idiv sil
     movsx rax, al
     mov r9, r8
     add r9, rax
     mov rax, rcx
     cbw
     idiv sil
     movzx edx, ah
     movsx rax, dl
     add r9, rax
     mov r8, rsi
     mov rax, rdi
     cbw
     idiv r8b
     movsx rax, al
     add r9, rax
     mov r8, rsi
     mov rax, rdi
     cbw
     idiv r8b
     movzx edx, ah
     movsx rax, dl
     add r9, rax
     mov rdx, rcx
     and dl, sil
     movsx rax, dl
     mov r8, r9
     add r8, rax
     mov rdx, rcx
     or dl, sil
     movsx rax, dl
     add r8, rax
     mov rdx, rcx
     xor dl, sil
     movsx rax, dl
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not dl
     and dl, al
     movsx rax, dl
     add r8, rax
     mov rax, rcx
     shl al, 0x02
     movsx rax, al
     add r8, rax
     mov rax, rcx
     sar al, 0x02
     movsx rax, al
     add r8, rax
     mov rax, rdi
     sar al, 0x02
     movsx rax, al
     add r8, rax
     cmp cl, sil
     jnle :0
     jmp :1

000: add r8, 0x01
001: cmp sil, cl
     jl :2
     jmp :3

002: add r8, 0x02
003: cmp cl, sil
     jnl :4
     jmp :5

004: add r8, 0x04
005: cmp cl, sil
     jle :6
     jmp :7

006: add r8, 0x08
007: cmp cl, sil
     jnz :8
     add r8, 0x10
     jmp :9

008: add r8, 0x30
009: cmp dil, sil
     jl :10
     jmp :11

010: add r8, 0x40
011: mov rax, r8
     ret
batch i16:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     imul ax, si
     movsx r8, ax
     mov rax, rcx
     cwd
     idiv si
     movsx rax, ax
     add r8, rax
     mov rax, rcx
     cwd
     idiv si
     movsx rax, dx
     add r8, rax
     mov rax, rdi
     cwd
     idiv si
     movsx rax, ax
     add r8, rax
     mov rax, rdi
     cwd
     idiv si
     movsx rax, dx
     add r8, rax
     mov rax, rcx
     and ax, si
     movsx rax, ax
     add r8, rax
     mov rax, rcx
     or ax, si
     movsx rax, ax
     add r8, rax
     mov rax, rcx
     xor ax, si
     movsx rax, ax
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not dx
     and dx, ax
     movsx rax, dx
     add r8, rax
     mov rax, rcx
     shl ax, 0x02
     movsx rax, ax
     add r8, rax
     mov rax, rcx
     sar ax, 0x02
     movsx rax, ax
     add r8, rax
     mov rax, rdi
     sar ax, 0x02
     movsx rax, ax
     add r8, rax
     cmp cx, si
     jnle :0
     jmp :1

000: add r8, 0x01
001: mov rdx, r8
     cmp si, cx
     jl :2
     jmp :3

002: add rdx, 0x02
003: cmp cx, si
     jnl :4
     jmp :5

004: add rdx, 0x04
005: cmp cx, si
     jle :6
     jmp :7

006: add rdx, 0x08
007: cmp cx, si
     jnz :8
     add rdx, 0x10
     jmp :9

008: add rdx, 0x30
009: cmp di, si
     jl :10
     jmp :11

010: add rdx, 0x40
011: mov rax, rdx
     ret
batch i32:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     imul eax, esi
     movsxd r8, eax
     mov rax, rcx
     cdq
     idiv esi
     movsxd rax, eax
     add r8, rax
     mov rax, rcx
     cdq
     idiv esi
     movsxd rax, edx
     add r8, rax
     mov rax, rdi
     cdq
     idiv esi
     movsxd rax, eax
     add r8, rax
     mov rax, rdi
     cdq
     idiv esi
     movsxd rax, edx
     add r8, rax
     mov rax, rcx
     and eax, esi
     movsxd rax, eax
     add r8, rax
     mov rax, rcx
     or eax, esi
     movsxd rax, eax
     add r8, rax
     mov rax, rcx
     xor eax, esi
     movsxd rax, eax
     add r8, rax
     mov rax, rcx
     mov rdx, rsi
     not edx
     and edx, eax
     movsxd rax, edx
     add r8, rax
     mov rax, rcx
     shl eax, 0x02
     movsxd rax, eax
     add r8, rax
     mov rax, rcx
     sar eax, 0x02
     movsxd rax, eax
     add r8, rax
     mov rax, rdi
     sar eax, 0x02
     movsxd rax, eax
     add r8, rax
     cmp ecx, esi
     jnle :0
     jmp :1

000: add r8, 0x01
001: mov rdx, r8
     cmp esi, ecx
     jl :2
     jmp :3

002: add rdx, 0x02
003: cmp ecx, esi
     jnl :4
     jmp :5

004: add rdx, 0x04
005: cmp ecx, esi
     jle :6
     jmp :7

006: add rdx, 0x08
007: cmp ecx, esi
     jnz :8
     add rdx, 0x10
     jmp :9

008: add rdx, 0x30
009: cmp edi, esi
     jl :10
     jmp :11

010: add rdx, 0x40
011: mov rax, rdx
     ret
=========== OPT LEVEL: aggresive ===========
batch:
main:
     mov rax, 0x9DC
     ret
batch int:
batch u8:
batch u16:
batch u32:
batch i8:
batch i16:
batch i32:
