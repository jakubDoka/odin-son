=========== OPT LEVEL: none ===========
main:
     xor eax, eax
     xor edi, edi
005: cmp rdi, 0x04
     jnl :0
     xor edx, edx
     xor ecx, ecx
002: cmp rdx, 0x04
     jnl :1
     add rcx, rdx
     add rdx, 0x01
     jmp :2

001: xor edx, edx
     xor esi, esi
004: cmp rsi, 0x04
     jnl :3
     lea rdx, [rdx+rsi*2]
     add rsi, 0x01
     jmp :4

003: add rcx, rdx
     add rax, rcx
     add rdi, 0x01
     jmp :5

000: mov rcx, 0xFB
     cqo
     idiv rcx
     mov rax, rdx
     ret
=========== OPT LEVEL: mininal ===========
main:
     xor eax, eax
     xor edi, edi
005: cmp rdi, 0x04
     jnl :0
     xor edx, edx
     xor ecx, ecx
002: cmp rdx, 0x04
     jnl :1
     add rcx, rdx
     add rdx, 0x01
     jmp :2

001: xor edx, edx
     xor esi, esi
004: cmp rsi, 0x04
     jnl :3
     lea rdx, [rdx+rsi*2]
     add rsi, 0x01
     jmp :4

003: add rcx, rdx
     add rax, rcx
     add rdi, 0x01
     jmp :5

000: mov rcx, 0xFB
     cqo
     idiv rcx
     mov rax, rdx
     ret
=========== OPT LEVEL: moderate ===========
main:
     xor eax, eax
     xor edi, edi
005: cmp rdi, 0x04
     jnl :0
     xor edx, edx
     xor ecx, ecx
002: cmp rdx, 0x04
     jnl :1
     add rcx, rdx
     add rdx, 0x01
     jmp :2

001: xor edx, edx
     xor esi, esi
004: cmp rsi, 0x04
     jnl :3
     lea rdx, [rdx+rsi*2]
     add rsi, 0x01
     jmp :4

003: add rcx, rdx
     add rax, rcx
     add rdi, 0x01
     jmp :5

000: mov rcx, 0xFB
     cqo
     idiv rcx
     mov rax, rdx
     ret
=========== OPT LEVEL: all ===========
main:
     xor eax, eax
     xor edi, edi
005: cmp rdi, 0x04
     jnl :0
     xor edx, edx
     xor ecx, ecx
002: cmp rdx, 0x04
     jnl :1
     add rcx, rdx
     add rdx, 0x01
     jmp :2

001: xor edx, edx
     xor esi, esi
004: cmp rsi, 0x04
     jnl :3
     lea rdx, [rdx+rsi*2]
     add rsi, 0x01
     jmp :4

003: add rcx, rdx
     add rax, rcx
     add rdi, 0x01
     jmp :5

000: mov rcx, 0xFB
     cqo
     idiv rcx
     mov rax, rdx
     ret
=========== OPT LEVEL: aggresive ===========
