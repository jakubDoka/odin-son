=========== OPT LEVEL: all ===========
Error{inst_idx = 20, code = "INVALID_OPCODE", _ = [0, 0, 0]}
mul8:
    madd x0, x0, x1, xzr
    ret
main:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    movz x0, #7, #0
    movz x1, #9, #0
    bl 0xfffffff8
    invalid
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
