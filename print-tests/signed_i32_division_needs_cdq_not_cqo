=========== OPT LEVEL: none ===========
main:
    push rbx
    push rbp
    sub rsp, 8
    mov rdi, 0xa
    call +0x3c
    mov rbp, rax
    mov rdi, 0x1e
    call +0x2d
    mov rbx, rax
    mov rdi, 3
    call +0x1e
    mov rcx, rax
    sub ebp, ebx
    mov rax, rbp
    cdq
    idiv ecx
    movsxd rax, eax
    add rsp, 8
    pop rbp
    pop rbx
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: mininal ===========
main:
    push rbx
    push rbp
    sub rsp, 8
    mov rdi, 0xa
    call +0x3c
    mov rbp, rax
    mov rdi, 0x1e
    call +0x2d
    mov rbx, rax
    mov rdi, 3
    call +0x1e
    mov rcx, rax
    sub ebp, ebx
    mov rax, rbp
    cdq
    idiv ecx
    movsxd rax, eax
    add rsp, 8
    pop rbp
    pop rbx
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: moderate ===========
main:
    push rbx
    push rbp
    sub rsp, 8
    mov rdi, 0xa
    call +0x3c
    mov rbp, rax
    mov rdi, 0x1e
    call +0x2d
    mov rbx, rax
    mov rdi, 3
    call +0x1e
    mov rcx, rax
    sub ebp, ebx
    mov rax, rbp
    cdq
    idiv ecx
    movsxd rax, eax
    add rsp, 8
    pop rbp
    pop rbx
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: all ===========
main:
    push rbx
    push rbp
    sub rsp, 8
    mov rdi, 0xa
    call +0x3c
    mov rbp, rax
    mov rdi, 0x1e
    call +0x2d
    mov rbx, rax
    mov rdi, 3
    call +0x1e
    mov rcx, rax
    sub ebp, ebx
    mov rax, rbp
    cdq
    idiv ecx
    movsxd rax, eax
    add rsp, 8
    pop rbp
    pop rbx
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, -6
    ret
opaque:
