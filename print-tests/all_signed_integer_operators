=========== OPT LEVEL: None ===========
main:
     push rbx
     push rbp
     mov rcx, 0x00
     mov rax, 0x06
     mov rdx, 0x00
     sub rdx, rax
     mov rbx, rdx
     mov rax, 0x07
     mov rdx, 0x00
     sub rdx, rax
     mov rbp, rdx
     mov rax, 0x14
     cqo
     idiv rbx
     mov rsi, rax
     add rsi, rcx
     mov rax, 0x14
     cqo
     idiv rbx
     add rsi, rdx
     mov rax, rbp
     cqo
     idiv rbx
     add rsi, rax
     mov rax, rbp
     cqo
     idiv rbx
     add rsi, rdx
     mov rcx, 0x01
     mov rax, rbp
     sar rax, cl
     add rsi, rax
     mov rax, rbx
     sar rax, cl
     add rsi, rax
     mov rax, 0x14
     mov rdx, rbx
     cmp rdx, rax
     setl dl
     movzx rdx, dl
     test rdx, rdx
     jz :0
     jmp :1

000: jmp :2

001: add rsi, rcx
     jmp :2

002: mov rcx, rsi
     mov rax, rbp
     cmp rax, rbx
     setl al
     movzx rax, al
     test rax, rax
     jz :3
     jmp :4

003: jmp :5

004: mov rax, 0x02
     add rax, rcx
     mov rcx, rax
     jmp :5

005: mov rax, 0x14
     cmp rax, rbx
     setnle al
     movzx rax, al
     test rax, rax
     jz :6
     jmp :7

006: jmp :8

007: mov rax, rcx
     mov rcx, 0x04
     add rcx, rax
     jmp :8

008: mov rax, rbp
     mov rdx, rbx
     cmp rdx, rax
     setle dl
     movzx rdx, dl
     test rdx, rdx
     jz :9
     jmp :10

009: jmp :11

010: mov rax, rcx
     mov rcx, 0x08
     add rcx, rax
     jmp :11

011: mov rax, 0x14
     cmp rax, rbx
     setnl al
     movzx rax, al
     test rax, rax
     jz :12
     jmp :13

012: jmp :14

013: mov rax, rcx
     mov rcx, 0x10
     add rcx, rax
     jmp :14

014: mov rax, rcx
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: Full ===========
main:
     mov rcx, 0x01
     test rcx, rcx
     jz :0
     jmp :1

000: mov rax, 0xFFFFFFFFFFFFFFF8
     jmp :2

001: mov rax, 0xFFFFFFFFFFFFFFF9
     jmp :2

002: test rcx, rcx
     jz :3
     jmp :4

003: jmp :5

004: add rax, 0x02
     jmp :5

005: test rcx, rcx
     jz :6
     jmp :7

006: jmp :8

007: add rax, 0x04
     jmp :8

008: mov rdx, 0x00
     test rdx, rdx
     jz :9
     jmp :10

009: jmp :11

010: add rax, 0x08
     jmp :11

011: test rcx, rcx
     jz :12
     jmp :13

012: jmp :14

013: add rax, 0x10
     jmp :14

014: ret
