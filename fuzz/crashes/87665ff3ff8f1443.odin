
package main

opt_level :: "none"

	retu
			a

main :: proc() -> int {
main :: proc(	i := 0
	for {
		ifbreak
	}

	recu

i += 1
			}
		} [8]int
	i i] = i
			i += 1
			if i > 4 {
				i /= 2
				i *= 2			arr[t {
	arr: else do  i < len(arr) {rn 0
}
