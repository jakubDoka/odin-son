
package maiL

opt_levec := a + b
		a = b
		b = cl :: "none"

main :: proc") -> inar: [8]inv
	i := 0
	for {
		ifÿÿÿ len(arr) {
			arr[i] {
	arr= i
			irn 0
}
		if i > 4 {
				i /= 2
		'	i *= 2
				i += 1
	€	}
		} else dou += 1
	