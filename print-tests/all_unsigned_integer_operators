=========== OPT LEVEL: none ===========
main:
     mov rcx, 0x06
     mov rax, 0x14
     xor rdx, rdx
     div rcx
     cmp rax, 0x03
     jnz :0
     jmp :1

000: mov rsi, 0x00
     jmp :2

001: mov rsi, 0x00
     add rsi, 0x01
002: mov rax, 0x14
     xor rdx, rdx
     div rcx
     cmp rdx, 0x02
     jnz :3
     jmp :4

003: jmp :5

004: add rsi, 0x02
005: mov rax, 0x00
     sub rax, 0x07
     mov rdi, rax
     mov rax, rdi
     xor rdx, rdx
     div rcx
     cmp rax, 0x14
     jbe :6
     jmp :7

006: jmp :8

007: add rsi, 0x04
008: mov rax, rdi
     xor rdx, rdx
     div rcx
     cmp rdx, 0x03
     jnz :9
     jmp :10

009: jmp :11

010: add rsi, 0x08
011: mov rax, rdi
     shr rax, 0x3C
     cmp rax, 0x0F
     jnz :12
     jmp :13

012: jmp :14

013: add rsi, 0x10
014: mov rax, 0x14
     shr rax, 0x02
     cmp rax, 0x05
     jnz :15
     jmp :16

015: jmp :17

016: add rsi, 0x20
017: mov rax, rdi
     cmp rax, 0x06
     jbe :18
     jmp :19

018: jmp :20

019: add rsi, 0x40
020: mov rax, rdi
     cmp rax, 0x14
     jb :21
     jmp :22

021: jmp :23

022: add rsi, 0x80
023: mov rax, rdi
     cmp rcx, rax
     jnb :24
     jmp :25

024: jmp :26

025: add rsi, 0x100
026: mov rdx, rsi
     mov rax, rdi
     cmp rcx, rax
     jnbe :27
     jmp :28

027: jmp :29

028: add rdx, 0x200
029: mov rax, 0x14
     mov rcx, rdi
     cmp rax, rcx
     jnb :30
     jmp :31

030: jmp :32

031: add rdx, 0x400
032: mov rax, rdx
     ret
=========== OPT LEVEL: mininal ===========
main:
     mov rcx, 0x01
     test rcx, rcx
     jz :0
     jmp :1

000: mov rsi, 0x00
     jmp :2

001: mov rsi, 0x00
     add rsi, 0x01
002: test rcx, rcx
     jz :3
     jmp :4

003: jmp :5

004: add rsi, 0x02
005: mov rdi, 0x06
     mov rax, 0x00
     sub rax, 0x07
     mov r8, rax
     mov rax, r8
     xor rdx, rdx
     div rdi
     cmp rax, 0x14
     jbe :6
     jmp :7

006: jmp :8

007: add rsi, 0x04
008: mov rax, r8
     xor rdx, rdx
     div rdi
     cmp rdx, 0x03
     jnz :9
     jmp :10

009: jmp :11

010: add rsi, 0x08
011: mov rax, r8
     shr rax, 0x3C
     cmp rax, 0x0F
     jnz :12
     jmp :13

012: jmp :14

013: add rsi, 0x10
014: test rcx, rcx
     jz :15
     jmp :16

015: jmp :17

016: add rsi, 0x20
017: mov rax, r8
     cmp rax, 0x06
     setnbe al
     movzx rax, al
     test rax, rax
     jz :18
     jmp :19

018: jmp :20

019: add rsi, 0x40
020: mov rcx, r8
     cmp rcx, 0x14
     setnb cl
     movzx rcx, cl
     test rcx, rcx
     jz :21
     jmp :22

021: jmp :23

022: add rsi, 0x80
023: cmp r8, 0x06
     jb :24
     jmp :25

024: jmp :26

025: add rsi, 0x100
026: test rax, rax
     jz :27
     jmp :28

027: jmp :29

028: add rsi, 0x200
029: test rcx, rcx
     jz :30
     jmp :31

030: jmp :32

031: add rsi, 0x400
032: mov rax, rsi
     ret
=========== OPT LEVEL: all ===========
main:
     mov rax, 0x7FF
     ret
