=========== OPT LEVEL: none ===========
main:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rsi, 6
    mov rdx, 7
    neg rdx
    mov rdi, 0x14
    call batch int
    mov rbx, rax
    mov rax, 0x14
    mov rbp, rax
    mov rdi, rbp
    mov sil, 6
    mov dl, 7
    call batch u8
    mov r12, rax
    mov rax, 0x14
    mov r13, rax
    mov rdi, r13
    mov rsi, 6
    mov rdx, 7
    call batch u16
    mov r14, rax
    mov rax, 0x14
    mov r15, rax
    mov rdi, r15
    mov rsi, 6
    mov rdx, 7
    call batch u32
    mov.q [rsp], rax
    mov al, 7
    neg al
    mov rdi, rbp
    mov sil, 6
    mov rdx, rax
    call batch i8
    mov rbp, rax
    mov rax, 7
    neg ax
    mov rdi, r13
    mov rsi, 6
    mov rdx, rax
    call batch i16
    mov r13, rax
    mov rax, 7
    neg eax
    mov rdi, r15
    mov rsi, 6
    mov rdx, rax
    call batch i32
    mov rdx, rax
    xor eax, eax
    add rax, rbx
    add rax, r12
    add rax, r14
    mov.q rcx, [rsp]
    add rax, rcx
    add rax, rbp
    add rax, r13
    add rax, rdx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
batch int:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul rax, rsi
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rax
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rax
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rcx
    and rax, rsi
    add r8, rax
    mov rax, rcx
    or rax, rsi
    add r8, rax
    mov rax, rcx
    xor rax, rsi
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not rdx
    and rdx, rax
    add r8, rdx
    mov rax, rcx
    shl rax, 2
    add r8, rax
    mov rax, rcx
    sar rax, 2
    add r8, rax
    mov rax, rdi
    sar rax, 2
    add r8, rax
    cmp rcx, rsi
    jnle 3
    jmp 4
3:
    add r8, 1
4:
    cmp rsi, rcx
    jnge 5
    jmp 6
5:
    add r8, 2
6:
    mov rdx, r8
    cmp rcx, rsi
    jnl 7
    jmp 8
7:
    add rdx, 4
8:
    cmp rcx, rsi
    jle 9
    jmp 10
9:
    add rdx, 8
10:
    cmp rcx, rcx
    jz 11
    jmp 12
11:
    add rdx, 0x10
12:
    cmp rcx, rsi
    jne 13
    jmp 14
13:
    add rdx, 0x20
14:
    cmp rdi, rsi
    jnge 15
    jmp 16
15:
    add rdx, 0x40
16:
    mov rax, rdx
    ret
batch u8:
    mov rcx, rdi
    mov rdi, rdx
    mov rdx, rcx
    mov rax, rsi
    imul dl
    movzx eax, al
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    movzx eax, al
    div sil
    movzx eax, al
    mov r9, r8
    add r9, rax
    mov rax, rcx
    movzx eax, al
    div sil
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    movzx eax, al
    div r8b
    movzx eax, al
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    movzx eax, al
    div r8b
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov rdx, rcx
    and dl, sil
    movzx eax, dl
    mov r8, r9
    add r8, rax
    mov rdx, rcx
    or dl, sil
    movzx eax, dl
    add r8, rax
    mov rdx, rcx
    xor dl, sil
    movzx eax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dl
    and dl, al
    movzx eax, dl
    add r8, rax
    mov rax, rcx
    shl al, 2
    movzx eax, al
    add r8, rax
    mov rax, rcx
    shr al, 2
    movzx eax, al
    add r8, rax
    mov rax, rdi
    shr al, 2
    movzx eax, al
    add r8, rax
    cmp cl, sil
    jnbe 18
    jmp 19
18:
    add r8, 1
19:
    cmp sil, cl
    jnae 20
    jmp 21
20:
    add r8, 2
21:
    cmp cl, sil
    jnc 22
    jmp 23
22:
    add r8, 4
23:
    cmp cl, sil
    jna 24
    jmp 25
24:
    add r8, 8
25:
    cmp cl, cl
    jz 26
    jmp 27
26:
    add r8, 0x10
27:
    cmp cl, sil
    jne 28
    jmp 29
28:
    add r8, 0x20
29:
    cmp dil, sil
    jnae 30
    jmp 31
30:
    add r8, 0x40
31:
    mov rax, r8
    ret
batch u16:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movzx eax, ax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    shl ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    shr ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    shr ax, 2
    movzx eax, ax
    add r8, rax
    cmp cx, si
    jnbe 33
    jmp 34
33:
    add r8, 1
34:
    mov rdx, r8
    cmp si, cx
    jnae 35
    jmp 36
35:
    add rdx, 2
36:
    cmp cx, si
    jnc 37
    jmp 38
37:
    add rdx, 4
38:
    cmp cx, si
    jna 39
    jmp 40
39:
    add rdx, 8
40:
    cmp cx, cx
    jz 41
    jmp 42
41:
    add rdx, 0x10
42:
    cmp cx, si
    jne 43
    jmp 44
43:
    add rdx, 0x20
44:
    cmp di, si
    jnae 45
    jmp 46
45:
    add rdx, 0x40
46:
    mov rax, rdx
    ret
batch u32:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    mov eax, eax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    mov eax, edx
    add r8, rax
    mov rax, rcx
    shl eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rcx
    shr eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rdi
    shr eax, 2
    mov eax, eax
    add r8, rax
    cmp ecx, esi
    jnbe 48
    jmp 49
48:
    add r8, 1
49:
    mov rdx, r8
    cmp esi, ecx
    jnae 50
    jmp 51
50:
    add rdx, 2
51:
    cmp ecx, esi
    jnc 52
    jmp 53
52:
    add rdx, 4
53:
    cmp ecx, esi
    jna 54
    jmp 55
54:
    add rdx, 8
55:
    cmp ecx, ecx
    jz 56
    jmp 57
56:
    add rdx, 0x10
57:
    cmp ecx, esi
    jne 58
    jmp 59
58:
    add rdx, 0x20
59:
    cmp edi, esi
    jnae 60
    jmp 61
60:
    add rdx, 0x40
61:
    mov rax, rdx
    ret
batch i8:
    mov rcx, rdi
    mov rdi, rdx
    mov rdx, rcx
    mov rax, rsi
    imul dl
    movsx rax, al
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cbw
    idiv sil
    movsx rax, al
    mov r9, r8
    add r9, rax
    mov rax, rcx
    cbw
    idiv sil
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    cbw
    idiv r8b
    movsx rax, al
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    cbw
    idiv r8b
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov rdx, rcx
    and dl, sil
    movsx rax, dl
    mov r8, r9
    add r8, rax
    mov rdx, rcx
    or dl, sil
    movsx rax, dl
    add r8, rax
    mov rdx, rcx
    xor dl, sil
    movsx rax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dl
    and dl, al
    movsx rax, dl
    add r8, rax
    mov rax, rcx
    shl al, 2
    movsx rax, al
    add r8, rax
    mov rax, rcx
    sar al, 2
    movsx rax, al
    add r8, rax
    mov rax, rdi
    sar al, 2
    movsx rax, al
    add r8, rax
    cmp cl, sil
    jnle 63
    jmp 64
63:
    add r8, 1
64:
    cmp sil, cl
    jnge 65
    jmp 66
65:
    add r8, 2
66:
    cmp cl, sil
    jnl 67
    jmp 68
67:
    add r8, 4
68:
    cmp cl, sil
    jle 69
    jmp 70
69:
    add r8, 8
70:
    cmp cl, cl
    jz 71
    jmp 72
71:
    add r8, 0x10
72:
    cmp cl, sil
    jne 73
    jmp 74
73:
    add r8, 0x20
74:
    cmp dil, sil
    jnge 75
    jmp 76
75:
    add r8, 0x40
76:
    mov rax, r8
    ret
batch i16:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movsx rax, ax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    shl ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    sar ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    sar ax, 2
    movsx rax, ax
    add r8, rax
    cmp cx, si
    jnle 78
    jmp 79
78:
    add r8, 1
79:
    mov rdx, r8
    cmp si, cx
    jnge 80
    jmp 81
80:
    add rdx, 2
81:
    cmp cx, si
    jnl 82
    jmp 83
82:
    add rdx, 4
83:
    cmp cx, si
    jle 84
    jmp 85
84:
    add rdx, 8
85:
    cmp cx, cx
    jz 86
    jmp 87
86:
    add rdx, 0x10
87:
    cmp cx, si
    jne 88
    jmp 89
88:
    add rdx, 0x20
89:
    cmp di, si
    jnge 90
    jmp 91
90:
    add rdx, 0x40
91:
    mov rax, rdx
    ret
batch i32:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    movsxd rax, eax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    shl eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    cmp ecx, esi
    jnle 93
    jmp 94
93:
    add r8, 1
94:
    mov rdx, r8
    cmp esi, ecx
    jnge 95
    jmp 96
95:
    add rdx, 2
96:
    cmp ecx, esi
    jnl 97
    jmp 98
97:
    add rdx, 4
98:
    cmp ecx, esi
    jle 99
    jmp 100
99:
    add rdx, 8
100:
    cmp ecx, ecx
    jz 101
    jmp 102
101:
    add rdx, 0x10
102:
    cmp ecx, esi
    jne 103
    jmp +0x7
103:
    add rdx, 0x20
    cmp edi, esi
    jnge 104
    jmp 105
104:
    add rdx, 0x40
105:
    mov rax, rdx
    ret
=========== OPT LEVEL: mininal ===========
main:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0x14
    mov rsi, 6
    mov rdx, -7
    call batch int
    mov rbx, rax
    mov dl, 7
    mov dil, 0x14
    mov sil, 6
    call batch u8
    mov rbp, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call batch u16
    mov r12, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call batch u32
    mov r13, rax
    mov dl, -7
    mov dil, 0x14
    mov sil, 6
    call batch i8
    mov r14, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call batch i16
    mov r15, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call batch i32
    mov rdx, rax
    xor eax, eax
    add rax, rbx
    add rax, rbp
    add rax, r12
    add rax, r13
    add rax, r14
    add rax, r15
    add rax, rdx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
batch int:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul rax, rsi
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rax
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rax
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rcx
    and rax, rsi
    add r8, rax
    mov rax, rcx
    or rax, rsi
    add r8, rax
    mov rax, rcx
    xor rax, rsi
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not rdx
    and rdx, rax
    add r8, rdx
    mov rax, rcx
    shl rax, 2
    add r8, rax
    mov rax, rcx
    sar rax, 2
    add r8, rax
    mov rax, rdi
    sar rax, 2
    add r8, rax
    cmp rcx, rsi
    jnle 3
    jmp 4
3:
    add r8, 1
4:
    cmp rsi, rcx
    jnge 5
    jmp 6
5:
    add r8, 2
6:
    mov rdx, r8
    cmp rcx, rsi
    jnl 7
    jmp 8
7:
    add rdx, 4
8:
    cmp rcx, rsi
    jle 9
    jmp 10
9:
    add rdx, 8
10:
    mov rax, 1
    test rax, rax
    jne 11
    jmp 12
11:
    add rdx, 0x10
12:
    cmp rcx, rsi
    jne 13
    jmp 14
13:
    add rdx, 0x20
14:
    cmp rdi, rsi
    jnge 15
    jmp 16
15:
    add rdx, 0x40
16:
    mov rax, rdx
    ret
batch u8:
    mov rcx, rdi
    mov rdi, rdx
    mov rdx, rcx
    mov rax, rsi
    imul dl
    movzx eax, al
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    movzx eax, al
    div sil
    movzx eax, al
    mov r9, r8
    add r9, rax
    mov rax, rcx
    movzx eax, al
    div sil
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    movzx eax, al
    div r8b
    movzx eax, al
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    movzx eax, al
    div r8b
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov rdx, rcx
    and dl, sil
    movzx eax, dl
    mov r8, r9
    add r8, rax
    mov rdx, rcx
    or dl, sil
    movzx eax, dl
    add r8, rax
    mov rdx, rcx
    xor dl, sil
    movzx eax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dl
    and dl, al
    movzx eax, dl
    add r8, rax
    mov rax, rcx
    shl al, 2
    movzx eax, al
    add r8, rax
    mov rax, rcx
    shr al, 2
    movzx eax, al
    add r8, rax
    mov rax, rdi
    shr al, 2
    movzx eax, al
    add r8, rax
    cmp cl, sil
    jnbe 18
    jmp 19
18:
    add r8, 1
19:
    cmp sil, cl
    jnae 20
    jmp 21
20:
    add r8, 2
21:
    cmp cl, sil
    jnc 22
    jmp 23
22:
    add r8, 4
23:
    cmp cl, sil
    jna 24
    jmp 25
24:
    add r8, 8
25:
    mov rax, 1
    test rax, rax
    jne 26
    jmp 27
26:
    add r8, 0x10
27:
    cmp cl, sil
    jne 28
    jmp 29
28:
    add r8, 0x20
29:
    cmp dil, sil
    jnae 30
    jmp 31
30:
    add r8, 0x40
31:
    mov rax, r8
    ret
batch u16:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movzx eax, ax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    shl ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    shr ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    shr ax, 2
    movzx eax, ax
    add r8, rax
    cmp cx, si
    jnbe 33
    jmp 34
33:
    add r8, 1
34:
    mov rdx, r8
    cmp si, cx
    jnae 35
    jmp 36
35:
    add rdx, 2
36:
    cmp cx, si
    jnc 37
    jmp 38
37:
    add rdx, 4
38:
    cmp cx, si
    jna 39
    jmp 40
39:
    add rdx, 8
40:
    mov rax, 1
    test rax, rax
    jne 41
    jmp 42
41:
    add rdx, 0x10
42:
    cmp cx, si
    jne 43
    jmp 44
43:
    add rdx, 0x20
44:
    cmp di, si
    jnae 45
    jmp 46
45:
    add rdx, 0x40
46:
    mov rax, rdx
    ret
batch u32:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    mov eax, eax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    mov eax, edx
    add r8, rax
    mov rax, rcx
    shl eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rcx
    shr eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rdi
    shr eax, 2
    mov eax, eax
    add r8, rax
    cmp ecx, esi
    jnbe 48
    jmp 49
48:
    add r8, 1
49:
    mov rdx, r8
    cmp esi, ecx
    jnae 50
    jmp 51
50:
    add rdx, 2
51:
    cmp ecx, esi
    jnc 52
    jmp 53
52:
    add rdx, 4
53:
    cmp ecx, esi
    jna 54
    jmp 55
54:
    add rdx, 8
55:
    mov rax, 1
    test rax, rax
    jne 56
    jmp 57
56:
    add rdx, 0x10
57:
    cmp ecx, esi
    jne 58
    jmp 59
58:
    add rdx, 0x20
59:
    cmp edi, esi
    jnae 60
    jmp 61
60:
    add rdx, 0x40
61:
    mov rax, rdx
    ret
batch i8:
    mov rcx, rdi
    mov rdi, rdx
    mov rdx, rcx
    mov rax, rsi
    imul dl
    movsx rax, al
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cbw
    idiv sil
    movsx rax, al
    mov r9, r8
    add r9, rax
    mov rax, rcx
    cbw
    idiv sil
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    cbw
    idiv r8b
    movsx rax, al
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    cbw
    idiv r8b
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov rdx, rcx
    and dl, sil
    movsx rax, dl
    mov r8, r9
    add r8, rax
    mov rdx, rcx
    or dl, sil
    movsx rax, dl
    add r8, rax
    mov rdx, rcx
    xor dl, sil
    movsx rax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dl
    and dl, al
    movsx rax, dl
    add r8, rax
    mov rax, rcx
    shl al, 2
    movsx rax, al
    add r8, rax
    mov rax, rcx
    sar al, 2
    movsx rax, al
    add r8, rax
    mov rax, rdi
    sar al, 2
    movsx rax, al
    add r8, rax
    cmp cl, sil
    jnle 63
    jmp 64
63:
    add r8, 1
64:
    cmp sil, cl
    jnge 65
    jmp 66
65:
    add r8, 2
66:
    cmp cl, sil
    jnl 67
    jmp 68
67:
    add r8, 4
68:
    cmp cl, sil
    jle 69
    jmp 70
69:
    add r8, 8
70:
    mov rax, 1
    test rax, rax
    jne 71
    jmp 72
71:
    add r8, 0x10
72:
    cmp cl, sil
    jne 73
    jmp 74
73:
    add r8, 0x20
74:
    cmp dil, sil
    jnge 75
    jmp 76
75:
    add r8, 0x40
76:
    mov rax, r8
    ret
batch i16:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movsx rax, ax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    shl ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    sar ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    sar ax, 2
    movsx rax, ax
    add r8, rax
    cmp cx, si
    jnle 78
    jmp 79
78:
    add r8, 1
79:
    mov rdx, r8
    cmp si, cx
    jnge 80
    jmp 81
80:
    add rdx, 2
81:
    cmp cx, si
    jnl 82
    jmp 83
82:
    add rdx, 4
83:
    cmp cx, si
    jle 84
    jmp 85
84:
    add rdx, 8
85:
    mov rax, 1
    test rax, rax
    jne 86
    jmp 87
86:
    add rdx, 0x10
87:
    cmp cx, si
    jne 88
    jmp 89
88:
    add rdx, 0x20
89:
    cmp di, si
    jnge 90
    jmp 91
90:
    add rdx, 0x40
91:
    mov rax, rdx
    ret
batch i32:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    movsxd rax, eax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    shl eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    cmp ecx, esi
    jnle 93
    jmp 94
93:
    add r8, 1
94:
    mov rdx, r8
    cmp esi, ecx
    jnge 95
    jmp 96
95:
    add rdx, 2
96:
    cmp ecx, esi
    jnl 97
    jmp 98
97:
    add rdx, 4
98:
    cmp ecx, esi
    jle 99
    jmp 100
99:
    add rdx, 8
100:
    mov rax, 1
    test rax, rax
    jne 101
    jmp 102
101:
    add rdx, 0x10
102:
    cmp ecx, esi
    jne 103
    jmp 104
103:
    add rdx, 0x20
104:
    cmp edi, esi
    jnge 105
    jmp 106
105:
    add rdx, 0x40
106:
    mov rax, rdx
    ret
=========== OPT LEVEL: moderate ===========
main:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0x14
    mov rsi, 6
    mov rdx, -7
    call batch int
    mov rbx, rax
    mov dl, 7
    mov dil, 0x14
    mov sil, 6
    call batch u8
    mov rbp, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call batch u16
    mov r12, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call batch u32
    mov r13, rax
    mov dl, -7
    mov dil, 0x14
    mov sil, 6
    call batch i8
    mov r14, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call batch i16
    mov r15, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call batch i32
    mov rdx, rbx
    add rdx, rbp
    add rdx, r12
    add rdx, r13
    add rdx, r14
    add rdx, r15
    add rdx, rax
    mov rax, rdx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
batch int:
    mov rcx, rdi
    mov rdi, rdx
    mov r8, rcx
    imul r8, rsi
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rax
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rax
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rcx
    and rax, rsi
    add r8, rax
    mov rax, rcx
    or rax, rsi
    add r8, rax
    mov rax, rcx
    xor rax, rsi
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not rdx
    and rdx, rax
    add r8, rdx
    mov rax, rcx
    shl rax, 2
    add r8, rax
    mov rax, rcx
    sar rax, 2
    add r8, rax
    mov rax, rdi
    sar rax, 2
    add r8, rax
    cmp rcx, rsi
    jnle 3
    jmp 4
3:
    add r8, 1
4:
    cmp rsi, rcx
    jnge 5
    jmp 6
5:
    add r8, 2
6:
    mov rdx, r8
    cmp rcx, rsi
    jnl 7
    jmp 8
7:
    add rdx, 4
8:
    cmp rcx, rsi
    jle 9
    jmp 10
9:
    add rdx, 8
10:
    cmp rcx, rsi
    jne 11
    add rdx, 0x10
    jmp 12
11:
    add rdx, 0x30
12:
    cmp rdi, rsi
    jnge 13
    jmp 14
13:
    add rdx, 0x40
14:
    mov rax, rdx
    ret
batch u8:
    mov rcx, rdi
    mov rdi, rdx
    mov rdx, rcx
    mov rax, rsi
    imul dl
    movzx r8d, al
    mov rax, rcx
    movzx eax, al
    div sil
    movzx eax, al
    mov r9, r8
    add r9, rax
    mov rax, rcx
    movzx eax, al
    div sil
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    movzx eax, al
    div r8b
    movzx eax, al
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    movzx eax, al
    div r8b
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov rdx, rcx
    and dl, sil
    movzx eax, dl
    mov r8, r9
    add r8, rax
    mov rdx, rcx
    or dl, sil
    movzx eax, dl
    add r8, rax
    mov rdx, rcx
    xor dl, sil
    movzx eax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dl
    and dl, al
    movzx eax, dl
    add r8, rax
    mov rax, rcx
    shl al, 2
    movzx eax, al
    add r8, rax
    mov rax, rcx
    shr al, 2
    movzx eax, al
    add r8, rax
    mov rax, rdi
    shr al, 2
    movzx eax, al
    add r8, rax
    cmp cl, sil
    jnbe 16
    jmp 17
16:
    add r8, 1
17:
    cmp sil, cl
    jnae 18
    jmp 19
18:
    add r8, 2
19:
    cmp cl, sil
    jnc +0x5
    jmp 20
    add r8, 4
20:
    cmp cl, sil
    jna 21
    jmp 22
21:
    add r8, 8
22:
    cmp cl, sil
    jne 23
    add r8, 0x10
    jmp 24
23:
    add r8, 0x30
24:
    cmp dil, sil
    jnae 25
    jmp 26
25:
    add r8, 0x40
26:
    mov rax, r8
    ret
batch u16:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movzx r8d, ax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    shl ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    shr ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    shr ax, 2
    movzx eax, ax
    add r8, rax
    cmp cx, si
    jnbe 28
    jmp 29
28:
    add r8, 1
29:
    mov rdx, r8
    cmp si, cx
    jnae 30
    jmp 31
30:
    add rdx, 2
31:
    cmp cx, si
    jnc +0x5
    jmp 32
    add rdx, 4
32:
    cmp cx, si
    jna 33
    jmp 34
33:
    add rdx, 8
34:
    cmp cx, si
    jne 35
    add rdx, 0x10
    jmp 36
35:
    add rdx, 0x30
36:
    cmp di, si
    jnae 37
    jmp 38
37:
    add rdx, 0x40
38:
    mov rax, rdx
    ret
batch u32:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    mov r8d, eax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    mov eax, edx
    add r8, rax
    mov rax, rcx
    shl eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rcx
    shr eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rdi
    shr eax, 2
    mov eax, eax
    add r8, rax
    cmp ecx, esi
    jnbe 40
    jmp 41
40:
    add r8, 1
41:
    mov rdx, r8
    cmp esi, ecx
    jnae 42
    jmp 43
42:
    add rdx, 2
43:
    cmp ecx, esi
    jnc 44
    jmp 45
44:
    add rdx, 4
45:
    cmp ecx, esi
    jna 46
    jmp +0x7
46:
    add rdx, 8
    cmp ecx, esi
    jne 47
    add rdx, 0x10
    jmp 48
47:
    add rdx, 0x30
48:
    cmp edi, esi
    jnae 49
    jmp 50
49:
    add rdx, 0x40
50:
    mov rax, rdx
    ret
batch i8:
    mov rcx, rdi
    mov rdi, rdx
    mov rdx, rcx
    mov rax, rsi
    imul dl
    movsx r8, al
    mov rax, rcx
    cbw
    idiv sil
    movsx rax, al
    mov r9, r8
    add r9, rax
    mov rax, rcx
    cbw
    idiv sil
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    cbw
    idiv r8b
    movsx rax, al
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    cbw
    idiv r8b
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov rdx, rcx
    and dl, sil
    movsx rax, dl
    mov r8, r9
    add r8, rax
    mov rdx, rcx
    or dl, sil
    movsx rax, dl
    add r8, rax
    mov rdx, rcx
    xor dl, sil
    movsx rax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dl
    and dl, al
    movsx rax, dl
    add r8, rax
    mov rax, rcx
    shl al, 2
    movsx rax, al
    add r8, rax
    mov rax, rcx
    sar al, 2
    movsx rax, al
    add r8, rax
    mov rax, rdi
    sar al, 2
    movsx rax, al
    add r8, rax
    cmp cl, sil
    jnle 52
    jmp 53
52:
    add r8, 1
53:
    cmp sil, cl
    jnge 54
    jmp 55
54:
    add r8, 2
55:
    cmp cl, sil
    jnl 56
    jmp 57
56:
    add r8, 4
57:
    cmp cl, sil
    jle 58
    jmp 59
58:
    add r8, 8
59:
    cmp cl, sil
    jne 60
    add r8, 0x10
    jmp 61
60:
    add r8, 0x30
61:
    cmp dil, sil
    jnge 62
    jmp 63
62:
    add r8, 0x40
63:
    mov rax, r8
    ret
batch i16:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movsx r8, ax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    shl ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    sar ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    sar ax, 2
    movsx rax, ax
    add r8, rax
    cmp cx, si
    jnle 65
    jmp 66
65:
    add r8, 1
66:
    mov rdx, r8
    cmp si, cx
    jnge 67
    jmp 68
67:
    add rdx, 2
68:
    cmp cx, si
    jnl 69
    jmp 70
69:
    add rdx, 4
70:
    cmp cx, si
    jle 71
    jmp 72
71:
    add rdx, 8
72:
    cmp cx, si
    jne 73
    add rdx, 0x10
    jmp 74
73:
    add rdx, 0x30
74:
    cmp di, si
    jnge 75
    jmp 76
75:
    add rdx, 0x40
76:
    mov rax, rdx
    ret
batch i32:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    movsxd r8, eax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    shl eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    cmp ecx, esi
    jnle 78
    jmp 79
78:
    add r8, 1
79:
    mov rdx, r8
    cmp esi, ecx
    jnge 80
    jmp 81
80:
    add rdx, 2
81:
    cmp ecx, esi
    jnl 82
    jmp 83
82:
    add rdx, 4
83:
    cmp ecx, esi
    jle 84
    jmp 85
84:
    add rdx, 8
85:
    cmp ecx, esi
    jne 86
    add rdx, 0x10
    jmp 87
86:
    add rdx, 0x30
87:
    cmp edi, esi
    jnge 88
    jmp 89
88:
    add rdx, 0x40
89:
    mov rax, rdx
    ret
=========== OPT LEVEL: all ===========
main:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0x14
    mov rsi, 6
    mov rdx, -7
    call batch int
    mov rbx, rax
    mov dl, 7
    mov dil, 0x14
    mov sil, 6
    call batch u8
    mov rbp, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call batch u16
    mov r12, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call batch u32
    mov r13, rax
    mov dl, -7
    mov dil, 0x14
    mov sil, 6
    call batch i8
    mov r14, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call batch i16
    mov r15, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call batch i32
    mov rdx, rbx
    add rdx, rbp
    add rdx, r12
    add rdx, r13
    add rdx, r14
    add rdx, r15
    add rdx, rax
    mov rax, rdx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
batch int:
    mov rcx, rdi
    mov rdi, rdx
    mov r8, rcx
    imul r8, rsi
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rax
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rax
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rcx
    and rax, rsi
    add r8, rax
    mov rax, rcx
    or rax, rsi
    add r8, rax
    mov rax, rcx
    xor rax, rsi
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not rdx
    and rdx, rax
    add r8, rdx
    mov rax, rcx
    shl rax, 2
    add r8, rax
    mov rax, rcx
    sar rax, 2
    add r8, rax
    mov rax, rdi
    sar rax, 2
    add r8, rax
    cmp rcx, rsi
    jnle 3
    jmp 4
3:
    add r8, 1
4:
    cmp rsi, rcx
    jnge 5
    jmp 6
5:
    add r8, 2
6:
    mov rdx, r8
    cmp rcx, rsi
    jnl 7
    jmp 8
7:
    add rdx, 4
8:
    cmp rcx, rsi
    jle 9
    jmp 10
9:
    add rdx, 8
10:
    cmp rcx, rsi
    jne 11
    add rdx, 0x10
    jmp 12
11:
    add rdx, 0x30
12:
    cmp rdi, rsi
    jnge 13
    jmp 14
13:
    add rdx, 0x40
14:
    mov rax, rdx
    ret
batch u8:
    mov rcx, rdi
    mov rdi, rdx
    mov rdx, rcx
    mov rax, rsi
    imul dl
    movzx r8d, al
    mov rax, rcx
    movzx eax, al
    div sil
    movzx eax, al
    mov r9, r8
    add r9, rax
    mov rax, rcx
    movzx eax, al
    div sil
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    movzx eax, al
    div r8b
    movzx eax, al
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    movzx eax, al
    div r8b
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov rdx, rcx
    and dl, sil
    movzx eax, dl
    mov r8, r9
    add r8, rax
    mov rdx, rcx
    or dl, sil
    movzx eax, dl
    add r8, rax
    mov rdx, rcx
    xor dl, sil
    movzx eax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dl
    and dl, al
    movzx eax, dl
    add r8, rax
    mov rax, rcx
    shl al, 2
    movzx eax, al
    add r8, rax
    mov rax, rcx
    shr al, 2
    movzx eax, al
    add r8, rax
    mov rax, rdi
    shr al, 2
    movzx eax, al
    add r8, rax
    cmp cl, sil
    jnbe 16
    jmp 17
16:
    add r8, 1
17:
    cmp sil, cl
    jnae 18
    jmp 19
18:
    add r8, 2
19:
    cmp cl, sil
    jnc +0x5
    jmp 20
    add r8, 4
20:
    cmp cl, sil
    jna 21
    jmp 22
21:
    add r8, 8
22:
    cmp cl, sil
    jne 23
    add r8, 0x10
    jmp 24
23:
    add r8, 0x30
24:
    cmp dil, sil
    jnae 25
    jmp 26
25:
    add r8, 0x40
26:
    mov rax, r8
    ret
batch u16:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movzx r8d, ax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    shl ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    shr ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    shr ax, 2
    movzx eax, ax
    add r8, rax
    cmp cx, si
    jnbe 28
    jmp 29
28:
    add r8, 1
29:
    mov rdx, r8
    cmp si, cx
    jnae 30
    jmp 31
30:
    add rdx, 2
31:
    cmp cx, si
    jnc +0x5
    jmp 32
    add rdx, 4
32:
    cmp cx, si
    jna 33
    jmp 34
33:
    add rdx, 8
34:
    cmp cx, si
    jne 35
    add rdx, 0x10
    jmp 36
35:
    add rdx, 0x30
36:
    cmp di, si
    jnae 37
    jmp 38
37:
    add rdx, 0x40
38:
    mov rax, rdx
    ret
batch u32:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    mov r8d, eax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    mov eax, edx
    add r8, rax
    mov rax, rcx
    shl eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rcx
    shr eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rdi
    shr eax, 2
    mov eax, eax
    add r8, rax
    cmp ecx, esi
    jnbe 40
    jmp 41
40:
    add r8, 1
41:
    mov rdx, r8
    cmp esi, ecx
    jnae 42
    jmp 43
42:
    add rdx, 2
43:
    cmp ecx, esi
    jnc 44
    jmp 45
44:
    add rdx, 4
45:
    cmp ecx, esi
    jna 46
    jmp +0x7
46:
    add rdx, 8
    cmp ecx, esi
    jne 47
    add rdx, 0x10
    jmp 48
47:
    add rdx, 0x30
48:
    cmp edi, esi
    jnae 49
    jmp 50
49:
    add rdx, 0x40
50:
    mov rax, rdx
    ret
batch i8:
    mov rcx, rdi
    mov rdi, rdx
    mov rdx, rcx
    mov rax, rsi
    imul dl
    movsx r8, al
    mov rax, rcx
    cbw
    idiv sil
    movsx rax, al
    mov r9, r8
    add r9, rax
    mov rax, rcx
    cbw
    idiv sil
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    cbw
    idiv r8b
    movsx rax, al
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    cbw
    idiv r8b
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov rdx, rcx
    and dl, sil
    movsx rax, dl
    mov r8, r9
    add r8, rax
    mov rdx, rcx
    or dl, sil
    movsx rax, dl
    add r8, rax
    mov rdx, rcx
    xor dl, sil
    movsx rax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dl
    and dl, al
    movsx rax, dl
    add r8, rax
    mov rax, rcx
    shl al, 2
    movsx rax, al
    add r8, rax
    mov rax, rcx
    sar al, 2
    movsx rax, al
    add r8, rax
    mov rax, rdi
    sar al, 2
    movsx rax, al
    add r8, rax
    cmp cl, sil
    jnle 52
    jmp 53
52:
    add r8, 1
53:
    cmp sil, cl
    jnge 54
    jmp 55
54:
    add r8, 2
55:
    cmp cl, sil
    jnl 56
    jmp 57
56:
    add r8, 4
57:
    cmp cl, sil
    jle 58
    jmp 59
58:
    add r8, 8
59:
    cmp cl, sil
    jne 60
    add r8, 0x10
    jmp 61
60:
    add r8, 0x30
61:
    cmp dil, sil
    jnge 62
    jmp 63
62:
    add r8, 0x40
63:
    mov rax, r8
    ret
batch i16:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movsx r8, ax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    shl ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    sar ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    sar ax, 2
    movsx rax, ax
    add r8, rax
    cmp cx, si
    jnle 65
    jmp 66
65:
    add r8, 1
66:
    mov rdx, r8
    cmp si, cx
    jnge 67
    jmp 68
67:
    add rdx, 2
68:
    cmp cx, si
    jnl 69
    jmp 70
69:
    add rdx, 4
70:
    cmp cx, si
    jle 71
    jmp 72
71:
    add rdx, 8
72:
    cmp cx, si
    jne 73
    add rdx, 0x10
    jmp 74
73:
    add rdx, 0x30
74:
    cmp di, si
    jnge 75
    jmp 76
75:
    add rdx, 0x40
76:
    mov rax, rdx
    ret
batch i32:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    movsxd r8, eax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    shl eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    cmp ecx, esi
    jnle 78
    jmp 79
78:
    add r8, 1
79:
    mov rdx, r8
    cmp esi, ecx
    jnge 80
    jmp 81
80:
    add rdx, 2
81:
    cmp ecx, esi
    jnl 82
    jmp 83
82:
    add rdx, 4
83:
    cmp ecx, esi
    jle 84
    jmp 85
84:
    add rdx, 8
85:
    cmp ecx, esi
    jne 86
    add rdx, 0x10
    jmp 87
86:
    add rdx, 0x30
87:
    cmp edi, esi
    jnge 88
    jmp 89
88:
    add rdx, 0x40
89:
    mov rax, rdx
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 0x9dc
    ret
