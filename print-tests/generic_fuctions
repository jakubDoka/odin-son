=========== OPT LEVEL: none ===========
main:
    push rbx
    mov rdi, 0xa
    call +0x22
    mov rbx, rax
    mov rdi, 0xa
    call +0x6b
    mov rcx, rax
    movsxd rax, ebx
    add rax, rcx
    pop rbx
    ret
fib:
fib i32:
1:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp ebx, 2
    jle 0
    mov rax, rbx
    sub eax, 1
    mov rdi, rax
    call 1
    mov rbp, rax
    sub ebx, 2
    mov rdi, rbx
    call 1
    add ebp, eax
    jmp 2
0:
    mov rbp, 1
2:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
fib int:
1:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jle 0
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call 1
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call 1
    add rbp, rax
    jmp 2
0:
    mov rbp, 1
2:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
main:
    push rbx
    mov rdi, 0xa
    call +0x22
    mov rbx, rax
    mov rdi, 0xa
    call +0x6b
    mov rcx, rax
    movsxd rax, ebx
    add rax, rcx
    pop rbx
    ret
fib:
fib i32:
1:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp ebx, 2
    jle 0
    mov rax, rbx
    sub eax, 1
    mov rdi, rax
    call 1
    mov rbp, rax
    sub ebx, 2
    mov rdi, rbx
    call 1
    add ebp, eax
    jmp 2
0:
    mov rbp, 1
2:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
fib int:
1:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jle 0
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call 1
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call 1
    add rbp, rax
    jmp 2
0:
    mov rbp, 1
2:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
main:
    push rbx
    mov rdi, 0xa
    call +0x22
    mov rbx, rax
    mov rdi, 0xa
    call +0x6b
    mov rcx, rax
    movsxd rax, ebx
    add rax, rcx
    pop rbx
    ret
fib:
fib i32:
1:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp ebx, 2
    jle 0
    mov rax, rbx
    sub eax, 1
    mov rdi, rax
    call 1
    mov rbp, rax
    sub ebx, 2
    mov rdi, rbx
    call 1
    add ebp, eax
    jmp 2
0:
    mov rbp, 1
2:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
fib int:
1:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jle 0
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call 1
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call 1
    add rbp, rax
    jmp 2
0:
    mov rbp, 1
2:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: all ===========
main:
    push rbx
    mov rdi, 0xa
    call +0x22
    mov rbx, rax
    mov rdi, 0xa
    call +0x6b
    mov rcx, rax
    movsxd rax, ebx
    add rax, rcx
    pop rbx
    ret
fib:
fib i32:
1:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp ebx, 2
    jle 0
    mov rax, rbx
    sub eax, 1
    mov rdi, rax
    call 1
    mov rbp, rax
    sub ebx, 2
    mov rdi, rbx
    call 1
    add ebp, eax
    jmp 2
0:
    mov rbp, 1
2:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
fib int:
1:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jle 0
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call 1
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call 1
    add rbp, rax
    jmp 2
0:
    mov rbp, 1
2:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
