=========== OPT LEVEL: none-quick ===========
main:
    push rbx
    sub rsp, 0x10
    mov rcx, 4
    mov rbx, 7
    mov rax, 0x64
    cqo
    idiv rbx
    mov rax, rax
    cqo
    idiv rcx
    mov.q [rsp], rdx
    mov rax, 0x64
    xor rdx, rdx
    div rbx
    mov rax, rax
    xor rdx, rdx
    div rcx
    mov.q rax, [rsp]
    add rax, rdx
    mov rax, rax
    add rsp, 0x10
    pop rbx
    ret
=========== OPT LEVEL: none ===========
main:
    mov rdi, 4
    mov rsi, 7
    mov rax, 0x64
    cqo
    idiv rsi
    cqo
    idiv rdi
    mov rcx, rdx
    mov rax, 0x64
    xor rdx, rdx
    div rsi
    xor rdx, rdx
    div rdi
    add rcx, rdx
    mov rax, rcx
    ret
=========== OPT LEVEL: mininal-quick ===========
main:
    push rbx
    sub rsp, 0x10
    mov rcx, 4
    mov rbx, 7
    mov rax, 0x64
    cqo
    idiv rbx
    mov rax, rax
    cqo
    idiv rcx
    mov.q [rsp], rdx
    mov rax, 0x64
    xor rdx, rdx
    div rbx
    mov rax, rax
    xor rdx, rdx
    div rcx
    mov.q rax, [rsp]
    add rax, rdx
    mov rax, rax
    add rsp, 0x10
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
main:
    mov rdi, 4
    mov rsi, 7
    mov rax, 0x64
    cqo
    idiv rsi
    cqo
    idiv rdi
    mov rcx, rdx
    mov rax, 0x64
    xor rdx, rdx
    div rsi
    xor rdx, rdx
    div rdi
    add rcx, rdx
    mov rax, rcx
    ret
=========== OPT LEVEL: moderate-quick ===========
main:
    mov rax, 4
    ret
=========== OPT LEVEL: moderate ===========
main:
    mov rax, 4
    ret
=========== OPT LEVEL: all ===========
main:
    mov rax, 4
    ret
=========== OPT LEVEL: aggresive ===========
