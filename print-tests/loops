=========== OPT LEVEL: none ===========
main:
     mov rcx, 0x0A
     mov rsi, 0x00
     mov rax, 0x01
002: cmp rcx, 0x00
     jnz :0
     jmp :1

000: mov rdx, rsi
     add rdx, rax
     sub rcx, 0x01
     mov rsi, rax
     mov rax, rdx
     jmp :2

001: ret
=========== OPT LEVEL: mininal ===========
main:
     mov rcx, 0x0A
     mov rsi, 0x00
     mov rax, 0x01
002: cmp rcx, 0x00
     jnz :0
     jmp :1

000: mov rdx, rsi
     add rdx, rax
     sub rcx, 0x01
     mov rsi, rax
     mov rax, rdx
     jmp :2

001: ret
=========== OPT LEVEL: all ===========
main:
     mov rcx, 0x0A
     mov rsi, 0x00
     mov rax, 0x01
002: cmp rcx, 0x00
     jnz :0
     jmp :1

000: mov rdx, rsi
     add rdx, rax
     sub rcx, 0x01
     mov rsi, rax
     mov rax, rdx
     jmp :2

001: ret
