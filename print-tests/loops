=========== OPT LEVEL: None ===========
main:
     push rbx
     push rbp
     mov rcx, 0x00
     mov rdx, 0x01
     mov rbp, 0x00
     mov rsi, 0x0A
     mov rax, 0x01
     jmp :0

000: mov rbx, rsi
     cmp rbx, rcx
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :1
     jmp :2

001: mov rbx, rbp
     add rbx, rax
     sub rsi, rdx
     mov rbp, rax
     mov rax, rbx
     jmp :0

002: pop rbp
     pop rbx
     ret
=========== OPT LEVEL: Full ===========
main:
     push rbx
     push rbp
     mov rcx, 0x00
     mov rbx, 0x00
     mov rbp, 0x0A
     mov rax, 0x01
     jmp :0

000: mov rdx, rbp
     cmp rdx, rcx
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :1
     jmp :2

001: mov rdx, rbx
     add rdx, rax
     sub rbp, 0x01
     mov rbx, rax
     mov rax, rdx
     jmp :0

002: pop rbp
     pop rbx
     ret
