=========== OPT LEVEL: none ===========
main:
    mov rax, 1
    mov rcx, 1
    add rcx, 1
    lea rax, [rax + rcx + 0x1]
    add rax, rax
    add rax, rax
    add rax, rax
    ret
=========== OPT LEVEL: mininal ===========
main:
    mov rax, 0x20
    ret
=========== OPT LEVEL: moderate ===========
main:
    mov rax, 0x20
    ret
=========== OPT LEVEL: all ===========
main:
    mov rax, 0x20
    ret
=========== OPT LEVEL: aggresive ===========
