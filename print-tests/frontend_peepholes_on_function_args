=========== OPT LEVEL: none ===========
main:
     mov rax, 0x02
     mov rcx, 0x02
     add rcx, rax
     mov rax, 0x02
     mov rdx, rcx
     add rdx, rax
     mov rdi, 0x02
     mov rsi, rcx
     call :funnel
     ret
funnel:
     mov rax, rdi
     add rax, rsi
     add rax, rdx
     ret
=========== OPT LEVEL: mininal ===========
main:
     mov rdi, 0x02
     mov rsi, 0x04
     mov rdx, 0x06
     call :funnel
     ret
funnel:
     mov rax, rdi
     add rax, rsi
     add rax, rdx
     ret
=========== OPT LEVEL: all ===========
main:
     mov rdi, 0x02
     mov rsi, 0x04
     mov rdx, 0x06
     call :funnel
     ret
funnel:
     mov rax, rdi
     add rax, rsi
     add rax, rdx
     ret
