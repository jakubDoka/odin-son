=========== OPT LEVEL: none ===========
main:
    mov rcx, 6
    mov rax, 0x14
    xor rdx, rdx
    div rcx
    cmp rax, 3
    jz 1
    xor esi, esi
    jmp 2
1:
    xor esi, esi
    add rsi, 1
2:
    mov rax, 0x14
    xor rdx, rdx
    div rcx
    cmp rdx, 2
    jz 3
    jmp 4
3:
    add rsi, 2
4:
    xor eax, eax
    sub rax, 7
    mov rdi, rax
    mov rax, rdi
    xor rdx, rdx
    div rcx
    cmp rax, 0x14
    jnbe 5
    jmp 6
5:
    add rsi, 4
6:
    mov rax, rdi
    xor rdx, rdx
    div rcx
    cmp rdx, 3
    jz 7
    jmp 8
7:
    add rsi, 8
8:
    mov rax, rdi
    shr rax, 0x3c
    cmp rax, 0xf
    jz 9
    jmp 10
9:
    add rsi, 0x10
10:
    mov rax, 0x14
    shr rax, 2
    cmp rax, 5
    jz 11
    jmp 12
11:
    add rsi, 0x20
12:
    cmp rdi, 6
    jnbe 13
    jmp 14
13:
    add rsi, 0x40
14:
    cmp rdi, 0x14
    jnc 15
    jmp 16
15:
    add rsi, 0x80
16:
    cmp rcx, rdi
    jnae 17
    jmp 18
17:
    add rsi, 0x100
18:
    mov rdx, rsi
    cmp rcx, rdi
    jna 19
    jmp 20
19:
    add rdx, 0x200
20:
    mov rax, 0x14
    cmp rax, rdi
    jnae 21
    jmp 22
21:
    add rdx, 0x400
22:
    mov rax, rdx
    ret
=========== OPT LEVEL: mininal ===========
main:
    mov cl, 1
    test cl, cl
    jne 1
    xor esi, esi
    jmp 2
1:
    xor esi, esi
    add rsi, 1
2:
    test cl, cl
    jne 3
    jmp 4
3:
    add rsi, 2
4:
    mov rdi, 6
    xor eax, eax
    sub rax, 7
    mov r8, rax
    mov rax, r8
    xor rdx, rdx
    div rdi
    cmp rax, 0x14
    jnbe 5
    jmp 6
5:
    add rsi, 4
6:
    mov rax, r8
    xor rdx, rdx
    div rdi
    cmp rdx, 3
    jz 7
    jmp 8
7:
    add rsi, 8
8:
    mov rax, r8
    shr rax, 0x3c
    cmp rax, 0xf
    jz 9
    jmp 10
9:
    add rsi, 0x10
10:
    test cl, cl
    jne 11
    jmp 12
11:
    add rsi, 0x20
12:
    cmp r8, 6
    setnbe al
    movzx rax, al
    test al, al
    jne 13
    jmp 14
13:
    add rsi, 0x40
14:
    cmp r8, 0x14
    jnc 15
    jmp 16
15:
    add rsi, 0x80
16:
    test al, al
    jne 17
    jmp 18
17:
    add rsi, 0x100
18:
    cmp r8, 6
    jnc 19
    jmp 20
19:
    add rsi, 0x200
20:
    cmp r8, 0x14
    jnbe 21
    jmp 22
21:
    add rsi, 0x400
22:
    mov rax, rsi
    ret
=========== OPT LEVEL: moderate ===========
main:
    mov rax, 0x7ff
    ret
=========== OPT LEVEL: all ===========
main:
    mov rax, 0x7ff
    ret
=========== OPT LEVEL: aggresive ===========
