=========== OPT LEVEL: none ===========
malloc:
free:
main:
     push rbx
     mov rdi, 0x08
     call :$malloc
     mov qword ptr [rax], 0x00
     mov rbx, [rax]
     mov rdi, rax
     call :$free
     mov rax, rbx
     pop rbx
     ret
=========== OPT LEVEL: mininal ===========
malloc:
free:
main:
     push rbx
     mov rdi, 0x08
     call :$malloc
     mov qword ptr [rax], 0x00
     mov rbx, [rax]
     mov rdi, rax
     call :$free
     mov rax, rbx
     pop rbx
     ret
=========== OPT LEVEL: moderate ===========
malloc:
free:
main:
     sub rsp, 0x08
     mov rdi, 0x08
     call :$malloc
     mov qword ptr [rax], 0x00
     mov rdi, rax
     call :$free
     xor eax, eax
     add rsp, 0x08
     ret
=========== OPT LEVEL: all ===========
malloc:
free:
main:
     sub rsp, 0x08
     mov rdi, 0x08
     call :$malloc
     mov qword ptr [rax], 0x00
     mov rdi, rax
     call :$free
     xor eax, eax
     add rsp, 0x08
     ret
=========== OPT LEVEL: aggresive ===========
