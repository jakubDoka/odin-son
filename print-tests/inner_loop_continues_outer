=========== OPT LEVEL: none ===========
main:
    xor eax, eax
    xor edx, edx
1:
    add rdx, 1
    cmp rdx, 5
    jz 5
    xor ecx, ecx
    add rcx, 1
    cmp rcx, 5
    jz 3
    mov rcx, rax
    xor eax, eax
2:
    add rax, 1
    add rcx, 1
    cmp rax, 2
    jz 4
    jmp 2
3:
    mov rcx, rax
4:
    mov rax, rcx
    jmp 1
5:
    ret
=========== OPT LEVEL: mininal ===========
main:
    xor eax, eax
    xor edx, edx
1:
    add rdx, 1
    cmp rdx, 5
    jz 5
    xor ecx, ecx
    add rcx, 1
    cmp rcx, 5
    jz 3
    mov rcx, rax
    xor eax, eax
2:
    add rax, 1
    add rcx, 1
    cmp rax, 2
    jz 4
    jmp 2
3:
    mov rcx, rax
4:
    mov rax, rcx
    jmp 1
5:
    ret
=========== OPT LEVEL: moderate ===========
main:
    xor eax, eax
    xor edx, edx
1:
    add rdx, 1
    cmp rdx, 5
    jz 3
    xor ecx, ecx
2:
    add rcx, 1
    add rax, 1
    cmp rcx, 2
    jz 1
    jmp 2
3:
    ret
=========== OPT LEVEL: all ===========
main:
    xor ecx, ecx
    xor esi, esi
1:
    xor edx, edx
2:
    add rdx, 1
    mov rax, rcx
    add rax, 1
    cmp rdx, 2
    mov rcx, rax
    jnz 2
    mov rcx, rsi
    add rcx, 2
    add rsi, 1
    cmp rcx, 5
    mov rcx, rax
    jnz 1
    ret
=========== OPT LEVEL: aggresive ===========
