=========== OPT LEVEL: none ===========
main:
     push rbx
     mov rdi, 0x0A
     call :fib i32
     mov rbx, rax
     mov rdi, 0x0A
     call :fib int
     mov rcx, rax
     movsxd rax, ebx
     add rax, rcx
     pop rbx
     ret
fib:
fib i32:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp ebx, 0x02
     jle :0
     mov rax, rbx
     sub eax, 0x01
     mov rdi, rax
     call :fib i32
     mov rbp, rax
     sub ebx, 0x02
     mov rdi, rbx
     call :fib i32
     add ebp, eax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
fib int:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp rbx, 0x02
     jle :0
     mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib int
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib int
     add rbp, rax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: mininal ===========
main:
     push rbx
     mov rdi, 0x0A
     call :fib i32
     mov rbx, rax
     mov rdi, 0x0A
     call :fib int
     mov rcx, rax
     movsxd rax, ebx
     add rax, rcx
     pop rbx
     ret
fib:
fib i32:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp ebx, 0x02
     jle :0
     mov rax, rbx
     sub eax, 0x01
     mov rdi, rax
     call :fib i32
     mov rbp, rax
     sub ebx, 0x02
     mov rdi, rbx
     call :fib i32
     add ebp, eax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
fib int:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp rbx, 0x02
     jle :0
     mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib int
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib int
     add rbp, rax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: moderate ===========
main:
     push rbx
     mov rdi, 0x0A
     call :fib i32
     mov rbx, rax
     mov rdi, 0x0A
     call :fib int
     mov rcx, rax
     movsxd rax, ebx
     add rax, rcx
     pop rbx
     ret
fib:
fib i32:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp ebx, 0x02
     jle :0
     mov rax, rbx
     sub eax, 0x01
     mov rdi, rax
     call :fib i32
     mov rbp, rax
     sub ebx, 0x02
     mov rdi, rbx
     call :fib i32
     add ebp, eax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
fib int:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp rbx, 0x02
     jle :0
     mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib int
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib int
     add rbp, rax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: all ===========
main:
     push rbx
     mov rdi, 0x0A
     call :fib i32
     mov rbx, rax
     mov rdi, 0x0A
     call :fib int
     mov rcx, rax
     movsxd rax, ebx
     add rax, rcx
     pop rbx
     ret
fib:
fib i32:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp ebx, 0x02
     jle :0
     mov rax, rbx
     sub eax, 0x01
     mov rdi, rax
     call :fib i32
     mov rbp, rax
     sub ebx, 0x02
     mov rdi, rbx
     call :fib i32
     add ebp, eax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
fib int:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp rbx, 0x02
     jle :0
     mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib int
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib int
     add rbp, rax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: aggresive ===========
