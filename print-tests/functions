=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call 2
    add rsp, 8
    ret
2:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jng 3
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call 2
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call 2
    add rbp, rax
    jmp 4
3:
    mov rbp, 1
4:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call 2
    add rsp, 8
    ret
2:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jng 3
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call 2
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call 2
    add rbp, rax
    jmp 4
3:
    mov rbp, 1
4:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call 2
    add rsp, 8
    ret
2:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jng 3
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call 2
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call 2
    add rbp, rax
    jmp 4
3:
    mov rbp, 1
4:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, 0xa
    call 2
    add rsp, 8
    ret
2:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jng 3
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call 2
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call 2
    add rbp, rax
    jmp 4
3:
    mov rbp, 1
4:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
