     push rcx
     push rbp
     mov rbp, 0x0A
     mov rcx, 0x00
     mov rdx, 0x01
     mov rax, rdx
     mov rsi, rcx
     jmp :0

000: mov rbx, rbp
     cmp rbx, rcx
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :1
     jmp :2

001: mov rbx, rsi
     add rbx, rax
     sub rbp, rdx
     mov rsi, rax
     mov rax, rbx
     jmp :0

002: pop rbp
     pop rcx
     ret
