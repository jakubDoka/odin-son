=========== OPT LEVEL: none ===========
opaque:
     mov rax, rdi
     ret
main:
     push rbx
     push rbp
     push r12
     push r13
     push r14
     push r15
     sub rsp, 0x08
     mov rdi, 0x00
     sub rdi, 0x64
     call :opaque
     mov rbx, rax
     mov rax, 0x00
     sub rax, 0xBB8
     mov rdi, rax
     call :opaque
     mov rbp, rax
     mov rax, 0x00
     sub rax, 0x11170
     mov rdi, rax
     call :opaque
     mov r12, rax
     mov rax, 0x00
     sub rax, 0x4C4B40
     mov rdi, rax
     call :opaque
     mov r13, rax
     mov rdi, 0xC8
     call :opaque
     mov r14, rax
     mov rdi, 0xEA60
     call :opaque
     mov r15, rax
     mov rdi, 0x3D0900
     call :opaque
     mov [rsp], rax
     mov rdi, 0x6ACFC0
     call :opaque
     movsd xmm1, qword ptr [0x00000000004002A2]
     mov rax, rax
     mov rcx, rbx
     movsx rcx, cl
     movsd xmm0, qword ptr [0x00000000004002AC]
     mov rdx, r13
     movss xmm2, dword ptr [0x00000000004002B6]
     cvtss2sd xmm3, xmm2
     mov rsi, [rsp]
     mov esi, esi
     movsd xmm2, qword ptr [0x00000000004002BC]
     subsd xmm0, xmm2
     mov rdi, rbp
     movsx rdi, di
     movss xmm2, dword ptr [0x00000000004002C6]
     mov r8, r14
     movzx r8d, r8b
     movsd xmm4, qword ptr [0x00000000004002CC]
     addsd xmm3, xmm4
     cvtsd2ss xmm3, xmm3
     cvttss2si r10, xmm3
     mov r9, 0x00
     add r10, r9
     cvtsi2ss xmm3, rcx
     cvttss2si r9, xmm3
     add r10, r9
     mov r9, r12
     movsxd r9, r9d
     cvtsi2sd xmm3, rcx
     cvttsd2si rcx, xmm3
     add r10, rcx
     mov rcx, r15
     movzx ecx, cx
     cvtsi2ss xmm3, rdi
     cvttss2si r11, xmm3
     add r10, r11
     cvtsi2sd xmm3, rdi
     cvttsd2si rdi, xmm3
     add r10, rdi
     cvtsi2ss xmm3, r9
     cvttss2si rdi, xmm3
     add r10, rdi
     cvtsi2sd xmm3, r9
     cvttsd2si rdi, xmm3
     add r10, rdi
     cvtsi2ss xmm3, rdx
     cvttss2si rdi, xmm3
     add r10, rdi
     cvtsi2sd xmm3, rdx
     cvttsd2si rdx, xmm3
     add r10, rdx
     mov rdi, r10
     cvtsi2ss xmm3, r8
     cvttss2si rdx, xmm3
     add rdi, rdx
     cvtsi2sd xmm3, r8
     cvttsd2si rdx, xmm3
     add rdi, rdx
     cvtsi2ss xmm3, rcx
     cvttss2si rdx, xmm3
     add rdi, rdx
     cvtsi2sd xmm3, rcx
     cvttsd2si rcx, xmm3
     add rdi, rcx
     cvtsi2ss xmm3, rsi
     cvttss2si rcx, xmm3
     add rdi, rcx
     cvtsi2sd xmm3, rsi
     cvttsd2si rcx, xmm3
     add rdi, rcx
     cvtsi2ss xmm3, rax
     cvttss2si rcx, xmm3
     add rdi, rcx
     cvtsi2sd xmm3, rax
     cvttsd2si rax, xmm3
     add rdi, rax
     cvttss2si eax, xmm2
     movsxd rax, eax
     add rdi, rax
     mov rcx, rdi
     cvttss2si rax, xmm2
     mov rax, rax
     add rcx, rax
     cvttsd2si eax, xmm0
     movsxd rax, eax
     add rcx, rax
     cvttsd2si rax, xmm0
     mov rax, rax
     add rcx, rax
     cvttsd2si eax, xmm1
     movzx eax, al
     add rcx, rax
     movss xmm0, dword ptr [0x00000000004002D6]
     cvttss2si eax, xmm0
     movzx eax, ax
     add rcx, rax
     cvttsd2si eax, xmm1
     mov eax, eax
     add rcx, rax
     cvttsd2si rax, xmm1
     mov rax, rax
     add rcx, rax
     mov rax, rcx
     add rsp, 0x08
     pop r15
     pop r14
     pop r13
     pop r12
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: mininal ===========
opaque:
     mov rax, rdi
     ret
main:
     push rbx
     push rbp
     push r12
     push r13
     push r14
     push r15
     sub rsp, 0x08
     mov rdi, 0xFFFFFFFFFFFFFF9C
     call :opaque
     mov rbx, rax
     mov rdi, 0xFFFFFFFFFFFFF448
     call :opaque
     mov rbp, rax
     mov rdi, 0xFFFFFFFFFFFEEE90
     call :opaque
     mov r12, rax
     mov rdi, 0xFFFFFFFFFFB3B4C0
     call :opaque
     mov r13, rax
     mov rdi, 0xC8
     call :opaque
     mov r14, rax
     mov rdi, 0xEA60
     call :opaque
     mov r15, rax
     mov rdi, 0x3D0900
     call :opaque
     mov [rsp], rax
     mov rdi, 0x6ACFC0
     call :opaque
     mov r10, rax
     mov rax, rbx
     movsx rcx, al
     mov rax, 0x00
     mov rdx, [rsp]
     mov edx, edx
     mov rsi, r14
     movzx esi, sil
     mov rdi, rbp
     movsx rdi, di
     mov r8, r12
     movsxd r8, r8d
     mov r9, r15
     movzx r9d, r9w
     cvtsi2ss xmm0, rcx
     cvttss2si r11, xmm0
     lea rax, [rax+r11*1+0x06]
     cvtsi2sd xmm0, rcx
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, rdi
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, rdi
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, r8
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, r8
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, r13
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, r13
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, rsi
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, rsi
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, r9
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, r9
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, rdx
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, rdx
     cvttsd2si rcx, xmm0
     add rax, rcx
     cvtsi2ss xmm0, r10
     cvttss2si rcx, xmm0
     add rax, rcx
     cvtsi2sd xmm0, r10
     cvttsd2si rcx, xmm0
     add rax, rcx
     add rax, 0x07
     add rax, 0x07
     add rax, 0xFFFFFFFFFFFFFFF4
     add rax, 0xFFFFFFFFFFFFFFF4
     add rax, 0xFA
     add rax, 0xFDE8
     add rax, 0xFA
     add rax, 0xFA
     add rsp, 0x08
     pop r15
     pop r14
     pop r13
     pop r12
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: moderate ===========
opaque:
     mov rax, rdi
     ret
main:
     push rbx
     push rbp
     push r12
     push r13
     push r14
     push r15
     sub rsp, 0x08
     mov rdi, 0xFFFFFFFFFFFFFF9C
     call :opaque
     mov rbx, rax
     mov rdi, 0xFFFFFFFFFFFFF448
     call :opaque
     mov rbp, rax
     mov rdi, 0xFFFFFFFFFFFEEE90
     call :opaque
     mov r12, rax
     mov rdi, 0xFFFFFFFFFFB3B4C0
     call :opaque
     mov r13, rax
     mov rdi, 0xC8
     call :opaque
     mov r14, rax
     mov rdi, 0xEA60
     call :opaque
     mov r15, rax
     mov rdi, 0x3D0900
     call :opaque
     mov [rsp], rax
     mov rdi, 0x6ACFC0
     call :opaque
     mov r8, rax
     mov rax, r14
     movzx eax, al
     mov rcx, rbx
     movsx rdi, cl
     mov rcx, rbp
     movsx rcx, cx
     mov rdx, [rsp]
     mov edx, edx
     cvtsi2ss xmm0, rdi
     cvttss2si r9, xmm0
     mov rsi, r12
     movsxd rsi, esi
     cvtsi2sd xmm0, rdi
     cvttsd2si rdi, xmm0
     lea r10, [r9+rdi*1+0x06]
     mov rdi, r15
     movzx edi, di
     cvtsi2ss xmm0, rcx
     cvttss2si r9, xmm0
     add r10, r9
     mov r9, r10
     cvtsi2sd xmm0, rcx
     cvttsd2si rcx, xmm0
     add r9, rcx
     cvtsi2ss xmm0, rsi
     cvttss2si rcx, xmm0
     add r9, rcx
     cvtsi2sd xmm0, rsi
     cvttsd2si rcx, xmm0
     add r9, rcx
     cvtsi2ss xmm0, r13
     cvttss2si rcx, xmm0
     add r9, rcx
     cvtsi2sd xmm0, r13
     cvttsd2si rcx, xmm0
     add r9, rcx
     mov rsi, r9
     cvtsi2ss xmm0, rax
     cvttss2si rcx, xmm0
     add rsi, rcx
     cvtsi2sd xmm0, rax
     cvttsd2si rax, xmm0
     add rsi, rax
     cvtsi2ss xmm0, rdi
     cvttss2si rax, xmm0
     add rsi, rax
     cvtsi2sd xmm0, rdi
     cvttsd2si rax, xmm0
     add rsi, rax
     cvtsi2ss xmm0, rdx
     cvttss2si rax, xmm0
     add rsi, rax
     cvtsi2sd xmm0, rdx
     cvttsd2si rax, xmm0
     add rsi, rax
     cvtsi2ss xmm0, r8
     cvttss2si rax, xmm0
     add rsi, rax
     cvtsi2sd xmm0, r8
     cvttsd2si rax, xmm0
     add rsi, rax
     mov rax, rsi
     add rax, 0x100CC
     add rsp, 0x08
     pop r15
     pop r14
     pop r13
     pop r12
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: all ===========
opaque:
     mov rax, rdi
     ret
main:
     push rbx
     push rbp
     push r12
     push r13
     push r14
     push r15
     sub rsp, 0x08
     mov rdi, 0xFFFFFFFFFFFFFF9C
     call :opaque
     mov rbx, rax
     mov rdi, 0xFFFFFFFFFFFFF448
     call :opaque
     mov rbp, rax
     mov rdi, 0xFFFFFFFFFFFEEE90
     call :opaque
     mov r12, rax
     mov rdi, 0xFFFFFFFFFFB3B4C0
     call :opaque
     mov r13, rax
     mov rdi, 0xC8
     call :opaque
     mov r14, rax
     mov rdi, 0xEA60
     call :opaque
     mov r15, rax
     mov rdi, 0x3D0900
     call :opaque
     mov [rsp], rax
     mov rdi, 0x6ACFC0
     call :opaque
     mov r8, rax
     mov rax, r14
     movzx eax, al
     mov rcx, rbx
     movsx rdi, cl
     mov rcx, rbp
     movsx rcx, cx
     mov rdx, [rsp]
     mov edx, edx
     cvtsi2ss xmm0, rdi
     cvttss2si r9, xmm0
     mov rsi, r12
     movsxd rsi, esi
     cvtsi2sd xmm0, rdi
     cvttsd2si rdi, xmm0
     lea r10, [r9+rdi*1+0x06]
     mov rdi, r15
     movzx edi, di
     cvtsi2ss xmm0, rcx
     cvttss2si r9, xmm0
     add r10, r9
     mov r9, r10
     cvtsi2sd xmm0, rcx
     cvttsd2si rcx, xmm0
     add r9, rcx
     cvtsi2ss xmm0, rsi
     cvttss2si rcx, xmm0
     add r9, rcx
     cvtsi2sd xmm0, rsi
     cvttsd2si rcx, xmm0
     add r9, rcx
     cvtsi2ss xmm0, r13
     cvttss2si rcx, xmm0
     add r9, rcx
     cvtsi2sd xmm0, r13
     cvttsd2si rcx, xmm0
     add r9, rcx
     mov rsi, r9
     cvtsi2ss xmm0, rax
     cvttss2si rcx, xmm0
     add rsi, rcx
     cvtsi2sd xmm0, rax
     cvttsd2si rax, xmm0
     add rsi, rax
     cvtsi2ss xmm0, rdi
     cvttss2si rax, xmm0
     add rsi, rax
     cvtsi2sd xmm0, rdi
     cvttsd2si rax, xmm0
     add rsi, rax
     cvtsi2ss xmm0, rdx
     cvttss2si rax, xmm0
     add rsi, rax
     cvtsi2sd xmm0, rdx
     cvttsd2si rax, xmm0
     add rsi, rax
     cvtsi2ss xmm0, r8
     cvttss2si rax, xmm0
     add rsi, rax
     cvtsi2sd xmm0, r8
     cvttsd2si rax, xmm0
     add rsi, rax
     mov rax, rsi
     add rax, 0x100CC
     add rsp, 0x08
     pop r15
     pop r14
     pop r13
     pop r12
     pop rbp
     pop rbx
     ret
