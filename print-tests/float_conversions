=========== OPT LEVEL: none ===========
opaque:
    mov rax, rdi
    ret
main:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    xor edi, edi
    sub rdi, 0x64
    call -0x27
    mov rbx, rax
    xor eax, eax
    sub rax, 0xbb8
    mov rdi, rax
    call -0x3c
    mov rbp, rax
    xor eax, eax
    sub rax, 0x11170
    mov rdi, rax
    call -0x51
    mov r12, rax
    xor eax, eax
    sub rax, 0x4c4b40
    mov rdi, rax
    call -0x66
    mov r13, rax
    mov rdi, 0xc8
    call -0x75
    mov r14, rax
    mov rdi, 0xea60
    call -0x84
    mov r15, rax
    mov rdi, 0x3d0900
    call -0x93
    mov.q [rsp], rax
    mov rdi, 0x6acfc0
    call -0xa3
    mov r10, rax
    movsd.q xmm1, [rip + 0x1ab]
    mov rax, rbx
    movsx rcx, al
    mov.q rax, [rsp]
    mov edx, eax
    movss.d xmm0, [rip + 0x19e]
    cvtss2sd xmm0, xmm0
    mov rax, rbp
    movsx rsi, ax
    mov rax, r12
    movsxd rdi, eax
    mov rax, r14
    movzx r8d, al
    mov rax, r15
    movzx r9d, ax
    addsd.q xmm0, [rip + 0x17b]
    cvtsd2ss xmm0, xmm0
    cvttss2si rax, xmm0
    xor r11d, r11d
    add rax, r11
    movss.d xmm2, [rip + 0x16c]
    cvtsi2ss xmm0, rcx
    cvttss2si r11, xmm0
    add rax, r11
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    movsd.q xmm0, [rip + 0x14f]
    subsd.q xmm0, [rip + 0x150]
    cvtsi2ss xmm3, rsi
    cvttss2si rcx, xmm3
    add rax, rcx
    cvtsi2sd xmm3, rsi
    cvttsd2si rcx, xmm3
    add rax, rcx
    cvtsi2ss xmm3, rdi
    cvttss2si rcx, xmm3
    add rax, rcx
    cvtsi2sd xmm3, rdi
    cvttsd2si rcx, xmm3
    add rax, rcx
    cvtsi2ss xmm3, r13
    cvttss2si rcx, xmm3
    add rax, rcx
    cvtsi2sd xmm3, r13
    cvttsd2si rcx, xmm3
    add rax, rcx
    cvtsi2ss xmm3, r8
    cvttss2si rcx, xmm3
    add rax, rcx
    cvtsi2sd xmm3, r8
    cvttsd2si rcx, xmm3
    add rax, rcx
    cvtsi2ss xmm3, r9
    cvttss2si rcx, xmm3
    add rax, rcx
    cvtsi2sd xmm3, r9
    cvttsd2si rcx, xmm3
    add rax, rcx
    cvtsi2ss xmm3, rdx
    cvttss2si rcx, xmm3
    add rax, rcx
    cvtsi2sd xmm3, rdx
    cvttsd2si rcx, xmm3
    add rax, rcx
    cvtsi2ss xmm3, r10
    cvttss2si rcx, xmm3
    add rax, rcx
    cvtsi2sd xmm3, r10
    cvttsd2si rcx, xmm3
    add rax, rcx
    cvttss2si ecx, xmm2
    movsxd rcx, ecx
    add rax, rcx
    cvttss2si rcx, xmm2
    add rax, rcx
    cvttsd2si ecx, xmm0
    movsxd rcx, ecx
    add rax, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvttsd2si ecx, xmm1
    movzx ecx, cl
    add rax, rcx
    movss.d xmm0, [rip + 0x69]
    cvttss2si ecx, xmm0
    movzx ecx, cx
    add rax, rcx
    cvttsd2si ecx, xmm1
    mov ecx, ecx
    add rax, rcx
    cvttsd2si rcx, xmm1
    add rax, rcx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
opaque:
    mov rax, rdi
    ret
main:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, -0x64
    call -0x24
    mov rbx, rax
    mov rdi, -0xbb8
    call -0x33
    mov rbp, rax
    mov rdi, -0x11170
    call -0x42
    mov r12, rax
    mov rdi, -0x4c4b40
    call -0x51
    mov r13, rax
    mov rdi, 0xc8
    call -0x60
    mov r14, rax
    mov rdi, 0xea60
    call -0x6f
    mov r15, rax
    mov rdi, 0x3d0900
    call -0x7e
    mov.q [rsp], rax
    mov rdi, 0x6acfc0
    call -0x8e
    mov r10, rax
    mov rax, rbx
    movsx rcx, al
    mov rax, rbp
    movsx rdx, ax
    mov rax, r12
    movsxd rsi, eax
    mov.q rax, [rsp]
    mov edi, eax
    mov rax, r14
    movzx r8d, al
    mov rax, r15
    movzx r9d, ax
    xor eax, eax
    cvtsi2ss xmm0, rcx
    cvttss2si r11, xmm0
    lea rax, [rax + r11 + 0x6]
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, rdx
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, rdx
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, rsi
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, rsi
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, r13
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, r13
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, r8
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, r8
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, r9
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, r9
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, rdi
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, rdi
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, r10
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, r10
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rax, 7
    add rax, 7
    add rax, -0xc
    add rax, -0xc
    add rax, 0xfa
    add rax, 0xfde8
    add rax, 0xfa
    add rax, 0xfa
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
opaque:
    mov rax, rdi
    ret
main:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, -0x64
    call -0x24
    mov rbx, rax
    mov rdi, -0xbb8
    call -0x33
    mov rbp, rax
    mov rdi, -0x11170
    call -0x42
    mov r12, rax
    mov rdi, -0x4c4b40
    call -0x51
    mov r13, rax
    mov rdi, 0xc8
    call -0x60
    mov r14, rax
    mov rdi, 0xea60
    call -0x6f
    mov r15, rax
    mov rdi, 0x3d0900
    call -0x7e
    mov.q [rsp], rax
    mov rdi, 0x6acfc0
    call -0x8e
    mov r9, rax
    mov rax, rbp
    movsx rcx, ax
    mov rax, r12
    movsxd rdx, eax
    mov rax, rbx
    movsx rax, al
    mov.q rsi, [rsp]
    mov esi, esi
    mov rdi, r14
    movzx edi, dil
    mov r8, r15
    movzx r8d, r8w
    cvtsi2ss xmm0, rax
    cvttss2si r10, xmm0
    cvtsi2sd xmm0, rax
    cvttsd2si rax, xmm0
    lea rax, [r10 + rax + 0x6]
    cvtsi2ss xmm0, rcx
    cvttss2si r10, xmm0
    add rax, r10
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, rdx
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, rdx
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, r13
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, r13
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, rdi
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, rdi
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, r8
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, r8
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, rsi
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, rsi
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, r9
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, r9
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rax, 0x100cc
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: all ===========
opaque:
    mov rax, rdi
    ret
main:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, -0x64
    call -0x24
    mov rbx, rax
    mov rdi, -0xbb8
    call -0x33
    mov rbp, rax
    mov rdi, -0x11170
    call -0x42
    mov r12, rax
    mov rdi, -0x4c4b40
    call -0x51
    mov r13, rax
    mov rdi, 0xc8
    call -0x60
    mov r14, rax
    mov rdi, 0xea60
    call -0x6f
    mov r15, rax
    mov rdi, 0x3d0900
    call -0x7e
    mov.q [rsp], rax
    mov rdi, 0x6acfc0
    call -0x8e
    mov r9, rax
    mov rax, rbp
    movsx rcx, ax
    mov rax, r12
    movsxd rdx, eax
    mov rax, rbx
    movsx rax, al
    mov.q rsi, [rsp]
    mov esi, esi
    mov rdi, r14
    movzx edi, dil
    mov r8, r15
    movzx r8d, r8w
    cvtsi2ss xmm0, rax
    cvttss2si r10, xmm0
    cvtsi2sd xmm0, rax
    cvttsd2si rax, xmm0
    lea rax, [r10 + rax + 0x6]
    cvtsi2ss xmm0, rcx
    cvttss2si r10, xmm0
    add rax, r10
    cvtsi2sd xmm0, rcx
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, rdx
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, rdx
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, r13
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, r13
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, rdi
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, rdi
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, r8
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, r8
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, rsi
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, rsi
    cvttsd2si rcx, xmm0
    add rax, rcx
    cvtsi2ss xmm0, r9
    cvttss2si rcx, xmm0
    add rax, rcx
    cvtsi2sd xmm0, r9
    cvttsd2si rcx, xmm0
    add rax, rcx
    add rax, 0x100cc
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
opaque:
main:
    mov rax, 0xb7b70a
    ret
