=========== OPT LEVEL: all ===========
main:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    movz x0, #10, #0
    bl fib
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
fib:
    sub sp, sp, #48
    str x19, [xzr]
    str x20, [xzr, #8]
    str x21, [xzr, #16]
    stp x29, x30, [xzr, #31]
    mov x20, x0
    movz x21, #2, #0
    movz x0, #1, #0
    cmp w20, w21
    b.le 0x44
    sub x0, x20, x0
    bl main
    mov x19, x0
    sub x0, x20, x21
    bl main
    add x0, x19, x0
    b 0x44
2:
    ldr x19, [xzr]
    ldr x20, [xzr, #8]
    ldr x21, [xzr, #16]
    ldp x29, x30, [xzr, #31]
    add sp, sp, #48
    ret
