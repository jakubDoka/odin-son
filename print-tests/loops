=========== OPT LEVEL: none ===========
main:
    mov rcx, 0xa
    mov rax, 1
    xor esi, esi
1:
    cmp rcx, 0
    jz 2
    sub rcx, 1
    mov rdx, rsi
    add rdx, rax
    mov rsi, rax
    mov rax, rdx
    jmp 1
2:
    ret
=========== OPT LEVEL: mininal ===========
main:
    mov rcx, 0xa
    mov rax, 1
    xor esi, esi
1:
    cmp rcx, 0
    jz 2
    sub rcx, 1
    mov rdx, rsi
    add rdx, rax
    mov rsi, rax
    mov rax, rdx
    jmp 1
2:
    ret
=========== OPT LEVEL: moderate ===========
main:
    mov rcx, 0xa
    mov rax, 1
    xor esi, esi
1:
    cmp rcx, 0
    jz 2
    sub rcx, 1
    mov rdx, rsi
    add rdx, rax
    mov rsi, rax
    mov rax, rdx
    jmp 1
2:
    ret
=========== OPT LEVEL: all ===========
main:
    mov rdx, 0xa
    xor esi, esi
    mov rdi, 1
1:
    mov rax, rdi
    mov rcx, rdx
    sub rcx, 1
    mov rdi, rsi
    add rdi, rax
    cmp rdx, 0
    mov rdx, rcx
    mov rsi, rax
    jnz 1
    ret
=========== OPT LEVEL: aggresive ===========
