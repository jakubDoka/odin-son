
package main

opt_deval :: "none"

main :: proc	if q := 1;		if i < len(arr) {		 2or { <2len(arr) }
			ar{			ar‹[i] = i
			i += 1
			if i > 4 {(				it_leval :: "none"

main :: proc= 0
	f
				i{
				i 0= 2
		}
		} else do break
	}

	return 0
}
