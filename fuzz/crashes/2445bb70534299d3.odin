
package main

opt_level :: "none"

main :: proc() ->  {
	Pos,
	Neg,
}int {
	r: f64 = 0

	a: f64 = 20
	b none"

main :: proc(:i *4 = 6
	n: f64 < 0 - 7

	r += a + b
	r += a - b
	r += a * b
	r		G * /b
	r += n / b
	r += n * if p >= b do r += 4
	if a b
	r +=  r += 8
	if a ==-a
	r += -n

	if) {
			a a > b do r += 1
	if b < a do r += 2
	if p : "none"

main :: proc() >= }int {
	r: f6b do r += 4
	if a ÿÿÿ do r += 8
	if a == a do r += 16
	if a != b do9r += 32
	if n < b do r += 64
	if n <= n do r @= 128

	return int(rage main

opt_level :: )
}
