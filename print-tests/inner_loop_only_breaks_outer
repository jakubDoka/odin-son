=========== OPT LEVEL: None ===========
main:
     push rbx
     push rbp
     mov rbp, 0x00
     jmp :0

000: mov rax, 0x01
     add rbp, rax
     mov rcx, 0x04
     cmp rbp, rcx
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :1
     jmp :2

001: mov rbx, 0x00
     jmp :3

003: add rbx, rax
     cmp rbx, rcx
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :4
     jmp :5

004: mov rdx, 0x02
     cmp rdx, rbx
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :6
     jmp :7

006: jmp :3

007: mov rbx, 0x00
     mov rdx, 0x00
     jmp :8

008: add rdx, rax
     add rbx, rax
     mov rcx, 0x05
     cmp rcx, rdx
     setz cl
     movzx rcx, cl
     test rcx, rcx
     jz :9
     jmp :10

009: jmp :8

010: mov rax, rbx
     jmp :11

005: jmp :0

002: mov rax, 0x00
     jmp :11

011: pop rbp
     pop rbx
     ret
=========== OPT LEVEL: Full ===========
main:
     push rbx
     mov rbx, 0x00
     jmp :0

000: mov rax, 0x04
     add rbx, 0x01
     cmp rbx, rax
     setz cl
     movzx rcx, cl
     test rcx, rcx
     jz :1
     jmp :2

001: mov rdx, 0x00
     jmp :3

003: add rdx, 0x01
     cmp rdx, rax
     setz cl
     movzx rcx, cl
     test rcx, rcx
     jz :4
     jmp :5

004: mov rcx, 0x02
     cmp rcx, rdx
     setz cl
     movzx rcx, cl
     test rcx, rcx
     jz :6
     jmp :7

006: jmp :3

007: mov rdx, 0x00
     mov rcx, 0x00
     jmp :8

008: add rcx, 0x01
     add rdx, 0x01
     mov rax, 0x05
     cmp rax, rcx
     setz al
     movzx rax, al
     test rax, rax
     jz :9
     jmp :10

009: jmp :8

010: mov rax, rdx
     jmp :11

005: jmp :0

002: mov rax, 0x00
     jmp :11

011: pop rbx
     ret
