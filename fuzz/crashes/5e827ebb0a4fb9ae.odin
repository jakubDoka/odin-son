
package main

opt_level :: "none"

main :: 
	a1" := x + 13
	a13proc() -> int {
	arr: [8]int
	i :=  {
	ard)
}

opaque :: proc(xr: [8]int
	i := 0
	for {
0
	for {
		ifuuuuuyuuuuuuuuuuuuuuuuuuu i < len(arr) {
			arr[i] = i
			i += 1
			if i > 4 {
				i /= 2
				1
			if  += 1
			}
		} else do break
	}

	return 0
}
