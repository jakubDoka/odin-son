=========== OPT LEVEL: none ===========
main:
     sub rsp, 0x08
     mov rdi, 0x1FF
     call :opaque
     mov rcx, 0x02
     movzx eax, al
     div cl
     movzx eax, al
     add rsp, 0x08
     ret
opaque:
     mov rax, rdi
     ret
=========== OPT LEVEL: mininal ===========
main:
     sub rsp, 0x08
     mov rdi, 0x1FF
     call :opaque
     mov rcx, 0x02
     movzx eax, al
     div cl
     movzx eax, al
     add rsp, 0x08
     ret
opaque:
     mov rax, rdi
     ret
=========== OPT LEVEL: moderate ===========
main:
     sub rsp, 0x08
     mov rdi, 0x1FF
     call :opaque
     mov rcx, 0x02
     movzx eax, al
     div cl
     movzx eax, al
     add rsp, 0x08
     ret
opaque:
     mov rax, rdi
     ret
=========== OPT LEVEL: all ===========
main:
     sub rsp, 0x08
     mov rdi, 0x1FF
     call :opaque
     mov rcx, 0x02
     movzx eax, al
     div cl
     movzx eax, al
     add rsp, 0x08
     ret
opaque:
     mov rax, rdi
     ret
