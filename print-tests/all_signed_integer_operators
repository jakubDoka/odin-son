=========== OPT LEVEL: none ===========
main:
     push rbx
     push rbp
     mov rax, 0x06
     mov rcx, 0x00
     sub rcx, rax
     mov rbx, rcx
     mov rax, 0x07
     mov rcx, 0x00
     sub rcx, rax
     mov rbp, rcx
     mov rax, 0x14
     cqo
     idiv rbx
     mov rcx, rax
     mov rax, 0x00
     add rcx, rax
     mov rsi, rcx
     mov rax, 0x14
     cqo
     idiv rbx
     add rsi, rdx
     mov rax, rbp
     cqo
     idiv rbx
     add rsi, rax
     mov rax, rbp
     cqo
     idiv rbx
     add rsi, rdx
     mov rcx, 0x01
     mov rax, rbp
     sar rax, cl
     add rsi, rax
     mov rax, rbx
     sar rax, cl
     add rsi, rax
     mov rax, 0x14
     cmp rbx, rax
     setl al
     movzx rax, al
     test rax, rax
     jz :0
     jmp :1

000: jmp :2

001: add rsi, rcx
     jmp :2

002: mov rcx, rsi
     mov rax, rbp
     cmp rax, rbx
     setl al
     movzx rax, al
     test rax, rax
     jz :3
     jmp :4

003: jmp :5

004: mov rax, 0x02
     add rax, rcx
     mov rcx, rax
     jmp :5

005: mov rax, 0x14
     cmp rax, rbx
     setnle al
     movzx rax, al
     test rax, rax
     jz :6
     jmp :7

006: jmp :8

007: mov rax, rcx
     mov rcx, 0x04
     add rcx, rax
     jmp :8

008: mov rax, rbp
     cmp rbx, rax
     setle al
     movzx rax, al
     test rax, rax
     jz :9
     jmp :10

009: jmp :11

010: mov rax, rcx
     mov rcx, 0x08
     add rcx, rax
     jmp :11

011: mov rax, 0x14
     cmp rax, rbx
     setnl al
     movzx rax, al
     test rax, rax
     jz :12
     jmp :13

012: jmp :14

013: mov rax, rcx
     mov rcx, 0x10
     add rcx, rax
     jmp :14

014: mov rax, rcx
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: mininal ===========
main:
     push rbx
     push rbp
     mov rax, 0x00
     mov rcx, 0xFFFFFFFFFFFFFFFD
     mov rdx, 0x02
     mov rbx, 0x01
     mov rsi, 0x00
     add rsi, rcx
     add rsi, rdx
     add rsi, rbx
     mov rbp, 0xFFFFFFFFFFFFFFFF
     add rsi, rbp
     mov rbp, 0xFFFFFFFFFFFFFFFC
     add rsi, rbp
     add rsi, rcx
     test rbx, rbx
     jz :0
     jmp :1

000: jmp :2

001: add rsi, rbx
     jmp :2

002: test rbx, rbx
     jz :3
     jmp :4

003: jmp :5

004: add rsi, rdx
     jmp :5

005: test rbx, rbx
     jz :6
     jmp :7

006: jmp :8

007: mov rcx, 0x04
     add rcx, rsi
     mov rsi, rcx
     jmp :8

008: test rax, rax
     jz :9
     jmp :10

009: jmp :11

010: mov rax, 0x08
     add rax, rsi
     mov rsi, rax
     jmp :11

011: test rbx, rbx
     jz :12
     jmp :13

012: mov rax, rsi
     jmp :14

013: mov rax, 0x10
     add rax, rsi
     jmp :14

014: pop rbp
     pop rbx
     ret
=========== OPT LEVEL: all ===========
main:
     mov rax, 0x0F
     ret
