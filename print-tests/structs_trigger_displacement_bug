=========== OPT LEVEL: None ===========
main:
     push rbx
     sub rsp, 0x10
     mov rsi, 0x00
     mov rdx, 0x10
     lea rdi, [rsp]
     call :$set
     mov rax, 0x00
     lea rcx, [rsp]
     add rax, rcx
     mov rcx, 0x01
     mov [rax], rcx
     mov rdx, 0x08
     lea rbx, [rsp]
     add rdx, rbx
     mov rbx, 0x02
     mov [rdx], rbx
     mov rbx, [rdx]
     add rbx, rcx
     mov [rax], rbx
     mov rax, [rax]
     mov rcx, [rdx]
     add rax, rcx
     sub rsp, 0xFFFFFFFFFFFFFFF0
     pop rbx
     ret
=========== OPT LEVEL: Full ===========
main:
     sub rsp, 0x10
     lea rdi, [rsp]
     mov rsi, 0x00
     mov rdx, 0x10
     call :$set
     mov qword ptr [rsp], 0x01
     mov qword ptr [rsp+0x08], 0x02
     mov rax, [rsp+0x08]
     add rax, 0x01
     mov [rsp], rax
     mov rax, [rsp]
     mov rcx, [rsp+0x08]
     add rax, rcx
     sub rsp, 0xFFFFFFFFFFFFFFF0
     ret
