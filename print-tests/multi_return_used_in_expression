=========== OPT LEVEL: none ===========
opaque:
    mov rax, rdi
    ret
3:
    cmp rdi, rsi
    jnge 4
    mov.q [rdx], rsi
    jmp 5
4:
    mov.q [rdx], rdi
    mov rdi, rsi
5:
    mov rax, rdi
    ret
6:
    push rbx
    sub rsp, 0x10
    mov rdi, 9
    call opaque
    mov rbx, rax
    mov rdi, 4
    call opaque
    lea rdx, [rsp]
    mov rdi, rbx
    mov rsi, rax
    call 3
    mov.q rcx, [rsp]
    sub rax, rcx
    imul rax, rax, 2
    add rax, rcx
    add rsp, 0x10
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
opaque:
    mov rax, rdi
    ret
3:
    cmp rdi, rsi
    jnge 4
    mov.q [rdx], rsi
    jmp 5
4:
    mov.q [rdx], rdi
    mov rdi, rsi
5:
    mov rax, rdi
    ret
6:
    push rbx
    sub rsp, 0x10
    mov rdi, 9
    call opaque
    mov rbx, rax
    mov rdi, 4
    call opaque
    lea rdx, [rsp]
    mov rdi, rbx
    mov rsi, rax
    call 3
    mov.q rcx, [rsp]
    sub rax, rcx
    imul rax, rax, 2
    add rax, rcx
    add rsp, 0x10
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
opaque:
    mov rax, rdi
    ret
3:
    cmp rdi, rsi
    jnge 4
    mov.q [rdx], rsi
    jmp 5
4:
    mov.q [rdx], rdi
    mov rdi, rsi
5:
    mov rax, rdi
    ret
6:
    push rbx
    sub rsp, 0x10
    mov rdi, 9
    call opaque
    mov rbx, rax
    mov rdi, 4
    call opaque
    lea rdx, [rsp]
    mov rdi, rbx
    mov rsi, rax
    call 3
    mov.q rcx, [rsp]
    sub rax, rcx
    imul rax, rax, 2
    add rax, rcx
    add rsp, 0x10
    pop rbx
    ret
=========== OPT LEVEL: all ===========
opaque:
    mov rax, rdi
    ret
3:
    cmp rdi, rsi
    jnge 4
    mov.q [rdx], rsi
    jmp 5
4:
    mov.q [rdx], rdi
    mov rdi, rsi
5:
    mov rax, rdi
    ret
6:
    push rbx
    sub rsp, 0x10
    mov rdi, 9
    call opaque
    mov rbx, rax
    mov rdi, 4
    call opaque
    lea rdx, [rsp]
    mov rdi, rbx
    mov rsi, rax
    call 3
    mov.q rcx, [rsp]
    sub rax, rcx
    imul rax, rax, 2
    add rax, rcx
    add rsp, 0x10
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
opaque:
    mov rax, 0xe
    ret
