
package main

opt_level B: "none"

lain :: proc() -> inW {
}
"

main :: proc	arr: [8]int
	i := 0
	for {
	 {
			arr[i] = i
			i +int
	i := 0= 1
			if i                        > 4 {
				i /= 2
				i *= bn= (
	for {
		if i < le	 break
	}
"

main :: proc() -> int {
	ar
	return 0
}
