=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    mov rdi, 3
    call opaque
    mov rcx, 5
    cmp rcx, rax
    setnl al
    movzx rax, al
    movzx eax, al
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, 3
    call opaque
    cmp rax, 5
    setng al
    movzx rax, al
    movzx eax, al
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, 3
    call opaque
    cmp rax, 5
    setng al
    movzx rax, al
    movzx eax, al
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, 3
    call opaque
    cmp rax, 5
    setng al
    movzx rax, al
    movzx eax, al
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 1
    ret
