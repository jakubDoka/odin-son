=========== OPT LEVEL: none ===========
opaque:
    mov rax, rdi
    ret
3:
    push rbx
    push rbp
    sub rsp, 0x18
    mov rbx, rdi
    mov rbp, rsi
    lea rdi, [rsp]
    xor esi, esi
    mov rdx, 0x10
    call.q [rip - 0x32]
    mov.q [rsp], rbx
    mov.q [rsp + 0x8], rbp
    mov.q rax, [rsp]
    mov.q rdx, [rsp + 0x8]
    add rsp, 0x18
    pop rbp
    pop rbx
    ret
4:
    push rbx
    sub rsp, 0x10
    xor esi, esi
    lea rdi, [rsp]
    mov rdx, 0x10
    call.q [rip - 0x6a]
    mov rdi, 6
    call opaque
    mov rbx, rax
    mov rdi, 9
    call opaque
    mov rdi, rbx
    mov rsi, rax
    call 3
    mov.q [rsp], rax
    mov.q [rsp + 0x8], rdx
    mov.q rax, [rsp + 0x8]
    add.q [rsp], rax
    mov.q rax, [rsp]
    imul rax, rax, 0x64
    add.q rax, [rsp + 0x8]
    add rsp, 0x10
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
opaque:
    mov rax, rdi
    ret
3:
    push rbx
    push rbp
    sub rsp, 0x18
    mov rbx, rdi
    mov rbp, rsi
    lea rdi, [rsp]
    xor esi, esi
    mov rdx, 0x10
    call.q [rip - 0x32]
    mov.q [rsp], rbx
    mov.q [rsp + 0x8], rbp
    mov.q rax, [rsp]
    mov.q rdx, [rsp + 0x8]
    add rsp, 0x18
    pop rbp
    pop rbx
    ret
4:
    push rbx
    sub rsp, 0x10
    xor esi, esi
    lea rdi, [rsp]
    mov rdx, 0x10
    call.q [rip - 0x6a]
    mov rdi, 6
    call opaque
    mov rbx, rax
    mov rdi, 9
    call opaque
    mov rdi, rbx
    mov rsi, rax
    call 3
    mov.q [rsp], rax
    mov.q [rsp + 0x8], rdx
    mov.q rax, [rsp + 0x8]
    add.q [rsp], rax
    mov.q rax, [rsp]
    imul rax, rax, 0x64
    add.q rax, [rsp + 0x8]
    add rsp, 0x10
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
opaque:
    mov rax, rdi
    ret
3:
    mov rax, rsi
    mov rcx, rdi
    mov rdx, rax
    mov rax, rcx
    ret
4:
    push rbx
    mov rdi, 6
    call opaque
    mov rbx, rax
    mov rdi, 9
    call opaque
    mov rdi, rbx
    mov rsi, rax
    call 3
    add rax, rdx
    imul rax, rax, 0x64
    add rax, rdx
    pop rbx
    ret
=========== OPT LEVEL: all ===========
opaque:
    mov rax, rdi
    ret
3:
    mov rax, rsi
    mov rcx, rdi
    mov rdx, rax
    mov rax, rcx
    ret
4:
    push rbx
    mov rdi, 6
    call opaque
    mov rbx, rax
    mov rdi, 9
    call opaque
    mov rdi, rbx
    mov rsi, rax
    call 3
    add rax, rdx
    imul rax, rax, 0x64
    add rax, rdx
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
opaque:
    mov rax, 0x5e5
    ret
