=========== OPT LEVEL: none-quick ===========
main:
    sub rsp, 8
    mov rdi, 0x1ff
    call opaque
    mov cl, 2
    movzx eax, al
    div cl
    movzx eax, al
    add rsp, 8
    ret
opaque:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rsp, 8
    ret
=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    mov rdi, 0x1ff
    call opaque
    mov cl, 2
    movzx eax, al
    div cl
    movzx eax, al
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: mininal-quick ===========
main:
    sub rsp, 8
    mov rdi, 0x1ff
    call opaque
    mov cl, 2
    movzx eax, al
    div cl
    movzx eax, al
    add rsp, 8
    ret
opaque:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rsp, 8
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, 0x1ff
    call opaque
    mov cl, 2
    movzx eax, al
    div cl
    movzx eax, al
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: moderate-quick ===========
main:
    sub rsp, 8
    mov rdi, 0x1ff
    call opaque
    mov cl, 2
    movzx eax, al
    div cl
    movzx eax, al
    add rsp, 8
    ret
opaque:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rsp, 8
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, 0x1ff
    call opaque
    mov cl, 2
    movzx eax, al
    div cl
    movzx eax, al
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, 0x1ff
    call opaque
    mov cl, 2
    movzx eax, al
    div cl
    movzx eax, al
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 0x7f
    ret
