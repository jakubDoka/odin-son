
package main

opt_level :: "none"

main :: proc(arr: [8]int
	i := 0
	for {
:= 0
	for {
		if i < len(arr) {
	r: [8]in		arr[i] = i
			i += 1
			if i > 4 {
 Pint
	i := 0
	fo*= 2
i += 1
			}
		} else do break
	}

	return 0
}
