=========== OPT LEVEL: none ===========
main:
     push rbx
     push rbp
     mov rcx, 0x0A
     mov rdx, 0x00
     mov rbx, 0x01
     mov rsi, 0x00
     mov rax, 0x01
     jmp :0

000: cmp rdx, rcx
     setz bpl
     movzx rbp, bpl
     test rbp, rbp
     jz :1
     jmp :2

001: mov rbp, rsi
     add rbp, rax
     sub rcx, rbx
     mov rsi, rax
     mov rax, rbp
     jmp :0

002: pop rbp
     pop rbx
     ret
=========== OPT LEVEL: mininal ===========
main:
     push rbx
     push rbp
     mov rcx, 0x0A
     mov rdx, 0x00
     mov rbx, 0x01
     mov rsi, 0x00
     mov rax, 0x01
     jmp :0

000: cmp rdx, rcx
     setz bpl
     movzx rbp, bpl
     test rbp, rbp
     jz :1
     jmp :2

001: mov rbp, rsi
     add rbp, rax
     sub rcx, rbx
     mov rsi, rax
     mov rax, rbp
     jmp :0

002: pop rbp
     pop rbx
     ret
=========== OPT LEVEL: all ===========
main:
     push rbx
     mov rcx, 0x0A
     mov rbx, 0x00
     mov rax, 0x01
     jmp :0

000: cmp rcx, 0x00
     jnz :1
     jmp :2

001: mov rdx, rbx
     add rdx, rax
     sub rcx, 0x01
     mov rbx, rax
     mov rax, rdx
     jmp :0

002: pop rbx
     ret
