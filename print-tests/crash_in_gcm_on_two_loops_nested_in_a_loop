=========== OPT LEVEL: none ===========
main:
    xor r8d, r8d
    xor eax, eax
1:
    cmp r8, 4
    jnl 6
    xor esi, esi
    xor edx, edx
2:
    cmp rdx, 4
    jnl 3
    mov rcx, rdx
    add rcx, 1
    add rsi, rdx
    mov rdx, rcx
    jmp 2
3:
    xor edx, edx
    xor edi, edi
4:
    cmp rdx, 4
    jnl 5
    mov rcx, rdx
    add rcx, 1
    lea rdi, [rdi + rdx*2]
    mov rdx, rcx
    jmp 4
5:
    add rsi, rdi
    add r8, 1
    add rax, rsi
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: mininal ===========
main:
    xor r8d, r8d
    xor eax, eax
1:
    cmp r8, 4
    jnl 6
    xor esi, esi
    xor edx, edx
2:
    cmp rdx, 4
    jnl 3
    mov rcx, rdx
    add rcx, 1
    add rsi, rdx
    mov rdx, rcx
    jmp 2
3:
    xor edx, edx
    xor edi, edi
4:
    cmp rdx, 4
    jnl 5
    mov rcx, rdx
    add rcx, 1
    lea rdi, [rdi + rdx*2]
    mov rdx, rcx
    jmp 4
5:
    add rsi, rdi
    add r8, 1
    add rax, rsi
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: moderate ===========
main:
    xor r8d, r8d
    xor eax, eax
1:
    cmp r8, 4
    jnl 6
    xor esi, esi
    xor edx, edx
2:
    cmp rdx, 4
    jnl 3
    mov rcx, rdx
    add rcx, 1
    add rsi, rdx
    mov rdx, rcx
    jmp 2
3:
    xor edx, edx
    xor edi, edi
4:
    cmp rdx, 4
    jnl 5
    mov rcx, rdx
    add rcx, 1
    lea rdi, [rdi + rdx*2]
    mov rdx, rcx
    jmp 4
5:
    add rsi, rdi
    add r8, 1
    add rax, rsi
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: all ===========
main:
    xor r9d, r9d
    xor r8d, r8d
1:
    xor ecx, ecx
    xor edx, edx
2:
    mov rdi, rdx
    mov rdx, rdi
    add rdx, rcx
    mov rax, rcx
    add rax, 1
    cmp rcx, 4
    mov rcx, rax
    jnge 2
    xor esi, esi
    xor edx, edx
3:
    mov rax, rdx
    mov rcx, rsi
    add rcx, 1
    lea rdx, [rax + rsi*2]
    cmp rsi, 4
    mov rsi, rcx
    jnge 3
    add r9, 1
    add rdi, rax
    mov rax, r8
    add rax, rdi
    cmp r9, 4
    mov r8, rax
    jnge 1
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: aggresive ===========
