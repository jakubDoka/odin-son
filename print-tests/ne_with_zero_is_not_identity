=========== OPT LEVEL: none ===========
opaque:
    mov rax, rdi
    ret
main:
    sub rsp, 8
    mov rdi, 5
    call opaque
    cmp rax, 0
    setnz al
    movzx rax, al
    movzx eax, al
    add rsp, 8
    ret
=========== OPT LEVEL: mininal ===========
opaque:
    mov rax, rdi
    ret
main:
    sub rsp, 8
    mov rdi, 5
    call opaque
    cmp rax, 0
    setnz al
    movzx rax, al
    movzx eax, al
    add rsp, 8
    ret
=========== OPT LEVEL: moderate ===========
opaque:
    mov rax, rdi
    ret
main:
    sub rsp, 8
    mov rdi, 5
    call opaque
    cmp rax, 0
    setnz al
    movzx rax, al
    movzx eax, al
    add rsp, 8
    ret
=========== OPT LEVEL: all ===========
opaque:
    mov rax, rdi
    ret
main:
    sub rsp, 8
    mov rdi, 5
    call opaque
    cmp rax, 0
    setnz al
    movzx rax, al
    movzx eax, al
    add rsp, 8
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 1
    ret
