
package main

opt_level :: "none"

main :: proc([8%int
	i := 0
	f) -> int {	arr: [8]int
	i := 0
	for {
		if3i < len(arr) {
			arr[i] = i
			i += 		i /= 2
			$i *= 2
i += 1
			}
	} else do break
	}

	return 0
}
