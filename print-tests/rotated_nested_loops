=========== OPT LEVEL: none ===========
main:
    xor esi, esi
    xor eax, eax
1:
    xor edx, edx
2:
    mov rcx, rsi
    imul rcx, rdx
    add rax, rcx
    add rdx, 1
    cmp rdx, 3
    jnle 3
    jmp 2
3:
    add rsi, 1
    cmp rsi, 3
    jnle 4
    jmp 1
4:
    ret
=========== OPT LEVEL: mininal ===========
main:
    xor esi, esi
    xor eax, eax
1:
    xor edx, edx
2:
    mov rcx, rsi
    imul rcx, rdx
    add rax, rcx
    add rdx, 1
    cmp rdx, 3
    jnle 3
    jmp 2
3:
    add rsi, 1
    cmp rsi, 3
    jnle 4
    jmp 1
4:
    ret
=========== OPT LEVEL: moderate ===========
main:
    xor esi, esi
    xor eax, eax
1:
    xor edx, edx
2:
    mov rcx, rsi
    imul rcx, rdx
    add rax, rcx
    add rdx, 1
    cmp rdx, 3
    jnle 3
    jmp 2
3:
    add rsi, 1
    cmp rsi, 3
    jnle 4
    jmp 1
4:
    ret
=========== OPT LEVEL: all ===========
main:
    xor edx, edx
    xor edi, edi
1:
    xor esi, esi
2:
    mov rcx, rdi
    imul rcx, rsi
    mov rax, rdx
    add rax, rcx
    add rsi, 1
    cmp rsi, 3
    mov rdx, rax
    jng 2
    add rdi, 1
    cmp rdi, 3
    mov rdx, rax
    jng 1
    ret
=========== OPT LEVEL: aggresive ===========
