=========== OPT LEVEL: none ===========
opaque:
    mov rax, rdi
    ret
2:
    sub rsp, 0x18
    lea rdi, [rsp]
    xor esi, esi
    mov rdx, 0x10
    call.q [rip - 0x28]
    mov.q [rsp], 0
    mov.q [rsp + 0x8], 0
    mov rdi, 5
    call opaque
    xor ecx, ecx
3:
    cmp rcx, rax
    jnl 4
    add.q [rsp], rcx
    add.q [rsp + 0x8], 1
    add rcx, 1
    jmp 3
4:
    mov.q rax, [rsp]
    imul rax, rax, 0x64
    add.q rax, [rsp + 0x8]
    add rsp, 0x18
    ret
=========== OPT LEVEL: mininal ===========
opaque:
    mov rax, rdi
    ret
2:
    sub rsp, 0x18
    lea rdi, [rsp]
    xor esi, esi
    mov rdx, 0x10
    call.q [rip - 0x28]
    mov.q [rsp], 0
    mov.q [rsp + 0x8], 0
    mov rdi, 5
    call opaque
    xor ecx, ecx
3:
    cmp rcx, rax
    jnl 4
    add.q [rsp], rcx
    add.q [rsp + 0x8], 1
    add rcx, 1
    jmp 3
4:
    mov.q rax, [rsp]
    imul rax, rax, 0x64
    add.q rax, [rsp + 0x8]
    add rsp, 0x18
    ret
=========== OPT LEVEL: moderate ===========
opaque:
    mov rax, rdi
    ret
2:
    sub rsp, 0x18
    mov.q [rsp], 0
    mov.q [rsp + 0x8], 0
    mov rdi, 5
    call opaque
    xor ecx, ecx
3:
    cmp rcx, rax
    jnl 4
    add.q [rsp], rcx
    add.q [rsp + 0x8], 1
    add rcx, 1
    jmp 3
4:
    mov.q rax, [rsp]
    imul rax, rax, 0x64
    add.q rax, [rsp + 0x8]
    add rsp, 0x18
    ret
=========== OPT LEVEL: all ===========
opaque:
    mov rax, rdi
    ret
2:
    sub rsp, 8
    mov rdi, 5
    call opaque
    xor edx, edx
    xor edi, edi
    xor esi, esi
3:
    cmp rdi, rax
    jnl 4
    mov rcx, rdi
    add rcx, 1
    add rsi, 1
    add rdx, rdi
    mov rdi, rcx
    jmp 3
4:
    imul rax, rdx, 0x64
    add rax, rsi
    add rsp, 8
    ret
=========== OPT LEVEL: aggresive ===========
opaque:
    xor edx, edx
    xor ecx, ecx
    xor esi, esi
2:
    cmp rdx, 5
    jnl 3
    mov rax, rdx
    add rax, 1
    add rcx, 1
    add rsi, rdx
    mov rdx, rax
    jmp 2
3:
    imul rax, rsi, 0x64
    add rax, rcx
    ret
