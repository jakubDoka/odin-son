=========== OPT LEVEL: none ===========
malloc:
free:
main:
     push rbx
     mov rdi, 0x08
     call :$copy
     mov rbx, rax
     mov qword ptr [rbx], 0x00
     mov rdi, rbx
     call :$set
     mov rax, [rbx]
     pop rbx
     ret
=========== OPT LEVEL: mininal ===========
malloc:
free:
main:
     push rbx
     mov rdi, 0x08
     call :$copy
     mov rbx, rax
     mov qword ptr [rbx], 0x00
     mov rdi, rbx
     call :$set
     mov rax, [rbx]
     pop rbx
     ret
=========== OPT LEVEL: moderate ===========
malloc:
free:
main:
     push rbx
     mov rdi, 0x08
     call :$copy
     mov rbx, rax
     mov qword ptr [rbx], 0x00
     mov rdi, rbx
     call :$set
     mov rax, [rbx]
     pop rbx
     ret
=========== OPT LEVEL: all ===========
malloc:
free:
main:
     push rbx
     mov rdi, 0x08
     call :$copy
     mov rbx, rax
     mov qword ptr [rbx], 0x00
     mov rdi, rbx
     call :$set
     mov rax, [rbx]
     pop rbx
     ret
