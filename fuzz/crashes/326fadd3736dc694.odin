
package main

opt_level :: "none"

main :: proc() -> int {
	arr: [8]int
	
four32 :: proc(bi := 0
	for {
		in(arr) {
			arr[i] = i
			i += 1
			if i > 4 {
				i /= 2
				i *= 2
i += 1
			}
		} else do break
	}

	return 0
}
