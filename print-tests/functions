=========== OPT LEVEL: none ===========
main:
     sub rsp, 0x08
     mov rdi, 0x0A
     call :fib
     add rsp, 0x08
     ret
fib:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     mov rax, rbx
     cmp rax, 0x02
     jnle :0
     jmp :1

000: mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib
     add rbp, rax
     jmp :2

001: mov rbp, 0x01
002: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: mininal ===========
main:
     sub rsp, 0x08
     mov rdi, 0x0A
     call :fib
     add rsp, 0x08
     ret
fib:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     mov rax, rbx
     cmp rax, 0x02
     jnle :0
     jmp :1

000: mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib
     add rbp, rax
     jmp :2

001: mov rbp, 0x01
002: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: all ===========
main:
     sub rsp, 0x08
     mov rdi, 0x0A
     call :fib
     add rsp, 0x08
     ret
fib:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     mov rax, rbx
     cmp rax, 0x02
     jnle :0
     jmp :1

000: mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib
     add rbp, rax
     jmp :2

001: mov rbp, 0x01
002: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
