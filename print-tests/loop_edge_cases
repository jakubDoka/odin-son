main:
     push rcx
     push rbp
     mov rax, 0x00
     mov rdx, rax
     mov rsi, rax
     jmp :0

000: mov rcx, 0x01
     mov rbx, rdx
     add rbx, rcx
     mov rdx, 0x02
     mov rbp, rbx
     cmp rbp, rdx
     setz bpl
     movzx rbp, bpl
     test rbp, rbp
     jz :1
     jmp :2

001: mov rdx, rbx
     cmp rdx, rcx
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :3
     jmp :4

003: mov rdx, rbx
     add rdx, rcx
     jmp :5

004: mov rcx, rsi
     mov rdx, rbx
     jmp :5

005: mov rsi, rcx
     jmp :0

002: mov rbp, rsi
     cmp rbp, rax
     setz bpl
     movzx rbp, bpl
     mov rbx, 0x03
     test rbp, rbp
     jz :6
     jmp :7

006: mov rdx, rsi
     jmp :8

007: mov rbp, rax
     mov rdi, rsi
     jmp :9

009: add rbp, rcx
     mov rsi, rbp
     cmp rsi, rbx
     setz sil
     movzx rsi, sil
     test rsi, rsi
     jz :10
     jmp :11

010: mov rsi, rbp
     cmp rsi, rdx
     setz sil
     movzx rsi, sil
     test rsi, rsi
     jz :12
     jmp :13

012: mov rsi, rbp
     cmp rsi, rcx
     setz sil
     movzx rsi, sil
     test rsi, rsi
     jz :14
     jmp :15

014: add rbp, rcx
     mov rsi, rdx
     jmp :16

015: jmp :17

013: jmp :17

017: mov rsi, rdi
     jmp :16

016: mov rdi, rsi
     jmp :9

011: mov rdx, rdi
     jmp :8

008: mov rbp, rdx
     cmp rbp, rax
     setnz bpl
     movzx rbp, bpl
     test rbp, rbp
     jz :18
     jmp :19

018: mov rax, rdx
     jmp :20

019: jmp :21

021: add rax, rcx
     mov rdx, rax
     cmp rdx, rbx
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :22
     jmp :23

022: add rax, rcx
     mov rdx, 0x04
     cmp rdx, rax
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :24
     jmp :25

024: jmp :21

025: mov rax, rbx
     jmp :26

023: mov rax, rcx
     jmp :26

026: jmp :20

020: pop rbp
     pop rcx
     ret
