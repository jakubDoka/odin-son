=========== OPT LEVEL: none-quick ===========
main:
    push rbx
    xor ecx, ecx
    mov rax, 0x2a
1:
    cmp rcx, 3
    jz 2
    mov rdx, rcx
    add rdx, 1
    mov rbx, rdx
    add rbx, 7
    mov rcx, rdx
    mov rax, rbx
    jmp 1
2:
    pop rbx
    ret
=========== OPT LEVEL: none ===========
main:
    xor ecx, ecx
    mov rax, 0x2a
1:
    cmp rcx, 3
    jz 2
    add rcx, 1
    mov rax, rcx
    add rax, 7
    jmp 1
2:
    ret
=========== OPT LEVEL: mininal-quick ===========
main:
    push rbx
    xor ecx, ecx
    mov rax, 0x2a
1:
    cmp rcx, 3
    jz 2
    mov rdx, rcx
    add rdx, 8
    mov rbx, rcx
    add rbx, 1
    mov rax, rdx
    mov rcx, rbx
    jmp 1
2:
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
main:
    xor ecx, ecx
    mov rax, 0x2a
1:
    cmp rcx, 3
    jz 2
    mov rax, rcx
    add rax, 8
    add rcx, 1
    jmp 1
2:
    ret
=========== OPT LEVEL: moderate-quick ===========
main:
    push rbx
    xor ecx, ecx
    mov rax, 0x2a
1:
    cmp rcx, 3
    jz 2
    mov rdx, rcx
    add rdx, 8
    mov rbx, rcx
    add rbx, 1
    mov rax, rdx
    mov rcx, rbx
    jmp 1
2:
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
main:
    xor ecx, ecx
    mov rax, 0x2a
1:
    cmp rcx, 3
    jz 2
    mov rax, rcx
    add rax, 8
    add rcx, 1
    jmp 1
2:
    ret
=========== OPT LEVEL: all ===========
main:
    xor edx, edx
    mov rcx, 0x2a
1:
    mov rax, rcx
    mov rcx, rdx
    add rcx, 8
    mov rsi, rdx
    add rsi, 1
    cmp rdx, 3
    mov rdx, rsi
    jnz 1
    ret
=========== OPT LEVEL: aggresive ===========
