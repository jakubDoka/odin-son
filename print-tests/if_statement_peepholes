=========== OPT LEVEL: none ===========
opaques:
    mov rax, rdi
    ret
2:
    cmp rdi, rsi
    jz 11
test_mixed_patterns:
    cmp rdi, rsi
    jnz 10
    cmp rdi, rsi
    jnge 9
    cmp rdi, rsi
    jnl 8
    cmp rdi, rsi
    jnle 7
    cmp rdi, rsi
    jng 6
    xor eax, eax
    jmp 12
6:
    mov rax, 6
    jmp 12
7:
    mov rax, 5
    jmp 12
8:
    mov rax, 4
    jmp 12
9:
    mov rax, 3
    jmp 12
10:
    mov rax, 2
    jmp 12
11:
    mov rax, 1
12:
    ret
13:
    cmp rdi, rsi
    jz 19
    cmp rdi, rsi
    jnz 18
    cmp rdi, rsi
    jc 17
    cmp rdi, rsi
    jnc 16
    cmp rdi, rsi
    jnbe 15
    cmp rdi, rsi
    jbe 14
    xor eax, eax
    jmp 20
14:
    mov rax, 0x3c
    jmp 20
15:
    mov rax, 0x32
    jmp 20
16:
    mov rax, 0x28
    jmp 20
17:
    mov rax, 0x1e
    jmp 20
18:
    mov rax, 0x14
    jmp 20
19:
    mov rax, 0xa
20:
    ret
21:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    mov rdi, rbp
    call opaques
    mov rbx, rax
    add rbp, 1
    mov rdi, rbp
    call opaques
    cmp rbx, rax
    jnge 22
    jmp 24
22:
    cmp rbx, rax
    jng 23
    jmp 24
23:
    cmp rbx, rax
    jnz 29
24:
    cmp rbx, rax
    jnle 25
    jmp 27
25:
    cmp rbx, rax
    jnl 26
    jmp 27
26:
    cmp rbx, rax
    jz 28
27:
    xor eax, eax
    jmp 30
28:
    mov rax, 0xc8
    jmp 30
29:
    mov rax, 0x64
30:
    add rsp, 8
    pop rbp
    pop rbx
    ret
31:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0xa
    mov rsi, 0x14
    call 2
    mov rbx, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call 2
    mov rbp, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call 2
    mov r12, rax
    mov rdi, 0xa
    mov rsi, 0x14
    call 13
    mov r13, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call 13
    mov r14, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call 13
    mov r15, rax
    mov rdi, 0x2a
    call 21
    mov rdx, rax
    xor eax, eax
    add rax, rbx
    add rax, rbp
    add rax, r12
    add rax, r13
    add rax, r14
    add rax, r15
    add rax, rdx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
opaques:
    mov rax, rdi
    ret
2:
    cmp rdi, rsi
    jz 11
test_mixed_patterns:
    cmp rdi, rsi
    jnz 10
    cmp rdi, rsi
    jnge 9
    cmp rdi, rsi
    jnl 8
    cmp rdi, rsi
    jnle 7
    cmp rdi, rsi
    jng 6
    xor eax, eax
    jmp 12
6:
    mov rax, 6
    jmp 12
7:
    mov rax, 5
    jmp 12
8:
    mov rax, 4
    jmp 12
9:
    mov rax, 3
    jmp 12
10:
    mov rax, 2
    jmp 12
11:
    mov rax, 1
12:
    ret
13:
    cmp rdi, rsi
    jz 19
    cmp rdi, rsi
    jnz 18
    cmp rdi, rsi
    jc 17
    cmp rdi, rsi
    jnc 16
    cmp rdi, rsi
    jnbe 15
    cmp rdi, rsi
    jbe 14
    xor eax, eax
    jmp 20
14:
    mov rax, 0x3c
    jmp 20
15:
    mov rax, 0x32
    jmp 20
16:
    mov rax, 0x28
    jmp 20
17:
    mov rax, 0x1e
    jmp 20
18:
    mov rax, 0x14
    jmp 20
19:
    mov rax, 0xa
20:
    ret
21:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    mov rdi, rbp
    call opaques
    mov rbx, rax
    add rbp, 1
    mov rdi, rbp
    call opaques
    cmp rbx, rax
    jnge 22
    jmp 24
22:
    cmp rbx, rax
    jng 23
    jmp 24
23:
    cmp rbx, rax
    jnz 29
24:
    cmp rbx, rax
    jnle 25
    jmp 27
25:
    cmp rbx, rax
    jnl 26
    jmp 27
26:
    cmp rbx, rax
    jz 28
27:
    xor eax, eax
    jmp 30
28:
    mov rax, 0xc8
    jmp 30
29:
    mov rax, 0x64
30:
    add rsp, 8
    pop rbp
    pop rbx
    ret
31:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0xa
    mov rsi, 0x14
    call 2
    mov rbx, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call 2
    mov rbp, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call 2
    mov r12, rax
    mov rdi, 0xa
    mov rsi, 0x14
    call 13
    mov r13, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call 13
    mov r14, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call 13
    mov r15, rax
    mov rdi, 0x2a
    call 21
    mov rdx, rax
    xor eax, eax
    add rax, rbx
    add rax, rbp
    add rax, r12
    add rax, r13
    add rax, r14
    add rax, r15
    add rax, rdx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
opaques:
    mov rax, rdi
    ret
2:
    cmp rdi, rsi
    jz 11
test_mixed_patterns:
    cmp rdi, rsi
    jnz 10
    cmp rdi, rsi
    jnge 9
    cmp rdi, rsi
    jnl 8
    cmp rdi, rsi
    jnle 7
    cmp rdi, rsi
    jng 6
    xor eax, eax
    jmp 12
6:
    mov rax, 6
    jmp 12
7:
    mov rax, 5
    jmp 12
8:
    mov rax, 4
    jmp 12
9:
    mov rax, 3
    jmp 12
10:
    mov rax, 2
    jmp 12
11:
    mov rax, 1
12:
    ret
13:
    cmp rdi, rsi
    jz 19
    cmp rdi, rsi
    jnz 18
    cmp rdi, rsi
    jc 17
    cmp rdi, rsi
    jnc 16
    cmp rdi, rsi
    jnbe 15
    cmp rdi, rsi
    jbe 14
    xor eax, eax
    jmp 20
14:
    mov rax, 0x3c
    jmp 20
15:
    mov rax, 0x32
    jmp 20
16:
    mov rax, 0x28
    jmp 20
17:
    mov rax, 0x1e
    jmp 20
18:
    mov rax, 0x14
    jmp 20
19:
    mov rax, 0xa
20:
    ret
21:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    mov rdi, rbp
    call opaques
    mov rbx, rax
    add rbp, 1
    mov rdi, rbp
    call opaques
    cmp rbx, rax
    jnge 22
    jmp 24
22:
    cmp rbx, rax
    jng 23
    jmp 24
23:
    cmp rbx, rax
    jnz 29
24:
    cmp rbx, rax
    jnle 25
    jmp 27
25:
    cmp rbx, rax
    jnl 26
    jmp 27
26:
    cmp rbx, rax
    jz 28
27:
    xor eax, eax
    jmp 30
28:
    mov rax, 0xc8
    jmp 30
29:
    mov rax, 0x64
30:
    add rsp, 8
    pop rbp
    pop rbx
    ret
31:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0xa
    mov rsi, 0x14
    call 2
    mov rbx, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call 2
    mov rbp, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call 2
    mov r12, rax
    mov rdi, 0xa
    mov rsi, 0x14
    call 13
    mov r13, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call 13
    mov r14, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call 13
    mov r15, rax
    mov rdi, 0x2a
    call 21
    mov rdx, rbx
    add rdx, rbp
    add rdx, r12
    add rdx, r13
    add rdx, r14
    add rdx, r15
    add rdx, rax
    mov rax, rdx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: all ===========
opaques:
    mov rax, rdi
    ret
2:
    cmp rdi, rsi
    jz 11
test_mixed_patterns:
    cmp rdi, rsi
    jnz 10
    cmp rdi, rsi
    jnge 9
    cmp rdi, rsi
    jnl 8
    cmp rdi, rsi
    jnle 7
    cmp rdi, rsi
    jng 6
    xor eax, eax
    jmp 12
6:
    mov rax, 6
    jmp 12
7:
    mov rax, 5
    jmp 12
8:
    mov rax, 4
    jmp 12
9:
    mov rax, 3
    jmp 12
10:
    mov rax, 2
    jmp 12
11:
    mov rax, 1
12:
    ret
13:
    cmp rdi, rsi
    jz 19
    cmp rdi, rsi
    jnz 18
    cmp rdi, rsi
    jc 17
    cmp rdi, rsi
    jnc 16
    cmp rdi, rsi
    jnbe 15
    cmp rdi, rsi
    jbe 14
    xor eax, eax
    jmp 20
14:
    mov rax, 0x3c
    jmp 20
15:
    mov rax, 0x32
    jmp 20
16:
    mov rax, 0x28
    jmp 20
17:
    mov rax, 0x1e
    jmp 20
18:
    mov rax, 0x14
    jmp 20
19:
    mov rax, 0xa
20:
    ret
21:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    mov rdi, rbp
    call opaques
    mov rbx, rax
    add rbp, 1
    mov rdi, rbp
    call opaques
    cmp rbx, rax
    jnge 22
    jmp 24
22:
    cmp rbx, rax
    jng 23
    jmp 24
23:
    cmp rbx, rax
    jnz 29
24:
    cmp rbx, rax
    jnle 25
    jmp 27
25:
    cmp rbx, rax
    jnl 26
    jmp 27
26:
    cmp rbx, rax
    jz 28
27:
    xor eax, eax
    jmp 30
28:
    mov rax, 0xc8
    jmp 30
29:
    mov rax, 0x64
30:
    add rsp, 8
    pop rbp
    pop rbx
    ret
31:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0xa
    mov rsi, 0x14
    call 2
    mov rbx, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call 2
    mov rbp, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call 2
    mov r12, rax
    mov rdi, 0xa
    mov rsi, 0x14
    call 13
    mov r13, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call 13
    mov r14, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call 13
    mov r15, rax
    mov rdi, 0x2a
    call 21
    mov rdx, rbx
    add rdx, rbp
    add rdx, r12
    add rdx, r13
    add rdx, r14
    add rdx, r15
    add rdx, rax
    mov rax, rdx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
opaques:
    cmp rdi, rsi
    jz 10
test_unsigned:
    cmp rdi, rsi
    jnz 9
    cmp rdi, rsi
    jnge 8
    cmp rdi, rsi
    jnl 7
    cmp rdi, rsi
    jnle 6
    cmp rdi, rsi
    jng 5
    xor eax, eax
    jmp 11
5:
    mov rax, 6
    jmp 11
6:
    mov rax, 5
    jmp 11
7:
    mov rax, 4
    jmp 11
8:
    mov rax, 3
    jmp 11
9:
    mov rax, 2
    jmp 11
10:
    mov rax, 1
11:
    ret
12:
    cmp rdi, rsi
    jz +0x71
    cmp rdi, rsi
    jnz +0x5c
    cmp rdi, rsi
    jc +0x47
    cmp rdi, rsi
    jnc +0x32
    cmp rdi, rsi
    jnbe +0x1d
    cmp rdi, rsi
    jbe +0x8
    xor eax, eax
    jmp +0x43
    mov rax, 0x3c
    jmp +0x37
    mov rax, 0x32
    jmp +0x2b
    mov rax, 0x28
    jmp +0x1f
    mov rax, 0x1e
    jmp +0x13
    mov rax, 0x14
    jmp +0x7
    mov rax, 0xa
    ret
13:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    mov rdi, 0xa
    mov rsi, 0x14
    call opaques
    mov rbx, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call opaques
    mov rbp, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call opaques
    mov r12, rax
    mov rdi, 0xa
    mov rsi, 0x14
    call 12
    mov r13, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call 12
    mov r14, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call 12
    mov rdx, rbx
    add rdx, rbp
    add rdx, r12
    add rdx, r13
    add rdx, r14
    add rdx, rax
    mov rax, rdx
    add rax, 0x64
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
