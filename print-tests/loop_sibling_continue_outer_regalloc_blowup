=========== OPT LEVEL: None ===========
main:
     push rbx
     push rbp
     mov rcx, 0x00
     mov rax, 0x00
     jmp :0

000: mov rdx, 0x01
     add rcx, rdx
     mov rbx, 0x08
     cmp rbx, rcx
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :1
     jmp :2

001: mov rdi, rax
     mov rsi, 0x00
     jmp :3

003: add rsi, rdx
     mov rax, 0x05
     cmp rsi, rax
     setz bl
     movzx rbx, bl
     mov rbp, 0x02
     test rbx, rbx
     jz :4
     jmp :5

004: cmp rsi, rbp
     setz al
     movzx rax, al
     test rax, rax
     jz :6
     jmp :7

006: add rdi, rdx
     jmp :3

007: jmp :8

005: mov rsi, 0x00
     jmp :9

009: add rsi, rdx
     cmp rsi, rax
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :10
     jmp :11

010: mov rbx, 0x03
     cmp rbx, rsi
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :12
     jmp :13

012: add rdi, rbp
     jmp :9

013: jmp :8

008: jmp :14

011: mov rax, 0x64
     add rax, rdi
     mov rdi, rax
     jmp :14

014: mov rax, rdi
     jmp :0

002: pop rbp
     pop rbx
     ret
=========== OPT LEVEL: Full ===========
main:
     push rbx
     push rbp
     mov rbp, 0x00
     mov rax, 0x00
     jmp :0

000: add rbp, 0x01
     mov rcx, 0x08
     cmp rbp, rcx
     jnz :1
     jmp :2

001: mov rbx, rax
     mov rcx, 0x00
     jmp :3

003: mov rax, 0x05
     add rcx, 0x01
     cmp rcx, rax
     jnz :4
     jmp :5

004: mov rax, 0x02
     cmp rcx, rax
     jnz :6
     jmp :7

006: add rbx, 0x01
     jmp :3

007: jmp :8

005: mov rdx, 0x00
     jmp :9

009: add rdx, 0x01
     cmp rdx, rax
     jnz :10
     jmp :11

010: mov rcx, 0x03
     cmp rdx, rcx
     jnz :12
     jmp :13

012: add rbx, 0x02
     jmp :9

013: jmp :8

008: jmp :14

011: add rbx, 0x64
     jmp :14

014: mov rax, rbx
     jmp :0

002: pop rbp
     pop rbx
     ret
