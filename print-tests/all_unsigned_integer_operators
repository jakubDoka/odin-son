=========== OPT LEVEL: None ===========
main:
     push rbx
     push rbp
     mov rbx, 0x06
     mov rcx, 0x03
     mov rax, 0x14
     xor rdx, rdx
     div rbx
     cmp rax, rcx
     setz al
     movzx rax, al
     test rax, rax
     jz :0
     jmp :1

000: mov rbp, 0x00
     jmp :2

001: mov rbp, 0x01
     mov rax, 0x00
     add rbp, rax
     jmp :2

002: mov rax, 0x14
     xor rdx, rdx
     div rbx
     mov rax, 0x02
     cmp rdx, rax
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :3
     jmp :4

003: jmp :5

004: mov rax, 0x02
     add rbp, rax
     jmp :5

005: mov rax, 0x07
     mov rdx, 0x00
     sub rdx, rax
     mov rsi, rdx
     mov rax, rsi
     xor rdx, rdx
     div rbx
     mov rdx, rax
     mov rax, 0x14
     cmp rdx, rax
     setnbe dl
     movzx rdx, dl
     test rdx, rdx
     jz :6
     jmp :7

006: jmp :8

007: mov rax, rbp
     mov rdx, 0x04
     add rdx, rax
     mov rbp, rdx
     jmp :8

008: mov rax, rsi
     xor rdx, rdx
     div rbx
     cmp rdx, rcx
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :9
     jmp :10

009: jmp :11

010: mov rax, rbp
     mov rbp, 0x08
     add rbp, rax
     jmp :11

011: mov rcx, 0x3C
     mov rdx, rsi
     shr rdx, cl
     mov rax, 0x0F
     cmp rdx, rax
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :12
     jmp :13

012: jmp :14

013: mov rax, rbp
     mov rbp, 0x10
     add rbp, rax
     jmp :14

014: mov rdx, 0x14
     mov rcx, 0x02
     shr rdx, cl
     mov rax, 0x05
     cmp rdx, rax
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :15
     jmp :16

015: jmp :17

016: mov rax, rbp
     mov rbp, 0x20
     add rbp, rax
     jmp :17

017: mov rax, rsi
     cmp rax, rbx
     setnbe al
     movzx rax, al
     test rax, rax
     jz :18
     jmp :19

018: jmp :20

019: mov rax, rbp
     mov rbp, 0x40
     add rbp, rax
     jmp :20

020: mov rax, 0x14
     mov rcx, rsi
     cmp rcx, rax
     setnb cl
     movzx rcx, cl
     test rcx, rcx
     jz :21
     jmp :22

021: jmp :23

022: mov rax, rbp
     mov rbp, 0x80
     add rbp, rax
     jmp :23

023: mov rax, rsi
     mov rcx, 0x06
     cmp rcx, rax
     setb cl
     movzx rcx, cl
     test rcx, rcx
     jz :24
     jmp :25

024: jmp :26

025: mov rax, rbp
     mov rcx, 0x100
     add rcx, rax
     mov rbp, rcx
     jmp :26

026: mov rdx, rbp
     mov rax, rsi
     mov rcx, 0x06
     cmp rcx, rax
     setbe cl
     movzx rcx, cl
     test rcx, rcx
     jz :27
     jmp :28

027: jmp :29

028: mov rax, rdx
     mov rdx, 0x200
     add rdx, rax
     jmp :29

029: mov rcx, 0x14
     mov rax, rsi
     cmp rcx, rax
     setb cl
     movzx rcx, cl
     test rcx, rcx
     jz :30
     jmp :31

030: jmp :32

031: mov rax, rdx
     mov rdx, 0x400
     add rdx, rax
     jmp :32

032: mov rax, rdx
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: Full ===========
main:
     mov rax, 0x7FF
     ret
