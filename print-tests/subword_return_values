=========== OPT LEVEL: none ===========
main:
    push rbx
get16:
    call 3
    mov rbx, rax
    call 4
    mov rcx, rax
    movsx rax, bl
    movsx rcx, cx
    add rax, rcx
    pop rbx
    ret
3:
    mov al, -0xa
    ret
4:
    mov rax, -0x3e8
    ret
=========== OPT LEVEL: mininal ===========
main:
    push rbx
get16:
    call 3
    mov rbx, rax
    call 4
    mov rcx, rax
    movsx rax, bl
    movsx rcx, cx
    add rax, rcx
    pop rbx
    ret
3:
    mov al, -0xa
    ret
4:
    mov rax, -0x3e8
    ret
=========== OPT LEVEL: moderate ===========
main:
    push rbx
get16:
    call 3
    mov rbx, rax
    call 4
    mov rcx, rax
    movsx rax, bl
    movsx rcx, cx
    add rax, rcx
    pop rbx
    ret
3:
    mov al, -0xa
    ret
4:
    mov rax, -0x3e8
    ret
=========== OPT LEVEL: all ===========
main:
    push rbx
get16:
    call 3
    mov rbx, rax
    call 4
    mov rcx, rax
    movsx rax, bl
    movsx rcx, cx
    add rax, rcx
    pop rbx
    ret
3:
    mov al, -0xa
    ret
4:
    mov rax, -0x3e8
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, -0x3f2
    ret
