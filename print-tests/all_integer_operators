=========== OPT LEVEL: none ===========
batch:
    push rbx
batch int:
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0x14
    mov rsi, 6
    mov rdx, -7
    call 9
    mov r15, rax
    mov dl, 7
    mov dil, 0x14
    mov sil, 6
    call 24
    mov r14, rax
batch i8:
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 39
    mov r13, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 54
    mov r12, rax
    mov dl, -7
    mov dil, 0x14
    mov sil, 6
    call 69
    mov rbp, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 84
    mov rbx, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 99
    mov rcx, rax
    xor eax, eax
    add rax, r15
    add rax, r14
    add rax, r13
    add rax, r12
    add rax, rbp
    add rax, rbx
    add rax, rcx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
9:
    mov r8, rdi
    mov rdi, rdx
    mov rax, r8
    imul rax, rsi
    xor ecx, ecx
    add rcx, rax
    mov rax, r8
    cqo
    idiv rsi
    add rcx, rax
    mov rax, r8
    cqo
    idiv rsi
    add rcx, rdx
    mov rax, rdi
    cqo
    idiv rsi
    add rcx, rax
    mov rax, rdi
    cqo
    idiv rsi
    add rcx, rdx
    mov rax, r8
    and rax, rsi
    add rcx, rax
    mov rax, r8
    or rax, rsi
    add rcx, rax
    mov rax, r8
    xor rax, rsi
    add rcx, rax
    mov rdx, r8
    mov rax, rsi
    not rax
    and rax, rdx
    add rcx, rax
    mov rax, r8
    sal rax, 2
    add rcx, rax
    mov rax, r8
    sar rax, 2
    add rcx, rax
    mov rax, rdi
    sar rax, 2
    add rcx, rax
    cmp r8, rsi
    jnle 10
    jmp 11
10:
    add rcx, 1
11:
    cmp rsi, r8
    jnge 12
    jmp 13
12:
    add rcx, 2
13:
    mov rdx, rcx
    cmp r8, rsi
    jnl 14
    jmp 15
14:
    add rdx, 4
15:
    cmp r8, rsi
    jng 16
    jmp 17
16:
    add rdx, 8
17:
    cmp r8, r8
    jz 18
    jmp 19
18:
    add rdx, 0x10
19:
    cmp r8, rsi
    jnz 20
    jmp 21
20:
    add rdx, 0x20
21:
    cmp rdi, rsi
    jnge 22
    jmp 23
22:
    add rdx, 0x40
23:
    mov rax, rdx
    ret
24:
    mov r9, rdi
    mov r8, rsi
    mov rdi, rdx
    mov rcx, r9
    mov rax, r8
    imul cl
    movzx eax, al
    xor esi, esi
    add rsi, rax
    mov rax, r9
    movzx eax, al
    div r8b
    movzx eax, al
    add rsi, rax
    mov rax, r9
    movzx eax, al
    div r8b
    movsx edx, ah
    movzx eax, dl
    add rsi, rax
    mov rcx, r8
    mov rax, rdi
    movzx eax, al
    div cl
    movzx eax, al
    add rsi, rax
    mov rcx, r8
    mov rax, rdi
    movzx eax, al
    div cl
    movsx edx, ah
    movzx eax, dl
    add rsi, rax
    mov rax, r9
    and al, r8b
    movzx eax, al
    mov rdx, rsi
    add rdx, rax
    mov rax, r9
    or al, r8b
    movzx eax, al
    add rdx, rax
    mov rax, r9
    xor al, r8b
    movzx eax, al
    add rdx, rax
    mov rcx, r9
    mov rax, r8
    not al
    and al, cl
    movzx eax, al
    add rdx, rax
    mov rax, r9
    shl al, 2
    movzx eax, al
    add rdx, rax
    mov rax, r9
    shr al, 2
    movzx eax, al
    add rdx, rax
    mov rax, rdi
    shr al, 2
    movzx eax, al
    add rdx, rax
    cmp r9b, r8b
    jnbe 25
    jmp 26
25:
    add rdx, 1
26:
    cmp r8b, r9b
    jc 27
    jmp 28
27:
    add rdx, 2
28:
    cmp r9b, r8b
    jnc 29
    jmp 30
29:
    add rdx, 4
30:
    cmp r9b, r8b
    jbe 31
    jmp 32
31:
    add rdx, 8
32:
    cmp r9b, r9b
    jz 33
    jmp 34
33:
    add rdx, 0x10
34:
    cmp r9b, r8b
    jnz 35
    jmp 36
35:
    add rdx, 0x20
36:
    cmp dil, r8b
    jc 37
    jmp 38
37:
    add rdx, 0x40
38:
    mov rax, rdx
    ret
39:
    mov r8, rdi
    mov rdi, rdx
    mov rax, r8
    imul ax, si
    movzx eax, ax
    xor ecx, ecx
    add rcx, rax
    mov rax, r8
    xor rdx, rdx
    div si
    movzx eax, ax
    add rcx, rax
    mov rax, r8
    xor rdx, rdx
    div si
    movzx eax, dx
    add rcx, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, ax
    add rcx, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, dx
    add rcx, rax
    mov rax, r8
    and ax, si
    movzx eax, ax
    add rcx, rax
    mov rax, r8
    or ax, si
    movzx eax, ax
    add rcx, rax
    mov rax, r8
    xor ax, si
    movzx eax, ax
    add rcx, rax
    mov rax, rcx
    mov rdx, r8
    mov rcx, rsi
    not cx
    and cx, dx
    movzx ecx, cx
    add rax, rcx
    mov rcx, r8
    sal cx, 2
    movzx ecx, cx
    add rax, rcx
    mov rcx, r8
    shr cx, 2
    movzx ecx, cx
    add rax, rcx
    mov rcx, rdi
    shr cx, 2
    movzx ecx, cx
    add rax, rcx
    cmp r8w, si
    jnbe 40
    jmp 41
40:
    add rax, 1
41:
    mov rdx, rax
    cmp si, r8w
    jc 42
    jmp 43
42:
    add rdx, 2
43:
    cmp r8w, si
    jnc 44
    jmp 45
44:
    add rdx, 4
45:
    cmp r8w, si
    jbe 46
    jmp 47
46:
    add rdx, 8
47:
    cmp r8w, r8w
    jz 48
    jmp 49
48:
    add rdx, 0x10
49:
    cmp r8w, si
    jnz 50
    jmp 51
50:
    add rdx, 0x20
51:
    cmp di, si
    jc 52
    jmp 53
52:
    add rdx, 0x40
53:
    mov rax, rdx
    ret
54:
    mov r8, rdi
    mov rdi, rdx
    mov rax, r8
    imul eax, esi
    mov eax, eax
    xor ecx, ecx
    add rcx, rax
    mov rax, r8
    xor rdx, rdx
    div esi
    mov eax, eax
    add rcx, rax
    mov rax, r8
    xor rdx, rdx
    div esi
    mov eax, edx
    add rcx, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, eax
    add rcx, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, edx
    add rcx, rax
    mov rax, r8
    and eax, esi
    mov eax, eax
    add rcx, rax
    mov rax, r8
    or eax, esi
    mov eax, eax
    add rcx, rax
    mov rax, r8
    xor eax, esi
    mov eax, eax
    add rcx, rax
    mov rax, rcx
    mov rdx, r8
    mov rcx, rsi
    not ecx
    and ecx, edx
    mov ecx, ecx
    add rax, rcx
    mov rcx, r8
    sal ecx, 2
    mov ecx, ecx
    add rax, rcx
    mov rcx, r8
    shr ecx, 2
    mov ecx, ecx
    add rax, rcx
    mov rcx, rdi
    shr ecx, 2
    mov ecx, ecx
    add rax, rcx
    cmp r8d, esi
    jnbe 55
    jmp 56
55:
    add rax, 1
56:
    mov rdx, rax
    cmp esi, r8d
    jc 57
    jmp 58
57:
    add rdx, 2
58:
    cmp r8d, esi
    jnc 59
    jmp 60
59:
    add rdx, 4
60:
    cmp r8d, esi
    jbe 61
    jmp 62
61:
    add rdx, 8
62:
    cmp r8d, r8d
    jz 63
    jmp 64
63:
    add rdx, 0x10
64:
    cmp r8d, esi
    jnz 65
    jmp 66
65:
    add rdx, 0x20
66:
    cmp edi, esi
    jc 67
    jmp 68
67:
    add rdx, 0x40
68:
    mov rax, rdx
    ret
69:
    mov r9, rdi
    mov r8, rsi
    mov rdi, rdx
    mov rcx, r9
    mov rax, r8
    imul cl
    movsx rax, al
    xor esi, esi
    add rsi, rax
    mov rax, r9
    cbw
    idiv r8b
    movsx rax, al
    add rsi, rax
    mov rax, r9
    cbw
    idiv r8b
    movzx edx, ah
    movsx rax, dl
    add rsi, rax
    mov rcx, r8
    mov rax, rdi
    cbw
    idiv cl
    movsx rax, al
    add rsi, rax
    mov rcx, r8
    mov rax, rdi
    cbw
    idiv cl
    movzx edx, ah
    movsx rax, dl
    add rsi, rax
    mov rax, r9
    and al, r8b
    movsx rax, al
    mov rdx, rsi
    add rdx, rax
    mov rax, r9
    or al, r8b
    movsx rax, al
    add rdx, rax
    mov rax, r9
    xor al, r8b
    movsx rax, al
    add rdx, rax
    mov rcx, r9
    mov rax, r8
    not al
    and al, cl
    movsx rax, al
    add rdx, rax
    mov rax, r9
    shl al, 2
    movsx rax, al
    add rdx, rax
    mov rax, r9
    sar al, 2
    movsx rax, al
    add rdx, rax
    mov rax, rdi
    sar al, 2
    movsx rax, al
    add rdx, rax
    cmp r9b, r8b
    jnle 70
    jmp 71
70:
    add rdx, 1
71:
    cmp r8b, r9b
    jnge 72
    jmp 73
72:
    add rdx, 2
73:
    cmp r9b, r8b
    jnl 74
    jmp 75
74:
    add rdx, 4
75:
    cmp r9b, r8b
    jng 76
    jmp 77
76:
    add rdx, 8
77:
    cmp r9b, r9b
    jz 78
    jmp 79
78:
    add rdx, 0x10
79:
    cmp r9b, r8b
    jnz 80
    jmp 81
80:
    add rdx, 0x20
81:
    cmp dil, r8b
    jnge 82
    jmp 83
82:
    add rdx, 0x40
83:
    mov rax, rdx
    ret
84:
    mov r8, rdi
    mov rdi, rdx
    mov rax, r8
    imul ax, si
    movsx rax, ax
    xor ecx, ecx
    add rcx, rax
    mov rax, r8
    cwd
    idiv si
    movsx rax, ax
    add rcx, rax
    mov rax, r8
    cwd
    idiv si
    movsx rax, dx
    add rcx, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, ax
    add rcx, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, dx
    add rcx, rax
    mov rax, r8
    and ax, si
    movsx rax, ax
    add rcx, rax
    mov rax, r8
    or ax, si
    movsx rax, ax
    add rcx, rax
    mov rax, r8
    xor ax, si
    movsx rax, ax
    add rcx, rax
    mov rax, rcx
    mov rdx, r8
    mov rcx, rsi
    not cx
    and cx, dx
    movsx rcx, cx
    add rax, rcx
    mov rcx, r8
    sal cx, 2
    movsx rcx, cx
    add rax, rcx
    mov rcx, r8
    sar cx, 2
    movsx rcx, cx
    add rax, rcx
    mov rcx, rdi
    sar cx, 2
    movsx rcx, cx
    add rax, rcx
    cmp r8w, si
    jnle 85
    jmp 86
85:
    add rax, 1
86:
    mov rdx, rax
    cmp si, r8w
    jnge 87
    jmp 88
87:
    add rdx, 2
88:
    cmp r8w, si
    jnl 89
    jmp 90
89:
    add rdx, 4
90:
    cmp r8w, si
    jng 91
    jmp 92
91:
    add rdx, 8
92:
    cmp r8w, r8w
    jz 93
    jmp 94
93:
    add rdx, 0x10
94:
    cmp r8w, si
    jnz 95
    jmp 96
95:
    add rdx, 0x20
96:
    cmp di, si
    jnge 97
    jmp 98
97:
    add rdx, 0x40
98:
    mov rax, rdx
    ret
99:
    mov r8, rdi
    mov rdi, rdx
    mov rax, r8
    imul eax, esi
    movsxd rax, eax
    xor ecx, ecx
    add rcx, rax
    mov rax, r8
    cdq
    idiv esi
    movsxd rax, eax
    add rcx, rax
    mov rax, r8
    cdq
    idiv esi
    movsxd rax, edx
    add rcx, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, eax
    add rcx, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, edx
    add rcx, rax
    mov rax, r8
    and eax, esi
    movsxd rax, eax
    add rcx, rax
    mov rax, r8
    or eax, esi
    movsxd rax, eax
    add rcx, rax
    mov rax, r8
    xor eax, esi
    movsxd rax, eax
    add rcx, rax
    mov rax, rcx
    mov rdx, r8
    mov rcx, rsi
    not ecx
    and ecx, edx
    movsxd rcx, ecx
    add rax, rcx
    mov rcx, r8
    sal ecx, 2
    movsxd rcx, ecx
    add rax, rcx
    mov rcx, r8
    sar ecx, 2
    movsxd rcx, ecx
    add rax, rcx
    mov rcx, rdi
    sar ecx, 2
    movsxd rcx, ecx
    add rax, rcx
    cmp r8d, esi
    jnle 100
    jmp 101
100:
    add rax, 1
101:
    mov rdx, rax
    cmp esi, r8d
    jnge 102
    jmp 103
102:
    add rdx, 2
103:
    cmp r8d, esi
    jnl 104
    jmp 105
104:
    add rdx, 4
105:
    cmp r8d, esi
    jng 106
    jmp 107
106:
    add rdx, 8
107:
    cmp r8d, r8d
    jz 108
    jmp 109
108:
    add rdx, 0x10
109:
    cmp r8d, esi
    jnz 110
    jmp 111
110:
    add rdx, 0x20
111:
    cmp edi, esi
    jnge 112
    jmp 113
112:
    add rdx, 0x40
113:
    mov rax, rdx
    ret
=========== OPT LEVEL: mininal ===========
batch:
    push rbx
batch int:
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0x14
    mov rsi, 6
    mov rdx, -7
    call 9
    mov r15, rax
    mov dl, 7
    mov dil, 0x14
    mov sil, 6
    call 24
    mov r14, rax
batch i8:
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 39
    mov r13, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 54
    mov r12, rax
    mov dl, -7
    mov dil, 0x14
    mov sil, 6
    call 69
    mov rbp, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 84
    mov rbx, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 99
    mov rcx, rax
    xor eax, eax
    add rax, r15
    add rax, r14
    add rax, r13
    add rax, r12
    add rax, rbp
    add rax, rbx
    add rax, rcx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
9:
    mov r8, rdi
    mov rdi, rdx
    mov rax, r8
    imul rax, rsi
    xor ecx, ecx
    add rcx, rax
    mov rax, r8
    cqo
    idiv rsi
    add rcx, rax
    mov rax, r8
    cqo
    idiv rsi
    add rcx, rdx
    mov rax, rdi
    cqo
    idiv rsi
    add rcx, rax
    mov rax, rdi
    cqo
    idiv rsi
    add rcx, rdx
    mov rax, r8
    and rax, rsi
    add rcx, rax
    mov rax, r8
    or rax, rsi
    add rcx, rax
    mov rax, r8
    xor rax, rsi
    add rcx, rax
    mov rdx, r8
    mov rax, rsi
    not rax
    and rax, rdx
    add rcx, rax
    mov rax, r8
    sal rax, 2
    add rcx, rax
    mov rax, r8
    sar rax, 2
    add rcx, rax
    mov rax, rdi
    sar rax, 2
    add rcx, rax
    cmp r8, rsi
    jnle 10
    jmp 11
10:
    add rcx, 1
11:
    cmp rsi, r8
    jnge 12
    jmp 13
12:
    add rcx, 2
13:
    cmp r8, rsi
    jnl 14
    jmp 15
14:
    add rcx, 4
15:
    cmp r8, rsi
    jng 16
    jmp 17
16:
    add rcx, 8
17:
    mov rax, 1
    test rax, rax
    jnz 18
    jmp 19
18:
    add rcx, 0x10
19:
    cmp r8, rsi
    jnz 20
    jmp 21
20:
    add rcx, 0x20
21:
    cmp rdi, rsi
    jnge 22
    jmp 23
22:
    add rcx, 0x40
23:
    mov rax, rcx
    ret
24:
    mov r9, rdi
    mov r8, rsi
    mov rdi, rdx
    mov rcx, r9
    mov rax, r8
    imul cl
    movzx eax, al
    xor esi, esi
    add rsi, rax
    mov rax, r9
    movzx eax, al
    div r8b
    movzx eax, al
    add rsi, rax
    mov rax, r9
    movzx eax, al
    div r8b
    movsx edx, ah
    movzx eax, dl
    add rsi, rax
    mov rcx, r8
    mov rax, rdi
    movzx eax, al
    div cl
    movzx eax, al
    add rsi, rax
    mov rcx, r8
    mov rax, rdi
    movzx eax, al
    div cl
    movsx edx, ah
    movzx eax, dl
    add rsi, rax
    mov rax, r9
    and al, r8b
    movzx eax, al
    mov rdx, rsi
    add rdx, rax
    mov rax, r9
    or al, r8b
    movzx eax, al
    add rdx, rax
    mov rax, r9
    xor al, r8b
    movzx eax, al
    add rdx, rax
    mov rcx, r9
    mov rax, r8
    not al
    and al, cl
    movzx eax, al
    add rdx, rax
    mov rax, r9
    shl al, 2
    movzx eax, al
    add rdx, rax
    mov rax, r9
    shr al, 2
    movzx eax, al
    add rdx, rax
    mov rax, rdi
    shr al, 2
    movzx eax, al
    add rdx, rax
    cmp r9b, r8b
    jnbe 25
    jmp 26
25:
    add rdx, 1
26:
    cmp r8b, r9b
    jc 27
    jmp 28
27:
    add rdx, 2
28:
    cmp r9b, r8b
    jnc 29
    jmp 30
29:
    add rdx, 4
30:
    cmp r9b, r8b
    jbe 31
    jmp 32
31:
    add rdx, 8
32:
    mov rax, 1
    test rax, rax
    jnz 33
    jmp 34
33:
    add rdx, 0x10
34:
    cmp r9b, r8b
    jnz 35
    jmp 36
35:
    add rdx, 0x20
36:
    cmp dil, r8b
    jc 37
    jmp 38
37:
    add rdx, 0x40
38:
    mov rax, rdx
    ret
39:
    mov r8, rdi
    mov rdi, rdx
    mov rax, r8
    imul ax, si
    movzx eax, ax
    xor ecx, ecx
    add rcx, rax
    mov rax, r8
    xor rdx, rdx
    div si
    movzx eax, ax
    add rcx, rax
    mov rax, r8
    xor rdx, rdx
    div si
    movzx eax, dx
    add rcx, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, ax
    add rcx, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, dx
    add rcx, rax
    mov rax, r8
    and ax, si
    movzx eax, ax
    add rcx, rax
    mov rax, r8
    or ax, si
    movzx eax, ax
    add rcx, rax
    mov rax, r8
    xor ax, si
    movzx eax, ax
    add rcx, rax
    mov rax, rcx
    mov rdx, r8
    mov rcx, rsi
    not cx
    and cx, dx
    movzx ecx, cx
    add rax, rcx
    mov rcx, r8
    sal cx, 2
    movzx ecx, cx
    add rax, rcx
    mov rcx, r8
    shr cx, 2
    movzx ecx, cx
    add rax, rcx
    mov rcx, rdi
    shr cx, 2
    movzx ecx, cx
    add rax, rcx
    cmp r8w, si
    jnbe 40
    jmp 41
40:
    add rax, 1
41:
    mov rcx, rax
    cmp si, r8w
    jc 42
    jmp 43
42:
    add rcx, 2
43:
    cmp r8w, si
    jnc 44
    jmp 45
44:
    add rcx, 4
45:
    cmp r8w, si
    jbe 46
    jmp 47
46:
    add rcx, 8
47:
    mov rax, 1
    test rax, rax
    jnz 48
    jmp 49
48:
    add rcx, 0x10
49:
    cmp r8w, si
    jnz 50
    jmp 51
50:
    add rcx, 0x20
51:
    cmp di, si
    jc 52
    jmp 53
52:
    add rcx, 0x40
53:
    mov rax, rcx
    ret
54:
    mov r8, rdi
    mov rdi, rdx
    mov rax, r8
    imul eax, esi
    mov eax, eax
    xor ecx, ecx
    add rcx, rax
    mov rax, r8
    xor rdx, rdx
    div esi
    mov eax, eax
    add rcx, rax
    mov rax, r8
    xor rdx, rdx
    div esi
    mov eax, edx
    add rcx, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, eax
    add rcx, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, edx
    add rcx, rax
    mov rax, r8
    and eax, esi
    mov eax, eax
    add rcx, rax
    mov rax, r8
    or eax, esi
    mov eax, eax
    add rcx, rax
    mov rax, r8
    xor eax, esi
    mov eax, eax
    add rcx, rax
    mov rax, rcx
    mov rdx, r8
    mov rcx, rsi
    not ecx
    and ecx, edx
    mov ecx, ecx
    add rax, rcx
    mov rcx, r8
    sal ecx, 2
    mov ecx, ecx
    add rax, rcx
    mov rcx, r8
    shr ecx, 2
    mov ecx, ecx
    add rax, rcx
    mov rcx, rdi
    shr ecx, 2
    mov ecx, ecx
    add rax, rcx
    cmp r8d, esi
    jnbe 55
    jmp 56
55:
    add rax, 1
56:
    mov rcx, rax
    cmp esi, r8d
    jc 57
    jmp 58
57:
    add rcx, 2
58:
    cmp r8d, esi
    jnc 59
    jmp 60
59:
    add rcx, 4
60:
    cmp r8d, esi
    jbe 61
    jmp 62
61:
    add rcx, 8
62:
    mov rax, 1
    test rax, rax
    jnz 63
    jmp 64
63:
    add rcx, 0x10
64:
    cmp r8d, esi
    jnz 65
    jmp 66
65:
    add rcx, 0x20
66:
    cmp edi, esi
    jc 67
    jmp 68
67:
    add rcx, 0x40
68:
    mov rax, rcx
    ret
69:
    mov r9, rdi
    mov r8, rsi
    mov rdi, rdx
    mov rcx, r9
    mov rax, r8
    imul cl
    movsx rax, al
    xor esi, esi
    add rsi, rax
    mov rax, r9
    cbw
    idiv r8b
    movsx rax, al
    add rsi, rax
    mov rax, r9
    cbw
    idiv r8b
    movzx edx, ah
    movsx rax, dl
    add rsi, rax
    mov rcx, r8
    mov rax, rdi
    cbw
    idiv cl
    movsx rax, al
    add rsi, rax
    mov rcx, r8
    mov rax, rdi
    cbw
    idiv cl
    movzx edx, ah
    movsx rax, dl
    add rsi, rax
    mov rax, r9
    and al, r8b
    movsx rax, al
    mov rdx, rsi
    add rdx, rax
    mov rax, r9
    or al, r8b
    movsx rax, al
    add rdx, rax
    mov rax, r9
    xor al, r8b
    movsx rax, al
    add rdx, rax
    mov rcx, r9
    mov rax, r8
    not al
    and al, cl
    movsx rax, al
    add rdx, rax
    mov rax, r9
    shl al, 2
    movsx rax, al
    add rdx, rax
    mov rax, r9
    sar al, 2
    movsx rax, al
    add rdx, rax
    mov rax, rdi
    sar al, 2
    movsx rax, al
    add rdx, rax
    cmp r9b, r8b
    jnle 70
    jmp 71
70:
    add rdx, 1
71:
    cmp r8b, r9b
    jnge 72
    jmp 73
72:
    add rdx, 2
73:
    cmp r9b, r8b
    jnl 74
    jmp 75
74:
    add rdx, 4
75:
    cmp r9b, r8b
    jng 76
    jmp 77
76:
    add rdx, 8
77:
    mov rax, 1
    test rax, rax
    jnz 78
    jmp 79
78:
    add rdx, 0x10
79:
    cmp r9b, r8b
    jnz 80
    jmp 81
80:
    add rdx, 0x20
81:
    cmp dil, r8b
    jnge 82
    jmp 83
82:
    add rdx, 0x40
83:
    mov rax, rdx
    ret
84:
    mov r8, rdi
    mov rdi, rdx
    mov rax, r8
    imul ax, si
    movsx rax, ax
    xor ecx, ecx
    add rcx, rax
    mov rax, r8
    cwd
    idiv si
    movsx rax, ax
    add rcx, rax
    mov rax, r8
    cwd
    idiv si
    movsx rax, dx
    add rcx, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, ax
    add rcx, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, dx
    add rcx, rax
    mov rax, r8
    and ax, si
    movsx rax, ax
    add rcx, rax
    mov rax, r8
    or ax, si
    movsx rax, ax
    add rcx, rax
    mov rax, r8
    xor ax, si
    movsx rax, ax
    add rcx, rax
    mov rax, rcx
    mov rdx, r8
    mov rcx, rsi
    not cx
    and cx, dx
    movsx rcx, cx
    add rax, rcx
    mov rcx, r8
    sal cx, 2
    movsx rcx, cx
    add rax, rcx
    mov rcx, r8
    sar cx, 2
    movsx rcx, cx
    add rax, rcx
    mov rcx, rdi
    sar cx, 2
    movsx rcx, cx
    add rax, rcx
    cmp r8w, si
    jnle 85
    jmp 86
85:
    add rax, 1
86:
    mov rcx, rax
    cmp si, r8w
    jnge 87
    jmp 88
87:
    add rcx, 2
88:
    cmp r8w, si
    jnl 89
    jmp 90
89:
    add rcx, 4
90:
    cmp r8w, si
    jng 91
    jmp 92
91:
    add rcx, 8
92:
    mov rax, 1
    test rax, rax
    jnz 93
    jmp 94
93:
    add rcx, 0x10
94:
    cmp r8w, si
    jnz 95
    jmp 96
95:
    add rcx, 0x20
96:
    cmp di, si
    jnge 97
    jmp 98
97:
    add rcx, 0x40
98:
    mov rax, rcx
    ret
99:
    mov r8, rdi
    mov rdi, rdx
    mov rax, r8
    imul eax, esi
    movsxd rax, eax
    xor ecx, ecx
    add rcx, rax
    mov rax, r8
    cdq
    idiv esi
    movsxd rax, eax
    add rcx, rax
    mov rax, r8
    cdq
    idiv esi
    movsxd rax, edx
    add rcx, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, eax
    add rcx, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, edx
    add rcx, rax
    mov rax, r8
    and eax, esi
    movsxd rax, eax
    add rcx, rax
    mov rax, r8
    or eax, esi
    movsxd rax, eax
    add rcx, rax
    mov rax, r8
    xor eax, esi
    movsxd rax, eax
    add rcx, rax
    mov rax, rcx
    mov rdx, r8
    mov rcx, rsi
    not ecx
    and ecx, edx
    movsxd rcx, ecx
    add rax, rcx
    mov rcx, r8
    sal ecx, 2
    movsxd rcx, ecx
    add rax, rcx
    mov rcx, r8
    sar ecx, 2
    movsxd rcx, ecx
    add rax, rcx
    mov rcx, rdi
    sar ecx, 2
    movsxd rcx, ecx
    add rax, rcx
    cmp r8d, esi
    jnle 100
    jmp 101
100:
    add rax, 1
101:
    mov rcx, rax
    cmp esi, r8d
    jnge 102
    jmp 103
102:
    add rcx, 2
103:
    cmp r8d, esi
    jnl 104
    jmp 105
104:
    add rcx, 4
105:
    cmp r8d, esi
    jng 106
    jmp 107
106:
    add rcx, 8
107:
    mov rax, 1
    test rax, rax
    jnz 108
    jmp 109
108:
    add rcx, 0x10
109:
    cmp r8d, esi
    jnz 110
    jmp 111
110:
    add rcx, 0x20
111:
    cmp edi, esi
    jnge 112
    jmp 113
112:
    add rcx, 0x40
113:
    mov rax, rcx
    ret
=========== OPT LEVEL: moderate ===========
batch:
    push rbx
batch int:
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0x14
    mov rsi, 6
    mov rdx, -7
    call 9
    mov r15, rax
    mov dl, 7
    mov dil, 0x14
    mov sil, 6
batch i8:
    call 22
    mov r14, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 35
    mov r13, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 48
    mov r12, rax
    mov dl, -7
    mov dil, 0x14
    mov sil, 6
    call 61
    mov rbp, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 74
    mov rbx, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 86
    mov rdx, rax
    mov rcx, r15
    add rcx, r14
    add rcx, r13
    add rcx, r12
    add rcx, rbp
    add rcx, rbx
    add rcx, rdx
    mov rax, rcx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
9:
    mov r8, rdi
    mov rdi, rdx
    mov rcx, r8
    imul rcx, rsi
    mov rax, r8
    cqo
    idiv rsi
    add rcx, rax
    mov rax, r8
    cqo
    idiv rsi
    add rcx, rdx
    mov rax, rdi
    cqo
    idiv rsi
    add rcx, rax
    mov rax, rdi
    cqo
    idiv rsi
    add rcx, rdx
    mov rax, r8
    and rax, rsi
    add rcx, rax
    mov rax, r8
    or rax, rsi
    add rcx, rax
    mov rax, r8
    xor rax, rsi
    add rcx, rax
    mov rdx, r8
    mov rax, rsi
    not rax
    and rax, rdx
    add rcx, rax
    mov rax, r8
    sal rax, 2
    add rcx, rax
    mov rax, r8
    sar rax, 2
    add rcx, rax
    mov rax, rdi
    sar rax, 2
    add rcx, rax
    cmp r8, rsi
    jnle 10
    jmp 11
10:
    add rcx, 1
11:
    cmp rsi, r8
    jnge 12
    jmp 13
12:
    add rcx, 2
13:
    cmp r8, rsi
    jnl 14
    jmp 15
14:
    add rcx, 4
15:
    cmp r8, rsi
    jng 16
    jmp 17
16:
    add rcx, 8
17:
    cmp r8, rsi
    jnz 18
    add rcx, 0x10
    jmp 19
18:
    add rcx, 0x30
19:
    cmp rdi, rsi
    jnge 20
    jmp 21
20:
    add rcx, 0x40
21:
    mov rax, rcx
    ret
22:
    mov r9, rdi
    mov r8, rsi
    mov rdi, rdx
    mov rcx, r9
    mov rax, r8
    imul cl
    movzx esi, al
    mov rax, r9
    movzx eax, al
    div r8b
    movzx eax, al
    add rsi, rax
    mov rax, r9
    movzx eax, al
    div r8b
    movsx edx, ah
    movzx eax, dl
    add rsi, rax
    mov rcx, r8
    mov rax, rdi
    movzx eax, al
    div cl
    movzx eax, al
    add rsi, rax
    mov rcx, r8
    mov rax, rdi
    movzx eax, al
    div cl
    movsx edx, ah
    movzx eax, dl
    add rsi, rax
    mov rax, r9
    and al, r8b
    movzx eax, al
    mov rdx, rsi
    add rdx, rax
    mov rax, r9
    or al, r8b
    movzx eax, al
    add rdx, rax
    mov rax, r9
    xor al, r8b
    movzx eax, al
    add rdx, rax
    mov rcx, r9
    mov rax, r8
    not al
    and al, cl
    movzx eax, al
    add rdx, rax
    mov rax, r9
    shl al, 2
    movzx eax, al
    add rdx, rax
    mov rax, r9
    shr al, 2
    movzx eax, al
    add rdx, rax
    mov rax, rdi
    shr al, 2
    movzx eax, al
    add rdx, rax
    cmp r9b, r8b
    jnbe 23
    jmp 24
23:
    add rdx, 1
24:
    cmp r8b, r9b
    jc 25
    jmp 26
25:
    add rdx, 2
26:
    cmp r9b, r8b
    jnc 27
    jmp 28
27:
    add rdx, 4
28:
    cmp r9b, r8b
    jbe 29
    jmp 30
29:
    add rdx, 8
30:
    cmp r9b, r8b
    jnz 31
    add rdx, 0x10
    jmp 32
31:
    add rdx, 0x30
32:
    cmp dil, r8b
    jc 33
    jmp 34
33:
    add rdx, 0x40
34:
    mov rax, rdx
    ret
35:
    mov r8, rdi
    mov rdi, rdx
    mov rax, r8
    imul ax, si
    movzx ecx, ax
    mov rax, r8
    xor rdx, rdx
    div si
    movzx eax, ax
    add rcx, rax
    mov rax, r8
    xor rdx, rdx
    div si
    movzx eax, dx
    add rcx, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, ax
    add rcx, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, dx
    add rcx, rax
    mov rax, r8
    and ax, si
    movzx eax, ax
    add rcx, rax
    mov rax, r8
    or ax, si
    movzx eax, ax
    add rcx, rax
    mov rax, r8
    xor ax, si
    movzx eax, ax
    add rcx, rax
    mov rax, rcx
    mov rdx, r8
    mov rcx, rsi
    not cx
    and cx, dx
    movzx ecx, cx
    add rax, rcx
    mov rcx, r8
    sal cx, 2
    movzx ecx, cx
    add rax, rcx
    mov rcx, r8
    shr cx, 2
    movzx ecx, cx
    add rax, rcx
    mov rcx, rdi
    shr cx, 2
    movzx ecx, cx
    add rax, rcx
    cmp r8w, si
    jnbe 36
    jmp 37
36:
    add rax, 1
37:
    mov rcx, rax
    cmp si, r8w
    jc 38
    jmp 39
38:
    add rcx, 2
39:
    cmp r8w, si
    jnc 40
    jmp 41
40:
    add rcx, 4
41:
    cmp r8w, si
    jbe 42
    jmp 43
42:
    add rcx, 8
43:
    cmp r8w, si
    jnz 44
    add rcx, 0x10
    jmp 45
44:
    add rcx, 0x30
45:
    cmp di, si
    jc 46
    jmp 47
46:
    add rcx, 0x40
47:
    mov rax, rcx
    ret
48:
    mov r8, rdi
    mov rdi, rdx
    mov rax, r8
    imul eax, esi
    mov ecx, eax
    mov rax, r8
    xor rdx, rdx
    div esi
    mov eax, eax
    add rcx, rax
    mov rax, r8
    xor rdx, rdx
    div esi
    mov eax, edx
    add rcx, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, eax
    add rcx, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, edx
    add rcx, rax
    mov rax, r8
    and eax, esi
    mov eax, eax
    add rcx, rax
    mov rax, r8
    or eax, esi
    mov eax, eax
    add rcx, rax
    mov rax, r8
    xor eax, esi
    mov eax, eax
    add rcx, rax
    mov rax, rcx
    mov rdx, r8
    mov rcx, rsi
    not ecx
    and ecx, edx
    mov ecx, ecx
    add rax, rcx
    mov rcx, r8
    sal ecx, 2
    mov ecx, ecx
    add rax, rcx
    mov rcx, r8
    shr ecx, 2
    mov ecx, ecx
    add rax, rcx
    mov rcx, rdi
    shr ecx, 2
    mov ecx, ecx
    add rax, rcx
    cmp r8d, esi
    jnbe 49
    jmp 50
49:
    add rax, 1
50:
    mov rcx, rax
    cmp esi, r8d
    jc 51
    jmp 52
51:
    add rcx, 2
52:
    cmp r8d, esi
    jnc 53
    jmp 54
53:
    add rcx, 4
54:
    cmp r8d, esi
    jbe 55
    jmp 56
55:
    add rcx, 8
56:
    cmp r8d, esi
    jnz 57
    add rcx, 0x10
    jmp 58
57:
    add rcx, 0x30
58:
    cmp edi, esi
    jc 59
    jmp 60
59:
    add rcx, 0x40
60:
    mov rax, rcx
    ret
61:
    mov r9, rdi
    mov r8, rsi
    mov rdi, rdx
    mov rcx, r9
    mov rax, r8
    imul cl
    movsx rsi, al
    mov rax, r9
    cbw
    idiv r8b
    movsx rax, al
    add rsi, rax
    mov rax, r9
    cbw
    idiv r8b
    movzx edx, ah
    movsx rax, dl
    add rsi, rax
    mov rcx, r8
    mov rax, rdi
    cbw
    idiv cl
    movsx rax, al
    add rsi, rax
    mov rcx, r8
    mov rax, rdi
    cbw
    idiv cl
    movzx edx, ah
    movsx rax, dl
    add rsi, rax
    mov rax, r9
    and al, r8b
    movsx rax, al
    mov rdx, rsi
    add rdx, rax
    mov rax, r9
    or al, r8b
    movsx rax, al
    add rdx, rax
    mov rax, r9
    xor al, r8b
    movsx rax, al
    add rdx, rax
    mov rcx, r9
    mov rax, r8
    not al
    and al, cl
    movsx rax, al
    add rdx, rax
    mov rax, r9
    shl al, 2
    movsx rax, al
    add rdx, rax
    mov rax, r9
    sar al, 2
    movsx rax, al
    add rdx, rax
    mov rax, rdi
    sar al, 2
    movsx rax, al
    add rdx, rax
    cmp r9b, r8b
    jnle 62
    jmp 63
62:
    add rdx, 1
63:
    cmp r8b, r9b
    jnge 64
    jmp 65
64:
    add rdx, 2
65:
    cmp r9b, r8b
    jnl 66
    jmp 67
66:
    add rdx, 4
67:
    cmp r9b, r8b
    jng 68
    jmp 69
68:
    add rdx, 8
69:
    cmp r9b, r8b
    jnz 70
    add rdx, 0x10
    jmp 71
70:
    add rdx, 0x30
71:
    cmp dil, r8b
    jnge 72
    jmp 73
72:
    add rdx, 0x40
73:
    mov rax, rdx
    ret
74:
    mov r8, rdi
    mov rdi, rdx
    mov rax, r8
    imul ax, si
    movsx rcx, ax
    mov rax, r8
    cwd
    idiv si
    movsx rax, ax
    add rcx, rax
    mov rax, r8
    cwd
    idiv si
    movsx rax, dx
    add rcx, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, ax
    add rcx, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, dx
    add rcx, rax
    mov rax, r8
    and ax, si
    movsx rax, ax
    add rcx, rax
    mov rax, r8
    or ax, si
    movsx rax, ax
    add rcx, rax
    mov rax, r8
    xor ax, si
    movsx rax, ax
    add rcx, rax
    mov rax, rcx
    mov rdx, r8
    mov rcx, rsi
    not cx
    and cx, dx
    movsx rcx, cx
    add rax, rcx
    mov rcx, r8
    sal cx, 2
    movsx rcx, cx
    add rax, rcx
    mov rcx, r8
    sar cx, 2
    movsx rcx, cx
    add rax, rcx
    mov rcx, rdi
    sar cx, 2
    movsx rcx, cx
    add rax, rcx
    cmp r8w, si
    jnle 75
    jmp 76
75:
    add rax, 1
76:
    mov rcx, rax
    cmp si, r8w
    jnge 77
    jmp +0x7
77:
    add rcx, 2
    cmp r8w, si
    jnl 78
    jmp 79
78:
    add rcx, 4
79:
    cmp r8w, si
    jng 80
    jmp 81
80:
    add rcx, 8
81:
    cmp r8w, si
    jnz 82
    add rcx, 0x10
    jmp 83
82:
    add rcx, 0x30
83:
    cmp di, si
    jnge 84
    jmp 85
84:
    add rcx, 0x40
85:
    mov rax, rcx
    ret
86:
    mov r8, rdi
    mov rdi, rdx
    mov rax, r8
    imul eax, esi
    movsxd rcx, eax
    mov rax, r8
    cdq
    idiv esi
    movsxd rax, eax
    add rcx, rax
    mov rax, r8
    cdq
    idiv esi
    movsxd rax, edx
    add rcx, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, eax
    add rcx, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, edx
    add rcx, rax
    mov rax, r8
    and eax, esi
    movsxd rax, eax
    add rcx, rax
    mov rax, r8
    or eax, esi
    movsxd rax, eax
    add rcx, rax
    mov rax, r8
    xor eax, esi
    movsxd rax, eax
    add rcx, rax
    mov rax, rcx
    mov rdx, r8
    mov rcx, rsi
    not ecx
    and ecx, edx
    movsxd rcx, ecx
    add rax, rcx
    mov rcx, r8
    sal ecx, 2
    movsxd rcx, ecx
    add rax, rcx
    mov rcx, r8
    sar ecx, 2
    movsxd rcx, ecx
    add rax, rcx
    mov rcx, rdi
    sar ecx, 2
    movsxd rcx, ecx
    add rax, rcx
    cmp r8d, esi
    jnle 87
    jmp 88
87:
    add rax, 1
88:
    mov rcx, rax
    cmp esi, r8d
    jnge 89
    jmp 90
89:
    add rcx, 2
90:
    cmp r8d, esi
    jnl 91
    jmp 92
91:
    add rcx, 4
92:
    cmp r8d, esi
    jng 93
    jmp 94
93:
    add rcx, 8
94:
    cmp r8d, esi
    jnz 95
    add rcx, 0x10
    jmp 96
95:
    add rcx, 0x30
96:
    cmp edi, esi
    jnge 97
    jmp 98
97:
    add rcx, 0x40
98:
    mov rax, rcx
    ret
=========== OPT LEVEL: all ===========
batch:
    push rbx
batch int:
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0x14
    mov rsi, 6
    mov rdx, -7
    call 9
    mov r15, rax
    mov dl, 7
    mov dil, 0x14
    mov sil, 6
batch i8:
    call 22
    mov r14, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 35
    mov r13, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 48
    mov r12, rax
    mov dl, -7
    mov dil, 0x14
    mov sil, 6
    call 61
    mov rbp, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 74
    mov rbx, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 86
    mov rdx, rax
    mov rcx, r15
    add rcx, r14
    add rcx, r13
    add rcx, r12
    add rcx, rbp
    add rcx, rbx
    add rcx, rdx
    mov rax, rcx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
9:
    mov r8, rdi
    mov rdi, rdx
    mov rcx, r8
    imul rcx, rsi
    mov rax, r8
    cqo
    idiv rsi
    add rcx, rax
    mov rax, r8
    cqo
    idiv rsi
    add rcx, rdx
    mov rax, rdi
    cqo
    idiv rsi
    add rcx, rax
    mov rax, rdi
    cqo
    idiv rsi
    add rcx, rdx
    mov rax, r8
    and rax, rsi
    add rcx, rax
    mov rax, r8
    or rax, rsi
    add rcx, rax
    mov rax, r8
    xor rax, rsi
    add rcx, rax
    mov rdx, r8
    mov rax, rsi
    not rax
    and rax, rdx
    add rcx, rax
    mov rax, r8
    sal rax, 2
    add rcx, rax
    mov rax, r8
    sar rax, 2
    add rcx, rax
    mov rax, rdi
    sar rax, 2
    add rcx, rax
    cmp r8, rsi
    jnle 10
    jmp 11
10:
    add rcx, 1
11:
    cmp rsi, r8
    jnge 12
    jmp 13
12:
    add rcx, 2
13:
    cmp r8, rsi
    jnl 14
    jmp 15
14:
    add rcx, 4
15:
    cmp r8, rsi
    jng 16
    jmp 17
16:
    add rcx, 8
17:
    cmp r8, rsi
    jnz 18
    add rcx, 0x10
    jmp 19
18:
    add rcx, 0x30
19:
    cmp rdi, rsi
    jnge 20
    jmp 21
20:
    add rcx, 0x40
21:
    mov rax, rcx
    ret
22:
    mov r9, rdi
    mov r8, rsi
    mov rdi, rdx
    mov rcx, r9
    mov rax, r8
    imul cl
    movzx esi, al
    mov rax, r9
    movzx eax, al
    div r8b
    movzx eax, al
    add rsi, rax
    mov rax, r9
    movzx eax, al
    div r8b
    movsx edx, ah
    movzx eax, dl
    add rsi, rax
    mov rcx, r8
    mov rax, rdi
    movzx eax, al
    div cl
    movzx eax, al
    add rsi, rax
    mov rcx, r8
    mov rax, rdi
    movzx eax, al
    div cl
    movsx edx, ah
    movzx eax, dl
    add rsi, rax
    mov rax, r9
    and al, r8b
    movzx eax, al
    mov rdx, rsi
    add rdx, rax
    mov rax, r9
    or al, r8b
    movzx eax, al
    add rdx, rax
    mov rax, r9
    xor al, r8b
    movzx eax, al
    add rdx, rax
    mov rcx, r9
    mov rax, r8
    not al
    and al, cl
    movzx eax, al
    add rdx, rax
    mov rax, r9
    shl al, 2
    movzx eax, al
    add rdx, rax
    mov rax, r9
    shr al, 2
    movzx eax, al
    add rdx, rax
    mov rax, rdi
    shr al, 2
    movzx eax, al
    add rdx, rax
    cmp r9b, r8b
    jnbe 23
    jmp 24
23:
    add rdx, 1
24:
    cmp r8b, r9b
    jc 25
    jmp 26
25:
    add rdx, 2
26:
    cmp r9b, r8b
    jnc 27
    jmp 28
27:
    add rdx, 4
28:
    cmp r9b, r8b
    jbe 29
    jmp 30
29:
    add rdx, 8
30:
    cmp r9b, r8b
    jnz 31
    add rdx, 0x10
    jmp 32
31:
    add rdx, 0x30
32:
    cmp dil, r8b
    jc 33
    jmp 34
33:
    add rdx, 0x40
34:
    mov rax, rdx
    ret
35:
    mov r8, rdi
    mov rdi, rdx
    mov rax, r8
    imul ax, si
    movzx ecx, ax
    mov rax, r8
    xor rdx, rdx
    div si
    movzx eax, ax
    add rcx, rax
    mov rax, r8
    xor rdx, rdx
    div si
    movzx eax, dx
    add rcx, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, ax
    add rcx, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, dx
    add rcx, rax
    mov rax, r8
    and ax, si
    movzx eax, ax
    add rcx, rax
    mov rax, r8
    or ax, si
    movzx eax, ax
    add rcx, rax
    mov rax, r8
    xor ax, si
    movzx eax, ax
    add rcx, rax
    mov rax, rcx
    mov rdx, r8
    mov rcx, rsi
    not cx
    and cx, dx
    movzx ecx, cx
    add rax, rcx
    mov rcx, r8
    sal cx, 2
    movzx ecx, cx
    add rax, rcx
    mov rcx, r8
    shr cx, 2
    movzx ecx, cx
    add rax, rcx
    mov rcx, rdi
    shr cx, 2
    movzx ecx, cx
    add rax, rcx
    cmp r8w, si
    jnbe 36
    jmp 37
36:
    add rax, 1
37:
    mov rcx, rax
    cmp si, r8w
    jc 38
    jmp 39
38:
    add rcx, 2
39:
    cmp r8w, si
    jnc 40
    jmp 41
40:
    add rcx, 4
41:
    cmp r8w, si
    jbe 42
    jmp 43
42:
    add rcx, 8
43:
    cmp r8w, si
    jnz 44
    add rcx, 0x10
    jmp 45
44:
    add rcx, 0x30
45:
    cmp di, si
    jc 46
    jmp 47
46:
    add rcx, 0x40
47:
    mov rax, rcx
    ret
48:
    mov r8, rdi
    mov rdi, rdx
    mov rax, r8
    imul eax, esi
    mov ecx, eax
    mov rax, r8
    xor rdx, rdx
    div esi
    mov eax, eax
    add rcx, rax
    mov rax, r8
    xor rdx, rdx
    div esi
    mov eax, edx
    add rcx, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, eax
    add rcx, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, edx
    add rcx, rax
    mov rax, r8
    and eax, esi
    mov eax, eax
    add rcx, rax
    mov rax, r8
    or eax, esi
    mov eax, eax
    add rcx, rax
    mov rax, r8
    xor eax, esi
    mov eax, eax
    add rcx, rax
    mov rax, rcx
    mov rdx, r8
    mov rcx, rsi
    not ecx
    and ecx, edx
    mov ecx, ecx
    add rax, rcx
    mov rcx, r8
    sal ecx, 2
    mov ecx, ecx
    add rax, rcx
    mov rcx, r8
    shr ecx, 2
    mov ecx, ecx
    add rax, rcx
    mov rcx, rdi
    shr ecx, 2
    mov ecx, ecx
    add rax, rcx
    cmp r8d, esi
    jnbe 49
    jmp 50
49:
    add rax, 1
50:
    mov rcx, rax
    cmp esi, r8d
    jc 51
    jmp 52
51:
    add rcx, 2
52:
    cmp r8d, esi
    jnc 53
    jmp 54
53:
    add rcx, 4
54:
    cmp r8d, esi
    jbe 55
    jmp 56
55:
    add rcx, 8
56:
    cmp r8d, esi
    jnz 57
    add rcx, 0x10
    jmp 58
57:
    add rcx, 0x30
58:
    cmp edi, esi
    jc 59
    jmp 60
59:
    add rcx, 0x40
60:
    mov rax, rcx
    ret
61:
    mov r9, rdi
    mov r8, rsi
    mov rdi, rdx
    mov rcx, r9
    mov rax, r8
    imul cl
    movsx rsi, al
    mov rax, r9
    cbw
    idiv r8b
    movsx rax, al
    add rsi, rax
    mov rax, r9
    cbw
    idiv r8b
    movzx edx, ah
    movsx rax, dl
    add rsi, rax
    mov rcx, r8
    mov rax, rdi
    cbw
    idiv cl
    movsx rax, al
    add rsi, rax
    mov rcx, r8
    mov rax, rdi
    cbw
    idiv cl
    movzx edx, ah
    movsx rax, dl
    add rsi, rax
    mov rax, r9
    and al, r8b
    movsx rax, al
    mov rdx, rsi
    add rdx, rax
    mov rax, r9
    or al, r8b
    movsx rax, al
    add rdx, rax
    mov rax, r9
    xor al, r8b
    movsx rax, al
    add rdx, rax
    mov rcx, r9
    mov rax, r8
    not al
    and al, cl
    movsx rax, al
    add rdx, rax
    mov rax, r9
    shl al, 2
    movsx rax, al
    add rdx, rax
    mov rax, r9
    sar al, 2
    movsx rax, al
    add rdx, rax
    mov rax, rdi
    sar al, 2
    movsx rax, al
    add rdx, rax
    cmp r9b, r8b
    jnle 62
    jmp 63
62:
    add rdx, 1
63:
    cmp r8b, r9b
    jnge 64
    jmp 65
64:
    add rdx, 2
65:
    cmp r9b, r8b
    jnl 66
    jmp 67
66:
    add rdx, 4
67:
    cmp r9b, r8b
    jng 68
    jmp 69
68:
    add rdx, 8
69:
    cmp r9b, r8b
    jnz 70
    add rdx, 0x10
    jmp 71
70:
    add rdx, 0x30
71:
    cmp dil, r8b
    jnge 72
    jmp 73
72:
    add rdx, 0x40
73:
    mov rax, rdx
    ret
74:
    mov r8, rdi
    mov rdi, rdx
    mov rax, r8
    imul ax, si
    movsx rcx, ax
    mov rax, r8
    cwd
    idiv si
    movsx rax, ax
    add rcx, rax
    mov rax, r8
    cwd
    idiv si
    movsx rax, dx
    add rcx, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, ax
    add rcx, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, dx
    add rcx, rax
    mov rax, r8
    and ax, si
    movsx rax, ax
    add rcx, rax
    mov rax, r8
    or ax, si
    movsx rax, ax
    add rcx, rax
    mov rax, r8
    xor ax, si
    movsx rax, ax
    add rcx, rax
    mov rax, rcx
    mov rdx, r8
    mov rcx, rsi
    not cx
    and cx, dx
    movsx rcx, cx
    add rax, rcx
    mov rcx, r8
    sal cx, 2
    movsx rcx, cx
    add rax, rcx
    mov rcx, r8
    sar cx, 2
    movsx rcx, cx
    add rax, rcx
    mov rcx, rdi
    sar cx, 2
    movsx rcx, cx
    add rax, rcx
    cmp r8w, si
    jnle 75
    jmp 76
75:
    add rax, 1
76:
    mov rcx, rax
    cmp si, r8w
    jnge 77
    jmp +0x7
77:
    add rcx, 2
    cmp r8w, si
    jnl 78
    jmp 79
78:
    add rcx, 4
79:
    cmp r8w, si
    jng 80
    jmp 81
80:
    add rcx, 8
81:
    cmp r8w, si
    jnz 82
    add rcx, 0x10
    jmp 83
82:
    add rcx, 0x30
83:
    cmp di, si
    jnge 84
    jmp 85
84:
    add rcx, 0x40
85:
    mov rax, rcx
    ret
86:
    mov r8, rdi
    mov rdi, rdx
    mov rax, r8
    imul eax, esi
    movsxd rcx, eax
    mov rax, r8
    cdq
    idiv esi
    movsxd rax, eax
    add rcx, rax
    mov rax, r8
    cdq
    idiv esi
    movsxd rax, edx
    add rcx, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, eax
    add rcx, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, edx
    add rcx, rax
    mov rax, r8
    and eax, esi
    movsxd rax, eax
    add rcx, rax
    mov rax, r8
    or eax, esi
    movsxd rax, eax
    add rcx, rax
    mov rax, r8
    xor eax, esi
    movsxd rax, eax
    add rcx, rax
    mov rax, rcx
    mov rdx, r8
    mov rcx, rsi
    not ecx
    and ecx, edx
    movsxd rcx, ecx
    add rax, rcx
    mov rcx, r8
    sal ecx, 2
    movsxd rcx, ecx
    add rax, rcx
    mov rcx, r8
    sar ecx, 2
    movsxd rcx, ecx
    add rax, rcx
    mov rcx, rdi
    sar ecx, 2
    movsxd rcx, ecx
    add rax, rcx
    cmp r8d, esi
    jnle 87
    jmp 88
87:
    add rax, 1
88:
    mov rcx, rax
    cmp esi, r8d
    jnge 89
    jmp 90
89:
    add rcx, 2
90:
    cmp r8d, esi
    jnl 91
    jmp 92
91:
    add rcx, 4
92:
    cmp r8d, esi
    jng 93
    jmp 94
93:
    add rcx, 8
94:
    cmp r8d, esi
    jnz 95
    add rcx, 0x10
    jmp 96
95:
    add rcx, 0x30
96:
    cmp edi, esi
    jnge 97
    jmp 98
97:
    add rcx, 0x40
98:
    mov rax, rcx
    ret
=========== OPT LEVEL: aggresive ===========
batch:
    mov rax, 0x9dc
batch i16:
    ret
