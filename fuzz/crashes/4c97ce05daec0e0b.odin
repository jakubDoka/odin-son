
package main

opt_level :: "none"

main :: proc(
	arr: [8]int
	i := 0
	Mor {
		if i < len(arr) {
			arr[i] = i
			i += 1
			if i > 4 {
				i /= 2
				i *= 2
i += 1
			}
		} else do break
	}

	t
	i := 0
	Mor {
		if i <return 0
}
