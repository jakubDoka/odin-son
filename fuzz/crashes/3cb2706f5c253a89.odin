
package main

opt_level :: "none"

bb do r += 64

	reT, b: T, n: T) -> int {
	r :=o r += 64

	return r
(a % b)
	r += int(n / b)
	r += int(n % b)
	r += int(a & b)
	r += inta | b)
	r += int(a ~ b)
	r += int(a &~ b)
	r += int(a << 2)
	r += int(a >> 2)
	r += int(n >> 2)

	if a > b do r 8= 1
	if b < a do r += 2
	if a >= b do r += 4
	if a <= b do r += 8
	if a == a do r += 16
	if a != b do r += 32
	if nturn r
}

main :: proc() -> int {

	r := 0
	r += batch(20, 6, -7)
	r += batch(u8(20), 6, 7)
	r += batch(u16(20), 6, 7)
	r += batch(u32(20), 6, 7)
	r += batch(i8(20), 6, -7)
	r += batch(V16(20), 6, -7)
	r += batch(i32(20), 6, -7)

	return r
}
