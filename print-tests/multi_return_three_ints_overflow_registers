=========== OPT LEVEL: none ===========
three:
    mov rax, rdi
    add rax, rsi
    mov.q [rcx], rax
    add rsi, rdx
    mov.q [r8], rsi
    add rdi, rdx
    mov rax, rdi
    ret
2:
    sub rsp, 0x18
    lea rcx, [rsp]
    mov rdx, 3
    mov rsi, 2
    mov rdi, 1
    lea r8, [rsp + 0x8]
    call three
    mov rdx, rax
    mov.q rcx, [rsp + 0x8]
    mov.q rax, [rsp]
    imul rax, rax, 0x64
    imul rcx, rcx, 0xa
    add rax, rcx
    add rax, rdx
    add rsp, 0x18
    ret
=========== OPT LEVEL: mininal ===========
three:
    mov rax, rdi
    add rax, rsi
    mov.q [rcx], rax
    add rsi, rdx
    mov.q [r8], rsi
    add rdi, rdx
    mov rax, rdi
    ret
2:
    sub rsp, 0x18
    lea rcx, [rsp]
    mov rdx, 3
    mov rsi, 2
    mov rdi, 1
    lea r8, [rsp + 0x8]
    call three
    mov rdx, rax
    mov.q rcx, [rsp + 0x8]
    mov.q rax, [rsp]
    imul rax, rax, 0x64
    imul rcx, rcx, 0xa
    add rax, rcx
    add rax, rdx
    add rsp, 0x18
    ret
=========== OPT LEVEL: moderate ===========
three:
    mov rax, rdi
    add rax, rsi
    mov.q [rcx], rax
    add rsi, rdx
    mov.q [r8], rsi
    add rdi, rdx
    mov rax, rdi
    ret
2:
    sub rsp, 0x18
    lea rcx, [rsp]
    mov rdx, 3
    mov rsi, 2
    mov rdi, 1
    lea r8, [rsp + 0x8]
    call three
    mov rdx, rax
    mov.q rcx, [rsp + 0x8]
    mov.q rax, [rsp]
    imul rax, rax, 0x64
    imul rcx, rcx, 0xa
    add rax, rcx
    add rax, rdx
    add rsp, 0x18
    ret
=========== OPT LEVEL: all ===========
three:
    mov rax, rdi
    add rax, rsi
    mov.q [rcx], rax
    add rsi, rdx
    mov.q [r8], rsi
    add rdi, rdx
    mov rax, rdi
    ret
2:
    sub rsp, 0x18
    lea rcx, [rsp]
    mov rdx, 3
    mov rsi, 2
    mov rdi, 1
    lea r8, [rsp + 0x8]
    call three
    mov rdx, rax
    mov.q rcx, [rsp + 0x8]
    mov.q rax, [rsp]
    imul rax, rax, 0x64
    imul rcx, rcx, 0xa
    add rax, rcx
    add rax, rdx
    add rsp, 0x18
    ret
=========== OPT LEVEL: aggresive ===========
three:
    mov rax, 0x162
    ret
