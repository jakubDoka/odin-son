=========== OPT LEVEL: none ===========
main:
     mov rdi, 0x00
     sub rdi, 0x05
     call :opaque
     mov rcx, rax
     mov rdx, rax
     mov rax, 0x03
     imul cx, ax
     movsx rcx, cx
     mov rax, 0x03
     imul edx, eax
     movsxd rax, edx
     add rcx, rax
     mov rax, rcx
     ret
opaque:
     mov rax, rdi
     ret
=========== OPT LEVEL: mininal ===========
main:
     mov rdi, 0xFFFFFFFFFFFFFFFB
     call :opaque
     mov rcx, rax
     mov rdx, rax
     mov rax, 0x03
     imul cx, ax
     movsx rcx, cx
     mov rax, 0x03
     imul edx, eax
     movsxd rax, edx
     add rcx, rax
     mov rax, rcx
     ret
opaque:
     mov rax, rdi
     ret
=========== OPT LEVEL: all ===========
main:
     mov rdi, 0xFFFFFFFFFFFFFFFB
     call :opaque
     mov rcx, rax
     mov rdx, rax
     mov rax, 0x03
     imul cx, ax
     movsx rcx, cx
     mov rax, 0x03
     imul edx, eax
     movsxd rax, edx
     add rcx, rax
     mov rax, rcx
     ret
opaque:
     mov rax, rdi
     ret
