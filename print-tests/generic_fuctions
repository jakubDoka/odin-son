=========== OPT LEVEL: none ===========
main:
     push rbx
     mov rdi, 0x0A
     call :fib
     mov rbx, rax
     mov rdi, 0x0A
     call :fib
     mov rcx, rax
     movsxd rax, ebx
     add rax, rcx
     pop rbx
     ret
fib:
fib:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp ebx, 0x02
     jle :0
     mov rax, rbx
     sub eax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub ebx, 0x02
     mov rdi, rbx
     call :fib
     add ebp, eax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
fib:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp rbx, 0x02
     jle :0
     mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib
     add rbp, rax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: mininal ===========
main:
     push rbx
     mov rdi, 0x0A
     call :fib
     mov rbx, rax
     mov rdi, 0x0A
     call :fib
     mov rcx, rax
     movsxd rax, ebx
     add rax, rcx
     pop rbx
     ret
fib:
fib:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp ebx, 0x02
     jle :0
     mov rax, rbx
     sub eax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub ebx, 0x02
     mov rdi, rbx
     call :fib
     add ebp, eax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
fib:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp rbx, 0x02
     jle :0
     mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib
     add rbp, rax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: moderate ===========
main:
     push rbx
     mov rdi, 0x0A
     call :fib
     mov rbx, rax
     mov rdi, 0x0A
     call :fib
     mov rcx, rax
     movsxd rax, ebx
     add rax, rcx
     pop rbx
     ret
fib:
fib:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp ebx, 0x02
     jle :0
     mov rax, rbx
     sub eax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub ebx, 0x02
     mov rdi, rbx
     call :fib
     add ebp, eax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
fib:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp rbx, 0x02
     jle :0
     mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib
     add rbp, rax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: all ===========
main:
     push rbx
     mov rdi, 0x0A
     call :fib
     mov rbx, rax
     mov rdi, 0x0A
     call :fib
     mov rcx, rax
     movsxd rax, ebx
     add rax, rcx
     pop rbx
     ret
fib:
fib:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp ebx, 0x02
     jle :0
     mov rax, rbx
     sub eax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub ebx, 0x02
     mov rdi, rbx
     call :fib
     add ebp, eax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
fib:
     push rbx
     push rbp
     sub rsp, 0x08
     mov rbx, rdi
     cmp rbx, 0x02
     jle :0
     mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib
     add rbp, rax
     jmp :1

000: mov rbp, 0x01
001: mov rax, rbp
     add rsp, 0x08
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: aggresive ===========
main:
     push rbx
     mov rdi, 0x0A
     call :fib
     mov rbx, rax
     mov rdi, 0x0A
     call :fib
     mov rcx, rax
     movsxd rax, ebx
     add rax, rcx
     pop rbx
     ret
fib:
fib:
     push rbx
     push rbp
     push r12
     mov rbx, rdi
     cmp ebx, 0x02
     jle :0
     mov rax, rbx
     sub eax, 0x01
     mov rbp, rax
     cmp ebp, 0x02
     jle :1
     mov rax, rbp
     sub eax, 0x01
     mov rdi, rax
     call :fib
     mov r12, rax
     mov rax, rbp
     sub eax, 0x02
     mov rdi, rax
     call :fib
     add r12d, eax
     jmp :2

001: mov r12, 0x01
002: mov rbp, r12
     mov rax, rbx
     sub eax, 0x02
     mov rbx, rax
     cmp ebx, 0x02
     jle :3
     mov rax, rbx
     sub eax, 0x01
     mov rdi, rax
     call :fib
     mov r12, rax
     mov rax, rbx
     sub eax, 0x02
     mov rdi, rax
     call :fib
     add r12d, eax
     jmp :4

003: mov r12, 0x01
004: mov rax, r12
     mov rcx, rbp
     add ecx, eax
     jmp :5

000: mov rcx, 0x01
005: mov rax, rcx
     pop r12
     pop rbp
     pop rbx
     ret
fib:
     push rbx
     push rbp
     push r12
     mov rbx, rdi
     cmp rbx, 0x02
     jle :0
     mov rax, rbx
     sub rax, 0x01
     mov rbp, rax
     cmp rbp, 0x02
     jle :1
     mov rax, rbp
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov r12, rax
     mov rax, rbp
     sub rax, 0x02
     mov rdi, rax
     call :fib
     add r12, rax
     jmp :2

001: mov r12, 0x01
002: mov rbp, r12
     mov rax, rbx
     sub rax, 0x02
     mov rbx, rax
     cmp rbx, 0x02
     jle :3
     mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov r12, rax
     mov rax, rbx
     sub rax, 0x02
     mov rdi, rax
     call :fib
     add r12, rax
     jmp :4

003: mov r12, 0x01
004: mov rax, r12
     mov rcx, rbp
     add rcx, rax
     jmp :5

000: mov rcx, 0x01
005: mov rax, rcx
     pop r12
     pop rbp
     pop rbx
     ret
