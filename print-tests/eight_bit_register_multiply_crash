=========== OPT LEVEL: none ===========
main:
     sub rsp, 0x08
     mov rdi, 0x14
     call :opaque
     mov rdx, rax
     xor ecx, ecx
     sub cl, 0x05
     mov al, 0x02
     imul cl
     movsx rcx, al
     mov al, 0x03
     imul dl
     movzx eax, al
     add rcx, rax
     mov rax, rcx
     add rsp, 0x08
     ret
opaque:
     mov rax, rdi
     ret
=========== OPT LEVEL: mininal ===========
main:
     sub rsp, 0x08
     mov rdi, 0x14
     call :opaque
     mov rcx, rax
     mov al, 0x03
     imul cl
     movzx eax, al
     add rax, 0xFFFFFFFFFFFFFFF6
     add rsp, 0x08
     ret
opaque:
     mov rax, rdi
     ret
=========== OPT LEVEL: moderate ===========
main:
     sub rsp, 0x08
     mov rdi, 0x14
     call :opaque
     mov rcx, rax
     mov al, 0x03
     imul cl
     movzx eax, al
     add rax, 0xFFFFFFFFFFFFFFF6
     add rsp, 0x08
     ret
opaque:
     mov rax, rdi
     ret
=========== OPT LEVEL: all ===========
main:
     sub rsp, 0x08
     mov rdi, 0x14
     call :opaque
     mov rcx, rax
     mov al, 0x03
     imul cl
     movzx eax, al
     add rax, 0xFFFFFFFFFFFFFFF6
     add rsp, 0x08
     ret
opaque:
     mov rax, rdi
     ret
=========== OPT LEVEL: aggresive ===========
main:
     mov rax, 0x32
     ret
opaque:
