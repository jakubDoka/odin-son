=========== OPT LEVEL: none ===========
main:
     push rbx
     push rbp
     mov rdi, 0x0A
     call :opaque
     mov rbp, rax
     mov rdi, 0x1E
     call :opaque
     mov rbx, rax
     mov rdi, 0x03
     call :opaque
     mov rcx, rax
     sub ebp, ebx
     mov rax, rbp
     cdq
     idiv ecx
     movsxd rax, eax
     pop rbp
     pop rbx
     ret
opaque:
     mov rax, rdi
     ret
=========== OPT LEVEL: mininal ===========
main:
     push rbx
     push rbp
     mov rdi, 0x0A
     call :opaque
     mov rbp, rax
     mov rdi, 0x1E
     call :opaque
     mov rbx, rax
     mov rdi, 0x03
     call :opaque
     mov rcx, rax
     sub ebp, ebx
     mov rax, rbp
     cdq
     idiv ecx
     movsxd rax, eax
     pop rbp
     pop rbx
     ret
opaque:
     mov rax, rdi
     ret
=========== OPT LEVEL: all ===========
main:
     push rbx
     push rbp
     mov rdi, 0x0A
     call :opaque
     mov rbp, rax
     mov rdi, 0x1E
     call :opaque
     mov rbx, rax
     mov rdi, 0x03
     call :opaque
     mov rcx, rax
     sub ebp, ebx
     mov rax, rbp
     cdq
     idiv ecx
     movsxd rax, eax
     pop rbp
     pop rbx
     ret
opaque:
     mov rax, rdi
     ret
