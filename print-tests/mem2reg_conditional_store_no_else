=========== OPT LEVEL: none ===========
opaque:
     mov rax, rdi
     ret
main:
     sub rsp, 0x18
     lea rdi, [rsp]
     mov rsi, 0x00
     mov rdx, 0x10
     call :$set
     mov rdi, 0x03
     call :opaque
     mov [rsp], rax
     mov rdi, 0x05
     call :opaque
     mov [rsp+0x08], rax
     mov rdi, 0x01
     call :opaque
     cmp rax, 0x00
     jnle :0
     mov qword ptr [rsp], 0x64
     jmp :0

000: mov rax, [rsp]
     add rsp, 0x18
     ret
=========== OPT LEVEL: mininal ===========
opaque:
     mov rax, rdi
     ret
main:
     sub rsp, 0x18
     lea rdi, [rsp]
     mov rsi, 0x00
     mov rdx, 0x10
     call :$set
     mov rdi, 0x03
     call :opaque
     mov [rsp], rax
     mov rdi, 0x05
     call :opaque
     mov [rsp+0x08], rax
     mov rdi, 0x01
     call :opaque
     cmp rax, 0x00
     jnle :0
     mov qword ptr [rsp], 0x64
     jmp :0

000: mov rax, [rsp]
     add rsp, 0x18
     ret
=========== OPT LEVEL: all ===========
opaque:
     mov rax, rdi
     ret
main:
     push rbx
     mov rdi, 0x03
     call :opaque
     mov rbx, rax
     mov rdi, 0x05
     call :opaque
     mov rdi, 0x01
     call :opaque
     cmp rax, 0x00
     jnle :0
     mov rbx, 0x64
     jmp :0

000: mov rax, rbx
     pop rbx
     ret
