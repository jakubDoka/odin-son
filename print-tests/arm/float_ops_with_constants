=========== OPT LEVEL: none-quick ===========
Error{inst_idx = 8, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 20, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 32, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 44, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 56, code = "INVALID_OPCODE", _ = [0, 0, 0]}
main:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    invalid
    bl opaque
    eor.v v1, v1, v1
    invalid
    fmul s2, s2, s0
    fadd s1, s1, s2
    invalid
    fadd s2, s2, s0
    fadd s1, s1, s2
    invalid
    fsub s2, s0, s2
    fadd s1, s1, s2
    invalid
    fdiv s0, s0, s2
    fadd s0, s1, s0
    fcvtzs x0, s0
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
opaque:
    ret
=========== OPT LEVEL: none ===========
Error{inst_idx = 8, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 20, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 32, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 44, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 56, code = "INVALID_OPCODE", _ = [0, 0, 0]}
main:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    invalid
    bl opaque
    eor.v v2, v2, v2
    invalid
    fmul s1, s1, s0
    fadd s2, s2, s1
    invalid
    fadd s1, s1, s0
    fadd s2, s2, s1
    invalid
    fsub s1, s0, s1
    fadd s2, s2, s1
    invalid
    fdiv s0, s0, s1
    fadd s0, s2, s0
    fcvtzs x0, s0
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
opaque:
    ret
=========== OPT LEVEL: mininal-quick ===========
Error{inst_idx = 8, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 20, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 32, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 44, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 56, code = "INVALID_OPCODE", _ = [0, 0, 0]}
main:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    invalid
    bl opaque
    eor.v v1, v1, v1
    invalid
    fmul s2, s2, s0
    fadd s1, s1, s2
    invalid
    fadd s2, s2, s0
    fadd s1, s1, s2
    invalid
    fsub s2, s0, s2
    fadd s1, s1, s2
    invalid
    fdiv s0, s0, s2
    fadd s0, s1, s0
    fcvtzs x0, s0
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
opaque:
    ret
=========== OPT LEVEL: mininal ===========
Error{inst_idx = 8, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 20, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 32, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 44, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 56, code = "INVALID_OPCODE", _ = [0, 0, 0]}
main:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    invalid
    bl opaque
    eor.v v2, v2, v2
    invalid
    fmul s1, s1, s0
    fadd s2, s2, s1
    invalid
    fadd s1, s1, s0
    fadd s2, s2, s1
    invalid
    fsub s1, s0, s1
    fadd s2, s2, s1
    invalid
    fdiv s0, s0, s1
    fadd s0, s2, s0
    fcvtzs x0, s0
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
opaque:
    ret
=========== OPT LEVEL: moderate-quick ===========
Error{inst_idx = 8, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 16, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 32, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 44, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 56, code = "INVALID_OPCODE", _ = [0, 0, 0]}
main:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    invalid
    bl opaque
    invalid
    fmul s1, s1, s0
    eor.v v2, v2, v2
    fadd s1, s1, s2
    invalid
    fadd s2, s2, s0
    fadd s1, s1, s2
    invalid
    fsub s2, s0, s2
    fadd s1, s1, s2
    invalid
    fdiv s0, s0, s2
    fadd s0, s1, s0
    fcvtzs x0, s0
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
opaque:
    ret
=========== OPT LEVEL: moderate ===========
Error{inst_idx = 8, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 16, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 32, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 44, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 56, code = "INVALID_OPCODE", _ = [0, 0, 0]}
main:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    invalid
    bl opaque
    invalid
    fmul s2, s1, s0
    eor.v v1, v1, v1
    fadd s2, s2, s1
    invalid
    fadd s1, s1, s0
    fadd s2, s2, s1
    invalid
    fsub s1, s0, s1
    fadd s2, s2, s1
    invalid
    fdiv s0, s0, s1
    fadd s0, s2, s0
    fcvtzs x0, s0
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
opaque:
    ret
=========== OPT LEVEL: all ===========
Error{inst_idx = 8, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 16, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 32, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 44, code = "INVALID_OPCODE", _ = [0, 0, 0]}
Error{inst_idx = 56, code = "INVALID_OPCODE", _ = [0, 0, 0]}
main:
    sub sp, sp, #16
    stp x29, x30, [xzr, #7]
    invalid
    bl opaque
    invalid
    fmul s2, s1, s0
    eor.v v1, v1, v1
    fadd s2, s2, s1
    invalid
    fadd s1, s1, s0
    fadd s2, s2, s1
    invalid
    fsub s1, s0, s1
    fadd s2, s2, s1
    invalid
    fdiv s0, s0, s1
    fadd s0, s2, s0
    fcvtzs x0, s0
    ldp x29, x30, [xzr, #7]
    add sp, sp, #16
    ret
opaque:
    ret
=========== OPT LEVEL: aggresive ===========
main:
    movz x0, #52, #0
    ret
