=========== OPT LEVEL: none-quick ===========
opaques:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rsp, 8
    ret
test_signed:
    cmp rdi, rsi
    jz 7
    cmp rdi, rsi
    jnz 6
    cmp rdi, rsi
    jnge 5
    cmp rdi, rsi
    jnl 4
    cmp rdi, rsi
    jnle 3
    cmp rdi, rsi
    jng 2
    xor eax, eax
    jmp 8
2:
    mov rax, 6
    jmp 8
3:
    mov rax, 5
    jmp 8
4:
    mov rax, 4
    jmp 8
5:
    mov rax, 3
    jmp 8
6:
    mov rax, 2
    jmp 8
7:
    mov rax, 1
8:
    ret
test_unsigned:
    cmp rdi, rsi
    jz +0x71
    cmp rdi, rsi
    jnz +0x5c
    cmp rdi, rsi
    jc +0x47
    cmp rdi, rsi
    jnc +0x32
    cmp rdi, rsi
    jnbe 11
    cmp rdi, rsi
    jbe 10
    xor eax, eax
    jmp 12
10:
    mov rax, 0x3c
    jmp 12
11:
    mov rax, 0x32
    jmp 12
    mov rax, 0x28
    jmp 12
    mov rax, 0x1e
    jmp 12
    mov rax, 0x14
    jmp 12
    mov rax, 0xa
12:
    ret
test_mixed_patterns:
    sub rsp, 0x18
    mov.q [rsp], rdi
    mov.q rdi, [rsp]
    call opaques
    mov.q [rsp + 0x8], rax
    mov.q rax, [rsp]
    add rax, 1
    mov rdi, rax
    call opaques
    mov.q rcx, [rsp + 0x8]
    cmp rcx, rax
    jnge 14
    jmp 16
14:
    mov.q rcx, [rsp + 0x8]
    cmp rcx, rax
    jng 15
    jmp 16
15:
    mov.q rcx, [rsp + 0x8]
    cmp rcx, rax
    jnz 20
16:
    mov.q rcx, [rsp + 0x8]
    cmp rcx, rax
    jnle 17
    jmp 19
17:
    mov.q rcx, [rsp + 0x8]
    cmp rcx, rax
    jnl 18
    jmp 19
18:
    mov.q rcx, [rsp + 0x8]
    cmp rcx, rax
    jz +0x8
19:
    xor eax, eax
    jmp 21
    mov rax, 0xc8
    jmp 21
20:
    mov rax, 0x64
21:
    add rsp, 0x18
    ret
main:
    sub rsp, 0x38
    mov rdi, 0xa
    mov rsi, 0x14
    call test_signed
    mov.q [rsp], rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_signed
    mov.q [rsp + 0x8], rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_signed
    mov.q [rsp + 0x10], rax
    mov rdi, 0xa
    mov rsi, 0x14
    call test_unsigned
    mov.q [rsp + 0x18], rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_unsigned
    mov.q [rsp + 0x20], rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_unsigned
    mov.q [rsp + 0x28], rax
    mov rdi, 0x2a
    call test_mixed_patterns
    mov.q [rsp + 0x30], rax
    xor eax, eax
    mov.q rcx, [rsp]
    add rax, rcx
    mov.q rcx, [rsp + 0x8]
    add rax, rcx
    mov.q rcx, [rsp + 0x10]
    add rax, rcx
    mov.q rcx, [rsp + 0x18]
    add rax, rcx
    mov.q rcx, [rsp + 0x20]
    add rax, rcx
    mov.q rcx, [rsp + 0x28]
    add rax, rcx
    mov.q rcx, [rsp + 0x30]
    add rax, rcx
    add rsp, 0x38
    ret
=========== OPT LEVEL: none ===========
opaques:
    mov rax, rdi
    ret
test_signed:
    cmp rdi, rsi
    jz 7
    cmp rdi, rsi
    jnz 6
    cmp rdi, rsi
    jnge 5
    cmp rdi, rsi
    jnl 4
    cmp rdi, rsi
    jnle 3
    cmp rdi, rsi
    jng 2
    xor eax, eax
    jmp 8
2:
    mov rax, 6
    jmp 8
3:
    mov rax, 5
    jmp 8
4:
    mov rax, 4
    jmp 8
5:
    mov rax, 3
    jmp 8
6:
    mov rax, 2
    jmp 8
7:
    mov rax, 1
8:
    ret
test_unsigned:
    cmp rdi, rsi
    jz 15
    cmp rdi, rsi
    jnz 14
    cmp rdi, rsi
    jc 13
    cmp rdi, rsi
    jnc 12
    cmp rdi, rsi
    jnbe 11
    cmp rdi, rsi
    jbe 10
    xor eax, eax
    jmp 16
10:
    mov rax, 0x3c
    jmp 16
11:
    mov rax, 0x32
    jmp 16
12:
    mov rax, 0x28
    jmp 16
13:
    mov rax, 0x1e
    jmp 16
14:
    mov rax, 0x14
    jmp 16
15:
    mov rax, 0xa
16:
    ret
test_mixed_patterns:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    mov rdi, rbx
    call opaques
    mov rbp, rax
    add rbx, 1
    mov rdi, rbx
    call opaques
    cmp rbp, rax
    jnge 18
    jmp 20
18:
    cmp rbp, rax
    jng 19
    jmp 20
19:
    cmp rbp, rax
    jnz 25
20:
    cmp rbp, rax
    jnle 21
    jmp 23
21:
    cmp rbp, rax
    jnl 22
    jmp 23
22:
    cmp rbp, rax
    jz 24
23:
    xor eax, eax
    jmp 26
24:
    mov rax, 0xc8
    jmp 26
25:
    mov rax, 0x64
26:
    add rsp, 8
    pop rbp
    pop rbx
    ret
main:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0xa
    mov rsi, 0x14
    call test_signed
    mov r15, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_signed
    mov r14, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_signed
    mov r13, rax
    mov rdi, 0xa
    mov rsi, 0x14
    call test_unsigned
    mov r12, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_unsigned
    mov rbp, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_unsigned
    mov rbx, rax
    mov rdi, 0x2a
    call test_mixed_patterns
    mov rcx, rax
    xor eax, eax
    add rax, r15
    add rax, r14
    add rax, r13
    add rax, r12
    add rax, rbp
    add rax, rbx
    add rax, rcx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal-quick ===========
opaques:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rsp, 8
    ret
test_signed:
    cmp rdi, rsi
    jz 7
    cmp rdi, rsi
    jnz 6
    cmp rdi, rsi
    jnge 5
    cmp rdi, rsi
    jnl 4
    cmp rdi, rsi
    jnle 3
    cmp rdi, rsi
    jng 2
    xor eax, eax
    jmp 8
2:
    mov rax, 6
    jmp 8
3:
    mov rax, 5
    jmp 8
4:
    mov rax, 4
    jmp 8
5:
    mov rax, 3
    jmp 8
6:
    mov rax, 2
    jmp 8
7:
    mov rax, 1
8:
    ret
test_unsigned:
    cmp rdi, rsi
    jz +0x71
    cmp rdi, rsi
    jnz +0x5c
    cmp rdi, rsi
    jc +0x47
    cmp rdi, rsi
    jnc +0x32
    cmp rdi, rsi
    jnbe 11
    cmp rdi, rsi
    jbe 10
    xor eax, eax
    jmp 12
10:
    mov rax, 0x3c
    jmp 12
11:
    mov rax, 0x32
    jmp 12
    mov rax, 0x28
    jmp 12
    mov rax, 0x1e
    jmp 12
    mov rax, 0x14
    jmp 12
    mov rax, 0xa
12:
    ret
test_mixed_patterns:
    sub rsp, 0x18
    mov.q [rsp], rdi
    mov.q rdi, [rsp]
    call opaques
    mov.q [rsp + 0x8], rax
    mov.q rax, [rsp]
    add rax, 1
    mov rdi, rax
    call opaques
    mov.q rcx, [rsp + 0x8]
    cmp rcx, rax
    jnge 14
    jmp 16
14:
    mov.q rcx, [rsp + 0x8]
    cmp rcx, rax
    jng 15
    jmp 16
15:
    mov.q rcx, [rsp + 0x8]
    cmp rcx, rax
    jnz 20
16:
    mov.q rcx, [rsp + 0x8]
    cmp rcx, rax
    jnle 17
    jmp 19
17:
    mov.q rcx, [rsp + 0x8]
    cmp rcx, rax
    jnl 18
    jmp 19
18:
    mov.q rcx, [rsp + 0x8]
    cmp rcx, rax
    jz +0x8
19:
    xor eax, eax
    jmp 21
    mov rax, 0xc8
    jmp 21
20:
    mov rax, 0x64
21:
    add rsp, 0x18
    ret
main:
    sub rsp, 0x38
    mov rdi, 0xa
    mov rsi, 0x14
    call test_signed
    mov.q [rsp], rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_signed
    mov.q [rsp + 0x8], rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_signed
    mov.q [rsp + 0x10], rax
    mov rdi, 0xa
    mov rsi, 0x14
    call test_unsigned
    mov.q [rsp + 0x18], rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_unsigned
    mov.q [rsp + 0x20], rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_unsigned
    mov.q [rsp + 0x28], rax
    mov rdi, 0x2a
    call test_mixed_patterns
    mov.q [rsp + 0x30], rax
    xor eax, eax
    mov.q rcx, [rsp]
    add rax, rcx
    mov.q rcx, [rsp + 0x8]
    add rax, rcx
    mov.q rcx, [rsp + 0x10]
    add rax, rcx
    mov.q rcx, [rsp + 0x18]
    add rax, rcx
    mov.q rcx, [rsp + 0x20]
    add rax, rcx
    mov.q rcx, [rsp + 0x28]
    add rax, rcx
    mov.q rcx, [rsp + 0x30]
    add rax, rcx
    add rsp, 0x38
    ret
=========== OPT LEVEL: mininal ===========
opaques:
    mov rax, rdi
    ret
test_signed:
    cmp rdi, rsi
    jz 7
    cmp rdi, rsi
    jnz 6
    cmp rdi, rsi
    jnge 5
    cmp rdi, rsi
    jnl 4
    cmp rdi, rsi
    jnle 3
    cmp rdi, rsi
    jng 2
    xor eax, eax
    jmp 8
2:
    mov rax, 6
    jmp 8
3:
    mov rax, 5
    jmp 8
4:
    mov rax, 4
    jmp 8
5:
    mov rax, 3
    jmp 8
6:
    mov rax, 2
    jmp 8
7:
    mov rax, 1
8:
    ret
test_unsigned:
    cmp rdi, rsi
    jz 15
    cmp rdi, rsi
    jnz 14
    cmp rdi, rsi
    jc 13
    cmp rdi, rsi
    jnc 12
    cmp rdi, rsi
    jnbe 11
    cmp rdi, rsi
    jbe 10
    xor eax, eax
    jmp 16
10:
    mov rax, 0x3c
    jmp 16
11:
    mov rax, 0x32
    jmp 16
12:
    mov rax, 0x28
    jmp 16
13:
    mov rax, 0x1e
    jmp 16
14:
    mov rax, 0x14
    jmp 16
15:
    mov rax, 0xa
16:
    ret
test_mixed_patterns:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    mov rdi, rbx
    call opaques
    mov rbp, rax
    add rbx, 1
    mov rdi, rbx
    call opaques
    cmp rbp, rax
    jnge 18
    jmp 20
18:
    cmp rbp, rax
    jng 19
    jmp 20
19:
    cmp rbp, rax
    jnz 25
20:
    cmp rbp, rax
    jnle 21
    jmp 23
21:
    cmp rbp, rax
    jnl 22
    jmp 23
22:
    cmp rbp, rax
    jz 24
23:
    xor eax, eax
    jmp 26
24:
    mov rax, 0xc8
    jmp 26
25:
    mov rax, 0x64
26:
    add rsp, 8
    pop rbp
    pop rbx
    ret
main:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0xa
    mov rsi, 0x14
    call test_signed
    mov r15, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_signed
    mov r14, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_signed
    mov r13, rax
    mov rdi, 0xa
    mov rsi, 0x14
    call test_unsigned
    mov r12, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_unsigned
    mov rbp, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_unsigned
    mov rbx, rax
    mov rdi, 0x2a
    call test_mixed_patterns
    mov rcx, rax
    xor eax, eax
    add rax, r15
    add rax, r14
    add rax, r13
    add rax, r12
    add rax, rbp
    add rax, rbx
    add rax, rcx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate-quick ===========
opaques:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rsp, 8
    ret
test_signed:
    cmp rdi, rsi
    jz 7
    cmp rdi, rsi
    jnz 6
    cmp rdi, rsi
    jnge 5
    cmp rdi, rsi
    jnl 4
    cmp rdi, rsi
    jnle 3
    cmp rdi, rsi
    jng 2
    xor eax, eax
    jmp 8
2:
    mov rax, 6
    jmp 8
3:
    mov rax, 5
    jmp 8
4:
    mov rax, 4
    jmp 8
5:
    mov rax, 3
    jmp 8
6:
    mov rax, 2
    jmp 8
7:
    mov rax, 1
8:
    ret
test_unsigned:
    cmp rdi, rsi
    jz +0x71
    cmp rdi, rsi
    jnz +0x5c
    cmp rdi, rsi
    jc +0x47
    cmp rdi, rsi
    jnc +0x32
    cmp rdi, rsi
    jnbe 11
    cmp rdi, rsi
    jbe 10
    xor eax, eax
    jmp 12
10:
    mov rax, 0x3c
    jmp 12
11:
    mov rax, 0x32
    jmp 12
    mov rax, 0x28
    jmp 12
    mov rax, 0x1e
    jmp 12
    mov rax, 0x14
    jmp 12
    mov rax, 0xa
12:
    ret
test_mixed_patterns:
    sub rsp, 0x18
    mov.q [rsp], rdi
    mov.q rdi, [rsp]
    call opaques
    mov.q [rsp + 0x8], rax
    mov.q rax, [rsp]
    add rax, 1
    mov rdi, rax
    call opaques
    mov.q rcx, [rsp + 0x8]
    cmp rcx, rax
    jnge 14
    jmp 16
14:
    mov.q rcx, [rsp + 0x8]
    cmp rcx, rax
    jng 15
    jmp 16
15:
    mov.q rcx, [rsp + 0x8]
    cmp rcx, rax
    jnz 20
16:
    mov.q rcx, [rsp + 0x8]
    cmp rcx, rax
    jnle 17
    jmp 19
17:
    mov.q rcx, [rsp + 0x8]
    cmp rcx, rax
    jnl 18
    jmp 19
18:
    mov.q rcx, [rsp + 0x8]
    cmp rcx, rax
    jz +0x8
19:
    xor eax, eax
    jmp 21
    mov rax, 0xc8
    jmp 21
20:
    mov rax, 0x64
21:
    add rsp, 0x18
    ret
main:
    sub rsp, 0x38
    mov rdi, 0xa
    mov rsi, 0x14
    call test_signed
    mov.q [rsp], rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_signed
    mov.q [rsp + 0x8], rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_signed
    mov.q [rsp + 0x10], rax
    mov rdi, 0xa
    mov rsi, 0x14
    call test_unsigned
    mov.q [rsp + 0x18], rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_unsigned
    mov.q [rsp + 0x20], rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_unsigned
    mov.q [rsp + 0x28], rax
    mov rdi, 0x2a
    call test_mixed_patterns
    mov.q rcx, [rsp]
    mov.q rdx, [rsp + 0x8]
    add rcx, rdx
    mov rcx, rcx
    mov.q rdx, [rsp + 0x10]
    add rcx, rdx
    mov rcx, rcx
    mov.q rdx, [rsp + 0x18]
    add rcx, rdx
    mov rcx, rcx
    mov.q rdx, [rsp + 0x20]
    add rcx, rdx
    mov rcx, rcx
    mov.q rdx, [rsp + 0x28]
    add rcx, rdx
    mov rcx, rcx
    add rcx, rax
    mov rax, rcx
    add rsp, 0x38
    ret
=========== OPT LEVEL: moderate ===========
opaques:
    mov rax, rdi
    ret
test_signed:
    cmp rdi, rsi
    jz 7
    cmp rdi, rsi
    jnz 6
    cmp rdi, rsi
    jnge 5
    cmp rdi, rsi
    jnl 4
    cmp rdi, rsi
    jnle 3
    cmp rdi, rsi
    jng 2
    xor eax, eax
    jmp 8
2:
    mov rax, 6
    jmp 8
3:
    mov rax, 5
    jmp 8
4:
    mov rax, 4
    jmp 8
5:
    mov rax, 3
    jmp 8
6:
    mov rax, 2
    jmp 8
7:
    mov rax, 1
8:
    ret
test_unsigned:
    cmp rdi, rsi
    jz 15
    cmp rdi, rsi
    jnz 14
    cmp rdi, rsi
    jc 13
    cmp rdi, rsi
    jnc 12
    cmp rdi, rsi
    jnbe 11
    cmp rdi, rsi
    jbe 10
    xor eax, eax
    jmp 16
10:
    mov rax, 0x3c
    jmp 16
11:
    mov rax, 0x32
    jmp 16
12:
    mov rax, 0x28
    jmp 16
13:
    mov rax, 0x1e
    jmp 16
14:
    mov rax, 0x14
    jmp 16
15:
    mov rax, 0xa
16:
    ret
test_mixed_patterns:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    mov rdi, rbx
    call opaques
    mov rbp, rax
    add rbx, 1
    mov rdi, rbx
    call opaques
    cmp rbp, rax
    jnge 18
    jmp 20
18:
    cmp rbp, rax
    jng 19
    jmp 20
19:
    cmp rbp, rax
    jnz 25
20:
    cmp rbp, rax
    jnle 21
    jmp 23
21:
    cmp rbp, rax
    jnl 22
    jmp 23
22:
    cmp rbp, rax
    jz 24
23:
    xor eax, eax
    jmp 26
24:
    mov rax, 0xc8
    jmp 26
25:
    mov rax, 0x64
26:
    add rsp, 8
    pop rbp
    pop rbx
    ret
main:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0xa
    mov rsi, 0x14
    call test_signed
    mov r15, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_signed
    mov r14, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_signed
    mov r13, rax
    mov rdi, 0xa
    mov rsi, 0x14
    call test_unsigned
    mov r12, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_unsigned
    mov rbp, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_unsigned
    mov rbx, rax
    mov rdi, 0x2a
    call test_mixed_patterns
    mov rdx, rax
    mov rcx, r15
    add rcx, r14
    add rcx, r13
    add rcx, r12
    add rcx, rbp
    add rcx, rbx
    add rcx, rdx
    mov rax, rcx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: all ===========
opaques:
    mov rax, rdi
    ret
test_signed:
    cmp rdi, rsi
    jz 7
    cmp rdi, rsi
    jnz 6
    cmp rdi, rsi
    jnge 5
    cmp rdi, rsi
    jnl 4
    cmp rdi, rsi
    jnle 3
    cmp rdi, rsi
    jng 2
    xor eax, eax
    jmp 8
2:
    mov rax, 6
    jmp 8
3:
    mov rax, 5
    jmp 8
4:
    mov rax, 4
    jmp 8
5:
    mov rax, 3
    jmp 8
6:
    mov rax, 2
    jmp 8
7:
    mov rax, 1
8:
    ret
test_unsigned:
    cmp rdi, rsi
    jz 15
    cmp rdi, rsi
    jnz 14
    cmp rdi, rsi
    jc 13
    cmp rdi, rsi
    jnc 12
    cmp rdi, rsi
    jnbe 11
    cmp rdi, rsi
    jbe 10
    xor eax, eax
    jmp 16
10:
    mov rax, 0x3c
    jmp 16
11:
    mov rax, 0x32
    jmp 16
12:
    mov rax, 0x28
    jmp 16
13:
    mov rax, 0x1e
    jmp 16
14:
    mov rax, 0x14
    jmp 16
15:
    mov rax, 0xa
16:
    ret
test_mixed_patterns:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    mov rdi, rbx
    call opaques
    mov rbp, rax
    add rbx, 1
    mov rdi, rbx
    call opaques
    cmp rbp, rax
    jnge 18
    jmp 20
18:
    cmp rbp, rax
    jng 19
    jmp 20
19:
    cmp rbp, rax
    jnz 25
20:
    cmp rbp, rax
    jnle 21
    jmp 23
21:
    cmp rbp, rax
    jnl 22
    jmp 23
22:
    cmp rbp, rax
    jz 24
23:
    xor eax, eax
    jmp 26
24:
    mov rax, 0xc8
    jmp 26
25:
    mov rax, 0x64
26:
    add rsp, 8
    pop rbp
    pop rbx
    ret
main:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0xa
    mov rsi, 0x14
    call test_signed
    mov r15, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_signed
    mov r14, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_signed
    mov r13, rax
    mov rdi, 0xa
    mov rsi, 0x14
    call test_unsigned
    mov r12, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_unsigned
    mov rbp, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_unsigned
    mov rbx, rax
    mov rdi, 0x2a
    call test_mixed_patterns
    mov rdx, rax
    mov rcx, r15
    add rcx, r14
    add rcx, r13
    add rcx, r12
    add rcx, rbp
    add rcx, rbx
    add rcx, rdx
    mov rax, rcx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
test_signed:
    cmp rdi, rsi
    jz 6
    cmp rdi, rsi
    jnz 5
    cmp rdi, rsi
    jnge 4
    cmp rdi, rsi
    jnl 3
    cmp rdi, rsi
    jnle 2
    cmp rdi, rsi
    jng 1
    xor eax, eax
    jmp 7
1:
    mov rax, 6
    jmp 7
2:
    mov rax, 5
    jmp 7
3:
    mov rax, 4
    jmp 7
4:
    mov rax, 3
    jmp 7
5:
    mov rax, 2
    jmp 7
6:
    mov rax, 1
7:
    ret
test_unsigned:
    cmp rdi, rsi
    jz +0x71
    cmp rdi, rsi
    jnz +0x5c
    cmp rdi, rsi
    jc +0x47
    cmp rdi, rsi
    jnc +0x32
    cmp rdi, rsi
    jnbe +0x1d
    cmp rdi, rsi
    jbe +0x8
    xor eax, eax
    jmp +0x43
    mov rax, 0x3c
    jmp +0x37
    mov rax, 0x32
    jmp +0x2b
    mov rax, 0x28
    jmp +0x1f
    mov rax, 0x1e
    jmp +0x13
    mov rax, 0x14
    jmp +0x7
    mov rax, 0xa
    ret
main:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    mov rdi, 0xa
    mov rsi, 0x14
    call test_signed
    mov r14, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_signed
    mov r13, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_signed
    mov r12, rax
    mov rdi, 0xa
    mov rsi, 0x14
    call test_unsigned
    mov rbp, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_unsigned
    mov rbx, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_unsigned
    mov rdx, rax
    mov rcx, r14
    add rcx, r13
    add rcx, r12
    add rcx, rbp
    add rcx, rbx
    add rcx, rdx
    mov rax, rcx
    add rax, 0x64
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
