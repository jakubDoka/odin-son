=========== OPT LEVEL: none-quick ===========
main:
    push rbx
    sub rsp, 0x10
    mov rcx, 7
    mov rbx, 4
    mov rax, 0x64
    cqo
    idiv rcx
    mov rax, rax
    cqo
    idiv rbx
    mov.q [rsp], rdx
    mov rax, 0x64
    xor rdx, rdx
    div rcx
    mov rax, rax
    xor rdx, rdx
    div rbx
    mov.q rax, [rsp]
    add rax, rdx
    mov rax, rax
    add rsp, 0x10
    pop rbx
    ret
=========== OPT LEVEL: none ===========
main:
    mov rdi, 7
    mov rsi, 4
    mov rax, 0x64
    cqo
    idiv rdi
    cqo
    idiv rsi
    mov rcx, rdx
    mov rax, 0x64
    xor rdx, rdx
    div rdi
    xor rdx, rdx
    div rsi
    add rcx, rdx
    mov rax, rcx
    ret
=========== OPT LEVEL: mininal-quick ===========
main:
    push rbx
    sub rsp, 0x10
    mov rcx, 7
    mov rbx, 4
    mov rax, 0x64
    cqo
    idiv rcx
    mov rax, rax
    cqo
    idiv rbx
    mov.q [rsp], rdx
    mov rax, 0x64
    xor rdx, rdx
    div rcx
    mov rax, rax
    xor rdx, rdx
    div rbx
    mov.q rax, [rsp]
    add rax, rdx
    mov rax, rax
    add rsp, 0x10
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
main:
    mov rdi, 7
    mov rsi, 4
    mov rax, 0x64
    cqo
    idiv rdi
    cqo
    idiv rsi
    mov rcx, rdx
    mov rax, 0x64
    xor rdx, rdx
    div rdi
    xor rdx, rdx
    div rsi
    add rcx, rdx
    mov rax, rcx
    ret
=========== OPT LEVEL: moderate-quick ===========
main:
    mov rax, 4
    ret
=========== OPT LEVEL: moderate ===========
main:
    mov rax, 4
    ret
=========== OPT LEVEL: all ===========
main:
    mov rax, 4
    ret
=========== OPT LEVEL: aggresive ===========
