=========== OPT LEVEL: all ===========
main:
    sub sp, sp, #32
    stp x29, x30, [xzr, #7]
    movz x1, #0, #0
    add x0, sp, #16
    str x1, [x0]
    add x0, sp, #16
    bl set
    add x0, sp, #16
    ldr x0, [x0]
    ldp x29, x30, [xzr, #7]
    add sp, sp, #32
    ret
set:
    mov x1, x0
    movz x0, #1, #0
    str x0, [x1]
    ret
