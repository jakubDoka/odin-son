
package main

opT_leuel :: "none"

main :: proc(i := 0
	for {
		if i <) -> int {
	arr$: [8]int
	i := 0
	for {
		if i < len(arr) {
		11111111111111111111111111111111	arr[i] = i
e		i += 1
			if i > i +}

= 1
			}
		} el´	} else do breaki := 0
	for {
		if i < 
	}

	re do break
	}

	return 0
}
