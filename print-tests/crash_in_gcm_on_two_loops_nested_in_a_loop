=========== OPT LEVEL: none ===========
main:
    xor edi, edi
    xor eax, eax
1:
    cmp rdi, 4
    jnl 6
    xor edx, edx
    xor ecx, ecx
2:
    cmp rcx, 4
    jnl 3
    add rdx, rcx
    add rcx, 1
    jmp 2
3:
    xor ecx, ecx
    xor esi, esi
4:
    cmp rcx, 4
    jnl 5
    lea rsi, [rsi + rcx*2]
    add rcx, 1
    jmp 4
5:
    add rdx, rsi
    add rdi, 1
    add rax, rdx
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: mininal ===========
main:
    xor edi, edi
    xor eax, eax
1:
    cmp rdi, 4
    jnl 6
    xor edx, edx
    xor ecx, ecx
2:
    cmp rcx, 4
    jnl 3
    add rdx, rcx
    add rcx, 1
    jmp 2
3:
    xor ecx, ecx
    xor esi, esi
4:
    cmp rcx, 4
    jnl 5
    lea rsi, [rsi + rcx*2]
    add rcx, 1
    jmp 4
5:
    add rdx, rsi
    add rdi, 1
    add rax, rdx
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: moderate ===========
main:
    xor edi, edi
    xor eax, eax
1:
    cmp rdi, 4
    jnl 6
    xor edx, edx
    xor ecx, ecx
2:
    cmp rcx, 4
    jnl 3
    add rdx, rcx
    add rcx, 1
    jmp 2
3:
    xor ecx, ecx
    xor esi, esi
4:
    cmp rcx, 4
    jnl 5
    lea rsi, [rsi + rcx*2]
    add rcx, 1
    jmp 4
5:
    add rdx, rsi
    add rdi, 1
    add rax, rdx
    jmp 1
6:
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: all ===========
main:
    xor r9d, r9d
    xor r8d, r8d
1:
    xor ecx, ecx
    xor edx, edx
2:
    mov rdi, rdx
    mov rdx, rdi
    add rdx, rcx
    mov rax, rcx
    add rax, 1
    cmp rcx, 4
    mov rcx, rax
    jnge 2
    xor esi, esi
    xor edx, edx
3:
    mov rax, rdx
    mov rcx, rsi
    add rcx, 1
    lea rdx, [rax + rsi*2]
    cmp rsi, 4
    mov rsi, rcx
    jnge 3
    add r9, 1
    add rdi, rax
    mov rax, r8
    add rax, rdi
    cmp r9, 4
    mov r8, rax
    jnge 1
    mov rcx, 0xfb
    cqo
    idiv rcx
    mov rax, rdx
    ret
=========== OPT LEVEL: aggresive ===========
