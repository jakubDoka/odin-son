=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    mov rdi, 1
    mov rsi, 2
    call +0x9
    add rsp, 8
    ret
a:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rsi
    cmp rdi, 0
    jz 0
    cmp rbx, 2
    jz 1
    cmp rdi, 1
    jz 2
    call +0x51
    mov rbp, rax
    mov rdi, rbx
    call +0x4e
    add rbp, rax
    jmp 3
2:
    call +0x39
    mov rbp, rax
    imul rdi, rbx, 2
    call +0x32
    add rbp, rax
    jmp 3
1:
    imul rbp, rbx, 6
    jmp 3
0:
    xor ebp, ebp
3:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
b:
    imul rax, rdi, 2
    ret
c:
    imul rax, rdi, 3
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, 1
    mov rsi, 2
    call +0x9
    add rsp, 8
    ret
a:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rsi
    cmp rdi, 0
    jz 0
    cmp rbx, 2
    jz 1
    cmp rdi, 1
    jz 2
    call +0x51
    mov rbp, rax
    mov rdi, rbx
    call +0x4e
    add rbp, rax
    jmp 3
2:
    call +0x39
    mov rbp, rax
    imul rdi, rbx, 2
    call +0x32
    add rbp, rax
    jmp 3
1:
    imul rbp, rbx, 6
    jmp 3
0:
    xor ebp, ebp
3:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
b:
    imul rax, rdi, 2
    ret
c:
    imul rax, rdi, 3
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, 1
    mov rsi, 2
    call +0x9
    add rsp, 8
    ret
a:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rsi
    cmp rdi, 0
    jz 0
    cmp rbx, 2
    jz 1
    cmp rdi, 1
    jz 2
    call +0x51
    mov rbp, rax
    mov rdi, rbx
    call +0x4e
    add rbp, rax
    jmp 3
2:
    call +0x39
    mov rbp, rax
    imul rdi, rbx, 2
    call +0x32
    add rbp, rax
    jmp 3
1:
    imul rbp, rbx, 6
    jmp 3
0:
    xor ebp, ebp
3:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
b:
    imul rax, rdi, 2
    ret
c:
    imul rax, rdi, 3
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, 1
    mov rsi, 2
    call +0x9
    add rsp, 8
    ret
a:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rsi
    cmp rdi, 0
    jz 0
    cmp rbx, 2
    jz 1
    cmp rdi, 1
    jz 2
    call +0x51
    mov rbp, rax
    mov rdi, rbx
    call +0x4e
    add rbp, rax
    jmp 3
2:
    call +0x39
    mov rbp, rax
    imul rdi, rbx, 2
    call +0x32
    add rbp, rax
    jmp 3
1:
    imul rbp, rbx, 6
    jmp 3
0:
    xor ebp, ebp
3:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
b:
    imul rax, rdi, 2
    ret
c:
    imul rax, rdi, 3
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 0xc
    ret
a:
b:
c:
