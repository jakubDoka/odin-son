=========== OPT LEVEL: none-quick ===========
main:
    sub rsp, 0x18
    mov rdi, 0xc
    call 2
    mov.q [rsp], rax
    xor edi, edi
    call 2
    mov.q rcx, [rsp]
    and rcx, 0xa
    mov rcx, rcx
    add rax, rcx
    mov.q rcx, [rsp]
    or rcx, 3
    mov rcx, rcx
    add rax, rcx
    mov.q rcx, [rsp]
    xor rcx, 6
    mov rcx, rcx
    add rax, rcx
    add rsp, 0x18
    ret
2:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rsp, 8
    ret
=========== OPT LEVEL: none ===========
main:
    push rbx
    mov rdi, 0xc
    call 2
    mov rbx, rax
    xor edi, edi
    call 2
    mov rcx, rbx
    and rcx, 0xa
    add rax, rcx
    mov rcx, rbx
    or rcx, 3
    add rax, rcx
    mov rcx, rbx
    xor rcx, 6
    add rax, rcx
    pop rbx
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: mininal-quick ===========
main:
    sub rsp, 0x18
    mov rdi, 0xc
    call 2
    mov.q [rsp], rax
    xor edi, edi
    call 2
    mov.q rcx, [rsp]
    and rcx, 0xa
    mov rcx, rcx
    add rax, rcx
    mov.q rcx, [rsp]
    or rcx, 3
    mov rcx, rcx
    add rax, rcx
    mov.q rcx, [rsp]
    xor rcx, 6
    mov rcx, rcx
    add rax, rcx
    add rsp, 0x18
    ret
2:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rsp, 8
    ret
=========== OPT LEVEL: mininal ===========
main:
    push rbx
    mov rdi, 0xc
    call 2
    mov rbx, rax
    xor edi, edi
    call 2
    mov rcx, rbx
    and rcx, 0xa
    add rax, rcx
    mov rcx, rbx
    or rcx, 3
    add rax, rcx
    mov rcx, rbx
    xor rcx, 6
    add rax, rcx
    pop rbx
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: moderate-quick ===========
main:
    sub rsp, 0x18
    mov rdi, 0xc
    call 2
    mov.q [rsp], rax
    xor edi, edi
    call 2
    mov.q rcx, [rsp]
    and rcx, 0xa
    mov rcx, rcx
    add rax, rcx
    mov.q rcx, [rsp]
    or rcx, 3
    mov rcx, rcx
    add rax, rcx
    mov.q rcx, [rsp]
    xor rcx, 6
    mov rcx, rcx
    add rax, rcx
    add rsp, 0x18
    ret
2:
    sub rsp, 8
    mov.q [rsp], rdi
    mov.q rax, [rsp]
    add rsp, 8
    ret
=========== OPT LEVEL: moderate ===========
main:
    push rbx
    mov rdi, 0xc
    call 2
    mov rbx, rax
    xor edi, edi
    call 2
    mov rcx, rbx
    and rcx, 0xa
    add rax, rcx
    mov rcx, rbx
    or rcx, 3
    add rax, rcx
    mov rcx, rbx
    xor rcx, 6
    add rax, rcx
    pop rbx
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: all ===========
main:
    push rbx
    mov rdi, 0xc
    call 2
    mov rbx, rax
    xor edi, edi
    call 2
    mov rcx, rbx
    and rcx, 0xa
    add rax, rcx
    mov rcx, rbx
    or rcx, 3
    add rax, rcx
    mov rcx, rbx
    xor rcx, 6
    add rax, rcx
    pop rbx
    ret
2:
    mov rax, rdi
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 0x21
    ret
