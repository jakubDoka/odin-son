=========== OPT LEVEL: none ===========
opaques:
    mov rax, rdi
    ret
test_signed:
    cmp rdi, rsi
    jz 2
    cmp rdi, rsi
    jne 3
    cmp rdi, rsi
    jnge 4
    cmp rdi, rsi
    jnl 5
    cmp rdi, rsi
    jnle 6
    cmp rdi, rsi
    jle 7
    xor eax, eax
    jmp 8
7:
    mov rax, 6
    jmp 8
6:
    mov rax, 5
    jmp 8
5:
    mov rax, 4
    jmp 8
4:
    mov rax, 3
    jmp 8
3:
    mov rax, 2
    jmp 8
2:
    mov rax, 1
8:
    ret
test_unsigned:
    cmp rdi, rsi
    jz +0x71
    cmp rdi, rsi
    jne +0x5c
    cmp rdi, rsi
    jnae +0x47
    cmp rdi, rsi
    jnc +0x32
    cmp rdi, rsi
    jnbe 10
    cmp rdi, rsi
    jna 11
    xor eax, eax
    jmp 12
11:
    mov rax, 0x3c
    jmp 12
10:
    mov rax, 0x32
    jmp 12
    mov rax, 0x28
    jmp 12
    mov rax, 0x1e
    jmp 12
    mov rax, 0x14
    jmp 12
    mov rax, 0xa
12:
    ret
test_mixed_patterns:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    mov rdi, rbp
    call opaques
    mov rbx, rax
    add rbp, 1
    mov rdi, rbp
    call opaques
    cmp rbx, rax
    jnge 14
    jmp 15
14:
    cmp rbx, rax
    jle 16
    jmp 15
16:
    cmp rbx, rax
    jne 17
15:
    cmp rbx, rax
    jnle 18
    jmp 19
18:
    cmp rbx, rax
    jnl 20
    jmp 19
20:
    cmp rbx, rax
    jz 21
19:
    xor eax, eax
    jmp 22
21:
    mov rax, 0xc8
    jmp 22
17:
    mov rax, 0x64
22:
    add rsp, 8
    pop rbp
    pop rbx
    ret
main:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0xa
    mov rsi, 0x14
    call test_signed
    mov rbx, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_signed
    mov rbp, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_signed
    mov r12, rax
    mov rdi, 0xa
    mov rsi, 0x14
    call test_unsigned
    mov r13, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_unsigned
    mov r14, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_unsigned
    mov r15, rax
    mov rdi, 0x2a
    call test_mixed_patterns
    mov rdx, rax
    xor eax, eax
    add rax, rbx
    add rax, rbp
    add rax, r12
    add rax, r13
    add rax, r14
    add rax, r15
    add rax, rdx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
opaques:
    mov rax, rdi
    ret
test_signed:
    cmp rdi, rsi
    jz 2
    cmp rdi, rsi
    jne 3
    cmp rdi, rsi
    jnge 4
    cmp rdi, rsi
    jnl 5
    cmp rdi, rsi
    jnle 6
    cmp rdi, rsi
    jle 7
    xor eax, eax
    jmp 8
7:
    mov rax, 6
    jmp 8
6:
    mov rax, 5
    jmp 8
5:
    mov rax, 4
    jmp 8
4:
    mov rax, 3
    jmp 8
3:
    mov rax, 2
    jmp 8
2:
    mov rax, 1
8:
    ret
test_unsigned:
    cmp rdi, rsi
    jz +0x71
    cmp rdi, rsi
    jne +0x5c
    cmp rdi, rsi
    jnae +0x47
    cmp rdi, rsi
    jnc +0x32
    cmp rdi, rsi
    jnbe 10
    cmp rdi, rsi
    jna 11
    xor eax, eax
    jmp 12
11:
    mov rax, 0x3c
    jmp 12
10:
    mov rax, 0x32
    jmp 12
    mov rax, 0x28
    jmp 12
    mov rax, 0x1e
    jmp 12
    mov rax, 0x14
    jmp 12
    mov rax, 0xa
12:
    ret
test_mixed_patterns:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    mov rdi, rbp
    call opaques
    mov rbx, rax
    add rbp, 1
    mov rdi, rbp
    call opaques
    cmp rbx, rax
    jnge 14
    jmp 15
14:
    cmp rbx, rax
    jle 16
    jmp 15
16:
    cmp rbx, rax
    jne 17
15:
    cmp rbx, rax
    jnle 18
    jmp 19
18:
    cmp rbx, rax
    jnl 20
    jmp 19
20:
    cmp rbx, rax
    jz 21
19:
    xor eax, eax
    jmp 22
21:
    mov rax, 0xc8
    jmp 22
17:
    mov rax, 0x64
22:
    add rsp, 8
    pop rbp
    pop rbx
    ret
main:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0xa
    mov rsi, 0x14
    call test_signed
    mov rbx, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_signed
    mov rbp, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_signed
    mov r12, rax
    mov rdi, 0xa
    mov rsi, 0x14
    call test_unsigned
    mov r13, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_unsigned
    mov r14, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_unsigned
    mov r15, rax
    mov rdi, 0x2a
    call test_mixed_patterns
    mov rdx, rax
    xor eax, eax
    add rax, rbx
    add rax, rbp
    add rax, r12
    add rax, r13
    add rax, r14
    add rax, r15
    add rax, rdx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
opaques:
    mov rax, rdi
    ret
test_signed:
    cmp rdi, rsi
    jz 2
    cmp rdi, rsi
    jne 3
    cmp rdi, rsi
    jnge 4
    cmp rdi, rsi
    jnl 5
    cmp rdi, rsi
    jnle 6
    cmp rdi, rsi
    jle 7
    xor eax, eax
    jmp 8
7:
    mov rax, 6
    jmp 8
6:
    mov rax, 5
    jmp 8
5:
    mov rax, 4
    jmp 8
4:
    mov rax, 3
    jmp 8
3:
    mov rax, 2
    jmp 8
2:
    mov rax, 1
8:
    ret
test_unsigned:
    cmp rdi, rsi
    jz +0x71
    cmp rdi, rsi
    jne +0x5c
    cmp rdi, rsi
    jnae +0x47
    cmp rdi, rsi
    jnc +0x32
    cmp rdi, rsi
    jnbe 10
    cmp rdi, rsi
    jna 11
    xor eax, eax
    jmp 12
11:
    mov rax, 0x3c
    jmp 12
10:
    mov rax, 0x32
    jmp 12
    mov rax, 0x28
    jmp 12
    mov rax, 0x1e
    jmp 12
    mov rax, 0x14
    jmp 12
    mov rax, 0xa
12:
    ret
test_mixed_patterns:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    mov rdi, rbp
    call opaques
    mov rbx, rax
    add rbp, 1
    mov rdi, rbp
    call opaques
    cmp rbx, rax
    jnge 14
    jmp 15
14:
    cmp rbx, rax
    jle 16
    jmp 15
16:
    cmp rbx, rax
    jne 17
15:
    cmp rbx, rax
    jnle 18
    jmp 19
18:
    cmp rbx, rax
    jnl 20
    jmp 19
20:
    cmp rbx, rax
    jz 21
19:
    xor eax, eax
    jmp 22
21:
    mov rax, 0xc8
    jmp 22
17:
    mov rax, 0x64
22:
    add rsp, 8
    pop rbp
    pop rbx
    ret
main:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0xa
    mov rsi, 0x14
    call test_signed
    mov rbx, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_signed
    mov rbp, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_signed
    mov r12, rax
    mov rdi, 0xa
    mov rsi, 0x14
    call test_unsigned
    mov r13, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_unsigned
    mov r14, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_unsigned
    mov r15, rax
    mov rdi, 0x2a
    call test_mixed_patterns
    mov rdx, rbx
    add rdx, rbp
    add rdx, r12
    add rdx, r13
    add rdx, r14
    add rdx, r15
    add rdx, rax
    mov rax, rdx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: all ===========
opaques:
    mov rax, rdi
    ret
test_signed:
    cmp rdi, rsi
    jz 2
    cmp rdi, rsi
    jne 3
    cmp rdi, rsi
    jnge 4
    cmp rdi, rsi
    jnl 5
    cmp rdi, rsi
    jnle 6
    cmp rdi, rsi
    jle 7
    xor eax, eax
    jmp 8
7:
    mov rax, 6
    jmp 8
6:
    mov rax, 5
    jmp 8
5:
    mov rax, 4
    jmp 8
4:
    mov rax, 3
    jmp 8
3:
    mov rax, 2
    jmp 8
2:
    mov rax, 1
8:
    ret
test_unsigned:
    cmp rdi, rsi
    jz +0x71
    cmp rdi, rsi
    jne +0x5c
    cmp rdi, rsi
    jnae +0x47
    cmp rdi, rsi
    jnc +0x32
    cmp rdi, rsi
    jnbe 10
    cmp rdi, rsi
    jna 11
    xor eax, eax
    jmp 12
11:
    mov rax, 0x3c
    jmp 12
10:
    mov rax, 0x32
    jmp 12
    mov rax, 0x28
    jmp 12
    mov rax, 0x1e
    jmp 12
    mov rax, 0x14
    jmp 12
    mov rax, 0xa
12:
    ret
test_mixed_patterns:
    push rbx
    push rbp
    sub rsp, 8
    mov rbp, rdi
    mov rdi, rbp
    call opaques
    mov rbx, rax
    add rbp, 1
    mov rdi, rbp
    call opaques
    cmp rbx, rax
    jnge 14
    jmp 15
14:
    cmp rbx, rax
    jle 16
    jmp 15
16:
    cmp rbx, rax
    jne 17
15:
    cmp rbx, rax
    jnle 18
    jmp 19
18:
    cmp rbx, rax
    jnl 20
    jmp 19
20:
    cmp rbx, rax
    jz 21
19:
    xor eax, eax
    jmp 22
21:
    mov rax, 0xc8
    jmp 22
17:
    mov rax, 0x64
22:
    add rsp, 8
    pop rbp
    pop rbx
    ret
main:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0xa
    mov rsi, 0x14
    call test_signed
    mov rbx, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_signed
    mov rbp, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_signed
    mov r12, rax
    mov rdi, 0xa
    mov rsi, 0x14
    call test_unsigned
    mov r13, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_unsigned
    mov r14, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_unsigned
    mov r15, rax
    mov rdi, 0x2a
    call test_mixed_patterns
    mov rdx, rbx
    add rdx, rbp
    add rdx, r12
    add rdx, r13
    add rdx, r14
    add rdx, r15
    add rdx, rax
    mov rax, rdx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
test_signed:
    cmp rdi, rsi
    jz 2
    cmp rdi, rsi
    jne 3
    cmp rdi, rsi
    jnge 4
    cmp rdi, rsi
    jnl 5
    cmp rdi, rsi
    jnle 6
    cmp rdi, rsi
    jle 7
    xor eax, eax
    jmp 8
7:
    mov rax, 6
    jmp 8
6:
    mov rax, 5
    jmp 8
5:
    mov rax, 4
    jmp 8
4:
    mov rax, 3
    jmp 8
3:
    mov rax, 2
    jmp 8
2:
    mov rax, 1
8:
    ret
test_unsigned:
    cmp rdi, rsi
    jz 10
    cmp rdi, rsi
    jne 11
    cmp rdi, rsi
    jnae 12
    cmp rdi, rsi
    jnc 13
    cmp rdi, rsi
    jnbe 14
    cmp rdi, rsi
    jna 15
    xor eax, eax
    jmp 16
15:
    mov rax, 0x3c
    jmp 16
14:
    mov rax, 0x32
    jmp 16
13:
    mov rax, 0x28
    jmp 16
12:
    mov rax, 0x1e
    jmp 16
11:
    mov rax, 0x14
    jmp 16
10:
    mov rax, 0xa
16:
    ret
main:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    mov rdi, 0xa
    mov rsi, 0x14
    call test_signed
    mov rbx, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_signed
    mov rbp, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_signed
    mov r12, rax
    mov rdi, 0xa
    mov rsi, 0x14
    call test_unsigned
    mov r13, rax
    mov rsi, 0x14
    mov rdi, 0x14
    call test_unsigned
    mov r14, rax
    mov rsi, 0xa
    mov rdi, 0x1e
    call test_unsigned
    mov rdx, rbx
    add rdx, rbp
    add rdx, r12
    add rdx, r13
    add rdx, r14
    add rdx, rax
    mov rax, rdx
    add rax, 0x64
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
