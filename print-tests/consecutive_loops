=========== OPT LEVEL: none ===========
main:
     xor eax, eax
     xor ecx, ecx
     test cl, cl
     jnz :0
     jmp :1

000: xor ecx, ecx
002: add rcx, 0x01
     cmp rcx, 0x01
     jz :1
     jmp :2

001: xor edx, edx
006: add rdx, 0x01
     cmp rdx, 0x03
     jz :3
     xor ecx, ecx
005: add rcx, 0x01
     cmp rcx, 0x03
     jz :4
     jmp :5

004: xor ecx, ecx
007: add rcx, 0x01
     cmp rcx, 0x02
     jz :3
     cmp rcx, 0x03
     jz :6
     jmp :7

003: ret
=========== OPT LEVEL: mininal ===========
main:
     xor eax, eax
     xor ecx, ecx
     test cl, cl
     jnz :0
     jmp :1

000: xor ecx, ecx
002: add rcx, 0x01
     cmp rcx, 0x01
     jz :1
     jmp :2

001: xor edx, edx
006: add rdx, 0x01
     cmp rdx, 0x03
     jz :3
     xor ecx, ecx
005: add rcx, 0x01
     cmp rcx, 0x03
     jz :4
     jmp :5

004: xor ecx, ecx
007: add rcx, 0x01
     cmp rcx, 0x02
     jz :3
     cmp rcx, 0x03
     jz :6
     jmp :7

003: ret
=========== OPT LEVEL: moderate ===========
main:
     xor eax, eax
     xor edx, edx
003: add rdx, 0x01
     cmp rdx, 0x03
     jz :0
     xor ecx, ecx
002: add rcx, 0x01
     cmp rcx, 0x03
     jz :1
     jmp :2

001: xor ecx, ecx
004: add rcx, 0x01
     cmp rcx, 0x02
     jz :0
     cmp rcx, 0x03
     jz :3
     jmp :4

000: ret
=========== OPT LEVEL: all ===========
main:
     xor eax, eax
     xor edx, edx
003: add rdx, 0x01
     cmp rdx, 0x03
     jz :0
     xor ecx, ecx
002: add rcx, 0x01
     cmp rcx, 0x03
     jz :1
     jmp :2

001: xor ecx, ecx
004: add rcx, 0x01
     cmp rcx, 0x02
     jz :0
     cmp rcx, 0x03
     jz :3
     jmp :4

000: ret
=========== OPT LEVEL: aggresive ===========
