=========== OPT LEVEL: none ===========
main:
    mov rcx, -6
    mov rax, 0x14
    cqo
    idiv rcx
    xor esi, esi
    add rsi, rax
    mov rax, 0x14
    cqo
    idiv rcx
    add rsi, rdx
    mov rax, -7
    cqo
    idiv rcx
    add rsi, rax
    mov rax, -7
    cqo
    idiv rcx
    add rsi, rdx
    mov rax, -7
    sar rax, 1
    add rsi, rax
    mov rax, -6
    sar rax, 1
    add rsi, rax
    cmp rcx, 0x14
    jnge 1
    jmp 2
1:
    add rsi, 1
2:
    mov rax, -7
    cmp rax, -6
    jnge 3
    jmp 4
3:
    add rsi, 2
4:
    mov rax, 0x14
    cmp rax, -6
    jnle 5
    jmp 6
5:
    add rsi, 4
6:
    cmp rcx, -7
    jle 7
    jmp 8
7:
    add rsi, 8
8:
    mov rax, 0x14
    cmp rax, -6
    jnl 9
    jmp 10
9:
    add rsi, 0x10
10:
    mov rax, rsi
    ret
=========== OPT LEVEL: mininal ===========
main:
    mov cl, 1
    xor eax, eax
    add rax, -3
    add rax, 2
    add rax, 1
    add rax, -1
    add rax, -4
    add rax, -3
    test cl, cl
    jne 1
    jmp 2
1:
    add rax, 1
2:
    test cl, cl
    jne 3
    jmp 4
3:
    add rax, 2
4:
    test cl, cl
    jne 5
    jmp 6
5:
    add rax, 4
6:
    xor edx, edx
    test dl, dl
    jne 7
    jmp 8
7:
    add rax, 8
8:
    test cl, cl
    jne 9
    jmp 10
9:
    add rax, 0x10
10:
    ret
=========== OPT LEVEL: moderate ===========
main:
    mov rax, 0xf
    ret
=========== OPT LEVEL: all ===========
main:
    mov rax, 0xf
    ret
=========== OPT LEVEL: aggresive ===========
