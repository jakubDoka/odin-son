=========== OPT LEVEL: none ===========
main:
     push rbx
     push rbp
     push r12
     push r13
     push r14
     push r15
     mov rcx, 0x06
     mov rax, 0x14
     cqo
     idiv rcx
     mov rdi, rax
     mov rax, 0x00
     add rdi, rax
     mov rax, 0x14
     cqo
     idiv rcx
     add rdi, rdx
     mov rax, 0x00
     sub rax, 0x07
     mov rsi, rax
     mov rax, rsi
     cqo
     idiv rcx
     add rdi, rax
     mov rax, rsi
     cqo
     idiv rcx
     add rdi, rdx
     mov rax, 0x14
     and rax, 0x06
     add rdi, rax
     mov rax, 0x14
     or rax, 0x06
     add rdi, rax
     mov rax, 0x14
     xor rax, 0x06
     add rdi, rax
     mov rax, 0x14
     mov rdx, 0x06
     not rdx
     and rdx, rax
     add rdi, rdx
     mov rax, 0x14
     shl rax, 0x02
     add rdi, rax
     mov rax, 0x14
     sar rax, 0x02
     add rdi, rax
     mov rax, rsi
     sar rax, 0x02
     add rdi, rax
     mov rax, 0x14
     cmp rax, 0x06
     jnle :0
     jmp :1

000: add rdi, 0x01
001: cmp rcx, 0x14
     jl :2
     jmp :3

002: add rdi, 0x02
003: mov rcx, rdi
     mov rax, 0x14
     cmp rax, 0x06
     jnl :4
     jmp :5

004: add rcx, 0x04
005: mov rax, 0x14
     cmp rax, 0x06
     jle :6
     jmp :7

006: add rcx, 0x08
007: mov rax, 0x14
     cmp rax, 0x14
     jz :8
     jmp :9

008: add rcx, 0x10
009: mov rax, 0x14
     cmp rax, 0x06
     jnz :10
     jmp :11

010: add rcx, 0x20
011: mov rax, rsi
     cmp rax, 0x06
     jl :12
     jmp :13

012: add rcx, 0x40
013: mov r9, 0x06
     mov rax, 0x14
     movzx eax, al
     div r9b
     movzx eax, al
     add rcx, rax
     mov rax, 0x14
     movzx eax, al
     div r9b
     movsx edx, ah
     movzx eax, dl
     add rcx, rax
     mov rax, 0x07
     movzx eax, al
     div r9b
     movzx eax, al
     add rcx, rax
     mov rax, 0x07
     movzx eax, al
     div r9b
     movsx edx, ah
     movzx eax, dl
     add rcx, rax
     mov rax, 0x14
     and rax, 0x06
     mov [rsp-0x78], rax
     mov rax, 0x14
     or rax, 0x06
     mov [rsp-0x70], rax
     mov rax, 0x14
     xor rax, 0x06
     mov [rsp-0x68], rax
     mov rax, 0x14
     mov rsi, 0x06
     not sil
     and sil, al
     mov rax, [rsp-0x78]
     movzx eax, al
     add rcx, rax
     mov rax, 0x14
     shl rax, 0x02
     mov [rsp-0x60], rax
     mov rax, [rsp-0x70]
     movzx eax, al
     add rcx, rax
     mov rax, [rsp-0x68]
     movzx eax, al
     add rcx, rax
     movzx eax, sil
     add rcx, rax
     mov rax, [rsp-0x60]
     movzx eax, al
     add rcx, rax
     mov rax, 0x14
     shr rax, 0x02
     movzx eax, al
     add rcx, rax
     mov rax, 0x07
     shr rax, 0x02
     movzx eax, al
     add rcx, rax
     mov rax, 0x14
     cmp al, 0x06
     jnbe :14
     jmp :15

014: add rcx, 0x01
015: cmp r9b, 0x14
     jb :16
     jmp :17

016: add rcx, 0x02
017: mov rax, 0x14
     cmp al, 0x06
     jnb :18
     jmp :19

018: add rcx, 0x04
019: mov rax, 0x14
     cmp al, 0x06
     jbe :20
     jmp :21

020: add rcx, 0x08
021: mov rax, 0x14
     cmp al, 0x14
     setz r10b
     movzx r10, r10b
     test r10, r10
     jnz :22
     jmp :23

022: add rcx, 0x10
023: mov rax, 0x14
     cmp al, 0x06
     setnz r11b
     movzx r11, r11b
     test r11, r11
     jnz :24
     jmp :25

024: add rcx, 0x20
025: mov rax, 0x07
     cmp al, 0x06
     jb :26
     jmp :27

026: add rcx, 0x40
027: mov rbx, 0x06
     mov rax, 0x14
     xor rdx, rdx
     div bx
     movzx eax, ax
     add rcx, rax
     mov rax, 0x14
     xor rdx, rdx
     div bx
     movzx eax, dx
     add rcx, rax
     mov rax, 0x07
     xor rdx, rdx
     div bx
     movzx eax, ax
     add rcx, rax
     mov rax, 0x07
     xor rdx, rdx
     div bx
     movzx eax, dx
     add rcx, rax
     mov rax, 0x14
     and rax, 0x06
     mov [rsp-0x58], rax
     mov rax, 0x14
     or rax, 0x06
     mov [rsp-0x50], rax
     mov rax, 0x14
     xor rax, 0x06
     mov [rsp-0x48], rax
     mov rax, 0x14
     mov rdi, 0x06
     not di
     and di, ax
     mov rax, [rsp-0x58]
     movzx eax, ax
     add rcx, rax
     mov rdx, rcx
     mov rax, 0x14
     shl rax, 0x02
     mov [rsp-0x40], rax
     mov rax, [rsp-0x50]
     movzx ecx, ax
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x48]
     movzx ecx, ax
     mov rax, rdx
     add rax, rcx
     movzx ecx, di
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x40]
     movzx ecx, ax
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, 0x14
     shr rax, 0x02
     movzx ecx, ax
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, 0x07
     shr rax, 0x02
     movzx ecx, ax
     mov rax, rdx
     add rax, rcx
     mov r8, rax
     mov rax, 0x14
     cmp ax, 0x06
     jnbe :28
     jmp :29

028: mov rax, r8
     add rax, 0x01
     mov r8, rax
029: cmp bx, 0x14
     jb :30
     jmp :31

030: mov rax, r8
     add rax, 0x02
     mov r8, rax
031: mov rax, 0x14
     cmp ax, 0x06
     jnb :32
     jmp :33

032: mov rax, r8
     add rax, 0x04
     mov r8, rax
033: mov rax, 0x14
     cmp ax, 0x06
     jbe :34
     jmp :35

034: mov rax, r8
     add rax, 0x08
     mov r8, rax
035: mov rax, 0x14
     cmp ax, 0x14
     setz bpl
     movzx rbp, bpl
     test rbp, rbp
     jnz :36
     jmp :37

036: mov rax, r8
     add rax, 0x10
     mov r8, rax
037: mov rax, 0x14
     cmp ax, 0x06
     setnz r12b
     movzx r12, r12b
     test r12, r12
     jnz :38
     jmp :39

038: mov rax, r8
     add rax, 0x20
     mov r8, rax
039: mov rax, 0x07
     cmp ax, 0x06
     jb :40
     jmp :41

040: mov rax, r8
     add rax, 0x40
     mov r8, rax
041: mov r13, 0x06
     mov rax, 0x14
     xor rdx, rdx
     div r13d
     mov ecx, eax
     mov rax, r8
     add rax, rcx
     mov r8, rax
     mov rax, 0x14
     xor rdx, rdx
     div r13d
     mov ecx, edx
     mov rax, r8
     add rax, rcx
     mov r8, rax
     mov rax, 0x07
     xor rdx, rdx
     div r13d
     mov ecx, eax
     mov rax, r8
     add rax, rcx
     mov r8, rax
     mov rax, 0x07
     xor rdx, rdx
     div r13d
     mov ecx, edx
     mov rax, r8
     add rax, rcx
     mov rdx, rax
     mov rax, 0x14
     and rax, 0x06
     mov [rsp-0x38], rax
     mov rax, 0x14
     or rax, 0x06
     mov [rsp-0x30], rax
     mov rax, 0x14
     xor rax, 0x06
     mov [rsp-0x28], rax
     mov rax, 0x14
     mov r8, 0x06
     not r8d
     and r8d, eax
     mov rax, [rsp-0x38]
     mov ecx, eax
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, 0x14
     shl rax, 0x02
     mov [rsp-0x20], rax
     mov rax, [rsp-0x30]
     mov ecx, eax
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x28]
     mov ecx, eax
     mov rax, rdx
     add rax, rcx
     mov ecx, r8d
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x20]
     mov ecx, eax
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, 0x14
     shr rax, 0x02
     mov ecx, eax
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, 0x07
     shr rax, 0x02
     mov ecx, eax
     mov rax, rdx
     add rax, rcx
     mov [rsp-0x18], rax
     mov rax, 0x14
     cmp eax, 0x06
     jnbe :42
     jmp :43

042: mov rax, [rsp-0x18]
     add rax, 0x01
     mov [rsp-0x18], rax
043: cmp r13d, 0x14
     jb :44
     jmp :45

044: mov rax, [rsp-0x18]
     add rax, 0x02
     mov [rsp-0x18], rax
045: mov rax, 0x14
     cmp eax, 0x06
     jnb :46
     jmp :47

046: mov rax, [rsp-0x18]
     add rax, 0x04
     mov [rsp-0x18], rax
047: mov rax, 0x14
     cmp eax, 0x06
     jbe :48
     jmp :49

048: mov rax, [rsp-0x18]
     add rax, 0x08
     mov [rsp-0x18], rax
049: mov rax, 0x14
     cmp eax, 0x14
     setz r14b
     movzx r14, r14b
     test r14, r14
     jnz :50
     jmp :51

050: mov rax, [rsp-0x18]
     add rax, 0x10
     mov [rsp-0x18], rax
051: mov rax, 0x14
     cmp eax, 0x06
     setnz r15b
     movzx r15, r15b
     test r15, r15
     jnz :52
     jmp :53

052: mov rax, [rsp-0x18]
     add rax, 0x20
     mov [rsp-0x18], rax
053: mov rax, 0x07
     cmp eax, 0x06
     jb :54
     jmp :55

054: mov rax, [rsp-0x18]
     add rax, 0x40
     mov [rsp-0x18], rax
055: mov rax, 0x14
     cbw
     idiv r9b
     movsx rcx, al
     mov rax, [rsp-0x18]
     add rax, rcx
     mov [rsp-0x18], rax
     mov rax, 0x14
     cbw
     idiv r9b
     movzx edx, ah
     movsx rcx, dl
     mov rax, [rsp-0x18]
     add rax, rcx
     mov [rsp-0x18], rax
     mov rax, 0x00
     sub rax, 0x07
     mov [rsp-0x10], rax
     mov rax, [rsp-0x10]
     cbw
     idiv r9b
     movsx rcx, al
     mov rax, [rsp-0x18]
     add rax, rcx
     mov [rsp-0x18], rax
     mov rax, [rsp-0x10]
     cbw
     idiv r9b
     movzx edx, ah
     movsx rcx, dl
     mov rax, [rsp-0x18]
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x78]
     movsx rcx, al
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x70]
     movsx rcx, al
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x68]
     movsx rcx, al
     mov rax, rdx
     add rax, rcx
     movsx rcx, sil
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x60]
     movsx rcx, al
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, 0x14
     sar rax, 0x02
     movsx rcx, al
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x10]
     sar rax, 0x02
     movsx rcx, al
     mov rax, rdx
     add rax, rcx
     mov rsi, rax
     mov rax, 0x14
     cmp al, 0x06
     jnle :56
     jmp :57

056: mov rax, rsi
     add rax, 0x01
     mov rsi, rax
057: cmp r9b, 0x14
     jl :58
     jmp :59

058: mov rax, rsi
     add rax, 0x02
     mov rsi, rax
059: mov rax, 0x14
     cmp al, 0x06
     jnl :60
     jmp :61

060: mov rax, rsi
     add rax, 0x04
     mov rsi, rax
061: mov rax, 0x14
     cmp al, 0x06
     jle :62
     jmp :63

062: mov rax, rsi
     add rax, 0x08
     mov rsi, rax
063: test r10, r10
     jnz :64
     jmp :65

064: mov rax, rsi
     add rax, 0x10
     mov rsi, rax
065: test r11, r11
     jnz :66
     jmp :67

066: mov rax, rsi
     add rax, 0x20
     mov rsi, rax
067: mov rax, [rsp-0x10]
     cmp al, 0x06
     jl :68
     jmp :69

068: mov rax, rsi
     add rax, 0x40
     mov rsi, rax
069: mov rax, 0x14
     cwd
     idiv bx
     movsx rcx, ax
     mov rax, rsi
     add rax, rcx
     mov rsi, rax
     mov rax, 0x14
     cwd
     idiv bx
     movsx rcx, dx
     mov rax, rsi
     add rax, rcx
     mov rsi, rax
     mov rax, 0x00
     sub rax, 0x07
     mov r9, rax
     mov rax, r9
     cwd
     idiv bx
     movsx rcx, ax
     mov rax, rsi
     add rax, rcx
     mov rsi, rax
     mov rax, r9
     cwd
     idiv bx
     movsx rcx, dx
     mov rax, rsi
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x58]
     movsx rcx, ax
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x50]
     movsx rcx, ax
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x48]
     movsx rcx, ax
     mov rax, rdx
     add rax, rcx
     mov rcx, rax
     movsx rax, di
     add rcx, rax
     mov rax, rcx
     mov rcx, [rsp-0x40]
     movsx rcx, cx
     add rax, rcx
     mov rcx, 0x14
     sar rcx, 0x02
     movsx rcx, cx
     add rax, rcx
     mov rcx, r9
     sar rcx, 0x02
     movsx rcx, cx
     mov rdx, rax
     add rdx, rcx
     mov rax, 0x14
     cmp ax, 0x06
     jnle :70
     jmp :71

070: add rdx, 0x01
071: cmp bx, 0x14
     jl :72
     jmp :73

072: add rdx, 0x02
073: mov rcx, rdx
     mov rax, 0x14
     cmp ax, 0x06
     jnl :74
     jmp :75

074: add rcx, 0x04
075: mov rax, 0x14
     cmp ax, 0x06
     jle :76
     jmp :77

076: add rcx, 0x08
077: test rbp, rbp
     jnz :78
     jmp :79

078: add rcx, 0x10
079: test r12, r12
     jnz :80
     jmp :81

080: add rcx, 0x20
081: mov rax, r9
     cmp ax, 0x06
     jl :82
     jmp :83

082: add rcx, 0x40
083: mov rax, 0x14
     cdq
     idiv r13d
     movsxd rax, eax
     add rcx, rax
     mov rax, 0x14
     cdq
     idiv r13d
     movsxd rax, edx
     add rcx, rax
     mov rax, 0x00
     sub rax, 0x07
     mov rsi, rax
     mov rax, rsi
     cdq
     idiv r13d
     movsxd rax, eax
     add rcx, rax
     mov rax, rsi
     cdq
     idiv r13d
     movsxd rax, edx
     add rcx, rax
     mov rax, [rsp-0x38]
     movsxd rax, eax
     add rcx, rax
     mov rax, [rsp-0x30]
     movsxd rax, eax
     add rcx, rax
     mov rax, [rsp-0x28]
     movsxd rax, eax
     add rcx, rax
     movsxd rax, r8d
     add rcx, rax
     mov rax, [rsp-0x20]
     movsxd rax, eax
     add rcx, rax
     mov rax, 0x14
     sar rax, 0x02
     movsxd rax, eax
     add rcx, rax
     mov rax, rsi
     sar rax, 0x02
     movsxd rax, eax
     add rcx, rax
     mov rax, 0x14
     cmp eax, 0x06
     jnle :84
     jmp :85

084: add rcx, 0x01
085: cmp r13d, 0x14
     jl :86
     jmp :87

086: add rcx, 0x02
087: mov rax, 0x14
     cmp eax, 0x06
     jnl :88
     jmp :89

088: add rcx, 0x04
089: mov rax, 0x14
     cmp eax, 0x06
     jle :90
     jmp :91

090: add rcx, 0x08
091: test r14, r14
     jnz :92
     jmp :93

092: add rcx, 0x10
093: test r15, r15
     jnz :94
     jmp :95

094: add rcx, 0x20
095: mov rax, rsi
     cmp eax, 0x06
     jl :96
     jmp :97

096: add rcx, 0x40
097: mov rax, rcx
     pop r15
     pop r14
     pop r13
     pop r12
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: mininal ===========
main:
     mov rax, 0x00
     mov rcx, 0x01
     mov rdx, 0x00
     add rdx, 0x03
     add rdx, 0x02
     add rdx, 0xFFFFFFFFFFFFFFFF
     add rdx, 0xFFFFFFFFFFFFFFFF
     add rdx, 0x04
     add rdx, 0x16
     add rdx, 0x12
     add rdx, 0x10
     add rdx, 0x50
     add rdx, 0x05
     add rdx, 0xFFFFFFFFFFFFFFFE
     test rcx, rcx
     jnz :0
     jmp :1

000: add rdx, 0x01
001: test rcx, rcx
     jnz :2
     jmp :3

002: add rdx, 0x02
003: test rcx, rcx
     jnz :4
     jmp :5

004: add rdx, 0x04
005: test rax, rax
     jnz :6
     mov rax, rdx
     jmp :7

006: mov rax, rdx
     add rax, 0x08
007: test rcx, rcx
     jnz :8
     jmp :9

008: add rax, 0x10
009: test rcx, rcx
     jnz :10
     jmp :11

010: add rax, 0x20
011: test rcx, rcx
     jnz :12
     jmp :13

012: add rax, 0x40
013: mov rcx, 0x01
     add rax, 0x03
     add rax, 0x02
     add rax, 0x01
     add rax, 0x01
     add rax, 0x04
     add rax, 0x16
     add rax, 0x12
     add rax, 0x10
     add rax, 0x50
     add rax, 0x05
     add rax, 0x01
     test rcx, rcx
     jnz :14
     jmp :15

014: add rax, 0x01
015: test rcx, rcx
     jnz :16
     jmp :17

016: add rax, 0x02
017: test rcx, rcx
     jnz :18
     jmp :19

018: add rax, 0x04
019: mov rdx, 0x00
     test rdx, rdx
     jnz :20
     jmp :21

020: add rax, 0x08
021: test rcx, rcx
     jnz :22
     jmp :23

022: add rax, 0x10
023: test rcx, rcx
     jnz :24
     jmp :25

024: add rax, 0x20
025: test rdx, rdx
     jnz :26
     jmp :27

026: add rax, 0x40
027: mov rsi, 0x01
     add rax, 0x03
     add rax, 0x02
     add rax, 0x01
     add rax, 0x01
     add rax, 0x04
     add rax, 0x16
     add rax, 0x12
     add rax, 0x10
     add rax, 0x50
     add rax, 0x05
     add rax, 0x01
     test rsi, rsi
     jnz :28
     jmp :29

028: add rax, 0x01
029: test rsi, rsi
     jnz :30
     jmp :31

030: add rax, 0x02
031: test rsi, rsi
     jnz :32
     jmp :33

032: add rax, 0x04
033: mov rdi, 0x00
     test rdi, rdi
     jnz :34
     jmp :35

034: add rax, 0x08
035: test rsi, rsi
     jnz :36
     jmp :37

036: add rax, 0x10
037: test rsi, rsi
     jnz :38
     jmp :39

038: add rax, 0x20
039: test rdi, rdi
     jnz :40
     jmp :41

040: add rax, 0x40
041: mov r8, 0x01
     add rax, 0x03
     add rax, 0x02
     add rax, 0x01
     add rax, 0x01
     add rax, 0x04
     add rax, 0x16
     add rax, 0x12
     add rax, 0x10
     add rax, 0x50
     add rax, 0x05
     add rax, 0x01
     test r8, r8
     jnz :42
     jmp :43

042: add rax, 0x01
043: test r8, r8
     jnz :44
     jmp :45

044: add rax, 0x02
045: test r8, r8
     jnz :46
     jmp :47

046: add rax, 0x04
047: mov r9, 0x00
     test r9, r9
     jnz :48
     jmp :49

048: add rax, 0x08
049: test r8, r8
     jnz :50
     jmp :51

050: add rax, 0x10
051: test r8, r8
     jnz :52
     jmp :53

052: add rax, 0x20
053: test r9, r9
     jnz :54
     jmp :55

054: add rax, 0x40
055: add rax, 0x03
     add rax, 0x02
     add rax, 0xFFFFFFFFFFFFFFFF
     add rax, 0xFFFFFFFFFFFFFFFF
     add rax, 0x04
     add rax, 0x16
     add rax, 0x12
     add rax, 0x10
     add rax, 0x50
     add rax, 0x05
     add rax, 0xFFFFFFFFFFFFFFFE
     test rcx, rcx
     jnz :56
     jmp :57

056: add rax, 0x01
057: test rcx, rcx
     jnz :58
     jmp :59

058: add rax, 0x02
059: test rcx, rcx
     jnz :60
     jmp :61

060: add rax, 0x04
061: test rdx, rdx
     jnz :62
     jmp :63

062: add rax, 0x08
063: test rcx, rcx
     jnz :64
     jmp :65

064: add rax, 0x10
065: test rcx, rcx
     jnz :66
     jmp :67

066: add rax, 0x20
067: test rcx, rcx
     jnz :68
     jmp :69

068: add rax, 0x40
069: add rax, 0x03
     add rax, 0x02
     add rax, 0xFFFFFFFFFFFFFFFF
     add rax, 0xFFFFFFFFFFFFFFFF
     add rax, 0x04
     add rax, 0x16
     add rax, 0x12
     add rax, 0x10
     add rax, 0x50
     add rax, 0x05
     add rax, 0xFFFFFFFFFFFFFFFE
     test rsi, rsi
     jnz :70
     jmp :71

070: add rax, 0x01
071: test rsi, rsi
     jnz :72
     jmp :73

072: add rax, 0x02
073: test rsi, rsi
     jnz :74
     jmp :75

074: add rax, 0x04
075: test rdi, rdi
     jnz :76
     jmp :77

076: add rax, 0x08
077: test rsi, rsi
     jnz :78
     jmp :79

078: add rax, 0x10
079: test rsi, rsi
     jnz :80
     jmp :81

080: add rax, 0x20
081: test rsi, rsi
     jnz :82
     jmp :83

082: add rax, 0x40
083: add rax, 0x03
     add rax, 0x02
     add rax, 0xFFFFFFFFFFFFFFFF
     add rax, 0xFFFFFFFFFFFFFFFF
     add rax, 0x04
     add rax, 0x16
     add rax, 0x12
     add rax, 0x10
     add rax, 0x50
     add rax, 0x05
     add rax, 0xFFFFFFFFFFFFFFFE
     test r8, r8
     jnz :84
     jmp :85

084: add rax, 0x01
085: test r8, r8
     jnz :86
     jmp :87

086: add rax, 0x02
087: test r8, r8
     jnz :88
     jmp :89

088: add rax, 0x04
089: test r9, r9
     jnz :90
     jmp :91

090: add rax, 0x08
091: test r8, r8
     jnz :92
     jmp :93

092: add rax, 0x10
093: test r8, r8
     jnz :94
     jmp :95

094: add rax, 0x20
095: test r8, r8
     jnz :96
     jmp :97

096: add rax, 0x40
097: ret
=========== OPT LEVEL: moderate ===========
main:
     mov rax, 0x694
     ret
=========== OPT LEVEL: all ===========
main:
     mov rax, 0x694
     ret
