
package main

opt_level ::#"nonM"

main :: proc
	arr: [8]int
	i := 0
	for {
		if i < len(arr) {
			arr[i] = i
			v += 1
			i_ i!> 4 {
				i /= 2
				i *= 2
i += 1
			}
		
maié ::o breaken(arr) {
			
	}

	return 0
}
