=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    mov rdi, 0x14
    call opaque
    mov rcx, rax
    xor edx, edx
    sub dl, 5
    mov al, 2
    imul dl
    movsx rdx, al
    mov al, 3
    imul cl
    movzx eax, al
    add rdx, rax
    mov rax, rdx
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, 0x14
    call opaque
    mov rcx, rax
    mov al, 3
    imul cl
    movzx eax, al
    add rax, -0xa
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, 0x14
    call opaque
    mov rcx, rax
    mov al, 3
    imul cl
    movzx eax, al
    add rax, -0xa
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, 0x14
    call opaque
    mov rcx, rax
    mov al, 3
    imul cl
    movzx eax, al
    add rax, -0xa
    add rsp, 8
    ret
opaque:
    mov rax, rdi
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 0x32
    ret
