=========== OPT LEVEL: none ===========
main:
     push rbx
     push rbp
     mov rbp, 0x00
     mov rax, 0x00
     jmp :0

000: mov rcx, 0x01
     add rbp, rcx
     mov rdx, 0x05
     cmp rbp, rdx
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :1
     jmp :2

001: mov rbx, 0x00
     add rbx, rcx
     cmp rbx, rdx
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :3
     jmp :4

003: mov rbx, rax
     mov rdx, 0x00
     jmp :5

005: add rdx, rcx
     add rbx, rcx
     mov rax, 0x02
     cmp rax, rdx
     setz al
     movzx rax, al
     test rax, rax
     jz :6
     jmp :7

006: jmp :5

007: jmp :8

004: mov rbx, rax
     jmp :8

008: mov rax, rbx
     jmp :0

002: pop rbp
     pop rbx
     ret
=========== OPT LEVEL: mininal ===========
main:
     push rbx
     push rbp
     mov rbp, 0x00
     mov rax, 0x00
     jmp :0

000: mov rcx, 0x01
     add rbp, rcx
     mov rdx, 0x05
     cmp rbp, rdx
     setz bl
     movzx rbx, bl
     test rbx, rbx
     jz :1
     jmp :2

001: mov rbx, 0x00
     add rbx, rcx
     cmp rbx, rdx
     setz dl
     movzx rdx, dl
     test rdx, rdx
     jz :3
     jmp :4

003: mov rbx, rax
     mov rdx, 0x00
     jmp :5

005: add rdx, rcx
     add rbx, rcx
     mov rax, 0x02
     cmp rax, rdx
     setz al
     movzx rax, al
     test rax, rax
     jz :6
     jmp :7

006: jmp :5

007: jmp :8

004: mov rbx, rax
     jmp :8

008: mov rax, rbx
     jmp :0

002: pop rbp
     pop rbx
     ret
=========== OPT LEVEL: all ===========
main:
     mov rdx, 0x00
     mov rax, 0x00
     jmp :0

000: add rdx, 0x01
     cmp rdx, 0x05
     jnz :1
     jmp :2

001: mov rcx, 0x00
     jmp :3

003: add rax, 0x01
     add rcx, 0x01
     cmp rcx, 0x02
     jnz :4
     jmp :5

004: jmp :3

005: jmp :0

002: ret
