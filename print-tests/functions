=========== OPT LEVEL: none ===========
main:
     mov rdi, 0x0A
     call :fib
     ret
fib:
     push rbx
     push rbp
     mov rbx, rdi
     mov rax, rbx
     cmp rax, 0x02
     jnle :0
     jmp :1

000: mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib
     add rbp, rax
     jmp :2

001: mov rbp, 0x01
     jmp :2

002: mov rax, rbp
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: mininal ===========
main:
     mov rdi, 0x0A
     call :fib
     ret
fib:
     push rbx
     push rbp
     mov rbx, rdi
     mov rax, rbx
     cmp rax, 0x02
     jnle :0
     jmp :1

000: mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib
     add rbp, rax
     jmp :2

001: mov rbp, 0x01
     jmp :2

002: mov rax, rbp
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: all ===========
main:
     mov rdi, 0x0A
     call :fib
     ret
fib:
     push rbx
     push rbp
     mov rbx, rdi
     mov rax, rbx
     cmp rax, 0x02
     jnle :0
     jmp :1

000: mov rax, rbx
     sub rax, 0x01
     mov rdi, rax
     call :fib
     mov rbp, rax
     sub rbx, 0x02
     mov rdi, rbx
     call :fib
     add rbp, rax
     jmp :2

001: mov rbp, 0x01
     jmp :2

002: mov rax, rbp
     pop rbp
     pop rbx
     ret
