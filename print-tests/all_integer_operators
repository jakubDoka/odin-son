=========== OPT LEVEL: none ===========
batch:
    push rbx
batch int:
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0x14
    mov rsi, 6
    mov rdx, -7
    call 9
    mov rbx, rax
    mov dl, 7
    mov dil, 0x14
    mov sil, 6
    call 24
    mov rbp, rax
    mov rdx, 7
    mov rdi, 0x14
batch i16:
    mov rsi, 6
    call 39
    mov r12, rax
batch i32:
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 53
    mov r13, rax
    mov dl, -7
    mov dil, 0x14
    mov sil, 6
    call 68
    mov r14, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 83
    mov r15, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 98
    mov rdx, rax
    xor eax, eax
    add rax, rbx
    add rax, rbp
    add rax, r12
    add rax, r13
    add rax, r14
    add rax, r15
    add rax, rdx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
9:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul rax, rsi
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rax
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rax
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rcx
    and rax, rsi
    add r8, rax
    mov rax, rcx
    or rax, rsi
    add r8, rax
    mov rax, rcx
    xor rax, rsi
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not rdx
    and rdx, rax
    add r8, rdx
    mov rax, rcx
    sal rax, 2
    add r8, rax
    mov rax, rcx
    sar rax, 2
    add r8, rax
    mov rax, rdi
    sar rax, 2
    add r8, rax
    cmp rcx, rsi
    jnle 10
    jmp 11
10:
    add r8, 1
11:
    cmp rsi, rcx
    jnge 12
    jmp 13
12:
    add r8, 2
13:
    mov rdx, r8
    cmp rcx, rsi
    jnl 14
    jmp 15
14:
    add rdx, 4
15:
    cmp rcx, rsi
    jng 16
    jmp 17
16:
    add rdx, 8
17:
    cmp rcx, rcx
    jz 18
    jmp 19
18:
    add rdx, 0x10
19:
    cmp rcx, rsi
    jnz 20
    jmp 21
20:
    add rdx, 0x20
21:
    cmp rdi, rsi
    jnge 22
    jmp 23
22:
    add rdx, 0x40
23:
    mov rax, rdx
    ret
24:
    mov rcx, rdi
    mov rdi, rdx
    mov rdx, rcx
    mov rax, rsi
    imul dl
    movzx eax, al
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    movzx eax, al
    div sil
    movzx eax, al
    mov r9, r8
    add r9, rax
    mov rax, rcx
    movzx eax, al
    div sil
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    movzx eax, al
    div r8b
    movzx eax, al
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    movzx eax, al
    div r8b
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov rdx, rcx
    and dl, sil
    movzx eax, dl
    mov r8, r9
    add r8, rax
    mov rdx, rcx
    or dl, sil
    movzx eax, dl
    add r8, rax
    mov rdx, rcx
    xor dl, sil
    movzx eax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dl
    and dl, al
    movzx eax, dl
    add r8, rax
    mov rax, rcx
    shl al, 2
    movzx eax, al
    add r8, rax
    mov rax, rcx
    shr al, 2
    movzx eax, al
    add r8, rax
    mov rax, rdi
    shr al, 2
    movzx eax, al
    add r8, rax
    cmp cl, sil
    jnbe 25
    jmp 26
25:
    add r8, 1
26:
    cmp sil, cl
    jc 27
    jmp 28
27:
    add r8, 2
28:
    cmp cl, sil
    jnc 29
    jmp 30
29:
    add r8, 4
30:
    cmp cl, sil
    jbe 31
    jmp 32
31:
    add r8, 8
32:
    cmp cl, cl
    jz 33
    jmp 34
33:
    add r8, 0x10
34:
    cmp cl, sil
    jnz 35
    jmp 36
35:
    add r8, 0x20
36:
    cmp dil, sil
    jc 37
    jmp 38
37:
    add r8, 0x40
38:
    mov rax, r8
    ret
39:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movzx eax, ax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    sal ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    shr ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    shr ax, 2
    movzx eax, ax
    add r8, rax
    cmp cx, si
    jnbe 40
    jmp 41
40:
    add r8, 1
41:
    mov rdx, r8
    cmp si, cx
    jc +0x5
    jmp 42
    add rdx, 2
42:
    cmp cx, si
    jnc 43
    jmp 44
43:
    add rdx, 4
44:
    cmp cx, si
    jbe 45
    jmp 46
45:
    add rdx, 8
46:
    cmp cx, cx
    jz 47
    jmp 48
47:
    add rdx, 0x10
48:
    cmp cx, si
    jnz 49
    jmp 50
49:
    add rdx, 0x20
50:
    cmp di, si
    jc 51
    jmp 52
51:
    add rdx, 0x40
52:
    mov rax, rdx
    ret
53:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    mov eax, eax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    mov eax, edx
    add r8, rax
    mov rax, rcx
    sal eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rcx
    shr eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rdi
    shr eax, 2
    mov eax, eax
    add r8, rax
    cmp ecx, esi
    jnbe 54
    jmp 55
54:
    add r8, 1
55:
    mov rdx, r8
    cmp esi, ecx
    jc 56
    jmp 57
56:
    add rdx, 2
57:
    cmp ecx, esi
    jnc 58
    jmp 59
58:
    add rdx, 4
59:
    cmp ecx, esi
    jbe 60
    jmp 61
60:
    add rdx, 8
61:
    cmp ecx, ecx
    jz 62
    jmp 63
62:
    add rdx, 0x10
63:
    cmp ecx, esi
    jnz 64
    jmp 65
64:
    add rdx, 0x20
65:
    cmp edi, esi
    jc 66
    jmp 67
66:
    add rdx, 0x40
67:
    mov rax, rdx
    ret
68:
    mov rcx, rdi
    mov rdi, rdx
    mov rdx, rcx
    mov rax, rsi
    imul dl
    movsx rax, al
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cbw
    idiv sil
    movsx rax, al
    mov r9, r8
    add r9, rax
    mov rax, rcx
    cbw
    idiv sil
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    cbw
    idiv r8b
    movsx rax, al
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    cbw
    idiv r8b
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov rdx, rcx
    and dl, sil
    movsx rax, dl
    mov r8, r9
    add r8, rax
    mov rdx, rcx
    or dl, sil
    movsx rax, dl
    add r8, rax
    mov rdx, rcx
    xor dl, sil
    movsx rax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dl
    and dl, al
    movsx rax, dl
    add r8, rax
    mov rax, rcx
    shl al, 2
    movsx rax, al
    add r8, rax
    mov rax, rcx
    sar al, 2
    movsx rax, al
    add r8, rax
    mov rax, rdi
    sar al, 2
    movsx rax, al
    add r8, rax
    cmp cl, sil
    jnle 69
    jmp 70
69:
    add r8, 1
70:
    cmp sil, cl
    jnge 71
    jmp 72
71:
    add r8, 2
72:
    cmp cl, sil
    jnl 73
    jmp 74
73:
    add r8, 4
74:
    cmp cl, sil
    jng 75
    jmp 76
75:
    add r8, 8
76:
    cmp cl, cl
    jz 77
    jmp 78
77:
    add r8, 0x10
78:
    cmp cl, sil
    jnz 79
    jmp 80
79:
    add r8, 0x20
80:
    cmp dil, sil
    jnge 81
    jmp 82
81:
    add r8, 0x40
82:
    mov rax, r8
    ret
83:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movsx rax, ax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    sal ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    sar ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    sar ax, 2
    movsx rax, ax
    add r8, rax
    cmp cx, si
    jnle 84
    jmp 85
84:
    add r8, 1
85:
    mov rdx, r8
    cmp si, cx
    jnge 86
    jmp 87
86:
    add rdx, 2
87:
    cmp cx, si
    jnl 88
    jmp 89
88:
    add rdx, 4
89:
    cmp cx, si
    jng 90
    jmp 91
90:
    add rdx, 8
91:
    cmp cx, cx
    jz 92
    jmp 93
92:
    add rdx, 0x10
93:
    cmp cx, si
    jnz 94
    jmp 95
94:
    add rdx, 0x20
95:
    cmp di, si
    jnge 96
    jmp 97
96:
    add rdx, 0x40
97:
    mov rax, rdx
    ret
98:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    movsxd rax, eax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    sal eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    cmp ecx, esi
    jnle 99
    jmp 100
99:
    add r8, 1
100:
    mov rdx, r8
    cmp esi, ecx
    jnge 101
    jmp 102
101:
    add rdx, 2
102:
    cmp ecx, esi
    jnl 103
    jmp 104
103:
    add rdx, 4
104:
    cmp ecx, esi
    jng 105
    jmp 106
105:
    add rdx, 8
106:
    cmp ecx, ecx
    jz 107
    jmp 108
107:
    add rdx, 0x10
108:
    cmp ecx, esi
    jnz 109
    jmp 110
109:
    add rdx, 0x20
110:
    cmp edi, esi
    jnge 111
    jmp 112
111:
    add rdx, 0x40
112:
    mov rax, rdx
    ret
=========== OPT LEVEL: mininal ===========
batch:
    push rbx
batch int:
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0x14
    mov rsi, 6
    mov rdx, -7
    call 9
    mov rbx, rax
    mov dl, 7
    mov dil, 0x14
    mov sil, 6
    call 24
    mov rbp, rax
    mov rdx, 7
    mov rdi, 0x14
batch i16:
    mov rsi, 6
    call 39
    mov r12, rax
batch i32:
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 53
    mov r13, rax
    mov dl, -7
    mov dil, 0x14
    mov sil, 6
    call 68
    mov r14, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 83
    mov r15, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 98
    mov rdx, rax
    xor eax, eax
    add rax, rbx
    add rax, rbp
    add rax, r12
    add rax, r13
    add rax, r14
    add rax, r15
    add rax, rdx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
9:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul rax, rsi
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rax
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rax
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rcx
    and rax, rsi
    add r8, rax
    mov rax, rcx
    or rax, rsi
    add r8, rax
    mov rax, rcx
    xor rax, rsi
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not rdx
    and rdx, rax
    add r8, rdx
    mov rax, rcx
    sal rax, 2
    add r8, rax
    mov rax, rcx
    sar rax, 2
    add r8, rax
    mov rax, rdi
    sar rax, 2
    add r8, rax
    cmp rcx, rsi
    jnle 10
    jmp 11
10:
    add r8, 1
11:
    cmp rsi, rcx
    jnge 12
    jmp 13
12:
    add r8, 2
13:
    mov rdx, r8
    cmp rcx, rsi
    jnl 14
    jmp 15
14:
    add rdx, 4
15:
    cmp rcx, rsi
    jng 16
    jmp 17
16:
    add rdx, 8
17:
    mov rax, 1
    test rax, rax
    jnz 18
    jmp 19
18:
    add rdx, 0x10
19:
    cmp rcx, rsi
    jnz 20
    jmp 21
20:
    add rdx, 0x20
21:
    cmp rdi, rsi
    jnge 22
    jmp 23
22:
    add rdx, 0x40
23:
    mov rax, rdx
    ret
24:
    mov rcx, rdi
    mov rdi, rdx
    mov rdx, rcx
    mov rax, rsi
    imul dl
    movzx eax, al
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    movzx eax, al
    div sil
    movzx eax, al
    mov r9, r8
    add r9, rax
    mov rax, rcx
    movzx eax, al
    div sil
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    movzx eax, al
    div r8b
    movzx eax, al
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    movzx eax, al
    div r8b
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov rdx, rcx
    and dl, sil
    movzx eax, dl
    mov r8, r9
    add r8, rax
    mov rdx, rcx
    or dl, sil
    movzx eax, dl
    add r8, rax
    mov rdx, rcx
    xor dl, sil
    movzx eax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dl
    and dl, al
    movzx eax, dl
    add r8, rax
    mov rax, rcx
    shl al, 2
    movzx eax, al
    add r8, rax
    mov rax, rcx
    shr al, 2
    movzx eax, al
    add r8, rax
    mov rax, rdi
    shr al, 2
    movzx eax, al
    add r8, rax
    cmp cl, sil
    jnbe 25
    jmp 26
25:
    add r8, 1
26:
    cmp sil, cl
    jc 27
    jmp 28
27:
    add r8, 2
28:
    cmp cl, sil
    jnc 29
    jmp 30
29:
    add r8, 4
30:
    cmp cl, sil
    jbe 31
    jmp 32
31:
    add r8, 8
32:
    mov rax, 1
    test rax, rax
    jnz 33
    jmp 34
33:
    add r8, 0x10
34:
    cmp cl, sil
    jnz 35
    jmp 36
35:
    add r8, 0x20
36:
    cmp dil, sil
    jc 37
    jmp 38
37:
    add r8, 0x40
38:
    mov rax, r8
    ret
39:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movzx eax, ax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    sal ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    shr ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    shr ax, 2
    movzx eax, ax
    add r8, rax
    cmp cx, si
    jnbe 40
    jmp 41
40:
    add r8, 1
41:
    mov rdx, r8
    cmp si, cx
    jc +0x5
    jmp 42
    add rdx, 2
42:
    cmp cx, si
    jnc 43
    jmp 44
43:
    add rdx, 4
44:
    cmp cx, si
    jbe 45
    jmp 46
45:
    add rdx, 8
46:
    mov rax, 1
    test rax, rax
    jnz 47
    jmp 48
47:
    add rdx, 0x10
48:
    cmp cx, si
    jnz 49
    jmp 50
49:
    add rdx, 0x20
50:
    cmp di, si
    jc 51
    jmp 52
51:
    add rdx, 0x40
52:
    mov rax, rdx
    ret
53:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    mov eax, eax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    mov eax, edx
    add r8, rax
    mov rax, rcx
    sal eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rcx
    shr eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rdi
    shr eax, 2
    mov eax, eax
    add r8, rax
    cmp ecx, esi
    jnbe 54
    jmp 55
54:
    add r8, 1
55:
    mov rdx, r8
    cmp esi, ecx
    jc 56
    jmp 57
56:
    add rdx, 2
57:
    cmp ecx, esi
    jnc 58
    jmp 59
58:
    add rdx, 4
59:
    cmp ecx, esi
    jbe 60
    jmp 61
60:
    add rdx, 8
61:
    mov rax, 1
    test rax, rax
    jnz 62
    jmp 63
62:
    add rdx, 0x10
63:
    cmp ecx, esi
    jnz 64
    jmp 65
64:
    add rdx, 0x20
65:
    cmp edi, esi
    jc 66
    jmp 67
66:
    add rdx, 0x40
67:
    mov rax, rdx
    ret
68:
    mov rcx, rdi
    mov rdi, rdx
    mov rdx, rcx
    mov rax, rsi
    imul dl
    movsx rax, al
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cbw
    idiv sil
    movsx rax, al
    mov r9, r8
    add r9, rax
    mov rax, rcx
    cbw
    idiv sil
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    cbw
    idiv r8b
    movsx rax, al
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    cbw
    idiv r8b
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov rdx, rcx
    and dl, sil
    movsx rax, dl
    mov r8, r9
    add r8, rax
    mov rdx, rcx
    or dl, sil
    movsx rax, dl
    add r8, rax
    mov rdx, rcx
    xor dl, sil
    movsx rax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dl
    and dl, al
    movsx rax, dl
    add r8, rax
    mov rax, rcx
    shl al, 2
    movsx rax, al
    add r8, rax
    mov rax, rcx
    sar al, 2
    movsx rax, al
    add r8, rax
    mov rax, rdi
    sar al, 2
    movsx rax, al
    add r8, rax
    cmp cl, sil
    jnle 69
    jmp 70
69:
    add r8, 1
70:
    cmp sil, cl
    jnge 71
    jmp 72
71:
    add r8, 2
72:
    cmp cl, sil
    jnl 73
    jmp 74
73:
    add r8, 4
74:
    cmp cl, sil
    jng 75
    jmp 76
75:
    add r8, 8
76:
    mov rax, 1
    test rax, rax
    jnz 77
    jmp 78
77:
    add r8, 0x10
78:
    cmp cl, sil
    jnz 79
    jmp 80
79:
    add r8, 0x20
80:
    cmp dil, sil
    jnge 81
    jmp 82
81:
    add r8, 0x40
82:
    mov rax, r8
    ret
83:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movsx rax, ax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    sal ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    sar ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    sar ax, 2
    movsx rax, ax
    add r8, rax
    cmp cx, si
    jnle 84
    jmp 85
84:
    add r8, 1
85:
    mov rdx, r8
    cmp si, cx
    jnge 86
    jmp 87
86:
    add rdx, 2
87:
    cmp cx, si
    jnl 88
    jmp 89
88:
    add rdx, 4
89:
    cmp cx, si
    jng 90
    jmp 91
90:
    add rdx, 8
91:
    mov rax, 1
    test rax, rax
    jnz 92
    jmp 93
92:
    add rdx, 0x10
93:
    cmp cx, si
    jnz 94
    jmp 95
94:
    add rdx, 0x20
95:
    cmp di, si
    jnge 96
    jmp 97
96:
    add rdx, 0x40
97:
    mov rax, rdx
    ret
98:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    movsxd rax, eax
    xor r8d, r8d
    add r8, rax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    sal eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    cmp ecx, esi
    jnle 99
    jmp 100
99:
    add r8, 1
100:
    mov rdx, r8
    cmp esi, ecx
    jnge 101
    jmp 102
101:
    add rdx, 2
102:
    cmp ecx, esi
    jnl 103
    jmp 104
103:
    add rdx, 4
104:
    cmp ecx, esi
    jng 105
    jmp 106
105:
    add rdx, 8
106:
    mov rax, 1
    test rax, rax
    jnz 107
    jmp 108
107:
    add rdx, 0x10
108:
    cmp ecx, esi
    jnz 109
    jmp 110
109:
    add rdx, 0x20
110:
    cmp edi, esi
    jnge 111
    jmp 112
111:
    add rdx, 0x40
112:
    mov rax, rdx
    ret
=========== OPT LEVEL: moderate ===========
batch:
    push rbx
batch int:
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0x14
    mov rsi, 6
    mov rdx, -7
    call 9
    mov rbx, rax
    mov dl, 7
    mov dil, 0x14
    mov sil, 6
batch i8:
    call 22
    mov rbp, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 35
    mov r12, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 48
    mov r13, rax
    mov dl, -7
    mov dil, 0x14
    mov sil, 6
    call 61
    mov r14, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 74
    mov r15, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 87
    mov rdx, rbx
    add rdx, rbp
    add rdx, r12
    add rdx, r13
    add rdx, r14
    add rdx, r15
    add rdx, rax
    mov rax, rdx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
9:
    mov rcx, rdi
    mov rdi, rdx
    mov r8, rcx
    imul r8, rsi
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rax
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rax
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rcx
    and rax, rsi
    add r8, rax
    mov rax, rcx
    or rax, rsi
    add r8, rax
    mov rax, rcx
    xor rax, rsi
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not rdx
    and rdx, rax
    add r8, rdx
    mov rax, rcx
    sal rax, 2
    add r8, rax
    mov rax, rcx
    sar rax, 2
    add r8, rax
    mov rax, rdi
    sar rax, 2
    add r8, rax
    cmp rcx, rsi
    jnle 10
    jmp 11
10:
    add r8, 1
11:
    cmp rsi, rcx
    jnge 12
    jmp 13
12:
    add r8, 2
13:
    mov rdx, r8
    cmp rcx, rsi
    jnl 14
    jmp 15
14:
    add rdx, 4
15:
    cmp rcx, rsi
    jng 16
    jmp 17
16:
    add rdx, 8
17:
    cmp rcx, rsi
    jnz 18
    add rdx, 0x10
    jmp 19
18:
    add rdx, 0x30
19:
    cmp rdi, rsi
    jnge 20
    jmp 21
20:
    add rdx, 0x40
21:
    mov rax, rdx
    ret
22:
    mov rcx, rdi
    mov rdi, rdx
    mov rdx, rcx
    mov rax, rsi
    imul dl
    movzx r8d, al
    mov rax, rcx
    movzx eax, al
    div sil
    movzx eax, al
    mov r9, r8
    add r9, rax
    mov rax, rcx
    movzx eax, al
    div sil
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    movzx eax, al
    div r8b
    movzx eax, al
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    movzx eax, al
    div r8b
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov rdx, rcx
    and dl, sil
    movzx eax, dl
    mov r8, r9
    add r8, rax
    mov rdx, rcx
    or dl, sil
    movzx eax, dl
    add r8, rax
    mov rdx, rcx
    xor dl, sil
    movzx eax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dl
    and dl, al
    movzx eax, dl
    add r8, rax
    mov rax, rcx
    shl al, 2
    movzx eax, al
    add r8, rax
    mov rax, rcx
    shr al, 2
    movzx eax, al
    add r8, rax
    mov rax, rdi
    shr al, 2
    movzx eax, al
    add r8, rax
    cmp cl, sil
    jnbe 23
    jmp 24
23:
    add r8, 1
24:
    cmp sil, cl
    jc 25
    jmp 26
25:
    add r8, 2
26:
    cmp cl, sil
    jnc 27
    jmp 28
27:
    add r8, 4
28:
    cmp cl, sil
    jbe 29
    jmp 30
29:
    add r8, 8
30:
    cmp cl, sil
    jnz 31
    add r8, 0x10
    jmp 32
31:
    add r8, 0x30
32:
    cmp dil, sil
    jc 33
    jmp 34
33:
    add r8, 0x40
34:
    mov rax, r8
    ret
35:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movzx r8d, ax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    sal ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    shr ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    shr ax, 2
    movzx eax, ax
    add r8, rax
    cmp cx, si
    jnbe 36
    jmp 37
36:
    add r8, 1
37:
    mov rdx, r8
    cmp si, cx
    jc 38
    jmp 39
38:
    add rdx, 2
39:
    cmp cx, si
    jnc 40
    jmp 41
40:
    add rdx, 4
41:
    cmp cx, si
    jbe 42
    jmp 43
42:
    add rdx, 8
43:
    cmp cx, si
    jnz 44
    add rdx, 0x10
    jmp 45
44:
    add rdx, 0x30
45:
    cmp di, si
    jc 46
    jmp 47
46:
    add rdx, 0x40
47:
    mov rax, rdx
    ret
48:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    mov r8d, eax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    mov eax, edx
    add r8, rax
    mov rax, rcx
    sal eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rcx
    shr eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rdi
    shr eax, 2
    mov eax, eax
    add r8, rax
    cmp ecx, esi
    jnbe 49
    jmp 50
49:
    add r8, 1
50:
    mov rdx, r8
    cmp esi, ecx
    jc 51
    jmp 52
51:
    add rdx, 2
52:
    cmp ecx, esi
    jnc 53
    jmp 54
53:
    add rdx, 4
54:
    cmp ecx, esi
    jbe 55
    jmp 56
55:
    add rdx, 8
56:
    cmp ecx, esi
    jnz 57
    add rdx, 0x10
    jmp 58
57:
    add rdx, 0x30
58:
    cmp edi, esi
    jc 59
    jmp 60
59:
    add rdx, 0x40
60:
    mov rax, rdx
    ret
61:
    mov rcx, rdi
    mov rdi, rdx
    mov rdx, rcx
    mov rax, rsi
    imul dl
    movsx r8, al
    mov rax, rcx
    cbw
    idiv sil
    movsx rax, al
    mov r9, r8
    add r9, rax
    mov rax, rcx
    cbw
    idiv sil
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    cbw
    idiv r8b
    movsx rax, al
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    cbw
    idiv r8b
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov rdx, rcx
    and dl, sil
    movsx rax, dl
    mov r8, r9
    add r8, rax
    mov rdx, rcx
    or dl, sil
    movsx rax, dl
    add r8, rax
    mov rdx, rcx
    xor dl, sil
    movsx rax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dl
    and dl, al
    movsx rax, dl
    add r8, rax
    mov rax, rcx
    shl al, 2
    movsx rax, al
    add r8, rax
    mov rax, rcx
    sar al, 2
    movsx rax, al
    add r8, rax
    mov rax, rdi
    sar al, 2
    movsx rax, al
    add r8, rax
    cmp cl, sil
    jnle 62
    jmp 63
62:
    add r8, 1
63:
    cmp sil, cl
    jnge 64
    jmp 65
64:
    add r8, 2
65:
    cmp cl, sil
    jnl 66
    jmp 67
66:
    add r8, 4
67:
    cmp cl, sil
    jng 68
    jmp 69
68:
    add r8, 8
69:
    cmp cl, sil
    jnz 70
    add r8, 0x10
    jmp 71
70:
    add r8, 0x30
71:
    cmp dil, sil
    jnge 72
    jmp 73
72:
    add r8, 0x40
73:
    mov rax, r8
    ret
74:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movsx r8, ax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    sal ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    sar ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    sar ax, 2
    movsx rax, ax
    add r8, rax
    cmp cx, si
    jnle 75
    jmp 76
75:
    add r8, 1
76:
    mov rdx, r8
    cmp si, cx
    jnge 77
    jmp 78
77:
    add rdx, 2
78:
    cmp cx, si
    jnl 79
    jmp 80
79:
    add rdx, 4
80:
    cmp cx, si
    jng 81
    jmp 82
81:
    add rdx, 8
82:
    cmp cx, si
    jnz 83
    add rdx, 0x10
    jmp 84
83:
    add rdx, 0x30
84:
    cmp di, si
    jnge 85
    jmp 86
85:
    add rdx, 0x40
86:
    mov rax, rdx
    ret
87:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    movsxd r8, eax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    sal eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    cmp ecx, esi
    jnle 88
    jmp 89
88:
    add r8, 1
89:
    mov rdx, r8
    cmp esi, ecx
    jnge 90
    jmp 91
90:
    add rdx, 2
91:
    cmp ecx, esi
    jnl 92
    jmp 93
92:
    add rdx, 4
93:
    cmp ecx, esi
    jng 94
    jmp 95
94:
    add rdx, 8
95:
    cmp ecx, esi
    jnz 96
    add rdx, 0x10
    jmp 97
96:
    add rdx, 0x30
97:
    cmp edi, esi
    jnge 98
    jmp 99
98:
    add rdx, 0x40
99:
    mov rax, rdx
    ret
=========== OPT LEVEL: all ===========
batch:
    push rbx
batch int:
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 8
    mov rdi, 0x14
    mov rsi, 6
    mov rdx, -7
    call 9
    mov rbx, rax
    mov dl, 7
    mov dil, 0x14
    mov sil, 6
batch i8:
    call 22
    mov rbp, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 35
    mov r12, rax
    mov rdx, 7
    mov rdi, 0x14
    mov rsi, 6
    call 48
    mov r13, rax
    mov dl, -7
    mov dil, 0x14
    mov sil, 6
    call 61
    mov r14, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 74
    mov r15, rax
    mov rdx, -7
    mov rdi, 0x14
    mov rsi, 6
    call 87
    mov rdx, rbx
    add rdx, rbp
    add rdx, r12
    add rdx, r13
    add rdx, r14
    add rdx, r15
    add rdx, rax
    mov rax, rdx
    add rsp, 8
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
9:
    mov rcx, rdi
    mov rdi, rdx
    mov r8, rcx
    imul r8, rsi
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rax
    mov rax, rcx
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rax
    mov rax, rdi
    cqo
    idiv rsi
    add r8, rdx
    mov rax, rcx
    and rax, rsi
    add r8, rax
    mov rax, rcx
    or rax, rsi
    add r8, rax
    mov rax, rcx
    xor rax, rsi
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not rdx
    and rdx, rax
    add r8, rdx
    mov rax, rcx
    sal rax, 2
    add r8, rax
    mov rax, rcx
    sar rax, 2
    add r8, rax
    mov rax, rdi
    sar rax, 2
    add r8, rax
    cmp rcx, rsi
    jnle 10
    jmp 11
10:
    add r8, 1
11:
    cmp rsi, rcx
    jnge 12
    jmp 13
12:
    add r8, 2
13:
    mov rdx, r8
    cmp rcx, rsi
    jnl 14
    jmp 15
14:
    add rdx, 4
15:
    cmp rcx, rsi
    jng 16
    jmp 17
16:
    add rdx, 8
17:
    cmp rcx, rsi
    jnz 18
    add rdx, 0x10
    jmp 19
18:
    add rdx, 0x30
19:
    cmp rdi, rsi
    jnge 20
    jmp 21
20:
    add rdx, 0x40
21:
    mov rax, rdx
    ret
22:
    mov rcx, rdi
    mov rdi, rdx
    mov rdx, rcx
    mov rax, rsi
    imul dl
    movzx r8d, al
    mov rax, rcx
    movzx eax, al
    div sil
    movzx eax, al
    mov r9, r8
    add r9, rax
    mov rax, rcx
    movzx eax, al
    div sil
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    movzx eax, al
    div r8b
    movzx eax, al
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    movzx eax, al
    div r8b
    movsx edx, ah
    movzx eax, dl
    add r9, rax
    mov rdx, rcx
    and dl, sil
    movzx eax, dl
    mov r8, r9
    add r8, rax
    mov rdx, rcx
    or dl, sil
    movzx eax, dl
    add r8, rax
    mov rdx, rcx
    xor dl, sil
    movzx eax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dl
    and dl, al
    movzx eax, dl
    add r8, rax
    mov rax, rcx
    shl al, 2
    movzx eax, al
    add r8, rax
    mov rax, rcx
    shr al, 2
    movzx eax, al
    add r8, rax
    mov rax, rdi
    shr al, 2
    movzx eax, al
    add r8, rax
    cmp cl, sil
    jnbe 23
    jmp 24
23:
    add r8, 1
24:
    cmp sil, cl
    jc 25
    jmp 26
25:
    add r8, 2
26:
    cmp cl, sil
    jnc 27
    jmp 28
27:
    add r8, 4
28:
    cmp cl, sil
    jbe 29
    jmp 30
29:
    add r8, 8
30:
    cmp cl, sil
    jnz 31
    add r8, 0x10
    jmp 32
31:
    add r8, 0x30
32:
    cmp dil, sil
    jc 33
    jmp 34
33:
    add r8, 0x40
34:
    mov rax, r8
    ret
35:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movzx r8d, ax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div si
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movzx eax, dx
    add r8, rax
    mov rax, rcx
    sal ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rcx
    shr ax, 2
    movzx eax, ax
    add r8, rax
    mov rax, rdi
    shr ax, 2
    movzx eax, ax
    add r8, rax
    cmp cx, si
    jnbe 36
    jmp 37
36:
    add r8, 1
37:
    mov rdx, r8
    cmp si, cx
    jc 38
    jmp 39
38:
    add rdx, 2
39:
    cmp cx, si
    jnc 40
    jmp 41
40:
    add rdx, 4
41:
    cmp cx, si
    jbe 42
    jmp 43
42:
    add rdx, 8
43:
    cmp cx, si
    jnz 44
    add rdx, 0x10
    jmp 45
44:
    add rdx, 0x30
45:
    cmp di, si
    jc 46
    jmp 47
46:
    add rdx, 0x40
47:
    mov rax, rdx
    ret
48:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    mov r8d, eax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, eax
    add r8, rax
    mov rax, rdi
    xor rdx, rdx
    div esi
    mov eax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    mov eax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    mov eax, edx
    add r8, rax
    mov rax, rcx
    sal eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rcx
    shr eax, 2
    mov eax, eax
    add r8, rax
    mov rax, rdi
    shr eax, 2
    mov eax, eax
    add r8, rax
    cmp ecx, esi
    jnbe 49
    jmp 50
49:
    add r8, 1
50:
    mov rdx, r8
    cmp esi, ecx
    jc 51
    jmp 52
51:
    add rdx, 2
52:
    cmp ecx, esi
    jnc 53
    jmp 54
53:
    add rdx, 4
54:
    cmp ecx, esi
    jbe 55
    jmp 56
55:
    add rdx, 8
56:
    cmp ecx, esi
    jnz 57
    add rdx, 0x10
    jmp 58
57:
    add rdx, 0x30
58:
    cmp edi, esi
    jc 59
    jmp 60
59:
    add rdx, 0x40
60:
    mov rax, rdx
    ret
61:
    mov rcx, rdi
    mov rdi, rdx
    mov rdx, rcx
    mov rax, rsi
    imul dl
    movsx r8, al
    mov rax, rcx
    cbw
    idiv sil
    movsx rax, al
    mov r9, r8
    add r9, rax
    mov rax, rcx
    cbw
    idiv sil
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    cbw
    idiv r8b
    movsx rax, al
    add r9, rax
    mov r8, rsi
    mov rax, rdi
    cbw
    idiv r8b
    movzx edx, ah
    movsx rax, dl
    add r9, rax
    mov rdx, rcx
    and dl, sil
    movsx rax, dl
    mov r8, r9
    add r8, rax
    mov rdx, rcx
    or dl, sil
    movsx rax, dl
    add r8, rax
    mov rdx, rcx
    xor dl, sil
    movsx rax, dl
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dl
    and dl, al
    movsx rax, dl
    add r8, rax
    mov rax, rcx
    shl al, 2
    movsx rax, al
    add r8, rax
    mov rax, rcx
    sar al, 2
    movsx rax, al
    add r8, rax
    mov rax, rdi
    sar al, 2
    movsx rax, al
    add r8, rax
    cmp cl, sil
    jnle 62
    jmp 63
62:
    add r8, 1
63:
    cmp sil, cl
    jnge 64
    jmp 65
64:
    add r8, 2
65:
    cmp cl, sil
    jnl 66
    jmp 67
66:
    add r8, 4
67:
    cmp cl, sil
    jng 68
    jmp 69
68:
    add r8, 8
69:
    cmp cl, sil
    jnz 70
    add r8, 0x10
    jmp 71
70:
    add r8, 0x30
71:
    cmp dil, sil
    jnge 72
    jmp 73
72:
    add r8, 0x40
73:
    mov rax, r8
    ret
74:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul ax, si
    movsx r8, ax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    cwd
    idiv si
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    and ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    or ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    xor ax, si
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not dx
    and dx, ax
    movsx rax, dx
    add r8, rax
    mov rax, rcx
    sal ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rcx
    sar ax, 2
    movsx rax, ax
    add r8, rax
    mov rax, rdi
    sar ax, 2
    movsx rax, ax
    add r8, rax
    cmp cx, si
    jnle 75
    jmp 76
75:
    add r8, 1
76:
    mov rdx, r8
    cmp si, cx
    jnge 77
    jmp 78
77:
    add rdx, 2
78:
    cmp cx, si
    jnl 79
    jmp 80
79:
    add rdx, 4
80:
    cmp cx, si
    jng 81
    jmp 82
81:
    add rdx, 8
82:
    cmp cx, si
    jnz 83
    add rdx, 0x10
    jmp 84
83:
    add rdx, 0x30
84:
    cmp di, si
    jnge 85
    jmp 86
85:
    add rdx, 0x40
86:
    mov rax, rdx
    ret
87:
    mov rcx, rdi
    mov rdi, rdx
    mov rax, rcx
    imul eax, esi
    movsxd r8, eax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    cdq
    idiv esi
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    and eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    or eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    xor eax, esi
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    mov rdx, rsi
    not edx
    and edx, eax
    movsxd rax, edx
    add r8, rax
    mov rax, rcx
    sal eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rcx
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    mov rax, rdi
    sar eax, 2
    movsxd rax, eax
    add r8, rax
    cmp ecx, esi
    jnle 88
    jmp 89
88:
    add r8, 1
89:
    mov rdx, r8
    cmp esi, ecx
    jnge 90
    jmp 91
90:
    add rdx, 2
91:
    cmp ecx, esi
    jnl 92
    jmp 93
92:
    add rdx, 4
93:
    cmp ecx, esi
    jng 94
    jmp 95
94:
    add rdx, 8
95:
    cmp ecx, esi
    jnz 96
    add rdx, 0x10
    jmp 97
96:
    add rdx, 0x30
97:
    cmp edi, esi
    jnge 98
    jmp 99
98:
    add rdx, 0x40
99:
    mov rax, rdx
    ret
=========== OPT LEVEL: aggresive ===========
batch:
    mov rax, 0x9dc
batch i16:
    ret
