=========== OPT LEVEL: none ===========
divmod:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     cqo
     idiv rsi
     mov [rdi], rax
     mov rax, rcx
     cqo
     idiv rsi
     mov rax, rdx
     ret
main:
     sub rsp, 0x08
     mov rdi, 0x2F
     mov rsi, 0x05
     lea rdx, [rsp]
     call :divmod
     mov rcx, rax
     mov rax, [rsp]
     imul rax, rax, 0x64
     add rax, rcx
     add rsp, 0x08
     ret
=========== OPT LEVEL: mininal ===========
divmod:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     cqo
     idiv rsi
     mov [rdi], rax
     mov rax, rcx
     cqo
     idiv rsi
     mov rax, rdx
     ret
main:
     sub rsp, 0x08
     mov rdi, 0x2F
     mov rsi, 0x05
     lea rdx, [rsp]
     call :divmod
     mov rcx, rax
     mov rax, [rsp]
     imul rax, rax, 0x64
     add rax, rcx
     add rsp, 0x08
     ret
=========== OPT LEVEL: moderate ===========
divmod:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     cqo
     idiv rsi
     mov [rdi], rax
     mov rax, rcx
     cqo
     idiv rsi
     mov rax, rdx
     ret
main:
     sub rsp, 0x08
     mov rdi, 0x2F
     mov rsi, 0x05
     lea rdx, [rsp]
     call :divmod
     mov rcx, rax
     mov rax, [rsp]
     imul rax, rax, 0x64
     add rax, rcx
     add rsp, 0x08
     ret
=========== OPT LEVEL: all ===========
divmod:
     mov rcx, rdi
     mov rdi, rdx
     mov rax, rcx
     cqo
     idiv rsi
     mov [rdi], rax
     mov rax, rcx
     cqo
     idiv rsi
     mov rax, rdx
     ret
main:
     sub rsp, 0x08
     mov rdi, 0x2F
     mov rsi, 0x05
     lea rdx, [rsp]
     call :divmod
     mov rcx, rax
     mov rax, [rsp]
     imul rax, rax, 0x64
     add rax, rcx
     add rsp, 0x08
     ret
=========== OPT LEVEL: aggresive ===========
divmod:
main:
     mov rax, 0x386
     ret
