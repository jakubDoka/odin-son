=========== OPT LEVEL: none ===========
main:
     mov rdi, 0x00
     mov rax, 0x00
005: cmp rdi, 0x04
     jnl :0
     mov rsi, 0x00
     mov rcx, 0x00
002: cmp rcx, 0x04
     jnl :1
     add rsi, rcx
     add rcx, 0x01
     jmp :2

001: mov rcx, 0x00
     mov rdx, 0x00
004: cmp rdx, 0x04
     jnl :3
     lea rcx, [rcx+rdx*2]
     add rdx, 0x01
     jmp :4

003: add rsi, rcx
     add rsi, rax
     add rdi, 0x01
     mov rax, rsi
     jmp :5

000: mov rcx, 0xFB
     cqo
     idiv rcx
     mov rax, rdx
     ret
=========== OPT LEVEL: mininal ===========
main:
     mov rdi, 0x00
     mov rax, 0x00
005: cmp rdi, 0x04
     jnl :0
     mov rsi, 0x00
     mov rcx, 0x00
002: cmp rcx, 0x04
     jnl :1
     add rsi, rcx
     add rcx, 0x01
     jmp :2

001: mov rcx, 0x00
     mov rdx, 0x00
004: cmp rdx, 0x04
     jnl :3
     lea rcx, [rcx+rdx*2]
     add rdx, 0x01
     jmp :4

003: add rsi, rcx
     add rsi, rax
     add rdi, 0x01
     mov rax, rsi
     jmp :5

000: mov rcx, 0xFB
     cqo
     idiv rcx
     mov rax, rdx
     ret
=========== OPT LEVEL: moderate ===========
main:
     mov rdi, 0x00
     mov rax, 0x00
005: cmp rdi, 0x04
     jnl :0
     mov rsi, 0x00
     mov rcx, 0x00
002: cmp rcx, 0x04
     jnl :1
     add rsi, rcx
     add rcx, 0x01
     jmp :2

001: mov rcx, 0x00
     mov rdx, 0x00
004: cmp rdx, 0x04
     jnl :3
     lea rcx, [rcx+rdx*2]
     add rdx, 0x01
     jmp :4

003: add rsi, rcx
     add rsi, rax
     add rdi, 0x01
     mov rax, rsi
     jmp :5

000: mov rcx, 0xFB
     cqo
     idiv rcx
     mov rax, rdx
     ret
=========== OPT LEVEL: all ===========
main:
     mov rdi, 0x00
     mov rax, 0x00
005: cmp rdi, 0x04
     jnl :0
     mov rsi, 0x00
     mov rcx, 0x00
002: cmp rcx, 0x04
     jnl :1
     add rsi, rcx
     add rcx, 0x01
     jmp :2

001: mov rcx, 0x00
     mov rdx, 0x00
004: cmp rdx, 0x04
     jnl :3
     lea rcx, [rcx+rdx*2]
     add rdx, 0x01
     jmp :4

003: add rsi, rcx
     add rsi, rax
     add rdi, 0x01
     mov rax, rsi
     jmp :5

000: mov rcx, 0xFB
     cqo
     idiv rcx
     mov rax, rdx
     ret
=========== OPT LEVEL: aggresive ===========
