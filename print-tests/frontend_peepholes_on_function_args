=========== OPT LEVEL: none ===========
main:
    sub rsp, 8
    mov rax, 2
    add rax, 2
    mov rcx, rax
    add rcx, 2
    mov rdi, 2
    mov rsi, rax
    mov rdx, rcx
    call +0xa
    add rsp, 8
    ret
funnel:
    mov rax, rdi
    add rax, rsi
    add rax, rdx
    ret
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 8
    mov rdi, 2
    mov rsi, 4
    mov rdx, 6
    call +0xa
    add rsp, 8
    ret
funnel:
    mov rax, rdi
    add rax, rsi
    add rax, rdx
    ret
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 8
    mov rdi, 2
    mov rsi, 4
    mov rdx, 6
    call +0xa
    add rsp, 8
    ret
funnel:
    mov rax, rdi
    add rax, rsi
    add rax, rdx
    ret
=========== OPT LEVEL: all ===========
main:
    sub rsp, 8
    mov rdi, 2
    mov rsi, 4
    mov rdx, 6
    call +0xa
    add rsp, 8
    ret
funnel:
    mov rax, rdi
    add rax, rsi
    add rax, rdx
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 0xc
    ret
funnel:
