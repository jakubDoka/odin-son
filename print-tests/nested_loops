=========== OPT LEVEL: none ===========
main:
     push rbx
     mov rbx, 0x00
     mov rax, 0x00
     jmp :0

000: cmp rbx, 0x03
     jnz :1
     jmp :2

001: mov rcx, 0x00
     mov rdx, rax
     jmp :3

003: cmp rcx, 0x03
     jnz :4
     jmp :5

004: mov rax, rbx
     imul rax, rcx
     add rax, rdx
     add rcx, 0x01
     mov rdx, rax
     jmp :3

005: add rbx, 0x01
     mov rax, rdx
     jmp :0

002: pop rbx
     ret
=========== OPT LEVEL: mininal ===========
main:
     push rbx
     mov rbx, 0x00
     mov rax, 0x00
     jmp :0

000: cmp rbx, 0x03
     jnz :1
     jmp :2

001: mov rcx, 0x00
     mov rdx, rax
     jmp :3

003: cmp rcx, 0x03
     jnz :4
     jmp :5

004: mov rax, rbx
     imul rax, rcx
     add rax, rdx
     add rcx, 0x01
     mov rdx, rax
     jmp :3

005: add rbx, 0x01
     mov rax, rdx
     jmp :0

002: pop rbx
     ret
=========== OPT LEVEL: all ===========
main:
     push rbx
     mov rbx, 0x00
     mov rax, 0x00
     jmp :0

000: cmp rbx, 0x03
     jnz :1
     jmp :2

001: mov rcx, 0x00
     mov rdx, rax
     jmp :3

003: cmp rcx, 0x03
     jnz :4
     jmp :5

004: mov rax, rbx
     imul rax, rcx
     add rax, rdx
     add rcx, 0x01
     mov rdx, rax
     jmp :3

005: add rbx, 0x01
     mov rax, rdx
     jmp :0

002: pop rbx
     ret
