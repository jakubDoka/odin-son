=========== OPT LEVEL: none ===========
main:
     push rbx
     push rbp
     push r12
     push r13
     push r14
     push r15
     mov rcx, 0x06
     mov rax, 0x14
     cqo
     idiv rcx
     mov rdi, rax
     mov rax, 0x00
     add rdi, rax
     mov rax, 0x14
     cqo
     idiv rcx
     add rdi, rdx
     mov rax, 0x00
     sub rax, 0x07
     mov rsi, rax
     mov rax, rsi
     cqo
     idiv rcx
     add rdi, rax
     mov rax, rsi
     cqo
     idiv rcx
     add rdi, rdx
     mov rax, 0x14
     and rax, 0x06
     add rdi, rax
     mov rax, 0x14
     or rax, 0x06
     add rdi, rax
     mov rax, 0x14
     xor rax, 0x06
     add rdi, rax
     mov rax, 0x14
     mov rdx, 0x06
     not rdx
     and rdx, rax
     add rdi, rdx
     mov rax, 0x14
     shl rax, 0x02
     add rdi, rax
     mov rax, 0x14
     sar rax, 0x02
     add rdi, rax
     mov rax, rsi
     sar rax, 0x02
     add rdi, rax
     mov rax, 0x14
     cmp rax, 0x06
     jle :0
     jmp :1

000: jmp :2

001: add rdi, 0x01
     jmp :2

002: cmp rcx, 0x14
     jnl :3
     jmp :4

003: jmp :5

004: add rdi, 0x02
     jmp :5

005: mov rcx, rdi
     mov rax, 0x14
     cmp rax, 0x06
     jl :6
     jmp :7

006: jmp :8

007: add rcx, 0x04
     jmp :8

008: mov rax, 0x14
     cmp rax, 0x06
     jnle :9
     jmp :10

009: jmp :11

010: add rcx, 0x08
     jmp :11

011: mov rax, 0x14
     cmp rax, 0x14
     jnz :12
     jmp :13

012: jmp :14

013: add rcx, 0x10
     jmp :14

014: mov rax, 0x14
     cmp rax, 0x06
     jz :15
     jmp :16

015: jmp :17

016: add rcx, 0x20
     jmp :17

017: mov rax, rsi
     cmp rax, 0x06
     jnl :18
     jmp :19

018: jmp :20

019: add rcx, 0x40
     jmp :20

020: mov r9, 0x06
     mov rax, 0x14
     movzx eax, al
     div r9b
     movzx eax, al
     add rcx, rax
     mov rax, 0x14
     movzx eax, al
     div r9b
     movsx edx, ah
     movzx eax, dl
     add rcx, rax
     mov rax, 0x07
     movzx eax, al
     div r9b
     movzx eax, al
     add rcx, rax
     mov rax, 0x07
     movzx eax, al
     div r9b
     movsx edx, ah
     movzx eax, dl
     add rcx, rax
     mov rax, 0x14
     and rax, 0x06
     mov [rsp-0x78], rax
     mov rax, 0x14
     or rax, 0x06
     mov [rsp-0x70], rax
     mov rax, 0x14
     xor rax, 0x06
     mov [rsp-0x68], rax
     mov rax, 0x14
     mov rsi, 0x06
     not sil
     and sil, al
     mov rax, [rsp-0x78]
     movzx eax, al
     add rcx, rax
     mov rax, 0x14
     shl rax, 0x02
     mov [rsp-0x60], rax
     mov rax, [rsp-0x70]
     movzx eax, al
     add rcx, rax
     mov rax, [rsp-0x68]
     movzx eax, al
     add rcx, rax
     movzx eax, sil
     add rcx, rax
     mov rax, [rsp-0x60]
     movzx eax, al
     add rcx, rax
     mov rax, 0x14
     shr rax, 0x02
     movzx eax, al
     add rcx, rax
     mov rax, 0x07
     shr rax, 0x02
     movzx eax, al
     add rcx, rax
     mov rax, 0x14
     cmp al, 0x06
     jbe :21
     jmp :22

021: jmp :23

022: add rcx, 0x01
     jmp :23

023: cmp r9b, 0x14
     jnb :24
     jmp :25

024: jmp :26

025: add rcx, 0x02
     jmp :26

026: mov rax, 0x14
     cmp al, 0x06
     jb :27
     jmp :28

027: jmp :29

028: add rcx, 0x04
     jmp :29

029: mov rax, 0x14
     cmp al, 0x06
     jnbe :30
     jmp :31

030: jmp :32

031: add rcx, 0x08
     jmp :32

032: mov rax, 0x14
     cmp al, 0x14
     setz r10b
     movzx r10, r10b
     test r10, r10
     jz :33
     jmp :34

033: jmp :35

034: add rcx, 0x10
     jmp :35

035: mov rax, 0x14
     cmp al, 0x06
     setnz r11b
     movzx r11, r11b
     test r11, r11
     jz :36
     jmp :37

036: jmp :38

037: add rcx, 0x20
     jmp :38

038: mov rax, 0x07
     cmp al, 0x06
     jnb :39
     jmp :40

039: jmp :41

040: add rcx, 0x40
     jmp :41

041: mov rbx, 0x06
     mov rax, 0x14
     xor rdx, rdx
     div bx
     movzx eax, ax
     add rcx, rax
     mov rax, 0x14
     xor rdx, rdx
     div bx
     movzx eax, dx
     add rcx, rax
     mov rax, 0x07
     xor rdx, rdx
     div bx
     movzx eax, ax
     add rcx, rax
     mov rax, 0x07
     xor rdx, rdx
     div bx
     movzx eax, dx
     add rcx, rax
     mov rax, 0x14
     and rax, 0x06
     mov [rsp-0x58], rax
     mov rax, 0x14
     or rax, 0x06
     mov [rsp-0x50], rax
     mov rax, 0x14
     xor rax, 0x06
     mov [rsp-0x48], rax
     mov rax, 0x14
     mov rdi, 0x06
     not di
     and di, ax
     mov rax, [rsp-0x58]
     movzx eax, ax
     add rcx, rax
     mov rdx, rcx
     mov rax, 0x14
     shl rax, 0x02
     mov [rsp-0x40], rax
     mov rax, [rsp-0x50]
     movzx ecx, ax
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x48]
     movzx ecx, ax
     mov rax, rdx
     add rax, rcx
     movzx ecx, di
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x40]
     movzx ecx, ax
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, 0x14
     shr rax, 0x02
     movzx ecx, ax
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, 0x07
     shr rax, 0x02
     movzx ecx, ax
     mov rax, rdx
     add rax, rcx
     mov r8, rax
     mov rax, 0x14
     cmp ax, 0x06
     jbe :42
     jmp :43

042: jmp :44

043: mov rax, r8
     add rax, 0x01
     mov r8, rax
     jmp :44

044: cmp bx, 0x14
     jnb :45
     jmp :46

045: jmp :47

046: mov rax, r8
     add rax, 0x02
     mov r8, rax
     jmp :47

047: mov rax, 0x14
     cmp ax, 0x06
     jb :48
     jmp :49

048: jmp :50

049: mov rax, r8
     add rax, 0x04
     mov r8, rax
     jmp :50

050: mov rax, 0x14
     cmp ax, 0x06
     jnbe :51
     jmp :52

051: jmp :53

052: mov rax, r8
     add rax, 0x08
     mov r8, rax
     jmp :53

053: mov rax, 0x14
     cmp ax, 0x14
     setz bpl
     movzx rbp, bpl
     test rbp, rbp
     jz :54
     jmp :55

054: jmp :56

055: mov rax, r8
     add rax, 0x10
     mov r8, rax
     jmp :56

056: mov rax, 0x14
     cmp ax, 0x06
     setnz r12b
     movzx r12, r12b
     test r12, r12
     jz :57
     jmp :58

057: jmp :59

058: mov rax, r8
     add rax, 0x20
     mov r8, rax
     jmp :59

059: mov rax, 0x07
     cmp ax, 0x06
     jnb :60
     jmp :61

060: jmp :62

061: mov rax, r8
     add rax, 0x40
     mov r8, rax
     jmp :62

062: mov r13, 0x06
     mov rax, 0x14
     xor rdx, rdx
     div r13d
     mov ecx, eax
     mov rax, r8
     add rax, rcx
     mov r8, rax
     mov rax, 0x14
     xor rdx, rdx
     div r13d
     mov ecx, edx
     mov rax, r8
     add rax, rcx
     mov r8, rax
     mov rax, 0x07
     xor rdx, rdx
     div r13d
     mov ecx, eax
     mov rax, r8
     add rax, rcx
     mov r8, rax
     mov rax, 0x07
     xor rdx, rdx
     div r13d
     mov ecx, edx
     mov rax, r8
     add rax, rcx
     mov rdx, rax
     mov rax, 0x14
     and rax, 0x06
     mov [rsp-0x38], rax
     mov rax, 0x14
     or rax, 0x06
     mov [rsp-0x30], rax
     mov rax, 0x14
     xor rax, 0x06
     mov [rsp-0x28], rax
     mov rax, 0x14
     mov r8, 0x06
     not r8d
     and r8d, eax
     mov rax, [rsp-0x38]
     mov ecx, eax
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, 0x14
     shl rax, 0x02
     mov [rsp-0x20], rax
     mov rax, [rsp-0x30]
     mov ecx, eax
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x28]
     mov ecx, eax
     mov rax, rdx
     add rax, rcx
     mov ecx, r8d
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x20]
     mov ecx, eax
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, 0x14
     shr rax, 0x02
     mov ecx, eax
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, 0x07
     shr rax, 0x02
     mov ecx, eax
     mov rax, rdx
     add rax, rcx
     mov [rsp-0x18], rax
     mov rax, 0x14
     cmp eax, 0x06
     jbe :63
     jmp :64

063: jmp :65

064: mov rax, [rsp-0x18]
     add rax, 0x01
     mov [rsp-0x18], rax
     jmp :65

065: cmp r13d, 0x14
     jnb :66
     jmp :67

066: jmp :68

067: mov rax, [rsp-0x18]
     add rax, 0x02
     mov [rsp-0x18], rax
     jmp :68

068: mov rax, 0x14
     cmp eax, 0x06
     jb :69
     jmp :70

069: jmp :71

070: mov rax, [rsp-0x18]
     add rax, 0x04
     mov [rsp-0x18], rax
     jmp :71

071: mov rax, 0x14
     cmp eax, 0x06
     jnbe :72
     jmp :73

072: jmp :74

073: mov rax, [rsp-0x18]
     add rax, 0x08
     mov [rsp-0x18], rax
     jmp :74

074: mov rax, 0x14
     cmp eax, 0x14
     setz r14b
     movzx r14, r14b
     test r14, r14
     jz :75
     jmp :76

075: jmp :77

076: mov rax, [rsp-0x18]
     add rax, 0x10
     mov [rsp-0x18], rax
     jmp :77

077: mov rax, 0x14
     cmp eax, 0x06
     setnz r15b
     movzx r15, r15b
     test r15, r15
     jz :78
     jmp :79

078: jmp :80

079: mov rax, [rsp-0x18]
     add rax, 0x20
     mov [rsp-0x18], rax
     jmp :80

080: mov rax, 0x07
     cmp eax, 0x06
     jnb :81
     jmp :82

081: jmp :83

082: mov rax, [rsp-0x18]
     add rax, 0x40
     mov [rsp-0x18], rax
     jmp :83

083: mov rax, 0x14
     cbw
     idiv r9b
     movsx rcx, al
     mov rax, [rsp-0x18]
     add rax, rcx
     mov [rsp-0x18], rax
     mov rax, 0x14
     cbw
     idiv r9b
     movzx edx, ah
     movsx rcx, dl
     mov rax, [rsp-0x18]
     add rax, rcx
     mov [rsp-0x18], rax
     mov rax, 0x00
     sub rax, 0x07
     mov [rsp-0x10], rax
     mov rax, [rsp-0x10]
     cbw
     idiv r9b
     movsx rcx, al
     mov rax, [rsp-0x18]
     add rax, rcx
     mov [rsp-0x18], rax
     mov rax, [rsp-0x10]
     cbw
     idiv r9b
     movzx edx, ah
     movsx rcx, dl
     mov rax, [rsp-0x18]
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x78]
     movsx rcx, al
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x70]
     movsx rcx, al
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x68]
     movsx rcx, al
     mov rax, rdx
     add rax, rcx
     movsx rcx, sil
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x60]
     movsx rcx, al
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, 0x14
     sar rax, 0x02
     movsx rcx, al
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x10]
     sar rax, 0x02
     movsx rcx, al
     mov rax, rdx
     add rax, rcx
     mov rsi, rax
     mov rax, 0x14
     cmp al, 0x06
     jle :84
     jmp :85

084: jmp :86

085: mov rax, rsi
     add rax, 0x01
     mov rsi, rax
     jmp :86

086: cmp r9b, 0x14
     jnl :87
     jmp :88

087: jmp :89

088: mov rax, rsi
     add rax, 0x02
     mov rsi, rax
     jmp :89

089: mov rax, 0x14
     cmp al, 0x06
     jl :90
     jmp :91

090: jmp :92

091: mov rax, rsi
     add rax, 0x04
     mov rsi, rax
     jmp :92

092: mov rax, 0x14
     cmp al, 0x06
     jnle :93
     jmp :94

093: jmp :95

094: mov rax, rsi
     add rax, 0x08
     mov rsi, rax
     jmp :95

095: test r10, r10
     jz :96
     jmp :97

096: jmp :98

097: mov rax, rsi
     add rax, 0x10
     mov rsi, rax
     jmp :98

098: test r11, r11
     jz :99
     jmp :100

099: jmp :101

100: mov rax, rsi
     add rax, 0x20
     mov rsi, rax
     jmp :101

101: mov rax, [rsp-0x10]
     cmp al, 0x06
     jnl :102
     jmp :103

102: jmp :104

103: mov rax, rsi
     add rax, 0x40
     mov rsi, rax
     jmp :104

104: mov rax, 0x14
     cwd
     idiv bx
     movsx rcx, ax
     mov rax, rsi
     add rax, rcx
     mov rsi, rax
     mov rax, 0x14
     cwd
     idiv bx
     movsx rcx, dx
     mov rax, rsi
     add rax, rcx
     mov rsi, rax
     mov rax, 0x00
     sub rax, 0x07
     mov r9, rax
     mov rax, r9
     cwd
     idiv bx
     movsx rcx, ax
     mov rax, rsi
     add rax, rcx
     mov rsi, rax
     mov rax, r9
     cwd
     idiv bx
     movsx rcx, dx
     mov rax, rsi
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x58]
     movsx rcx, ax
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x50]
     movsx rcx, ax
     mov rax, rdx
     add rax, rcx
     mov rdx, rax
     mov rax, [rsp-0x48]
     movsx rcx, ax
     mov rax, rdx
     add rax, rcx
     mov rcx, rax
     movsx rax, di
     add rcx, rax
     mov rax, rcx
     mov rcx, [rsp-0x40]
     movsx rcx, cx
     add rax, rcx
     mov rcx, 0x14
     sar rcx, 0x02
     movsx rcx, cx
     add rax, rcx
     mov rcx, r9
     sar rcx, 0x02
     movsx rcx, cx
     mov rdx, rax
     add rdx, rcx
     mov rax, 0x14
     cmp ax, 0x06
     jle :105
     jmp :106

105: jmp :107

106: add rdx, 0x01
     jmp :107

107: cmp bx, 0x14
     jnl :108
     jmp :109

108: jmp :110

109: add rdx, 0x02
     jmp :110

110: mov rcx, rdx
     mov rax, 0x14
     cmp ax, 0x06
     jl :111
     jmp :112

111: jmp :113

112: add rcx, 0x04
     jmp :113

113: mov rax, 0x14
     cmp ax, 0x06
     jnle :114
     jmp :115

114: jmp :116

115: add rcx, 0x08
     jmp :116

116: test rbp, rbp
     jz :117
     jmp :118

117: jmp :119

118: add rcx, 0x10
     jmp :119

119: test r12, r12
     jz :120
     jmp :121

120: jmp :122

121: add rcx, 0x20
     jmp :122

122: mov rax, r9
     cmp ax, 0x06
     jnl :123
     jmp :124

123: jmp :125

124: add rcx, 0x40
     jmp :125

125: mov rax, 0x14
     cdq
     idiv r13d
     movsxd rax, eax
     add rcx, rax
     mov rax, 0x14
     cdq
     idiv r13d
     movsxd rax, edx
     add rcx, rax
     mov rax, 0x00
     sub rax, 0x07
     mov rsi, rax
     mov rax, rsi
     cdq
     idiv r13d
     movsxd rax, eax
     add rcx, rax
     mov rax, rsi
     cdq
     idiv r13d
     movsxd rax, edx
     add rcx, rax
     mov rax, [rsp-0x38]
     movsxd rax, eax
     add rcx, rax
     mov rax, [rsp-0x30]
     movsxd rax, eax
     add rcx, rax
     mov rax, [rsp-0x28]
     movsxd rax, eax
     add rcx, rax
     movsxd rax, r8d
     add rcx, rax
     mov rax, [rsp-0x20]
     movsxd rax, eax
     add rcx, rax
     mov rax, 0x14
     sar rax, 0x02
     movsxd rax, eax
     add rcx, rax
     mov rax, rsi
     sar rax, 0x02
     movsxd rax, eax
     add rcx, rax
     mov rax, 0x14
     cmp eax, 0x06
     jle :126
     jmp :127

126: jmp :128

127: add rcx, 0x01
     jmp :128

128: cmp r13d, 0x14
     jnl :129
     jmp :130

129: jmp :131

130: add rcx, 0x02
     jmp :131

131: mov rax, 0x14
     cmp eax, 0x06
     jl :132
     jmp :133

132: jmp :134

133: add rcx, 0x04
     jmp :134

134: mov rax, 0x14
     cmp eax, 0x06
     jnle :135
     jmp :136

135: jmp :137

136: add rcx, 0x08
     jmp :137

137: test r14, r14
     jz :138
     jmp :139

138: jmp :140

139: add rcx, 0x10
     jmp :140

140: test r15, r15
     jz :141
     jmp :142

141: jmp :143

142: add rcx, 0x20
     jmp :143

143: mov rax, rsi
     cmp eax, 0x06
     jnl :144
     jmp :145

144: jmp :146

145: add rcx, 0x40
     jmp :146

146: mov rax, rcx
     pop r15
     pop r14
     pop r13
     pop r12
     pop rbp
     pop rbx
     ret
=========== OPT LEVEL: mininal ===========
main:
     mov rax, 0x00
     mov rcx, 0x01
     mov rdx, 0x00
     add rdx, 0x03
     add rdx, 0x02
     add rdx, 0xFFFFFFFFFFFFFFFF
     add rdx, 0xFFFFFFFFFFFFFFFF
     add rdx, 0x04
     add rdx, 0x16
     add rdx, 0x12
     add rdx, 0x10
     add rdx, 0x50
     add rdx, 0x05
     add rdx, 0xFFFFFFFFFFFFFFFE
     test rcx, rcx
     jz :0
     jmp :1

000: jmp :2

001: add rdx, 0x01
     jmp :2

002: test rcx, rcx
     jz :3
     jmp :4

003: jmp :5

004: add rdx, 0x02
     jmp :5

005: test rcx, rcx
     jz :6
     jmp :7

006: jmp :8

007: add rdx, 0x04
     jmp :8

008: test rax, rax
     jz :9
     jmp :10

009: mov rax, rdx
     jmp :11

010: mov rax, rdx
     add rax, 0x08
     jmp :11

011: test rcx, rcx
     jz :12
     jmp :13

012: jmp :14

013: add rax, 0x10
     jmp :14

014: test rcx, rcx
     jz :15
     jmp :16

015: jmp :17

016: add rax, 0x20
     jmp :17

017: test rcx, rcx
     jz :18
     jmp :19

018: jmp :20

019: add rax, 0x40
     jmp :20

020: mov rcx, 0x01
     add rax, 0x03
     add rax, 0x02
     add rax, 0x01
     add rax, 0x01
     add rax, 0x04
     add rax, 0x16
     add rax, 0x12
     add rax, 0x10
     add rax, 0x50
     add rax, 0x05
     add rax, 0x01
     test rcx, rcx
     jz :21
     jmp :22

021: jmp :23

022: add rax, 0x01
     jmp :23

023: test rcx, rcx
     jz :24
     jmp :25

024: jmp :26

025: add rax, 0x02
     jmp :26

026: test rcx, rcx
     jz :27
     jmp :28

027: jmp :29

028: add rax, 0x04
     jmp :29

029: mov rdx, 0x00
     test rdx, rdx
     jz :30
     jmp :31

030: jmp :32

031: add rax, 0x08
     jmp :32

032: test rcx, rcx
     jz :33
     jmp :34

033: jmp :35

034: add rax, 0x10
     jmp :35

035: test rcx, rcx
     jz :36
     jmp :37

036: jmp :38

037: add rax, 0x20
     jmp :38

038: test rdx, rdx
     jz :39
     jmp :40

039: jmp :41

040: add rax, 0x40
     jmp :41

041: mov rsi, 0x01
     add rax, 0x03
     add rax, 0x02
     add rax, 0x01
     add rax, 0x01
     add rax, 0x04
     add rax, 0x16
     add rax, 0x12
     add rax, 0x10
     add rax, 0x50
     add rax, 0x05
     add rax, 0x01
     test rsi, rsi
     jz :42
     jmp :43

042: jmp :44

043: add rax, 0x01
     jmp :44

044: test rsi, rsi
     jz :45
     jmp :46

045: jmp :47

046: add rax, 0x02
     jmp :47

047: test rsi, rsi
     jz :48
     jmp :49

048: jmp :50

049: add rax, 0x04
     jmp :50

050: mov rdi, 0x00
     test rdi, rdi
     jz :51
     jmp :52

051: jmp :53

052: add rax, 0x08
     jmp :53

053: test rsi, rsi
     jz :54
     jmp :55

054: jmp :56

055: add rax, 0x10
     jmp :56

056: test rsi, rsi
     jz :57
     jmp :58

057: jmp :59

058: add rax, 0x20
     jmp :59

059: test rdi, rdi
     jz :60
     jmp :61

060: jmp :62

061: add rax, 0x40
     jmp :62

062: mov r8, 0x01
     add rax, 0x03
     add rax, 0x02
     add rax, 0x01
     add rax, 0x01
     add rax, 0x04
     add rax, 0x16
     add rax, 0x12
     add rax, 0x10
     add rax, 0x50
     add rax, 0x05
     add rax, 0x01
     test r8, r8
     jz :63
     jmp :64

063: jmp :65

064: add rax, 0x01
     jmp :65

065: test r8, r8
     jz :66
     jmp :67

066: jmp :68

067: add rax, 0x02
     jmp :68

068: test r8, r8
     jz :69
     jmp :70

069: jmp :71

070: add rax, 0x04
     jmp :71

071: mov r9, 0x00
     test r9, r9
     jz :72
     jmp :73

072: jmp :74

073: add rax, 0x08
     jmp :74

074: test r8, r8
     jz :75
     jmp :76

075: jmp :77

076: add rax, 0x10
     jmp :77

077: test r8, r8
     jz :78
     jmp :79

078: jmp :80

079: add rax, 0x20
     jmp :80

080: test r9, r9
     jz :81
     jmp :82

081: jmp :83

082: add rax, 0x40
     jmp :83

083: add rax, 0x03
     add rax, 0x02
     add rax, 0xFFFFFFFFFFFFFFFF
     add rax, 0xFFFFFFFFFFFFFFFF
     add rax, 0x04
     add rax, 0x16
     add rax, 0x12
     add rax, 0x10
     add rax, 0x50
     add rax, 0x05
     add rax, 0xFFFFFFFFFFFFFFFE
     test rcx, rcx
     jz :84
     jmp :85

084: jmp :86

085: add rax, 0x01
     jmp :86

086: test rcx, rcx
     jz :87
     jmp :88

087: jmp :89

088: add rax, 0x02
     jmp :89

089: test rcx, rcx
     jz :90
     jmp :91

090: jmp :92

091: add rax, 0x04
     jmp :92

092: test rdx, rdx
     jz :93
     jmp :94

093: jmp :95

094: add rax, 0x08
     jmp :95

095: test rcx, rcx
     jz :96
     jmp :97

096: jmp :98

097: add rax, 0x10
     jmp :98

098: test rcx, rcx
     jz :99
     jmp :100

099: jmp :101

100: add rax, 0x20
     jmp :101

101: test rcx, rcx
     jz :102
     jmp :103

102: jmp :104

103: add rax, 0x40
     jmp :104

104: add rax, 0x03
     add rax, 0x02
     add rax, 0xFFFFFFFFFFFFFFFF
     add rax, 0xFFFFFFFFFFFFFFFF
     add rax, 0x04
     add rax, 0x16
     add rax, 0x12
     add rax, 0x10
     add rax, 0x50
     add rax, 0x05
     add rax, 0xFFFFFFFFFFFFFFFE
     test rsi, rsi
     jz :105
     jmp :106

105: jmp :107

106: add rax, 0x01
     jmp :107

107: test rsi, rsi
     jz :108
     jmp :109

108: jmp :110

109: add rax, 0x02
     jmp :110

110: test rsi, rsi
     jz :111
     jmp :112

111: jmp :113

112: add rax, 0x04
     jmp :113

113: test rdi, rdi
     jz :114
     jmp :115

114: jmp :116

115: add rax, 0x08
     jmp :116

116: test rsi, rsi
     jz :117
     jmp :118

117: jmp :119

118: add rax, 0x10
     jmp :119

119: test rsi, rsi
     jz :120
     jmp :121

120: jmp :122

121: add rax, 0x20
     jmp :122

122: test rsi, rsi
     jz :123
     jmp :124

123: jmp :125

124: add rax, 0x40
     jmp :125

125: add rax, 0x03
     add rax, 0x02
     add rax, 0xFFFFFFFFFFFFFFFF
     add rax, 0xFFFFFFFFFFFFFFFF
     add rax, 0x04
     add rax, 0x16
     add rax, 0x12
     add rax, 0x10
     add rax, 0x50
     add rax, 0x05
     add rax, 0xFFFFFFFFFFFFFFFE
     test r8, r8
     jz :126
     jmp :127

126: jmp :128

127: add rax, 0x01
     jmp :128

128: test r8, r8
     jz :129
     jmp :130

129: jmp :131

130: add rax, 0x02
     jmp :131

131: test r8, r8
     jz :132
     jmp :133

132: jmp :134

133: add rax, 0x04
     jmp :134

134: test r9, r9
     jz :135
     jmp :136

135: jmp :137

136: add rax, 0x08
     jmp :137

137: test r8, r8
     jz :138
     jmp :139

138: jmp :140

139: add rax, 0x10
     jmp :140

140: test r8, r8
     jz :141
     jmp :142

141: jmp :143

142: add rax, 0x20
     jmp :143

143: test r8, r8
     jz :144
     jmp :145

144: jmp :146

145: add rax, 0x40
     jmp :146

146: ret
=========== OPT LEVEL: all ===========
main:
     mov rax, 0x694
     ret
