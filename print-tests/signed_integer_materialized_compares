=========== OPT LEVEL: none ===========
s8:
    xor ecx, ecx
s64:
    cmp dil, sil
    setz al
    movzx rax, al
    movzx eax, al
    add rcx, rax
    cmp dil, sil
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    cmp dil, sil
    setnge al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    cmp dil, sil
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    cmp dil, sil
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp dil, sil
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
6:
    xor ecx, ecx
    cmp di, si
    setz al
    movzx rax, al
    movzx eax, al
    add rcx, rax
    cmp di, si
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    cmp di, si
    setnge al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    cmp di, si
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    cmp di, si
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp di, si
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
7:
    xor ecx, ecx
    cmp edi, esi
    setz al
    movzx rax, al
    movzx eax, al
    add rcx, rax
    cmp edi, esi
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    cmp edi, esi
    setnge al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    cmp edi, esi
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    cmp edi, esi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp edi, esi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
8:
    xor ecx, ecx
    cmp rdi, rsi
    setz al
    movzx rax, al
    movzx eax, al
    add rcx, rax
    cmp rdi, rsi
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    cmp rdi, rsi
    setnge al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    cmp rdi, rsi
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    cmp rdi, rsi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp rdi, rsi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
9:
    xor ecx, ecx
    cmp rdi, rsi
    setz al
    movzx rax, al
    movzx eax, al
    add rcx, rax
    cmp rdi, rsi
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    cmp rdi, rsi
    setnge al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    cmp rdi, rsi
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    cmp rdi, rsi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp rdi, rsi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
10:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 0x48
    mov dil, -1
    mov sil, 1
    call s8
    mov.q [rsp + 0x38], rax
    mov sil, 5
    mov dil, 5
    call s8
    mov.q [rsp + 0x30], rax
    mov dil, 7
    mov sil, -3
    call s8
    mov.q [rsp + 0x28], rax
    mov rdi, -1
    mov rsi, 1
    call 6
    mov.q [rsp + 0x20], rax
    mov rsi, 5
    mov rdi, 5
    call 6
    mov.q [rsp + 0x18], rax
    mov rdi, 7
    mov rsi, -3
    call 6
    mov.q [rsp + 0x10], rax
    mov rdi, -1
    mov rsi, 1
    call 7
    mov.q [rsp + 0x8], rax
    mov rsi, 5
    mov rdi, 5
    call 7
    mov.q [rsp], rax
    mov rdi, 7
    mov rsi, -3
    call 7
    mov r15, rax
    mov rdi, -1
    mov rsi, 1
    call 8
    mov r14, rax
    mov rsi, 5
    mov rdi, 5
    call 8
    mov r13, rax
    mov rdi, 7
    mov rsi, -3
    call 8
    mov r12, rax
    mov rdi, -1
    mov rsi, 1
    call 9
    mov rbp, rax
    mov rsi, 5
    mov rdi, 5
    call 9
    mov rbx, rax
    mov rdi, 7
    mov rsi, -3
    call 9
    mov rsi, rax
    mov.q rax, [rsp + 0x38]
    mov.q rcx, [rsp + 0x30]
    add rax, rcx
    mov rcx, rax
    mov.q rax, [rsp + 0x28]
    add rcx, rax
    xor eax, eax
    add rax, rcx
    mov rdx, rax
    mov.q rcx, [rsp + 0x20]
    mov.q rax, [rsp + 0x18]
    add rcx, rax
    mov rax, rcx
    mov.q rcx, [rsp + 0x10]
    add rax, rcx
    add rdx, rax
    mov.q rax, [rsp + 0x8]
    mov.q rcx, [rsp]
    add rax, rcx
    add rax, r15
    add rdx, rax
    mov rcx, r14
    add rcx, r13
    add rcx, r12
    mov rax, rdx
    add rax, rcx
    mov rcx, rbp
    add rcx, rbx
    add rcx, rsi
    add rax, rcx
    add rsp, 0x48
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
s8:
    xor ecx, ecx
s64:
    cmp dil, sil
    setz al
    movzx rax, al
    movzx eax, al
    add rcx, rax
    cmp dil, sil
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    cmp dil, sil
    setnge al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    cmp dil, sil
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    cmp dil, sil
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp dil, sil
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
6:
    xor ecx, ecx
    cmp di, si
    setz al
    movzx rax, al
    movzx eax, al
    add rcx, rax
    cmp di, si
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    cmp di, si
    setnge al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    cmp di, si
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    cmp di, si
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp di, si
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
7:
    xor ecx, ecx
    cmp edi, esi
    setz al
    movzx rax, al
    movzx eax, al
    add rcx, rax
    cmp edi, esi
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    cmp edi, esi
    setnge al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    cmp edi, esi
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    cmp edi, esi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp edi, esi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
8:
    xor ecx, ecx
    cmp rdi, rsi
    setz al
    movzx rax, al
    movzx eax, al
    add rcx, rax
    cmp rdi, rsi
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    cmp rdi, rsi
    setnge al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    cmp rdi, rsi
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    cmp rdi, rsi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp rdi, rsi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
9:
    xor ecx, ecx
    cmp rdi, rsi
    setz al
    movzx rax, al
    movzx eax, al
    add rcx, rax
    cmp rdi, rsi
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    cmp rdi, rsi
    setnge al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    cmp rdi, rsi
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    cmp rdi, rsi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp rdi, rsi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
10:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 0x48
    mov dil, -1
    mov sil, 1
    call s8
    mov.q [rsp + 0x38], rax
    mov sil, 5
    mov dil, 5
    call s8
    mov.q [rsp + 0x30], rax
    mov dil, 7
    mov sil, -3
    call s8
    mov.q [rsp + 0x28], rax
    mov rdi, -1
    mov rsi, 1
    call 6
    mov.q [rsp + 0x20], rax
    mov rsi, 5
    mov rdi, 5
    call 6
    mov.q [rsp + 0x18], rax
    mov rdi, 7
    mov rsi, -3
    call 6
    mov.q [rsp + 0x10], rax
    mov rdi, -1
    mov rsi, 1
    call 7
    mov.q [rsp + 0x8], rax
    mov rsi, 5
    mov rdi, 5
    call 7
    mov.q [rsp], rax
    mov rdi, 7
    mov rsi, -3
    call 7
    mov r15, rax
    mov rdi, -1
    mov rsi, 1
    call 8
    mov r14, rax
    mov rsi, 5
    mov rdi, 5
    call 8
    mov r13, rax
    mov rdi, 7
    mov rsi, -3
    call 8
    mov r12, rax
    mov rdi, -1
    mov rsi, 1
    call 9
    mov rbp, rax
    mov rsi, 5
    mov rdi, 5
    call 9
    mov rbx, rax
    mov rdi, 7
    mov rsi, -3
    call 9
    mov rsi, rax
    mov.q rax, [rsp + 0x38]
    mov.q rcx, [rsp + 0x30]
    add rax, rcx
    mov rcx, rax
    mov.q rax, [rsp + 0x28]
    add rcx, rax
    xor eax, eax
    add rax, rcx
    mov rdx, rax
    mov.q rcx, [rsp + 0x20]
    mov.q rax, [rsp + 0x18]
    add rcx, rax
    mov rax, rcx
    mov.q rcx, [rsp + 0x10]
    add rax, rcx
    add rdx, rax
    mov.q rax, [rsp + 0x8]
    mov.q rcx, [rsp]
    add rax, rcx
    add rax, r15
    add rdx, rax
    mov rcx, r14
    add rcx, r13
    add rcx, r12
    mov rax, rdx
    add rax, rcx
    mov rcx, rbp
    add rcx, rbx
    add rcx, rsi
    add rax, rcx
    add rsp, 0x48
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
s8:
    cmp dil, sil
s64:
    setz al
    movzx rax, al
    movzx ecx, al
    cmp dil, sil
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    cmp dil, sil
    setnge al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    cmp dil, sil
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    cmp dil, sil
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp dil, sil
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
6:
    cmp di, si
    setz al
    movzx rax, al
    movzx ecx, al
    cmp di, si
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    cmp di, si
    setnge al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    cmp di, si
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    cmp di, si
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp di, si
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
7:
    cmp edi, esi
    setz al
    movzx rax, al
    movzx ecx, al
    cmp edi, esi
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    cmp edi, esi
    setnge al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    cmp edi, esi
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    cmp edi, esi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp edi, esi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
8:
    cmp rdi, rsi
    setz al
    movzx rax, al
    movzx ecx, al
    cmp rdi, rsi
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    cmp rdi, rsi
    setnge al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    cmp rdi, rsi
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    cmp rdi, rsi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp rdi, rsi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
9:
    cmp rdi, rsi
    setz al
    movzx rax, al
    movzx ecx, al
    cmp rdi, rsi
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    cmp rdi, rsi
    setnge al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    cmp rdi, rsi
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    cmp rdi, rsi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp rdi, rsi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
10:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 0x48
    mov dil, -1
    mov sil, 1
    call s8
    mov.q [rsp + 0x38], rax
    mov sil, 5
    mov dil, 5
    call s8
    mov.q [rsp + 0x30], rax
    mov dil, 7
    mov sil, -3
    call s8
    mov.q [rsp + 0x28], rax
    mov rdi, -1
    mov rsi, 1
    call 6
    mov.q [rsp + 0x20], rax
    mov rsi, 5
    mov rdi, 5
    call 6
    mov.q [rsp + 0x18], rax
    mov rdi, 7
    mov rsi, -3
    call 6
    mov.q [rsp + 0x10], rax
    mov rdi, -1
    mov rsi, 1
    call 7
    mov.q [rsp + 0x8], rax
    mov rsi, 5
    mov rdi, 5
    call 7
    mov.q [rsp], rax
    mov rdi, 7
    mov rsi, -3
    call 7
    mov r15, rax
    mov rdi, -1
    mov rsi, 1
    call 8
    mov r14, rax
    mov rsi, 5
    mov rdi, 5
    call 8
    mov r13, rax
    mov rdi, 7
    mov rsi, -3
    call 8
    mov r12, rax
    mov rdi, -1
    mov rsi, 1
    call 9
    mov rbp, rax
    mov rsi, 5
    mov rdi, 5
    call 9
    mov rbx, rax
    mov rdi, 7
    mov rsi, -3
    call 9
    mov rsi, rax
    mov.q rax, [rsp + 0x38]
    mov.q rcx, [rsp + 0x30]
    add rax, rcx
    mov rcx, rax
    mov.q rax, [rsp + 0x28]
    add rcx, rax
    mov rdx, rcx
    mov.q rcx, [rsp + 0x20]
    mov.q rax, [rsp + 0x18]
    add rcx, rax
    mov rax, rcx
    mov.q rcx, [rsp + 0x10]
    add rax, rcx
    add rdx, rax
    mov.q rax, [rsp + 0x8]
    mov.q rcx, [rsp]
    add rax, rcx
    add rax, r15
    add rdx, rax
    mov rax, r14
    add rax, r13
    add rax, r12
    mov rcx, rdx
    add rcx, rax
    mov rax, rbp
    add rax, rbx
    add rax, rsi
    add rcx, rax
    mov rax, rcx
    add rsp, 0x48
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: all ===========
s8:
    cmp dil, sil
s64:
    setz al
    movzx rax, al
    movzx ecx, al
    cmp dil, sil
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    cmp dil, sil
    setnge al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    cmp dil, sil
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    cmp dil, sil
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp dil, sil
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
6:
    cmp di, si
    setz al
    movzx rax, al
    movzx ecx, al
    cmp di, si
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    cmp di, si
    setnge al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    cmp di, si
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    cmp di, si
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp di, si
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
7:
    cmp edi, esi
    setz al
    movzx rax, al
    movzx ecx, al
    cmp edi, esi
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    cmp edi, esi
    setnge al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    cmp edi, esi
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    cmp edi, esi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp edi, esi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
8:
    cmp rdi, rsi
    setz al
    movzx rax, al
    movzx ecx, al
    cmp rdi, rsi
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    cmp rdi, rsi
    setnge al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    cmp rdi, rsi
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    cmp rdi, rsi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp rdi, rsi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
9:
    cmp rdi, rsi
    setz al
    movzx rax, al
    movzx ecx, al
    cmp rdi, rsi
    setnz al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*2]
    cmp rdi, rsi
    setnge al
    movzx rax, al
    movzx eax, al
    lea rcx, [rcx + rax*4]
    cmp rdi, rsi
    setng al
    movzx rax, al
    movzx eax, al
    lea rax, [rcx + rax*8]
    cmp rdi, rsi
    setnle cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x10
    add rax, rcx
    cmp rdi, rsi
    setnl cl
    movzx rcx, cl
    movzx ecx, cl
    imul rcx, rcx, 0x20
    add rax, rcx
    ret
10:
    push rbx
    push rbp
    push r12
    push r13
    push r14
    push r15
    sub rsp, 0x48
    mov dil, -1
    mov sil, 1
    call s8
    mov.q [rsp + 0x38], rax
    mov sil, 5
    mov dil, 5
    call s8
    mov.q [rsp + 0x30], rax
    mov dil, 7
    mov sil, -3
    call s8
    mov.q [rsp + 0x28], rax
    mov rdi, -1
    mov rsi, 1
    call 6
    mov.q [rsp + 0x20], rax
    mov rsi, 5
    mov rdi, 5
    call 6
    mov.q [rsp + 0x18], rax
    mov rdi, 7
    mov rsi, -3
    call 6
    mov.q [rsp + 0x10], rax
    mov rdi, -1
    mov rsi, 1
    call 7
    mov.q [rsp + 0x8], rax
    mov rsi, 5
    mov rdi, 5
    call 7
    mov.q [rsp], rax
    mov rdi, 7
    mov rsi, -3
    call 7
    mov r15, rax
    mov rdi, -1
    mov rsi, 1
    call 8
    mov r14, rax
    mov rsi, 5
    mov rdi, 5
    call 8
    mov r13, rax
    mov rdi, 7
    mov rsi, -3
    call 8
    mov r12, rax
    mov rdi, -1
    mov rsi, 1
    call 9
    mov rbp, rax
    mov rsi, 5
    mov rdi, 5
    call 9
    mov rbx, rax
    mov rdi, 7
    mov rsi, -3
    call 9
    mov rsi, rax
    mov.q rax, [rsp + 0x38]
    mov.q rcx, [rsp + 0x30]
    add rax, rcx
    mov rcx, rax
    mov.q rax, [rsp + 0x28]
    add rcx, rax
    mov rdx, rcx
    mov.q rcx, [rsp + 0x20]
    mov.q rax, [rsp + 0x18]
    add rcx, rax
    mov rax, rcx
    mov.q rcx, [rsp + 0x10]
    add rax, rcx
    add rdx, rax
    mov.q rax, [rsp + 0x8]
    mov.q rcx, [rsp]
    add rax, rcx
    add rax, r15
    add rdx, rax
    mov rax, r14
    add rax, r13
    add rax, r12
    mov rcx, rdx
    add rcx, rax
    mov rax, rbp
    add rax, rbx
    add rax, rsi
    add rcx, rax
    mov rax, rcx
    add rsp, 0x48
    pop r15
    pop r14
    pop r13
    pop r12
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
