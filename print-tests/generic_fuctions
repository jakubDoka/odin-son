=========== OPT LEVEL: none ===========
main:
    push rbx
fib i32:
    mov rdi, 0xa
    call 4
    mov rbx, rax
    mov rdi, 0xa
    call 7
    mov rcx, rax
    movsxd rax, ebx
    add rax, rcx
    pop rbx
    ret
4:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp ebx, 2
    jng 5
    mov rax, rbx
    sub eax, 1
    mov rdi, rax
    call 4
    mov rbp, rax
    sub ebx, 2
    mov rdi, rbx
    call 4
    add ebp, eax
    jmp 6
5:
    mov rbp, 1
6:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
7:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jng 8
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call 7
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call 7
    add rbp, rax
    jmp 9
8:
    mov rbp, 1
9:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: mininal ===========
main:
    push rbx
fib i32:
    mov rdi, 0xa
    call 4
    mov rbx, rax
    mov rdi, 0xa
    call 7
    mov rcx, rax
    movsxd rax, ebx
    add rax, rcx
    pop rbx
    ret
4:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp ebx, 2
    jng 5
    mov rax, rbx
    sub eax, 1
    mov rdi, rax
    call 4
    mov rbp, rax
    sub ebx, 2
    mov rdi, rbx
    call 4
    add ebp, eax
    jmp 6
5:
    mov rbp, 1
6:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
7:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jng 8
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call 7
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call 7
    add rbp, rax
    jmp 9
8:
    mov rbp, 1
9:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: moderate ===========
main:
    push rbx
fib i32:
    mov rdi, 0xa
    call 4
    mov rbx, rax
    mov rdi, 0xa
    call 7
    mov rcx, rax
    movsxd rax, ebx
    add rax, rcx
    pop rbx
    ret
4:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp ebx, 2
    jng 5
    mov rax, rbx
    sub eax, 1
    mov rdi, rax
    call 4
    mov rbp, rax
    sub ebx, 2
    mov rdi, rbx
    call 4
    add ebp, eax
    jmp 6
5:
    mov rbp, 1
6:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
7:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jng 8
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call 7
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call 7
    add rbp, rax
    jmp 9
8:
    mov rbp, 1
9:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: all ===========
main:
    push rbx
fib i32:
    mov rdi, 0xa
    call 4
    mov rbx, rax
    mov rdi, 0xa
    call 7
    mov rcx, rax
    movsxd rax, ebx
    add rax, rcx
    pop rbx
    ret
4:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp ebx, 2
    jng 5
    mov rax, rbx
    sub eax, 1
    mov rdi, rax
    call 4
    mov rbp, rax
    sub ebx, 2
    mov rdi, rbx
    call 4
    add ebp, eax
    jmp 6
5:
    mov rbp, 1
6:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
7:
    push rbx
    push rbp
    sub rsp, 8
    mov rbx, rdi
    cmp rbx, 2
    jng 8
    mov rax, rbx
    sub rax, 1
    mov rdi, rax
    call 7
    mov rbp, rax
    sub rbx, 2
    mov rdi, rbx
    call 7
    add rbp, rax
    jmp 9
8:
    mov rbp, 1
9:
    mov rax, rbp
    add rsp, 8
    pop rbp
    pop rbx
    ret
=========== OPT LEVEL: aggresive ===========
