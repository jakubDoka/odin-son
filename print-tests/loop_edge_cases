=========== OPT LEVEL: none ===========
main:
     xor eax, eax
     xor ecx, ecx
002: add rcx, 0x01
     cmp rcx, 0x02
     jz :0
     cmp rcx, 0x01
     jz :1
     add rcx, 0x01
     mov rax, 0x01
     jmp :1

001: jmp :2

000: cmp rax, 0x00
     jz :3
     jmp :4

003: xor ecx, ecx
006: add rcx, 0x01
     cmp rcx, 0x03
     jz :4
     cmp rcx, 0x02
     jz :5
     cmp rcx, 0x01
     jz :5
     add rcx, 0x01
     mov rax, 0x02
     jmp :5

005: jmp :6

004: cmp rax, 0x00
     jnz :7
     jmp :8

007: xor eax, eax
011: add rax, 0x01
     cmp rax, 0x03
     jz :9
     add rax, 0x01
     cmp rax, 0x04
     jz :10
     jmp :11

010: mov rax, 0x03
     jmp :8

009: mov rax, 0x01
008: ret
=========== OPT LEVEL: mininal ===========
main:
     xor eax, eax
     xor ecx, ecx
002: add rcx, 0x01
     cmp rcx, 0x02
     jz :0
     cmp rcx, 0x01
     jz :1
     add rcx, 0x01
     mov rax, 0x01
     jmp :1

001: jmp :2

000: cmp rax, 0x00
     jz :3
     jmp :4

003: xor ecx, ecx
006: add rcx, 0x01
     cmp rcx, 0x03
     jz :4
     cmp rcx, 0x02
     jz :5
     cmp rcx, 0x01
     jz :5
     add rcx, 0x01
     mov rax, 0x02
     jmp :5

005: jmp :6

004: test rax, rax
     jnz :7
     jmp :8

007: xor eax, eax
011: add rax, 0x01
     cmp rax, 0x03
     jz :9
     add rax, 0x01
     cmp rax, 0x04
     jz :10
     jmp :11

010: mov rax, 0x03
     jmp :8

009: mov rax, 0x01
008: ret
=========== OPT LEVEL: moderate ===========
main:
     xor ecx, ecx
     xor edx, edx
003: mov rax, rcx
     add rax, 0x01
     cmp rax, 0x02
     jz :0
     cmp rax, 0x01
     jz :1
     add rcx, 0x02
     mov rdx, 0x01
     jmp :2

001: mov rcx, rax
002: jmp :3

000: cmp rdx, 0x00
     jz :4
     jmp :5

004: xor ecx, ecx
008: mov rax, rcx
     add rax, 0x01
     cmp rax, 0x03
     jz :5
     cmp rax, 0x02
     jz :6
     cmp rax, 0x01
     jz :6
     add rcx, 0x02
     mov rdx, 0x02
     jmp :7

006: mov rcx, rax
007: jmp :8

005: test rdx, rdx
     jnz :9
     mov rax, rdx
     jmp :10

009: xor ecx, ecx
013: mov rax, rcx
     add rax, 0x01
     cmp rax, 0x03
     jz :11
     add rcx, 0x02
     cmp rcx, 0x04
     jz :12
     jmp :13

012: mov rax, 0x03
     jmp :10

011: mov rax, 0x01
010: ret
=========== OPT LEVEL: all ===========
main:
     xor ecx, ecx
     xor edx, edx
003: mov rax, rcx
     add rax, 0x01
     cmp rax, 0x02
     jz :0
     cmp rax, 0x01
     jz :1
     add rcx, 0x02
     mov rdx, 0x01
     jmp :2

001: mov rcx, rax
002: jmp :3

000: cmp rdx, 0x00
     jz :4
     jmp :5

004: xor ecx, ecx
008: mov rax, rcx
     add rax, 0x01
     cmp rax, 0x03
     jz :5
     cmp rax, 0x02
     jz :6
     cmp rax, 0x01
     jz :6
     add rcx, 0x02
     mov rdx, 0x02
     jmp :7

006: mov rcx, rax
007: jmp :8

005: test rdx, rdx
     jnz :9
     mov rax, rdx
     jmp :10

009: xor ecx, ecx
013: mov rax, rcx
     add rax, 0x01
     cmp rax, 0x03
     jz :11
     add rcx, 0x02
     cmp rcx, 0x04
     jz :12
     jmp :13

012: mov rax, 0x03
     jmp :10

011: mov rax, 0x01
010: ret
=========== OPT LEVEL: aggresive ===========
