=========== OPT LEVEL: none ===========
main:
    sub rsp, 0x48
    lea rdi, [rsp]
    xor esi, esi
    mov rdx, 0x40
    call.q [rip - 0x20]
    xor eax, eax
    xor ecx, ecx
1:
    cmp rcx, 8
    jnge 2
    add rsp, 0x48
    ret
2:
    mov.q [rsp + rcx*8], rcx
    add rcx, 1
    jmp 1
=========== OPT LEVEL: mininal ===========
main:
    sub rsp, 0x48
    lea rdi, [rsp]
    xor esi, esi
    mov rdx, 0x40
    call.q [rip - 0x20]
    xor eax, eax
    xor ecx, ecx
1:
    cmp rcx, 8
    jnge 2
    add rsp, 0x48
    ret
2:
    mov.q [rsp + rcx*8], rcx
    add rcx, 1
    jmp 1
=========== OPT LEVEL: moderate ===========
main:
    sub rsp, 0x48
    pxor xmm0, xmm0
    movups.x [rsp + 0x10], xmm0
    movups.x [rsp], xmm0
    movups.x [rsp + 0x30], xmm0
    movups.x [rsp + 0x20], xmm0
    xor eax, eax
    xor ecx, ecx
1:
    cmp rcx, 8
    jnge 2
    add rsp, 0x48
    ret
2:
    mov.q [rsp + rcx*8], rcx
    add rcx, 1
    jmp 1
=========== OPT LEVEL: all ===========
main:
    sub rsp, 0x48
    pxor xmm0, xmm0
    movups.x [rsp + 0x10], xmm0
    movups.x [rsp], xmm0
    movups.x [rsp + 0x30], xmm0
    movups.x [rsp + 0x20], xmm0
    xor eax, eax
    xor ecx, ecx
1:
    mov.q [rsp + rcx*8], rcx
    add rcx, 1
    cmp rcx, 8
    jnge 1
    add rsp, 0x48
    ret
=========== OPT LEVEL: aggresive ===========
