=========== OPT LEVEL: none ===========
apply:
    mov rcx, rsi
    cmp rdi, 1
    jz 1
    jmp 2
1:
    mov rax, 0x64
    sub rax, rcx
    mov rcx, rax
2:
    mov rax, rcx
    ret
main:
    sub rsp, 8
    mov rdi, 1
    mov rsi, 9
    call apply
    add rsp, 8
    ret
=========== OPT LEVEL: mininal ===========
apply:
    mov rcx, rsi
    cmp rdi, 1
    jz 1
    jmp 2
1:
    mov rax, 0x64
    sub rax, rcx
    mov rcx, rax
2:
    mov rax, rcx
    ret
main:
    sub rsp, 8
    mov rdi, 1
    mov rsi, 9
    call apply
    add rsp, 8
    ret
=========== OPT LEVEL: moderate ===========
apply:
    mov rcx, rsi
    cmp rdi, 1
    jz 1
    jmp 2
1:
    mov rax, 0x64
    sub rax, rcx
    mov rcx, rax
2:
    mov rax, rcx
    ret
main:
    sub rsp, 8
    mov rdi, 1
    mov rsi, 9
    call apply
    add rsp, 8
    ret
=========== OPT LEVEL: all ===========
apply:
    mov rcx, rsi
    cmp rdi, 1
    jz 1
    jmp 2
1:
    mov rax, 0x64
    sub rax, rcx
    mov rcx, rax
2:
    mov rax, rcx
    ret
main:
    sub rsp, 8
    mov rdi, 1
    mov rsi, 9
    call apply
    add rsp, 8
    ret
=========== OPT LEVEL: aggresive ===========
main:
    mov rax, 0x5b
    ret
