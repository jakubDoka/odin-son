=========== OPT LEVEL: none ===========
main:
    mov rcx, 0xa
    xor esi, esi
    mov rax, 1
1:
    cmp rcx, 0
    jz 2
    sub rcx, 1
    mov rdx, rsi
    add rdx, rax
    mov rsi, rax
    mov rax, rdx
    jmp 1
2:
    ret
=========== OPT LEVEL: mininal ===========
main:
    mov rcx, 0xa
    xor esi, esi
    mov rax, 1
1:
    cmp rcx, 0
    jz 2
    sub rcx, 1
    mov rdx, rsi
    add rdx, rax
    mov rsi, rax
    mov rax, rdx
    jmp 1
2:
    ret
=========== OPT LEVEL: moderate ===========
main:
    mov rcx, 0xa
    xor esi, esi
    mov rax, 1
1:
    cmp rcx, 0
    jz 2
    sub rcx, 1
    mov rdx, rsi
    add rdx, rax
    mov rsi, rax
    mov rax, rdx
    jmp 1
2:
    ret
=========== OPT LEVEL: all ===========
main:
    mov rcx, 0xa
    xor esi, esi
    mov rax, 1
1:
    cmp rcx, 0
    jz 2
    sub rcx, 1
    mov rdx, rsi
    add rdx, rax
    mov rsi, rax
    mov rax, rdx
    jmp 1
2:
    ret
=========== OPT LEVEL: aggresive ===========
