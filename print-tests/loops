=========== OPT LEVEL: none ===========
main:
     push rbx
     mov rcx, 0x0A
     mov rbx, 0x00
     mov rax, 0x01
     jmp :0

000: cmp rcx, 0x00
     jnz :1
     jmp :2

001: mov rdx, rbx
     add rdx, rax
     sub rcx, 0x01
     mov rbx, rax
     mov rax, rdx
     jmp :0

002: pop rbx
     ret
=========== OPT LEVEL: mininal ===========
main:
     push rbx
     mov rcx, 0x0A
     mov rbx, 0x00
     mov rax, 0x01
     jmp :0

000: cmp rcx, 0x00
     jnz :1
     jmp :2

001: mov rdx, rbx
     add rdx, rax
     sub rcx, 0x01
     mov rbx, rax
     mov rax, rdx
     jmp :0

002: pop rbx
     ret
=========== OPT LEVEL: all ===========
main:
     push rbx
     mov rcx, 0x0A
     mov rbx, 0x00
     mov rax, 0x01
     jmp :0

000: cmp rcx, 0x00
     jnz :1
     jmp :2

001: mov rdx, rbx
     add rdx, rax
     sub rcx, 0x01
     mov rbx, rax
     mov rax, rdx
     jmp :0

002: pop rbx
     ret
