=========== OPT LEVEL: none ===========
main:
    xor esi, esi
    xor eax, eax
1:
    cmp rsi, 3
    jz 4
    mov rdx, rax
    xor ecx, ecx
2:
    cmp rcx, 3
    jz 3
    mov rax, rsi
    imul rax, rcx
    add rdx, rax
    add rcx, 1
    jmp 2
3:
    add rsi, 1
    mov rax, rdx
    jmp 1
4:
    ret
=========== OPT LEVEL: mininal ===========
main:
    xor esi, esi
    xor eax, eax
1:
    cmp rsi, 3
    jz 4
    mov rdx, rax
    xor ecx, ecx
2:
    cmp rcx, 3
    jz 3
    mov rax, rsi
    imul rax, rcx
    add rdx, rax
    add rcx, 1
    jmp 2
3:
    add rsi, 1
    mov rax, rdx
    jmp 1
4:
    ret
=========== OPT LEVEL: moderate ===========
main:
    xor esi, esi
    xor eax, eax
1:
    cmp rsi, 3
    jz 4
    mov rdx, rax
    xor ecx, ecx
2:
    cmp rcx, 3
    jz 3
    mov rax, rsi
    imul rax, rcx
    add rdx, rax
    add rcx, 1
    jmp 2
3:
    add rsi, 1
    mov rax, rdx
    jmp 1
4:
    ret
=========== OPT LEVEL: all ===========
main:
    xor edi, edi
    xor esi, esi
1:
    xor edx, edx
2:
    mov rax, rsi
    mov rcx, rdi
    imul rcx, rdx
    mov rsi, rax
    add rsi, rcx
    mov rcx, rdx
    add rcx, 1
    cmp rdx, 3
    mov rdx, rcx
    jnz 2
    add rdi, 1
    cmp rdi, 3
    mov rsi, rax
    jnz 1
    ret
=========== OPT LEVEL: aggresive ===========
